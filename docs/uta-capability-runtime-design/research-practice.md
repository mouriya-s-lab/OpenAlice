# UTA 设计研究：裁决、产物与有效实践

本记录的权威来自[方法论与边界完整原文](../uta-capability-runtime-design.md#design-methodology)。它记录实际调查怎样改变了提问、哪些论断被采纳或拒绝，以及下一轮为何值得做；不是替换原文的新方法论，也不是最终架构。

状态：本文保留此前后继、投影和跨业务研究的实际裁决与归档范围；当前完整设计推进及形式论证见[本轮研究记录](current-research.md)。这些局部结论不自行构成整项收敛。工作保留在 `docs/uta-effect-runtime-architecture` 分支；[唯一活动计划](../../plans/uta-core-design-research.md)随实际表达缺口调整，不以维护者审阅 hold、提交或阶段验证作为停手条件。

## 现存设计的起点

主书已经有能力关联、schema 构造、计算组合、交付、纯决定、受控写入和延后激活。§13 的 rich Candle、动态外国 Provider、Candle 激活 Order 并返回 Agent 是新目标输入，不是从旧系统能否实现来决定其存在。初版的 Decision/Evolution 与效果解释关系也仍有研究价值。

需要继续论证的是这些关系如何连接、何时成立，而不是继续补对象目录。例如主书 §5.4 提出值依赖组合与 E/R/P 保持，§16.5 提出透明事件投影，旧能力调查提出 GrossNotional 和 cooldown 的批准消费边界。这些都是可检验的候选关系或政策，不因正文写了“选择”“实施规格”“accepted”就成为推导前提。

## 首轮实际改变了什么

| 轮次 | 实际做法 | Main 裁决与后果 |
|---|---|---|
| 小样本 | `bounded` 与 `specified` 对同一材料独立研究“前结果怎样决定后继” | 不放行。出现把 Candle 输入安到旧 guard、把无一致性证明说成伪造快照、把动态分支覆盖收紧为有限枚举等外推 |
| 校准 | 第二档追真实 caller/guard 状态；第三档比较明确前提下的候选解释；Main 回到源码并运行局部实验 | 分开当前事实、思想实验与候选政策。保留短路构造规律，定位 eager 状态写入；不采纳“要求上界就是授权” |
| 再对照 | 第三档比较描述范围与授权时机；第二档挑战共享解释的前提 | 只采纳有依据的部分。得到有 B 无 C、实际选择 B 的区分场景；拒绝把不同维度绑成互斥架构 |
| 有限扩展 | 两项第二档调查构造描述和风险语义；透明投影用第二档查源码、第三档做有限数值推演 | Main 独立核对并运行两个针对原样例的反例。没有按整份报告盖章，没有转成完整设计批量填表 |

这是本轮执行记录，不是以后必须复制的四轮模板。相同材料的首轮是跨模型观点对照；后续改变了材料和任务形状，是同题异法或互补调查，不冒称控制变量充分的 A/B 性能实验，也不据此排名模型。

原始任务输入、完整模型输出、拒收记录、实际脚本与输出保存在[研究证据归档](research-evidence.tar.gz)。失败报告没有改写成看似最初就通过的结论。

## 目前获得的设计判断

### 后继的描述范围与执行许可不能混为一谈

主书 §5.4 的 `A -> Query` 关系已细化为后继构造、声明余域、解释入口与消费者观察的关联。构造时可知参数化契约，实际结果选择具体后继；构造和执行分开，公开描述不靠反射任意闭包取得。

有限实例选择、参数化声明族和运行时认证关联都有合法使用范围，不需要选一种覆盖所有程序。有限业务分支不是核心特判；family 的权限上界只是要求描述，不是已经授予的许可。静态 TypeScript 不会因发现远端 schema 而长出新类型，但动态文档可以保留精确 binding/slot 后被明确消费。

一个有效思想实验只需要两个候选后继 B/C：主体有 B 权限、没有 C 权限，前段 Q 的实际结果选择 B。若业务承诺“Q 开始前全部后继均已获准”，它会拒绝 Q；若承诺“选中后继在执行前获准”，Q 与 B 可以运行。两者都能避免未授权执行，仍有不同结果。是否选择其中一种，不能从 FP 或类型并集自动推导；也不需要假定 Q 无成本、可重试或必然写 checkpoint。

**实际反例。** 第15章归档的 `specimen/algebra.ts` 中，`combineReads` 派生 product、失败联合、资源集合和 child binding，但 `Meaning.permission` 是手传的单字符串。`specimen/catalog.ts` 只检查外层这一项。Main 用原代码创建左右两项读取、组合只声明左权限：主体直接解析右侧入口被拒绝，解析组合却成功，左右本地处理器各执行一次。

因此，现有样例没有机械保证书面的 `P1 ∪ P2`；不能用它为这条保证背书。这不是生产漏洞结论，不是静态或动态组合不可能，也没有在本轮用补一个权限判断来替代整体关系研究。

后继调查进一步判定：普通内部 HOF 不必全部成为目录叶子，可公开组合则须从同一声明关联派生类型、校验和描述，不能在出口另抄 schema。消费者只有在后继 IO 前必须了解分支输入、费用或权限并参与构造时，才需要公开 staged selection；只需读后精确结果时，final-composite 关联结果也合法。静态 R 不因最后只选一支而消失；惰性获取只决定哪些原生连接真正打开。动态结果进入已知单元需要合法总投影或带失败通道的转换，不能把字段同形当语义相同。

### 旧 guard 带来的约束在状态消费，而非否定高阶组合

当前 `createGuardPipeline` 顺序短路，同一 operation 的检查共享 context；不同 operation 会重新调用账户/仓位读取。重新调用不保证 Provider 返回不同对象或新的原子快照。`OperationGuard` 没有纯度契约，`GuardContext` 的浅层 readonly 也不是运行时冻结。

实际调用边界见 `services/uta/src/domain/trading/UnifiedTradingAccount.ts:180-205`，顺序消费与结果接纳见 `services/uta/src/domain/trading/git/TradingGit.ts:119-185,929-967`；读接口见 `packages/uta-protocol/src/types/broker.ts:553-566`。这些源码不含 Candle crossing，也未承诺组合级原子拒绝。

**实际实验。** 对真实 pipeline 注入本地读取和计数 dispatcher：两个不改变业务状态的检查共享同一 context，可以继续；测试仅额外记录 context 以核对共享，不能据此宣称现有 guard API 强制纯度。把 `CooldownGuard` 放在一个必拒检查前，第一次被后置检查拒绝，第二次被 cooldown 拒绝，dispatcher 总调用为零。`guards/cooldown.ts:15-31` 在返回允许前已经修改自身 Map。

主书 §9.1 已把提议、后续可见状态与真正提交分开：同一个纯 `evolve` 可在候选事件上计算推测状态，下一决定消费该状态；writer 原子提交后才发布持久权威。这保留高阶短路与渐进判断，不为每个 guard 新造状态机，也不让一次读取或一个 receipt 自动取得相同占用与释放语义。

GrossNotional、减仓适用性和 cooldown 从批准、派发还是原生接受开始计时，是业务政策而非 FP 定理。尤其不能照抄本轮风险报告的“已证实减仓卖单必被候选 GrossNotional 拒绝”：`docs/uta-effect-runtime-design/solutions/Guards/entries.md:326,341,350,579` 还允许声明并证明 non-increasing 的叶子为 `NotApplicable`。必须先确定这项适用关系，再计算结果。可表达某个政策，不证明它就是正确的政策。

### 结构投影与行为透明之间需要消费函数的关系

第三档给出的有限轨迹有区分力：阈值为100，先有 `(close=99,vwap=99)`，再有 `(close=99,vwap=101)`。基础消费者看 close，富值消费者改看 vwap，候选是否产生及状态就不同；即使丢弃 vwap 的基础投影总定义，基础字段也完全相同。

这个反例只说明改变谓词以后不能继续声称原来的行为透明。它不要求新增核心 Candle 类型，也不要求核心内置一个阈值 DSL。业务函数仍可以特化；透明性需要另行说明转移与投影如何相容。声明“读了哪些字段”也不是充分证明：同样只读 close、使用不同阈值的两个函数仍可产生不同轨迹。原报告对释放责任、coverage 也发生变化的断言没有相应前提，未采纳。

第二档在真实事件样例中发现了更隐蔽的消费：`FeedControlMachine.accept` 对完整事件做 `JSON.stringify` 以判断重复内容，实际读到了扩展字段。Main 执行两条独立路径，保持 eventId、sequence、基础 payload 不变，只把 vwap 从101.25改为102.25：

- 富事件先接纳：`rejected / duplicate-id-conflict`。
- 先投影再由基础机接纳：`accepted / duplicate`。

**结论边界很重要。** 这是 schema 合法但与已接纳内容冲突的输入。主书 §16.5 又将规律限定在可接纳事件上，因此本实验不推翻已接纳合法轨迹上的条件律。原 CLI 先经 rich machine 再投影，也不能据此声称它漏掉冲突。被检验的是“可任意交换接纳与投影”这个更强命题：只比较合法输出的 tag/payload，不足以证明拒绝、去重和状态消费也相容。

[主书 §16.5](../uta-capability-runtime-design.md#projection-laws)现在独立定义富入口的已接纳轨迹、预先约定的基础观察、状态/身份关系和允许的重复零步。验证入口替换另需在含状态冲突的候选上证明接纳相容；业务谓词透明又是不同条件。Main 启动真实归档 CLI 输入 Started、两条不同 vwap 的 Data、其中一条精确重复及 Completed：rich/base 各留下四条完整对应记录；rich 的 vwap 判断为 `[false,true]`，base 的 close 判断为 `[false,false]`。这既验证该 CLI 的重复消去，也展示合法业务特化不等于透明替换。

## 扩大调查后形成的局部设计结论

后继和投影的局部结论通过检查后，研究继续扩展到纯决定与持久控制，再用 NewsGroup、异构动态 Provider 和两成员受控效果挑战同一组关系，没有改成按 Provider 名称分发表格。

- **NewsGroup。** 两个查询组合的组与 Provider 自有组共享精确结果供业务函数消费的关系，不自动共享成员可读性、修订或完整性。Partial 保留缺口，但不一律阻止判断：已有匹配可证明存在，已有反例可否定全称；只有依赖未观察成员的结论才不确定。实际 RSS/store 读取与真实 JSONL 实验核对了同 GUID 改文被丢弃、晚到文章进入同一发布时间窗口、重建后记录保持，不能把这些旧语义冒称可靠 correction 或全源 complete。
- **异构 Provider。** 原样例保留运行时 schema、binding/slot 与明确的进程生命周期。`CertifiedDocument.project` 仅证明绑定检查后的结构解析，不证明语义投影；Catalog 取得的 Entry 保留描述上下文，返回 wire JSON 不自动证明关联已经丢失。样例没有展示丢弃该上下文的实际误用，也没有实现完整调用时授权复核、命名 evaluator 或目标 Effect 资源模型。缺少这些证据是样例边界，不是动态抽象不可能。
- **受控组合。** Query 的值可以成为 Controlled 接纳命令的构造输入；混合解释不能冒充只读 Query。A receipt 后接纳 B 只保证接纳因果，不能证明 A 已成交。A fill 先于 ack，B 拒绝或 Unknown 时，各成员的事实、判据与恢复责任继续存在；补偿是另一个受控关联，不是通用 undo。现有目标不要求再增加公共 `sequenceAdmission` 算子或任意持久工作流语言。

本次产物进入主书 §§2、3.2、5.4、5.5、6.4、9.1、10.4、16.5，以及事件流册 EF02。主书说明为什么保持关联、业务函数如何进入、计算在何处解释、结果如何消费；不是仅在研究记录里承诺以后再写。

## 当前论证范围与未完成问题

后继研究说明了静态/动态结果、推测/已提交状态、receipt/原生事实以及合法轨迹/入口替换之间的若干关系；旧 guard、来源选择与 RSS 的具体差异有保留或修正解释。这些结论继续有效。最后一轮对抗综合只检查四条指定连接，并以阻断性矛盾为主要输出；未发现矛盾不等于已经正面论证整个设计。Main 将该局部结果提升为整项收敛的判断不成立，归档保留原任务和裁决以供追溯。

共同声明的调查已产生下面的构造关系，并进入主书；它不是靠重复“必须保持一致”得到的结论。接下来的检查沿真实消费关系继续到数据激活、Agent 决策与受控恢复的连接，不预排所有模块。具体语法、存储和进程实现仍可后定；Provider 的原生完整查询、幂等、finality、watermark、replay、组原子性及补偿等级仍须按环境取证。这些原生义务不能掩盖尚未说明的主机构造关系。

## 共同声明、值消费与结案条件的构造研究

首先缩小到固定后继的三个操作数 D0、D1、M。第[5.4.1节](../uta-capability-runtime-design.md#declaration-construction)给出实际派生关系：输入、结果、阶段失败、描述及解释闭包消费同一组定义，而不是一份元数据另配自由 k。第三档在明确前提下推演这项组合，也给出了没有前提的 `Pm`；Main 保留构造推导，拒绝替纯 mapper 发明权限要求。基础 Constraint 则沿原投影调用同一个 evaluator，不把字段展开当约束保持证明。

**原样例的实际区分实验。** Main 对未修改的 `adaptInput` 与 Candle interpreter 提供两个同型 mapper：一个保留请求数量3，一个替换为1；它们使用相同 schema 与版本字符串。实际调用分别返回 close `[101,102,103]` 与 `[101]`，公开 descriptor 却完全相同。驱动的严格类型检查也通过。这证明该窄样例的描述没有绑定 mapper 实现身份，不证明声明组合不可能，更不是生产漏洞。主书因此区分结构派生、实现身份与业务正确性，没有用一个哈希同时冒充三项保证。

动态输入随后成为真正的新问题：认证输出不能生成所选 schema 新要求的 session。第二档调查原外国进程样例，第三档分别推演“已有真实政策”“允许构造失败”“调用者参与选择”的有限情形。Main 接受这三种契约的不同消费需要，拒绝无 adapter 却要求失败带 adapterVersion、强制每次成员变化都改外层 family，以及从运行时依赖字符串生成静态 R 的推断。第5.4.2节正面说明选中定义、输入构造、调用与认证的关联。

扩大前再用两个消费者检查做法，而不是按旧能力分组填表。首次账户研究读错主稿，Close 研究又想换回更容易的 Place 样例；这些输出不放行。后续给出精确当前稿路径、已经核实的旧行为以及需要解释的函数关系，第二档才形成可用的账户/FX 与 Close 推导。Main 仍修正了其中把本地 capture 时间当原生 as-of、把 generation 当修订顺序、以及读取 FX 后丢失账户值 A 的错误。

因此主书13.5的估值计算明确调用 `F(I,A,B)`，输入积引用原账户与 FX 槽位；缺少 FX 时不返回伪总值，也不由新字段补出缺失的来源时间或 coverage。13.4和第[10.1.1节](../uta-capability-runtime-design.md#controlled-construction)则由同一私有 Recipe 构造有限提交、持久编码、状态/失败消费者，并在重启后重新绑定原槽位与业务函数；receipt-only 实验不再被当作完整 Controlled 的证明。

另一项第三档思考只给定“初始10、目标减4、后来观察6、存在另一交易者”及两个候选判据，不让它猜 Broker。它构造出相同观察、不同本次执行结果的两条历史。第二档随后把这个区分接到历史解释与恢复：状态目标满足可以和本次尝试 Unknown 并存。Main 据此修改主书与 EF07/08/09 的结案条件，分别消费 criterion、attempt 解析和持久责任，而不是把一个业务成功谓词当作控制终态。

这里采纳的是有限反例和构造关系，不是报告附带的所有政策。未采纳“某个通用 guard 即可安全重试”“digest 是唯一必要原生身份”“所有 Close 都必须具备条件式原生减仓”等外推；也不因一段旧源码未展示子账户保存，就声称已明确绑定 scope 的目标设计缺失该关系。原始输出、后续修正与 Main 裁决分别保留。

## 可以固化的实践，以及不能固化的东西

### 从问题选择工作形状，而不是从档次分配固定工种

本轮第二档的真实 caller 调查和隐式去重依赖调查有价值；第三档的权限时机与 close/vwap 思想实验也产生了可用区分。两档同时出现过无证据外推。经验不是“哪档永远可信”，而是：前提已明确时可把逻辑比较交给第三档；普通源码未知交给第二档；需要开放探索或陌生外部系统时升级任务能力，不能让任何档次猜。

同题独立看法适合暴露阅读偏差；源码与逻辑互补适合分离“发生了什么”与“由此能推出什么”。实际结果决定是否换视角、补一个来源、做最小实验或扩大，不能把同一 prompt 发更多次当纠偏。

### 接受的是有依据的论断，不是模型的一整份报告

保存原始输出，再明确采纳、限缩或拒绝的部分。新要求不能被写成旧代码已经违反的事实；候选要求、当前实现、条件推论和原生证据也不能排成一个万能可信度顺序——它们回答不同问题。

本轮拒收过：从 `Promise.all` 推出原子快照承诺、从 branch coverage 推出必须枚举、从要求上界推出授权、从同字段推出同转移、漏掉 `NotApplicable` 后下数值结论。裁决须落到触发这些错误的前提与来源，不能用“存在局限”把它们掩过去。

后继扩大还暴露了一次同名稿件路径误读：研究者把初版 architecture 的行号当作 capability 主书。Main 回到实际文件，拒收初稿的旧术语及“evolve 只能消费已接纳事件”等外推，要求按精确文件重读。还有三项被限缩：Partial 不等于任意谓词都不可决定；wire 结果不自带证书不等于调用上下文必然丢失；没有 Effect R 模型不等于证明 R 被错误消解。这些记录说明接受必须落在论断和前提上，不能由一份格式完整的报告代替。

### 实验服从问题，不让实验替设计选模型

本轮运行既有代码和归档解释器，是为了区分 eager 状态、权限传播和投影顺序；没有为方便执行而先造一套新 UTA 模型。通过或失败都只支持实际观察。一个良性样例不足以证明普遍规律；一个冲突输入也不能越过原律的定义域宣判它失败。

### 扩大的是经过检查的问题，不是尚未论证的答案

下一批给出为什么值得调查、所需材料和不可越过的证据边界即可，不预分所有字段、类型和消息。若新结果又在复述候选规则，或反例依赖未给定的前提，应退回小样本；若只是原生事实未验证，则将那项义务定位到 Provider 边界，不停止已有契约的内部推演。

这里没有普遍有效的固定 prompt、评分阈值、每批人数或完整 todolist。固化的是保留原文、动态调整分工、核对论断、允许不放行，以及把每次改变的理由留给下一位研究者。

检查点由 Main 在研究过程中执行：有依据就接续下一项调查，前提错误就调整，不能把阶段报告、证据归档或提交推送当作任务完成。分支 hold 限制的是合并时机，不要求研究暂停；只有目标产物收敛、用户明确暂停或经调查确认的外部阻塞，才构成结束或暂停条件。

## 验证与复现

### 启动与校准证据

启动与校准实际执行了 subagent 调查及 Main 裁决，并运行三组隔离研究程序。它们都使用本地输入，不读取交易凭据，不连接 Broker，不修改生产代码或用户状态。Node 为 v26.8.1；归档样例依赖 Effect 3.22.1、Zod 4.3.6、tsx 4.21.0。保留了工具链 DEP0205 警告，没有抑制它。

发布检查从研究归档重新解压到新目录，完整执行了三组程序，得到相同的状态残留、权限组合和投影分类结果；对已有执行目录返回 exit 2 并拒绝覆盖。另让一个新 `bounded` 研究者从落盘记录接手，它选出 Instrument 到 Candle 有限读取的一条局部调查，保留源码事实与目标承诺的区别，没有扩成全量 Provider 分工；其报告与 Main 的限缩裁决也在归档中。Markdown 实际渲染通过，14 个本地链接及方法论锚点可解析。这些是本轮可复现与可接手的证据，不是对未来不偏离的保证。

在当前研究分支 checkout 根目录，选择新的本地证据目录和新的执行目录：

```sh
mkdir -p "$EVIDENCE_DIR"
tar -xzf docs/uta-capability-runtime-design/research-evidence.tar.gz -C "$EVIDENCE_DIR"
sh "$EVIDENCE_DIR/uta-research-evidence/replay.sh" "$PWD" "$NEW_RUN_DIR"
```

`NEW_RUN_DIR` 必须尚不存在，脚本拒绝覆盖。它解压两个原实验归档，仅安装第一份锁定依赖，第二份复用相同版本的运行依赖；再执行当前 checkout 的 guard 实验和两个原样例反例。三个 `.stdout`、`.stderr` 保存实际结果，成功只表示预期研究现象重现。原始输出与任务上下文也在研究归档内，不依赖聊天或隐藏推理。

这些不是新 UTA 产品的 runtime 验收，也没有完成原生 Provider conformance、架构接受、迁移或交易能力。文档渲染与链接检查只证明记录可被读取。

### 核心关系研究证据

[后继研究归档](research-continuation-evidence.tar.gz)保存本次各档任务、原始输出、Main 逐项裁决与 steering、源码/原归档摘要、实际驱动及完整结果。原始报告中的未采纳推断仍保留；应以 `construction-gate.md`、`semantic-gate.md` 和本记录解释证据，不能把整个模型报告当成设计权威。

| 实际执行 | 观察 | 不能推出 |
|---|---|---|
| Effect 3.22.1 continuation 探针 | 构造阶段未执行；只有选中分支授权/获取/读取/释放；拒绝或缺资源短路，不静默换源 | UTA 自动派生 descriptor/P；未提供的静态 R 已消解；任意回调纯度 |
| 未改动的归档 CLI `project` | 精确重复不再次投递；rich/base 完整事件含重绑定身份相容；不同谓词产生不同决定 | 强接纳等价；所有消费者透明；原生流、资源或完整 R/P 保证 |
| 当前 `NewsCollectorStore` + 隔离 JSONL | 同 GUID 内容变化返回 null；晚到成员改变同一 publication-time 查询；本地 seq 与发布时间顺序不同；重建后结果相同 | 在线 RSS 成功；发布者修订协议；全源完整性或交易行为 |

发布核验从归档重新安装锁定依赖，三个程序全部成功；重复使用执行目录返回 exit 2，16 个已有观察文件保持不变。另完成两份 TypeScript 驱动的严格类型检查、4 份 Markdown 实际渲染、639 处本地链接及锚点/源码行号范围检查、20 张 Mermaid 图解析。方法论两段原文逐字保持，168 份原设计/映射等保留材料及三份旧实验归档摘要不变。文档检查发现的主书旧行号引用已改为稳定章节锚点；源码行号检查只确认范围有效，不替代对引用论断的研究核对。

复现使用新的执行目录，读取当前 checkout 的 News store 和两份原始实验归档；不安装到仓库、不连接服务、不运行项目测试套件：

```sh
mkdir -p "$EVIDENCE_DIR"
tar -xzf docs/uta-capability-runtime-design/research-continuation-evidence.tar.gz -C "$EVIDENCE_DIR"
sh "$EVIDENCE_DIR/uta-continuation-research/replay.sh" "$PWD" "$NEW_RUN_DIR"
```

`NEW_RUN_DIR` 必须不存在。每次运行保留三个程序的 stdout/stderr、CLI 输入与持久 journal、News JSONL 与重建观察；工具链警告保留。归档中的 `local://uta-continuation-research/` 引用对应归档同名根目录，运行依赖与解压的旧样例由 replay 创建，不随新归档重复打包。
