{
  "group": "HttpRoutes",
  "designBasis": "capability-composition-v1",
  "analysesSha256": "04a0df9ceb90130cc14ba92ad2b39e417fba46d9632f50884153b83c6ef090b6",
  "designSha256": "481b3545b604a9499c715cea4afdbd3975aff0a703dc96f4c78649f3ce829e04",
  "contractSha256": "73f5235ae86d3f1c9038dfd214e7207cdfb1833be7bc369f628d7d3f9017dd4f",
  "entries": [
    {
      "id": "MAP-38469CADA4",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-38469CADA4. Source fact: services/uta/src/__tests__/trading-tools.spec.ts:1-15: Imports createTradingTools, UTAManager, UnifiedTradingAccount, MockBroker, IBKR Order/OpenOrder values, and contract extensions; the comments explicitly place this spec at Alice's tool boundary over co-located UTA broker/domain stubs. The source problem is 该 spec 直接把 createTradingTools 与 UTAManager、UnifiedTradingAccount、MockBroker 及 IBKR Order/OpenOrder 放在同一进程；因此工具边界可以绕过序列化协议，测试不会发现 native SDK 值或进程内对象泄漏到 Alice/HTTP。 and the current target is Tool-boundary fixture is a declared capability leaf plus its typed input/result/error schemas and a provider interpreter; the fixture exposes protocol values only, while IBKR Order/OpenOrder remain adapter-local evidence. No complete broker interface is required. Scope is explicit Public or Account(AccountScope) when the capability is account-backed; no route invents a default account or subaccount. which preserves 保留该 spec 作为 Alice tool boundary 的行为证据。; 保留 MockBroker 与工具层的端到端关联，但移除 native SDK 值穿过公共边界。. Its boundary is Test composition supplies one selected provider capability through a typed client/adapter port; HTTP/tool codecs stop at the UTA-facing boundary and never construct or pass native SDK objects. and its entry-specific falsifier is A tool request decodes to a schema-valid projection with no IBKR-only object or field; a provider-native fixture is accepted only behind its declared leaf, and an absent leaf is not represented by an empty successful result.. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-3FABE05A73",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-3FABE05A73. Source fact: services/uta/src/__tests__/trading-tools.spec.ts:17-24: Constructs one UnifiedTradingAccount for each MockBroker and registers each account in a UTAManager, giving tool tests an in-process multi-account manager. The source problem is 测试通过 `new UnifiedTradingAccount` 和 `UTAManager.add` 建立账户目录；这只证明进程内注册，不证明 AccountScope、权限目录或投影在跨进程读取时保持一致。 and the current target is Provider-instance registry fixture: each configured instance declares capability descriptors, labels, and a provider interpreter handle; an account-backed instance additionally declares an explicit AccountScope, while a public instance declares Public and no account/subaccount. Lookup is against the declared registry, not a universal UTAManager/SDK surface. which preserves 保留多账户查询可同时建立两个独立账户。; 保留标签仅为展示，不用 label 路由交易。. Its boundary is Composition builds the registry and injects it into account/capability query ports; tests do not construct a concrete UnifiedTradingAccount as the transport dependency. and its entry-specific falsifier is Two instances with the same query leaf return two scoped summaries; duplicate stable capability identity fails at load time without replacing the first instance or writing an event.. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-4AFBEA2B00",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-4AFBEA2B00. Source fact: services/uta/src/__tests__/trading-tools.spec.ts:27-36: Casts an in-process UTAManager to UTAManagerSDK because the tool accepts an HTTP-adapter-shaped interface; the comments document that getFxRates is absent and silently falls through to empty rates. The source problem is 测试把 UTAManager 强制 cast 成 UTAManagerSDK，而且缺少的 getFxRates 被工具层 catch 成空 rates；因此服务能力缺失会被伪装成成功的空查询。 and the current target is Typed client for discovered capability leaves: list/query methods and an optional FX leaf are generated or decoded from each declared schema; absence of the FX leaf is an explicit availability result, never a cast to a complete SDK. which preserves 保留工具可以在不创建 broker stub 的情况下查询账户。; 保留缺失 FX 能力不会影响不需要 FX 的订单查询。. Its boundary is The HTTP client owns credential, correlation, schema negotiation, and response decoding; application ports expose only the selected query capability. A missing provider leaf remains unavailable rather than becoming empty FX data. and its entry-specific falsifier is A client whose provider lacks the FX leaf returns CapabilityNotDeclared/Unavailable while an unrelated account query still works; malformed FX output is rejected before reaching the tool.. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-468691D82C",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-468691D82C. Source fact: services/uta/src/__tests__/trading-tools.spec.ts:40-65: Checks account-source resolution semantics: no source returns every registered UTA, an exact source returns one matching account, and an unknown source returns an empty array. The source problem is 测试把 unknown source 断言为空数组，同时把 all/exact 解析留给进程内 manager；这会把 UnknownAccount 与合法 Complete(empty) 混淆，也未验证 principal 的 scope 过滤。 and the current target is Account selection is a small query union, AllAuthorized | ExactAccount(AccountId), evaluated against the capability/account registry and principal scope; it does not imply that every provider has the same account commands. which preserves 保留 source 缺省返回全部注册账户。; 保留 exact id 返回单一账户，未知 source 不触发 broker 调用。. Its boundary is The application query port performs selection and scope filtering; HTTP decodes the query and encodes the typed listing, without calling a manager or broker directly. and its entry-specific falsifier is Omitted source returns all authorized visible rows with explicit coverage; exact source returns one row; an unknown exact source is a typed failure and never an empty success or broker call.. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-A88187B75A",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-A88187B75A. Source fact: services/uta/src/__tests__/trading-tools.spec.ts:69-87: Returns the sole UTA for an exact source and throws a string-bearing error when no UTA matches the requested source. The source problem is resolveOne 在未命中时抛出依赖错误文本的异常；调用方无法区分 unknown 与 ambiguous，也无法在协议层稳定处理 404/409。 and the current target is Resolve-one is a query-level cardinality result, Resolved(AccountScope) | UnknownAccount | AmbiguousAccount(matches), produced from the visible registry and the selected provider leaf; error text is not protocol. which preserves 保留成功时返回唯一匹配账户。; 把旧 Error message 改为可稳定解析的 code 与 matches。. Its boundary is Account-query application performs the lookup; transport maps the result to a stable response status without touching a broker or relying on thrown strings. and its entry-specific falsifier is Unknown source returns UnknownAccount with no provider invocation; duplicate visible aliases return AmbiguousAccount and require an explicit identity.. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-8588C24A12",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-8588C24A12. Source fact: services/uta/src/__tests__/trading-tools.spec.ts:91-99: Calls the tool layer with no arguments and verifies that it returns an array of summaries containing the registered account identifier. The source problem is listUTAs 只断言返回数组和 id；它未约束 health、capability、scope、freshness 或凭据脱敏，工具可以继续依赖过于乐观的摘要。 and the current target is AccountListResponse is the projection of a declared account/capability-list leaf: each row has branded AccountId/AccountScope, label, reach/health, capability descriptors or availability, and freshness; credentials and native handles are never fields. which preserves 保留无参数 listUTAs 返回所有已注册账户摘要与 id。; 不把密钥、native adapter 或 raw broker health object 发送给工具。. Its boundary is Application query reads the account/capability projection and transport encodes the provider-neutral outer descriptor plus declared leaf metadata; no concrete broker summary crosses HTTP. and its entry-specific falsifier is The response contains the registered account id and label plus freshness/capability evidence, but no credential or SDK object; a provider leaf that is disconnected is listed as unavailable rather than omitted or fabricated.. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-0B6606BE1C",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-0B6606BE1C. Source fact: services/uta/src/__tests__/trading-tools.spec.ts:103-119: Spies on two separate MockBroker instances and verifies that searchContracts aggregates one matching contract description from each registered UTA. The source problem is 搜索测试仅用两个 broker spy 聚合 ContractDescription；它没有表达 allSettled 后的 Partial coverage，也没有证明 native contract 已被转换为 canonical InstrumentId。 and the current target is Contract search is a provider-declared data capability leaf. Its query schema declares pattern, hint, and source selection; each provider may add exact search fields, and results use normalized InstrumentId plus a provider extension where declared. Scope is explicit Public or Account(AccountScope) when the capability is account-backed; no route invents a default account or subaccount. which preserves 保留两个 MockBroker 的同 pattern 结果聚合为两条。; 保留单 broker 失败不使全量搜索失败。. Its boundary is Application invokes the selected provider search interpreters concurrently; native ContractDescription values are decoded at the adapter boundary before the HTTP response. and its entry-specific falsifier is Two provider instances return two scoped AAPL hits; one rejected instance yields Partial with its identity and the healthy hit; a provider without search has no search leaf rather than an Unsupported-filled handler.. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-1E2638E1F3",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-1E2638E1F3. Source fact: services/uta/src/__tests__/trading-tools.spec.ts:123-135: Builds an OpenOrder fixture from native IBKR Order, OrderState, and contract values, including Decimal quantities/prices and an Alice identifier. The source problem is makeOpenOrder 直接构造 IBKR Order、OrderState 和 Contract；测试因此把 adapter 输入当成公共 OrderSnapshot，未验证 sentinel、scope 和稳定 order identity 的解码边界。 and the current target is The fixture represents a provider-declared order-observation leaf: normalized BrokerOrderId, AccountScope, InstrumentId, provider-declared order fields, and observation status/version. Native Order/OrderState/Contract stay inside the adapter fixture and are not a universal Order schema. which preserves 保留 fixture 能表达 Decimal 数量/价格与 Alice identity。; 保留 adapter-specific construction 作为 broker conformance fixture，但不扩大公共 schema。. Its boundary is A provider interpreter or conformance fixture supplies native observations; the HTTP/tool boundary consumes the selected leaf codec and never imports Order/OrderState. and its entry-specific falsifier is A mock-paper|AAPL native fixture decodes to a scoped protocol observation with exact Decimal fields and no raw SDK object; a missing identity or unknown provider field stops the projection with a typed failure.. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-EBC1DF5535",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-EBC1DF5535. Source fact: services/uta/src/__tests__/trading-tools.spec.ts:137-167: Executes a staged and pushed market order, then checks that getOrders returns compact fields such as source, action, orderType, quantity, and status while excluding raw IBKR fields. The source problem is 测试先走 stage/commit/push 再查询 compact order；HTTP/tool 结果因此仍可把 Git push 成功当作订单事实，并未约束 projection stale 或 broker observation 延迟。 and the current target is Get-orders is a finite pull over a provider-declared order-observation leaf. OrderSummary contains only declared common facts plus exact provider extensions, with status/identity/freshness and structural absence explicit; it is not a complete broker action model. which preserves 保留 compact 输出核心 fields 并排除 softDollarTier/transmit/blockOrder/sweepToFill。; 保留已提交市场单后查询仍能看到规范状态。. Its boundary is Account-query invokes an order-observation port or reads its projection; adapters decode native fields, while tool/HTTP only encodes the selected output schema. and its entry-specific falsifier is A finite response retains source/action/type/quantity/status when declared, excludes IBKR presentation fields, and marks delayed or partial observations explicitly; no HTTP 200 is interpreted as an Ack or fill.. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-0682C42EA8",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-0682C42EA8. Source fact: services/uta/src/__tests__/trading-tools.spec.ts:169-193: Mocks a raw OpenOrder with IBKR UNSET numeric values and zero parentId, then verifies that unset optional fields are omitted from the returned order summary. The source problem is 测试在工具摘要层过滤 IBKR UNSET_DOUBLE、UNSET_DECIMAL 与 parentId=0；若 decoder 不在 adapter 统一处理，重启或其它 broker 仍可能把 sentinel 持久化成业务值。 and the current target is Optional native fields are an adapter-local, versioned presence profile for the provider leaf: Present(value) or structural Absent only when that adapter/profile/field proves the sentinel. Unknown profile/value remains unavailable and never becomes a guessed business field. which preserves 保留未设置 lmt/aux/trail/parent/tpsl 不出现在 summary。; 保留 status/order identity 等非 optional 字段。. Its boundary is Each selected broker adapter owns native sentinel decoding; projection and HTTP codecs consume the resulting declared optional field and perform no IBKR sentinel tests. and its entry-specific falsifier is The confirmed Mock/IBKR UNSET price and parentId=0 fixture omits those fields; a real zero or another adapter/profile without presence evidence yields typed unavailable/violation instead of being conflated with Absent.. Empirical obligation retained: 仍需在各 broker adapter 核实 native sentinel 的完整集合与版本；Mock/IBKR 当前证据只覆盖此测试使用的几种。。 Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-0CD7FA4541",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-0CD7FA4541. Source fact: services/uta/src/__tests__/trading-tools.spec.ts:195-211: Mocks a limit order with a lmtPrice and nested take-profit/stop-loss values and verifies that explicitly present optional fields survive summarization. The source problem is 测试只确认 lmtPrice 和 tpsl 在摘要中出现；它没有把这些字段绑定到 OrderType/ProtectionSpec 的合法关系，也无法区分 capability unavailable 与静默丢失。 and the current target is Limit and protection fields are provider-declared order-observation extensions or, when an effect leaf declares them, HOF-composed order terms with explicit preconditions. A response includes Present terms only when declared; an unsupported attached-protection parameter variant is omitted from that leaf's exact schema and does not remove other declared variants. A declared-but-disconnected leaf retains its identity and reports availability separately; no global optional bag or universal protection set is assumed. which preserves 保留显式 lmtPrice 与 nested takeProfit/stopLoss 在 summary 中可见。; 保留精确价格字符串。. Its boundary is The selected adapter decodes lmt/take-profit/stop-loss values; application/transport validates the selected leaf schema. An undeclared protection parameter variant is absent from that schema and rejected as unknown input, whereas a declared-but-disconnected leaf retains its identity and reports named availability. and its entry-specific falsifier is An explicitly present limit and nested protection values survive the declared observation/plan schema with exact decimals; when a provider declares the Order leaf but not an attached-protection parameter variant, that variant is absent from the exact schema and an attempted field is rejected while other declared variants remain available; a disconnected declared leaf reports named availability.. Repair applied: an unsupported attached-protection parameter is absent from the selected leaf schema, while a declared-but-disconnected leaf retains identity with separate availability. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-07DB18F4C8",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-07DB18F4C8. Source fact: services/uta/src/__tests__/trading-tools.spec.ts:213-228: Sets a small Decimal limit price and verifies that the tool emits the exact decimal string instead of a JavaScript number. The source problem is 测试证明小数价格在当前工具输出仍是字符串，却没有证明 Decimal 文本穿过 draft、journal、projection 和 native compile 后仍精确。 and the current target is Decimal fields are refined value objects in each declared schema (price, quantity, money, or provider extension), encoded as canonical decimal text; wire numbers are not substituted. The invariant applies to data reads as well as controlled effects. which preserves 保留 `0.00001234` 原样输出为字符串。; 保留 Decimal arithmetic 在 adapter/domain 内完成。. Its boundary is Protocol codecs and projection serializers own decimal encoding; provider adapters perform native arithmetic/decoding, and HTTP does not use JavaScript Number to normalize a result. and its entry-specific falsifier is A small limit price remains the same JSON string through the declared query/projection path; for an effect leaf it remains unchanged through draft/prepare/journal/native compile, while a malformed or unitless number is rejected before the handler.. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-0C2AAEE0A8",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-0C2AAEE0A8. Source fact: services/uta/src/__tests__/trading-tools.spec.ts:230-242: Provides a string orderId from getPendingOrderIds while the native Order carries a numeric sentinel, and verifies that the returned summary preserves the explicit string identity. The source problem is 测试以 getPendingOrderIds 的字符串覆盖 native Order 的数值 sentinel；若该 join 规则不进入持久 projection，长 ID 或重启后仍可能被 numeric orderId 覆盖。 and the current target is BrokerOrderId is the provider-declared identity used by the order-observation leaf. A separate native numeric field may remain adapter evidence; joining pending-index and observation identities must not let a sentinel or conversion overwrite the declared id. which preserves 保留 `uuid-abc-123` 从 getPendingOrderIds 输出。; 避免 19 位 CCXT id 被 native numeric orderId 截断。. Its boundary is The provider adapter decodes native identity and the account-query projector joins it with pending observations; transport consumes the resulting identity schema and does not cast numeric SDK fields. and its entry-specific falsifier is uuid-abc-123 remains the returned id when native orderId is zero; a 19+ digit id survives serialization/reload unchanged and an inconsistent join is marked for reconciliation rather than silently replaced.. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-CA93BD9449",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-CA93BD9449. Source fact: services/uta/src/__tests__/trading-tools.spec.ts:244-270: Mocks three pending orders across AAPL and ETH and verifies that groupBy contract returns an object keyed by each Alice contract identifier with the expected order clusters. The source problem is 测试以 Alice 字符串作为 object key 聚类 AAPL/ETH；display alias 可能把同符号不同 venue 或不同 scope 合并，也没有为无法解析 identity 保留失败状态。 and the current target is Grouped orders are a derived finite view over declared InstrumentId and AccountScope values: an array of groups with exact provider extensions, not an untyped object keyed by display aliases. which preserves 保留 AAPL 两单、ETH 一单分别聚类。; 保留结果可按 Alice contract identity 显示，但内部 identity 为 InstrumentId。. Its boundary is The projection reader supplies normalized observations; the route/tool codec encodes the grouped data response and never constructs or mutates a broker order. and its entry-specific falsifier is Two AAPL rows in one scope group together; same symbol on different venues or scopes remains separate, and an unresolved identity is surfaced rather than put under a guessed key.. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-DF17C576FF",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-DF17C576FF. Source fact: services/uta/src/__tests__/trading-tools.spec.ts:274-289: Resolves mock-paper|AAPL through the UTA, spies on broker.getQuote, verifies that the broker receives a native contract with populated symbol/localSymbol and the original Alice identifier, and checks the quote source. The source problem is 测试通过 getQuote spy 检查 native Contract 的 symbol/localSymbol/aliceId；这验证了 adapter 事实，但当前 tool 边界仍直接暴露 native construction，未验证授权后返回的规范 Quote。 and the current target is Quote is a provider-declared pull capability with input QuoteRequest (scope: Public | Account(AccountScope), InstrumentRef, and declared provider extension) and output QuoteObservation (prices, currency/unit, observedAt, source, and exact extension); native Contract construction is adapter-local. Scope is explicit Public or Account(AccountScope) when the capability is account-backed; no route invents a default account or subaccount. which preserves 保留 aliceId 解析后 broker 收到 symbol/localSymbol 与原 Alice identity。; 保留返回 source/account ownership。. Its boundary is Application selects the authorized provider quote leaf; its adapter builds the native request and decodes the response. Tool/HTTP receives only the declared wire schema. and its entry-specific falsifier is An authorized mock-paper|AAPL request yields a typed quote with exact observation values and source; a cross-scope ref is rejected before provider invocation and native response fields do not appear unvalidated on the wire.. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-560B7EEA32",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-560B7EEA32. Source fact: services/uta/src/__tests__/trading-tools.spec.ts:291-296: Invokes getQuote with an identifier lacking the expected separator and verifies that the result contains an Invalid aliceId error. The source problem is 测试只检查 malformed aliceId 的错误文本；分隔符解析和 ownership 失败若未成为 typed codec，调用方仍可能在 broker 调用后才发现身份无效。 and the current target is The legacy AccountId|nativeKey value is a boundary codec for a provider-declared instrument reference. It parses into refined account/scope and provider key values; it is not a universal instrument identity and cannot be passed through on failure. which preserves 保留无分隔符输入返回 Invalid/Malformed aliceId。; 保留 malformed 请求在 broker 调用前失败。. Its boundary is Transport/tool decoding performs syntax checks; application authorization runs before the selected provider adapter receives a native key. and its entry-specific falsifier is no-separator-here is rejected before any provider call or projection change; a legal ref reaches only its authorized account leaf and returns a typed quote/detail result.. Repair applied: the parser returns named LegacyInstrumentRef rather than an anonymous product. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-CEE9B91D31",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-CEE9B91D31. Source fact: services/uta/src/__tests__/trading-tools.spec.ts:298-310: Registers Alpaca and Bybit accounts, calls getQuote with bybit-main|BTC and no explicit source, and verifies only the Bybit broker receives the request. The source problem is 测试以 Alice ID 的 provider 前缀选择 bybit，并仅用 spy 证明 alpaca 未被调用；source mismatch、授权 scope 和不可用账户仍没有稳定的协议结果。 and the current target is Source routing is a small query selection value, ExplicitAccount(AccountId) or an owner embedded in a provider reference; when both are present they must agree. Fan-out is used only by a declared all-authorized query leaf. which preserves 保留无 explicit source 时由 aliceId 路由到 bybit-main。; 保留 alpaca broker 不接收请求。. Its boundary is The account-query application performs routing and authorization; only the selected provider interpreter sees the native request, and route code does not inspect concrete broker classes. and its entry-specific falsifier is bybit-main|BTC invokes only the Bybit quote leaf; explicit Alpaca plus the Bybit owner returns SourceMismatch with no Alpaca or Bybit provider call.. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-08E9053B72",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-08E9053B72. Source fact: services/uta/src/__tests__/trading-tools.spec.ts:314-329: Calls getContractDetails with a source and Alice identifier, spies on the broker, and verifies that the broker receives an expanded native query retaining the original identifier. The source problem is 测试用 broker spy 证明 aliceId 被展开为 native contract；details 的 expansion filters、identity provenance 和 raw ContractDetails 脱敏没有被协议约束。 and the current target is Contract details is a provider-declared instrument/details leaf composed from a typed Public/Account reference and provider-specific expansion filters. Its output is normalized base metadata plus an exact declared leaf/directory/continuation extension; native ContractDetails stays in the adapter. A filter variant not declared by the selected leaf is absent from its input schema; it does not remove the details leaf, and declared-but-unavailable leaves report availability separately. Scope is explicit Public or Account(AccountScope) when the capability is account-backed; no route invents a default account or subaccount. which preserves 保留输入 aliceId 展开为 native query 且 identity 不丢失。; 保留 search->details 的 drilldown 用法。. Its boundary is The selected application instrument-query port invokes the provider adapter; transport only validates/encodes that leaf and never assigns arbitrary fields to a native SDK object. and its entry-specific falsifier is mock-paper|AAPL expands to a native query with its identity evidence and returns a normalized details result; a filter not declared by the selected leaf is absent from its input schema and an attempted field is rejected before provider access, while a disconnected declared leaf reports named availability rather than a null/raw object.. Repair applied: an undeclared details filter is schema-absent and rejected before provider access; it does not remove the details leaf. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-E66037B98B",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-E66037B98B. Source fact: services/uta/src/__tests__/trading-tools.spec.ts:331-340: Passes an Alice identifier belonging to another account while selecting mock-paper and verifies that the tool reports the account mismatch without a broker call assertion. The source problem is 测试以错误字符串报告 other-account|AAPL，但没有把 runtime canonical ownership 作为 broker 调用前的决定；nominal TypeScript brand 不能替代该检查。 and the current target is Instrument ownership is a runtime refinement between the requested AccountScope and the provider-resolved InstrumentId/native key. Nominal TypeScript brands alone are insufficient; a mismatch stops the selected leaf before provider effects. which preserves 保留 other-account|AAPL 对 mock-paper source 报账户不匹配。; 保留错误在 broker call 前可观察。. Its boundary is The application authorization boundary compares canonical runtime identifiers, then delegates only an authorized query to the selected adapter. and its entry-specific falsifier is mock-paper with other-account|AAPL fails as AccountInstrumentMismatch before provider access or projection mutation; an authorized same-symbol ref succeeds only in its bound scope.. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-27AD48107A",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-27AD48107A. Source fact: services/uta/src/__tests__/trading-tools.spec.ts:344-371: Uses the placeOrder input schema with empty strings for optional numeric fields and verifies parsing succeeds, empty fields become undefined, and totalQuantity remains the supplied decimal string. The source problem is 测试允许 optional numeric 空字符串转 undefined，却仍把 placeOrder 输入作为可选字段袋；该边界需要明确 Absent、sizing XOR 和未知字段拒绝，而不能由后续 legacy staging 猜测。 and the current target is The place-order boundary is a provider-declared input schema composed from an Order semantic unit and optional provider extensions. Legacy empty strings may decode to structural Absent only in the declared adapter schema; sizing alternatives are a local XOR, not a global order product. which preserves 保留 LLM 发送 `cashQty/lmtPrice/...: \"\"` 时成功并变成 undefined/Absent。; 保留 totalQuantity `0.01` 字符串。. Its boundary is The tool/HTTP codec validates the selected order leaf, then an optional withTransaction wrapper accepts the intent; provider-native compilation occurs only in the provider interpreter after approval. and its entry-specific falsifier is A legacy empty optional numeric field decodes to Absent while `0.01` remains canonical text; both sizing alternatives or neither are rejected when the selected leaf requires exactly one, and no provider/effect is invoked on invalid input.. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-4DE6045663",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-4DE6045663. Source fact: services/uta/src/__tests__/trading-tools.spec.ts:373-387: Uses a non-positive totalQuantity and verifies that the placeOrder schema rejects the input before execution. The source problem is 测试只在 Zod schema 阶段拒绝 totalQuantity=0；正数、有限 Decimal 和 journal 未写入的 invariant 还没有贯穿 application admission。 and the current target is Positive quantity/notional is a refined field constraint declared by the selected provider order schema (or another quantity-bearing capability). It is checked at the wire/application boundary and is not evidence that every provider exposes an order leaf. which preserves 保留 totalQuantity=0 在 schema gate 失败。; 保留 invalid optional numerics 不进入 execution。. Its boundary is Schema/application admission performs the constraint; a rejected query or draft never reaches a provider adapter. If wrapped as a transaction, the same refined value is persisted in the prepared payload. and its entry-specific falsifier is 0, -1, and non-finite values are rejected before provider invocation or journal write; a positive decimal reaches the selected input schema without float conversion.. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-6FA0B99F88",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-6FA0B99F88. Source fact: services/uta/src/http/routes-simulator.ts:17-22: The HTTP module imports Hono/Zod, UTAEngineContext, the concrete MockBroker class, and SecType contract metadata, binding the transport directly to the in-process simulator implementation. The source problem is The route boundary has a concrete MockBroker dependency and no shared named request/response protocol, so it is not the target transport-to-application dependency direction. and the current target is Simulator admin transport is a separately mounted capability tree whose leaves are declared by the mock-simulator provider and bound to explicit AccountScope; it depends on simulator-control application ports, not a global broker interface or concrete MockBroker import. which preserves 保留 /api/simulator 作为 dev simulator control surface。; 保留 real broker 不能接受 god-view commands。. Its boundary is Composition injects a simulator-control service only for an explicitly enabled mock-simulator instance; production HTTP mounts no simulator leaves and real provider adapters cannot receive god-view commands. and its entry-specific falsifier is An authorized mock-simulator leaf changes fixture state; a real, keyless, or readOnly account has no simulator leaf and returns typed forbidden/unavailable without provider mutation.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-631DC01624",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-631DC01624. Source fact: services/uta/src/http/routes-simulator.ts:24-41: Simulator price and fill bodies are validated with local Zod schemas; numericString accepts either a string or a number and normalizes both to String, while price movement and fill quantity remain unbranded primitive values. The source problem is Local schemas accept ordinary numbers and erase nominal financial types during normalization; the target requires shared runtime schemas and branded decimal/domain values at the boundary. and the current target is Simulator price/fill inputs are leaf-specific schemas bound to explicit AccountScope: SetMarkPrice and TickPrice remain a small control union, while fill quantity/price use refined Decimal values. The simulator schema is not an order contract and does not imply sibling operations for providers that do not declare them. which preserves 保留现有 mark/tick/fill 三种控制的语义区分。; 保留精确字符串在价格/数量中的精度。. Its boundary is Transport decodes the selected simulator leaf and calls SimulatorControlService; only the mock interpreter converts refined values to its in-memory representation. and its entry-specific falsifier is `0.00001234` remains exact through the simulator leaf; malformed/non-finite values and invalid percentages fail before state change, and the response is decoded as the named control result rather than `{ok:true}`.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-6A13EE36A7",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-6A13EE36A7. Source fact: services/uta/src/http/routes-simulator.ts:43-77: The simulator accepts a permissive IBKR-like contract object plus nativeKey and wallet/external-trade fields; derivative metadata, secType, side, quantity, and price are validated locally but remain optional/native-key request fields. The source problem is The public body shape is a permissive broker-specific contract surface and direct state-mutation command, not a named domain/protocol ADT. Optional fields allow invalid combinations that the target order/action contract forbids. and the current target is External simulator inputs are provider-declared observation/control leaves with explicit AccountScope. Each leaf has an exact instrument metadata extension and variant-specific fields; Deposit, Withdraw, and Trade are distinct simulator leaves, not universal Alice Order effects. If one distinct leaf is undeclared, only that leaf is absent; unsupported parameter variants are omitted from its exact schema while declared-leaf availability remains separate. which preserves 保留 OPT/FUT/CASH/BOND/CRYPTO 元数据可供 simulator fixture 建模。; 保留 deposit/withdraw 不走 order pipeline，trade 可改变 cash/position。. Its boundary is Only the simulator interpreter consumes native metadata; the application records an external observation and updates the mock projection. Production account projections consume broker observations, not arbitrary HTTP bodies. and its entry-specific falsifier is An invalid option/future relation is rejected at decode; a valid external deposit/trade updates only the scoped mock observation/projection and produces no transaction approval or dispatch job. If the Trade leaf is undeclared, only that distinct leaf is absent while Deposit/Withdraw remain independently discoverable; a declared but unavailable leaf reports named availability.. Repair applied: Deposit, Withdraw, and Trade remain distinct simulator leaves, with schema-absent parameter variants separated from declared-leaf availability. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-3A43AEAF75",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-3A43AEAF75. Source fact: services/uta/src/http/routes-simulator.ts:81-101: resolveMock looks up a UTA by path id, performs an instanceof MockBroker check, and emits ad hoc string errors; parseBody catches invalid JSON and returns Zod issue arrays in a local {error, issues} envelope. The source problem is Resolution and validation are local imperative checks with concrete-class detection and unstructured errors; there is no typed authorization, capability, or protocol failure mapping. and the current target is Simulator resolution is a typed capability-handle lookup followed by schema decoding: InvalidAccountId, SimulatorNotFound, NotSimulator, and Forbidden are distinct protocol variants. Concrete-class checks stay inside the simulator composition/interpreter. A resolved simulator handle always carries explicit AccountScope. which preserves 保留 missing id=400、unknown id=404、real broker=400 的语义差异，但转为 named errors。; 保留 malformed JSON 与 schema issues 不进入 MockBroker。. Its boundary is Application resolves the selected simulator capability and transport maps typed failures to stable status/code; malformed JSON never reaches a provider interpreter. and its entry-specific falsifier is Malformed JSON invokes no provider; a real account cannot become a simulator by body fields, and a principal lacking the simulator leaf gets Forbidden without leaking hidden account details.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-0E76C75480",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-0E76C75480. Source fact: services/uta/src/http/routes-simulator.ts:105-114: Creates an independent Hono app and lists only manager entries whose broker is a MockBroker, returning id/label summaries. The source problem is This endpoint is a simulator fixture discovery surface, not a target transaction API; it must not be mistaken for the authoritative account/capability listing. The route has no authenticated transport principal or separate operation-authorization check; direct MockBroker mutation is mounted as an HTTP surface. and the current target is SimulatorListResponse is a separate finite discovery projection for declared mock-simulator capabilities, containing explicit AccountScope-bound identity, label, generation, availability, and descriptor revision; it is not the production account list. which preserves 保留 GET /utas 只列 MockBroker simulator，不列 real object。; 保留 id/label 对 dev UI 的发现能力。. Its boundary is SimulatorControlService reads registry/projection state. The route is mounted only when that capability tree is enabled and does not call provider mutation. and its entry-specific falsifier is An authorized mock simulator is listed with id/label/generation; real and keyless data-only instances are absent from this list, and unauthorized callers receive a typed denial without existence leakage.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-D266753674",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-D266753674. Source fact: services/uta/src/http/routes-simulator.ts:116-121: Resolves a MockBroker and serializes its full simulator state directly through getSimulatorState without an application query or explicit response schema. The source problem is The response is an internal simulator state view and is not the target authoritative account or transaction projection. The route has no authenticated transport principal or separate operation-authorization check; direct MockBroker mutation is mounted as an HTTP surface. and the current target is SimulatorStateView is a versioned finite pull result declared by the simulator capability: cash/mark/position/order values use refined domain schemas and exact provider extensions, plus generation, sequence, and as-of; it is not production AccountState. The state view is bound to explicit AccountScope and simulator generation. which preserves 保留 state endpoint 能显示 cash、markPrices、positions、pendingOrders。; 保留 unknown UTA=404、non-simulator=400 的可诊断性（typed）。. Its boundary is A simulator query application port reads the mock interpreter/projection and HTTP encodes its declared response; raw getSimulatorState class instances do not cross the boundary. and its entry-specific falsifier is After mark price 80000, the state response contains an exact Decimal price and no SDK object; an old generation/cursor is reported stale rather than represented as current state.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-21CA670156",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-21CA670156. Source fact: services/uta/src/http/routes-simulator.ts:123-135: Validates a simulator mark price, calls MockBroker.setMarkPrice directly, allows the broker to auto-match pending orders, and returns the filled order ids; broker exceptions become a 400 string error. The source problem is Direct mark-price mutation and auto-fill are god-view test controls, not a durable TransactionCommand or broker dispatch with DispatchIdentity. The route has no authenticated transport principal or separate operation-authorization check; direct MockBroker mutation is mounted as an HTTP surface. and the current target is SetMarkPrice is a simulator control leaf with explicit AccountScope, simulator key, and Price input and a named MarkPriceApplied result containing fixture observation identity and triggered order observations. Auto-match is simulator behavior, not PlaceOrder dispatch. which preserves 保留 setMarkPrice 自动撮合 pending orders 并返回 filled ids。; 保留 broker exception 不再变成 generic Error.message，而成为 typed fill rejection。. Its boundary is SimulatorControlService invokes the mock control port only under simulator capability authorization; production schedulers and transaction effect interpreters do not use this HTTP leaf as venue execution. and its entry-specific falsifier is A pending BUY@79000 remains pending at mark 80000 and fills once at 79000; replaying one fixture command does not duplicate its observation, and a non-simulator scope cannot invoke the leaf.. Repair applied: mark-price transition uses named MarkPriceTransition. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-AB73E0949C",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-AB73E0949C. Source fact: services/uta/src/http/routes-simulator.ts:137-149: Validates nativeKey and deltaPercent, calls MockBroker.tickPrice directly, and returns auto-filled order ids; missing mark price or other broker errors are converted to a 400 string error. The source problem is This is a deterministic test control and not a broker-observed market event or durable trading transaction. The route has no authenticated transport principal or separate operation-authorization check; direct MockBroker mutation is mounted as an HTTP surface. and the current target is TickPrice is a simulator control leaf with a refined Percent input and the same declared MarkPriceApplied result as the simulator schema permits; it is not a market-data stream or a trading approval path. TickPrice is bound to explicit AccountScope. which preserves 保留无 markPrice 先 set 才能 tick 的顺序约束。; 保留 tick 后自动撮合返回 filled ids。. Its boundary is The selected simulator interpreter applies the deterministic tick and optional auto-match; HTTP only decodes/encodes the leaf and maps typed failures. and its entry-specific falsifier is Tick without an initialized BTC mark returns MissingMarkPrice; +5% from 100 yields exact 105 and triggers the same mock rules, with no order transaction state changed.. Repair applied: tick-price transition uses named TickPriceTransition. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-24F7AD27D6",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-24F7AD27D6. Source fact: services/uta/src/http/routes-simulator.ts:151-165: Resolves a MockBroker, accepts optional price/qty, directly forces a pending order fill, and returns {ok:true}; broker exceptions become a 400 string error. The source problem is Manual fill bypasses prepare, write-ahead dispatch, compensation, and remote-outcome reconciliation by mutating the simulator directly. The route has no authenticated transport principal or separate operation-authorization check; direct MockBroker mutation is mounted as an HTTP surface. and the current target is ManualFill is a simulator-only control leaf with explicit AccountScope and BrokerOrderId and optional declared Price/Quantity fields, returning a typed FillObservation/remaining state. It bypasses venue transactions by design and must not be exposed as production Cancel/Fill authority. which preserves 保留 optional price 默认 mark/limit/100 以及 optional partial qty。; 保留 unknown orderId 400。. Its boundary is The mock control interpreter performs the fixture mutation; production order projections consume broker observations associated with a transaction effect, not this HTTP command. and its entry-specific falsifier is A full pending fill removes it from the pending view; a partial fill leaves Submitted with remaining/cumulative values; unknown or terminal ids return a typed error without state change.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-DFB69CBAAD",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-DFB69CBAAD. Source fact: services/uta/src/http/routes-simulator.ts:167-179: Directly invokes MockBroker.cancelPendingOrder for a forced simulator cancellation and emits an unstructured 400 on failure. services/uta/src/domain/trading/brokers/mock/MockBroker.ts:623-628: cancelPendingOrder only checks that the order exists, then unconditionally writes status=Cancelled even for a terminal order. The source problem is Simulator force-cancel calls MockBroker.cancelPendingOrder, whose source implementation overwrites any existing order status without checking Submitted; this unsafe fixture behavior must be replaced by an explicit pending-only control command, separate from production CancelOrder. and the current target is SimulatorCancel is a distinct pending-only control leaf with explicit AccountScope and BrokerOrderId. It may transition Submitted to fixture-cancelled, but has no relationship to a provider-declared production cancel leaf unless that leaf is separately exposed and transaction-wrapped. which preserves 保留强制取消 pending simulator order 的清理用途。; 保留不存在 order 返回 400，改为 typed。. Its boundary is SimulatorControlService calls only the mock fixture interpreter. A real provider cancellation, if declared, uses its own Order semantic schema and optional withTransaction wrapper, never this bypass. and its entry-specific falsifier is Submitted becomes Cancelled; Filled or already Cancelled remains unchanged and returns OrderAlreadyTerminal; a real account has no simulator cancel leaf.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-1C7C835F5A",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-1C7C835F5A. Source fact: services/uta/src/http/routes-simulator.ts:181-193: Directly injects a wallet-source position into MockBroker through externalDeposit, bypassing the order pipeline, then returns {ok:true}; failures become string 400 responses. services/uta/src/domain/trading/brokers/mock/MockBroker.ts:680-697: externalDeposit accepts positive quantity, leaves cash unchanged, and uses markPrice or Decimal(0) as wallet avgCost for a new position. The source problem is The route directly injects a wallet-source position and returns {ok:true}; when no mark exists the MockBroker source uses zero as avgCost, so the target must make missing-mark placeholder policy explicit rather than presenting zero as valuation truth. and the current target is ExternalDeposit is a simulator observation leaf with explicit AccountScope-bound instrument/Quantity, fixtureId, timestamp, source, and an explicit cost-basis variant MarkPresent | MissingMark(Indeterminate). It is data/accounting evidence, not an Alice order effect. which preserves 保留 deposit 不扣 cash、wallet-source tag、markPrice 作为 placeholder avgCost。; 保留 external event 绕过 order pipeline 的模拟含义。. Its boundary is Only an authorized simulator observation injector may create the fixture; accounting/projection consumes the typed observation. Production external transfers come from provider observations, not HTTP body mutation. and its entry-specific falsifier is After mark BTC=80000, deposit 1.0093 keeps cash unchanged and records MarkPresent(80000); without a mark it records MissingMark/Indeterminate; replaying the fixture does not double quantity.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-2444CEB754",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-2444CEB754. Source fact: services/uta/src/http/routes-simulator.ts:195-207: Directly injects a simulator withdrawal by calling MockBroker.externalWithdraw(nativeKey, quantity), bypassing transaction and accounting services, with string 400 errors. services/uta/src/domain/trading/brokers/mock/MockBroker.ts:700-710: externalWithdraw subtracts requested quantity and deletes the position whenever the remainder is <=0; it does not reject an over-withdrawal or non-positive quantity. The source problem is The current external-withdraw route can turn an over-sized or non-positive request into deletion/negative intermediate exposure and reports only {ok:true}; the target must validate Quantity against the observed position before recording a typed external observation. and the current target is ExternalWithdraw is a simulator observation leaf with explicit AccountScope-bound instrument/Quantity and fixture identity. Its pure accounting transition validates existing exposure before applying a delta; it is not a production cancel/order transaction. which preserves 保留 cash 不变、全部提取删除 position、部分提取保留 wallet cost source；将当前 oversell/delete permissiveness 改为 typed InsufficientQuantity rejection。. Its boundary is Simulator observation service applies the fixture and updates its projection; real-provider withdrawals arrive through broker account/position observations and are reconciled by the accounting boundary. and its entry-specific falsifier is With 1 BTC, withdraw .4 leaves .6 and cash unchanged; withdraw 2 or a non-positive amount returns a typed rejection and leaves exposure and cursor unchanged.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-6CE9D89D9B",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-6CE9D89D9B. Source fact: services/uta/src/http/routes-simulator.ts:209-221: Directly injects a user-executed exchange trade into MockBroker.externalTrade with native contract metadata and returns {ok:true}; errors are unstructured 400 strings. The source problem is The route directly changes position/cash state with no durable transaction/observation identity, accounting event, compensation, or unknown-outcome handling. The route has no authenticated transport principal or separate operation-authorization check; direct MockBroker mutation is mounted as an HTTP surface. and the current target is ExternalTrade is a simulator observation leaf with an explicit AccountScope-bound instrument, side, Quantity, Price, exact metadata extension, source, timestamp, and fixture identity. BUY/SELL are observed external activity, not controlled order dispatch. which preserves 保留 BUY .5 BTC@60000 使 cash 100k->70k、avgCost=60000、wallet tag。; 保留外部 SELL 开空与 Alice order SELL 无仓拒绝的语义差异。. Its boundary is The simulator control service is the only HTTP-facing injector; accounting/reconciliation projects the typed external observation, while real fills use provider observation identities at their own boundary. and its entry-specific falsifier is BUY .5 BTC@60000 changes cash 100000 to 70000 with wallet provenance and exact cost; replay does not double debit, and oversell is rejected without pretending a dispatch occurred.. Repair applied: external-trade fold uses named ExternalTradeProjection. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-D034C92CBD",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-D034C92CBD. Source fact: services/uta/src/http/routes-trading-wallet.spec.ts:1-14: Builds a minimal UTAEngineContext whose manager returns one arbitrary mock UTA and imports the legacy TradingGit PendingHashConflictError; tests exercise createTradingRoutes directly without authentication or real transaction services. The source problem is wallet route 测试 cast 一个只含 manager.get 的 UTAEngineContext，并导入 PendingHashConflictError；它无法观察 authenticated principal、SQLite receipt、jobs/locks 或 broker interpreter 的真实边界。 and the current target is Wallet-route integration fixture composes only the selected transaction-wrapped effect leaf with transport verifier, typed codecs, writer, and projection reader. It does not require a complete UTAEngineContext or a universal broker handler set. Any account-backed fixture binds Principal and explicit AccountScope. which preserves 保留该文件覆盖 wallet approval route 的行为意图。; 保留测试隔离与可控 broker failure，但把 stub 替成 typed interpreter。. Its boundary is Test composition may use an in-process writer and selected Mock/provider interpreter, but HTTP crosses the same Principal/schema boundary as production. Data-only routes use separate query fixtures. and its entry-specific falsifier is A selected effect command can be followed by one durable receipt query; invalid or missing leaves do not invoke a provider and one test cannot leak events/locks into another scope.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-B198583232",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-B198583232. Source fact: services/uta/src/http/routes-trading-wallet.spec.ts:16-39: Asserts push and reject return 409 PENDING_HASH_REQUIRED and do not call mutation methods when the legacy pending hash is absent. The source problem is 测试把缺 expectedPendingHash 映射成 PENDING_HASH_REQUIRED；该旧检查只保护 Git staging，不能替代 transactionId/revision/digest/expiry/principal 的 canonical command 校验。 and the current target is Approval/review input is the schema of a selected transaction wrapper: transaction identity, revision/digest/expiry and actor/policy binding are required only for that controlled effect. A Git pending hash is not a transport-wide approval contract; data leaves have no such fields. The selected account effect carries explicit AccountScope; data leaves have no approval scope. which preserves 保留缺少 expected pending hash 时不会调用 push/reject。; 保留旧 hash conflict 的 409 类语义，改为 typed transaction conflict。. Its boundary is HTTP decodes the selected effect command and application atomically CAS-checks the durable transaction record; the provider interpreter is not called on missing/stale binding. and its entry-specific falsifier is A body containing only the old hash is rejected with MissingApprovalBinding; stale revision/digest creates no event or provider call; a valid binding reaches the wrapper’s queued state.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-6AF6021791",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-6AF6021791. Source fact: services/uta/src/http/routes-trading-wallet.spec.ts:41-57: Stubs push to throw PendingHashConflictError and verifies a stale expected hash yields 409 PENDING_HASH_CONFLICT while push receives the stale hash. The source problem is 测试以 PendingHashConflictError 模拟 stale hash；冲突是否保持 durable transaction version、无 jobs/locks/events，不能由一次 vi throw 得出。 and the current target is Stale approval is an effect-wrapper CAS conflict over the declared transaction identity/revision/digest; it is not a PendingHashConflictError and is not a reason to add a rejection handler to every provider. The selected account effect retains explicit AccountScope during the CAS decision. which preserves 保留 stale client 不得把 hash change 当 success。; 保留调用方能看到冲突并重新读状态。. Its boundary is The writer owns atomic compare-and-append for the selected effect; HTTP only encodes the conflict and never forwards a legacy hash to a provider. and its entry-specific falsifier is A stale expected revision returns a stable conflict, leaves the effect projection unchanged, and produces no broker/provider mutation; rereading the plan exposes the current revision.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-FE1C608463",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-FE1C608463. Source fact: services/uta/src/http/routes-trading-wallet.spec.ts:59-78: Stubs push to return a Git PushResult and verifies matching expectedPendingHash produces HTTP 200 and forwards the hash to push. The source problem is 测试把 matching hash 的 Git PushResult 当作 HTTP success；这没有区分 approval receipt、DispatchStarted、Ack 和 fill，也无法证明 response 丢失后的 command convergence。 and the current target is Approve returns the selected transaction wrapper’s durable receipt/schema (command identity, effect revision, descriptor/policy evidence, queued work). It never returns a Git PushResult or claims remote Ack/fill; auto approval is a server-derived policy decision. The selected account effect binds the receipt to explicit AccountScope. which preserves 保留 matching expected hash historically permits push；new success means approved/queued, with Human mode when AI trading is disabled and AuthorizedAuto mode only from server policy when enabled。; 保留 UI 可在成功后查询 transaction status。. Its boundary is The writer commits approval and the wrapper’s dispatch plan/jobs; a scheduler/provider interpreter runs later. HTTP ends at the durable receipt, while a data leaf remains a finite read. and its entry-specific falsifier is A valid approval returns one queued receipt and no PushResult; a body-only allow-AI flag cannot authorize; response loss converges to the same receipt and later unknown outcomes remain unknown.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-F3C5E04898",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-F3C5E04898. Source fact: services/uta/src/http/routes-trading.ts:1-11: The boundary imports Hono/Zod but also UnifiedTradingAccount, BrokerError, order-entry, TradingGit hash handling, order-history projections, and a domain contract-search helper; route composition therefore reaches legacy execution and Git persistence APIs directly. The source problem is The HTTP module directly imports old UTA/Git execution and projection helpers rather than target application/protocol abstractions, and BrokerError is collapsed later into ad hoc HTTP fields. and the current target is HTTP composition depends on protocol codecs, transport verification, and selected application capability ports. It must not import a concrete UTA, TradingGit, broker SDK, or a hand-maintained global action map; each provider route tree is mounted from declared leaves. Account-backed capability requests carry explicit AccountScope; public data leaves remain Public. which preserves 保留现有 Hono route mount 与 UI/SDK URL 迁移窗口。; 保留 query 与 mutation 分离，删除 direct legacy authority。. Its boundary is The composition root injects the capability registry and application ports; HTTP performs no native compilation, SQL journal write, Git access, or broker lifecycle. Provider adapters remain behind their own SPI/process boundary. and its entry-specific falsifier is An authenticated request reaches exactly the selected application port and returns its declared schema; a provider with no leaf has no route, and a transaction-wrapped effect’s receipt is observable without exposing UTA/Git/native objects.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-3CBE1C6172",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-3CBE1C6172. Source fact: services/uta/src/http/routes-trading.ts:13-60: Local Zod schemas validate the legacy wallet/order request shapes. Place-order uses optional quantity/cash/price/order fields with a free-form orderType and aliceId; close/cancel retain legacy ids and commit message requirements. The source problem is Optional-field bags and free-form orderType make the route-local schema unable to describe the exact provider capability; aliceId/message are legacy surface concerns, and the schema is not the selected declaration-derived protocol. and the current target is Route request schemas are derived per capability leaf. A provider may declare an Order semantic unit and compose sizing, terms, relations, or native extensions with HOFs; no global Place/Modify/Close/Cancel product or Cartesian order enum is required. If the Order leaf is declared but one parameter variant is unsupported, that variant is omitted from the leaf's exact schema and does not remove other variants; only an absent capability leaf has no route. Audit rationale and Principal remain separate metadata. Account-backed order leaves carry explicit AccountScope; public data leaves remain scope-appropriate. which preserves 保留旧输入中已被调查的 trailing、GTD/outsideRth、parent/OCA、TP/SL 与 Decimal string precision；仅在选定 provider schema 声明时保留。; 将 commit message 与 action/approval payload 分离。. Its boundary is Transport decodes the chosen leaf, authenticates Principal, and calls its application port; native request compilation and capability checks happen in the selected adapter, not in route-local Zod or a universal switch. and its entry-specific falsifier is A provider Order leaf that declares a sizing XOR and protection extension round-trips the declared fields; an unsupported protection parameter is absent from that leaf's input schema and an attempted field is rejected while the leaf and its other declared variants remain discoverable; only a provider without the Order capability has no route, and invalid input causes no effect/journal mutation.. Repair applied: unsupported parameter variants remain schema-absent without deleting a declared Order leaf; only a missing capability leaf has no route. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-75C6D069F1",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-75C6D069F1. Source fact: services/uta/src/http/routes-trading.ts:61-78: Maps executeOneShotOrder's stage/commit/push string error union to HTTP 400/500 and returns {error, phase}; successful requests return the push result directly. The source problem is runOneShot 把 stage、commit、push 的错误 union 直接映射为 phase 与 400/500，并返回 push result；这种 transport phase 不能表达 durable preparation、approval gate 或 Unknown outcome。 and the current target is The one-shot phase map is removed from the target contract. A route either invokes a finite data leaf or a selected capability wrapped withTransaction; that wrapper exposes explicit prepare/approve/review/recovery states, while provider-specific domain errors remain in the declared error schema. The selected account effect retains explicit AccountScope; public data routes have no effect state. which preserves 保留 route 能报告阶段性失败，但改用 state/failure variant。; 保留 commit response loss 可由 commandId 查询收敛；`allowAiTrading` disabled remains AwaitingApproval, enabled may request auto-approval only after Principal/policy checks.. Its boundary is HTTP calls an application port; only the selected transaction wrapper creates durable jobs and the scheduler/provider interpreter performs controlled effects. Data query/stream handlers never acquire this machinery. and its entry-specific falsifier is Preparing never calls the provider; approval creates one durable job; response loss returns the same receipt; provider rejection has evidence and a post-dispatch disconnect remains Reconciling/Unknown rather than a blind 500 retry.. Repair applied: decodeSelectedRequest is explicit that HTTP decoding does not dispatch provider effects. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-D58BA63EC0",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-D58BA63EC0. Source fact: services/uta/src/http/routes-trading.ts:80-82: Defines a local allow-list for the contract-search query and uses a protocol AssetClassHint type, but it is not itself a decoded request schema or capability ADT. The source problem is A local string Set and cast only filters one query hint; it does not represent target capability derivation or validate the full contract-search request. and the current target is AssetClassHint is a declared search-input refinement, not a capability map. The selected provider search leaf may accept it, normalize it, or omit the field; an unsupported hint parameter is omitted from the selected input schema rather than removing the search leaf; a declared but disconnected leaf reports availability separately, and neither outcome is a route-local global Set. A provider-declared account search leaf carries explicit AccountScope, while a public leaf remains Public. which preserves 保留 equity/crypto/currency/commodity/unknown hints 与 crypto normalization。; 保留 invalid hint 不使 route crash，但将其变成稳定 validation failure 而不是静默当作 unknown。. Its boundary is The application search capability owns normalization and capability metadata; HTTP decodes the selected schema and does not decide broker support by a cast. and its entry-specific falsifier is A declared crypto hint normalizes BTCUSD according to its provider rule; omitted hint uses the declared default; an undeclared hint parameter is absent from the selected schema and an attempted field fails validation; an invalid declared value never becomes a capability claim.. Repair applied: an unsupported search-hint parameter is omitted from the selected schema, not treated as disappearance of the search capability. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-0FC7B6DD68",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-0FC7B6DD68. Source fact: services/uta/src/http/routes-trading.ts:84-88: Extracts and trims a legacy expectedPendingHash from an unknown request body, returning undefined for absent/non-string values. The source problem is readExpectedPendingHash 从 unknown body 中提取并 trim Git hash；它让 process-local staging hash 成为 approval 身份，绕过 canonical plan digest、revision、expiry 和 actor binding。 and the current target is Legacy expectedPendingHash is removed from the transport schema. Only a selected transaction wrapper may declare revision, planDigest, expiry, and actor/policy binding; ordinary data leaves never receive approval fields. A selected account effect binds any wrapper approval to explicit AccountScope. which preserves 保留旧 hash 作为 optimistic conflict 的安全目的，不保留其 Git-specific 形状。; 保留缺字段不触发 mutation。. Its boundary is The selected application transaction port reads durable wrapper state and calls the writer; HTTP never reads Git pending state or invokes a provider from a body hash. and its entry-specific falsifier is A body with only expectedPendingHash is rejected; digest/revision mismatch writes no approval; a valid selected effect binding queues its own durable receipt while a quote/data leaf has no such operation.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-146D2109B6",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-146D2109B6. Source fact: services/uta/src/http/routes-trading.ts:90-95: Reads the path id and directly resolves a UnifiedTradingAccount through UTAEngineContext. Missing ids return null for each caller to turn into a local 404. The source problem is Path strings are passed directly into in-process account lookup with no protocol decoding, authorization decision, or projection/service boundary. and the current target is Account route resolution is a typed registry/capability lookup: path syntax yields AccountId, then Principal/scope authorization yields an AuthorizedAccount handle with declared capabilities and availability. It does not expose UnifiedTradingAccount. Authorization resolves an explicit AccountScope before an account-backed capability is invoked. which preserves 保留不存在账户 404 语义。; 保留同一 route 可以按 account id 定位，但不暴露 UnifiedTradingAccount。. Its boundary is Application account-directory/query ports perform lookup and scope filtering; HTTP only maps typed failures and passes the selected capability request. and its entry-specific falsifier is Malformed/unknown id fails before provider invocation; an authorized account returns explicit scope/capability state, and an unauthorized existing account is not collapsed into a local 404.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-5C97C6CC79",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-5C97C6CC79. Source fact: services/uta/src/http/routes-trading.ts:97-125: Implements shared route query handling: offline accounts are nudged and return 503 with health info; broker errors are converted to {error, code, transient} and 500/503 based on a permanent boolean; successful values are serialized directly. The source problem is Health and broker failures are represented by local strings/booleans and direct side effects; expected failures are not the target ADTs and successful/native values have no named response schema. and the current target is Query invocation is a protocol adapter for a selected finite pull or declared push stream. It maps typed availability, delivery, and provider-domain errors without `nudgeRecovery`, BrokerError text, or a universal query result shape that every provider must implement. Account-backed leaves carry explicit AccountScope; public leaves remain Public. which preserves 保留 offline 返回 unavailable 类状态；保留 transient/permanent 可供 UI 重试提示。; 保留 native broker errors 被分类，但改为 typed code。. Its boundary is Connection/resource supervision and recovery are application services; HTTP performs pure authentication, decode, encode, and stream cancellation. A provider leaf may be unavailable while retaining its descriptor. and its entry-specific falsifier is Offline pull returns typed unavailable with health evidence and no transaction mutation; a declared push emits data/error/end frames and cancels its owned resource on disconnect; malformed provider output is rejected.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-2F69B76FDC",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-2F69B76FDC. Source fact: services/uta/src/http/routes-trading.ts:127-135: Creates an unauthenticated Hono route set and lists ctx.utaManager.listUTAs() directly as the response for GET /uta. The source problem is Alice authenticates the user-facing proxy, but this UTA loopback route exposes raw manager summaries and has no named protocol schema or typed transport-principal boundary. Transport authentication must not be conflated with transaction authorization. and the current target is Account listing is an authenticated finite pull capability using the fixed descriptor envelope. A provisioned transport verifier creates Principal, then the application filters visible scopes and returns Complete/Partial/Unavailable; body actors and loopback are never authorization. Each account row carries its explicit AccountScope, while capability availability remains separate from schema presence. which preserves 保留 GET /uta 的 id/label listing。; 保留 Alice proxy path，但不再把 loopback reach 当授权。. Its boundary is Deployment/Guardian owns credential provisioning and verifier integration; UTA transport maps the verified Principal to application query ports. No route-local crypto, body-supplied actor, or fallback trust in localhost. and its entry-specific falsifier is Unauthenticated loopback and invalid credentials return 401; an unprovisioned verifier returns typed unavailable; a valid Principal sees only authorized scopes and partial coverage is not Complete/empty.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Empirical obligation retained: The repository does not provide the UTA↔Alice service credential/issuer/verifier implementation; Main/deployment must designate the secret provisioning and verifier owner without route-local crypto or a body-supplied identity.。 Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-A7201E902B",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-A7201E902B. Source fact: services/uta/src/http/routes-trading.ts:137-142: Calls utaManager.getAggregatedEquity() and serializes its result without a request/response schema or accounting/valuation error model. The source problem is The route directly invokes a manager aggregate and returns an untyped aggregate shape; target accounting owns currency/FX/uncertainty and read projections are rebuildable. and the current target is Equity is an accounting/data projection capability. Its declared output contains per-AccountScope Money values, valuation/FX provenance, freshness/coherence, and provider extensions; it is not an order contract and never requires order approval or compensation. which preserves 保留跨账户聚合 equity/cash/PnL 以及 per-account rows。; 保留 keyless/data accounts 不混入 funded portfolio。. Its boundary is The accounting application reads account/position observations and FX capability leaves; HTTP encodes the resulting projection and does not call a manager or broker write API. and its entry-specific falsifier is Mixed-currency rows aggregate only with declared fresh FX; missing FX remains Partial/indeterminate and no rate=1/empty-success fallback is emitted.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-3CDB550874",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-3CDB550874. Source fact: services/uta/src/http/routes-trading.ts:144-165: Reads pattern/query, assetClass, and source/accountId query strings, invokes searchTradeableContracts across configured accounts, and returns raw hits/count plus a configured-account count. The source problem is The route performs permissive query parsing and delegates a broker-side heuristic over UTA instances; response and errors are not named protocol values and capability context is incomplete. and the current target is Contract search route is a provider-declared finite pull leaf with typed pattern/hint/source-selection input and ListingResult output. Provider-specific search filters/extensions are composed in the leaf schema; no route-wide broker search API is assumed. Scope is explicit Public or Account(AccountScope) when the capability is account-backed; no route invents a default account or subaccount. which preserves 保留 pattern/query alias、assetClass hint、source/accountId filter。; 保留 count 与 configured account visibility，但改为 typed completeness。. Its boundary is Application instrument-query selects declared provider leaves and owns all-settled failure isolation; HTTP does not enumerate manager accounts or pass raw vendor objects. and its entry-specific falsifier is A source-scoped search invokes only that declared leaf; all-authorized search preserves healthy hits and names missing scopes; no configured namespace returns Complete(empty) only with explicit coverage.. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-95C382B4F8",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-95C382B4F8. Source fact: services/uta/src/http/routes-trading.ts:167-190: Walks each UTA; skips offline accounts and catches errors only around positions/account reads. It collects non-USD currencies, then calls ctx.fxService.getRate outside that catch, so FX failures escape. Returns primitive rate/source/timestamp records. The source problem is Transport performs live aggregation, omits offline or failed account reads, lets FX failures escape as generic route failure, and returns primitive strings/numbers without currency-qualified values or typed uncertainty. and the current target is FX rates are a finite data pull over a declared FX observation leaf. Output uses exact Decimal rates, currency/unit, source, timestamp, freshness, and missing-scope/currency markers; offline account reads are not silently skipped. Account-scoped observations identify the AccountScope whose currencies were observed; public FX leaves remain Public. which preserves 保留 USD 排除与 non-USD currency discovery。; 保留 per-currency source/updatedAt 供工具解释。. Its boundary is Accounting/FX application services perform account observation and FX leaf calls; HTTP only decodes query and encodes the typed response. and its entry-specific falsifier is One account failure is named while healthy currencies remain; an FX provider failure yields Partial/Unavailable with evidence rather than uncaught 500 or empty rates, and decimal text remains exact.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-3D3861C921",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-3D3861C921. Source fact: services/uta/src/http/routes-trading.ts:192-215: Parses an arbitrary request into utaConfigSchema, dynamically creates a broker, calls init/getAccount/getPositions directly, returns raw account/positions, and best-effort closes the broker in finally. The source problem is The HTTP handler directly constructs a concrete broker and invokes lifecycle/query methods, accepts arbitrary config at the process boundary, and exposes raw broker results; it bypasses application/broker-spi layering and shared protocol schemas. and the current target is Connection probe is a provider-declared, ephemeral capability leaf with a typed config reference/preset input and capability/reach/account-read output. Native credentials/config and lifecycle handles stay inside the selected provider process. An account-read probe reports explicit AccountScope where applicable. which preserves 保留 setup wizard 能验证连接并读取 account+positions 的成功信号。; 保留 best-effort close 作为 Scope finalizer，而非 route finally swallow。. Its boundary is A connection-control application port resolves provisioned credentials/config references, scopes the provider connection, performs probe/close, and returns the selected leaf schema; HTTP does not dynamically create brokers. and its entry-specific falsifier is Invalid config fails before provider init; a successful probe returns named reach/capability facts with no key/native class; close failure is visible and cannot mark the provider healthy.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-E98645ED1A",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-E98645ED1A. Source fact: services/uta/src/http/routes-trading.ts:217-224: Calls utaManager.reconnectUTA(id) directly and chooses HTTP 200 or 500 from a success boolean, with no account existence, authorization, typed connection state, or protocol failure mapping. The source problem is Connection lifecycle is reached through the legacy manager and reduced to success/500 rather than target scoped Layer/supervision and typed degraded capability. and the current target is Reconnect is a provider connection-lifecycle capability with AccountScope/instance generation and typed transitions. It is not order transaction recovery; restoring a connection does not imply any dispatch is safe. which preserves 保留 reconnect endpoint 与 success/failure response intent。; 不再把 whole-process restart boolean 当单账户 broker readiness。. Its boundary is Connection-control/supervision owns reconnect and provider lifecycle; HTTP invokes that selected capability and returns a transition/availability schema rather than a success boolean. and its entry-specific falsifier is Unknown id is typed not-found; a reconnect yields a new generation/health only after evidence, while a transaction with unknown dispatch remains Reconciling until its own observation path resolves.. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-3194C27217",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-3194C27217. Source fact: services/uta/src/http/routes-trading.ts:226-239: Accepts an unvalidated optional numeric delayMs and calls uta.sync directly to force broker state synchronization; all exceptions become a 500 string error. The source problem is The route has no command schema, durable job identity, lease/reconciliation semantics, or unknown-outcome handling; it calls legacy in-memory UTA synchronization directly. and the current target is Sync is a provider-declared data observation/refresh leaf with a bounded scheduling hint if the provider exposes one. It must not become a universal order-recovery command; unknown post-dispatch order outcomes use the selected effect wrapper’s recovery capability. When sync is account-backed, its request and observation are bound to explicit AccountScope. which preserves 保留 AI 可主动触发 stale state synchronization。; 保留 optional delay only as a bounded 0–60000ms scheduling hint; it never decides business authority or sleeps inside a request.. Its boundary is Application resource supervision schedules the provider observation and returns a typed receipt/result; HTTP never sleeps or invokes an imperative UTA.sync directly. and its entry-specific falsifier is Negative/fractional/over-limit hints fail before scheduling; a refresh does not create a PlaceOrder or compensation; a declared effect recovery request remains an explicit observe/reconcile operation.. Repair applied: data refresh uses DataDeliveryUnknown, kept distinct from post-dispatch order OutcomeUnknown. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-44AA5A48AB",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-44AA5A48AB. Source fact: services/uta/src/http/routes-trading.ts:241-259: Accepts any array-valued changes field, calls uta.simulatePriceChange directly for all account types, and classifies thrown errors by regex matching 'simulate' or 'mock' to choose 400 versus 500. services/uta/src/domain/trading/git/TradingGit.ts:753-879: simulatePriceChange uses Decimal arithmetic, supports `symbol=all` by scaling each position's own mark (including derivatives), excludes derivative rows from symbol-level underlying changes, and returns currentState/simulatedState/summary projections. The source problem is Input is only checked as Array, the route reaches a legacy UTA method, and error category is inferred from text, contrary to typed protocol/error channels. and the current target is Price simulation is an AccountScope-bound finite valuation-preview capability over current account exposure. Its input/output schemas carry explicit AccountScope, canonical InstrumentId, hypothetical Price observations, Decimal values, uncertainty, and the source-declared scenario behavior; it is read-only and not a broker mutation or order approval route. The migrated leaf retains the source's `symbol=all` own-mark scaling (including derivative rows) and its symbol-level underlying rule that excludes derivative rows, rather than generalizing either rule across providers. which preserves 保留 AI PnL exploration endpoint intent。; MockBroker-specific unsupported result remains clean 4xx typed, real broker query errors remain distinct.. Its boundary is Market/accounting application computes the AccountScope-bound preview from projections and declared market facts; HTTP maps typed failures and never uses provider error text/regex to choose authorization or mutates broker mark prices. and its entry-specific falsifier is For one AccountScope, `symbol=all` applies each position's own mark, including derivative rows, while a symbol-level underlying change excludes derivative rows; the result returns deterministic exact `current`/`simulated`/`summary` deltas; malformed changes fail before provider state changes, and a provider without the preview leaf is absent/unavailable rather than regex-classified.. Repair applied: simulation now binds explicit AccountScope and preserves the source’s symbol=all own-mark rule (including derivatives) versus symbol-level derivative exclusion, with named result products. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-4EFAFBF364",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-4EFAFBF364. Source fact: services/uta/src/http/routes-trading.ts:261-267: Resolves an account then calls account.listSubAccounts through queryAccount, returning raw sub-account values and health-derived ad hoc errors. The source problem is The route calls the legacy account object directly and does not provide named sub-account/query schemas or capability context. and the current target is Sub-account discovery is a scoped data capability with output Discovering | Available(scopes, revision) | Unavailable(reason). Even a one-wallet provider declares its explicit scope; discovery does not imply an order transaction. The selected discovery query is bound to explicit AccountScope and never synthesizes a default wallet. which preserves 保留 ordinary broker one-wallet listing 与 CCXT multiple wallet listing。; 保留 explicit `subAccountId` query use case。. Its boundary is Account-query reads provider directory/cache and returns the selected scope-discovery leaf; transport does not call an account class directly. and its entry-specific falsifier is Single-wallet output contains an explicit scope; pending discovery remains Discovering and does not authorize an effect; unknown wallet never silently resolves to the aggregate account.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-87FA16A102",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-87FA16A102. Source fact: services/uta/src/http/routes-trading.ts:269-274: Resolves a UTA and directly calls getAccount, optionally passing an unvalidated subAccountId query string; queryAccount serializes the native account result. The source problem is The handler exposes a legacy broker/account return object and primitive query id directly; it is not a typed projection query at the transport boundary. and the current target is Account state is a finite pull capability returning a SnapshotViewEnvelope over the provider-declared AccountSnapshot schema. Explicit AccountScope versus an aggregate view is a local query choice; native account fields remain adapter-specific extensions. which preserves 保留 omission=subaccount aggregate 与 explicit wallet query 的 distinction。; 保留 native account fields 的业务内容，但 normalized values。. Its boundary is Account-query/application projection reads the provider observation; broker refresh is a separate declared resource capability, and HTTP only encodes the envelope. and its entry-specific falsifier is Explicit and aggregate requests produce distinct scoped envelopes; stale observations are marked, not empty; no SDK AccountInfo object or implicit wallet default appears on the wire.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-D250B2BDC3",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-D250B2BDC3. Source fact: services/uta/src/http/routes-trading.ts:276-281: Calls account.getPositions with an optional raw subAccountId and returns {positions: ...} through queryAccount, exposing broker/account-native position values. The source problem is The route directly queries the legacy account/broker object and serializes the result without target projection, observation, or domain-value schemas. and the current target is Positions are a finite data projection over the provider-declared Position/Candle-like semantic unit: AccountScope, InstrumentId, Quantity, units/currency/multiplier when evidenced, and observation provenance. Spot/perp and provider extensions remain distinct. which preserves 保留 `{positions}` response shape concept and optional wallet filtering。; 保留 Decimal quantity/market values; no float conversion。. Its boundary is Account/market accounting application reads typed observations; adapters decode native positions and extensions. HTTP does not call getPositions or serialize broker-native values directly. and its entry-specific falsifier is Same display symbol on spot/perp remains distinct; partial observation is Partial/Unknown rather than empty positions, and quantity/money strings retain precision.. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-8239373D6C",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-8239373D6C. Source fact: services/uta/src/http/routes-trading.ts:283-293: Accepts comma-separated raw ids or derives pending ids from account.getPendingOrderIds, calls account.getOrders directly, and returns raw order objects. The source problem is Raw comma-separated ids and direct pending-order calls bypass typed identifiers, projection authority, preconditions, and shared response schemas. and the current target is Orders query is a finite pull leaf over a provider-declared order-observation schema. Typed BrokerOrderId filters and cursor/coverage are local to that leaf; an absent order-list leaf means no route, not a full handler returning Unsupported. Every account-backed order listing is bound to explicit AccountScope. which preserves 保留 optional ids filter and default pending-order view.; 保留 raw order details only through normalized OrderSnapshot.. Its boundary is ProjectionReader or a declared observation-refresh capability supplies order rows; HTTP does not call getPendingOrderIds/getOrders or own provider native identities. and its entry-specific falsifier is A comma-bearing or long id remains one text identity under the declared codec; partial listing is named; empty Complete requires explicit coverage; GET never mutates orders or invokes approval.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-D3212B77F2",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-D3212B77F2. Source fact: services/uta/src/http/routes-trading.ts:295-300: Calls account.getMarketClock directly and serializes its account-scoped result through queryAccount. The source problem is The target explicitly says market clock is not an account-wide boolean, while this route delegates to an account-wide getMarketClock with no venue/instrument/session dimensions. and the current target is Market session is a venue/calendar data capability with explicit Instrument/venue/session dimensions and an Open | Closed | Unknown | NextWindow observation. It is not an account boolean and does not imply transaction approval. Scope is explicit Public or Account(AccountScope) when the capability is account-backed; no route invents a default account or subaccount. which preserves 保留 market clock query surface for callers needing session info。; 保留 closed/open distinction, but make dimensions explicit.. Its boundary is Market/calendar application invokes the selected provider/calendar leaf; HTTP does not call account.getMarketClock or convert a broker boolean. Public session queries do not receive an invented account scope. and its entry-specific falsifier is Two instruments/venues can return distinct states; missing calendar remains Unknown/Unavailable, and a stale session cannot authorize dispatch by itself.. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-5FBCD3EE4E",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-5FBCD3EE4E. Source fact: services/uta/src/http/routes-trading.ts:302-315: Preserves a legacy symbol path, constructs an @traderalice/ibkr Contract in the HTTP handler, sets symbol, and calls account.getQuote directly through queryAccount. The source problem is The transport constructs and passes a concrete broker SDK object and exposes a legacy symbol-only query, violating the prohibition on broker SDK values in public protocol/transport. and the current target is Legacy symbol quote GET is an alias projection into the selected provider quote leaf. The public input resolves to Public or Account(AccountScope) plus InstrumentId; native Contract construction and provider extensions remain adapter-local, and quote remains finite data. Scope is explicit Public or Account(AccountScope) when the capability is account-backed; no route invents a default account or subaccount. which preserves 保留 path `:symbol` compatibility during caller migration via explicit symbol->InstrumentId resolver.; 保留 source account routing.. Its boundary is Application market query selects the provider quote capability and adapter builds the native request; transport never imports @traderalice/ibkr. and its entry-specific falsifier is AAPL resolves only within the selected account/venue; the response contains typed decimal quote fields and no raw SDK object, while an unknown alias fails before provider access.. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-8456875518",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-8456875518. Source fact: services/uta/src/http/routes-trading.ts:317-328: Accepts arbitrary JSON, Object.assigns it into a native IBKR Contract, calls account.getQuote, and returns the native result or a string 500 error. The source problem is Arbitrary caller fields become broker SDK state through Object.assign and the raw broker result crosses HTTP; no request/response runtime schema or typed capability/error mapping exists. and the current target is Rich quote POST is a provider-declared quote input leaf: exact InstrumentRef plus provider extension fields, strict unknown-field handling, and a declared QuoteObservation/error output. It is not a raw native Contract endpoint. Scope is explicit Public or Account(AccountScope) when the capability is account-backed; no route invents a default account or subaccount. which preserves 保留 POST quote for clients holding richer instrument refs.; 保留 broker-specific error visibility via structured code.. Its boundary is The selected provider adapter constructs/decodes native Contract values; HTTP invokes only the application quote port and maps its typed result. and its entry-specific falsifier is An extra native field is rejected if not declared; a valid provider extension survives round-trip; provider rejection is typed and cannot be inferred from HTTP string/error regex.. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-861915C783",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-861915C783. Source fact: services/uta/src/http/routes-trading.ts:330-341: Accepts arbitrary body fields, stringifies aliceId, passes unknown filters to account.expandContract, and returns the raw expansion or a string 500 error. The source problem is The route accepts unknown filters and legacy aliceId text, then returns a native/domain expansion object without schema or jurisdiction/capability validation. and the current target is Contract expansion is a provider-declared data capability with typed hub InstrumentId input with Public or Account(AccountScope) only when declared and exact filter extension. Its output is a local Leaves | Directory | Grid | Partial(cursor) union only where that provider declares those delivery variants. An undeclared filter or delivery variant is omitted from the selected leaf schema and does not remove the expansion leaf; a provider without the expansion capability has no route, while a declared but unavailable leaf reports availability separately. Scope is explicit Public or Account(AccountScope) when the capability is account-backed; no route invents a default account or subaccount. which preserves 保留 hub→leaves expansion for bonds/options/futures.; 保留 `aliceId` compatibility only as decoded canonical alias, not as free string.. Its boundary is Instrument-query application invokes the selected provider expansion adapter; HTTP does not stringify arbitrary Alice ids or pass unknown filters. and its entry-specific falsifier is A declared directory result carries continuation and a leaf result carries canonical InstrumentId; a filter or delivery variant not declared by the selected expansion leaf is absent from its schema and an attempted field is rejected, while a provider without any expansion leaf has no route and a declared but unavailable leaf returns named availability rather than an empty object.. Repair applied: expansion filter/delivery variants are omitted only from the selected schema, while whole-leaf absence and declared-leaf unavailability remain distinct. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-3C6243143F",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-3C6243143F. Source fact: services/uta/src/http/routes-trading.ts:343-361: Accepts arbitrary contract/params JSON, constructs an IBKR Contract, revives start/end dates imperatively, calls account.getHistorical, and returns raw bars or a string 500 error. The source problem is Broker-native Contract construction and permissive params cross the transport boundary, and date revival is not a shared runtime schema; raw adapter values are exposed. and the current target is Historical bars are a finite pull capability over declared Candle<Extension> output. Input interval/time/what-to-show fields and provider-native Contract/date forms are exact leaf schemas; bars carry quality, cursor, freshness, and gap/finality evidence. Scope is explicit Public or Account(AccountScope) when the capability is account-backed; no route invents a default account or subaccount. which preserves 保留 start/end ISO input and `{bars}` conceptual response.; 保留 interval/whatToShow values only where capability evidence supports them.. Its boundary is Market-data application calls the selected provider historical adapter; HTTP does not revive Date or construct a Contract. Public bars remain public unless the provider schema explicitly binds an AccountScope. A future push bars leaf uses stream lifecycle/backpressure, not transaction control. and its entry-specific falsifier is ISO times decode deterministically; malformed dates fail before provider invocation; truncation carries cursor/Partial; no JS Date or raw native bar crosses the wire.. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-2F790BE2E6",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-2F790BE2E6. Source fact: services/uta/src/http/routes-trading.ts:363-378: Builds an IBKR Contract from arbitrary body JSON, calls account.getContractDetails, returns a raw detail object/null, and maps all errors to a string 500. The source problem is The handler turns arbitrary JSON into a native SDK object and exposes raw broker details, which the target transport/protocol boundary forbids. and the current target is Contract details is a provider-declared finite pull with Public/Account scope only when its schema requires it with normalized InstrumentDetails and a Found(details) | NotFound result only if that provider declares it. Native null/detail values are decoded at the adapter and never become raw HTTP output. Scope is explicit Public or Account(AccountScope) when the capability is account-backed; no route invents a default account or subaccount. which preserves 保留 details drilldown 的 found/not-found distinction, expressed as a typed response variant rather than raw null。; 保留 aliceId/symbol/secType/currency filters through typed query.. Its boundary is Instrument application/adapter owns native Contract creation, provider response decoding, and extension preservation; transport maps typed availability/errors only. Public details are not assigned a default account. and its entry-specific falsifier is A valid AAPL response is Found with normalized identity and declared derivative extension; missing contract is NotFound, unknown native secType is decode failure, and no raw Contract crosses the boundary.. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-FC80F364B6",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-FC80F364B6. Source fact: services/uta/src/http/routes-trading.ts:380-388: Reads a legacy TradingGit log directly from UnifiedTradingAccount, with ad hoc numeric limit/symbol query parsing, and returns commit records. The source problem is The route reads UTA-owned TradingGit state directly, while target Git is rebuildable human-readable projection and SQLite journal is authoritative. and the current target is Wallet log is a finite AccountScope-bound audit/projection capability over journal and provider observations. Its query schema has explicit scope, bounded pagination/filter fields; Git commit metadata is an optional display extension, never transaction authority. which preserves 保留 log limit/symbol browsing；保留 human-readable commit/audit metadata 作为 projection。; 不再让 Git records authorize execution。. Its boundary is AuditQuery application reads journal/projection stores; HTTP does not invoke TradingGit or UnifiedTradingAccount and does not expose mutable staging. and its entry-specific falsifier is Rebuilding from the same journal yields the same page/sequence; missing storage is typed unavailable rather than empty success, and Git absence does not erase journal-addressable audit events.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-F42728FD63",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-F42728FD63. Source fact: services/uta/src/http/routes-trading.ts:390-399: Projects lifecycle-collapsed order rows from uta.exportGitState().commits using domain order-history logic and returns them as an exchange-frontend view. The source problem is Current order history treats Git commits as its source, contrary to the target journal authority and rebuildable projection contract. and the current target is Order history is an explicit AccountScope-bound, rebuildable projection capability over declared order observations and, where present, transaction-effect events. It may expose a lifecycle row with provider extensions and typed status, but it does not require a global action set or infer fills from Git. which preserves 保留一行 per order lifecycle collapsed exchange-front-end view。; 保留 limit parameter and order source labels, expanded to include prepared/unknown semantics.. Its boundary is Projection application combines selected provider observations and journal evidence; HTTP reads the named output schema, while Git hash is at most an audit correlation. and its entry-specific falsifier is Accepted-but-working is not Filled; partial fills preserve cumulative/delta correctness; projection rebuild is sequence-equivalent and no Git commit success creates a fill.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-F589132F03",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-F589132F03. Source fact: services/uta/src/http/routes-trading.ts:401-406: Projects fill-only trade rows from exported Git commits and returns them as a raw trade list. The source problem is The route uses Git commit export as history authority and returns untyped records, with no broker-observed execution/reconciliation or accounting projection boundary. and the current target is Trade history is a finite projection of declared fill/external-observation records with exact AccountScope/instrument/quantity/price/money and an explicit provenance variant. It emits only evidenced fills, never a generic transaction row from Git. which preserves 保留 fill-only trade history and source labels order/external/reconcile。; 保留 limit/read-only route; remove Git authority.. Its boundary is Accounting/projection application owns normalization and provenance; HTTP exposes the selected output schema and optional Git audit reference only. and its entry-specific falsifier is An accepted order with no observed fill produces no trade row; duplicate execution identity produces one row; an external trade is tagged external with observation provenance and exact values.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-B9C114CCA4",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-B9C114CCA4. Source fact: services/uta/src/http/routes-trading.ts:408-414: Looks up a Git commit by hash through uta.show and returns the commit or a 404 string error. The source problem is The route treats a Git hash as the lookup authority, while target Git hashes are not transaction identifiers and Git persistence is not recovery source. and the current target is Audit entry lookup is an explicit AccountScope-bound finite projection query by a declared AuditRef (projection id, journal sequence, or optional historical Git correlation). TransactionId/DispatchId remain separate identities and no lookup authorizes an effect. which preserves 保留 UI deep-link to a human-readable historical entry where possible.; 保留 missing entry 404 semantics.. Its boundary is AuditQuery reads the rebuildable projection/journal page; transport does not call `uta.show` or retrieve mutable Git state as authority. and its entry-specific falsifier is An unknown hash/reference returns typed NotFound; a journal event remains addressable after projection rebuild even if Git is unavailable, and lookup causes no transaction state change.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-AC8C619E99",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-AC8C619E99. Source fact: services/uta/src/http/routes-trading.ts:416-420: Returns uta.status(), exposing pending Git message/staging/head state as the wallet status response. The source problem is The current route exposes live TradingGit authority. It cannot be deleted while UTAAccountSDK.status remains public, but it must not authorize execution. and the current target is Wallet status is an explicit AccountScope-bound read projection capability whose output is selected from the provider/effect descriptors: data resource health and, for a transaction-wrapped effect, Draft/Prepared/Review/Queued/Executing/Reconciling states. Mutable Git staging is never authority. which preserves 保留 current SDK status() during migration as read-only view.; 保留 UI can show staged/pending/recovery information, with renamed semantics.. Its boundary is ProjectionReader/application query serves status; HTTP does not call UTA.status or write Git. Transaction state is read from the selected wrapper, while data leaf status stays resource-scoped. and its entry-specific falsifier is A draft/effect status shows its revision and cannot be submitted by reading the status; a stale/RecoveryRequired view is explicit, and a data-only provider has no wallet status route.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-F454EFC3B2",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-F454EFC3B2. Source fact: services/uta/src/http/routes-trading.ts:422-441: Validates a nonempty message, calls uta.commit synchronously to finalize staged Git operations, and returns the prepared Git commit or a generic 500 string error. The source problem is wallet/commit 只校验 message 后同步调用 uta.commit 并返回 Git commit；它把 preparation、审计文本和 Git persistence 混成一个结果，且没有 serializable plan 或 commit-after-approval barrier。 and the current target is Commit is replaced by a prepare capability available only on a selected controlled-effect wrapper. Its input/output schemas carry subject/revision/Principal and a serializable PreparedPlanRecord; audit rationale is separate and there is no universal commit route. This account-backed prepare capability is always bound to explicit AccountScope; no default subaccount is synthesized. which preserves 保留 current commit as a validation/preparation boundary.; 保留 hash-like digest for optimistic review, but make digest canonical plan identity.. Its boundary is Transaction application gathers facts, then the selected writer atomically records preparation state; provider-native compilation occurs at the adapter boundary and no broker dispatch occurs during preparation. and its entry-specific falsifier is Prepare success returns one serializable plan/digest with provider state untouched; insufficient facts stay Draft/typed failure; reload after writer commit returns the same preparation identity.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-6BF94B5092",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-6BF94B5092. Source fact: services/uta/src/http/routes-trading.ts:443-469: Requires pending Git state and an expectedPendingHash, optionally accepts a reason, calls uta.reject, and maps hash conflicts to a 409 code while all other errors become 500 strings. The source problem is The route authorizes rejection with a process-local Git hash and returns string errors. It has no shared runtime schema or typed transaction-state conflict; revision/actor are not fields of the canonical Reject command. and the current target is Reject is a command on a selected transaction wrapper’s review capability, with exact transaction/revision/review identity and optional reason. It can retire an unsent plan only after CAS; it is not a universal broker handler or Git hash operation. This account-backed review command carries explicit AccountScope; no default subaccount is synthesized. which preserves 保留 optional human reason and user-rejected audit projection。; 保留 stale optimistic guard returns conflict rather than silently accepting.. Its boundary is Application loads the selected durable effect state and calls its writer; no TradingGit.reject or provider dispatch occurs. Data-only leaves have no reject route. and its entry-specific falsifier is An unstarted prepared effect retires with no provider mutation and one durable result; a dispatched/unknown effect returns RecoveryRequired with retained evidence; a data leaf has no reject operation.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-7B9EA96B62",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-7B9EA96B62. Source fact: services/uta/src/http/routes-trading.ts:471-496: Requires pending Git state and expectedPendingHash, calls uta.push to execute staged operations, maps PendingHashConflictError to 409, and exposes push result or string 500. The source problem is wallet/push 依赖 pending Git state/hash 后直接调用 uta.push，且把异常变成 409/500；它没有 durable DispatchPlanned/DispatchStarted，也没有区分人工 approval 与受授权的 AI auto-approval。 and the current target is Approve is the selected transaction wrapper’s explicit review/authorization leaf. It binds effect revision/digest/expiry, Principal and server policy, then returns a queued durable receipt; Human or authorized-auto policy is derived server-side and is not a body field. This account-backed approval command carries explicit AccountScope; no default subaccount is synthesized. which preserves 保留 human approval wall when AI trading is disabled；保留 source tool 的 enabled path as an explicit service-authorized auto-approval mode, replacing Git hash with plan binding。; 无论 mode，approval 仍先写 durable plan/jobs，Ack ≠ fill。. Its boundary is The wrapper writer atomically records approval, dispatch plan, jobs, locks, and receipt; scheduler/provider interpreter later performs the selected effect. No approval route invokes a data reader or universal broker method. and its entry-specific falsifier is Disabled policy remains AwaitingReview; authorized policy queues exactly one receipt after binding checks; body allow-AI cannot self-authorize; a post-DispatchStarted disconnect becomes Unknown/observe rather than duplicate dispatch.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-58D4065C2F",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-58D4065C2F. Source fact: services/uta/src/http/routes-trading.ts:498-516: Stage-only route accepts arbitrary JSON and calls uta.stagePlaceOrder directly, returning TradingGit.add-like result or a string 400 error. The source problem is stage-place-order 接受任意 JSON 并直接执行 TradingGit.add-like staging；draft 的完整 OrderSpec、scope/revision 和 no-job-before-approval invariant 因而不在 authority 中。 and the current target is Stage-place-order becomes a draft capability for a provider-declared Order semantic unit, optionally wrapped for controlled effects. Its input is a composed schema with declared provider extensions and local sizing/terms constraints; no universal order variant list is imposed. An account-backed draft carries explicit AccountScope; a public data leaf has no draft state. which preserves 保留 stage-only agent ergonomics（draft now, review/approve later）作为选定受控 effect 的可选能力。; 保留 provider schema 已声明的订单字段与精度，不把一个 provider 的扩展强加给其他 provider。. Its boundary is HTTP/application writes only the selected wrapper’s durable draft via its writer; provider-native compilation and availability are deferred to that leaf’s prepare/interpreter. and its entry-specific falsifier is A declared draft preserves provider terms and canonical decimals; invalid relation/unknown field produces no event; repeated command identity converges to one draft receipt and no provider mutation.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-9B42BF213C",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-9B42BF213C. Source fact: services/uta/src/http/routes-trading.ts:518-528: Stage-only route accepts arbitrary JSON and directly calls uta.stageModifyOrder, returning a legacy Git add result or a string 400 error. The source problem is stage-modify-order 把任意 body 转给 legacy stageModifyOrder；它没有目标订单的 ScopedOrderRef/version，也不能表示 native amend 与 cancel-replace 的语义损失。 and the current target is Modify draft is available only when the provider declares a modify-capable Order effect. Its schema carries a scoped provider order identity, current revision/before-image, and an exact replacement/patch form declared by that leaf; no Partial<Order> universal contract. The selected amend target also carries explicit AccountScope; no default subaccount is synthesized. which preserves 保留 modify endpoint intent but require complete semantics and target identity.; 保留 broker identity/queue-loss risk as explicit outcome, not fake success.. Its boundary is Application gathers the selected provider observation and submits a draft; adapter compiles native amend/cancel-replace according to its declared capability and reports semantic-loss evidence. and its entry-specific falsifier is Stale identity cannot prepare; a declared native amend preserves identity evidence; a cancel-replace provider reports semantic loss/unknown risk, and an undeclared modify capability has no route.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-98CE02C334",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-98CE02C334. Source fact: services/uta/src/http/routes-trading.ts:530-540: Stage-only route accepts arbitrary JSON and directly calls uta.stageClosePosition, returning a legacy Git add result or a string 400 error. The source problem is stage-close-position 直接把任意 body 交给 stageClosePosition；qty 缺省同时承载 close-all 和缺字段两种含义，且没有 exposure before-image 或准备期重检。 and the current target is Close-position draft is a provider-declared Order effect leaf when supported. Quantity|All is a small local union only if that leaf declares it; scope, InstrumentId, exposure revision, and before-image are explicit, while no transaction state is imposed on data readers. The selected close effect carries explicit AccountScope; no default subaccount is synthesized. which preserves 保留 explicit qty precision and close-all semantics.; 保留 close as risk-reducing action only if policy explicitly grants exemption.. Its boundary is Application/accounting gathers exposure for the selected effect; its writer prepares durable state and the provider interpreter dispatches only after approval. A provider without close has no close leaf. and its entry-specific falsifier is Explicit .001 remains exact; All on exposure 1.5 persists 1.5 before approval; exposure drift blocks/requires reprepare and an undeclared provider does not expose close.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-D5B92C2A08",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-D5B92C2A08. Source fact: services/uta/src/http/routes-trading.ts:542-555: Stage-only cancel route manually checks that orderId is a string, calls uta.stageCancelOrder, and returns a legacy Git add result or string 400 error. The source problem is stage-cancel-order 只检查 orderId 是 string 后就写入 staging；它没有 ListingComplete、order version/fills before-image 或与 simulator force-cancel 的边界。 and the current target is Cancel draft exists only for a provider-declared cancel-capable Order effect. It uses a scoped provider identity/version and any declared status/fill preconditions; simulator force-cancel is a separate simulator leaf and cannot fill this route. The selected cancel effect carries explicit AccountScope; no default subaccount is synthesized. which preserves 保留 orderId validation and stage-before-approval flow.; Preserve late fill/identity loss as typed observation/compensation risk.. Its boundary is Selected application effect prepares the cancel and provider adapter interprets it after approval; HTTP performs no force-cancel or native call. and its entry-specific falsifier is Empty id/stale version fails without a writer event; an accepted cancel yields a queued effect receipt, and later Ack does not imply terminal cancellation when a late fill is observed.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-52CB0F382D",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-52CB0F382D. Source fact: services/uta/src/http/routes-trading.ts:557-576: One-shot handler validates the legacy place-order bag, strips message, calls uta.stagePlaceOrder, then runOneShot performs commit and push in one HTTP request. The source problem is one-shot place-order 在同一请求中 stage、commit、push；因此输入请求隐式获得 venue mutation 权限，prepare/approval 前无法证明 broker ledger 为空。 and the current target is The legacy one-shot place route is not an execution contract. Replace it only where a provider declares an Order effect and transaction wrapper: draft -> prepare -> review/approve -> durable dispatch/recovery. Providers without that capability expose no one-shot route. Any account-backed one-shot migration request carries explicit AccountScope; data leaves remain scope-appropriate and transaction-free. which preserves 保留 existing user-facing order entry intent via multi-step API, not single roundtrip.; 保留 precision and full OrderSpec.. Its boundary is HTTP may submit a draft/prepare request; only the selected transaction wrapper writer creates dispatch work and scheduler/provider interpreter executes it. A data leaf remains pull/push data delivery. and its entry-specific falsifier is Draft/prepare leave provider ledger untouched; only explicit human or server-authorized policy approval queues a job; a post-dispatch crash yields one identity plus observe/recovery, never an implicit second order.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-32AD0C3DCD",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-32AD0C3DCD. Source fact: services/uta/src/http/routes-trading.ts:578-592: One-shot close handler validates the legacy close body, strips message, preserves qty as a string, then stages/commits/pushes through runOneShot. The source problem is one-shot close-position 把 string qty（或缺省 qty）带过 stage→commit→push；执行前的 All resolution、exposure drift 与 partial-fill recovery 均被旧 Git pipeline 隐藏。 and the current target is Legacy one-shot close is replaced by a declared close-effect flow with explicit Quantity|All, scoped exposure evidence, prepare/review/approve, and provider-specific outcome schema. No provider close capability means no route; no data route receives effect state. The close-effect flow carries explicit AccountScope; no default subaccount is synthesized. which preserves 保留 qty string precision and no-qty close-all behavior.; Preserve target distinction between close and place SELL (risk/authorization).. Its boundary is Accounting/application prepares the selected effect and its writer/scheduler/provider interpreter handle durable execution; HTTP returns effect state/receipt, not a Git push result. and its entry-specific falsifier is All captures current exposure at prepare; a pre-approval external fill forces reprepare; an accepted close with partial/unknown outcome remains explicit until observation proves the declared criterion.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-AC5E619E59",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-AC5E619E59. Source fact: services/uta/src/http/routes-trading.ts:594-606: One-shot cancel validates orderId/message, stages cancellation, and immediately commits/pushes through the Git pipeline. The source problem is one-shot cancel 校验 orderId/message 后立即 stage、commit、push；并发 fill、order version 和 Unknown outcome 没有 durable observation 边界。 and the current target is Legacy one-shot cancel is removed as authority. A provider-declared cancel effect uses its own input/precondition schema and optional withTransaction wrapper; immediate Git push and simulator force-cancel are structurally absent from this capability. The selected cancel effect carries explicit AccountScope; no default subaccount is synthesized. which preserves 保留 cancellation caller endpoint intent and order identity precision.; Do not silently turn cancel into success when venue rejects/race fills.. Its boundary is The selected wrapper writer creates approval/job identity; scheduler/provider adapter dispatches and observes late fills. HTTP only encodes the durable state. and its entry-specific falsifier is An approved working order yields one queued job; a race fill is retained as late observation rather than falsely cancelled; duplicate command identity converges without a second dispatch.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-4D35581185",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-4D35581185. Source fact: services/uta/src/http/routes-trading.ts:608-630: GET snapshots reads an optional file-backed SnapshotService and silently returns an empty list on missing service/errors; DELETE decodes a timestamp and directly deletes a snapshot, returning simple success/not-found responses. The source problem is The route delegates to the current file-backed snapshot service, hides all read/delete failures as empty/simple responses, and has no shared schemas or projection checkpoint semantics. and the current target is Snapshots are a finite data pull capability over SnapshotViewEnvelope and a separate authorized retention capability. Snapshot schema carries scope/time/schemaVersion/sourceSequence/freshness; retention is not an order approval/compensation flow and file JSONL is not authority. Account snapshots and retention requests carry explicit AccountScope; public snapshots remain Public only if declared. which preserves 保留 per-account recent snapshots query and optional limit.; 保留 not-found delete response, but typed; preserve auditability.. Its boundary is Projection service owns typed snapshot reads; a retention application port performs authorized deletion/audit. HTTP only verifies Principal, decodes the selected leaf, and encodes results; no direct file surgery. and its entry-specific falsifier is Malformed timestamp/limit fails before store access; missing storage is Unavailable; stale/partial data remains visible; authorized retention changes only requested scope/time and records audit, unauthorized retention changes nothing.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-8F1E449867",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-8F1E449867. Source fact: services/uta/src/http/routes-trading.ts:632-694: Loads per-account snapshots, groups timestamps by minute, sums Number-converted net liquidation values, carries forward missing accounts, and silently returns {points:[]} on any failure. The source problem is Transport performs aggregation over file-backed snapshots, coerces financial values to JavaScript numbers, mutates intermediate records for carry-forward, and hides projection/storage failures as empty data. and the current target is Equity curve is a finite accounting/data projection capability using exact Money/Decimal, explicit bucket/carry-forward policy, scope rows, FX provenance, and coherence. It does not use order locks, approval, or compensation. Curve rows retain explicit AccountScope and never synthesize a default wallet. which preserves 保留 minute grouping, chronological sorting, per-account values and carried values where policy permits.; Preserve empty curve only when Complete query has no observations, not on any failure.. Its boundary is Accounting/projection service computes the curve from declared snapshot/FX data leaves; HTTP reads/encodes the result and never performs live broker aggregation. and its entry-specific falsifier is Two snapshots in one declared bucket sum exactly; a missing account carries only within max age and is marked stale; FX/store failure yields Partial/Unavailable rather than rate=1, empty points, or uncaught success.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-248B2B5279",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-248B2B5279. Source fact: services/uta/src/http/simulator.spec.ts:1-12: Documents and imports an end-to-end HTTP adapter spec intended to use a real MockBroker, explicitly checking route wiring and markPrice-to-position behavior rather than vi.fn broker stubs. The source problem is simulator.spec 声明用真实 MockBroker 验证 HTTP wiring 与 markPrice-to-position；它只覆盖一条进程内 vertical slice，不能证明认证、fixture journal 或 production transaction 隔离。 and the current target is The simulator integration spec is a provider-specific vertical slice: real MockBroker interpreter behind a typed simulator-control leaf, shared transport codec, explicit Principal/capability, and no claim that this proves other provider trees or transaction runtime. The simulator scenario binds its control and observation to explicit AccountScope. which preserves 保留 real MockBroker rather than vi.fn for matching behavior.; 保留 markPrice-to-position assertion as evidence.. Its boundary is Test composition supplies the real mock interpreter and simulator application port; HTTP request/response codecs run end-to-end within the fixture. and its entry-specific falsifier is An authorized mark-price scenario changes the real mock state exactly as its declared rule says, while the test records no production order approval/job and does not treat HTTP status as dispatch proof.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-2B0B0BF616",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-2B0B0BF616. Source fact: services/uta/src/http/simulator.spec.ts:13-31: Creates a fake UTA map with MockBroker or arbitrary object values, exposes get/resolve/listUTAs manager stubs, and casts the context to UTAEngineContext. The source problem is simulator 测试用任意 object 充当 broker 并 cast UTAEngineContext；错误 capability、scope 或非 simulator 实例可在请求前被类型系统放过。 and the current target is Simulator test context is a typed registry of declared simulator capability instances plus application ports/principal. It rejects arbitrary objects masquerading as provider handles and does not model a complete UTAEngineContext. Simulator fixture configuration carries explicit AccountScope rather than an implicit wallet. which preserves 保留 one mock + one non-mock listing scenarios.; 保留 per-test isolation.. Its boundary is Test Layer hides MockBroker behind the simulator SPI and supplies the selected capability; non-simulator providers are represented by their own declared descriptors, not casts. and its entry-specific falsifier is A non-Mock object cannot install as a simulator capability; a real provider remains excluded without pretending its handler exists, and wrong scope/principal fails before request execution.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-DB305A9D5D",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-DB305A9D5D. Source fact: services/uta/src/http/simulator.spec.ts:33-42: Builds Hono Request objects with optional JSON body, invokes the route app, parses any non-204 response as JSON, and returns status/body pairs. The source problem is 测试 helper 直接构造 Hono Request、读取任意 JSON status/body；它没有统一 credential、protocol decoder 或 mutation 后 durable receipt 的观察路径。 and the current target is HTTP test helper is an outer protocol harness: it builds authenticated requests, decodes each selected capability’s success/error schema, and supports finite pull, no-content, and declared push lifecycle. It does not choose a universal response JSON shape. When the selected capability is account-backed, the helper carries explicit AccountScope in the decoded request. which preserves 保留 optional JSON body and 204 handling.; 保留 direct Hono request convenience.. Its boundary is The helper drives the actual Hono transport while application/resource services are supplied by the fixture; credential/Principal is provisioned by the test boundary, never by request body actor fields. and its entry-specific falsifier is Missing credential fails before service; malformed provider response fails decoding; a valid 204 is NoContent; a selected push closes/cancels its resource without cancelling unrelated durable effects.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-793589DE30",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-793589DE30. Source fact: services/uta/src/http/simulator.spec.ts:46-56: Creates one real MockBroker and one non-Mock object, verifies GET /utas returns 200 and includes only the simulator id. The source problem is GET /utas 测试只断言 real MockBroker 被列出而 arbitrary object 被排除；它未验证 simulator admin authorization、generation/coverage，且可能与生产 account listing 混用。 and the current target is The simulator listing test targets a finite simulator discovery leaf with Complete coverage only for the declared registry namespace; production AccountList is a separate capability. Mock inclusion is metadata/capability evidence, not an instanceof-based universal rule. Each listed simulator identity carries explicit AccountScope and is checked against that scope authorization. which preserves one real MockBroker listed, arbitrary non-Mock excluded, HTTP 200 only with authorized principal.. Its boundary is The test invokes typed simulator-control discovery through application composition, not direct manager enumeration; real providers and keyless data instances remain outside this leaf. and its entry-specific falsifier is One authorized Mock simulator appears with scoped metadata; a real provider is absent without leakage, and missing admin capability yields typed 401/403 with no mutation.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-3C4CDD1C56",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-3C4CDD1C56. Source fact: services/uta/src/http/simulator.spec.ts:58-86: Verifies a full raw MockBroker snapshot after setting a mark price, 404 for unknown ids, and 400 when the selected broker is not a simulator. The source problem is state 测试序列化 raw MockBroker snapshot 并覆盖 404/non-simulator；没有约束 versioned simulator projection、freshness 或原生对象不得穿过 wire。 and the current target is Simulator state test consumes the simulator leaf’s versioned state output: exact cash/price/position/pending schemas, generation/as-of, and typed NotFound/NotSimulator/Unavailable variants. It is not production AccountState or transaction status. State lookup is explicit AccountScope-bound. which preserves 保留 full snapshot assertion cash=50000, mark BTC=80000; 404 unknown and 400 non-sim behavior.. Its boundary is Typed simulator application query reads the real mock interpreter/projection; HTTP does not serialize a raw MockBroker snapshot. and its entry-specific falsifier is Mark 80000 appears as exact text; unknown/non-simulator branches return named errors; no Contract/Order class crosses wire and stale generation is not accepted as current.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-9DB36046FA",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-9DB36046FA. Source fact: services/uta/src/http/simulator.spec.ts:88-128: Uses a real MockBroker to place a limit order, verifies mark-price updates leave it pending then auto-fill it at the trigger price and update positions, and verifies malformed body returns 400. The source problem is mark-price 测试通过真实 MockBroker 验证 80k pending 到 79k fill；它没有把 SetMarkPrice/FillObservation identity、replay 和 admin capability 纳入 durable fixture。 and the current target is Mark-price integration remains a simulator control scenario over the declared SetMarkPrice leaf and FillObservation output. Its deterministic pending->filled rule is fixture evidence, not proof of provider order idempotency or durable transaction dispatch. The control scenario is explicit AccountScope-bound. which preserves 保留 80k pending then 79k auto-fill, filled id, pending=0, position=1.; 保留 wrongField 400 intent.. Its boundary is The real MockBroker interpreter is invoked through simulator application control; production order effects and provider observations are not inferred from this route. and its entry-specific falsifier is Initial 80000 leaves BUY@79000 pending; 79000 produces one fill and position; replay does not duplicate; malformed body leaves state unchanged and no effect receipt is created.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-1BDBE358B1",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-1BDBE358B1. Source fact: services/uta/src/http/simulator.spec.ts:130-150: Calls the simulator endpoint with a wallet-source deposit, then checks MockBroker position quantity, wallet avg-cost source, and mark-price placeholder cost. The source problem is external-deposit 测试只观察进程内 position 的 wallet cost source/mark placeholder；没有 observation identity、dedup、MissingMark/Indeterminate 或 accounting projection 证据。 and the current target is External-deposit test exercises the declared simulator observation leaf and accounting projection, including Quantity, wallet provenance, MarkPresent/MissingMark cost basis, cash invariant, and fixture dedup. The observation and projection are explicit AccountScope-bound. which preserves 保留 1.0093 quantity, wallet source, avgCost=80000 after mark, cash unchanged.. Its boundary is The real MockBroker is reached only through SimulatorControlService; production external transfer tests consume provider observations through accounting/reconciliation, not this route. and its entry-specific falsifier is Deposit 1.0093 after mark 80000 updates only scoped simulator position and keeps cash; duplicate does not double quantity; missing mark remains indeterminate rather than zero.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-8878826403",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-8878826403. Source fact: services/uta/src/http/simulator.spec.ts:152-174: Injects a BUY trade through the simulator endpoint and verifies position avg cost, wallet-source tagging, and cash decrease on the real MockBroker. The source problem is external-trade 测试只观察 MockBroker position/cash；它不能证明 external fill 与 Alice dispatch 分离，也不能证明 journal replay 不重复扣款。 and the current target is External-trade test exercises a simulator observation leaf and accounting fold, not TransactionCommitted. Output carries Position/Cash snapshots, wallet source, side/quantity/price, and declared provenance. The observation and accounting fold are explicit AccountScope-bound. which preserves BUY .5@60000 creates wallet-sourced position avgCost 60000 and cash 70000 from 100k.. Its boundary is Simulator control invokes the real mock interpreter; accounting projection consumes the observation. Controlled provider fills use a separate transaction wrapper and observation identity. and its entry-specific falsifier is BUY .5@60000 yields exact cash 70000 and wallet cost 60000; replay does not debit again; external provenance remains distinct from a controlled order result.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-25883E620A",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-25883E620A. Source fact: services/uta/src/http/simulator.spec.ts:176-208: Places a pending limit order on a real MockBroker, manually fills it through the simulator endpoint, verifies pending state clears, and verifies an unknown order id returns 400. The source problem is manual-fill 测试通过 route 直接清掉 pending order，并以 400 表示 unknown；它没有 partial-fill cumulative state、fixture observation identity 或 production observation boundary。 and the current target is Manual-fill test exercises a simulator-only fill control leaf with declared BrokerOrderId, optional Price/Quantity, and FillObservation output. Production fills remain provider observations associated with a selected transaction effect. The control leaf is explicit AccountScope-bound. which preserves 保留 default mark/limit fill behavior, pending state clears on full fill, unknown id 400.. Its boundary is The real MockBroker is reached through simulator control; HTTP status is not a production execution result and no generic provider fill API is assumed. and its entry-specific falsifier is Default mark/limit full fill removes pending; partial fill keeps remainder/cumulative; unknown id returns typed error with no projection/event and real provider routes have no simulator handler.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-8D000718DA",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-8D000718DA. Source fact: services/uta/src/http/trading-order-entry.spec.ts:1-18: Defines the current one-shot contract as one HTTP roundtrip wrapping stage→commit→push, with phase-specific errors, message validation, and numeric-string precision expectations, then imports createTradingRoutes. The source problem is order-entry spec 把 route contract 定义为一次 stage→commit→push roundtrip；这会把 legacy Git phases、message requirement 和 HTTP status 当作新 transaction protocol。 and the current target is Order-entry HTTP integration targets a declared provider effect leaf plus optional withTransaction wrapper, not a fixed one-shot action API. The contract covers schema decode, review/approval, durable receipt, and subsequent provider observation only for that selected leaf. For account-backed effects, intent, receipt, and later observations carry explicit AccountScope. which preserves 保留 current one-shot scenarios as migration evidence.; 保留 phase-specific failure intent but map to typed transaction states.. Its boundary is The test composition includes transport verifier, selected application/effect writer, scheduler, and provider interpreter; data/query leaves are tested through their own finite/push ports. and its entry-specific falsifier is Draft/prepare leaves provider state untouched; approval returns a durable receipt; provider observation determines later status; response loss/restart converges by command/dispatch identity.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-6837C09018",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-6837C09018. Source fact: services/uta/src/http/trading-order-entry.spec.ts:20-69: Builds a vi-stubbed UTA capturing stagePlaceOrder/stageClosePosition/stageCancelOrder, synchronous commit, async push, reject, and status calls; configured failures model stage, commit, or push phases and return a legacy PushResult. The source problem is makeMockUTA 用 vi stubs 捕获 staging/commit/push/reject/status；异常调用顺序不是 SQL writer、scheduler lease 或 broker ledger 的 crash/recovery 证据。 and the current target is Failure fixture composes named ports only for the selected controlled effect: JournalWriter, scheduler, provider interpreter, projection reader, and capability descriptor. It does not create a vi-shaped universal UTA or require every provider method. The selected account effect retains explicit AccountScope through each failure state. which preserves 保留 stage/commit/push failure scenarios as regression input categories.; 保留 reject no prepared hash safety intent as no-dispatch state rule.. Its boundary is Failures are injected at named protocol/application/provider ports; a real Mock interpreter or conformance fake stays behind its declared leaf and data route fixtures remain independent. and its entry-specific falsifier is Writer failure commits no receipt/events; accepted-before-ack crash resumes as Reconciling/ObserveUnknown; stale lease cannot dispatch a second time; a data leaf failure does not invoke order recovery.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-8A6F5C59E6",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-8A6F5C59E6. Source fact: services/uta/src/http/trading-order-entry.spec.ts:71-93: Creates a minimal casted UTAEngineContext containing only manager.get and invokes createTradingRoutes; post sends JSON to the Hono app and parses the response. The source problem is 测试只提供 manager.get 的 casted context 并解析 JSON response；它没有 authenticated transport principal、shared codec 或 response 后的 journal state 对照。 and the current target is HTTP helper receives the transport verifier and selected application capability context, then uses descriptor-bound codecs. It must not cast a partial UTAEngineContext or allow body actor strings to substitute for Principal. When the selected capability is account-backed, its decoded request carries explicit AccountScope. which preserves 保留 Hono request helper and JSON response parse convenience.; 保留 route-specific path calls during migration.. Its boundary is The helper drives real Hono transport and injected application ports; credential fixture follows the deployment verifier contract, while provider-native objects remain below the adapter boundary. and its entry-specific falsifier is No credential is rejected before application; same declared command has stable digest; valid data/effect response decodes and its relevant projection/receipt agrees without any casted manager call.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-6E09B3ED77",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-6E09B3ED77. Source fact: services/uta/src/http/trading-order-entry.spec.ts:95-114: Posts a legacy place-order body and asserts HTTP 200, a PushResult hash, and exact stagePlaceOrder→commit→push call order. The source problem is happy-path 测试断言 stagePlaceOrder→commit→push 和 Git hash；它没有验证 Draft/Prepare/Approve 顺序、durable job/DispatchIdentity 或 Ack 与 Filled 的差别。 and the current target is Place-order happy path becomes a selected provider Order effect wrapped withTransaction. Its success is a durable receipt and later provider observation; the provider’s exact order schema/extensions are preserved, and no Git hash or universal action is assumed. The selected Order effect is explicitly AccountScope-bound. which preserves 保留 happy path places market BUY qty `0.001`.; Preserve exact action association and no implicit approval by POST.. Its boundary is Writer persists the selected effect intent/jobs before scheduler/provider interpreter invocation; projection reader observes provider order/fill. HTTP returns the wrapper receipt/view. and its entry-specific falsifier is Before approval provider ledger is empty; approval creates one durable job; provider observation yields Working/Filled according to evidence; duplicate command returns the same receipt and no second dispatch.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-9CF3FC0756",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-9CF3FC0756. Source fact: services/uta/src/http/trading-order-entry.spec.ts:116-142: Verifies missing commit message and missing totalQuantity/cashQty return 400 without invoking stage, commit, or push. The source problem is 验证缺 message 或缺 sizing 时不调用 legacy methods；新边界还必须分别拒绝缺 command metadata、非法 action relation 和 unauthorized scope，并证明 writer/broker 未变。 and the current target is Validation integration covers the selected capability schema before its handler. Missing command metadata, declared sizing/relation requirements, and unauthorized scope are rejected only when that leaf requires them; no universal order schema or stage method is invoked. For an account-backed leaf, explicit AccountScope is a declared input rather than an inferred route default. which preserves 保留 missing message/quantity 等旧输入不产生 staging/dispatch side effect 的证据；message 不再成为所有 effect 的必填 action 字段。; 保留 schema/application 早拒绝的安全目的。. Its boundary is Transport/application validation is the only path for malformed requests; selected data leaves and effect leaves use their own descriptors and error schemas. and its entry-specific falsifier is Missing declared sizing or actor fails before DraftReplaced; invalid relation/unknown field is rejected; event count/provider ledger remains unchanged, while a valid data leaf requires no order metadata.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-EA13BA2333",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-EA13BA2333. Source fact: services/uta/src/http/trading-order-entry.spec.ts:144-176: Configures the UTA stub to throw during staging or commit, then verifies phase=stage/commit, HTTP 400, no later phase, and no reject when commit has no prepared hash. The source problem is 测试把 staging/commit throw 映射为 phase=stage/commit；这种 stub 失败不能证明 PrepareFailure 的状态演进、SQL rollback 或 Preparing 恢复。 and the current target is Preparation/review failures are typed states of a selected transaction wrapper, not HTTP `phase` strings. Data query/stream capabilities have their own delivery failures and do not enter Preparing or rejection machinery. An account-backed wrapper retains explicit AccountScope even when preparation fails. which preserves 保留 stage/commit errors stop later work and no reject when no prepared hash.; Change generic `Error` to named failure and durable state.. Its boundary is Writer commits wrapper state atomically; provider is not called when preparation facts/validation/storage fail. Transport only encodes the selected error schema. and its entry-specific falsifier is Fact failure leaves Draft/issues and no provider call; writer rollback leaves no partial locks/jobs; stale preparation is rejected; data leaf requests do not acquire an effect state.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-2FADFA4CDF",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-2FADFA4CDF. Source fact: services/uta/src/http/trading-order-entry.spec.ts:178-191: Configures push to throw and asserts HTTP 500 with phase=push, exercising the final legacy broker execution phase without checking durable intent or remote outcome. The source problem is 测试把 push throw 映射为 HTTP 500 phase=push；异常既可能发生在 remote mutation 前后，不能据此声称 rejected，必须以 DispatchStarted 和 observation 分类。 and the current target is Dispatch failure integration belongs to the selected transaction wrapper/provider effect. The wrapper writes DispatchStarted before provider invocation and classifies provider evidence as Rejected or OutcomeUnknown; HTTP does not turn a thrown method into a universal 500 contract. Dispatch/recovery state retains explicit AccountScope for the selected account effect. which preserves 保留 push failure scenario as final broker-phase failure.; Do not claim remote rejection from thrown push exception without observation evidence.. Its boundary is Scheduler owns lease/epoch/dispatch identity and invokes the selected provider interpreter; HTTP only returns/querys the durable wrapper view. Data leaves are not routed through this path. and its entry-specific falsifier is Known provider rejection has evidence and no blind continuation beyond policy; crash after DispatchStarted yields Reconciling/ObserveUnknown and one future identity, not immediate 500-as-retry.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-A33EC0E470",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-A33EC0E470. Source fact: services/uta/src/http/trading-order-entry.spec.ts:193-203: Posts to an unknown account id and asserts HTTP 404 before the legacy pipeline runs. The source problem is unknown account 测试只观察 legacy route 的 404；它没有区分 malformed AccountId、TransactionNotFound 和跨 scope Forbidden，也没有持久化不变性。 and the current target is Account/transaction resolution first decodes branded identities and Principal scope. A transaction id is resolved only for a selected transaction-wrapper capability; data leaves resolve AccountScope without inventing transaction state. which preserves 保留 unknown account returns 404 before action.; Preserve no broker call/no staged state.. Its boundary is Application directory/transaction reader performs identity lookup; HTTP maps typed 404/403/401 and never invokes legacy UTA staging. and its entry-specific falsifier is Unknown account returns typed 404 with no mutation; an existing subject outside Principal scope returns 403 without leakage; a data-only provider has no transaction resolution path.. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-891F1FEA91",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-891F1FEA91. Source fact: services/uta/src/http/trading-order-entry.spec.ts:205-220: Posts a high-precision totalQuantity and inspects captured stage arguments to ensure the exact string reaches legacy staging without float conversion. The source problem is 测试只检查高精度 quantity 到达 stage 参数；它未证明 canonical decimal 在 journal digest、prepared native plan、重启 projection 中保持同一文本。 and the current target is Decimal precision is a cross-boundary invariant for any declared financial field, including a selected Order effect if present: wire schema -> refined value -> projection/plan -> provider adapter. It is not evidence that all routes are order transactions. A selected account effect preserves AccountScope across the precision boundary. which preserves 保留 high-precision quantity exact string requirement.; Extend proof through persistence and dispatch rather than stopping at staging.. Its boundary is HTTP codecs, application/domain values, projection store, and selected provider adapter preserve the declared decimal type; tests inspect durable/output values rather than legacy stage call args. and its entry-specific falsifier is `0.123456789012345` survives the selected wire->projection/effect path and reload with deterministic digest; no IEEE-754 rounding or provider-unproven precision guarantee is claimed.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-165076A00B",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-165076A00B. Source fact: services/uta/src/http/trading-order-entry.spec.ts:222-233: Posts a one-shot order and asserts the exact user message is passed to legacy uta.commit. The source problem is 测试把 human message 转发给 uta.commit；message 因此看似执行输入，未区分 TradeAction、approval actor、audit rationale 和 broker payload。 and the current target is Audit rationale is metadata on a selected controlled-effect command, separate from the Order semantic input, ApprovalActor Principal, and provider payload. Data capabilities do not require a commit message or audit mutation. The selected account effect binds that metadata to explicit AccountScope. which preserves 保留 exact human message visible in audit history where supplied.; Remove forwarding assertion to `uta.commit(message)` as execution contract.. Its boundary is Selected transaction application accepts verified actor and optional reason, writer records causation/correlation, and projection renders the message; HTTP does not forward free-form text to a native provider. and its entry-specific falsifier is Different rationale affects only declared audit metadata/digest policy; body actor cannot override verified Principal; provider request contains no free-form commit message.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-EC8F4C5533",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-EC8F4C5533. Source fact: services/uta/src/http/trading-order-entry.spec.ts:236-267: Covers explicit close quantity and close-all behavior, asserting HTTP 200 and that legacy stageClosePosition receives a string qty or no qty, preserving Decimal precision and All semantics through stage/commit/push. The source problem is close 测试用缺省 qty 表达 close-all，并以 stage 参数检查精度；它没有验证 prepare 时解析 All、exposure before-image、partial fill 和补偿评估。 and the current target is Close integration tests a provider-declared close Order effect with Quantity|All only where declared, an ExposureSnapshot before-image, exact Decimal values, and a selected transaction wrapper’s criterion/recovery evidence. It does not assert a universal ClosePosition API. The close effect is explicitly AccountScope-bound. which preserves 保留 explicit qty string and no qty = close all.; Preserve precision and close-after-fill cleanup intent.. Its boundary is Accounting/application gathers exposure, the selected wrapper writer creates plan/jobs, and the provider adapter observes residual exposure; HTTP returns effect receipt/view rather than Git result. and its entry-specific falsifier is `.001` remains exact; All captures exposure at prepare; external change requires reprepare; partial outcome remains Reconciling and full completion requires declared observed criterion, not Ack alone.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-6AED1E318D",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-6AED1E318D. Source fact: services/uta/src/http/trading-order-entry.spec.ts:269-277: Verifies missing commit message returns 400 and no staging occurs for close-position. The source problem is close validation 测试把缺 message 当作唯一 400 条件；新 action 必须验证 principal/commandId/scope/instrument/Quantity|All，而不需要 Git message。 and the current target is Close validation is the input schema of a selected close-capable effect. Required Principal/command identity/explicit AccountScope/instrument and Quantity|All are enforced only if declared; Git commit message is not an action field. which preserves 保留 missing message test as malformed legacy request, but change reason to missing approval metadata if needed.; Preserve no staging behavior.. Its boundary is Transport/application validation runs before the selected writer/provider; data-only capabilities are unaffected and do not need close metadata. and its entry-specific falsifier is Missing declared metadata fails with no event/provider call; valid All creates a draft only and cannot directly dispatch; a data leaf has no message/close validation route.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-D236542360",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-D236542360. Source fact: services/uta/src/http/trading-order-entry.spec.ts:280-296: Posts a legacy cancel-order body and asserts HTTP 200 plus a staged orderId forwarded to stageCancelOrder. The source problem is cancel 测试只断言 orderId 被转发到 stageCancelOrder；它没有建立 ScopedOrderRef/version、approval job 或 late-fill/Unknown outcome。 and the current target is Cancel integration tests a provider-declared cancel Order effect with scoped BrokerOrderId/version/precondition and a selected transaction wrapper. Response records effect identity/receipt; it does not assert forwarding to a global stageCancelOrder method. The cancel effect is explicitly AccountScope-bound. which preserves 保留 orderId ord-42 accepted through staging intent.; Preserve explicit cancel before approval.. Its boundary is Writer/scheduler/provider adapter own the selected effect; HTTP only submits/queries it. Simulator force-cancel remains a separate simulator leaf. and its entry-specific falsifier is A valid scoped id creates one selected effect job; stale version writes no event; late fill is recorded and status is not falsely Cancelled.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    },
    {
      "id": "MAP-A034B94A3A",
      "verdict": "accepted",
      "reason": "Independent source review of MAP-A034B94A3A. Source fact: services/uta/src/http/trading-order-entry.spec.ts:298-315: Verifies empty orderId and empty commit message each return HTTP 400 for the one-shot legacy route. The source problem is cancel validation 测试把空 orderId 与空 commit message 放在同一 legacy 400 contract；新协议应仅以 branded nonempty id 和 command envelope 判定，并禁止隐式 dispatch。 and the current target is Cancel validation is the selected provider leaf’s strict input codec: nonempty BrokerOrderId/explicit AccountScope and command metadata where effect-wrapped, with no commit-message requirement. No valid command bypasses its separate review/prepare boundary. which preserves 保留 empty orderId returns 400 intent.; Replace empty commit message check with typed approval/audit metadata rule.. Its boundary is HTTP/application boundary validates the selected leaf and wrapper; no legacy stage/commit/push or simulator control is invoked. and its entry-specific falsifier is Empty id or missing verified Principal rejects before journal/provider; a valid id creates a draft only and cannot directly dispatch; undeclared cancel has no route.. Repair applied: the account-backed or simulator/admin target now names explicit AccountScope instead of relying on an implicit route scope. Verdict: accepted as source-consistent target design only; this review does not claim native-provider, production-runtime, or end-to-end execution evidence."
    }
  ]
}
