{
  "group": "HttpRoutes",
  "designBasis": "capability-composition-v1",
  "entries": [
    {
      "id": "MAP-38469CADA4",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/__tests__/trading-tools.spec.ts:1-15: Imports createTradingTools, UTAManager, UnifiedTradingAccount, MockBroker, IBKR Order/OpenOrder values, and contract extensions; the comments explicitly place this spec at Alice's tool boundary over co-located UTA broker/domain stubs."
      ],
      "currentBehavior": "Imports createTradingTools, UTAManager, UnifiedTradingAccount, MockBroker, IBKR Order/OpenOrder values, and contract extensions; the comments explicitly place this spec at Alice's tool boundary over co-located UTA broker/domain stubs.",
      "problem": "该 spec 直接把 createTradingTools 与 UTAManager、UnifiedTradingAccount、MockBroker 及 IBKR Order/OpenOrder 放在同一进程；因此工具边界可以绕过序列化协议，测试不会发现 native SDK 值或进程内对象泄漏到 Alice/HTTP。",
      "targetModel": "Tool-boundary fixture is a declared capability leaf plus its typed input/result/error schemas and a provider interpreter; the fixture exposes protocol values only, while IBKR Order/OpenOrder remain adapter-local evidence. No complete broker interface is required. Scope is explicit Public or Account(AccountScope) when the capability is account-backed; no route invents a default account or subaccount.",
      "pureTransitions": [
        "decodeFixture(wire, leafSchema) -> Result<ProtocolFixture, FixtureDecodeFailure>",
        "composeToolQuery(leaf, typedClient) -> Result<ToolObservation, ProtocolFailure>; composition is leaf-specific, not a global action map."
      ],
      "effectBoundary": "Test composition supplies one selected provider capability through a typed client/adapter port; HTTP/tool codecs stop at the UTA-facing boundary and never construct or pass native SDK objects.",
      "durability": "The fixture itself has no authority. If an effect leaf is exercised, the integration fixture may attach the transaction writer and projection store; finite data reads remain projection reads and do not acquire order WAL or approval state.",
      "errors": [
        "FixtureDecodeFailure(fields)",
        "CapabilityUnavailable(capability)",
        "ProtocolDecodeFailure(response)"
      ],
      "preservedBehavior": [
        "保留该 spec 作为 Alice tool boundary 的行为证据。",
        "保留 MockBroker 与工具层的端到端关联，但移除 native SDK 值穿过公共边界。"
      ],
      "replacementSteps": [
        "Replace direct UTA/IBKR imports at the public fixture seam with a provider declaration and protocol fixture builder.",
        "Keep native construction in an adapter conformance fixture and assert the exported schema at the tool boundary.",
        "Use a transaction wrapper only in tests that explicitly exercise a controlled effect."
      ],
      "verificationCases": [
        "A tool request decodes to a schema-valid projection with no IBKR-only object or field; a provider-native fixture is accepted only behind its declared leaf, and an absent leaf is not represented by an empty successful result."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source imports and co-locates tool, UTA, MockBroker, and IBKR values, so it proves an in-process test seam rather than a wire contract. Retain the tool behavior evidence but reframe the fixture around one declared capability and adapter-local native decoding, avoiding a universal SDK.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-3FABE05A73",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/__tests__/trading-tools.spec.ts:17-24: Constructs one UnifiedTradingAccount for each MockBroker and registers each account in a UTAManager, giving tool tests an in-process multi-account manager."
      ],
      "currentBehavior": "Constructs one UnifiedTradingAccount for each MockBroker and registers each account in a UTAManager, giving tool tests an in-process multi-account manager.",
      "problem": "测试通过 `new UnifiedTradingAccount` 和 `UTAManager.add` 建立账户目录；这只证明进程内注册，不证明 AccountScope、权限目录或投影在跨进程读取时保持一致。",
      "targetModel": "Provider-instance registry fixture: each configured instance declares capability descriptors, labels, and a provider interpreter handle; an account-backed instance additionally declares an explicit AccountScope, while a public instance declares Public and no account/subaccount. Lookup is against the declared registry, not a universal UTAManager/SDK surface.",
      "pureTransitions": [
        "registerInstance(config, existing) -> Result<Registry, DuplicateCapabilityIdentity|DuplicateAccountId>",
        "resolveVisible(selection, principal, registry) -> Result<VisibleInstance[], AuthorizationFailure> with deterministic ordering."
      ],
      "effectBoundary": "Composition builds the registry and injects it into account/capability query ports; tests do not construct a concrete UnifiedTradingAccount as the transport dependency.",
      "durability": "Registry construction is configuration/projection state, not transaction authority; query checkpoints may describe visibility, while controlled effects persist their own intent and receipt only when wrapped.",
      "errors": [
        "DuplicateAccountId(accountId)",
        "CapabilityNameConflict(identity)",
        "UnauthorizedAccountScope(scope)"
      ],
      "preservedBehavior": [
        "保留多账户查询可同时建立两个独立账户。",
        "保留标签仅为展示，不用 label 路由交易。"
      ],
      "replacementSteps": [
        "Replace makeUta/makeManager with a typed provider-instance fixture containing declared capabilities and scope.",
        "Pass a principal and authorized scope set to each query helper.",
        "Keep provider-native broker state behind the selected interpreter."
      ],
      "verificationCases": [
        "Two instances with the same query leaf return two scoped summaries; duplicate stable capability identity fails at load time without replacing the first instance or writing an event."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source creates one UnifiedTradingAccount per MockBroker and registers it in UTAManager; that establishes multi-instance setup but not a cross-process contract. Retain multi-account coverage while deriving visible leaves from per-instance declarations.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-4AFBEA2B00",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/__tests__/trading-tools.spec.ts:27-36: Casts an in-process UTAManager to UTAManagerSDK because the tool accepts an HTTP-adapter-shaped interface; the comments document that getFxRates is absent and silently falls through to empty rates."
      ],
      "currentBehavior": "Casts an in-process UTAManager to UTAManagerSDK because the tool accepts an HTTP-adapter-shaped interface; the comments document that getFxRates is absent and silently falls through to empty rates.",
      "problem": "测试把 UTAManager 强制 cast 成 UTAManagerSDK，而且缺少的 getFxRates 被工具层 catch 成空 rates；因此服务能力缺失会被伪装成成功的空查询。",
      "targetModel": "Typed client for discovered capability leaves: list/query methods and an optional FX leaf are generated or decoded from each declared schema; absence of the FX leaf is an explicit availability result, never a cast to a complete SDK.",
      "pureTransitions": [
        "describe(instance) -> CapabilityDescriptor[]",
        "invokeLeaf(descriptor, wireInput) -> Result<unknown-at-decode, ProtocolFailure>; decode immediately with that leaf result schema and preserve its provider extension."
      ],
      "effectBoundary": "The HTTP client owns credential, correlation, schema negotiation, and response decoding; application ports expose only the selected query capability. A missing provider leaf remains unavailable rather than becoming empty FX data.",
      "durability": "Read calls use projection checkpoints/as-of metadata. No order transaction records are created for a missing or successful data leaf; an effect leaf gets durable state only through its explicit transaction wrapper.",
      "errors": [
        "CapabilityNotDeclared(capability)",
        "ProtocolDecodeFailure(response)",
        "CapabilityUnavailable(reason)",
        "UnauthenticatedTransport"
      ],
      "preservedBehavior": [
        "保留工具可以在不创建 broker stub 的情况下查询账户。",
        "保留缺失 FX 能力不会影响不需要 FX 的订单查询。"
      ],
      "replacementSteps": [
        "Remove the structural cast and catch-to-empty-rates fallback.",
        "Discover/describe the requested leaf, decode its exact input/output/error schemas, and surface availability to callers.",
        "Use an explicit effect wrapper for any mutation instead of expanding this client into a full broker API."
      ],
      "verificationCases": [
        "A client whose provider lacks the FX leaf returns CapabilityNotDeclared/Unavailable while an unrelated account query still works; malformed FX output is rejected before reaching the tool."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The cast and absent getFxRates are concrete evidence of a false SDK shape and silent success. Retain independent query behavior but replace the cast with declaration-derived leaf invocation and named availability.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-468691D82C",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/__tests__/trading-tools.spec.ts:40-65: Checks account-source resolution semantics: no source returns every registered UTA, an exact source returns one matching account, and an unknown source returns an empty array."
      ],
      "currentBehavior": "Checks account-source resolution semantics: no source returns every registered UTA, an exact source returns one matching account, and an unknown source returns an empty array.",
      "problem": "测试把 unknown source 断言为空数组，同时把 all/exact 解析留给进程内 manager；这会把 UnknownAccount 与合法 Complete(empty) 混淆，也未验证 principal 的 scope 过滤。",
      "targetModel": "Account selection is a small query union, AllAuthorized | ExactAccount(AccountId), evaluated against the capability/account registry and principal scope; it does not imply that every provider has the same account commands.",
      "pureTransitions": [
        "decodeSelection(query) -> Result<AllAuthorized|ExactAccount(AccountId), InvalidSelection>",
        "resolveSelection(selection, visibleRows, coverage) -> Result<ListingResult<ScopedSummary>, UnknownAccount|AuthorizationFailure> where Complete requires explicit namespace coverage."
      ],
      "effectBoundary": "The application query port performs selection and scope filtering; HTTP decodes the query and encodes the typed listing, without calling a manager or broker directly.",
      "durability": "Selection is a finite pull read with checkpoint/freshness evidence and no transaction mutation. A selected account may separately expose provider capabilities and availability.",
      "errors": [
        "InvalidAccountId",
        "UnknownAccount(accountId)",
        "ForbiddenAccountScope(accountId)",
        "ListingPartial(missing)"
      ],
      "preservedBehavior": [
        "保留 source 缺省返回全部注册账户。",
        "保留 exact id 返回单一账户，未知 source 不触发 broker 调用。"
      ],
      "replacementSteps": [
        "Decode source into AccountId or AllAuthorized.",
        "Move all/exact semantics into the account-list capability and preserve authorization filtering.",
        "Teach clients to distinguish Complete(empty), Partial, Unknown, and Forbidden."
      ],
      "verificationCases": [
        "Omitted source returns all authorized visible rows with explicit coverage; exact source returns one row; an unknown exact source is a typed failure and never an empty success or broker call."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source proves meaningful all/exact/unknown selection behavior. Retain those semantics, but make them a scoped account-list leaf with explicit completeness rather than inheriting a manager-wide contract.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K05",
        "K06",
        "K12"
      ]
    },
    {
      "id": "MAP-A88187B75A",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/__tests__/trading-tools.spec.ts:69-87: Returns the sole UTA for an exact source and throws a string-bearing error when no UTA matches the requested source."
      ],
      "currentBehavior": "Returns the sole UTA for an exact source and throws a string-bearing error when no UTA matches the requested source.",
      "problem": "resolveOne 在未命中时抛出依赖错误文本的异常；调用方无法区分 unknown 与 ambiguous，也无法在协议层稳定处理 404/409。",
      "targetModel": "Resolve-one is a query-level cardinality result, Resolved(AccountScope) | UnknownAccount | AmbiguousAccount(matches), produced from the visible registry and the selected provider leaf; error text is not protocol.",
      "pureTransitions": [
        "resolveOne(selection, visibleRows) -> Result<ResolvedAccount, UnknownAccount|AmbiguousAccount>",
        "encodeResolution(result) -> ProtocolResponse with stable variant/code and redacted authorized identifiers."
      ],
      "effectBoundary": "Account-query application performs the lookup; transport maps the result to a stable response status without touching a broker or relying on thrown strings.",
      "durability": "Resolution is a finite read and records only registry checkpoint/freshness; no draft, approval, dispatch, or data-stream transaction is created.",
      "errors": [
        "UnknownAccount(selection)",
        "AmbiguousAccount(selection, matches)",
        "ForbiddenAccountScope"
      ],
      "preservedBehavior": [
        "保留成功时返回唯一匹配账户。",
        "把旧 Error message 改为可稳定解析的 code 与 matches。"
      ],
      "replacementSteps": [
        "Add a runtime AccountId/source codec.",
        "Migrate callers from catch-by-message to exhaustive resolution variants.",
        "Keep the exact-one convenience as a query helper, not as a universal SDK method."
      ],
      "verificationCases": [
        "Unknown source returns UnknownAccount with no provider invocation; duplicate visible aliases return AmbiguousAccount and require an explicit identity."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source keeps the useful exact-one convenience but communicates a string-bearing exception for misses. Retain cardinality semantics while making the result schema and authorization boundary explicit.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-8588C24A12",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/__tests__/trading-tools.spec.ts:91-99: Calls the tool layer with no arguments and verifies that it returns an array of summaries containing the registered account identifier."
      ],
      "currentBehavior": "Calls the tool layer with no arguments and verifies that it returns an array of summaries containing the registered account identifier.",
      "problem": "listUTAs 只断言返回数组和 id；它未约束 health、capability、scope、freshness 或凭据脱敏，工具可以继续依赖过于乐观的摘要。",
      "targetModel": "AccountListResponse is the projection of a declared account/capability-list leaf: each row has branded AccountId/AccountScope, label, reach/health, capability descriptors or availability, and freshness; credentials and native handles are never fields.",
      "pureTransitions": [
        "projectAccountList(rows, principal, coverage) -> Result<ListingResult<AccountSummaryProjection>, ProjectionFailure>",
        "encodeCapabilityAvailability(descriptor, availability) -> named metadata without claiming an unavailable leaf is unsupported."
      ],
      "effectBoundary": "Application query reads the account/capability projection and transport encodes the provider-neutral outer descriptor plus declared leaf metadata; no concrete broker summary crosses HTTP.",
      "durability": "Rows carry revision/checkpoint/as-of evidence. Listing is a finite data read and never writes order journal state; controlled effect receipts are queried through their own capability.",
      "errors": [
        "AccountProjectionUnavailable(reason)",
        "ForbiddenAccountScope(accountId)",
        "CapabilityAvailabilityUnknown(capability)",
        "ProtocolDecodeFailure"
      ],
      "preservedBehavior": [
        "保留无参数 listUTAs 返回所有已注册账户摘要与 id。",
        "不把密钥、native adapter 或 raw broker health object 发送给工具。"
      ],
      "replacementSteps": [
        "Expose named summary and capability descriptor schemas from the registry.",
        "Filter by principal before encoding and keep availability separate from schema presence.",
        "Migrate tool/UI consumers from optimistic id-only summaries."
      ],
      "verificationCases": [
        "The response contains the registered account id and label plus freshness/capability evidence, but no credential or SDK object; a provider leaf that is disconnected is listed as unavailable rather than omitted or fabricated."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source behavior of listing registered identifiers is valuable. Retain it as a finite scoped projection while adding the fixed descriptor envelope and separating declared capability from current availability.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K05",
        "K06",
        "K11",
        "K12"
      ]
    },
    {
      "id": "MAP-0B6606BE1C",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/__tests__/trading-tools.spec.ts:103-119: Spies on two separate MockBroker instances and verifies that searchContracts aggregates one matching contract description from each registered UTA."
      ],
      "currentBehavior": "Spies on two separate MockBroker instances and verifies that searchContracts aggregates one matching contract description from each registered UTA.",
      "problem": "搜索测试仅用两个 broker spy 聚合 ContractDescription；它没有表达 allSettled 后的 Partial coverage，也没有证明 native contract 已被转换为 canonical InstrumentId。",
      "targetModel": "Contract search is a provider-declared data capability leaf. Its query schema declares pattern, hint, and source selection; each provider may add exact search fields, and results use normalized InstrumentId plus a provider extension where declared. Scope is explicit Public or Account(AccountScope) when the capability is account-backed; no route invents a default account or subaccount.",
      "pureTransitions": [
        "normalizePattern(input, providerSchema) -> Result<ProviderSearchInput, EmptyPattern|ValidationFailed>; scope is Public or Account(AccountScope) only when selected by the leaf schema.",
        "aggregateResults(settled) -> ListingResult<ContractSearchHit> retaining healthy hits and named missing scopes; no result is invented for an absent leaf."
      ],
      "effectBoundary": "Application invokes the selected provider search interpreters concurrently; native ContractDescription values are decoded at the adapter boundary before the HTTP response.",
      "durability": "Search may update a catalog/cache projection with version/as-of evidence, but it does not write order intent, approval, compensation, or per-result transaction WAL.",
      "errors": [
        "EmptyPattern",
        "BrokerSearchUnavailable(accountId, reason)",
        "InvalidInstrumentMetadata(detail)",
        "CapabilityNotDeclared(search)"
      ],
      "preservedBehavior": [
        "保留两个 MockBroker 的同 pattern 结果聚合为两条。",
        "保留单 broker 失败不使全量搜索失败。"
      ],
      "replacementSteps": [
        "Move pattern normalization and all-settled aggregation into the instrument-query application service.",
        "Derive the input/output/error codec from each provider declaration and retain source/scope in every hit.",
        "Return Complete/Partial/Unavailable coverage instead of flattening failures to an empty array."
      ],
      "verificationCases": [
        "Two provider instances return two scoped AAPL hits; one rejected instance yields Partial with its identity and the healthy hit; a provider without search has no search leaf rather than an Unsupported-filled handler."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source proves multi-provider aggregation and healthy-hit retention. Retain that failure isolation, but make search a composable data leaf with declaration-specific schema and explicit coverage.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K04",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-1E2638E1F3",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/__tests__/trading-tools.spec.ts:123-135: Builds an OpenOrder fixture from native IBKR Order, OrderState, and contract values, including Decimal quantities/prices and an Alice identifier."
      ],
      "currentBehavior": "Builds an OpenOrder fixture from native IBKR Order, OrderState, and contract values, including Decimal quantities/prices and an Alice identifier.",
      "problem": "makeOpenOrder 直接构造 IBKR Order、OrderState 和 Contract；测试因此把 adapter 输入当成公共 OrderSnapshot，未验证 sentinel、scope 和稳定 order identity 的解码边界。",
      "targetModel": "The fixture represents a provider-declared order-observation leaf: normalized BrokerOrderId, AccountScope, InstrumentId, provider-declared order fields, and observation status/version. Native Order/OrderState/Contract stay inside the adapter fixture and are not a universal Order schema.",
      "pureTransitions": [
        "decodeNativeOrder(adapterProfile, native) -> Result<DeclaredOrderObservation, NativeDecodeFailure>",
        "projectObservation(observation, leafSchema) -> Result<ProtocolOrderSnapshot, ProjectionFailure> preserving exact provider extensions and structural absence."
      ],
      "effectBoundary": "A provider interpreter or conformance fixture supplies native observations; the HTTP/tool boundary consumes the selected leaf codec and never imports Order/OrderState.",
      "durability": "This read observation is checkpointed/deduplicated by the provider-declared order identity and observation version. Transaction WAL is used only if a separate order effect wrapper references the observation.",
      "errors": [
        "NativeDecodeFailure(field)",
        "MissingBrokerOrderId",
        "SchemaDrift(observed, expected)",
        "CapabilityUnavailable(order-observation)"
      ],
      "preservedBehavior": [
        "保留 fixture 能表达 Decimal 数量/价格与 Alice identity。",
        "保留 adapter-specific construction 作为 broker conformance fixture，但不扩大公共 schema。"
      ],
      "replacementSteps": [
        "Move makeOpenOrder into the provider adapter/conformance fixture.",
        "Decode into a named provider order-observation schema and test its wire representation.",
        "Keep any transaction plan/receipt linkage in the optional effect wrapper, not in the read fixture."
      ],
      "verificationCases": [
        "A mock-paper|AAPL native fixture decodes to a scoped protocol observation with exact Decimal fields and no raw SDK object; a missing identity or unknown provider field stops the projection with a typed failure."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source only constructs an IBKR fixture, which is useful adapter evidence but not a transport contract. Reframe it as one provider order-observation declaration and preserve the native values only at the adapter boundary.",
      "contractAnchors": [
        "K02",
        "K03",
        "K04",
        "K05",
        "K07",
        "K11",
        "K12"
      ]
    },
    {
      "id": "MAP-EBC1DF5535",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/__tests__/trading-tools.spec.ts:137-167: Executes a staged and pushed market order, then checks that getOrders returns compact fields such as source, action, orderType, quantity, and status while excluding raw IBKR fields."
      ],
      "currentBehavior": "Executes a staged and pushed market order, then checks that getOrders returns compact fields such as source, action, orderType, quantity, and status while excluding raw IBKR fields.",
      "problem": "测试先走 stage/commit/push 再查询 compact order；HTTP/tool 结果因此仍可把 Git push 成功当作订单事实，并未约束 projection stale 或 broker observation 延迟。",
      "targetModel": "Get-orders is a finite pull over a provider-declared order-observation leaf. OrderSummary contains only declared common facts plus exact provider extensions, with status/identity/freshness and structural absence explicit; it is not a complete broker action model.",
      "pureTransitions": [
        "projectOrders(observations, outputSchema) -> Result<ListingResult<OrderSummary>, ProjectionFailure>",
        "classifyCoverage(listing) -> Complete|Partial|Unavailable; HTTP success never infers execution, fill, or cancellation from status alone."
      ],
      "effectBoundary": "Account-query invokes an order-observation port or reads its projection; adapters decode native fields, while tool/HTTP only encodes the selected output schema.",
      "durability": "The listing records source sequence/cursor and observation freshness. It remains data delivery: no order approval, lock, compensation, or transaction WAL is created by GET /orders.",
      "errors": [
        "OrderProjectionStale(orderId, asOf)",
        "OrderObservationDecodeFailure(orderId, field)",
        "ListingIncomplete(namespace)",
        "CapabilityNotDeclared(order-observation)"
      ],
      "preservedBehavior": [
        "保留 compact 输出核心 fields 并排除 softDollarTier/transmit/blockOrder/sweepToFill。",
        "保留已提交市场单后查询仍能看到规范状态。"
      ],
      "replacementSteps": [
        "Remove the in-process structural cast and expose a typed order-observation leaf.",
        "Retain the compact fields proven by the fixture and add provider extension decoding without leaking presentation-only SDK fields.",
        "Make listing coverage and stale state visible to callers."
      ],
      "verificationCases": [
        "A finite response retains source/action/type/quantity/status when declared, excludes IBKR presentation fields, and marks delayed or partial observations explicitly; no HTTP 200 is interpreted as an Ack or fill."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source proves a compact order read and deliberate exclusion of raw IBKR fields. Retain that projection behavior while moving it to a provider-declared finite data capability rather than a universal order SDK.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K04",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-0682C42EA8",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/__tests__/trading-tools.spec.ts:169-193: Mocks a raw OpenOrder with IBKR UNSET numeric values and zero parentId, then verifies that unset optional fields are omitted from the returned order summary."
      ],
      "currentBehavior": "Mocks a raw OpenOrder with IBKR UNSET numeric values and zero parentId, then verifies that unset optional fields are omitted from the returned order summary.",
      "problem": "测试在工具摘要层过滤 IBKR UNSET_DOUBLE、UNSET_DECIMAL 与 parentId=0；若 decoder 不在 adapter 统一处理，重启或其它 broker 仍可能把 sentinel 持久化成业务值。",
      "targetModel": "Optional native fields are an adapter-local, versioned presence profile for the provider leaf: Present(value) or structural Absent only when that adapter/profile/field proves the sentinel. Unknown profile/value remains unavailable and never becomes a guessed business field.",
      "pureTransitions": [
        "decodeOptional(profile, fieldSchema, raw, presence) -> Result<Absent|Present<DeclaredValue>, UnknownNativeSentinel|PresenceRequired|SchemaDrift>",
        "encodeProjection(value) -> outputSchema with absent members omitted and provider extension schema preserved."
      ],
      "effectBoundary": "Each selected broker adapter owns native sentinel decoding; projection and HTTP codecs consume the resulting declared optional field and perform no IBKR sentinel tests.",
      "durability": "Only the declared value/presence and profile fingerprint are retained with observations or a transaction plan when relevant; raw unknown sentinels never enter an order snapshot or prepared effect.",
      "errors": [
        "UnknownNativeSentinel(field, adapter, profileVersion)",
        "NativeSentinelSchemaDrift(field, expectedVersion, observedVersion)",
        "PresenceRequired(field)",
        "OptionalFieldUnitError(field)"
      ],
      "preservedBehavior": [
        "保留未设置 lmt/aux/trail/parent/tpsl 不出现在 summary。",
        "保留 status/order identity 等非 optional 字段。"
      ],
      "replacementSteps": [
        "Maintain a versioned profile per adapter/codec, enabling only evidence-backed mappings.",
        "Replace tool-layer undefined guessing with a presence-aware schema value.",
        "Add round-trip evidence for Present and omission for Absent; keep unknown values out of public projection."
      ],
      "verificationCases": [
        "The confirmed Mock/IBKR UNSET price and parentId=0 fixture omits those fields; a real zero or another adapter/profile without presence evidence yields typed unavailable/violation instead of being conflated with Absent."
      ],
      "openQuestions": [
        "仍需在各 broker adapter 核实 native sentinel 的完整集合与版本；Mock/IBKR 当前证据只覆盖此测试使用的几种。"
      ],
      "contractDisposition": "retained",
      "contractRationale": "The source omission behavior is a worthwhile safety invariant and the unresolved cross-adapter sentinel inventory remains open. Retain the omission, but localize authority to each provider profile instead of a global sentinel rule.",
      "contractAnchors": [
        "K03",
        "K04",
        "K07",
        "K11",
        "K12"
      ]
    },
    {
      "id": "MAP-0CD7FA4541",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/__tests__/trading-tools.spec.ts:195-211: Mocks a limit order with a lmtPrice and nested take-profit/stop-loss values and verifies that explicitly present optional fields survive summarization."
      ],
      "currentBehavior": "Mocks a limit order with a lmtPrice and nested take-profit/stop-loss values and verifies that explicitly present optional fields survive summarization.",
      "problem": "测试只确认 lmtPrice 和 tpsl 在摘要中出现；它没有把这些字段绑定到 OrderType/ProtectionSpec 的合法关系，也无法区分 capability unavailable 与静默丢失。",
      "targetModel": "Limit and protection fields are provider-declared order-observation extensions or, when an effect leaf declares them, HOF-composed order terms with explicit preconditions. A response includes Present terms only when declared; an unsupported attached-protection parameter variant is omitted from that leaf's exact schema and does not remove other declared variants. A declared-but-disconnected leaf retains its identity and reports availability separately; no global optional bag or universal protection set is assumed.",
      "pureTransitions": [
        "decodeDeclaredTerms(native, leafSchema) -> Result<ProviderOrderTerms, RelationFailure|NativeDecodeFailure>",
        "composeEffectTerms(base, extension) -> Result<ComposedInput, SchemaConflict> without overwriting or dropping an existing extension."
      ],
      "effectBoundary": "The selected adapter decodes lmt/take-profit/stop-loss values; application/transport validates the selected leaf schema. An undeclared protection parameter variant is absent from that schema and rejected as unknown input, whereas a declared-but-disconnected leaf retains its identity and reports named availability.",
      "durability": "Read terms are stored in the order observation projection. Prepared payloads and digests retain terms only for a transaction-wrapped effect leaf; data reads do not receive locks or compensation machinery.",
      "errors": [
        "OrderTermsRelationFailure(fields)",
        "ValidationFailed(unknownProtectionField)",
        "NativeDecodeFailure(field)",
        "CapabilityUnavailable(protection)"
      ],
      "preservedBehavior": [
        "保留显式 lmtPrice 与 nested takeProfit/stopLoss 在 summary 中可见。",
        "保留精确价格字符串。"
      ],
      "replacementSteps": [
        "Declare the exact terms schema at the provider leaf and preserve it through output/plan codecs.",
        "Use an explicit HOF/extension composition for controlled effects instead of a shared Partial<Order> bag.",
        "Report an undeclared protection parameter through schema absence/unknown-field validation; keep a declared-but-unavailable leaf as separate availability without silently rewriting the order."
      ],
      "verificationCases": [
        "An explicitly present limit and nested protection values survive the declared observation/plan schema with exact decimals; when a provider declares the Order leaf but not an attached-protection parameter variant, that variant is absent from the exact schema and an attempted field is rejected while other declared variants remain available; a disconnected declared leaf reports named availability."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The fixture demonstrates that present optional values must survive summarization. Retain that precision and visibility, while making the terms provider-declared and HOF-composed rather than globally enumerated. The review explicitly separates an absent parameter variant from a missing capability leaf and from temporary availability.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K04",
        "K07",
        "K08",
        "K12"
      ]
    },
    {
      "id": "MAP-07DB18F4C8",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/__tests__/trading-tools.spec.ts:213-228: Sets a small Decimal limit price and verifies that the tool emits the exact decimal string instead of a JavaScript number."
      ],
      "currentBehavior": "Sets a small Decimal limit price and verifies that the tool emits the exact decimal string instead of a JavaScript number.",
      "problem": "测试证明小数价格在当前工具输出仍是字符串，却没有证明 Decimal 文本穿过 draft、journal、projection 和 native compile 后仍精确。",
      "targetModel": "Decimal fields are refined value objects in each declared schema (price, quantity, money, or provider extension), encoded as canonical decimal text; wire numbers are not substituted. The invariant applies to data reads as well as controlled effects.",
      "pureTransitions": [
        "parseDecimal(text, unit) -> Result<DecimalValue, InvalidDecimal|UnitMismatch>",
        "encodeDecimal(value) -> canonical string with exact round-trip; composeDeclaredSchema preserves the field unit and provider extension."
      ],
      "effectBoundary": "Protocol codecs and projection serializers own decimal encoding; provider adapters perform native arithmetic/decoding, and HTTP does not use JavaScript Number to normalize a result.",
      "durability": "Canonical text and schema/semantic version are retained in read projections; an order plan/digest retains the same text only when the selected effect is transaction-wrapped. A quote or data query creates no transaction record.",
      "errors": [
        "InvalidDecimal(text)",
        "PrecisionLossDetected(original, encoded)",
        "UnitMismatch(field)",
        "ProtocolDecodeFailure(response)"
      ],
      "preservedBehavior": [
        "保留 `0.00001234` 原样输出为字符串。",
        "保留 Decimal arithmetic 在 adapter/domain 内完成。"
      ],
      "replacementSteps": [
        "Use one canonical decimal codec for declared query and effect schemas.",
        "Remove numeric fallback from compact/transport projections.",
        "Prove precision across the relevant leaf boundary; do not infer guarantees for fields a provider has not declared."
      ],
      "verificationCases": [
        "A small limit price remains the same JSON string through the declared query/projection path; for an effect leaf it remains unchanged through draft/prepare/journal/native compile, while a malformed or unitless number is rejected before the handler."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source proves that a small decimal is emitted as text. Retain the exact-string behavior and extend it only to the selected declared schema, without making every HTTP route an order transaction.",
      "contractAnchors": [
        "K03",
        "K04",
        "K05",
        "K08",
        "K12"
      ]
    },
    {
      "id": "MAP-0C2AAEE0A8",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/__tests__/trading-tools.spec.ts:230-242: Provides a string orderId from getPendingOrderIds while the native Order carries a numeric sentinel, and verifies that the returned summary preserves the explicit string identity."
      ],
      "currentBehavior": "Provides a string orderId from getPendingOrderIds while the native Order carries a numeric sentinel, and verifies that the returned summary preserves the explicit string identity.",
      "problem": "测试以 getPendingOrderIds 的字符串覆盖 native Order 的数值 sentinel；若该 join 规则不进入持久 projection，长 ID 或重启后仍可能被 numeric orderId 覆盖。",
      "targetModel": "BrokerOrderId is the provider-declared identity used by the order-observation leaf. A separate native numeric field may remain adapter evidence; joining pending-index and observation identities must not let a sentinel or conversion overwrite the declared id.",
      "pureTransitions": [
        "joinOrderIdentity(pending, observation, identitySchema) -> Result<OrderSummary, IdentityConflict|MissingOrderIdentity>",
        "projectIdentity(id) -> canonical text preserving long/non-numeric ids exactly."
      ],
      "effectBoundary": "The provider adapter decodes native identity and the account-query projector joins it with pending observations; transport consumes the resulting identity schema and does not cast numeric SDK fields.",
      "durability": "The declared BrokerOrderId and observation sequence are durable join keys for the read projection. If an order effect references it, the same identity is bound into that capability’s prepared payload; ordinary reads do not create transaction state.",
      "errors": [
        "MissingOrderIdentity",
        "IdentityConflict(expected, observed)",
        "NativeIdentityDecodeFailure",
        "ListingIncomplete(namespace)"
      ],
      "preservedBehavior": [
        "保留 `uuid-abc-123` 从 getPendingOrderIds 输出。",
        "避免 19 位 CCXT id 被 native numeric orderId 截断。"
      ],
      "replacementSteps": [
        "Make the pending-index identity a named provider value object.",
        "Remove raw native orderId fallback as authority and retain adapter evidence separately.",
        "Exercise long and non-numeric identities across projection reload."
      ],
      "verificationCases": [
        "uuid-abc-123 remains the returned id when native orderId is zero; a 19+ digit id survives serialization/reload unchanged and an inconsistent join is marked for reconciliation rather than silently replaced."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source explicitly protects a string identity from a numeric sentinel. Retain that behavior as a provider identity/projection rule and keep any effect linkage scoped to the selected transaction wrapper.",
      "contractAnchors": [
        "K03",
        "K04",
        "K05",
        "K07",
        "K08",
        "K12"
      ]
    },
    {
      "id": "MAP-CA93BD9449",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/__tests__/trading-tools.spec.ts:244-270: Mocks three pending orders across AAPL and ETH and verifies that groupBy contract returns an object keyed by each Alice contract identifier with the expected order clusters."
      ],
      "currentBehavior": "Mocks three pending orders across AAPL and ETH and verifies that groupBy contract returns an object keyed by each Alice contract identifier with the expected order clusters.",
      "problem": "测试以 Alice 字符串作为 object key 聚类 AAPL/ETH；display alias 可能把同符号不同 venue 或不同 scope 合并，也没有为无法解析 identity 保留失败状态。",
      "targetModel": "Grouped orders are a derived finite view over declared InstrumentId and AccountScope values: an array of groups with exact provider extensions, not an untyped object keyed by display aliases.",
      "pureTransitions": [
        "groupByInstrument(rows, identitySchema) -> Result<GroupedOrders, InstrumentIdentityMissing|CrossScopeGroupAttempt>",
        "encodeGroups(groups) -> schema-valid array with deterministic ordering and no display-symbol merge."
      ],
      "effectBoundary": "The projection reader supplies normalized observations; the route/tool codec encodes the grouped data response and never constructs or mutates a broker order.",
      "durability": "Grouping is rebuildable read projection with source sequence/as-of metadata. It does not acquire order locks or enter approval/recovery; if later used as effect evidence, the selected identity is persisted separately by the effect wrapper.",
      "errors": [
        "InstrumentIdentityMissing(orderId)",
        "CrossScopeGroupAttempt(scope)",
        "ProjectionStale",
        "CapabilityUnavailable(grouping)"
      ],
      "preservedBehavior": [
        "保留 AAPL 两单、ETH 一单分别聚类。",
        "保留结果可按 Alice contract identity 显示，但内部 identity 为 InstrumentId。"
      ],
      "replacementSteps": [
        "Use canonical InstrumentId and scope in the grouping projection.",
        "Replace object-key assumptions with a named output schema.",
        "Preserve the display alias only as a non-authoritative label/correlation."
      ],
      "verificationCases": [
        "Two AAPL rows in one scope group together; same symbol on different venues or scopes remains separate, and an unresolved identity is surfaced rather than put under a guessed key."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source grouping is useful for AAPL/ETH presentation, but a display string is not a safe identity. Retain the grouping result while deriving it from declared instrument/scope values.",
      "contractAnchors": [
        "K03",
        "K04",
        "K05",
        "K06",
        "K12"
      ]
    },
    {
      "id": "MAP-DF17C576FF",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/__tests__/trading-tools.spec.ts:274-289: Resolves mock-paper|AAPL through the UTA, spies on broker.getQuote, verifies that the broker receives a native contract with populated symbol/localSymbol and the original Alice identifier, and checks the quote source."
      ],
      "currentBehavior": "Resolves mock-paper|AAPL through the UTA, spies on broker.getQuote, verifies that the broker receives a native contract with populated symbol/localSymbol and the original Alice identifier, and checks the quote source.",
      "problem": "测试通过 getQuote spy 检查 native Contract 的 symbol/localSymbol/aliceId；这验证了 adapter 事实，但当前 tool 边界仍直接暴露 native construction，未验证授权后返回的规范 Quote。",
      "targetModel": "Quote is a provider-declared pull capability with input QuoteRequest (scope: Public | Account(AccountScope), InstrumentRef, and declared provider extension) and output QuoteObservation (prices, currency/unit, observedAt, source, and exact extension); native Contract construction is adapter-local. Scope is explicit Public or Account(AccountScope) when the capability is account-backed; no route invents a default account or subaccount.",
      "pureTransitions": [
        "resolveInstrument(ref, catalog) -> Result<InstrumentId, IdentityFailure>",
        "decodeQuote(native, outputSchema) -> Result<QuoteObservation, NativeQuoteDecodeFailure> preserving provider-native fields declared by the leaf."
      ],
      "effectBoundary": "Application selects the authorized provider quote leaf; its adapter builds the native request and decodes the response. Tool/HTTP receives only the declared wire schema.",
      "durability": "Quote observations may update a market projection/cache with sequence and freshness. They are finite data reads, not prepared orders, and cannot be treated as execution acknowledgement without a separately versioned effect decision.",
      "errors": [
        "AccountNotFound(accountId)",
        "InstrumentNotFound(ref)",
        "UnauthorizedAccountScope",
        "QuoteUnavailable(reason)",
        "NativeQuoteDecodeFailure(field)"
      ],
      "preservedBehavior": [
        "保留 aliceId 解析后 broker 收到 symbol/localSymbol 与原 Alice identity。",
        "保留返回 source/account ownership。"
      ],
      "replacementSteps": [
        "Move contractFromAliceId and native quote construction behind the provider adapter.",
        "Publish the quote input/output/error schema and preserve source/account ownership.",
        "Keep symbol aliases as an explicit resolver, not as a raw SDK object."
      ],
      "verificationCases": [
        "An authorized mock-paper|AAPL request yields a typed quote with exact observation values and source; a cross-scope ref is rejected before provider invocation and native response fields do not appear unvalidated on the wire."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source proves account-scoped native expansion and quote source attribution. Retain routing and provenance, but compose them as a data leaf rather than requiring a common broker quote interface.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K04",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-560B7EEA32",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/__tests__/trading-tools.spec.ts:291-296: Invokes getQuote with an identifier lacking the expected separator and verifies that the result contains an Invalid aliceId error."
      ],
      "currentBehavior": "Invokes getQuote with an identifier lacking the expected separator and verifies that the result contains an Invalid aliceId error.",
      "problem": "测试只检查 malformed aliceId 的错误文本；分隔符解析和 ownership 失败若未成为 typed codec，调用方仍可能在 broker 调用后才发现身份无效。",
      "targetModel": "The legacy AccountId|nativeKey value is a boundary codec for a provider-declared instrument reference. It parses into refined account/scope and provider key values; it is not a universal instrument identity and cannot be passed through on failure.",
      "pureTransitions": [
        "parseLegacyInstrumentRef(text, providerSchema) -> Result<LegacyInstrumentRef, MalformedInstrumentRef>",
        "authorizeRef(principal, ref, directory) -> Result<AuthorizedInstrument, UnauthorizedAccountScope|InstrumentNotFound>."
      ],
      "effectBoundary": "Transport/tool decoding performs syntax checks; application authorization runs before the selected provider adapter receives a native key.",
      "durability": "A successful query may retain canonical account/instrument fields and the legacy alias as correlation; malformed input produces no receipt, observation, or effect state.",
      "errors": [
        "MalformedInstrumentRef(inputShape)",
        "UnauthorizedAccountScope(accountId)",
        "InstrumentNotFound(nativeKey)",
        "CapabilityUnavailable(quote)"
      ],
      "preservedBehavior": [
        "保留无分隔符输入返回 Invalid/Malformed aliceId。",
        "保留 malformed 请求在 broker 调用前失败。"
      ],
      "replacementSteps": [
        "Replace string/error-text handling with the shared boundary codec.",
        "Carry field paths and stable error codes in the protocol envelope.",
        "Use the parsed value only with the selected provider declaration and scope."
      ],
      "verificationCases": [
        "no-separator-here is rejected before any provider call or projection change; a legal ref reaches only its authorized account leaf and returns a typed quote/detail result."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source proves malformed identifiers fail before the broker. Retain that early refusal while making the legacy syntax an explicit provider alias codec, not a cross-provider universal key.",
      "contractAnchors": [
        "K03",
        "K04",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-CEE9B91D31",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/__tests__/trading-tools.spec.ts:298-310: Registers Alpaca and Bybit accounts, calls getQuote with bybit-main|BTC and no explicit source, and verifies only the Bybit broker receives the request."
      ],
      "currentBehavior": "Registers Alpaca and Bybit accounts, calls getQuote with bybit-main|BTC and no explicit source, and verifies only the Bybit broker receives the request.",
      "problem": "测试以 Alice ID 的 provider 前缀选择 bybit，并仅用 spy 证明 alpaca 未被调用；source mismatch、授权 scope 和不可用账户仍没有稳定的协议结果。",
      "targetModel": "Source routing is a small query selection value, ExplicitAccount(AccountId) or an owner embedded in a provider reference; when both are present they must agree. Fan-out is used only by a declared all-authorized query leaf.",
      "pureTransitions": [
        "resolveSource(refOwner, requested, visibleInstances) -> Result<SelectedInstance, SourceMismatch|UnknownAccount|ForbiddenAccountScope>",
        "invokeSelected(leaf, selected) -> Result<QueryObservation, QueryFailure> without invoking unselected providers."
      ],
      "effectBoundary": "The account-query application performs routing and authorization; only the selected provider interpreter sees the native request, and route code does not inspect concrete broker classes.",
      "durability": "The observation is scoped to the selected AccountScope and may update a market projection; no transaction journal or approval state is created for a quote query.",
      "errors": [
        "SourceMismatch(encodedOwner, requestedSource)",
        "UnauthorizedAccountScope(accountId)",
        "AccountUnavailable(accountId)",
        "CapabilityNotDeclared(quote)"
      ],
      "preservedBehavior": [
        "保留无 explicit source 时由 aliceId 路由到 bybit-main。",
        "保留 alpaca broker 不接收请求。"
      ],
      "replacementSteps": [
        "Centralize owner/source agreement in the source-selection codec.",
        "Use AccountId/AccountScope rather than broker-name string dispatch in callers.",
        "Record selected identity and capability revision in query evidence."
      ],
      "verificationCases": [
        "bybit-main|BTC invokes only the Bybit quote leaf; explicit Alpaca plus the Bybit owner returns SourceMismatch with no Alpaca or Bybit provider call."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source directly verifies that only the Bybit broker receives the request. Retain single-source routing and no-fan-out semantics, but route through declared capability instances and availability.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-08E9053B72",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/__tests__/trading-tools.spec.ts:314-329: Calls getContractDetails with a source and Alice identifier, spies on the broker, and verifies that the broker receives an expanded native query retaining the original identifier."
      ],
      "currentBehavior": "Calls getContractDetails with a source and Alice identifier, spies on the broker, and verifies that the broker receives an expanded native query retaining the original identifier.",
      "problem": "测试用 broker spy 证明 aliceId 被展开为 native contract；details 的 expansion filters、identity provenance 和 raw ContractDetails 脱敏没有被协议约束。",
      "targetModel": "Contract details is a provider-declared instrument/details leaf composed from a typed Public/Account reference and provider-specific expansion filters. Its output is normalized base metadata plus an exact declared leaf/directory/continuation extension; native ContractDetails stays in the adapter. A filter variant not declared by the selected leaf is absent from its input schema; it does not remove the details leaf, and declared-but-unavailable leaves report availability separately. Scope is explicit Public or Account(AccountScope) when the capability is account-backed; no route invents a default account or subaccount.",
      "pureTransitions": [
        "resolveAndExpand(ref, filters, catalog) -> Result<ProviderDetailsInput, IdentityFailure|FilterFailure>",
        "decodeDetails(native, outputSchema) -> Result<ContractDetailsProjection, NativeDecodeFailure> preserving original reference correlation."
      ],
      "effectBoundary": "The selected application instrument-query port invokes the provider adapter; transport only validates/encodes that leaf and never assigns arbitrary fields to a native SDK object.",
      "durability": "Details/catalog observations may be cached with revision/as-of and cursor; they do not modify transaction authority or create order approval state.",
      "errors": [
        "AccountNotFound",
        "InstrumentOwnershipMismatch",
        "ValidationFailed(unknownFilter)",
        "ContractDetailsUnavailable",
        "NativeDetailsDecodeFailure"
      ],
      "preservedBehavior": [
        "保留输入 aliceId 展开为 native query 且 identity 不丢失。",
        "保留 search->details 的 drilldown 用法。"
      ],
      "replacementSteps": [
        "Define the details input/output/error schema at the provider leaf.",
        "Keep native construction and decoding in the adapter and retain the Alice alias only as correlation.",
        "Migrate drill-down callers to typed continuation/leaf variants."
      ],
      "verificationCases": [
        "mock-paper|AAPL expands to a native query with its identity evidence and returns a normalized details result; a filter not declared by the selected leaf is absent from its input schema and an attempted field is rejected before provider access, while a disconnected declared leaf reports named availability rather than a null/raw object."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source proves identity-preserving details expansion. Retain that drill-down behavior and exact source attribution while moving native details behind a composable provider leaf. The filter rule now preserves the details leaf when only one optional filter variant is unsupported.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K04",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-E66037B98B",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/__tests__/trading-tools.spec.ts:331-340: Passes an Alice identifier belonging to another account while selecting mock-paper and verifies that the tool reports the account mismatch without a broker call assertion."
      ],
      "currentBehavior": "Passes an Alice identifier belonging to another account while selecting mock-paper and verifies that the tool reports the account mismatch without a broker call assertion.",
      "problem": "测试以错误字符串报告 other-account|AAPL，但没有把 runtime canonical ownership 作为 broker 调用前的决定；nominal TypeScript brand 不能替代该检查。",
      "targetModel": "Instrument ownership is a runtime refinement between the requested AccountScope and the provider-resolved InstrumentId/native key. Nominal TypeScript brands alone are insufficient; a mismatch stops the selected leaf before provider effects.",
      "pureTransitions": [
        "authorizeInstrument(requestedScope, resolvedRef, directory) -> Result<AuthorizedInstrument, AccountInstrumentMismatch|MalformedInstrumentRef>",
        "rejectBeforeInvoke(result) -> ProtocolFailure with no provider call."
      ],
      "effectBoundary": "The application authorization boundary compares canonical runtime identifiers, then delegates only an authorized query to the selected adapter.",
      "durability": "Rejected queries create no observation, receipt, or journal event. A successful data query remains a projection read; a controlled effect must repeat scope binding inside its own transaction wrapper.",
      "errors": [
        "AccountInstrumentMismatch(expected, actual)",
        "MalformedInstrumentRef",
        "UnauthorizedAccountScope",
        "InstrumentNotFound"
      ],
      "preservedBehavior": [
        "保留 other-account|AAPL 对 mock-paper source 报账户不匹配。",
        "保留错误在 broker call 前可观察。"
      ],
      "replacementSteps": [
        "Use one runtime ownership constructor for every Alice/provider reference endpoint.",
        "Return typed, minimally disclosed mismatch details rather than string errors.",
        "Cover same-symbol cross-account cases before adapter invocation."
      ],
      "verificationCases": [
        "mock-paper with other-account|AAPL fails as AccountInstrumentMismatch before provider access or projection mutation; an authorized same-symbol ref succeeds only in its bound scope."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source already distinguishes an identifier belonging to another account. Retain the pre-provider refusal, but make it an explicit scope refinement rather than a broker-specific string check.",
      "contractAnchors": [
        "K02",
        "K03",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-27AD48107A",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/__tests__/trading-tools.spec.ts:344-371: Uses the placeOrder input schema with empty strings for optional numeric fields and verifies parsing succeeds, empty fields become undefined, and totalQuantity remains the supplied decimal string."
      ],
      "currentBehavior": "Uses the placeOrder input schema with empty strings for optional numeric fields and verifies parsing succeeds, empty fields become undefined, and totalQuantity remains the supplied decimal string.",
      "problem": "测试允许 optional numeric 空字符串转 undefined，却仍把 placeOrder 输入作为可选字段袋；该边界需要明确 Absent、sizing XOR 和未知字段拒绝，而不能由后续 legacy staging 猜测。",
      "targetModel": "The place-order boundary is a provider-declared input schema composed from an Order semantic unit and optional provider extensions. Legacy empty strings may decode to structural Absent only in the declared adapter schema; sizing alternatives are a local XOR, not a global order product.",
      "pureTransitions": [
        "decodeDeclaredOrderInput(wire, inputSchema) -> Result<OrderInput, ValidationFailed>",
        "composeOrderTerms(base, extension) -> Result<ComposedOrderInput, SchemaConflict|RelationFailure>; preserve exact extension fields and Decimal text."
      ],
      "effectBoundary": "The tool/HTTP codec validates the selected order leaf, then an optional withTransaction wrapper accepts the intent; provider-native compilation occurs only in the provider interpreter after approval.",
      "durability": "A draft stores the schema-versioned serializable input and revision only when the capability is transaction-wrapped. Invalid data writes nothing; a data-only provider has no order route or approval state.",
      "errors": [
        "ValidationFailed(path, reason)",
        "MissingOrderSize",
        "OrderRelationInvalid",
        "CapabilityNotDeclared(order)",
        "InstrumentResolutionFailure"
      ],
      "preservedBehavior": [
        "保留 LLM 发送 `cashQty/lmtPrice/...: \"\"` 时成功并变成 undefined/Absent。",
        "保留 totalQuantity `0.01` 字符串。"
      ],
      "replacementSteps": [
        "Replace the optional-field bag with the selected provider input schema and a narrow sizing union where that schema requires it.",
        "Normalize legacy empty strings at the boundary without turning them into universal optional fields.",
        "Use withTransaction only for an explicitly declared controlled effect and preserve provider extensions."
      ],
      "verificationCases": [
        "A legacy empty optional numeric field decodes to Absent while `0.01` remains canonical text; both sizing alternatives or neither are rejected when the selected leaf requires exactly one, and no provider/effect is invoked on invalid input."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source demonstrates useful AI ergonomics but the old target made the route own a shared order field bag. Retain empty-string and decimal handling while deriving required fields and relations from the selected provider schema/HOF.",
      "contractAnchors": [
        "K01",
        "K03",
        "K04",
        "K08",
        "K12"
      ]
    },
    {
      "id": "MAP-4DE6045663",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/__tests__/trading-tools.spec.ts:373-387: Uses a non-positive totalQuantity and verifies that the placeOrder schema rejects the input before execution."
      ],
      "currentBehavior": "Uses a non-positive totalQuantity and verifies that the placeOrder schema rejects the input before execution.",
      "problem": "测试只在 Zod schema 阶段拒绝 totalQuantity=0；正数、有限 Decimal 和 journal 未写入的 invariant 还没有贯穿 application admission。",
      "targetModel": "Positive quantity/notional is a refined field constraint declared by the selected provider order schema (or another quantity-bearing capability). It is checked at the wire/application boundary and is not evidence that every provider exposes an order leaf.",
      "pureTransitions": [
        "parsePositiveDecimal(text, unit) -> Result<PositiveValue, NonPositiveValue|InvalidDecimal>",
        "admitDeclaredInput(value, schema) -> Result<AcceptedInput, ValidationFailed> before any handler/effect."
      ],
      "effectBoundary": "Schema/application admission performs the constraint; a rejected query or draft never reaches a provider adapter. If wrapped as a transaction, the same refined value is persisted in the prepared payload.",
      "durability": "Invalid input creates no draft, job, or receipt. Accepted canonical text is retained in the relevant data projection or transaction plan without numeric coercion.",
      "errors": [
        "NonPositiveQuantity(value)",
        "InvalidDecimal(value)",
        "MissingOrderSize",
        "CapabilityNotDeclared(order)"
      ],
      "preservedBehavior": [
        "保留 totalQuantity=0 在 schema gate 失败。",
        "保留 invalid optional numerics 不进入 execution。"
      ],
      "replacementSteps": [
        "Use the declared quantity constructor at the protocol boundary.",
        "Remove later broker/legacy throws as the first validation gate.",
        "Keep the constraint local to quantity-bearing leaves and test no-state-change rejection."
      ],
      "verificationCases": [
        "0, -1, and non-finite values are rejected before provider invocation or journal write; a positive decimal reaches the selected input schema without float conversion."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source proves a non-positive quantity is rejected before execution. Retain this safety boundary while scoping it to the provider capability that declares quantity semantics.",
      "contractAnchors": [
        "K03",
        "K04",
        "K08",
        "K12"
      ]
    },
    {
      "id": "MAP-6FA0B99F88",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-simulator.ts:17-22: The HTTP module imports Hono/Zod, UTAEngineContext, the concrete MockBroker class, and SecType contract metadata, binding the transport directly to the in-process simulator implementation."
      ],
      "currentBehavior": "The HTTP module imports Hono/Zod, UTAEngineContext, the concrete MockBroker class, and SecType contract metadata, binding the transport directly to the in-process simulator implementation.",
      "problem": "The route boundary has a concrete MockBroker dependency and no shared named request/response protocol, so it is not the target transport-to-application dependency direction.",
      "targetModel": "Simulator admin transport is a separately mounted capability tree whose leaves are declared by the mock-simulator provider and bound to explicit AccountScope; it depends on simulator-control application ports, not a global broker interface or concrete MockBroker import.",
      "pureTransitions": [
        "decodeSimulatorCommand(wire, leafSchema) -> Result<SimulatorCommand, ProtocolDecodeFailure>",
        "authorizeSimulator(principal, scope, descriptor) -> Result<AuthorizedControl, AuthorizationFailure> before invoking the selected simulator interpreter."
      ],
      "effectBoundary": "Composition injects a simulator-control service only for an explicitly enabled mock-simulator instance; production HTTP mounts no simulator leaves and real provider adapters cannot receive god-view commands.",
      "durability": "Simulator controls may append fixture command/observation evidence and generation, but they are not order intents, approvals, dispatch jobs, or compensation transactions. A transaction wrapper is not silently attached.",
      "errors": [
        "MissingSimulatorCapability(accountId)",
        "SimulatorNotAllowedForProductionAccount(accountId)",
        "ProtocolDecodeFailure",
        "ForbiddenSimulatorOperation"
      ],
      "preservedBehavior": [
        "保留 /api/simulator 作为 dev simulator control surface。",
        "保留 real broker 不能接受 god-view commands。"
      ],
      "replacementSteps": [
        "Split simulator protocol/codecs from generic trading transport.",
        "Mount only the declared simulator capability behind authenticated admin scope and preset gates.",
        "Keep MockBroker details inside the simulator interpreter and preserve no-production-control invariant."
      ],
      "verificationCases": [
        "An authorized mock-simulator leaf changes fixture state; a real, keyless, or readOnly account has no simulator leaf and returns typed forbidden/unavailable without provider mutation."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source directly imports MockBroker and mounts a god-view app. Retain the explicit dev-only boundary, but reframe the transport as a separately declared simulator capability with no universal broker dependency.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K05",
        "K07",
        "K08",
        "K12"
      ]
    },
    {
      "id": "MAP-631DC01624",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-simulator.ts:24-41: Simulator price and fill bodies are validated with local Zod schemas; numericString accepts either a string or a number and normalizes both to String, while price movement and fill quantity remain unbranded primitive values."
      ],
      "currentBehavior": "Simulator price and fill bodies are validated with local Zod schemas; numericString accepts either a string or a number and normalizes both to String, while price movement and fill quantity remain unbranded primitive values.",
      "problem": "Local schemas accept ordinary numbers and erase nominal financial types during normalization; the target requires shared runtime schemas and branded decimal/domain values at the boundary.",
      "targetModel": "Simulator price/fill inputs are leaf-specific schemas bound to explicit AccountScope: SetMarkPrice and TickPrice remain a small control union, while fill quantity/price use refined Decimal values. The simulator schema is not an order contract and does not imply sibling operations for providers that do not declare them.",
      "pureTransitions": [
        "decodeSimulatorValue(wire, fieldSchema) -> Result<Price|Percent|Quantity, InvalidDecimal|InvalidPercent>",
        "encodeControlResult(resultSchema) -> Result<DeclaredControlResult, ProtocolDecodeFailure> without number-to-string guessing."
      ],
      "effectBoundary": "Transport decodes the selected simulator leaf and calls SimulatorControlService; only the mock interpreter converts refined values to its in-memory representation.",
      "durability": "Control input/output may be recorded with simulator generation and fixture sequence for reproducible tests, but it creates no approval, dispatch, or order WAL state.",
      "errors": [
        "InvalidDecimal(field)",
        "InvalidPercent(value)",
        "MissingNativeKey",
        "SimulatorCapabilityUnavailable",
        "ProtocolDecodeFailure"
      ],
      "preservedBehavior": [
        "保留现有 mark/tick/fill 三种控制的语义区分。",
        "保留精确字符串在价格/数量中的精度。"
      ],
      "replacementSteps": [
        "Replace local permissive schemas with declaration-derived control codecs.",
        "Require canonical decimal text where the leaf declares financial values and reject JSON numbers that would erase precision.",
        "Keep set/tick/fill semantics separate in the simulator tree."
      ],
      "verificationCases": [
        "`0.00001234` remains exact through the simulator leaf; malformed/non-finite values and invalid percentages fail before state change, and the response is decoded as the named control result rather than `{ok:true}`."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source preserves distinct set/tick/fill controls but currently normalizes numbers through String. Retain the control semantics and exactness while making each leaf schema explicit and non-transactional.",
      "contractAnchors": [
        "K01",
        "K03",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-6A13EE36A7",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-simulator.ts:43-77: The simulator accepts a permissive IBKR-like contract object plus nativeKey and wallet/external-trade fields; derivative metadata, secType, side, quantity, and price are validated locally but remain optional/native-key request fields."
      ],
      "currentBehavior": "The simulator accepts a permissive IBKR-like contract object plus nativeKey and wallet/external-trade fields; derivative metadata, secType, side, quantity, and price are validated locally but remain optional/native-key request fields.",
      "problem": "The public body shape is a permissive broker-specific contract surface and direct state-mutation command, not a named domain/protocol ADT. Optional fields allow invalid combinations that the target order/action contract forbids.",
      "targetModel": "External simulator inputs are provider-declared observation/control leaves with explicit AccountScope. Each leaf has an exact instrument metadata extension and variant-specific fields; Deposit, Withdraw, and Trade are distinct simulator leaves, not universal Alice Order effects. If one distinct leaf is undeclared, only that leaf is absent; unsupported parameter variants are omitted from its exact schema while declared-leaf availability remains separate.",
      "pureTransitions": [
        "decodeExternalLeaf(wire, leafSchema) -> Result<ExternalObservation, ValidationFailed|RelationFailure>",
        "normalizeInstrument(metadata, providerExtension) -> Result<InstrumentRef, InvalidInstrumentMetadata> while retaining precise native extension data."
      ],
      "effectBoundary": "Only the simulator interpreter consumes native metadata; the application records an external observation and updates the mock projection. Production account projections consume broker observations, not arbitrary HTTP bodies.",
      "durability": "External fixture observations may be durable and deduplicated by fixtureId/sequence, but they do not create TransactionApproved, Dispatch, order locks, or compensation jobs.",
      "errors": [
        "InvalidInstrumentMetadata",
        "ExternalQuantityNotPositive",
        "ExternalTradePriceInvalid",
        "ValidationFailed(unknownVariantField)",
        "SimulatorCapabilityUnavailable"
      ],
      "preservedBehavior": [
        "保留 OPT/FUT/CASH/BOND/CRYPTO 元数据可供 simulator fixture 建模。",
        "保留 deposit/withdraw 不走 order pipeline，trade 可改变 cash/position。"
      ],
      "replacementSteps": [
        "Define separate schemas for each declared external leaf with required fields by variant.",
        "Preserve derivative metadata as an exact provider extension rather than a permissive SDK-shaped object.",
        "Keep external observation provenance separate from controlled effect provenance."
      ],
      "verificationCases": [
        "An invalid option/future relation is rejected at decode; a valid external deposit/trade updates only the scoped mock observation/projection and produces no transaction approval or dispatch job. If the Trade leaf is undeclared, only that distinct leaf is absent while Deposit/Withdraw remain independently discoverable; a declared but unavailable leaf reports named availability."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source accepts a permissive IBKR-like contract and multiple external mutations in one body family. Retain derivative fixture coverage while separating each provider-declared observation leaf from Alice order effects. The three simulator controls remain independent leaves, with parameter-schema absence distinguished from leaf absence and availability.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K04",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-3A43AEAF75",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-simulator.ts:81-101: resolveMock looks up a UTA by path id, performs an instanceof MockBroker check, and emits ad hoc string errors; parseBody catches invalid JSON and returns Zod issue arrays in a local {error, issues} envelope."
      ],
      "currentBehavior": "resolveMock looks up a UTA by path id, performs an instanceof MockBroker check, and emits ad hoc string errors; parseBody catches invalid JSON and returns Zod issue arrays in a local {error, issues} envelope.",
      "problem": "Resolution and validation are local imperative checks with concrete-class detection and unstructured errors; there is no typed authorization, capability, or protocol failure mapping.",
      "targetModel": "Simulator resolution is a typed capability-handle lookup followed by schema decoding: InvalidAccountId, SimulatorNotFound, NotSimulator, and Forbidden are distinct protocol variants. Concrete-class checks stay inside the simulator composition/interpreter. A resolved simulator handle always carries explicit AccountScope.",
      "pureTransitions": [
        "decodePath(path) -> Result<AccountId, InvalidAccountId>",
        "resolveCapability(accountId, principal, registry) -> Result<SimulatorHandle, SimulatorNotFound|NotSimulator|Forbidden>",
        "parseBody(wire, leafSchema) -> Result<DeclaredCommand, ProtocolDecodeFailure>."
      ],
      "effectBoundary": "Application resolves the selected simulator capability and transport maps typed failures to stable status/code; malformed JSON never reaches a provider interpreter.",
      "durability": "Decode/authorization failures write nothing. A successful control command may record a fixture observation identity and generation, not a trading transaction.",
      "errors": [
        "InvalidAccountId",
        "SimulatorNotFound",
        "NotSimulator(accountId)",
        "Forbidden(operation, principal)",
        "ProtocolDecodeFailure"
      ],
      "preservedBehavior": [
        "保留 missing id=400、unknown id=404、real broker=400 的语义差异，但转为 named errors。",
        "保留 malformed JSON 与 schema issues 不进入 MockBroker。"
      ],
      "replacementSteps": [
        "Replace instanceof/string helpers at the transport seam with registry capability lookup.",
        "Use the provisioned transport principal and explicit simulator operation authorization.",
        "Return one shared error envelope without exposing constructor names or schema internals."
      ],
      "verificationCases": [
        "Malformed JSON invokes no provider; a real account cannot become a simulator by body fields, and a principal lacking the simulator leaf gets Forbidden without leaking hidden account details."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source combines path lookup, instanceof MockBroker, and ad hoc Zod errors. Retain the distinct missing/non-simulator diagnostics but make capability resolution and parsing typed at the declared boundary.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K06",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-0E76C75480",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-simulator.ts:105-114: Creates an independent Hono app and lists only manager entries whose broker is a MockBroker, returning id/label summaries."
      ],
      "currentBehavior": "Creates an independent Hono app and lists only manager entries whose broker is a MockBroker, returning id/label summaries.",
      "problem": "This endpoint is a simulator fixture discovery surface, not a target transaction API; it must not be mistaken for the authoritative account/capability listing. The route has no authenticated transport principal or separate operation-authorization check; direct MockBroker mutation is mounted as an HTTP surface.",
      "targetModel": "SimulatorListResponse is a separate finite discovery projection for declared mock-simulator capabilities, containing explicit AccountScope-bound identity, label, generation, availability, and descriptor revision; it is not the production account list.",
      "pureTransitions": [
        "listSimulatorLeaves(principal, registry, coverage) -> Result<ListingResult<SimulatorSummary>, ForbiddenSimulatorAdmin|ProjectionUnavailable>",
        "encodeList(result) -> schema-valid simulator-only response; no concrete broker inspection in transport."
      ],
      "effectBoundary": "SimulatorControlService reads registry/projection state. The route is mounted only when that capability tree is enabled and does not call provider mutation.",
      "durability": "The list carries projection/generation evidence and is a read operation; listing never authorizes an order or creates transaction state.",
      "errors": [
        "ForbiddenSimulatorAdmin",
        "ProjectionUnavailable",
        "CapabilityAvailabilityUnknown"
      ],
      "preservedBehavior": [
        "保留 GET /utas 只列 MockBroker simulator，不列 real object。",
        "保留 id/label 对 dev UI 的发现能力。"
      ],
      "replacementSteps": [
        "Place the list under an explicit simulator/admin capability namespace.",
        "Derive summaries from declared capability metadata and authenticated scope.",
        "Keep production account listing and simulator discovery as separate leaves/mounts."
      ],
      "verificationCases": [
        "An authorized mock simulator is listed with id/label/generation; real and keyless data-only instances are absent from this list, and unauthorized callers receive a typed denial without existence leakage."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source intentionally lists only MockBroker-backed entries. Retain that dev discovery behavior while making it a separate capability projection instead of a second account manager view.",
      "contractAnchors": [
        "K01",
        "K02",
        "K05",
        "K06",
        "K12"
      ]
    },
    {
      "id": "MAP-D266753674",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-simulator.ts:116-121: Resolves a MockBroker and serializes its full simulator state directly through getSimulatorState without an application query or explicit response schema."
      ],
      "currentBehavior": "Resolves a MockBroker and serializes its full simulator state directly through getSimulatorState without an application query or explicit response schema.",
      "problem": "The response is an internal simulator state view and is not the target authoritative account or transaction projection. The route has no authenticated transport principal or separate operation-authorization check; direct MockBroker mutation is mounted as an HTTP surface.",
      "targetModel": "SimulatorStateView is a versioned finite pull result declared by the simulator capability: cash/mark/position/order values use refined domain schemas and exact provider extensions, plus generation, sequence, and as-of; it is not production AccountState. The state view is bound to explicit AccountScope and simulator generation.",
      "pureTransitions": [
        "projectSimulatorState(nativeState, outputSchema) -> Result<SimulatorStateView, NativeDecodeFailure|ProjectionFailure>",
        "classifyStateFreshness(state, generation) -> Fresh|Stale|Unavailable without treating stale as empty."
      ],
      "effectBoundary": "A simulator query application port reads the mock interpreter/projection and HTTP encodes its declared response; raw getSimulatorState class instances do not cross the boundary.",
      "durability": "The state view is a rebuildable fixture projection with sequence/generation. It does not write transaction intent or order WAL; production account state is a separate account-query capability.",
      "errors": [
        "SimulatorStateUnavailable",
        "ProjectionStale(instanceGeneration)",
        "NativeDecodeFailure(field)",
        "ForbiddenSimulatorAdmin"
      ],
      "preservedBehavior": [
        "保留 state endpoint 能显示 cash、markPrices、positions、pendingOrders。",
        "保留 unknown UTA=404、non-simulator=400 的可诊断性（typed）。"
      ],
      "replacementSteps": [
        "Define a simulator-state response codec and normalization step.",
        "Keep native MockBroker serialization behind the interpreter.",
        "Mount production account snapshots separately and preserve generation/cursor checks."
      ],
      "verificationCases": [
        "After mark price 80000, the state response contains an exact Decimal price and no SDK object; an old generation/cursor is reported stale rather than represented as current state."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source state endpoint is valuable fixture inspection but exposes raw simulator state. Retain its finite snapshot purpose while giving it a versioned capability-specific projection.",
      "contractAnchors": [
        "K02",
        "K03",
        "K04",
        "K05",
        "K06",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-21CA670156",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-simulator.ts:123-135: Validates a simulator mark price, calls MockBroker.setMarkPrice directly, allows the broker to auto-match pending orders, and returns the filled order ids; broker exceptions become a 400 string error."
      ],
      "currentBehavior": "Validates a simulator mark price, calls MockBroker.setMarkPrice directly, allows the broker to auto-match pending orders, and returns the filled order ids; broker exceptions become a 400 string error.",
      "problem": "Direct mark-price mutation and auto-fill are god-view test controls, not a durable TransactionCommand or broker dispatch with DispatchIdentity. The route has no authenticated transport principal or separate operation-authorization check; direct MockBroker mutation is mounted as an HTTP surface.",
      "targetModel": "SetMarkPrice is a simulator control leaf with explicit AccountScope, simulator key, and Price input and a named MarkPriceApplied result containing fixture observation identity and triggered order observations. Auto-match is simulator behavior, not PlaceOrder dispatch.",
      "pureTransitions": [
        "applyMarkPrice(state, key, price) -> Result<MarkPriceTransition, InvalidPrice|UnknownNativeKey|SimulatorOrderFillRejected>",
        "matchLimit(state, mark) -> deterministic Mock rule: BUY at or below limit, SELL at or above; fill price equals mark."
      ],
      "effectBoundary": "SimulatorControlService invokes the mock control port only under simulator capability authorization; production schedulers and transaction effect interpreters do not use this HTTP leaf as venue execution.",
      "durability": "Record fixture command/observation identity and generation if replay evidence is needed; do not create approval, dispatch, lock, or compensation records for this control.",
      "errors": [
        "InvalidPrice",
        "UnknownNativeKey",
        "SimulatorOrderFillRejected",
        "ForbiddenSimulatorMutation",
        "ObservationDuplicate"
      ],
      "preservedBehavior": [
        "保留 setMarkPrice 自动撮合 pending orders 并返回 filled ids。",
        "保留 broker exception 不再变成 generic Error.message，而成为 typed fill rejection。"
      ],
      "replacementSteps": [
        "Replace direct MockBroker call with a typed simulator control leaf.",
        "Encode triggered ids as declared BrokerOrderId values and record fixture observations.",
        "Keep this behavior out of the production order-effect path."
      ],
      "verificationCases": [
        "A pending BUY@79000 remains pending at mark 80000 and fills once at 79000; replaying one fixture command does not duplicate its observation, and a non-simulator scope cannot invoke the leaf."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source contains a precise deterministic matching rule worth preserving. Retain that simulator falsifier while clearly separating control observations from transaction dispatch.",
      "contractAnchors": [
        "K01",
        "K03",
        "K04",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-AB73E0949C",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-simulator.ts:137-149: Validates nativeKey and deltaPercent, calls MockBroker.tickPrice directly, and returns auto-filled order ids; missing mark price or other broker errors are converted to a 400 string error."
      ],
      "currentBehavior": "Validates nativeKey and deltaPercent, calls MockBroker.tickPrice directly, and returns auto-filled order ids; missing mark price or other broker errors are converted to a 400 string error.",
      "problem": "This is a deterministic test control and not a broker-observed market event or durable trading transaction. The route has no authenticated transport principal or separate operation-authorization check; direct MockBroker mutation is mounted as an HTTP surface.",
      "targetModel": "TickPrice is a simulator control leaf with a refined Percent input and the same declared MarkPriceApplied result as the simulator schema permits; it is not a market-data stream or a trading approval path. TickPrice is bound to explicit AccountScope.",
      "pureTransitions": [
        "tickPrice(state, key, percent) -> Result<TickPriceTransition, MissingMarkPrice|InvalidPercent>",
        "calculateDelta(current, percent) -> DecimalValue using the simulator leaf unit, never a JavaScript number."
      ],
      "effectBoundary": "The selected simulator interpreter applies the deterministic tick and optional auto-match; HTTP only decodes/encodes the leaf and maps typed failures.",
      "durability": "Input/output sequence and simulator generation may be retained for fixture replay. No transaction approval/job/WAL is created, and a market-data projection must not claim this is venue truth.",
      "errors": [
        "MissingMarkPrice(nativeKey)",
        "InvalidPercent(value)",
        "AutoMatchFailure",
        "SimulatorCapabilityUnavailable"
      ],
      "preservedBehavior": [
        "保留无 markPrice 先 set 才能 tick 的顺序约束。",
        "保留 tick 后自动撮合返回 filled ids。"
      ],
      "replacementSteps": [
        "Share refined Percent/Price codecs with set-mark price.",
        "Replace message/number-based errors with the declared error schema.",
        "Require explicit simulator capability in UI/test callers."
      ],
      "verificationCases": [
        "Tick without an initialized BTC mark returns MissingMarkPrice; +5% from 100 yields exact 105 and triggers the same mock rules, with no order transaction state changed."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source proves ordering and deterministic percentage behavior. Retain both constraints, but keep tick as a mock control leaf instead of presenting it as a universal broker/data guarantee.",
      "contractAnchors": [
        "K01",
        "K03",
        "K04",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-24F7AD27D6",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-simulator.ts:151-165: Resolves a MockBroker, accepts optional price/qty, directly forces a pending order fill, and returns {ok:true}; broker exceptions become a 400 string error."
      ],
      "currentBehavior": "Resolves a MockBroker, accepts optional price/qty, directly forces a pending order fill, and returns {ok:true}; broker exceptions become a 400 string error.",
      "problem": "Manual fill bypasses prepare, write-ahead dispatch, compensation, and remote-outcome reconciliation by mutating the simulator directly. The route has no authenticated transport principal or separate operation-authorization check; direct MockBroker mutation is mounted as an HTTP surface.",
      "targetModel": "ManualFill is a simulator-only control leaf with explicit AccountScope and BrokerOrderId and optional declared Price/Quantity fields, returning a typed FillObservation/remaining state. It bypasses venue transactions by design and must not be exposed as production Cancel/Fill authority.",
      "pureTransitions": [
        "fillSimulatorOrder(state, id, options) -> Result<FillObservation, OrderNotPending|QuantityExceeded|InvalidPrice|InvalidQuantity>",
        "evolveFill(state, observation) -> state with Submitted remainder or Filled terminal status and quantity-weighted values."
      ],
      "effectBoundary": "The mock control interpreter performs the fixture mutation; production order projections consume broker observations associated with a transaction effect, not this HTTP command.",
      "durability": "Fixture observationId/order version makes replay and partial fill evidence durable when needed. It never writes TransactionApproved, DispatchStarted, or a fabricated remote Ack.",
      "errors": [
        "OrderNotPending(orderId)",
        "FillQuantityExceeded(orderId)",
        "NonPositiveQuantity",
        "InvalidPrice",
        "SimulatorCapabilityUnavailable"
      ],
      "preservedBehavior": [
        "保留 optional price 默认 mark/limit/100 以及 optional partial qty。",
        "保留 unknown orderId 400。"
      ],
      "replacementSteps": [
        "Decode path/order id as the simulator/provider identity schema.",
        "Return named fill state rather than `{ok:true}`.",
        "Keep production fills on the provider observation path and test partial/duplicate fixture behavior separately."
      ],
      "verificationCases": [
        "A full pending fill removes it from the pending view; a partial fill leaves Submitted with remaining/cumulative values; unknown or terminal ids return a typed error without state change."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source proves useful full/partial simulator fill controls and precise failure behavior. Retain them as fixture evidence while refusing to conflate the control with transaction execution.",
      "contractAnchors": [
        "K03",
        "K04",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-DFB69CBAAD",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-simulator.ts:167-179: Directly invokes MockBroker.cancelPendingOrder for a forced simulator cancellation and emits an unstructured 400 on failure.",
        "services/uta/src/domain/trading/brokers/mock/MockBroker.ts:623-628: cancelPendingOrder only checks that the order exists, then unconditionally writes status=Cancelled even for a terminal order."
      ],
      "currentBehavior": "The route resolves a MockBroker and calls cancelPendingOrder; MockBroker only checks existence and then unconditionally sets status=Cancelled, so the god-view command can overwrite a Filled/Cancelled order.",
      "problem": "Simulator force-cancel calls MockBroker.cancelPendingOrder, whose source implementation overwrites any existing order status without checking Submitted; this unsafe fixture behavior must be replaced by an explicit pending-only control command, separate from production CancelOrder.",
      "targetModel": "SimulatorCancel is a distinct pending-only control leaf with explicit AccountScope and BrokerOrderId. It may transition Submitted to fixture-cancelled, but has no relationship to a provider-declared production cancel leaf unless that leaf is separately exposed and transaction-wrapped.",
      "pureTransitions": [
        "cancelSimulatorOrder(state, id) -> Result<SimulatorOrderCancelled, OrderNotFound|OrderAlreadyTerminal>",
        "applyPendingOnly(state, id) -> state unchanged on Filled/Cancelled and no inferred terminal overwrite."
      ],
      "effectBoundary": "SimulatorControlService calls only the mock fixture interpreter. A real provider cancellation, if declared, uses its own Order semantic schema and optional withTransaction wrapper, never this bypass.",
      "durability": "Fixture order version/observation can be recorded for replay; no production cancellation transaction, approval, compensation, or dispatch job is created.",
      "errors": [
        "OrderNotFound(orderId)",
        "OrderAlreadyTerminal(status)",
        "ForbiddenProductionCancel",
        "SimulatorCapabilityUnavailable"
      ],
      "preservedBehavior": [
        "保留强制取消 pending simulator order 的清理用途。",
        "保留不存在 order 返回 400，改为 typed。"
      ],
      "replacementSteps": [
        "Add a simulator-only operation capability and pending-state guard.",
        "Encode the named terminal result/error rather than constructor/message text.",
        "Do not reuse this handler in generic trading routes."
      ],
      "verificationCases": [
        "Submitted becomes Cancelled; Filled or already Cancelled remains unchanged and returns OrderAlreadyTerminal; a real account has no simulator cancel leaf."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source exposes an unsafe overwrite of terminal status, so the pending-only safety rule is a valuable retained fixture constraint. Reframe it as a simulator leaf distinct from production cancel.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-1C7C835F5A",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-simulator.ts:181-193: Directly injects a wallet-source position into MockBroker through externalDeposit, bypassing the order pipeline, then returns {ok:true}; failures become string 400 responses.",
        "services/uta/src/domain/trading/brokers/mock/MockBroker.ts:680-697: externalDeposit accepts positive quantity, leaves cash unchanged, and uses markPrice or Decimal(0) as wallet avgCost for a new position."
      ],
      "currentBehavior": "Directly injects a wallet-source position into MockBroker through externalDeposit, bypassing the order pipeline, then returns {ok:true}; failures become string 400 responses.",
      "problem": "The route directly injects a wallet-source position and returns {ok:true}; when no mark exists the MockBroker source uses zero as avgCost, so the target must make missing-mark placeholder policy explicit rather than presenting zero as valuation truth.",
      "targetModel": "ExternalDeposit is a simulator observation leaf with explicit AccountScope-bound instrument/Quantity, fixtureId, timestamp, source, and an explicit cost-basis variant MarkPresent | MissingMark(Indeterminate). It is data/accounting evidence, not an Alice order effect.",
      "pureTransitions": [
        "applyExternalDeposit(observation, projection) -> Result<Projection, DuplicateFixture|InvalidQuantity|InstrumentResolutionFailure>",
        "deriveCostBasis(observation, mark) -> MarkPresent(price)|MissingMark(Indeterminate) without substituting numeric zero."
      ],
      "effectBoundary": "Only an authorized simulator observation injector may create the fixture; accounting/projection consumes the typed observation. Production external transfers come from provider observations, not HTTP body mutation.",
      "durability": "Persist fixture identity/sequence before updating the simulator projection so replay is idempotent; do not create order WAL, approval, dispatch, locks, or compensation.",
      "errors": [
        "NonPositiveQuantity",
        "InstrumentResolutionFailure",
        "ObservationDuplicate(fixtureId)",
        "SimulatorAdminForbidden",
        "CostBasisIndeterminate"
      ],
      "preservedBehavior": [
        "保留 deposit 不扣 cash、wallet-source tag、markPrice 作为 placeholder avgCost。",
        "保留 external event 绕过 order pipeline 的模拟含义。"
      ],
      "replacementSteps": [
        "Name the observation source and stable identity.",
        "Wrap MockBroker mutation in simulator control and projection adapters.",
        "Keep missing mark explicit in response and accounting state; never expose zero as valuation truth."
      ],
      "verificationCases": [
        "After mark BTC=80000, deposit 1.0093 keeps cash unchanged and records MarkPresent(80000); without a mark it records MissingMark/Indeterminate; replaying the fixture does not double quantity."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source’s wallet-source and cash invariant are useful simulator evidence, while its zero placeholder is unsafe. Retain the behavior with an explicit indeterminate cost-basis variant and observation durability.",
      "contractAnchors": [
        "K03",
        "K04",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-2444CEB754",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-simulator.ts:195-207: Directly injects a simulator withdrawal by calling MockBroker.externalWithdraw(nativeKey, quantity), bypassing transaction and accounting services, with string 400 errors.",
        "services/uta/src/domain/trading/brokers/mock/MockBroker.ts:700-710: externalWithdraw subtracts requested quantity and deletes the position whenever the remainder is <=0; it does not reject an over-withdrawal or non-positive quantity."
      ],
      "currentBehavior": "The route passes nativeKey and quantity directly to MockBroker.externalWithdraw; that method subtracts without quantity/exposure validation and deletes the position whenever the remainder is non-positive, then returns {ok:true}.",
      "problem": "The current external-withdraw route can turn an over-sized or non-positive request into deletion/negative intermediate exposure and reports only {ok:true}; the target must validate Quantity against the observed position before recording a typed external observation.",
      "targetModel": "ExternalWithdraw is a simulator observation leaf with explicit AccountScope-bound instrument/Quantity and fixture identity. Its pure accounting transition validates existing exposure before applying a delta; it is not a production cancel/order transaction.",
      "pureTransitions": [
        "applyExternalWithdraw(observation, projection) -> Result<ExposureDelta, NoPosition|InsufficientQuantity|InvalidQuantity|DuplicateFixture>",
        "foldExposure(delta) -> non-negative remaining exposure or unchanged state on rejection."
      ],
      "effectBoundary": "Simulator observation service applies the fixture and updates its projection; real-provider withdrawals arrive through broker account/position observations and are reconciled by the accounting boundary.",
      "durability": "Persist observation identity/sequence before projection update and retain rejected/duplicate status as typed evidence if policy requires; no approval, order lock, dispatch, or compensation state.",
      "errors": [
        "NoPosition(nativeKey)",
        "InsufficientQuantity(observed, requested)",
        "NonPositiveQuantity",
        "ObservationDuplicate",
        "SimulatorAdminForbidden"
      ],
      "preservedBehavior": [
        "保留 cash 不变、全部提取删除 position、部分提取保留 wallet cost source；将当前 oversell/delete permissiveness 改为 typed InsufficientQuantity rejection。"
      ],
      "replacementSteps": [
        "Decode native key and quantity into a scoped observation.",
        "Replace direct mutate/string error with a typed accounting fold.",
        "Reject oversize/non-positive requests without deleting or creating a negative position."
      ],
      "verificationCases": [
        "With 1 BTC, withdraw .4 leaves .6 and cash unchanged; withdraw 2 or a non-positive amount returns a typed rejection and leaves exposure and cursor unchanged."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source reveals a concrete over-withdrawal/deletion defect while preserving the intended partial/full withdrawal behavior. Retain valid deltas and make rejection a pure observation-fold boundary.",
      "contractAnchors": [
        "K03",
        "K04",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-6CE9D89D9B",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-simulator.ts:209-221: Directly injects a user-executed exchange trade into MockBroker.externalTrade with native contract metadata and returns {ok:true}; errors are unstructured 400 strings."
      ],
      "currentBehavior": "Directly injects a user-executed exchange trade into MockBroker.externalTrade with native contract metadata and returns {ok:true}; errors are unstructured 400 strings.",
      "problem": "The route directly changes position/cash state with no durable transaction/observation identity, accounting event, compensation, or unknown-outcome handling. The route has no authenticated transport principal or separate operation-authorization check; direct MockBroker mutation is mounted as an HTTP surface.",
      "targetModel": "ExternalTrade is a simulator observation leaf with an explicit AccountScope-bound instrument, side, Quantity, Price, exact metadata extension, source, timestamp, and fixture identity. BUY/SELL are observed external activity, not controlled order dispatch.",
      "pureTransitions": [
        "applyExternalTrade(observation, projection) -> Result<ExternalTradeProjection, InvalidTrade|OversellExposure|DuplicateFixture>",
        "foldCostAndCash(current, observation) -> deterministic Decimal/WAC transition preserving source provenance."
      ],
      "effectBoundary": "The simulator control service is the only HTTP-facing injector; accounting/reconciliation projects the typed external observation, while real fills use provider observation identities at their own boundary.",
      "durability": "FixtureId and observation sequence deduplicate cash/position changes and permit rebuild. No TransactionApproved, DispatchStarted, order WAL, or compensation is inferred from the HTTP result.",
      "errors": [
        "NonPositiveQuantity",
        "InvalidPrice",
        "OversellExposure",
        "InstrumentMetadataInvalid",
        "ObservationDuplicate",
        "SimulatorAdminForbidden"
      ],
      "preservedBehavior": [
        "保留 BUY .5 BTC@60000 使 cash 100k->70k、avgCost=60000、wallet tag。",
        "保留外部 SELL 开空与 Alice order SELL 无仓拒绝的语义差异。"
      ],
      "replacementSteps": [
        "Define the external trade observation schema and accounting fold.",
        "Keep native metadata in the provider extension decoder and redact SDK objects from responses.",
        "Exercise replay/unknown/partial observation separately from controlled order execution."
      ],
      "verificationCases": [
        "BUY .5 BTC@60000 changes cash 100000 to 70000 with wallet provenance and exact cost; replay does not double debit, and oversell is rejected without pretending a dispatch occurred."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source provides precise wallet trade accounting evidence and explicitly differs from an Alice order. Retain those accounting semantics while making provenance and idempotency explicit outside transaction approval.",
      "contractAnchors": [
        "K03",
        "K04",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-D034C92CBD",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading-wallet.spec.ts:1-14: Builds a minimal UTAEngineContext whose manager returns one arbitrary mock UTA and imports the legacy TradingGit PendingHashConflictError; tests exercise createTradingRoutes directly without authentication or real transaction services."
      ],
      "currentBehavior": "Builds a minimal UTAEngineContext whose manager returns one arbitrary mock UTA and imports the legacy TradingGit PendingHashConflictError; tests exercise createTradingRoutes directly without authentication or real transaction services.",
      "problem": "wallet route 测试 cast 一个只含 manager.get 的 UTAEngineContext，并导入 PendingHashConflictError；它无法观察 authenticated principal、SQLite receipt、jobs/locks 或 broker interpreter 的真实边界。",
      "targetModel": "Wallet-route integration fixture composes only the selected transaction-wrapped effect leaf with transport verifier, typed codecs, writer, and projection reader. It does not require a complete UTAEngineContext or a universal broker handler set. Any account-backed fixture binds Principal and explicit AccountScope.",
      "pureTransitions": [
        "decodeCommand(wire, leafSchema) -> Result<AuthorizedCommand, Validation|AuthorizationFailure>",
        "evolveEffect(state, command) -> Result<Events, DomainFailure> for that declared effect only."
      ],
      "effectBoundary": "Test composition may use an in-process writer and selected Mock/provider interpreter, but HTTP crosses the same Principal/schema boundary as production. Data-only routes use separate query fixtures.",
      "durability": "The fixture observes authoritative events, jobs, locks, and receipts only for the transaction wrapper under test; no Git object or in-memory OpenOrder is authority.",
      "errors": [
        "StorageFailure",
        "InvalidState",
        "AuthorizationFailure",
        "ProtocolDecodeFailure",
        "CapabilityNotDeclared(effect)"
      ],
      "preservedBehavior": [
        "保留该文件覆盖 wallet approval route 的行为意图。",
        "保留测试隔离与可控 broker failure，但把 stub 替成 typed interpreter。"
      ],
      "replacementSteps": [
        "Build a shared authenticated request fixture around a declared effect descriptor.",
        "Replace PendingHashConflictError assertions with revision/digest/policy evidence where the effect declares them.",
        "Assert durable store state and isolate each fixture scope."
      ],
      "verificationCases": [
        "A selected effect command can be followed by one durable receipt query; invalid or missing leaves do not invoke a provider and one test cannot leak events/locks into another scope."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source uses a casted manager context and legacy TradingGit error. Retain the wallet-route test intent and isolation, but only compose durable transaction machinery for a capability explicitly wrapped withTransaction.",
      "contractAnchors": [
        "K01",
        "K03",
        "K07",
        "K08",
        "K11",
        "K12"
      ]
    },
    {
      "id": "MAP-B198583232",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading-wallet.spec.ts:16-39: Asserts push and reject return 409 PENDING_HASH_REQUIRED and do not call mutation methods when the legacy pending hash is absent."
      ],
      "currentBehavior": "Asserts push and reject return 409 PENDING_HASH_REQUIRED and do not call mutation methods when the legacy pending hash is absent.",
      "problem": "测试把缺 expectedPendingHash 映射成 PENDING_HASH_REQUIRED；该旧检查只保护 Git staging，不能替代 transactionId/revision/digest/expiry/principal 的 canonical command 校验。",
      "targetModel": "Approval/review input is the schema of a selected transaction wrapper: transaction identity, revision/digest/expiry and actor/policy binding are required only for that controlled effect. A Git pending hash is not a transport-wide approval contract; data leaves have no such fields. The selected account effect carries explicit AccountScope; data leaves have no approval scope.",
      "pureTransitions": [
        "decodeApproval(wire, effectSchema) -> Result<ApprovalBinding, ValidationFailed>",
        "compareRevisionAndDigest(current, binding) -> Result<AuthorizedApproval, VersionConflict|DigestMismatch|Expired|Unauthorized> before effect events."
      ],
      "effectBoundary": "HTTP decodes the selected effect command and application atomically CAS-checks the durable transaction record; the provider interpreter is not called on missing/stale binding.",
      "durability": "A failed approval/rejection leaves the effect record, jobs, and locks unchanged. A successful approval creates the wrapper’s durable receipt/jobs only after writer commit; query leaves remain read-only.",
      "errors": [
        "MissingApprovalBinding",
        "VersionConflict",
        "PlanDigestMismatch",
        "ApprovalExpired",
        "UnauthorizedOperation",
        "CapabilityNotDeclared(effect)"
      ],
      "preservedBehavior": [
        "保留缺少 expected pending hash 时不会调用 push/reject。",
        "保留旧 hash conflict 的 409 类语义，改为 typed transaction conflict。"
      ],
      "replacementSteps": [
        "Remove PENDING_HASH_REQUIRED as a public contract.",
        "Generate approval/review codecs from the selected transaction wrapper and keep expected revision explicit.",
        "Cover no-dispatch rejection and binding conflicts with durable state assertions."
      ],
      "verificationCases": [
        "A body containing only the old hash is rejected with MissingApprovalBinding; stale revision/digest creates no event or provider call; a valid binding reaches the wrapper’s queued state."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source’s no-mutation behavior for absent expected hash is worth keeping, but its Git shape is not. Reframe the guard as effect-specific schema/CAS validation and leave non-effect capabilities free of approval machinery.",
      "contractAnchors": [
        "K03",
        "K08",
        "K09",
        "K10",
        "K12"
      ]
    },
    {
      "id": "MAP-6AF6021791",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading-wallet.spec.ts:41-57: Stubs push to throw PendingHashConflictError and verifies a stale expected hash yields 409 PENDING_HASH_CONFLICT while push receives the stale hash."
      ],
      "currentBehavior": "Stubs push to throw PendingHashConflictError and verifies a stale expected hash yields 409 PENDING_HASH_CONFLICT while push receives the stale hash.",
      "problem": "测试以 PendingHashConflictError 模拟 stale hash；冲突是否保持 durable transaction version、无 jobs/locks/events，不能由一次 vi throw 得出。",
      "targetModel": "Stale approval is an effect-wrapper CAS conflict over the declared transaction identity/revision/digest; it is not a PendingHashConflictError and is not a reason to add a rejection handler to every provider. The selected account effect retains explicit AccountScope during the CAS decision.",
      "pureTransitions": [
        "compareBinding(current, expected) -> Result<AuthorizedEffectApproval, VersionConflict|PlanDigestMismatch>",
        "onConflict(state) -> unchanged durable state with a typed protocol response."
      ],
      "effectBoundary": "The writer owns atomic compare-and-append for the selected effect; HTTP only encodes the conflict and never forwards a legacy hash to a provider.",
      "durability": "No partial approval event, job, lock, or receipt is committed on mismatch; the original plan remains queryable for a later explicit review command.",
      "errors": [
        "VersionConflict",
        "PlanDigestMismatch",
        "StaleRevision",
        "StorageUnavailable"
      ],
      "preservedBehavior": [
        "保留 stale client 不得把 hash change 当 success。",
        "保留调用方能看到冲突并重新读状态。"
      ],
      "replacementSteps": [
        "Replace the stubbed PendingHashConflictError with a writer-backed CAS scenario.",
        "Preserve command identity/idempotency for retries of the selected effect.",
        "Verify provider dispatch remains zero on conflict."
      ],
      "verificationCases": [
        "A stale expected revision returns a stable conflict, leaves the effect projection unchanged, and produces no broker/provider mutation; rereading the plan exposes the current revision."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source proves stale optimistic state must not be treated as success, but only with a Git hash and vi stub. Retain the conflict invariant at the selected durable effect wrapper and remove hash-specific/global assumptions.",
      "contractAnchors": [
        "K03",
        "K08",
        "K09",
        "K10",
        "K12"
      ]
    },
    {
      "id": "MAP-FE1C608463",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading-wallet.spec.ts:59-78: Stubs push to return a Git PushResult and verifies matching expectedPendingHash produces HTTP 200 and forwards the hash to push."
      ],
      "currentBehavior": "Stubs push to return a Git PushResult and verifies matching expectedPendingHash produces HTTP 200 and forwards the hash to push.",
      "problem": "测试把 matching hash 的 Git PushResult 当作 HTTP success；这没有区分 approval receipt、DispatchStarted、Ack 和 fill，也无法证明 response 丢失后的 command convergence。",
      "targetModel": "Approve returns the selected transaction wrapper’s durable receipt/schema (command identity, effect revision, descriptor/policy evidence, queued work). It never returns a Git PushResult or claims remote Ack/fill; auto approval is a server-derived policy decision. The selected account effect binds the receipt to explicit AccountScope.",
      "pureTransitions": [
        "approvePrepared(state, binding, policy) -> Result<QueuedReceipt, InvalidState|Expired|CapabilityRevoked|AuthorizationFailure>",
        "projectReceipt(receiptSchema) -> protocol result distinct from later Ack/observation."
      ],
      "effectBoundary": "The writer commits approval and the wrapper’s dispatch plan/jobs; a scheduler/provider interpreter runs later. HTTP ends at the durable receipt, while a data leaf remains a finite read.",
      "durability": "Approval, effect jobs, locks, command receipt, and revision are atomically durable for the selected effect. Remote Ack, fill, or unknown outcome is persisted only by the subsequent provider observation path.",
      "errors": [
        "InvalidState",
        "ApprovalExpired",
        "CapabilityRevoked",
        "StorageUnavailable",
        "OutcomeUnknown"
      ],
      "preservedBehavior": [
        "保留 matching expected hash historically permits push；new success means approved/queued, with Human mode when AI trading is disabled and AuthorizedAuto mode only from server policy when enabled。",
        "保留 UI 可在成功后查询 transaction status。"
      ],
      "replacementSteps": [
        "Migrate push callers to the selected approve/review capability.",
        "Derive human/authorized-auto policy from the server and principal binding, never from a body boolean.",
        "Separate receipt polling and provider outcome observation from approval response."
      ],
      "verificationCases": [
        "A valid approval returns one queued receipt and no PushResult; a body-only allow-AI flag cannot authorize; response loss converges to the same receipt and later unknown outcomes remain unknown."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source equates matching pending hash with HTTP success. Retain the approval wall and post-success polling intent, but move success to a typed durable receipt of one selected effect wrapper.",
      "contractAnchors": [
        "K03",
        "K08",
        "K09",
        "K10",
        "K12"
      ]
    },
    {
      "id": "MAP-F3C5E04898",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:1-11: The boundary imports Hono/Zod but also UnifiedTradingAccount, BrokerError, order-entry, TradingGit hash handling, order-history projections, and a domain contract-search helper; route composition therefore reaches legacy execution and Git persistence APIs directly."
      ],
      "currentBehavior": "The boundary imports Hono/Zod but also UnifiedTradingAccount, BrokerError, order-entry, TradingGit hash handling, order-history projections, and a domain contract-search helper; route composition therefore reaches legacy execution and Git persistence APIs directly.",
      "problem": "The HTTP module directly imports old UTA/Git execution and projection helpers rather than target application/protocol abstractions, and BrokerError is collapsed later into ad hoc HTTP fields.",
      "targetModel": "HTTP composition depends on protocol codecs, transport verification, and selected application capability ports. It must not import a concrete UTA, TradingGit, broker SDK, or a hand-maintained global action map; each provider route tree is mounted from declared leaves. Account-backed capability requests carry explicit AccountScope; public data leaves remain Public.",
      "pureTransitions": [
        "decodeRoute(method, path, wire, descriptor) -> Result<CapabilityRequest, ProtocolDecodeFailure>",
        "encodeApplicationResult(result, descriptor) -> ProtocolResponse while preserving declared output/error schema and availability."
      ],
      "effectBoundary": "The composition root injects the capability registry and application ports; HTTP performs no native compilation, SQL journal write, Git access, or broker lifecycle. Provider adapters remain behind their own SPI/process boundary.",
      "durability": "Query routes read projections/streams and carry checkpoint/freshness; only a selected controlled-effect port writes durable intent/approval/dispatch/recovery records through its writer.",
      "errors": [
        "ProtocolDecodeFailure",
        "UnauthenticatedTransport",
        "CapabilityNotDeclared",
        "CapabilityUnavailable",
        "StorageUnavailable"
      ],
      "preservedBehavior": [
        "保留现有 Hono route mount 与 UI/SDK URL 迁移窗口。",
        "保留 query 与 mutation 分离，删除 direct legacy authority。"
      ],
      "replacementSteps": [
        "Define the fixed descriptor/transport outer envelope and derive route codecs from provider leaves.",
        "Migrate main composition to inject ports instead of UTAEngineContext concrete objects.",
        "Delete direct legacy imports only after caller cutover, preserving importer/read evidence as needed."
      ],
      "verificationCases": [
        "An authenticated request reaches exactly the selected application port and returns its declared schema; a provider with no leaf has no route, and a transaction-wrapped effect’s receipt is observable without exposing UTA/Git/native objects."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source import list is direct evidence of a transport-to-domain leak. Retain the route mount/caller migration concern, but replace the complete SDK assumption with descriptor-driven capability composition.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K06",
        "K07",
        "K11",
        "K12"
      ]
    },
    {
      "id": "MAP-3CBE1C6172",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:13-60: Local Zod schemas validate the legacy wallet/order request shapes. Place-order uses optional quantity/cash/price/order fields with a free-form orderType and aliceId; close/cancel retain legacy ids and commit message requirements."
      ],
      "currentBehavior": "Local Zod schemas validate the legacy wallet/order request shapes. Place-order uses optional quantity/cash/price/order fields with a free-form orderType and aliceId; close/cancel retain legacy ids and commit message requirements.",
      "problem": "Optional-field bags and free-form orderType make the route-local schema unable to describe the exact provider capability; aliceId/message are legacy surface concerns, and the schema is not the selected declaration-derived protocol.",
      "targetModel": "Route request schemas are derived per capability leaf. A provider may declare an Order semantic unit and compose sizing, terms, relations, or native extensions with HOFs; no global Place/Modify/Close/Cancel product or Cartesian order enum is required. If the Order leaf is declared but one parameter variant is unsupported, that variant is omitted from the leaf's exact schema and does not remove other variants; only an absent capability leaf has no route. Audit rationale and Principal remain separate metadata. Account-backed order leaves carry explicit AccountScope; public data leaves remain scope-appropriate.",
      "pureTransitions": [
        "decodeLeafInput(wire, descriptor.inputSchema) -> Result<DeclaredInput, ValidationFailed>",
        "composeDeclaredOrder(baseSchema, providerExtensions) -> Result<ComposedSchema, SchemaConflict|RelationFailure>; reject only relations declared by that leaf."
      ],
      "effectBoundary": "Transport decodes the chosen leaf, authenticates Principal, and calls its application port; native request compilation and capability checks happen in the selected adapter, not in route-local Zod or a universal switch.",
      "durability": "Data leaf requests do not write transaction state. A controlled order/effect leaf stores its serializable intent, revision, and plan evidence only through withTransaction; free-form message belongs to optional audit metadata, not native payload.",
      "errors": [
        "ValidationFailed(path, issue)",
        "ScopeRequired|UnknownScope",
        "RelationFailure",
        "CapabilityUnavailable(capability)",
        "ProtocolDecodeFailure"
      ],
      "preservedBehavior": [
        "保留旧输入中已被调查的 trailing、GTD/outsideRth、parent/OCA、TP/SL 与 Decimal string precision；仅在选定 provider schema 声明时保留。",
        "将 commit message 与 action/approval payload 分离。"
      ],
      "replacementSteps": [
        "Replace route-local optional bags with leaf schemas exported from declarations.",
        "Use small local unions (for example sizing or Quantity|All) only when the selected provider schema requires them.",
        "Migrate Alice identifiers to refined scope/instrument values and preserve provider extensions exactly."
      ],
      "verificationCases": [
        "A provider Order leaf that declares a sizing XOR and protection extension round-trips the declared fields; an unsupported protection parameter is absent from that leaf's input schema and an attempted field is rejected while the leaf and its other declared variants remain discoverable; only a provider without the Order capability has no route, and invalid input causes no effect/journal mutation."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source’s optional/free-form bag is the real defect; the prior target incorrectly made a closed action universe authoritative. Reframe validation around the selected declaration and optional transaction wrapper while preserving input precision and relations. The schema rule now keeps unsupported parameter variants out of the selected schema without deleting the remaining Order leaf.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K04",
        "K06",
        "K07",
        "K08",
        "K12"
      ]
    },
    {
      "id": "MAP-75C6D069F1",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:61-78: Maps executeOneShotOrder's stage/commit/push string error union to HTTP 400/500 and returns {error, phase}; successful requests return the push result directly."
      ],
      "currentBehavior": "Maps executeOneShotOrder's stage/commit/push string error union to HTTP 400/500 and returns {error, phase}; successful requests return the push result directly.",
      "problem": "runOneShot 把 stage、commit、push 的错误 union 直接映射为 phase 与 400/500，并返回 push result；这种 transport phase 不能表达 durable preparation、approval gate 或 Unknown outcome。",
      "targetModel": "The one-shot phase map is removed from the target contract. A route either invokes a finite data leaf or a selected capability wrapped withTransaction; that wrapper exposes explicit prepare/approve/review/recovery states, while provider-specific domain errors remain in the declared error schema. The selected account effect retains explicit AccountScope; public data routes have no effect state.",
      "pureTransitions": [
        "decodeSelectedRequest(descriptor, wire) -> Result<AcceptedRequest, ProtocolFailure> without combining approval and provider dispatch",
        "evolveWrappedEffect(state, event) -> Result<NextState, EffectFailure> with exhaustive handling of known rejection versus OutcomeUnknown."
      ],
      "effectBoundary": "HTTP calls an application port; only the selected transaction wrapper creates durable jobs and the scheduler/provider interpreter performs controlled effects. Data query/stream handlers never acquire this machinery.",
      "durability": "Prepare/review/approval receipts and dispatch identities are durable for wrapped effects; response loss is resolved by command identity. A provider disconnect after dispatch is OutcomeUnknown and cannot be mapped to a generic 500 retry.",
      "errors": [
        "InvalidState",
        "ValidationFailed",
        "PreconditionFailed",
        "CapabilityUnavailable",
        "StorageUnavailable",
        "OutcomeUnknown(dispatchId)"
      ],
      "preservedBehavior": [
        "保留 route 能报告阶段性失败，但改用 state/failure variant。",
        "保留 commit response loss 可由 commandId 查询收敛；`allowAiTrading` disabled remains AwaitingApproval, enabled may request auto-approval only after Principal/policy checks."
      ],
      "replacementSteps": [
        "Delete executeOneShotOrder and PHASE_STATUS as authority.",
        "Expose explicit draft/prepare/approve/review/query leaves only where declared.",
        "Retain phase failure evidence as typed effect-state transitions and keep data routes finite/read-only."
      ],
      "verificationCases": [
        "Preparing never calls the provider; approval creates one durable job; response loss returns the same receipt; provider rejection has evidence and a post-dispatch disconnect remains Reconciling/Unknown rather than a blind 500 retry."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source collapses stage/commit/push into route status. Retain the need to report progression and failures, but replace legacy phases with the selected effect wrapper’s durable state machine and keep data capabilities outside it.",
      "contractAnchors": [
        "K01",
        "K03",
        "K06",
        "K08",
        "K09",
        "K10",
        "K12"
      ]
    },
    {
      "id": "MAP-D58BA63EC0",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:80-82: Defines a local allow-list for the contract-search query and uses a protocol AssetClassHint type, but it is not itself a decoded request schema or capability ADT."
      ],
      "currentBehavior": "Defines a local allow-list for the contract-search query and uses a protocol AssetClassHint type, but it is not itself a decoded request schema or capability ADT.",
      "problem": "A local string Set and cast only filters one query hint; it does not represent target capability derivation or validate the full contract-search request.",
      "targetModel": "AssetClassHint is a declared search-input refinement, not a capability map. The selected provider search leaf may accept it, normalize it, or omit the field; an unsupported hint parameter is omitted from the selected input schema rather than removing the search leaf; a declared but disconnected leaf reports availability separately, and neither outcome is a route-local global Set. A provider-declared account search leaf carries explicit AccountScope, while a public leaf remains Public.",
      "pureTransitions": [
        "decodeAssetHint(raw, leafSchema) -> Result<DeclaredHint, InvalidAssetClass>",
        "normalizeSearchInput(input, providerRules) -> Result<ProviderSearchInput, EmptyPattern|ValidationFailed> with the provider’s exact extension."
      ],
      "effectBoundary": "The application search capability owns normalization and capability metadata; HTTP decodes the selected schema and does not decide broker support by a cast.",
      "durability": "Search is a finite data pull/cache observation; capability revision/as-of may be returned, but no transaction intent or order approval is created.",
      "errors": [
        "InvalidAssetClass(value)",
        "EmptyPattern",
        "CapabilityNotDeclared(search)",
        "SearchUnavailable(context)"
      ],
      "preservedBehavior": [
        "保留 equity/crypto/currency/commodity/unknown hints 与 crypto normalization。",
        "保留 invalid hint 不使 route crash，但将其变成稳定 validation failure 而不是静默当作 unknown。"
      ],
      "replacementSteps": [
        "Move the allowed values into the shared/provider-declared schema.",
        "Preserve crypto/currency normalization only where its provider rule is declared.",
        "Return availability/coverage explicitly instead of coercing invalid hints to unknown."
      ],
      "verificationCases": [
        "A declared crypto hint normalizes BTCUSD according to its provider rule; omitted hint uses the declared default; an undeclared hint parameter is absent from the selected schema and an attempted field fails validation; an invalid declared value never becomes a capability claim."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source’s normalization hint is useful but its local allow-list/cast is not authoritative. Retain the hint semantics under provider schema and separate capability declaration from availability. An omitted search parameter variant is not treated as absence of the whole search capability.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K04",
        "K05",
        "K12"
      ]
    },
    {
      "id": "MAP-0FC7B6DD68",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:84-88: Extracts and trims a legacy expectedPendingHash from an unknown request body, returning undefined for absent/non-string values."
      ],
      "currentBehavior": "Extracts and trims a legacy expectedPendingHash from an unknown request body, returning undefined for absent/non-string values.",
      "problem": "readExpectedPendingHash 从 unknown body 中提取并 trim Git hash；它让 process-local staging hash 成为 approval 身份，绕过 canonical plan digest、revision、expiry 和 actor binding。",
      "targetModel": "Legacy expectedPendingHash is removed from the transport schema. Only a selected transaction wrapper may declare revision, planDigest, expiry, and actor/policy binding; ordinary data leaves never receive approval fields. A selected account effect binds any wrapper approval to explicit AccountScope.",
      "pureTransitions": [
        "decodeWrapperApproval(wire, descriptor) -> Result<ApprovalBinding, ValidationFailed>",
        "verifyBinding(current, binding, now) -> Result<AuthorizedApproval, VersionConflict|DigestMismatch|ApprovalExpired|UnauthorizedOperation>."
      ],
      "effectBoundary": "The selected application transaction port reads durable wrapper state and calls the writer; HTTP never reads Git pending state or invokes a provider from a body hash.",
      "durability": "Git hashes may remain historical display evidence, but are not transaction identity or authorization. Successful wrapper approval creates a durable receipt only after atomic commit; malformed binding creates none.",
      "errors": [
        "MissingApprovalBinding",
        "DigestMismatch",
        "VersionConflict",
        "ApprovalExpired",
        "UnauthorizedOperation",
        "CapabilityNotDeclared(effect)"
      ],
      "preservedBehavior": [
        "保留旧 hash 作为 optimistic conflict 的安全目的，不保留其 Git-specific 形状。",
        "保留缺字段不触发 mutation。"
      ],
      "replacementSteps": [
        "Delete readExpectedPendingHash from the public contract.",
        "Generate the binding codec from the transaction wrapper declaration and keep policy/actor verification server-side.",
        "Retain the optimistic-conflict purpose in tests using revision/digest, not Git state."
      ],
      "verificationCases": [
        "A body with only expectedPendingHash is rejected; digest/revision mismatch writes no approval; a valid selected effect binding queues its own durable receipt while a quote/data leaf has no such operation."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source extracts a process-local hash from unknown JSON, which is the exact authority leak. Retain optimistic concurrency protection but move it to a selected transaction wrapper and make non-effect leaves structurally absent.",
      "contractAnchors": [
        "K03",
        "K08",
        "K09",
        "K10",
        "K12"
      ]
    },
    {
      "id": "MAP-146D2109B6",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:90-95: Reads the path id and directly resolves a UnifiedTradingAccount through UTAEngineContext. Missing ids return null for each caller to turn into a local 404."
      ],
      "currentBehavior": "Reads the path id and directly resolves a UnifiedTradingAccount through UTAEngineContext. Missing ids return null for each caller to turn into a local 404.",
      "problem": "Path strings are passed directly into in-process account lookup with no protocol decoding, authorization decision, or projection/service boundary.",
      "targetModel": "Account route resolution is a typed registry/capability lookup: path syntax yields AccountId, then Principal/scope authorization yields an AuthorizedAccount handle with declared capabilities and availability. It does not expose UnifiedTradingAccount. Authorization resolves an explicit AccountScope before an account-backed capability is invoked.",
      "pureTransitions": [
        "parseAccountId(path) -> Result<AccountId, InvalidAccountId>",
        "resolveAuthorized(principal, id, registry) -> Result<AuthorizedAccount, UnknownAccount|ForbiddenAccountScope|DiscoveryPending>."
      ],
      "effectBoundary": "Application account-directory/query ports perform lookup and scope filtering; HTTP only maps typed failures and passes the selected capability request.",
      "durability": "Read resolution carries projection checkpoint/freshness. A later controlled effect rechecks scope/revision in its own writer; a path lookup never creates intent or locks.",
      "errors": [
        "InvalidAccountId",
        "UnknownAccount",
        "ForbiddenAccountScope",
        "AccountDiscoveryPending",
        "CapabilityUnavailable"
      ],
      "preservedBehavior": [
        "保留不存在账户 404 语义。",
        "保留同一 route 可以按 account id 定位，但不暴露 UnifiedTradingAccount。"
      ],
      "replacementSteps": [
        "Introduce the path AccountId codec and registry handle.",
        "Update every per-account handler to request a declared leaf through application ports.",
        "Keep provider-native handles private to the selected adapter."
      ],
      "verificationCases": [
        "Malformed/unknown id fails before provider invocation; an authorized account returns explicit scope/capability state, and an unauthorized existing account is not collapsed into a local 404."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source directly returns a UnifiedTradingAccount or null. Retain account-id routing and not-found intent while separating decoding, authorization, and capability selection.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K05",
        "K06",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-5C97C6CC79",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:97-125: Implements shared route query handling: offline accounts are nudged and return 503 with health info; broker errors are converted to {error, code, transient} and 500/503 based on a permanent boolean; successful values are serialized directly."
      ],
      "currentBehavior": "Implements shared route query handling: offline accounts are nudged and return 503 with health info; broker errors are converted to {error, code, transient} and 500/503 based on a permanent boolean; successful values are serialized directly.",
      "problem": "Health and broker failures are represented by local strings/booleans and direct side effects; expected failures are not the target ADTs and successful/native values have no named response schema.",
      "targetModel": "Query invocation is a protocol adapter for a selected finite pull or declared push stream. It maps typed availability, delivery, and provider-domain errors without `nudgeRecovery`, BrokerError text, or a universal query result shape that every provider must implement. Account-backed leaves carry explicit AccountScope; public leaves remain Public.",
      "pureTransitions": [
        "decodeQueryResult(wire, descriptor.outputSchema/errorSchema) -> Result<DeclaredResult, ProtocolDecodeFailure>",
        "classifyDelivery(failure) -> Availability|DeliveryFailure|DomainFailure; preserve provider-specific error extensions and stream end/cancel frames."
      ],
      "effectBoundary": "Connection/resource supervision and recovery are application services; HTTP performs pure authentication, decode, encode, and stream cancellation. A provider leaf may be unavailable while retaining its descriptor.",
      "durability": "Finite query reads carry checkpoint/as-of/freshness; push streams carry cursor/lifecycle/backpressure evidence. Neither writes order WAL. A transaction effect’s unknown recovery is a separate wrapper path.",
      "errors": [
        "AccountOffline(accountId)",
        "ConnectionDegraded(reason)",
        "ProjectionStale(asOf)",
        "ProviderQueryRejected(code)",
        "StorageUnavailable",
        "ProtocolDecodeFailure"
      ],
      "preservedBehavior": [
        "保留 offline 返回 unavailable 类状态；保留 transient/permanent 可供 UI 重试提示。",
        "保留 native broker errors 被分类，但改为 typed code。"
      ],
      "replacementSteps": [
        "Remove route-side recovery nudges and BrokerError message classification.",
        "Derive query/stream result and error codecs from the selected capability.",
        "Expose named availability and lifecycle frames without fabricating empty data."
      ],
      "verificationCases": [
        "Offline pull returns typed unavailable with health evidence and no transaction mutation; a declared push emits data/error/end frames and cancels its owned resource on disconnect; malformed provider output is rejected."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source helper conflates health recovery, error text, and direct serialization. Retain health-aware failure visibility, but make delivery capability-specific and keep order recovery confined to transaction effects.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K05",
        "K06",
        "K07",
        "K08",
        "K12"
      ]
    },
    {
      "id": "MAP-2F69B76FDC",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:127-135: Creates an unauthenticated Hono route set and lists ctx.utaManager.listUTAs() directly as the response for GET /uta."
      ],
      "currentBehavior": "Creates an unauthenticated Hono route set and lists ctx.utaManager.listUTAs() directly as the response for GET /uta.",
      "problem": "Alice authenticates the user-facing proxy, but this UTA loopback route exposes raw manager summaries and has no named protocol schema or typed transport-principal boundary. Transport authentication must not be conflated with transaction authorization.",
      "targetModel": "Account listing is an authenticated finite pull capability using the fixed descriptor envelope. A provisioned transport verifier creates Principal, then the application filters visible scopes and returns Complete/Partial/Unavailable; body actors and loopback are never authorization. Each account row carries its explicit AccountScope, while capability availability remains separate from schema presence.",
      "pureTransitions": [
        "authenticateTransport(metadata, verifier) -> Result<Principal, UnauthenticatedTransport|VerifierUnavailable>",
        "listAccounts(principal, registry, coverage) -> Result<ListingResult<AccountSummary>, Forbidden|ProjectionUnavailable> with capability availability separate from schema."
      ],
      "effectBoundary": "Deployment/Guardian owns credential provisioning and verifier integration; UTA transport maps the verified Principal to application query ports. No route-local crypto, body-supplied actor, or fallback trust in localhost.",
      "durability": "Listing reads projection/checkpoint only and never stores credentials or transaction authority. Controlled operations independently bind Principal, scope, descriptor revision, policy, and expiry inside their transaction wrapper.",
      "errors": [
        "UnauthenticatedTransport",
        "TransportVerifierUnavailable",
        "ForbiddenAccountListing",
        "ListingPartial(missing)",
        "ProjectionUnavailable"
      ],
      "preservedBehavior": [
        "保留 GET /uta 的 id/label listing。",
        "保留 Alice proxy path，但不再把 loopback reach 当授权。"
      ],
      "replacementSteps": [
        "Install the provisioned service-credential verifier at transport admission, failing closed while unresolved.",
        "Add AccountListResponse coverage/freshness schema and migrate SDK caller.",
        "Keep transaction authorization separate from account discovery."
      ],
      "verificationCases": [
        "Unauthenticated loopback and invalid credentials return 401; an unprovisioned verifier returns typed unavailable; a valid Principal sees only authorized scopes and partial coverage is not Complete/empty."
      ],
      "openQuestions": [
        "The repository does not provide the UTA↔Alice service credential/issuer/verifier implementation; Main/deployment must designate the secret provisioning and verifier owner without route-local crypto or a body-supplied identity."
      ],
      "contractDisposition": "reframed",
      "contractRationale": "The source has no authentication and returns manager summaries. Retain the listing surface, but add the unresolved deployment-owned verifier boundary and fixed outer descriptor without claiming implementation exists.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K05",
        "K06",
        "K07",
        "K11",
        "K12"
      ]
    },
    {
      "id": "MAP-A7201E902B",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:137-142: Calls utaManager.getAggregatedEquity() and serializes its result without a request/response schema or accounting/valuation error model."
      ],
      "currentBehavior": "Calls utaManager.getAggregatedEquity() and serializes its result without a request/response schema or accounting/valuation error model.",
      "problem": "The route directly invokes a manager aggregate and returns an untyped aggregate shape; target accounting owns currency/FX/uncertainty and read projections are rebuildable.",
      "targetModel": "Equity is an accounting/data projection capability. Its declared output contains per-AccountScope Money values, valuation/FX provenance, freshness/coherence, and provider extensions; it is not an order contract and never requires order approval or compensation.",
      "pureTransitions": [
        "aggregateEquity(rows, fxFacts, policy) -> Result<EquityProjection, MissingFx|CurrencyMismatch|StaleSource>",
        "encodeEquity(outputSchema) -> exact Money/Decimal text with Partial/Unavailable coverage."
      ],
      "effectBoundary": "The accounting application reads account/position observations and FX capability leaves; HTTP encodes the resulting projection and does not call a manager or broker write API.",
      "durability": "Projection checkpoints/as-of and valuation warnings are durable read evidence. No order intent, lock, approval, or dispatch state is changed by an equity query.",
      "errors": [
        "FxUnavailable(currency)",
        "FxExpired(currency, observedAt)",
        "AccountSnapshotStale(accountId)",
        "AggregationStorageFailure",
        "CapabilityNotDeclared(fx)"
      ],
      "preservedBehavior": [
        "保留跨账户聚合 equity/cash/PnL 以及 per-account rows。",
        "保留 keyless/data accounts 不混入 funded portfolio。"
      ],
      "replacementSteps": [
        "Move aggregation into accounting/projection service.",
        "Derive Money/FX output and error schemas from the selected data capabilities.",
        "Make missing scopes/FX explicit and migrate SDK/UI away from raw aggregate shape."
      ],
      "verificationCases": [
        "Mixed-currency rows aggregate only with declared fresh FX; missing FX remains Partial/indeterminate and no rate=1/empty-success fallback is emitted."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source proves direct untyped manager aggregation, not a universal order concern. Retain cross-account valuation semantics while placing it in data/accounting capabilities with no transaction machinery.",
      "contractAnchors": [
        "K01",
        "K03",
        "K04",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-3CDB550874",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:144-165: Reads pattern/query, assetClass, and source/accountId query strings, invokes searchTradeableContracts across configured accounts, and returns raw hits/count plus a configured-account count."
      ],
      "currentBehavior": "Reads pattern/query, assetClass, and source/accountId query strings, invokes searchTradeableContracts across configured accounts, and returns raw hits/count plus a configured-account count.",
      "problem": "The route performs permissive query parsing and delegates a broker-side heuristic over UTA instances; response and errors are not named protocol values and capability context is incomplete.",
      "targetModel": "Contract search route is a provider-declared finite pull leaf with typed pattern/hint/source-selection input and ListingResult output. Provider-specific search filters/extensions are composed in the leaf schema; no route-wide broker search API is assumed. Scope is explicit Public or Account(AccountScope) when the capability is account-backed; no route invents a default account or subaccount.",
      "pureTransitions": [
        "decodeSearchQuery(wire, descriptor.inputSchema) -> Result<DeclaredSearchQuery, ValidationFailed>; the schema explicitly distinguishes Public from Account(AccountScope) selection.",
        "aggregateSearch(leaves, coverage) -> ListingResult<InstrumentHit> preserving source/scope and Partial markers."
      ],
      "effectBoundary": "Application instrument-query selects declared provider leaves and owns all-settled failure isolation; HTTP does not enumerate manager accounts or pass raw vendor objects.",
      "durability": "Catalog/search cache may persist version/as-of observations, but search remains data delivery without order journal, approval, or compensation.",
      "errors": [
        "InvalidPattern",
        "UnknownAccount",
        "SearchPartial(missingAccounts)",
        "BrokerSearchUnavailable",
        "CapabilityNotDeclared(search)"
      ],
      "preservedBehavior": [
        "保留 pattern/query alias、assetClass hint、source/accountId filter。",
        "保留 count 与 configured account visibility，但改为 typed completeness。"
      ],
      "replacementSteps": [
        "Move normalization/orchestration to instrument query application.",
        "Share exact codecs with SDK/client and preserve source selection.",
        "Return coverage/degraded markers instead of count/empty ambiguity."
      ],
      "verificationCases": [
        "A source-scoped search invokes only that declared leaf; all-authorized search preserves healthy hits and names missing scopes; no configured namespace returns Complete(empty) only with explicit coverage."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source’s alias parsing and per-account search are useful behavior. Retain them while making the route a finite schema-derived data leaf and separating capability absence from an empty result.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K04",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-95C382B4F8",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:167-190: Walks each UTA; skips offline accounts and catches errors only around positions/account reads. It collects non-USD currencies, then calls ctx.fxService.getRate outside that catch, so FX failures escape. Returns primitive rate/source/timestamp records."
      ],
      "currentBehavior": "Walks each UTA; skips offline accounts and catches errors only around positions/account reads. It collects non-USD currencies, then calls ctx.fxService.getRate outside that catch, so FX failures escape. Returns primitive rate/source/timestamp records.",
      "problem": "Transport performs live aggregation, omits offline or failed account reads, lets FX failures escape as generic route failure, and returns primitive strings/numbers without currency-qualified values or typed uncertainty.",
      "targetModel": "FX rates are a finite data pull over a declared FX observation leaf. Output uses exact Decimal rates, currency/unit, source, timestamp, freshness, and missing-scope/currency markers; offline account reads are not silently skipped. Account-scoped observations identify the AccountScope whose currencies were observed; public FX leaves remain Public.",
      "pureTransitions": [
        "collectCurrencies(snapshots, schema) -> Set<CurrencyCode>",
        "resolveFx(currencies, observations) -> Result<Complete|Partial<FxObservation>, FxUnavailable|ProviderFailure> preserving last-known/stale evidence explicitly."
      ],
      "effectBoundary": "Accounting/FX application services perform account observation and FX leaf calls; HTTP only decodes query and encodes the typed response.",
      "durability": "FX observations/cache checkpoints are durable data evidence. No FX failure creates an order compensation or approval state; a transaction wrapper may later choose whether a snapshot is an effect precondition.",
      "errors": [
        "AccountSnapshotUnavailable(accountId)",
        "FxUnavailable(currency)",
        "FxProviderFailure(code)",
        "FxObservationExpired",
        "CapabilityNotDeclared(fx)"
      ],
      "preservedBehavior": [
        "保留 USD 排除与 non-USD currency discovery。",
        "保留 per-currency source/updatedAt 供工具解释。"
      ],
      "replacementSteps": [
        "Move currency collection and FX resolution out of the route.",
        "Decode exact FX schema and typed freshness/partial state.",
        "Remove catch-and-drop and primitive-number fallback while preserving source/updatedAt visibility."
      ],
      "verificationCases": [
        "One account failure is named while healthy currencies remain; an FX provider failure yields Partial/Unavailable with evidence rather than uncaught 500 or empty rates, and decimal text remains exact."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source’s account loop and FX call leak data orchestration into transport and skip failures. Retain per-currency provenance and failure isolation, but keep it a data leaf outside order approval/recovery.",
      "contractAnchors": [
        "K01",
        "K03",
        "K04",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-3D3861C921",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:192-215: Parses an arbitrary request into utaConfigSchema, dynamically creates a broker, calls init/getAccount/getPositions directly, returns raw account/positions, and best-effort closes the broker in finally."
      ],
      "currentBehavior": "Parses an arbitrary request into utaConfigSchema, dynamically creates a broker, calls init/getAccount/getPositions directly, returns raw account/positions, and best-effort closes the broker in finally.",
      "problem": "The HTTP handler directly constructs a concrete broker and invokes lifecycle/query methods, accepts arbitrary config at the process boundary, and exposes raw broker results; it bypasses application/broker-spi layering and shared protocol schemas.",
      "targetModel": "Connection probe is a provider-declared, ephemeral capability leaf with a typed config reference/preset input and capability/reach/account-read output. Native credentials/config and lifecycle handles stay inside the selected provider process. An account-read probe reports explicit AccountScope where applicable.",
      "pureTransitions": [
        "decodeProbeInput(wire, descriptor.inputSchema) -> Result<ProbeRequest, ConfigValidationFailure>",
        "projectProbe(nativeFacts, outputSchema) -> Result<ProbeResult, ConnectionFailure|AccountReadIncomplete> without exposing native response objects."
      ],
      "effectBoundary": "A connection-control application port resolves provisioned credentials/config references, scopes the provider connection, performs probe/close, and returns the selected leaf schema; HTTP does not dynamically create brokers.",
      "durability": "Probe may record health/capability generation and attempt evidence, but is a data/resource operation, not a transaction intent or order dispatch. Secrets never enter response or journal payload.",
      "errors": [
        "InvalidBrokerConfig",
        "CredentialUnavailable",
        "ConnectionFailure(code)",
        "AccountReadIncomplete",
        "ForbiddenProbe",
        "CapabilityNotDeclared(probe)"
      ],
      "preservedBehavior": [
        "保留 setup wizard 能验证连接并读取 account+positions 的成功信号。",
        "保留 best-effort close 作为 Scope finalizer，而非 route finally swallow。"
      ],
      "replacementSteps": [
        "Replace arbitrary body/config parsing with preset-specific declared input and config references.",
        "Use the deployment/IaC credential provider rather than caller-supplied secrets.",
        "Return typed reach/capability/completeness and keep close failure observable."
      ],
      "verificationCases": [
        "Invalid config fails before provider init; a successful probe returns named reach/capability facts with no key/native class; close failure is visible and cannot mark the provider healthy."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source dynamically creates a broker from arbitrary JSON and returns raw account/positions. Retain setup-wizard probe intent but compose it as one provider capability with secret references and a finite typed result.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-E98645ED1A",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:217-224: Calls utaManager.reconnectUTA(id) directly and chooses HTTP 200 or 500 from a success boolean, with no account existence, authorization, typed connection state, or protocol failure mapping."
      ],
      "currentBehavior": "Calls utaManager.reconnectUTA(id) directly and chooses HTTP 200 or 500 from a success boolean, with no account existence, authorization, typed connection state, or protocol failure mapping.",
      "problem": "Connection lifecycle is reached through the legacy manager and reduced to success/500 rather than target scoped Layer/supervision and typed degraded capability.",
      "targetModel": "Reconnect is a provider connection-lifecycle capability with AccountScope/instance generation and typed transitions. It is not order transaction recovery; restoring a connection does not imply any dispatch is safe.",
      "pureTransitions": [
        "decideReconnect(command, directory) -> Result<ReconnectAccepted, UnknownAccount|Forbidden|AlreadyRecovering>",
        "foldConnectionEvent(state, event) -> ConnectionState/CapabilityAvailability with exhaustive typed transitions."
      ],
      "effectBoundary": "Connection-control/supervision owns reconnect and provider lifecycle; HTTP invokes that selected capability and returns a transition/availability schema rather than a success boolean.",
      "durability": "Generation/reconnect attempt evidence may be checkpointed for the provider resource. Transaction recovery and unknown outcomes remain separate wrapper capabilities and are not auto-completed by reconnect.",
      "errors": [
        "UnknownAccount",
        "ForbiddenAccountScope",
        "ConnectionAlreadyRecovering",
        "ConnectionFailure",
        "RecoveryBarrierPending",
        "CapabilityNotDeclared(reconnect)"
      ],
      "preservedBehavior": [
        "保留 reconnect endpoint 与 success/failure response intent。",
        "不再把 whole-process restart boolean 当单账户 broker readiness。"
      ],
      "replacementSteps": [
        "Replace manager.reconnectUTA with a declared connection lifecycle leaf.",
        "Expose generation/reach/readiness evidence.",
        "Keep transaction outcome recovery as an explicit withTransaction operation."
      ],
      "verificationCases": [
        "Unknown id is typed not-found; a reconnect yields a new generation/health only after evidence, while a transaction with unknown dispatch remains Reconciling until its own observation path resolves."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source maps whole-manager reconnect to a boolean HTTP response. Retain scoped reconnect intent but distinguish provider resource lifecycle from transaction recovery and capability availability.",
      "contractAnchors": [
        "K01",
        "K02",
        "K05",
        "K07",
        "K08",
        "K12"
      ]
    },
    {
      "id": "MAP-3194C27217",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:226-239: Accepts an unvalidated optional numeric delayMs and calls uta.sync directly to force broker state synchronization; all exceptions become a 500 string error."
      ],
      "currentBehavior": "Accepts an unvalidated optional numeric delayMs and calls uta.sync directly to force broker state synchronization; all exceptions become a 500 string error.",
      "problem": "The route has no command schema, durable job identity, lease/reconciliation semantics, or unknown-outcome handling; it calls legacy in-memory UTA synchronization directly.",
      "targetModel": "Sync is a provider-declared data observation/refresh leaf with a bounded scheduling hint if the provider exposes one. It must not become a universal order-recovery command; unknown post-dispatch order outcomes use the selected effect wrapper’s recovery capability. When sync is account-backed, its request and observation are bound to explicit AccountScope.",
      "pureTransitions": [
        "decodeSyncInput(wire, descriptor.inputSchema) -> Result<RefreshRequest, ValidationFailed>",
        "classifyRefreshResult(result) -> DataObservation|RefreshUnavailable|DataDeliveryUnknown; any transaction recovery is handled by a separate declared wrapper."
      ],
      "effectBoundary": "Application resource supervision schedules the provider observation and returns a typed receipt/result; HTTP never sleeps or invokes an imperative UTA.sync directly.",
      "durability": "Refresh receipts/cursors and observations may be durable data projection evidence. They do not create approval/jobs/locks; a transaction wrapper separately persists ObserveUnknown and reconcile state when required.",
      "errors": [
        "InvalidRefreshScope",
        "CapabilityNotDeclared(sync)",
        "RefreshUnavailable",
        "StorageUnavailable",
        "DataDeliveryUnknown"
      ],
      "preservedBehavior": [
        "保留 AI 可主动触发 stale state synchronization。",
        "保留 optional delay only as a bounded 0–60000ms scheduling hint; it never decides business authority or sleeps inside a request."
      ],
      "replacementSteps": [
        "Replace arbitrary delayMs with a schema-declared bounded notBefore hint or omit it.",
        "Move observation scheduling to the application/resource layer.",
        "Keep transaction unknown recovery explicit and separate from ordinary data synchronization."
      ],
      "verificationCases": [
        "Negative/fractional/over-limit hints fail before scheduling; a refresh does not create a PlaceOrder or compensation; a declared effect recovery request remains an explicit observe/reconcile operation."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source calls legacy sync with unvalidated delay and generic 500. Retain the caller’s stale-state refresh intent, but distinguish data refresh from the durable recovery obligations of controlled effects.",
      "contractAnchors": [
        "K01",
        "K03",
        "K05",
        "K07",
        "K08",
        "K12"
      ]
    },
    {
      "id": "MAP-44AA5A48AB",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:241-259: Accepts any array-valued changes field, calls uta.simulatePriceChange directly for all account types, and classifies thrown errors by regex matching 'simulate' or 'mock' to choose 400 versus 500.",
        "services/uta/src/domain/trading/git/TradingGit.ts:753-879: simulatePriceChange uses Decimal arithmetic, supports `symbol=all` by scaling each position's own mark (including derivatives), excludes derivative rows from symbol-level underlying changes, and returns currentState/simulatedState/summary projections."
      ],
      "currentBehavior": "Accepts any array-valued changes field, calls uta.simulatePriceChange directly for all account types, and classifies thrown errors by regex matching 'simulate' or 'mock' to choose 400 versus 500.",
      "problem": "Input is only checked as Array, the route reaches a legacy UTA method, and error category is inferred from text, contrary to typed protocol/error channels.",
      "targetModel": "Price simulation is an AccountScope-bound finite valuation-preview capability over current account exposure. Its input/output schemas carry explicit AccountScope, canonical InstrumentId, hypothetical Price observations, Decimal values, uncertainty, and the source-declared scenario behavior; it is read-only and not a broker mutation or order approval route. The migrated leaf retains the source's `symbol=all` own-mark scaling (including derivative rows) and its symbol-level underlying rule that excludes derivative rows, rather than generalizing either rule across providers.",
      "pureTransitions": [
        "decodeScenario(wire, descriptor.inputSchema) -> Result<AccountScopedPriceScenario, InvalidScenario>",
        "evaluateScenario(scope, exposure, prices, policy) -> Result<ValuationPreview, MissingFx|UnknownMultiplier|ValuationIndeterminate> using Decimal arithmetic and declared source scenario rules.",
        "projectScenarioResult(current, simulated, summary, outputSchema) -> Result<PriceSimulationResult, ProjectionFailure> preserving source-specific current/simulated/summary values."
      ],
      "effectBoundary": "Market/accounting application computes the AccountScope-bound preview from projections and declared market facts; HTTP maps typed failures and never uses provider error text/regex to choose authorization or mutates broker mark prices.",
      "durability": "A preview may have scenario identity/as-of and cache metadata, but does not create transaction intent, lock, approval, dispatch, or mark-price mutation.",
      "errors": [
        "InvalidScenario",
        "InstrumentNotSupported",
        "MissingFx",
        "ValuationIndeterminate",
        "CapabilityNotDeclared(price-preview)",
        "CapabilityUnavailable(price-preview)"
      ],
      "preservedBehavior": [
        "保留 AI PnL exploration endpoint intent。",
        "MockBroker-specific unsupported result remains clean 4xx typed, real broker query errors remain distinct."
      ],
      "replacementSteps": [
        "Replace changes-array permissiveness with the selected scenario schema.",
        "Move valuation and availability checks into application services.",
        "Migrate SDK to named preview output and preserve Mock-only evidence as provider-specific availability."
      ],
      "verificationCases": [
        "For one AccountScope, `symbol=all` applies each position's own mark, including derivative rows, while a symbol-level underlying change excludes derivative rows; the result returns deterministic exact `current`/`simulated`/`summary` deltas; malformed changes fail before provider state changes, and a provider without the preview leaf is absent/unavailable rather than regex-classified."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source establishes a useful AI PnL exploration surface and explicit Mock-only failures. Retain the preview semantics but keep it a data capability with no order effect machinery. The account-backed source is now explicit: simulation is scoped valuation projection, retaining all-position own-mark scaling and the separate underlying/derivative exclusion behavior without turning either into a universal market rule.",
      "contractAnchors": [
        "K01",
        "K03",
        "K04",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-4EFAFBF364",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:261-267: Resolves an account then calls account.listSubAccounts through queryAccount, returning raw sub-account values and health-derived ad hoc errors."
      ],
      "currentBehavior": "Resolves an account then calls account.listSubAccounts through queryAccount, returning raw sub-account values and health-derived ad hoc errors.",
      "problem": "The route calls the legacy account object directly and does not provide named sub-account/query schemas or capability context.",
      "targetModel": "Sub-account discovery is a scoped data capability with output Discovering | Available(scopes, revision) | Unavailable(reason). Even a one-wallet provider declares its explicit scope; discovery does not imply an order transaction. The selected discovery query is bound to explicit AccountScope and never synthesizes a default wallet.",
      "pureTransitions": [
        "decodeScopeQuery(accountId, requested) -> Result<ScopeQuery, ScopeRequired|InvalidScope>",
        "foldDiscovery(facts) -> Discovering|Available|Unavailable preserving revision/generation."
      ],
      "effectBoundary": "Account-query reads provider directory/cache and returns the selected scope-discovery leaf; transport does not call an account class directly.",
      "durability": "Directory revision/source sequence is projection state. Pending discovery may block a separate effect wrapper’s prepare, but list itself is finite data and creates no WAL/approval.",
      "errors": [
        "ScopeDiscoveryPending(accountId)",
        "UnknownSubAccount(accountId, subAccountId)",
        "AccountOffline",
        "ProjectionStale",
        "CapabilityNotDeclared(subaccounts)"
      ],
      "preservedBehavior": [
        "保留 ordinary broker one-wallet listing 与 CCXT multiple wallet listing。",
        "保留 explicit `subAccountId` query use case。"
      ],
      "replacementSteps": [
        "Use one AccountScope codec for list/query/effect leaves.",
        "Expose discovery status rather than defaulting to aggregate/empty.",
        "Remove raw broker subaccount types from the wire and retain provider extension data only when declared."
      ],
      "verificationCases": [
        "Single-wallet output contains an explicit scope; pending discovery remains Discovering and does not authorize an effect; unknown wallet never silently resolves to the aggregate account."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source exposes raw optional subAccountId and ad hoc health errors. Retain multi-wallet/single-wallet distinction while making discovery a typed data capability independent from transactions.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K05",
        "K06",
        "K12"
      ]
    },
    {
      "id": "MAP-87FA16A102",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:269-274: Resolves a UTA and directly calls getAccount, optionally passing an unvalidated subAccountId query string; queryAccount serializes the native account result."
      ],
      "currentBehavior": "Resolves a UTA and directly calls getAccount, optionally passing an unvalidated subAccountId query string; queryAccount serializes the native account result.",
      "problem": "The handler exposes a legacy broker/account return object and primitive query id directly; it is not a typed projection query at the transport boundary.",
      "targetModel": "Account state is a finite pull capability returning a SnapshotViewEnvelope over the provider-declared AccountSnapshot schema. Explicit AccountScope versus an aggregate view is a local query choice; native account fields remain adapter-specific extensions.",
      "pureTransitions": [
        "resolveReadScope(directory, requested) -> Result<ExplicitScope|DeclaredAggregate, ScopeDiscoveryPending|UnknownScope>",
        "decodeAccountSnapshot(observation, outputSchema) -> Result<AccountSnapshot, NativeDecodeFailure|ProjectionFailure>."
      ],
      "effectBoundary": "Account-query/application projection reads the provider observation; broker refresh is a separate declared resource capability, and HTTP only encodes the envelope.",
      "durability": "Envelope carries schema version, source sequence/checkpoint, as-of/capturedAt, and component freshness/coherence. It is a read projection and does not acquire transaction locks or approval state.",
      "errors": [
        "ScopeRequired",
        "UnknownScope",
        "AccountSnapshotUnavailable",
        "ProjectionStale",
        "BrokerObservationUnknown",
        "CapabilityNotDeclared(account)"
      ],
      "preservedBehavior": [
        "保留 omission=subaccount aggregate 与 explicit wallet query 的 distinction。",
        "保留 native account fields 的业务内容，但 normalized values。"
      ],
      "replacementSteps": [
        "Define explicit scope/aggregate query schema and envelope.",
        "Migrate SDK to the selected account-state leaf.",
        "Keep native AccountInfo values behind provider decoder and preserve unknown/stale markers."
      ],
      "verificationCases": [
        "Explicit and aggregate requests produce distinct scoped envelopes; stale observations are marked, not empty; no SDK AccountInfo object or implicit wallet default appears on the wire."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source directly serializes getAccount with an optional raw wallet id. Retain account facts and scope modes, but make the boundary a provider-declared pull projection with no transaction state.",
      "contractAnchors": [
        "K01",
        "K03",
        "K04",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-D250B2BDC3",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:276-281: Calls account.getPositions with an optional raw subAccountId and returns {positions: ...} through queryAccount, exposing broker/account-native position values."
      ],
      "currentBehavior": "Calls account.getPositions with an optional raw subAccountId and returns {positions: ...} through queryAccount, exposing broker/account-native position values.",
      "problem": "The route directly queries the legacy account/broker object and serializes the result without target projection, observation, or domain-value schemas.",
      "targetModel": "Positions are a finite data projection over the provider-declared Position/Candle-like semantic unit: AccountScope, InstrumentId, Quantity, units/currency/multiplier when evidenced, and observation provenance. Spot/perp and provider extensions remain distinct.",
      "pureTransitions": [
        "foldPositionObservations(observations, outputSchema) -> Result<PositionSnapshot[], AccountingFailure>",
        "encodePositions(snapshotEnvelope) -> exact values plus partial/freshness evidence; no stale observation becomes empty exposure."
      ],
      "effectBoundary": "Account/market accounting application reads typed observations; adapters decode native positions and extensions. HTTP does not call getPositions or serialize broker-native values directly.",
      "durability": "Projection rows are keyed by scope/instrument/observation identity and carry sequence/coherence. Position reads do not create order WAL, approval, or compensation; an effect may persist selected evidence separately.",
      "errors": [
        "UnknownScope",
        "PositionObservationUnknown",
        "MultiplierUnavailable",
        "CurrencyMismatch",
        "ProjectionStale",
        "CapabilityNotDeclared(positions)"
      ],
      "preservedBehavior": [
        "保留 `{positions}` response shape concept and optional wallet filtering。",
        "保留 Decimal quantity/market values; no float conversion。"
      ],
      "replacementSteps": [
        "Normalize legacy positions through a declared output schema.",
        "Add typed scope and observation envelope.",
        "Move refresh/reconcile outside HTTP and keep unknown multiplier/currency explicit."
      ],
      "verificationCases": [
        "Same display symbol on spot/perp remains distinct; partial observation is Partial/Unknown rather than empty positions, and quantity/money strings retain precision."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source serializes raw account-native positions with optional strings. Retain scoped position facts and decimal precision while treating positions as data delivery, not an order transaction.",
      "contractAnchors": [
        "K01",
        "K03",
        "K04",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-8239373D6C",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:283-293: Accepts comma-separated raw ids or derives pending ids from account.getPendingOrderIds, calls account.getOrders directly, and returns raw order objects."
      ],
      "currentBehavior": "Accepts comma-separated raw ids or derives pending ids from account.getPendingOrderIds, calls account.getOrders directly, and returns raw order objects.",
      "problem": "Raw comma-separated ids and direct pending-order calls bypass typed identifiers, projection authority, preconditions, and shared response schemas.",
      "targetModel": "Orders query is a finite pull leaf over a provider-declared order-observation schema. Typed BrokerOrderId filters and cursor/coverage are local to that leaf; an absent order-list leaf means no route, not a full handler returning Unsupported. Every account-backed order listing is bound to explicit AccountScope.",
      "pureTransitions": [
        "decodeOrderListingQuery(wire, descriptor.inputSchema) -> Result<DeclaredOrderQuery, ValidationFailed>",
        "selectListing(observations, coverage) -> Complete|Partial|Unavailable; absence is not inferred from an incomplete page."
      ],
      "effectBoundary": "ProjectionReader or a declared observation-refresh capability supplies order rows; HTTP does not call getPendingOrderIds/getOrders or own provider native identities.",
      "durability": "Listing stores cursor/coverage/source sequence for read replay. It creates no transaction state; only a later selected effect wrapper may persist chosen order evidence before dispatch.",
      "errors": [
        "InvalidBrokerOrderId",
        "ListingIncomplete(namespace)",
        "OrderProjectionStale",
        "ObservationUnavailable",
        "CapabilityNotDeclared(order-list)"
      ],
      "preservedBehavior": [
        "保留 optional ids filter and default pending-order view.",
        "保留 raw order details only through normalized OrderSnapshot."
      ],
      "replacementSteps": [
        "Replace CSV splitting with the leaf’s repeated-id/cursor codec.",
        "Migrate SDK to typed listing output and coverage.",
        "Require Complete evidence before any separate effect that relies on absence, without turning listing into cancellation authority."
      ],
      "verificationCases": [
        "A comma-bearing or long id remains one text identity under the declared codec; partial listing is named; empty Complete requires explicit coverage; GET never mutates orders or invokes approval."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source conflates CSV/pending defaults with raw order reads. Retain finite listing and identity behavior, but make it a provider-declared data leaf and keep effect decisions separate.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K04",
        "K05",
        "K07",
        "K08",
        "K12"
      ]
    },
    {
      "id": "MAP-D3212B77F2",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:295-300: Calls account.getMarketClock directly and serializes its account-scoped result through queryAccount."
      ],
      "currentBehavior": "Calls account.getMarketClock directly and serializes its account-scoped result through queryAccount.",
      "problem": "The target explicitly says market clock is not an account-wide boolean, while this route delegates to an account-wide getMarketClock with no venue/instrument/session dimensions.",
      "targetModel": "Market session is a venue/calendar data capability with explicit Instrument/venue/session dimensions and an Open | Closed | Unknown | NextWindow observation. It is not an account boolean and does not imply transaction approval. Scope is explicit Public or Account(AccountScope) when the capability is account-backed; no route invents a default account or subaccount.",
      "pureTransitions": [
        "decodeSessionQuery(wire, descriptor.inputSchema) -> Result<SessionQuery, ValidationFailed>",
        "evaluateSession(calendar, query) -> Result<SessionObservation, CalendarUnavailable|UnknownSession> with calendar version/freshness."
      ],
      "effectBoundary": "Market/calendar application invokes the selected provider/calendar leaf; HTTP does not call account.getMarketClock or convert a broker boolean. Public session queries do not receive an invented account scope.",
      "durability": "Session observations carry calendar version/as-of and may be used as a versioned precondition by a selected transaction wrapper, but a query alone writes no order state.",
      "errors": [
        "InstrumentVenueUnknown",
        "CalendarUnavailable",
        "SessionObservationStale",
        "CapabilityNotDeclared(session)"
      ],
      "preservedBehavior": [
        "保留 market clock query surface for callers needing session info。",
        "保留 closed/open distinction, but make dimensions explicit."
      ],
      "replacementSteps": [
        "Define session dimensions and output ADT in the provider leaf schema.",
        "Move native market-clock calls behind calendar/provider adapter.",
        "Let effect preparation explicitly bind a fresh session observation only when its schema requires it."
      ],
      "verificationCases": [
        "Two instruments/venues can return distinct states; missing calendar remains Unknown/Unavailable, and a stale session cannot authorize dispatch by itself."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source’s account-scoped boolean is insufficient, but the market-clock query intent is worth retaining. Reframe it as an explicit data capability with no universal account method.",
      "contractAnchors": [
        "K01",
        "K03",
        "K04",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-5FBCD3EE4E",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:302-315: Preserves a legacy symbol path, constructs an @traderalice/ibkr Contract in the HTTP handler, sets symbol, and calls account.getQuote directly through queryAccount."
      ],
      "currentBehavior": "Preserves a legacy symbol path, constructs an @traderalice/ibkr Contract in the HTTP handler, sets symbol, and calls account.getQuote directly through queryAccount.",
      "problem": "The transport constructs and passes a concrete broker SDK object and exposes a legacy symbol-only query, violating the prohibition on broker SDK values in public protocol/transport.",
      "targetModel": "Legacy symbol quote GET is an alias projection into the selected provider quote leaf. The public input resolves to Public or Account(AccountScope) plus InstrumentId; native Contract construction and provider extensions remain adapter-local, and quote remains finite data. Scope is explicit Public or Account(AccountScope) when the capability is account-backed; no route invents a default account or subaccount.",
      "pureTransitions": [
        "resolveSymbolAlias(scope, symbol, catalog) -> Result<InstrumentRef, IdentityFailure>",
        "invokeQuoteLeaf(ref, descriptor) -> Result<QuoteObservation, QuoteFailure> with exact Decimal/currency/time fields."
      ],
      "effectBoundary": "Application market query selects the provider quote capability and adapter builds the native request; transport never imports @traderalice/ibkr.",
      "durability": "Quote observation/cache carries source sequence/freshness. It does not create order plans or approvals; a transaction wrapper may record a selected quote as evidence only when explicitly declared.",
      "errors": [
        "InvalidInstrument",
        "UnauthorizedAccountScope",
        "QuoteUnavailable",
        "NativeQuoteDecodeFailure",
        "CapabilityNotDeclared(quote)"
      ],
      "preservedBehavior": [
        "保留 path `:symbol` compatibility during caller migration via explicit symbol->InstrumentId resolver.",
        "保留 source account routing."
      ],
      "replacementSteps": [
        "Keep the symbol path only as a typed alias codec during migration.",
        "Remove dynamic Contract construction from HTTP and publish the quote leaf schema.",
        "Preserve source/account routing and exact observed values."
      ],
      "verificationCases": [
        "AAPL resolves only within the selected account/venue; the response contains typed decimal quote fields and no raw SDK object, while an unknown alias fails before provider access."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source constructs an IBKR Contract in HTTP even though callers need a symbol alias. Retain compatibility routing but move identity and native construction into the selected provider data leaf.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K04",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-8456875518",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:317-328: Accepts arbitrary JSON, Object.assigns it into a native IBKR Contract, calls account.getQuote, and returns the native result or a string 500 error."
      ],
      "currentBehavior": "Accepts arbitrary JSON, Object.assigns it into a native IBKR Contract, calls account.getQuote, and returns the native result or a string 500 error.",
      "problem": "Arbitrary caller fields become broker SDK state through Object.assign and the raw broker result crosses HTTP; no request/response runtime schema or typed capability/error mapping exists.",
      "targetModel": "Rich quote POST is a provider-declared quote input leaf: exact InstrumentRef plus provider extension fields, strict unknown-field handling, and a declared QuoteObservation/error output. It is not a raw native Contract endpoint. Scope is explicit Public or Account(AccountScope) when the capability is account-backed; no route invents a default account or subaccount.",
      "pureTransitions": [
        "decodeQuoteRequest(wire, descriptor.inputSchema) -> Result<DeclaredQuoteInput, ValidationFailed>",
        "decodeProviderQuote(native, descriptor.outputSchema) -> Result<QuoteObservation, NativeDecodeFailure|ProviderRejected>."
      ],
      "effectBoundary": "The selected provider adapter constructs/decodes native Contract values; HTTP invokes only the application quote port and maps its typed result.",
      "durability": "Successful quote observation may update a market projection/checkpoint; failed/malformed data produces no order or transaction state.",
      "errors": [
        "ValidationFailed(unknownField)",
        "CapabilityUnavailable",
        "QuoteProviderRejected",
        "NativeQuoteDecodeFailure",
        "ProjectionStorageFailure"
      ],
      "preservedBehavior": [
        "保留 POST quote for clients holding richer instrument refs.",
        "保留 broker-specific error visibility via structured code."
      ],
      "replacementSteps": [
        "Replace arbitrary body/Object.assign with the declared quote schema.",
        "Keep native fields as exact provider extensions at the adapter boundary.",
        "Migrate SDK callers to named output and error schemas."
      ],
      "verificationCases": [
        "An extra native field is rejected if not declared; a valid provider extension survives round-trip; provider rejection is typed and cannot be inferred from HTTP string/error regex."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source’s arbitrary JSON-to-Contract assignment is the defect. Retain the richer quote use case but make both input and output declaration-bound and structurally precise.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K04",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-861915C783",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:330-341: Accepts arbitrary body fields, stringifies aliceId, passes unknown filters to account.expandContract, and returns the raw expansion or a string 500 error."
      ],
      "currentBehavior": "Accepts arbitrary body fields, stringifies aliceId, passes unknown filters to account.expandContract, and returns the raw expansion or a string 500 error.",
      "problem": "The route accepts unknown filters and legacy aliceId text, then returns a native/domain expansion object without schema or jurisdiction/capability validation.",
      "targetModel": "Contract expansion is a provider-declared data capability with typed hub InstrumentId input with Public or Account(AccountScope) only when declared and exact filter extension. Its output is a local Leaves | Directory | Grid | Partial(cursor) union only where that provider declares those delivery variants. An undeclared filter or delivery variant is omitted from the selected leaf schema and does not remove the expansion leaf; a provider without the expansion capability has no route, while a declared but unavailable leaf reports availability separately. Scope is explicit Public or Account(AccountScope) when the capability is account-backed; no route invents a default account or subaccount.",
      "pureTransitions": [
        "decodeExpansionFilters(wire, descriptor.inputSchema) -> Result<DeclaredFilters, FilterFailure>",
        "decodeExpansion(native, descriptor.outputSchema) -> Result<DeclaredExpansion, NativeDecodeFailure|NoMatch> preserving provider extension fields."
      ],
      "effectBoundary": "Instrument-query application invokes the selected provider expansion adapter; HTTP does not stringify arbitrary Alice ids or pass unknown filters.",
      "durability": "Catalog/expansion projection and cursor are versioned read state, not transaction authority or order approval. Push expansion, if declared later, follows stream lifecycle/backpressure rather than order locks.",
      "errors": [
        "MalformedInstrumentRef",
        "ValidationFailed(unknownExpansionField)",
        "ExpansionTruncated(cursor)",
        "InstrumentNotFound",
        "CapabilityNotDeclared(expansion)",
        "CapabilityUnavailable(expansion)"
      ],
      "preservedBehavior": [
        "保留 hub→leaves expansion for bonds/options/futures.",
        "保留 `aliceId` compatibility only as decoded canonical alias, not as free string."
      ],
      "replacementSteps": [
        "Declare filter and output variants per provider capability.",
        "Move native expansion and decoding into the adapter.",
        "Expose cursor/coverage so empty and unsupported remain distinguishable."
      ],
      "verificationCases": [
        "A declared directory result carries continuation and a leaf result carries canonical InstrumentId; a filter or delivery variant not declared by the selected expansion leaf is absent from its schema and an attempted field is rejected, while a provider without any expansion leaf has no route and a declared but unavailable leaf returns named availability rather than an empty object."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source’s hub-to-leaves drilldown is useful data behavior. Retain its shape and continuation evidence while making every filter/variant schema provider-declared. The review distinguishes unsupported filter/delivery parameters from a provider that lacks the expansion capability entirely.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K04",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-3C6243143F",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:343-361: Accepts arbitrary contract/params JSON, constructs an IBKR Contract, revives start/end dates imperatively, calls account.getHistorical, and returns raw bars or a string 500 error."
      ],
      "currentBehavior": "Accepts arbitrary contract/params JSON, constructs an IBKR Contract, revives start/end dates imperatively, calls account.getHistorical, and returns raw bars or a string 500 error.",
      "problem": "Broker-native Contract construction and permissive params cross the transport boundary, and date revival is not a shared runtime schema; raw adapter values are exposed.",
      "targetModel": "Historical bars are a finite pull capability over declared Candle<Extension> output. Input interval/time/what-to-show fields and provider-native Contract/date forms are exact leaf schemas; bars carry quality, cursor, freshness, and gap/finality evidence. Scope is explicit Public or Account(AccountScope) when the capability is account-backed; no route invents a default account or subaccount.",
      "pureTransitions": [
        "decodeBarQuery(wire, descriptor.inputSchema) -> Result<BarQuery, InvalidInstant|InvalidInterval|UnboundedLimit>",
        "decodeBars(native, descriptor.outputSchema) -> Result<BarSeries, NativeBarDecodeFailure> preserving Candle extensions and quality."
      ],
      "effectBoundary": "Market-data application calls the selected provider historical adapter; HTTP does not revive Date or construct a Contract. Public bars remain public unless the provider schema explicitly binds an AccountScope. A future push bars leaf uses stream lifecycle/backpressure, not transaction control.",
      "durability": "Bars are cache/projection data with source sequence/as-of/quality and no order WAL, approval, or compensation. A controlled effect may persist selected evidence separately, never the whole stream as a transaction.",
      "errors": [
        "InvalidTimeRange",
        "HistoricalBarsUnavailable(quality)",
        "BarsTruncated(cursor)",
        "NativeBarDecodeFailure",
        "StorageUnavailable",
        "CapabilityNotDeclared(historical-bars)"
      ],
      "preservedBehavior": [
        "保留 start/end ISO input and `{bars}` conceptual response.",
        "保留 interval/whatToShow values only where capability evidence supports them."
      ],
      "replacementSteps": [
        "Define bounded historical input/output codecs and branded Instant.",
        "Move native Contract/date handling to the adapter.",
        "Migrate callers to quality/cursor-aware finite results and preserve gaps/finality."
      ],
      "verificationCases": [
        "ISO times decode deterministically; malformed dates fail before provider invocation; truncation carries cursor/Partial; no JS Date or raw native bar crosses the wire."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source establishes ISO/date and raw bar behavior but not transaction semantics. Retain finite OHLCV precision/quality while treating bars as Candle data delivery.",
      "contractAnchors": [
        "K01",
        "K03",
        "K04",
        "K05",
        "K06",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-2F790BE2E6",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:363-378: Builds an IBKR Contract from arbitrary body JSON, calls account.getContractDetails, returns a raw detail object/null, and maps all errors to a string 500."
      ],
      "currentBehavior": "Builds an IBKR Contract from arbitrary body JSON, calls account.getContractDetails, returns a raw detail object/null, and maps all errors to a string 500.",
      "problem": "The handler turns arbitrary JSON into a native SDK object and exposes raw broker details, which the target transport/protocol boundary forbids.",
      "targetModel": "Contract details is a provider-declared finite pull with Public/Account scope only when its schema requires it with normalized InstrumentDetails and a Found(details) | NotFound result only if that provider declares it. Native null/detail values are decoded at the adapter and never become raw HTTP output. Scope is explicit Public or Account(AccountScope) when the capability is account-backed; no route invents a default account or subaccount.",
      "pureTransitions": [
        "decodeDetailsInput(wire, descriptor.inputSchema) -> Result<DeclaredDetailsQuery, ValidationFailed>",
        "decodeDetailsOutput(native, descriptor.outputSchema) -> Result<Found|NotFound, NativeDetailsDecodeFailure>."
      ],
      "effectBoundary": "Instrument application/adapter owns native Contract creation, provider response decoding, and extension preservation; transport maps typed availability/errors only. Public details are not assigned a default account.",
      "durability": "Catalog/details observations carry version/as-of and may be cached; no transaction intent, approval, or order recovery is involved.",
      "errors": [
        "InstrumentNotFound",
        "DetailsUnavailable",
        "NativeDetailsDecodeFailure",
        "UnauthorizedAccountScope",
        "CapabilityNotDeclared(details)"
      ],
      "preservedBehavior": [
        "保留 details drilldown 的 found/not-found distinction, expressed as a typed response variant rather than raw null。",
        "保留 aliceId/symbol/secType/currency filters through typed query."
      ],
      "replacementSteps": [
        "Remove arbitrary body-to-Contract assignment.",
        "Add a declaration-derived details codec and adapter decoder.",
        "Migrate tool/SDK callers from raw null/native objects to the selected result variants."
      ],
      "verificationCases": [
        "A valid AAPL response is Found with normalized identity and declared derivative extension; missing contract is NotFound, unknown native secType is decode failure, and no raw Contract crosses the boundary."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source exposes arbitrary native body/detail/null values. Retain found/not-found drilldown but make it a provider details leaf with schema-bound decoding and structural absence.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K04",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-FC80F364B6",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:380-388: Reads a legacy TradingGit log directly from UnifiedTradingAccount, with ad hoc numeric limit/symbol query parsing, and returns commit records."
      ],
      "currentBehavior": "Reads a legacy TradingGit log directly from UnifiedTradingAccount, with ad hoc numeric limit/symbol query parsing, and returns commit records.",
      "problem": "The route reads UTA-owned TradingGit state directly, while target Git is rebuildable human-readable projection and SQLite journal is authoritative.",
      "targetModel": "Wallet log is a finite AccountScope-bound audit/projection capability over journal and provider observations. Its query schema has explicit scope, bounded pagination/filter fields; Git commit metadata is an optional display extension, never transaction authority.",
      "pureTransitions": [
        "decodeAuditQuery(wire, descriptor.inputSchema) -> Result<AccountAuditQuery, InvalidCursor|LimitExceeded>",
        "projectAudit(events, observations) -> Result<AuditPage, AuditProjectionFailure> preserving causation/actor/time."
      ],
      "effectBoundary": "AuditQuery application reads journal/projection stores; HTTP does not invoke TradingGit or UnifiedTradingAccount and does not expose mutable staging.",
      "durability": "Journal/events remain authority and audit projection checkpoints are rebuildable. Reading history does not create order effects; legacy Git records may be imported as LegacyEvidence without inventing dispatch identity.",
      "errors": [
        "InvalidAuditCursor",
        "AuditProjectionStale",
        "JournalUnavailable",
        "UnauthorizedAccountScope",
        "CapabilityNotDeclared(audit-log)"
      ],
      "preservedBehavior": [
        "保留 log limit/symbol browsing；保留 human-readable commit/audit metadata 作为 projection。",
        "不再让 Git records authorize execution。"
      ],
      "replacementSteps": [
        "Implement a named audit page schema and bounded cursor.",
        "Migrate SDK log callers to the projection capability.",
        "Retain a read-only historical Git importer only as evidence, not as a write/approval path."
      ],
      "verificationCases": [
        "Rebuilding from the same journal yields the same page/sequence; missing storage is typed unavailable rather than empty success, and Git absence does not erase journal-addressable audit events."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source directly reads TradingGit log with permissive parsing. Retain human-readable browsing and history evidence, but make the route a rebuildable audit data capability.",
      "contractAnchors": [
        "K01",
        "K03",
        "K05",
        "K06",
        "K07",
        "K11",
        "K12"
      ]
    },
    {
      "id": "MAP-F42728FD63",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:390-399: Projects lifecycle-collapsed order rows from uta.exportGitState().commits using domain order-history logic and returns them as an exchange-frontend view."
      ],
      "currentBehavior": "Projects lifecycle-collapsed order rows from uta.exportGitState().commits using domain order-history logic and returns them as an exchange-frontend view.",
      "problem": "Current order history treats Git commits as its source, contrary to the target journal authority and rebuildable projection contract.",
      "targetModel": "Order history is an explicit AccountScope-bound, rebuildable projection capability over declared order observations and, where present, transaction-effect events. It may expose a lifecycle row with provider extensions and typed status, but it does not require a global action set or infer fills from Git.",
      "pureTransitions": [
        "foldOrderHistory(scope, events, observations, outputSchema) -> Result<OrderHistoryPage, ProjectionFailure>",
        "classifyStatus(evidence) -> Working|Filled|Partial|Cancelled|Unknown as declared, never from HTTP status or commit existence."
      ],
      "effectBoundary": "Projection application combines selected provider observations and journal evidence; HTTP reads the named output schema, while Git hash is at most an audit correlation.",
      "durability": "Projection checkpoint/sequence is durable and rebuildable. Transaction events remain authority for controlled effects, while ordinary order-history reads do not acquire locks or dispatch work.",
      "errors": [
        "HistoryProjectionCorrupt",
        "ObservationUnknown(orderId)",
        "ProjectionStale",
        "UnauthorizedAccountScope",
        "CapabilityNotDeclared(order-history)"
      ],
      "preservedBehavior": [
        "保留一行 per order lifecycle collapsed exchange-front-end view。",
        "保留 limit parameter and order source labels, expanded to include prepared/unknown semantics."
      ],
      "replacementSteps": [
        "Replace exportGitState as source with journal/observation projection.",
        "Keep one-row-per-lifecycle presentation only where the output leaf declares it.",
        "Mark legacy rows as LegacyEvidence when dispatch identity is missing."
      ],
      "verificationCases": [
        "Accepted-but-working is not Filled; partial fills preserve cumulative/delta correctness; projection rebuild is sequence-equivalent and no Git commit success creates a fill."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source’s one-row lifecycle view is useful, but its Git source is not authoritative. Retain the presentation and statuses while deriving them from declared observations/events.",
      "contractAnchors": [
        "K03",
        "K04",
        "K05",
        "K08",
        "K11",
        "K12"
      ]
    },
    {
      "id": "MAP-F589132F03",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:401-406: Projects fill-only trade rows from exported Git commits and returns them as a raw trade list."
      ],
      "currentBehavior": "Projects fill-only trade rows from exported Git commits and returns them as a raw trade list.",
      "problem": "The route uses Git commit export as history authority and returns untyped records, with no broker-observed execution/reconciliation or accounting projection boundary.",
      "targetModel": "Trade history is a finite projection of declared fill/external-observation records with exact AccountScope/instrument/quantity/price/money and an explicit provenance variant. It emits only evidenced fills, never a generic transaction row from Git.",
      "pureTransitions": [
        "foldTradeHistory(scope, observations, effectEvents, accounting) -> Result<TradeRows, ProjectionFailure>",
        "dedupeFill(identity, sequence) -> one row per declared execution/observation; unknown identity remains unresolved."
      ],
      "effectBoundary": "Accounting/projection application owns normalization and provenance; HTTP exposes the selected output schema and optional Git audit reference only.",
      "durability": "Provider execution identities/observation sequences make rows rebuildable and corrections idempotent. External observations are data evidence and do not acquire order approval or compensation machinery.",
      "errors": [
        "ExecutionIdentityMissing",
        "ObservationUnknown",
        "AccountingDecodeFailure",
        "TradeProjectionStale",
        "CapabilityNotDeclared(trade-history)"
      ],
      "preservedBehavior": [
        "保留 fill-only trade history and source labels order/external/reconcile。",
        "保留 limit/read-only route; remove Git authority."
      ],
      "replacementSteps": [
        "Build the output from provider observations and controlled-effect events.",
        "Migrate SDK trade-history callers to the typed projection.",
        "Never synthesize a fill from commit/approval/HTTP success."
      ],
      "verificationCases": [
        "An accepted order with no observed fill produces no trade row; duplicate execution identity produces one row; an external trade is tagged external with observation provenance and exact values."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source projects fills from Git commits, but its fill-only presentation and source labels are useful. Retain those outcomes while making provider observation/effect evidence authoritative and explicit.",
      "contractAnchors": [
        "K03",
        "K04",
        "K05",
        "K08",
        "K11",
        "K12"
      ]
    },
    {
      "id": "MAP-B9C114CCA4",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:408-414: Looks up a Git commit by hash through uta.show and returns the commit or a 404 string error."
      ],
      "currentBehavior": "Looks up a Git commit by hash through uta.show and returns the commit or a 404 string error.",
      "problem": "The route treats a Git hash as the lookup authority, while target Git hashes are not transaction identifiers and Git persistence is not recovery source.",
      "targetModel": "Audit entry lookup is an explicit AccountScope-bound finite projection query by a declared AuditRef (projection id, journal sequence, or optional historical Git correlation). TransactionId/DispatchId remain separate identities and no lookup authorizes an effect.",
      "pureTransitions": [
        "decodeAuditRef(raw, descriptor.inputSchema) -> Result<AccountAuditRef, InvalidAuditRef>",
        "lookupAudit(ref, projection) -> Result<AuditEntry, AuditEntryNotFound|ProjectionFailure>."
      ],
      "effectBoundary": "AuditQuery reads the rebuildable projection/journal page; transport does not call `uta.show` or retrieve mutable Git state as authority.",
      "durability": "Response carries source sequence/checkpoint and may include Git display correlation. The lookup is read-only and never creates order or recovery state.",
      "errors": [
        "InvalidAuditRef",
        "AuditEntryNotFound",
        "AuditProjectionStale",
        "UnauthorizedAccountScope",
        "CapabilityNotDeclared(audit-entry)"
      ],
      "preservedBehavior": [
        "保留 UI deep-link to a human-readable historical entry where possible.",
        "保留 missing entry 404 semantics."
      ],
      "replacementSteps": [
        "Define a stable audit projection key and optional historical hash index.",
        "Migrate SDK catch-by-message to typed NotFound.",
        "Keep Git import/read evidence isolated from effect authorization."
      ],
      "verificationCases": [
        "An unknown hash/reference returns typed NotFound; a journal event remains addressable after projection rebuild even if Git is unavailable, and lookup causes no transaction state change."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source treats a Git hash as the lookup authority. Retain deep-link browsing but explicitly separate display references from transaction/effect identities.",
      "contractAnchors": [
        "K01",
        "K03",
        "K05",
        "K06",
        "K11",
        "K12"
      ]
    },
    {
      "id": "MAP-AC8C619E99",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:416-420: Returns uta.status(), exposing pending Git message/staging/head state as the wallet status response."
      ],
      "currentBehavior": "Returns uta.status(), exposing pending Git message/staging/head state as the wallet status response.",
      "problem": "The current route exposes live TradingGit authority. It cannot be deleted while UTAAccountSDK.status remains public, but it must not authorize execution.",
      "targetModel": "Wallet status is an explicit AccountScope-bound read projection capability whose output is selected from the provider/effect descriptors: data resource health and, for a transaction-wrapped effect, Draft/Prepared/Review/Queued/Executing/Reconciling states. Mutable Git staging is never authority.",
      "pureTransitions": [
        "projectStatus(scope, rows, descriptor.outputSchema) -> Result<StatusView, ProjectionStale|NoSubject>",
        "classifyStatusEvidence(evidence) -> declared state variants; no side effect occurs during a status query."
      ],
      "effectBoundary": "ProjectionReader/application query serves status; HTTP does not call UTA.status or write Git. Transaction state is read from the selected wrapper, while data leaf status stays resource-scoped.",
      "durability": "Status includes revision/checkpoint/freshness and may include a presentation-only audit message. It cannot approve, push, or resurrect an effect; provider data streams use their own lifecycle frames.",
      "errors": [
        "NoSubjectFound",
        "ProjectionStale",
        "RecoveryRequired",
        "UnauthorizedAccountScope",
        "CapabilityNotDeclared(status)"
      ],
      "preservedBehavior": [
        "保留 current SDK status() during migration as read-only view.",
        "保留 UI can show staged/pending/recovery information, with renamed semantics."
      ],
      "replacementSteps": [
        "Provide an explicit transaction/effect status query and a separate provider-resource status projection.",
        "Migrate SDK/UI from Git status.",
        "Remove mutable staging as an execution input after callers migrate."
      ],
      "verificationCases": [
        "A draft/effect status shows its revision and cannot be submitted by reading the status; a stale/RecoveryRequired view is explicit, and a data-only provider has no wallet status route."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source exposes pending Git message/staging/head as wallet status. Retain status visibility but make its authority depend on the selected capability and keep Git as presentation evidence only.",
      "contractAnchors": [
        "K01",
        "K03",
        "K05",
        "K08",
        "K11",
        "K12"
      ]
    },
    {
      "id": "MAP-F454EFC3B2",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:422-441: Validates a nonempty message, calls uta.commit synchronously to finalize staged Git operations, and returns the prepared Git commit or a generic 500 string error."
      ],
      "currentBehavior": "Validates a nonempty message, calls uta.commit synchronously to finalize staged Git operations, and returns the prepared Git commit or a generic 500 string error.",
      "problem": "wallet/commit 只校验 message 后同步调用 uta.commit 并返回 Git commit；它把 preparation、审计文本和 Git persistence 混成一个结果，且没有 serializable plan 或 commit-after-approval barrier。",
      "targetModel": "Commit is replaced by a prepare capability available only on a selected controlled-effect wrapper. Its input/output schemas carry subject/revision/Principal and a serializable PreparedPlanRecord; audit rationale is separate and there is no universal commit route. This account-backed prepare capability is always bound to explicit AccountScope; no default subaccount is synthesized.",
      "pureTransitions": [
        "prepareEffect(scope, subject, facts, policy, now) -> Result<PreparedPlanRecord, PrepareFailure|PreconditionFailed>",
        "encodePreparedPlan(planSchema) -> serializable provider payload/semantic evidence with no function, SDK, secret, or raw scope object."
      ],
      "effectBoundary": "Transaction application gathers facts, then the selected writer atomically records preparation state; provider-native compilation occurs at the adapter boundary and no broker dispatch occurs during preparation.",
      "durability": "Prepared plan, revision, digest, before-image/evidence, expiry, and capability revision are durable only for this wrapper; response receipt follows writer commit. Data leaves do not acquire prepare state.",
      "errors": [
        "PrepareFailure",
        "PlanNotRecoverable",
        "PreconditionFailed",
        "StorageUnavailable",
        "ScopeDiscoveryPending",
        "CapabilityNotDeclared(prepare)"
      ],
      "preservedBehavior": [
        "保留 current commit as a validation/preparation boundary.",
        "保留 hash-like digest for optimistic review, but make digest canonical plan identity."
      ],
      "replacementSteps": [
        "Replace wallet/commit with a selected effect prepare descriptor.",
        "Migrate SDK caller to typed prepare response and optional audit metadata.",
        "Prove no provider mutation before approval and preserve provider-native extension payload exactly."
      ],
      "verificationCases": [
        "Prepare success returns one serializable plan/digest with provider state untouched; insufficient facts stay Draft/typed failure; reload after writer commit returns the same preparation identity."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source conflates message, Git commit, and preparation. Retain commit as a review boundary only where a controlled effect declares it, and remove any implication that every provider must implement commit.",
      "contractAnchors": [
        "K01",
        "K03",
        "K07",
        "K08",
        "K09",
        "K10",
        "K12"
      ]
    },
    {
      "id": "MAP-6BF94B5092",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:443-469: Requires pending Git state and an expectedPendingHash, optionally accepts a reason, calls uta.reject, and maps hash conflicts to a 409 code while all other errors become 500 strings."
      ],
      "currentBehavior": "Requires pending Git state and an expectedPendingHash, optionally accepts a reason, calls uta.reject, and maps hash conflicts to a 409 code while all other errors become 500 strings.",
      "problem": "The route authorizes rejection with a process-local Git hash and returns string errors. It has no shared runtime schema or typed transaction-state conflict; revision/actor are not fields of the canonical Reject command.",
      "targetModel": "Reject is a command on a selected transaction wrapper’s review capability, with exact transaction/revision/review identity and optional reason. It can retire an unsent plan only after CAS; it is not a universal broker handler or Git hash operation. This account-backed review command carries explicit AccountScope; no default subaccount is synthesized.",
      "pureTransitions": [
        "rejectEffect(scope, state, command, now) -> Result<PlanRetired|ReviewRejected|RecoveryRequired, InvalidState|VersionConflict|Unauthorized>",
        "validateReviewIdentity(state, command) -> Result<AcceptedReject, Conflict> before writer append."
      ],
      "effectBoundary": "Application loads the selected durable effect state and calls its writer; no TradingGit.reject or provider dispatch occurs. Data-only leaves have no reject route.",
      "durability": "Rejection/retirement and cleanup of unstarted wrapper jobs/locks are atomic; once dispatch may have occurred, the wrapper retains RecoveryCase/unknown evidence and does not discard it.",
      "errors": [
        "InvalidState",
        "VersionConflict",
        "UnauthorizedOperation",
        "RecoveryRequired",
        "StorageUnavailable",
        "CapabilityNotDeclared(reject)"
      ],
      "preservedBehavior": [
        "保留 optional human reason and user-rejected audit projection。",
        "保留 stale optimistic guard returns conflict rather than silently accepting."
      ],
      "replacementSteps": [
        "Replace pending Git/hash checks with selected review schema and CAS.",
        "Keep human reason in audit metadata.",
        "Map post-dispatch/unknown states to recovery rather than pretending a rejected effect."
      ],
      "verificationCases": [
        "An unstarted prepared effect retires with no provider mutation and one durable result; a dispatched/unknown effect returns RecoveryRequired with retained evidence; a data leaf has no reject operation."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source’s stale-hash reject protects Git staging but assumes every route has it. Retain CAS and no-dispatch safety only within the selected transaction wrapper.",
      "contractAnchors": [
        "K03",
        "K08",
        "K09",
        "K10",
        "K12"
      ]
    },
    {
      "id": "MAP-7B9EA96B62",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:471-496: Requires pending Git state and expectedPendingHash, calls uta.push to execute staged operations, maps PendingHashConflictError to 409, and exposes push result or string 500."
      ],
      "currentBehavior": "Requires pending Git state and expectedPendingHash, calls uta.push to execute staged operations, maps PendingHashConflictError to 409, and exposes push result or string 500.",
      "problem": "wallet/push 依赖 pending Git state/hash 后直接调用 uta.push，且把异常变成 409/500；它没有 durable DispatchPlanned/DispatchStarted，也没有区分人工 approval 与受授权的 AI auto-approval。",
      "targetModel": "Approve is the selected transaction wrapper’s explicit review/authorization leaf. It binds effect revision/digest/expiry, Principal and server policy, then returns a queued durable receipt; Human or authorized-auto policy is derived server-side and is not a body field. This account-backed approval command carries explicit AccountScope; no default subaccount is synthesized.",
      "pureTransitions": [
        "approveEffect(scope, state, binding, policy, now) -> Result<QueuedReceipt, VersionConflict|DigestMismatch|Expired|CapabilityRevoked|InvalidState>",
        "classifyRemoteOutcome(outcome) -> Ack|Rejected|Unknown in the later provider-observation path, never in the HTTP approval response."
      ],
      "effectBoundary": "The wrapper writer atomically records approval, dispatch plan, jobs, locks, and receipt; scheduler/provider interpreter later performs the selected effect. No approval route invokes a data reader or universal broker method.",
      "durability": "Receipt/jobs/locks are durable after writer commit; DispatchStarted precedes provider call, and Ack is not fill/terminal completion. Unknown pauses forward work and schedules only declared observation/recovery.",
      "errors": [
        "VersionConflict",
        "PlanDigestMismatch",
        "ApprovalExpired",
        "CapabilityRevoked",
        "OutcomeUnknown",
        "StorageUnavailable",
        "CapabilityNotDeclared(approve)"
      ],
      "preservedBehavior": [
        "保留 human approval wall when AI trading is disabled；保留 source tool 的 enabled path as an explicit service-authorized auto-approval mode, replacing Git hash with plan binding。",
        "无论 mode，approval 仍先写 durable plan/jobs，Ack ≠ fill。"
      ],
      "replacementSteps": [
        "Replace wallet/push with an effect-specific approval descriptor.",
        "Derive mode from verified policy/actor and preserve allowAiTrading as deployment policy evidence, never request authorization.",
        "Delete Git push authority after caller cutover and retain provider outcome evidence separately."
      ],
      "verificationCases": [
        "Disabled policy remains AwaitingReview; authorized policy queues exactly one receipt after binding checks; body allow-AI cannot self-authorize; a post-DispatchStarted disconnect becomes Unknown/observe rather than duplicate dispatch."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source maps pending Git push to HTTP success and assumes a global push method. Retain human approval and auto-policy distinctions while confining durable execution to the selected transaction wrapper.",
      "contractAnchors": [
        "K03",
        "K08",
        "K09",
        "K10",
        "K12"
      ]
    },
    {
      "id": "MAP-58D4065C2F",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:498-516: Stage-only route accepts arbitrary JSON and calls uta.stagePlaceOrder directly, returning TradingGit.add-like result or a string 400 error."
      ],
      "currentBehavior": "Stage-only route accepts arbitrary JSON and calls uta.stagePlaceOrder directly, returning TradingGit.add-like result or a string 400 error.",
      "problem": "stage-place-order 接受任意 JSON 并直接执行 TradingGit.add-like staging；draft 的完整 OrderSpec、scope/revision 和 no-job-before-approval invariant 因而不在 authority 中。",
      "targetModel": "Stage-place-order becomes a draft capability for a provider-declared Order semantic unit, optionally wrapped for controlled effects. Its input is a composed schema with declared provider extensions and local sizing/terms constraints; no universal order variant list is imposed. An account-backed draft carries explicit AccountScope; a public data leaf has no draft state.",
      "pureTransitions": [
        "decodeDraftInput(wire, descriptor.inputSchema) -> Result<AccountScopedOrderDraft, ValidationFailed|RelationFailure>",
        "replaceDraft(current, draft, expectedRevision) -> Result<DraftRevision, VersionConflict> with canonical digest and no provider IO."
      ],
      "effectBoundary": "HTTP/application writes only the selected wrapper’s durable draft via its writer; provider-native compilation and availability are deferred to that leaf’s prepare/interpreter.",
      "durability": "Draft revision/input/schema fingerprint are serializable and durable only for the selected effect; no Git staging, dispatch job, approval, or lock is created by draft submission.",
      "errors": [
        "ValidationFailed",
        "ScopeRequired",
        "InstrumentResolutionFailure",
        "VersionConflict",
        "DuplicateCommand",
        "CapabilityNotDeclared(order-draft)"
      ],
      "preservedBehavior": [
        "保留 stage-only agent ergonomics（draft now, review/approve later）作为选定受控 effect 的可选能力。",
        "保留 provider schema 已声明的订单字段与精度，不把一个 provider 的扩展强加给其他 provider。"
      ],
      "replacementSteps": [
        "Replace stage-place route with a declared draft leaf and shared codec.",
        "Migrate caller to provider-specific schema while preserving exact extension fields.",
        "Do not require a provider to expose draft if it does not declare a controlled order effect."
      ],
      "verificationCases": [
        "A declared draft preserves provider terms and canonical decimals; invalid relation/unknown field produces no event; repeated command identity converges to one draft receipt and no provider mutation."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source uses arbitrary JSON and TradingGit.add. Retain stage-now/review-later ergonomics, but make drafting a schema-derived capability and keep transaction durability optional per provider declaration.",
      "contractAnchors": [
        "K01",
        "K03",
        "K04",
        "K08",
        "K09",
        "K12"
      ]
    },
    {
      "id": "MAP-9B42BF213C",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:518-528: Stage-only route accepts arbitrary JSON and directly calls uta.stageModifyOrder, returning a legacy Git add result or a string 400 error."
      ],
      "currentBehavior": "Stage-only route accepts arbitrary JSON and directly calls uta.stageModifyOrder, returning a legacy Git add result or a string 400 error.",
      "problem": "stage-modify-order 把任意 body 转给 legacy stageModifyOrder；它没有目标订单的 ScopedOrderRef/version，也不能表示 native amend 与 cancel-replace 的语义损失。",
      "targetModel": "Modify draft is available only when the provider declares a modify-capable Order effect. Its schema carries a scoped provider order identity, current revision/before-image, and an exact replacement/patch form declared by that leaf; no Partial<Order> universal contract. The selected amend target also carries explicit AccountScope; no default subaccount is synthesized.",
      "pureTransitions": [
        "resolveModifyTarget(scope, listing, command) -> Result<BeforeImage, OrderNotFound|VersionConflict|ListingIncomplete>",
        "composeModifyInput(providerSchema, replacement) -> Result<DeclaredModifyInput, RelationFailure|CapabilityUnavailable>."
      ],
      "effectBoundary": "Application gathers the selected provider observation and submits a draft; adapter compiles native amend/cancel-replace according to its declared capability and reports semantic-loss evidence.",
      "durability": "Draft/plan retains target identity/version, replacement schema, provider capability evidence, and compensation assessment only for the selected transaction wrapper; data listings remain read-only.",
      "errors": [
        "OrderNotFound",
        "OrderVersionConflict",
        "ListingIncomplete",
        "ValidationFailed(unknownField)",
        "CapabilityUnavailable",
        "CapabilityNotDeclared(modify)"
      ],
      "preservedBehavior": [
        "保留 modify endpoint intent but require complete semantics and target identity.",
        "保留 broker identity/queue-loss risk as explicit outcome, not fake success."
      ],
      "replacementSteps": [
        "Replace arbitrary body with the provider’s modify schema.",
        "Require complete semantics where the leaf declares replacement, or omit the modify route when absent.",
        "Record amend versus cancel-replace evidence without claiming native guarantees."
      ],
      "verificationCases": [
        "Stale identity cannot prepare; a declared native amend preserves identity evidence; a cancel-replace provider reports semantic loss/unknown risk, and an undeclared modify capability has no route."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source forwards any body to a legacy modify method. Retain target identity/version and semantic-loss concerns, but let each provider declare its exact modify form rather than imposing a common partial-order API.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K04",
        "K08",
        "K12"
      ]
    },
    {
      "id": "MAP-98CE02C334",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:530-540: Stage-only route accepts arbitrary JSON and directly calls uta.stageClosePosition, returning a legacy Git add result or a string 400 error."
      ],
      "currentBehavior": "Stage-only route accepts arbitrary JSON and directly calls uta.stageClosePosition, returning a legacy Git add result or a string 400 error.",
      "problem": "stage-close-position 直接把任意 body 交给 stageClosePosition；qty 缺省同时承载 close-all 和缺字段两种含义，且没有 exposure before-image 或准备期重检。",
      "targetModel": "Close-position draft is a provider-declared Order effect leaf when supported. Quantity|All is a small local union only if that leaf declares it; scope, InstrumentId, exposure revision, and before-image are explicit, while no transaction state is imposed on data readers. The selected close effect carries explicit AccountScope; no default subaccount is synthesized.",
      "pureTransitions": [
        "decodeCloseDraft(scope, wire, descriptor.inputSchema) -> Result<DeclaredCloseInput, ValidationFailed>",
        "resolveAll(scope, exposure, command) -> Result<PreparedTarget, ExposureChanged|NoExposure> and persist the resolved target before approval."
      ],
      "effectBoundary": "Application/accounting gathers exposure for the selected effect; its writer prepares durable state and the provider interpreter dispatches only after approval. A provider without close has no close leaf.",
      "durability": "Draft/plan stores explicit All resolution, exposure precondition, scope, and provider capability evidence. Partial/unknown outcomes use the wrapper’s recovery path, not data-query compensation.",
      "errors": [
        "NoExposure",
        "ExposureVersionConflict",
        "QuantityExceedsExposure",
        "ScopeRequired",
        "CapabilityUnavailable",
        "CapabilityNotDeclared(close)"
      ],
      "preservedBehavior": [
        "保留 explicit qty precision and close-all semantics.",
        "保留 close as risk-reducing action only if policy explicitly grants exemption."
      ],
      "replacementSteps": [
        "Replace stage-close with the declared close schema.",
        "Keep All as an explicit tag and recheck exposure at prepare.",
        "Separate risk-reducing close execution from ordinary position reads and do not invent native compensation guarantees."
      ],
      "verificationCases": [
        "Explicit .001 remains exact; All on exposure 1.5 persists 1.5 before approval; exposure drift blocks/requires reprepare and an undeclared provider does not expose close."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source’s omitted qty conflates All and missing data, and its staging authority is Git. Retain close-all/precision intent while making the variant provider-declared and effect-scoped.",
      "contractAnchors": [
        "K01",
        "K03",
        "K04",
        "K08",
        "K09",
        "K10",
        "K12"
      ]
    },
    {
      "id": "MAP-D5B92C2A08",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:542-555: Stage-only cancel route manually checks that orderId is a string, calls uta.stageCancelOrder, and returns a legacy Git add result or string 400 error."
      ],
      "currentBehavior": "Stage-only cancel route manually checks that orderId is a string, calls uta.stageCancelOrder, and returns a legacy Git add result or string 400 error.",
      "problem": "stage-cancel-order 只检查 orderId 是 string 后就写入 staging；它没有 ListingComplete、order version/fills before-image 或与 simulator force-cancel 的边界。",
      "targetModel": "Cancel draft exists only for a provider-declared cancel-capable Order effect. It uses a scoped provider identity/version and any declared status/fill preconditions; simulator force-cancel is a separate simulator leaf and cannot fill this route. The selected cancel effect carries explicit AccountScope; no default subaccount is synthesized.",
      "pureTransitions": [
        "decodeCancelDraft(scope, wire, descriptor.inputSchema) -> Result<DeclaredCancelInput, InvalidBrokerOrderId>",
        "resolveCancelTarget(scope, listing, command) -> Result<PreparedTarget, OrderNotFound|ListingIncomplete|VersionConflict|TerminalState>."
      ],
      "effectBoundary": "Selected application effect prepares the cancel and provider adapter interprets it after approval; HTTP performs no force-cancel or native call.",
      "durability": "Draft/plan retains target identity/version, before-image/fills, plan digest, and provider capability evidence; unknown/late fills remain effect recovery observations, not a data listing result.",
      "errors": [
        "InvalidBrokerOrderId",
        "OrderVersionConflict",
        "OrderAlreadyTerminal",
        "ListingIncomplete",
        "CapabilityUnavailable",
        "CapabilityNotDeclared(cancel)"
      ],
      "preservedBehavior": [
        "保留 orderId validation and stage-before-approval flow.",
        "Preserve late fill/identity loss as typed observation/compensation risk."
      ],
      "replacementSteps": [
        "Use provider-declared identity/precondition schema instead of manual string check.",
        "Migrate caller to effect draft/review path.",
        "Keep simulator admin cancel isolated and require Complete listing evidence only where provider effect declares it."
      ],
      "verificationCases": [
        "Empty id/stale version fails without a writer event; an accepted cancel yields a queued effect receipt, and later Ack does not imply terminal cancellation when a late fill is observed."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source only checks a string and stages Git, while simulator force-cancel has different semantics. Retain identity validation and late-fill risk, but make cancellation capability-specific and effect-scoped.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K04",
        "K08",
        "K12"
      ]
    },
    {
      "id": "MAP-52CB0F382D",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:557-576: One-shot handler validates the legacy place-order bag, strips message, calls uta.stagePlaceOrder, then runOneShot performs commit and push in one HTTP request."
      ],
      "currentBehavior": "One-shot handler validates the legacy place-order bag, strips message, calls uta.stagePlaceOrder, then runOneShot performs commit and push in one HTTP request.",
      "problem": "one-shot place-order 在同一请求中 stage、commit、push；因此输入请求隐式获得 venue mutation 权限，prepare/approval 前无法证明 broker ledger 为空。",
      "targetModel": "The legacy one-shot place route is not an execution contract. Replace it only where a provider declares an Order effect and transaction wrapper: draft -> prepare -> review/approve -> durable dispatch/recovery. Providers without that capability expose no one-shot route. Any account-backed one-shot migration request carries explicit AccountScope; data leaves remain scope-appropriate and transaction-free.",
      "pureTransitions": [
        "decodeLegacyOneShot(scope, wire, descriptor) -> Result<MigrationRequest, ValidationFailed|ApprovalRequired>",
        "composeEffectFlow(leaf, withTransaction) -> Result<DurableReceipt, NoImplicitApproval> without combining approval and provider dispatch in one function."
      ],
      "effectBoundary": "HTTP may submit a draft/prepare request; only the selected transaction wrapper writer creates dispatch work and scheduler/provider interpreter executes it. A data leaf remains pull/push data delivery.",
      "durability": "Write-ahead intent and event/job identity precede provider mutation for the selected wrapper; unknown pauses remaining effect steps and enters its declared recovery/review path.",
      "errors": [
        "ApprovalRequired",
        "ValidationFailed",
        "DuplicateCommand",
        "StorageUnavailable",
        "OutcomeUnknown",
        "CapabilityNotDeclared(order-effect)"
      ],
      "preservedBehavior": [
        "保留 existing user-facing order entry intent via multi-step API, not single roundtrip.",
        "保留 precision and full OrderSpec."
      ],
      "replacementSteps": [
        "Remove one-shot route as mutation authority after callers migrate.",
        "Expose declared draft/prepare/approve/recovery leaves and update proxy policy.",
        "Do not add compatibility fallback that silently dispatches through the old path."
      ],
      "verificationCases": [
        "Draft/prepare leave provider ledger untouched; only explicit human or server-authorized policy approval queues a job; a post-dispatch crash yields one identity plus observe/recovery, never an implicit second order."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source gives one request stage/commit/push authority. Retain user-facing order entry intent but replace the one-shot route with a selected transaction wrapper, without requiring every provider to implement it.",
      "contractAnchors": [
        "K01",
        "K03",
        "K04",
        "K08",
        "K09",
        "K10",
        "K12"
      ]
    },
    {
      "id": "MAP-32AD0C3DCD",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:578-592: One-shot close handler validates the legacy close body, strips message, preserves qty as a string, then stages/commits/pushes through runOneShot."
      ],
      "currentBehavior": "One-shot close handler validates the legacy close body, strips message, preserves qty as a string, then stages/commits/pushes through runOneShot.",
      "problem": "one-shot close-position 把 string qty（或缺省 qty）带过 stage→commit→push；执行前的 All resolution、exposure drift 与 partial-fill recovery 均被旧 Git pipeline 隐藏。",
      "targetModel": "Legacy one-shot close is replaced by a declared close-effect flow with explicit Quantity|All, scoped exposure evidence, prepare/review/approve, and provider-specific outcome schema. No provider close capability means no route; no data route receives effect state. The close-effect flow carries explicit AccountScope; no default subaccount is synthesized.",
      "pureTransitions": [
        "decodeCloseEffect(scope, wire, descriptor) -> Result<DeclaredCloseInput, ValidationFailed>",
        "prepareClose(scope, exposureFacts, now) -> Result<PreparedTarget, ExposureChanged|NoExposure> with All resolved before review."
      ],
      "effectBoundary": "Accounting/application prepares the selected effect and its writer/scheduler/provider interpreter handle durable execution; HTTP returns effect state/receipt, not a Git push result.",
      "durability": "Resolved All quantity, before-image, criterion, and provider evidence are durable in the wrapper; partial/unknown outcomes remain reconciling/observed and do not trigger generic data compensation.",
      "errors": [
        "ValidationFailed",
        "ScopeRequired",
        "ExposureChanged",
        "CapabilityUnavailable",
        "OutcomeUnknown",
        "CapabilityNotDeclared(close)"
      ],
      "preservedBehavior": [
        "保留 qty string precision and no-qty close-all behavior.",
        "Preserve target distinction between close and place SELL (risk/authorization)."
      ],
      "replacementSteps": [
        "Delete one-shot close authority after caller migration.",
        "Add a typed close leaf only for providers that declare it.",
        "Cover external exposure change and partial outcomes through the selected wrapper’s recovery evidence."
      ],
      "verificationCases": [
        "All captures current exposure at prepare; a pre-approval external fill forces reprepare; an accepted close with partial/unknown outcome remains explicit until observation proves the declared criterion."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source preserves qty precision but hides All resolution and outcome handling in Git. Retain those close semantics while making the effect flow provider-declared and durable only when selected.",
      "contractAnchors": [
        "K01",
        "K03",
        "K04",
        "K08",
        "K09",
        "K10",
        "K12"
      ]
    },
    {
      "id": "MAP-AC5E619E59",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:594-606: One-shot cancel validates orderId/message, stages cancellation, and immediately commits/pushes through the Git pipeline."
      ],
      "currentBehavior": "One-shot cancel validates orderId/message, stages cancellation, and immediately commits/pushes through the Git pipeline.",
      "problem": "one-shot cancel 校验 orderId/message 后立即 stage、commit、push；并发 fill、order version 和 Unknown outcome 没有 durable observation 边界。",
      "targetModel": "Legacy one-shot cancel is removed as authority. A provider-declared cancel effect uses its own input/precondition schema and optional withTransaction wrapper; immediate Git push and simulator force-cancel are structurally absent from this capability. The selected cancel effect carries explicit AccountScope; no default subaccount is synthesized.",
      "pureTransitions": [
        "decodeCancelEffect(scope, wire, descriptor) -> Result<DeclaredCancelInput, ValidationFailed>",
        "decideCancel(scope, state, command) -> Result<EffectEvents|InvalidState|VersionConflict> before scheduler/provider invocation."
      ],
      "effectBoundary": "The selected wrapper writer creates approval/job identity; scheduler/provider adapter dispatches and observes late fills. HTTP only encodes the durable state.",
      "durability": "Order identity/version/precondition and DispatchIdentity are durable for the selected effect; Ack is not terminal cancellation and unknown remains RecoveryRequired/Reconciling.",
      "errors": [
        "ValidationFailed",
        "OrderVersionConflict",
        "OrderAlreadyTerminal",
        "OutcomeUnknown",
        "StorageUnavailable",
        "CapabilityNotDeclared(cancel)"
      ],
      "preservedBehavior": [
        "保留 cancellation caller endpoint intent and order identity precision.",
        "Do not silently turn cancel into success when venue rejects/race fills."
      ],
      "replacementSteps": [
        "Replace one-shot route with the declared cancel/effect endpoints.",
        "Migrate SDK/UI and preserve typed identity precision.",
        "Test concurrent fill/unknown observation without reusing simulator control."
      ],
      "verificationCases": [
        "An approved working order yields one queued job; a race fill is retained as late observation rather than falsely cancelled; duplicate command identity converges without a second dispatch."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source combines order validation and immediate Git dispatch, while simulator force-cancel is a separate concern. Retain cancel intent and race safety via a selected effect wrapper only.",
      "contractAnchors": [
        "K01",
        "K03",
        "K04",
        "K08",
        "K09",
        "K10",
        "K12"
      ]
    },
    {
      "id": "MAP-4D35581185",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:608-630: GET snapshots reads an optional file-backed SnapshotService and silently returns an empty list on missing service/errors; DELETE decodes a timestamp and directly deletes a snapshot, returning simple success/not-found responses."
      ],
      "currentBehavior": "GET snapshots reads an optional file-backed SnapshotService and silently returns an empty list on missing service/errors; DELETE decodes a timestamp and directly deletes a snapshot, returning simple success/not-found responses.",
      "problem": "The route delegates to the current file-backed snapshot service, hides all read/delete failures as empty/simple responses, and has no shared schemas or projection checkpoint semantics.",
      "targetModel": "Snapshots are a finite data pull capability over SnapshotViewEnvelope and a separate authorized retention capability. Snapshot schema carries scope/time/schemaVersion/sourceSequence/freshness; retention is not an order approval/compensation flow and file JSONL is not authority. Account snapshots and retention requests carry explicit AccountScope; public snapshots remain Public only if declared.",
      "pureTransitions": [
        "decodeSnapshotQuery(wire, descriptor.inputSchema) -> Result<SnapshotQuery, InvalidScope|InvalidTimestamp|InvalidLimit>",
        "selectSnapshots(projection, coverage) -> Complete|Partial|Unavailable; decodeRetention(ref) -> Result<RetentionRequest, ValidationFailed>."
      ],
      "effectBoundary": "Projection service owns typed snapshot reads; a retention application port performs authorized deletion/audit. HTTP only verifies Principal, decodes the selected leaf, and encodes results; no direct file surgery.",
      "durability": "Snapshots/checkpoints rebuild from journal/observations; retention records its own audit/evidence atomically where required. Read failures are explicit and never `{snapshots: []}`; no order WAL is used for data reads.",
      "errors": [
        "SnapshotServiceUnavailable",
        "ProjectionStale",
        "SnapshotNotFound",
        "ForbiddenRetention",
        "StorageUnavailable",
        "CapabilityNotDeclared(retention)"
      ],
      "preservedBehavior": [
        "保留 per-account recent snapshots query and optional limit.",
        "保留 not-found delete response, but typed; preserve auditability."
      ],
      "replacementSteps": [
        "Migrate SnapshotService reads to projection/retention capability ports.",
        "Define bounded query/ref codecs and Complete/Partial/Unavailable response.",
        "Keep legacy file data as read-only migration input until importer/rebuild evidence exists."
      ],
      "verificationCases": [
        "Malformed timestamp/limit fails before store access; missing storage is Unavailable; stale/partial data remains visible; authorized retention changes only requested scope/time and records audit, unauthorized retention changes nothing."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source hides snapshot failures and deletes file rows directly. Retain recent snapshot and retention intent, but separate finite data delivery from the independent retention effect and its audit evidence.",
      "contractAnchors": [
        "K01",
        "K03",
        "K05",
        "K06",
        "K07",
        "K11",
        "K12"
      ]
    },
    {
      "id": "MAP-8F1E449867",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/routes-trading.ts:632-694: Loads per-account snapshots, groups timestamps by minute, sums Number-converted net liquidation values, carries forward missing accounts, and silently returns {points:[]} on any failure."
      ],
      "currentBehavior": "Loads per-account snapshots, groups timestamps by minute, sums Number-converted net liquidation values, carries forward missing accounts, and silently returns {points:[]} on any failure.",
      "problem": "Transport performs aggregation over file-backed snapshots, coerces financial values to JavaScript numbers, mutates intermediate records for carry-forward, and hides projection/storage failures as empty data.",
      "targetModel": "Equity curve is a finite accounting/data projection capability using exact Money/Decimal, explicit bucket/carry-forward policy, scope rows, FX provenance, and coherence. It does not use order locks, approval, or compensation. Curve rows retain explicit AccountScope and never synthesize a default wallet.",
      "pureTransitions": [
        "aggregateByBucket(snapshots, bucketPolicy, carryForwardPolicy) -> Result<EquityCurve, MissingSources|IncoherentSnapshot>",
        "markCarryForward(value, agePolicy) -> Fresh|Stale|Unavailable without fabricating zeros or mutating source snapshots."
      ],
      "effectBoundary": "Accounting/projection service computes the curve from declared snapshot/FX data leaves; HTTP reads/encodes the result and never performs live broker aggregation.",
      "durability": "Curve checkpoint/source sequence/as-of are durable read evidence; carry-forward is bounded and marked stale. No transaction intent or recovery state is created by this query.",
      "errors": [
        "EquityProjectionUnavailable",
        "FxUnavailable",
        "SnapshotIncoherent",
        "ProjectionStale",
        "AccountScopeUnauthorized",
        "CapabilityNotDeclared(equity-curve)"
      ],
      "preservedBehavior": [
        "保留 minute grouping, chronological sorting, per-account values and carried values where policy permits.",
        "Preserve empty curve only when Complete query has no observations, not on any failure."
      ],
      "replacementSteps": [
        "Move aggregation and carry-forward policy into accounting projection.",
        "Use exact Decimal/Money and explicit UTC/bucket schema.",
        "Migrate SDK/UI to typed partial/stale states and preserve empty only for Complete(empty)."
      ],
      "verificationCases": [
        "Two snapshots in one declared bucket sum exactly; a missing account carries only within max age and is marked stale; FX/store failure yields Partial/Unavailable rather than rate=1, empty points, or uncaught success."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source performs Number conversion and mutable carry-forward in transport. Retain minute grouping/carry policy as data behavior, but make it a typed projection outside order execution.",
      "contractAnchors": [
        "K01",
        "K03",
        "K04",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-248B2B5279",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/simulator.spec.ts:1-12: Documents and imports an end-to-end HTTP adapter spec intended to use a real MockBroker, explicitly checking route wiring and markPrice-to-position behavior rather than vi.fn broker stubs."
      ],
      "currentBehavior": "Documents and imports an end-to-end HTTP adapter spec intended to use a real MockBroker, explicitly checking route wiring and markPrice-to-position behavior rather than vi.fn broker stubs.",
      "problem": "simulator.spec 声明用真实 MockBroker 验证 HTTP wiring 与 markPrice-to-position；它只覆盖一条进程内 vertical slice，不能证明认证、fixture journal 或 production transaction 隔离。",
      "targetModel": "The simulator integration spec is a provider-specific vertical slice: real MockBroker interpreter behind a typed simulator-control leaf, shared transport codec, explicit Principal/capability, and no claim that this proves other provider trees or transaction runtime. The simulator scenario binds its control and observation to explicit AccountScope.",
      "pureTransitions": [
        "runScenario(input, leafSchema) -> Result<ProtocolResult, ProtocolFailure>",
        "assertSimulatorInvariant(before, after, observationSchema) -> Result<ScenarioEvidence, InvariantFailure> without inferring unexercised guarantees."
      ],
      "effectBoundary": "Test composition supplies the real mock interpreter and simulator application port; HTTP request/response codecs run end-to-end within the fixture.",
      "durability": "This scenario has no durable transaction proof. Where a separate test exercises an effect wrapper, it must inspect writer/jobs/provider ledger independently; simulator control itself records only fixture observations.",
      "errors": [
        "ProtocolDecodeFailure",
        "SimulatorCapabilityUnavailable",
        "UnexpectedRemoteOutcome",
        "CapabilityNotDeclared(effect)"
      ],
      "preservedBehavior": [
        "保留 real MockBroker rather than vi.fn for matching behavior.",
        "保留 markPrice-to-position assertion as evidence."
      ],
      "replacementSteps": [
        "Replace casted context with typed simulator capability composition.",
        "Keep real MockBroker matching evidence for this provider.",
        "Add separate durable effect/recovery scenarios rather than inflating this spec into a global completion claim."
      ],
      "verificationCases": [
        "An authorized mark-price scenario changes the real mock state exactly as its declared rule says, while the test records no production order approval/job and does not treat HTTP status as dispatch proof."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source deliberately tests real MockBroker wiring instead of a spy. Retain that empirical simulator evidence and bound its claim to the selected provider leaf.",
      "contractAnchors": [
        "K01",
        "K03",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-2B0B0BF616",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/simulator.spec.ts:13-31: Creates a fake UTA map with MockBroker or arbitrary object values, exposes get/resolve/listUTAs manager stubs, and casts the context to UTAEngineContext."
      ],
      "currentBehavior": "Creates a fake UTA map with MockBroker or arbitrary object values, exposes get/resolve/listUTAs manager stubs, and casts the context to UTAEngineContext.",
      "problem": "simulator 测试用任意 object 充当 broker 并 cast UTAEngineContext；错误 capability、scope 或非 simulator 实例可在请求前被类型系统放过。",
      "targetModel": "Simulator test context is a typed registry of declared simulator capability instances plus application ports/principal. It rejects arbitrary objects masquerading as provider handles and does not model a complete UTAEngineContext. Simulator fixture configuration carries explicit AccountScope rather than an implicit wallet.",
      "pureTransitions": [
        "makeSimulatorContext(config) -> Result<SimulatorTestContext, FixtureTypeError|DuplicateCapabilityIdentity>",
        "authorizeFixture(principal, scope, descriptor) -> Result<AuthorizedFixture, UnauthorizedTestPrincipal>."
      ],
      "effectBoundary": "Test Layer hides MockBroker behind the simulator SPI and supplies the selected capability; non-simulator providers are represented by their own declared descriptors, not casts.",
      "durability": "The context map is fixture wiring, not authority. An optional durable store belongs only to tests of the selected effect wrapper; simulator data controls remain fixture observations.",
      "errors": [
        "FixtureTypeError",
        "SimulatorCapabilityUnavailable",
        "UnauthorizedTestPrincipal",
        "CapabilityNameConflict"
      ],
      "preservedBehavior": [
        "保留 one mock + one non-mock listing scenarios.",
        "保留 per-test isolation."
      ],
      "replacementSteps": [
        "Delete FakeUTA/unknown broker casts.",
        "Use typed provider capability registration and principal scope in every helper.",
        "Retain one mock/one non-mock listing scenario as a boundary test."
      ],
      "verificationCases": [
        "A non-Mock object cannot install as a simulator capability; a real provider remains excluded without pretending its handler exists, and wrong scope/principal fails before request execution."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source uses arbitrary objects and a casted context, which hides capability mistakes. Retain isolation and mixed-instance coverage with an explicit capability registry.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-DB305A9D5D",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/simulator.spec.ts:33-42: Builds Hono Request objects with optional JSON body, invokes the route app, parses any non-204 response as JSON, and returns status/body pairs."
      ],
      "currentBehavior": "Builds Hono Request objects with optional JSON body, invokes the route app, parses any non-204 response as JSON, and returns status/body pairs.",
      "problem": "测试 helper 直接构造 Hono Request、读取任意 JSON status/body；它没有统一 credential、protocol decoder 或 mutation 后 durable receipt 的观察路径。",
      "targetModel": "HTTP test helper is an outer protocol harness: it builds authenticated requests, decodes each selected capability’s success/error schema, and supports finite pull, no-content, and declared push lifecycle. It does not choose a universal response JSON shape. When the selected capability is account-backed, the helper carries explicit AccountScope in the decoded request.",
      "pureTransitions": [
        "buildRequest(method, path, body, credential) -> Request",
        "decodeResponse(status, wire, descriptor) -> Result<DeclaredSuccess, DeclaredFailure|ProtocolDecodeFailure>."
      ],
      "effectBoundary": "The helper drives the actual Hono transport while application/resource services are supplied by the fixture; credential/Principal is provisioned by the test boundary, never by request body actor fields.",
      "durability": "Mutation assertions inspect durable receipt/events only when the selected capability is transaction-wrapped; data pulls/streams inspect projection/cursor/lifecycle evidence and do not use order WAL.",
      "errors": [
        "ProtocolDecodeFailure",
        "UnauthenticatedTransport",
        "UnexpectedStatus(code)",
        "CapabilityNotDeclared"
      ],
      "preservedBehavior": [
        "保留 optional JSON body and 204 handling.",
        "保留 direct Hono request convenience."
      ],
      "replacementSteps": [
        "Add service credential and content-type setup tied to the transport verifier contract.",
        "Decode against the descriptor’s exact schema instead of casting arbitrary JSON.",
        "For mutation leaves, assert downstream durable state; for push leaves, assert cancel/end/backpressure behavior."
      ],
      "verificationCases": [
        "Missing credential fails before service; malformed provider response fails decoding; a valid 204 is NoContent; a selected push closes/cancels its resource without cancelling unrelated durable effects."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source helper only wraps Hono status/body parsing. Retain its convenience while making authentication and declaration-bound decoding explicit and keeping durability expectations capability-specific.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K05",
        "K06",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-793589DE30",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/simulator.spec.ts:46-56: Creates one real MockBroker and one non-Mock object, verifies GET /utas returns 200 and includes only the simulator id."
      ],
      "currentBehavior": "Creates one real MockBroker and one non-Mock object, verifies GET /utas returns 200 and includes only the simulator id.",
      "problem": "GET /utas 测试只断言 real MockBroker 被列出而 arbitrary object 被排除；它未验证 simulator admin authorization、generation/coverage，且可能与生产 account listing 混用。",
      "targetModel": "The simulator listing test targets a finite simulator discovery leaf with Complete coverage only for the declared registry namespace; production AccountList is a separate capability. Mock inclusion is metadata/capability evidence, not an instanceof-based universal rule. Each listed simulator identity carries explicit AccountScope and is checked against that scope authorization.",
      "pureTransitions": [
        "listSimulatorProjection(registry, principal) -> Result<ListingResult<SimulatorSummary>, Forbidden|ListingUnavailable>",
        "assertCoverage(result) -> Complete only when the simulator namespace is explicitly covered."
      ],
      "effectBoundary": "The test invokes typed simulator-control discovery through application composition, not direct manager enumeration; real providers and keyless data instances remain outside this leaf.",
      "durability": "The list is read-only and carries generation/checkpoint; no transaction/effect state changes or provider calls occur while listing.",
      "errors": [
        "ForbiddenSimulatorAdmin",
        "ListingUnavailable",
        "CapabilityNotDeclared(simulator)"
      ],
      "preservedBehavior": [
        "one real MockBroker listed, arbitrary non-Mock excluded, HTTP 200 only with authorized principal."
      ],
      "replacementSteps": [
        "Decode test result with simulator list schema.",
        "Add credential/capability and generation/coverage assertions.",
        "Keep production account listing separate and avoid object-shape heuristics."
      ],
      "verificationCases": [
        "One authorized Mock simulator appears with scoped metadata; a real provider is absent without leakage, and missing admin capability yields typed 401/403 with no mutation."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source verifies that a real MockBroker is listed and an arbitrary object is not. Retain the inclusion/exclusion evidence, but express it through declared capability metadata and authorization.",
      "contractAnchors": [
        "K01",
        "K02",
        "K05",
        "K06",
        "K12"
      ]
    },
    {
      "id": "MAP-3C4CDD1C56",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/simulator.spec.ts:58-86: Verifies a full raw MockBroker snapshot after setting a mark price, 404 for unknown ids, and 400 when the selected broker is not a simulator."
      ],
      "currentBehavior": "Verifies a full raw MockBroker snapshot after setting a mark price, 404 for unknown ids, and 400 when the selected broker is not a simulator.",
      "problem": "state 测试序列化 raw MockBroker snapshot 并覆盖 404/non-simulator；没有约束 versioned simulator projection、freshness 或原生对象不得穿过 wire。",
      "targetModel": "Simulator state test consumes the simulator leaf’s versioned state output: exact cash/price/position/pending schemas, generation/as-of, and typed NotFound/NotSimulator/Unavailable variants. It is not production AccountState or transaction status. State lookup is explicit AccountScope-bound.",
      "pureTransitions": [
        "decodeSimulatorState(wire, descriptor.outputSchema) -> Result<SimulatorStateView, ProtocolDecodeFailure>",
        "classifyStateLookup(registry, id) -> Result<StateView, SimulatorNotFound|NotSimulator|Stale>."
      ],
      "effectBoundary": "Typed simulator application query reads the real mock interpreter/projection; HTTP does not serialize a raw MockBroker snapshot.",
      "durability": "State remains rebuildable fixture projection with generation/sequence; target order-effect tests independently inspect transaction writer/provider observations.",
      "errors": [
        "SimulatorNotFound",
        "NotSimulator",
        "SimulatorStateStale",
        "ForbiddenSimulatorAdmin",
        "ProtocolDecodeFailure"
      ],
      "preservedBehavior": [
        "保留 full snapshot assertion cash=50000, mark BTC=80000; 404 unknown and 400 non-sim behavior."
      ],
      "replacementSteps": [
        "Use shared state codec and Principal/capability setup.",
        "Assert exact decimal/identity fields and freshness.",
        "Keep production account-state and effect status tests separate."
      ],
      "verificationCases": [
        "Mark 80000 appears as exact text; unknown/non-simulator branches return named errors; no Contract/Order class crosses wire and stale generation is not accepted as current."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source exercises a full raw mock snapshot and error distinction. Retain the snapshot/failure evidence, but bound it to the simulator data leaf and typed projection.",
      "contractAnchors": [
        "K02",
        "K03",
        "K04",
        "K05",
        "K06",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-9DB36046FA",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/simulator.spec.ts:88-128: Uses a real MockBroker to place a limit order, verifies mark-price updates leave it pending then auto-fill it at the trigger price and update positions, and verifies malformed body returns 400."
      ],
      "currentBehavior": "Uses a real MockBroker to place a limit order, verifies mark-price updates leave it pending then auto-fill it at the trigger price and update positions, and verifies malformed body returns 400.",
      "problem": "mark-price 测试通过真实 MockBroker 验证 80k pending 到 79k fill；它没有把 SetMarkPrice/FillObservation identity、replay 和 admin capability 纳入 durable fixture。",
      "targetModel": "Mark-price integration remains a simulator control scenario over the declared SetMarkPrice leaf and FillObservation output. Its deterministic pending->filled rule is fixture evidence, not proof of provider order idempotency or durable transaction dispatch. The control scenario is explicit AccountScope-bound.",
      "pureTransitions": [
        "decodeMarkPriceScenario(wire) -> Result<DeclaredControlInput, ValidationFailed>",
        "foldMockMatch(state, mark) -> pending/position transition plus one declared FillObservation; duplicate fixture identity is idempotent."
      ],
      "effectBoundary": "The real MockBroker interpreter is invoked through simulator application control; production order effects and provider observations are not inferred from this route.",
      "durability": "Record fixture command/observation identity and generation for replay tests; no transaction journal/approval/job is created by SetMarkPrice itself.",
      "errors": [
        "ValidationFailed",
        "SimulatorOrderFillRejected",
        "ObservationDuplicate",
        "OutcomeUnknown",
        "SimulatorCapabilityUnavailable"
      ],
      "preservedBehavior": [
        "保留 80k pending then 79k auto-fill, filled id, pending=0, position=1.",
        "保留 wrongField 400 intent."
      ],
      "replacementSteps": [
        "Use typed request/response codecs and admin authorization.",
        "Keep 80k/79k matching evidence.",
        "Add separate durable effect/restart tests instead of treating this scenario as transaction proof."
      ],
      "verificationCases": [
        "Initial 80000 leaves BUY@79000 pending; 79000 produces one fill and position; replay does not duplicate; malformed body leaves state unchanged and no effect receipt is created."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source’s real MockBroker mark/fill scenario is precise empirical evidence. Retain it as simulator behavior while expressly limiting its architectural claim.",
      "contractAnchors": [
        "K01",
        "K03",
        "K04",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-1BDBE358B1",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/simulator.spec.ts:130-150: Calls the simulator endpoint with a wallet-source deposit, then checks MockBroker position quantity, wallet avg-cost source, and mark-price placeholder cost."
      ],
      "currentBehavior": "Calls the simulator endpoint with a wallet-source deposit, then checks MockBroker position quantity, wallet avg-cost source, and mark-price placeholder cost.",
      "problem": "external-deposit 测试只观察进程内 position 的 wallet cost source/mark placeholder；没有 observation identity、dedup、MissingMark/Indeterminate 或 accounting projection 证据。",
      "targetModel": "External-deposit test exercises the declared simulator observation leaf and accounting projection, including Quantity, wallet provenance, MarkPresent/MissingMark cost basis, cash invariant, and fixture dedup. The observation and projection are explicit AccountScope-bound.",
      "pureTransitions": [
        "decodeDepositScenario(wire, descriptor.inputSchema) -> Result<ExternalDepositObservation, ValidationFailed>",
        "foldDeposit(observation, projection) -> Result<Projection, DuplicateFixture|ProjectionFailure> with explicit cost-basis variant."
      ],
      "effectBoundary": "The real MockBroker is reached only through SimulatorControlService; production external transfer tests consume provider observations through accounting/reconciliation, not this route.",
      "durability": "FixtureId/sequence and projection checkpoint are the durable evidence; no Alice transaction approval/dispatch or order WAL is created.",
      "errors": [
        "InvalidQuantity",
        "ObservationDuplicate",
        "ProjectionStale",
        "ForbiddenSimulatorAdmin",
        "CostBasisIndeterminate"
      ],
      "preservedBehavior": [
        "保留 1.0093 quantity, wallet source, avgCost=80000 after mark, cash unchanged."
      ],
      "replacementSteps": [
        "Decode typed response/provenance.",
        "Add duplicate/restart and journal/projection assertions for the fixture observation.",
        "Keep simulator and real-provider external transfer leaves separate."
      ],
      "verificationCases": [
        "Deposit 1.0093 after mark 80000 updates only scoped simulator position and keeps cash; duplicate does not double quantity; missing mark remains indeterminate rather than zero."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source verifies wallet-source cost and cash behavior on a real mock. Retain that accounting evidence and explicitly classify it as an external observation leaf.",
      "contractAnchors": [
        "K03",
        "K04",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-8878826403",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/simulator.spec.ts:152-174: Injects a BUY trade through the simulator endpoint and verifies position avg cost, wallet-source tagging, and cash decrease on the real MockBroker."
      ],
      "currentBehavior": "Injects a BUY trade through the simulator endpoint and verifies position avg cost, wallet-source tagging, and cash decrease on the real MockBroker.",
      "problem": "external-trade 测试只观察 MockBroker position/cash；它不能证明 external fill 与 Alice dispatch 分离，也不能证明 journal replay 不重复扣款。",
      "targetModel": "External-trade test exercises a simulator observation leaf and accounting fold, not TransactionCommitted. Output carries Position/Cash snapshots, wallet source, side/quantity/price, and declared provenance. The observation and accounting fold are explicit AccountScope-bound.",
      "pureTransitions": [
        "decodeExternalTradeScenario(wire, descriptor.inputSchema) -> Result<ExternalTradeObservation, InvalidTrade>",
        "foldExternalTrade(observation, projection) -> deterministic Decimal cash/exposure/WAC result with fixture-id idempotency."
      ],
      "effectBoundary": "Simulator control invokes the real mock interpreter; accounting projection consumes the observation. Controlled provider fills use a separate transaction wrapper and observation identity.",
      "durability": "Cash/exposure projection and fixture identity are durable/rebuildable test evidence; no approval, dispatch, order lock, or compensation is inferred.",
      "errors": [
        "InvalidTrade",
        "OversellExposure",
        "ObservationDuplicate",
        "AccountingProjectionFailure",
        "ForbiddenSimulatorAdmin"
      ],
      "preservedBehavior": [
        "BUY .5@60000 creates wallet-sourced position avgCost 60000 and cash 70000 from 100k."
      ],
      "replacementSteps": [
        "Migrate assertions to typed position/account projections.",
        "Record external source and observation identity.",
        "Add replay/unknown cases without labeling this as Alice dispatch."
      ],
      "verificationCases": [
        "BUY .5@60000 yields exact cash 70000 and wallet cost 60000; replay does not debit again; external provenance remains distinct from a controlled order result."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source provides concrete wallet accounting evidence and intentionally bypasses order entry. Retain it as data/observation behavior, not a universal effect test.",
      "contractAnchors": [
        "K03",
        "K04",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-25883E620A",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/simulator.spec.ts:176-208: Places a pending limit order on a real MockBroker, manually fills it through the simulator endpoint, verifies pending state clears, and verifies an unknown order id returns 400."
      ],
      "currentBehavior": "Places a pending limit order on a real MockBroker, manually fills it through the simulator endpoint, verifies pending state clears, and verifies an unknown order id returns 400.",
      "problem": "manual-fill 测试通过 route 直接清掉 pending order，并以 400 表示 unknown；它没有 partial-fill cumulative state、fixture observation identity 或 production observation boundary。",
      "targetModel": "Manual-fill test exercises a simulator-only fill control leaf with declared BrokerOrderId, optional Price/Quantity, and FillObservation output. Production fills remain provider observations associated with a selected transaction effect. The control leaf is explicit AccountScope-bound.",
      "pureTransitions": [
        "decodeManualFill(wire, descriptor.inputSchema) -> Result<ManualFillInput, ValidationFailed>",
        "foldFill(state, input) -> Result<FillObservation, OrderNotPending|QuantityExceeded|InvalidQuantity> preserving partial/remaining state."
      ],
      "effectBoundary": "The real MockBroker is reached through simulator control; HTTP status is not a production execution result and no generic provider fill API is assumed.",
      "durability": "Fixture observation/order version must be unique for replay; no transaction approval/job or fabricated remote Ack is written by manual fill.",
      "errors": [
        "OrderNotPending",
        "FillQuantityExceeded",
        "InvalidQuantity",
        "ObservationUnknown",
        "ForbiddenSimulatorAdmin"
      ],
      "preservedBehavior": [
        "保留 default mark/limit fill behavior, pending state clears on full fill, unknown id 400."
      ],
      "replacementSteps": [
        "Decode order id as the declared simulator identity.",
        "Assert typed result and no change on unknown/terminal.",
        "Add partial/cumulative/restart evidence independently from controlled effect dispatch."
      ],
      "verificationCases": [
        "Default mark/limit full fill removes pending; partial fill keeps remainder/cumulative; unknown id returns typed error with no projection/event and real provider routes have no simulator handler."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source’s real pending-to-filled scenario is valuable but route-local. Retain default/partial/unknown semantics behind the simulator leaf and keep production observation boundaries separate.",
      "contractAnchors": [
        "K01",
        "K03",
        "K04",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-8D000718DA",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/trading-order-entry.spec.ts:1-18: Defines the current one-shot contract as one HTTP roundtrip wrapping stage→commit→push, with phase-specific errors, message validation, and numeric-string precision expectations, then imports createTradingRoutes."
      ],
      "currentBehavior": "Defines the current one-shot contract as one HTTP roundtrip wrapping stage→commit→push, with phase-specific errors, message validation, and numeric-string precision expectations, then imports createTradingRoutes.",
      "problem": "order-entry spec 把 route contract 定义为一次 stage→commit→push roundtrip；这会把 legacy Git phases、message requirement 和 HTTP status 当作新 transaction protocol。",
      "targetModel": "Order-entry HTTP integration targets a declared provider effect leaf plus optional withTransaction wrapper, not a fixed one-shot action API. The contract covers schema decode, review/approval, durable receipt, and subsequent provider observation only for that selected leaf. For account-backed effects, intent, receipt, and later observations carry explicit AccountScope.",
      "pureTransitions": [
        "decodeEntry(wire, descriptor.inputSchema) -> Result<AccountScopedDeclaredEffectInput, ValidationFailed>",
        "observeWrappedFlow(receipt, providerEvents) -> Result<EffectView, Ack|Rejected|OutcomeUnknown> with separate delivery semantics."
      ],
      "effectBoundary": "The test composition includes transport verifier, selected application/effect writer, scheduler, and provider interpreter; data/query leaves are tested through their own finite/push ports.",
      "durability": "The test checks receipt/event/job/lock and provider ledger only for the wrapped effect; HTTP status/PushResult is never sole proof, and crash cuts produce typed unknown/recovery.",
      "errors": [
        "ValidationFailed",
        "AuthorizationFailure",
        "StorageUnavailable",
        "OutcomeUnknown",
        "InvalidState",
        "CapabilityNotDeclared(effect)"
      ],
      "preservedBehavior": [
        "保留 current one-shot scenarios as migration evidence.",
        "保留 phase-specific failure intent but map to typed transaction states."
      ],
      "replacementSteps": [
        "Rewrite the test contract around selected descriptors/codecs and explicit effect wrapper states.",
        "Retain precision and close/cancel scenarios only where the provider declares them.",
        "Delete old call-order/PushResult assertions after caller migration."
      ],
      "verificationCases": [
        "Draft/prepare leaves provider state untouched; approval returns a durable receipt; provider observation determines later status; response loss/restart converges by command/dispatch identity."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source defines stage→commit→push as one HTTP contract. Retain its end-to-end intent and precision obligations, but exercise the declaration-driven effect wrapper rather than a universal one-shot route.",
      "contractAnchors": [
        "K01",
        "K03",
        "K07",
        "K08",
        "K09",
        "K10",
        "K12"
      ]
    },
    {
      "id": "MAP-6837C09018",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/trading-order-entry.spec.ts:20-69: Builds a vi-stubbed UTA capturing stagePlaceOrder/stageClosePosition/stageCancelOrder, synchronous commit, async push, reject, and status calls; configured failures model stage, commit, or push phases and return a legacy PushResult."
      ],
      "currentBehavior": "Builds a vi-stubbed UTA capturing stagePlaceOrder/stageClosePosition/stageCancelOrder, synchronous commit, async push, reject, and status calls; configured failures model stage, commit, or push phases and return a legacy PushResult.",
      "problem": "makeMockUTA 用 vi stubs 捕获 staging/commit/push/reject/status；异常调用顺序不是 SQL writer、scheduler lease 或 broker ledger 的 crash/recovery 证据。",
      "targetModel": "Failure fixture composes named ports only for the selected controlled effect: JournalWriter, scheduler, provider interpreter, projection reader, and capability descriptor. It does not create a vi-shaped universal UTA or require every provider method. The selected account effect retains explicit AccountScope through each failure state.",
      "pureTransitions": [
        "injectFailure(port, scenario) -> Result<EffectOutcome, StorageFailure|ProviderRejected|OutcomeUnknown>",
        "foldFailure(state, outcome, policy) -> typed state; Unknown suspends forward steps and schedules declared observation/recovery."
      ],
      "effectBoundary": "Failures are injected at named protocol/application/provider ports; a real Mock interpreter or conformance fake stays behind its declared leaf and data route fixtures remain independent.",
      "durability": "The fixture exposes events/jobs/locks/receipts and independent provider ledger only when the selected effect wrapper requires them; stale leases cannot blindly redispatch.",
      "errors": [
        "StorageFailure",
        "BrokerRejected",
        "OutcomeUnknown",
        "ReplayViolation",
        "CapabilityUnavailable"
      ],
      "preservedBehavior": [
        "保留 stage/commit/push failure scenarios as regression input categories.",
        "保留 reject no prepared hash safety intent as no-dispatch state rule."
      ],
      "replacementSteps": [
        "Replace makeMockUTA/vi call-order proof with a typed composition fixture.",
        "Inject failures at writer/provider boundaries and inspect durable state.",
        "Keep no-dispatch/unknown obligations scoped to the selected transaction wrapper."
      ],
      "verificationCases": [
        "Writer failure commits no receipt/events; accepted-before-ack crash resumes as Reconciling/ObserveUnknown; stale lease cannot dispatch a second time; a data leaf failure does not invoke order recovery."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source stubs an entire UTA and treats method order as evidence. Retain deterministic failure categories but compose only the ports needed by the selected capability.",
      "contractAnchors": [
        "K01",
        "K03",
        "K07",
        "K08",
        "K12"
      ]
    },
    {
      "id": "MAP-8A6F5C59E6",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/trading-order-entry.spec.ts:71-93: Creates a minimal casted UTAEngineContext containing only manager.get and invokes createTradingRoutes; post sends JSON to the Hono app and parses the response."
      ],
      "currentBehavior": "Creates a minimal casted UTAEngineContext containing only manager.get and invokes createTradingRoutes; post sends JSON to the Hono app and parses the response.",
      "problem": "测试只提供 manager.get 的 casted context 并解析 JSON response；它没有 authenticated transport principal、shared codec 或 response 后的 journal state 对照。",
      "targetModel": "HTTP helper receives the transport verifier and selected application capability context, then uses descriptor-bound codecs. It must not cast a partial UTAEngineContext or allow body actor strings to substitute for Principal. When the selected capability is account-backed, its decoded request carries explicit AccountScope.",
      "pureTransitions": [
        "post(path, wire, credential, descriptor) -> Result<DecodedResponse, ProtocolDecodeFailure|TransportFailure>",
        "canonicalizeCommand(wire, descriptor) -> Result<StableInput, ValidationFailed> for selected effects only."
      ],
      "effectBoundary": "The helper drives real Hono transport and injected application ports; credential fixture follows the deployment verifier contract, while provider-native objects remain below the adapter boundary.",
      "durability": "Effect responses are cross-checked with durable receipt/state after writer commit; data responses carry projection checkpoint or stream lifecycle, not universal transaction state.",
      "errors": [
        "UnauthenticatedTransport",
        "ValidationFailed",
        "ProtocolDecodeFailure",
        "UnexpectedStatus",
        "CapabilityNotDeclared"
      ],
      "preservedBehavior": [
        "保留 Hono request helper and JSON response parse convenience.",
        "保留 route-specific path calls during migration."
      ],
      "replacementSteps": [
        "Provide a complete typed context through composition instead of `as unknown as UTAEngineContext`.",
        "Decode per-leaf success/error schema and assert actor comes from verified credentials.",
        "Check downstream durable state only for wrapped effects."
      ],
      "verificationCases": [
        "No credential is rejected before application; same declared command has stable digest; valid data/effect response decodes and its relevant projection/receipt agrees without any casted manager call."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source helper relies on a partial context cast and arbitrary JSON. Retain Hono convenience while adding the actual transport/schema seam and capability-specific durability assertions.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K06",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-6E09B3ED77",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/trading-order-entry.spec.ts:95-114: Posts a legacy place-order body and asserts HTTP 200, a PushResult hash, and exact stagePlaceOrder→commit→push call order."
      ],
      "currentBehavior": "Posts a legacy place-order body and asserts HTTP 200, a PushResult hash, and exact stagePlaceOrder→commit→push call order.",
      "problem": "happy-path 测试断言 stagePlaceOrder→commit→push 和 Git hash；它没有验证 Draft/Prepare/Approve 顺序、durable job/DispatchIdentity 或 Ack 与 Filled 的差别。",
      "targetModel": "Place-order happy path becomes a selected provider Order effect wrapped withTransaction. Its success is a durable receipt and later provider observation; the provider’s exact order schema/extensions are preserved, and no Git hash or universal action is assumed. The selected Order effect is explicitly AccountScope-bound.",
      "pureTransitions": [
        "decodeDeclaredOrder(wire, descriptor.inputSchema) -> Result<AccountScopedOrderIntent, ValidationFailed|RelationFailure>",
        "foldEffectOutcome(receipt, providerOutcome) -> Queued|Executing|Ack|Working|Filled|Unknown according to declared criterion."
      ],
      "effectBoundary": "Writer persists the selected effect intent/jobs before scheduler/provider interpreter invocation; projection reader observes provider order/fill. HTTP returns the wrapper receipt/view.",
      "durability": "One transaction/command/revision/plan identity and one dispatch identity are asserted for the selected effect; Ack is not Filled, and duplicate command identity is idempotent.",
      "errors": [
        "ValidationFailed",
        "PreconditionFailed",
        "BrokerRejected",
        "OutcomeUnknown",
        "CapabilityUnavailable"
      ],
      "preservedBehavior": [
        "保留 happy path places market BUY qty `0.001`.",
        "Preserve exact action association and no implicit approval by POST."
      ],
      "replacementSteps": [
        "Rewrite body/route around the provider’s declared order leaf and wrapper.",
        "Replace stage/commit/push call-order/hash assertions with events/jobs/ledger observations.",
        "Retain the precise BUY quantity scenario only as a provider fixture, not a global order guarantee."
      ],
      "verificationCases": [
        "Before approval provider ledger is empty; approval creates one durable job; provider observation yields Working/Filled according to evidence; duplicate command returns the same receipt and no second dispatch."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source proves happy one-shot Git behavior, not remote execution. Retain the market BUY precision scenario as an effect fixture while shifting authority to declaration-bound durable execution.",
      "contractAnchors": [
        "K01",
        "K03",
        "K04",
        "K08",
        "K09",
        "K10",
        "K12"
      ]
    },
    {
      "id": "MAP-9CF3FC0756",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/trading-order-entry.spec.ts:116-142: Verifies missing commit message and missing totalQuantity/cashQty return 400 without invoking stage, commit, or push."
      ],
      "currentBehavior": "Verifies missing commit message and missing totalQuantity/cashQty return 400 without invoking stage, commit, or push.",
      "problem": "验证缺 message 或缺 sizing 时不调用 legacy methods；新边界还必须分别拒绝缺 command metadata、非法 action relation 和 unauthorized scope，并证明 writer/broker 未变。",
      "targetModel": "Validation integration covers the selected capability schema before its handler. Missing command metadata, declared sizing/relation requirements, and unauthorized scope are rejected only when that leaf requires them; no universal order schema or stage method is invoked. For an account-backed leaf, explicit AccountScope is a declared input rather than an inferred route default.",
      "pureTransitions": [
        "decodeBeforeHandler(wire, descriptor.inputSchema) -> Result<DeclaredInput, ValidationFailed>",
        "admitScopeAndRelations(input, principal, descriptor) -> Result<AcceptedInput, AuthorizationFailure|RelationFailure> with no writer/provider call on failure."
      ],
      "effectBoundary": "Transport/application validation is the only path for malformed requests; selected data leaves and effect leaves use their own descriptors and error schemas.",
      "durability": "Invalid input leaves event/receipt/provider state unchanged. A valid controlled effect creates only its draft when the selected wrapper declares drafting; data queries remain side-effect-free.",
      "errors": [
        "ValidationFailed(path)",
        "MissingOrderSize",
        "UnauthorizedAccountScope",
        "OrderRelationInvalid",
        "CapabilityNotDeclared"
      ],
      "preservedBehavior": [
        "保留 missing message/quantity 等旧输入不产生 staging/dispatch side effect 的证据；message 不再成为所有 effect 的必填 action 字段。",
        "保留 schema/application 早拒绝的安全目的。"
      ],
      "replacementSteps": [
        "Update fixtures with credential/command metadata only where the selected capability declares them.",
        "Assert typed issue paths and no provider/writer mutation.",
        "Add provider-specific unsupported/extension cases rather than global variant rows."
      ],
      "verificationCases": [
        "Missing declared sizing or actor fails before DraftReplaced; invalid relation/unknown field is rejected; event count/provider ledger remains unchanged, while a valid data leaf requires no order metadata."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source safety intent is no stage/commit/push on malformed input, but the prior target called a closed action set universal. Retain pre-handler refusal and scope it to each declaration.",
      "contractAnchors": [
        "K01",
        "K03",
        "K04",
        "K07",
        "K08",
        "K12"
      ]
    },
    {
      "id": "MAP-EA13BA2333",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/trading-order-entry.spec.ts:144-176: Configures the UTA stub to throw during staging or commit, then verifies phase=stage/commit, HTTP 400, no later phase, and no reject when commit has no prepared hash."
      ],
      "currentBehavior": "Configures the UTA stub to throw during staging or commit, then verifies phase=stage/commit, HTTP 400, no later phase, and no reject when commit has no prepared hash.",
      "problem": "测试把 staging/commit throw 映射为 phase=stage/commit；这种 stub 失败不能证明 PrepareFailure 的状态演进、SQL rollback 或 Preparing 恢复。",
      "targetModel": "Preparation/review failures are typed states of a selected transaction wrapper, not HTTP `phase` strings. Data query/stream capabilities have their own delivery failures and do not enter Preparing or rejection machinery. An account-backed wrapper retains explicit AccountScope even when preparation fails.",
      "pureTransitions": [
        "prepareEffect(state, facts) -> Result<PreparedPlan, PrepareFailure|PreconditionFailed|CapabilityUnavailable>",
        "recoverPreparation(state, failure) -> Draft|PreparingRecovery|ReprepareRequired without catch-driven side effects."
      ],
      "effectBoundary": "Writer commits wrapper state atomically; provider is not called when preparation facts/validation/storage fail. Transport only encodes the selected error schema.",
      "durability": "Failed prepare has no partial plan/jobs/locks; any durable failure/recovery record is written by the wrapper’s writer. A provider/data route is unaffected by this effect state.",
      "errors": [
        "PrepareFailure",
        "PreconditionFailed",
        "StorageUnavailable",
        "InvalidState",
        "StalePreparation",
        "CapabilityNotDeclared(prepare)"
      ],
      "preservedBehavior": [
        "保留 stage/commit errors stop later work and no reject when no prepared hash.",
        "Change generic `Error` to named failure and durable state."
      ],
      "replacementSteps": [
        "Inject failures at named codec/application/writer boundaries.",
        "Inspect wrapper state/event/lock tables rather than phase strings.",
        "Remove legacy stage/commit compatibility after caller migration."
      ],
      "verificationCases": [
        "Fact failure leaves Draft/issues and no provider call; writer rollback leaves no partial locks/jobs; stale preparation is rejected; data leaf requests do not acquire an effect state."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source only proves stage/commit stub throws stop later calls. Retain stop/no-side-effect behavior but represent it in a selected durable effect wrapper, not a universal HTTP phase protocol.",
      "contractAnchors": [
        "K03",
        "K08",
        "K09",
        "K10",
        "K12"
      ]
    },
    {
      "id": "MAP-2FADFA4CDF",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/trading-order-entry.spec.ts:178-191: Configures push to throw and asserts HTTP 500 with phase=push, exercising the final legacy broker execution phase without checking durable intent or remote outcome."
      ],
      "currentBehavior": "Configures push to throw and asserts HTTP 500 with phase=push, exercising the final legacy broker execution phase without checking durable intent or remote outcome.",
      "problem": "测试把 push throw 映射为 HTTP 500 phase=push；异常既可能发生在 remote mutation 前后，不能据此声称 rejected，必须以 DispatchStarted 和 observation 分类。",
      "targetModel": "Dispatch failure integration belongs to the selected transaction wrapper/provider effect. The wrapper writes DispatchStarted before provider invocation and classifies provider evidence as Rejected or OutcomeUnknown; HTTP does not turn a thrown method into a universal 500 contract. Dispatch/recovery state retains explicit AccountScope for the selected account effect.",
      "pureTransitions": [
        "classifyProviderOutcome(result) -> Ack|Rejected|Unknown using provider-declared error/delivery schema",
        "foldDispatchFailure(state, outcome, policy) -> Rejected|Reconciling|RecoveryRequired; Unknown suspends remaining forward effects."
      ],
      "effectBoundary": "Scheduler owns lease/epoch/dispatch identity and invokes the selected provider interpreter; HTTP only returns/querys the durable wrapper view. Data leaves are not routed through this path.",
      "durability": "DispatchAttempt, provider outcome evidence, lease epoch, and follow-up Observe job are durable. No blind retry after a crash or timeout and no claim of rejection without evidence.",
      "errors": [
        "BrokerRejected",
        "ConnectionUnavailable",
        "OutcomeUnknown",
        "CapabilityRevoked",
        "StorageFailure",
        "CapabilityNotDeclared(effect)"
      ],
      "preservedBehavior": [
        "保留 push failure scenario as final broker-phase failure.",
        "Do not claim remote rejection from thrown push exception without observation evidence."
      ],
      "replacementSteps": [
        "Replace push-throw stub with provider outcome/crash-cut injection for a selected effect.",
        "Assert durable journal/provider ledger after restart.",
        "Expose recovery/operator state through the effect’s declared query schema."
      ],
      "verificationCases": [
        "Known provider rejection has evidence and no blind continuation beyond policy; crash after DispatchStarted yields Reconciling/ObserveUnknown and one future identity, not immediate 500-as-retry."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source cannot tell whether push threw before or after remote mutation. Retain that uncertainty obligation, but make classification provider/effect-specific and durable rather than HTTP-phase based.",
      "contractAnchors": [
        "K03",
        "K07",
        "K08",
        "K09",
        "K10",
        "K12"
      ]
    },
    {
      "id": "MAP-A33EC0E470",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/trading-order-entry.spec.ts:193-203: Posts to an unknown account id and asserts HTTP 404 before the legacy pipeline runs."
      ],
      "currentBehavior": "Posts to an unknown account id and asserts HTTP 404 before the legacy pipeline runs.",
      "problem": "unknown account 测试只观察 legacy route 的 404；它没有区分 malformed AccountId、TransactionNotFound 和跨 scope Forbidden，也没有持久化不变性。",
      "targetModel": "Account/transaction resolution first decodes branded identities and Principal scope. A transaction id is resolved only for a selected transaction-wrapper capability; data leaves resolve AccountScope without inventing transaction state.",
      "pureTransitions": [
        "resolveSubject(wire, registry, principal, descriptor) -> Result<SubjectRef, InvalidId|TransactionNotFound|UnknownAccount|ForbiddenAccountScope>",
        "rejectBeforeHandler(result) -> no provider/writer call for malformed/unauthorized subject."
      ],
      "effectBoundary": "Application directory/transaction reader performs identity lookup; HTTP maps typed 404/403/401 and never invokes legacy UTA staging.",
      "durability": "Failed resolution leaves events/jobs/locks/receipts unchanged. A data query may return a scoped projection; only a selected effect wrapper can create a transaction receipt.",
      "errors": [
        "TransactionNotFound",
        "UnknownAccount",
        "ForbiddenAccountScope",
        "UnauthenticatedTransport",
        "CapabilityNotDeclared(effect)"
      ],
      "preservedBehavior": [
        "保留 unknown account returns 404 before action.",
        "Preserve no broker call/no staged state."
      ],
      "replacementSteps": [
        "Update route/test to selected subject/descriptor endpoint.",
        "Add cross-account Principal scenarios and stable error codes.",
        "Keep provider native identity private and separate from transaction id."
      ],
      "verificationCases": [
        "Unknown account returns typed 404 with no mutation; an existing subject outside Principal scope returns 403 without leakage; a data-only provider has no transaction resolution path."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source verifies an early unknown-account 404, which remains important. Reframe lookup around runtime scope and optional transaction wrappers rather than a universal legacy pipeline.",
      "contractAnchors": [
        "K02",
        "K03",
        "K07",
        "K08",
        "K12"
      ]
    },
    {
      "id": "MAP-891F1FEA91",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/trading-order-entry.spec.ts:205-220: Posts a high-precision totalQuantity and inspects captured stage arguments to ensure the exact string reaches legacy staging without float conversion."
      ],
      "currentBehavior": "Posts a high-precision totalQuantity and inspects captured stage arguments to ensure the exact string reaches legacy staging without float conversion.",
      "problem": "测试只检查高精度 quantity 到达 stage 参数；它未证明 canonical decimal 在 journal digest、prepared native plan、重启 projection 中保持同一文本。",
      "targetModel": "Decimal precision is a cross-boundary invariant for any declared financial field, including a selected Order effect if present: wire schema -> refined value -> projection/plan -> provider adapter. It is not evidence that all routes are order transactions. A selected account effect preserves AccountScope across the precision boundary.",
      "pureTransitions": [
        "decodeEncodeDecimal(text, declaredUnit) -> Result<CanonicalDecimal, InvalidDecimal|PrecisionLossDetected|UnitMismatch>",
        "roundTripAcrossLeaf(value, schemas) -> same canonical text without IEEE-754 conversion."
      ],
      "effectBoundary": "HTTP codecs, application/domain values, projection store, and selected provider adapter preserve the declared decimal type; tests inspect durable/output values rather than legacy stage call args.",
      "durability": "Read projections retain canonical text and schema fingerprint; effect plans/dispatch payloads do so only for the selected wrapper. No Number/float enters identity or digest.",
      "errors": [
        "InvalidDecimal",
        "PrecisionLossDetected",
        "UnitMismatch",
        "ProtocolDecodeFailure"
      ],
      "preservedBehavior": [
        "保留 high-precision quantity exact string requirement.",
        "Extend proof through persistence and dispatch rather than stopping at staging."
      ],
      "replacementSteps": [
        "Add leaf-specific codec and durable/output assertions.",
        "Remove numeric coercions from compact and protocol paths.",
        "Test very small/large values only where the provider schema declares the field."
      ],
      "verificationCases": [
        "`0.123456789012345` survives the selected wire->projection/effect path and reload with deterministic digest; no IEEE-754 rounding or provider-unproven precision guarantee is claimed."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The source proves high-precision quantity must remain text through staging. Retain exactness and extend it to the selected declared boundary without spreading order machinery to data routes.",
      "contractAnchors": [
        "K03",
        "K04",
        "K05",
        "K08",
        "K12"
      ]
    },
    {
      "id": "MAP-165076A00B",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/trading-order-entry.spec.ts:222-233: Posts a one-shot order and asserts the exact user message is passed to legacy uta.commit."
      ],
      "currentBehavior": "Posts a one-shot order and asserts the exact user message is passed to legacy uta.commit.",
      "problem": "测试把 human message 转发给 uta.commit；message 因此看似执行输入，未区分 TradeAction、approval actor、audit rationale 和 broker payload。",
      "targetModel": "Audit rationale is metadata on a selected controlled-effect command, separate from the Order semantic input, ApprovalActor Principal, and provider payload. Data capabilities do not require a commit message or audit mutation. The selected account effect binds that metadata to explicit AccountScope.",
      "pureTransitions": [
        "canonicalizeAuditMetadata(intent, actor, reason, descriptor) -> Result<AuditMetadata, AuditMetadataInvalid>",
        "projectAuditMessage(metadata) -> presentation text without changing the declared effect input or dispatch payload."
      ],
      "effectBoundary": "Selected transaction application accepts verified actor and optional reason, writer records causation/correlation, and projection renders the message; HTTP does not forward free-form text to a native provider.",
      "durability": "Actor/reason/correlation are durable with the selected effect event and immutable after commit according to its schema. A query/stream only reads existing audit metadata and does not write it.",
      "errors": [
        "MissingPrincipal",
        "AuditMetadataInvalid",
        "UnauthorizedOperation",
        "StorageUnavailable",
        "CapabilityNotDeclared(effect)"
      ],
      "preservedBehavior": [
        "保留 exact human message visible in audit history where supplied.",
        "Remove forwarding assertion to `uta.commit(message)` as execution contract."
      ],
      "replacementSteps": [
        "Change test to inspect event actor/reason/digest binding and projected text.",
        "Migrate commit caller to selected prepare/review/approve leaves.",
        "Keep provider payload free of untyped message text."
      ],
      "verificationCases": [
        "Different rationale affects only declared audit metadata/digest policy; body actor cannot override verified Principal; provider request contains no free-form commit message."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source forwards human message into `uta.commit`, conflating explanation with execution. Retain visible rationale but scope it to the selected effect’s audit metadata and remove message authority from data/SDK surfaces.",
      "contractAnchors": [
        "K03",
        "K08",
        "K09",
        "K10",
        "K12"
      ]
    },
    {
      "id": "MAP-EC8F4C5533",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/trading-order-entry.spec.ts:236-267: Covers explicit close quantity and close-all behavior, asserting HTTP 200 and that legacy stageClosePosition receives a string qty or no qty, preserving Decimal precision and All semantics through stage/commit/push."
      ],
      "currentBehavior": "Covers explicit close quantity and close-all behavior, asserting HTTP 200 and that legacy stageClosePosition receives a string qty or no qty, preserving Decimal precision and All semantics through stage/commit/push.",
      "problem": "close 测试用缺省 qty 表达 close-all，并以 stage 参数检查精度；它没有验证 prepare 时解析 All、exposure before-image、partial fill 和补偿评估。",
      "targetModel": "Close integration tests a provider-declared close Order effect with Quantity|All only where declared, an ExposureSnapshot before-image, exact Decimal values, and a selected transaction wrapper’s criterion/recovery evidence. It does not assert a universal ClosePosition API. The close effect is explicitly AccountScope-bound.",
      "pureTransitions": [
        "decodeCloseInput(wire, descriptor.inputSchema) -> Result<AccountScopedDeclaredCloseInput, ValidationFailed>",
        "prepareClose(scope, exposure, command) -> Result<PreparedTarget, ExposureFailure> with All resolved and before-image persisted."
      ],
      "effectBoundary": "Accounting/application gathers exposure, the selected wrapper writer creates plan/jobs, and the provider adapter observes residual exposure; HTTP returns effect receipt/view rather than Git result.",
      "durability": "Before-image/scope/criterion are durable for the selected effect; partial/unknown provider outcome remains explicit and requires observation/recovery, while position data reads remain independent.",
      "errors": [
        "NoExposure",
        "QuantityExceedsExposure",
        "ExposureChanged",
        "OutcomeUnknown",
        "CompensationRequired",
        "CapabilityNotDeclared(close)"
      ],
      "preservedBehavior": [
        "保留 explicit qty string and no qty = close all.",
        "Preserve precision and close-after-fill cleanup intent."
      ],
      "replacementSteps": [
        "Split legacy one-shot fixture into leaf decode/draft/prepare/review/approve scenarios.",
        "Assert All tag/resolved target and provider observation rather than stage args.",
        "Add partial-fill/external-change recovery only for the selected effect."
      ],
      "verificationCases": [
        "`.001` remains exact; All captures exposure at prepare; external change requires reprepare; partial outcome remains Reconciling and full completion requires declared observed criterion, not Ack alone."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source preserves close precision/All ergonomics through Git, but that pipeline hides exposure and outcome evidence. Retain the behavior in an explicitly declared close effect wrapper only.",
      "contractAnchors": [
        "K01",
        "K03",
        "K04",
        "K08",
        "K09",
        "K10",
        "K12"
      ]
    },
    {
      "id": "MAP-6AED1E318D",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/trading-order-entry.spec.ts:269-277: Verifies missing commit message returns 400 and no staging occurs for close-position."
      ],
      "currentBehavior": "Verifies missing commit message returns 400 and no staging occurs for close-position.",
      "problem": "close validation 测试把缺 message 当作唯一 400 条件；新 action 必须验证 principal/commandId/scope/instrument/Quantity|All，而不需要 Git message。",
      "targetModel": "Close validation is the input schema of a selected close-capable effect. Required Principal/command identity/explicit AccountScope/instrument and Quantity|All are enforced only if declared; Git commit message is not an action field.",
      "pureTransitions": [
        "decodeCloseCommand(wire, descriptor.inputSchema) -> Result<DeclaredCloseInput, ValidationFailed>",
        "admitBeforeDraft(input, principal) -> Result<AcceptedDraft, AuthorizationFailure> with no writer/provider call on rejection."
      ],
      "effectBoundary": "Transport/application validation runs before the selected writer/provider; data-only capabilities are unaffected and do not need close metadata.",
      "durability": "Invalid input leaves the selected effect’s event/revision unchanged and creates no job/receipt; valid input may create only a draft when that capability declares one.",
      "errors": [
        "ValidationFailed",
        "ScopeRequired",
        "MissingInstrument",
        "InvalidQuantity",
        "CapabilityNotDeclared(close)"
      ],
      "preservedBehavior": [
        "保留 missing message test as malformed legacy request, but change reason to missing approval metadata if needed.",
        "Preserve no staging behavior."
      ],
      "replacementSteps": [
        "Replace message-only fixture with descriptor input and verified Principal.",
        "Assert typed issue paths and no mutation.",
        "Add explicit All case only if the selected provider schema declares it."
      ],
      "verificationCases": [
        "Missing declared metadata fails with no event/provider call; valid All creates a draft only and cannot directly dispatch; a data leaf has no message/close validation route."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source tests missing commit message as the guard, but the useful invariant is no staging on malformed input. Retain that no-mutation boundary and derive required fields from a selected capability.",
      "contractAnchors": [
        "K01",
        "K03",
        "K04",
        "K07",
        "K08",
        "K12"
      ]
    },
    {
      "id": "MAP-D236542360",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/trading-order-entry.spec.ts:280-296: Posts a legacy cancel-order body and asserts HTTP 200 plus a staged orderId forwarded to stageCancelOrder."
      ],
      "currentBehavior": "Posts a legacy cancel-order body and asserts HTTP 200 plus a staged orderId forwarded to stageCancelOrder.",
      "problem": "cancel 测试只断言 orderId 被转发到 stageCancelOrder；它没有建立 ScopedOrderRef/version、approval job 或 late-fill/Unknown outcome。",
      "targetModel": "Cancel integration tests a provider-declared cancel Order effect with scoped BrokerOrderId/version/precondition and a selected transaction wrapper. Response records effect identity/receipt; it does not assert forwarding to a global stageCancelOrder method. The cancel effect is explicitly AccountScope-bound.",
      "pureTransitions": [
        "resolveCancel(scope, orderProjection, command) -> Result<PreparedCancel, OrderVersionConflict|ListingIncomplete|OrderNotFound>",
        "foldCancelOutcome(receipt, providerObservation) -> Working|Cancelled|LateFill|Unknown according to declared evidence."
      ],
      "effectBoundary": "Writer/scheduler/provider adapter own the selected effect; HTTP only submits/queries it. Simulator force-cancel remains a separate simulator leaf.",
      "durability": "Before-image/fills/version, command/dispatch identity, and provider observations are durable for the selected effect; Ack does not prove terminal state.",
      "errors": [
        "InvalidBrokerOrderId",
        "OrderVersionConflict",
        "OrderNotFound",
        "OutcomeUnknown",
        "CapabilityNotDeclared(cancel)"
      ],
      "preservedBehavior": [
        "保留 orderId ord-42 accepted through staging intent.",
        "Preserve explicit cancel before approval."
      ],
      "replacementSteps": [
        "Rewrite test around provider cancel schema and durable job.",
        "Use real/mock provider observation rather than stage forwarding.",
        "Add fill race and no-duplicate command identity."
      ],
      "verificationCases": [
        "A valid scoped id creates one selected effect job; stale version writes no event; late fill is recorded and status is not falsely Cancelled."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source proves string id reaches legacy staging, but that is not durable effect evidence. Retain identity and explicit approval intent through a provider-declared cancel wrapper.",
      "contractAnchors": [
        "K01",
        "K03",
        "K04",
        "K08",
        "K09",
        "K10",
        "K12"
      ]
    },
    {
      "id": "MAP-A034B94A3A",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/http/trading-order-entry.spec.ts:298-315: Verifies empty orderId and empty commit message each return HTTP 400 for the one-shot legacy route."
      ],
      "currentBehavior": "Verifies empty orderId and empty commit message each return HTTP 400 for the one-shot legacy route.",
      "problem": "cancel validation 测试把空 orderId 与空 commit message 放在同一 legacy 400 contract；新协议应仅以 branded nonempty id 和 command envelope 判定，并禁止隐式 dispatch。",
      "targetModel": "Cancel validation is the selected provider leaf’s strict input codec: nonempty BrokerOrderId/explicit AccountScope and command metadata where effect-wrapped, with no commit-message requirement. No valid command bypasses its separate review/prepare boundary.",
      "pureTransitions": [
        "decodeCancelCommand(wire, descriptor.inputSchema) -> Result<DeclaredCancelInput, ValidationFailed>",
        "validateEffectEnvelope(input, principal) -> Result<AcceptedDraft, MissingPrincipal|MissingCommandId|AuthorizationFailure> before writer/provider."
      ],
      "effectBoundary": "HTTP/application boundary validates the selected leaf and wrapper; no legacy stage/commit/push or simulator control is invoked.",
      "durability": "Invalid input leaves events/jobs/receipts unchanged. A valid input creates only the selected draft/prepare state and requires the wrapper’s explicit approval before provider dispatch.",
      "errors": [
        "InvalidBrokerOrderId",
        "MissingCommandId",
        "MissingPrincipal",
        "ValidationFailed",
        "CapabilityNotDeclared(cancel)"
      ],
      "preservedBehavior": [
        "保留 empty orderId returns 400 intent.",
        "Replace empty commit message check with typed approval/audit metadata rule."
      ],
      "replacementSteps": [
        "Update payload to the selected canonical schema and remove message assertion.",
        "Assert shared error envelope/no mutation.",
        "Add stale revision/authorization only if the selected effect wrapper declares those fields."
      ],
      "verificationCases": [
        "Empty id or missing verified Principal rejects before journal/provider; a valid id creates a draft only and cannot directly dispatch; undeclared cancel has no route."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source combines empty id and commit-message checks in a legacy route. Retain empty-id early rejection and no dispatch, but make message optional audit metadata and cancellation schema capability-specific.",
      "contractAnchors": [
        "K01",
        "K03",
        "K04",
        "K07",
        "K08",
        "K12"
      ]
    }
  ],
  "crossGroupContracts": [
    "Scope boundary: account-backed facts, account capability queries, simulator/admin controls, and controlled effect/order references use explicit AccountScope; public Candle/Instrument/News/NewsGroup and other public data capabilities use Public or their declared scope. No route invents a default wallet/subaccount. See MAP-146D2109B6, MAP-4EFAFBF364, MAP-87FA16A102, MAP-5FBCD3EE4E.",
    "The fixed UTA-facing outer contract is CapabilityDescriptor (descriptor identity, schema fingerprints, delivery/effect metadata, resources, source and availability). Provider input/output/error trees are declaration-derived and open-ended; no global ActionContractMap, universal IBroker/SDK, or mandatory sibling handlers returning Unsupported. See MAP-F3C5E04898, MAP-4AFBEA2B00, MAP-6FA0B99F88.",
    "Provider-native fields and protocol details remain inside the selected adapter/process, while the HTTP boundary validates the exact declared JSON schema and preserves exact extensions. Availability/authorization/schema absence are distinct; an unsupported parameter variant is absent only from the selected leaf schema, while an undeclared leaf has no route and a declared-but-unavailable leaf retains identity. See MAP-0B6606BE1C, MAP-8456875518, MAP-3A43AEAF75.",
    "Order, Instrument, Candle, News, and NewsGroup are semantic units rather than a universal method list. An Order effect may compose provider-declared sizing/terms/relations with HOFs and preserve native extensions; local unions such as Units|Notional or Quantity|All are allowed only when that provider schema declares them. Do not infer non-market/notional or other cross-provider restrictions. See MAP-3CBE1C6172, MAP-27AD48107A, MAP-58D4065C2F.",
    "Candle/Instrument/News data pulls are finite results; declared push leaves are resource-scoped streams with lifecycle, cursor, backpressure, gaps, and finality evidence. Data reads/cache failures do not use order prepare/approve/compensate, order locks, or per-frame WAL. See MAP-5C97C6CC79, MAP-3C6243143F, MAP-8F1E449867.",
    "Only a controlled effect explicitly wrapped withTransaction receives durable intent/approval/dispatch/recovery, idempotent command identity, writer CAS/lease, and OutcomeUnknown rules. ReturnToAgent is a durable review/outbox branch with explicit retain/rearm/revise/discard; an event source is evidence, never authority. See MAP-0FC7B6DD68, MAP-7B9EA96B62, MAP-6BF94B5092, MAP-165076A00B.",
    "Transport admission requires a deployment-provisioned service credential/verifier and a verified Principal; loopback and a body actor do not authorize. Account/scope/capability/revision/expiry/policy are checked again at the selected application/effect boundary. The verifier owner and native sentinel/idempotency/relations evidence remain open. See MAP-2F69B76FDC, MAP-D034C92CBD, MAP-0682C42EA8.",
    "Git logs/status/hashes, snapshots, simulator state, and external simulator events are rebuildable or fixture projections. Git is not effect authority; simulator controls and external observations do not create TransactionApproved/Dispatch jobs, while snapshot retention is its own authorized non-trading capability. See MAP-FC80F364B6, MAP-AC8C619E99, MAP-1C7C835F5A, MAP-4D35581185.",
    "Verification must exercise declaration composition and runtime boundaries: add a provider field and observe schema/type/metadata/CLI changes; omit unsupported leaves and unsupported parameter variants only from their selected schemas; reject before handler; distinguish pull/push cancellation; validate external output; prove ReturnToAgent zero-dispatch/review recovery; and test duplicate events/late replies/new identities. Existing Mock/IBKR and route tests are evidence for their bounded scenarios only. See MAP-38469CADA4, MAP-248B2B5279, MAP-8D000718DA, MAP-2FADFA4CDF."
  ],
  "additionalFindings": [
    "routes-trading.ts:1-11,97-125 imports UnifiedTradingAccount, BrokerError, legacy order-entry, TradingGit and raw query helpers. This is direct source evidence of transport coupling, not a target interface; the replacement boundary is descriptor-bound protocol codecs plus selected application ports.",
    "routes-trading.ts:133-190 exposes manager listing/equity/search/FX directly. Account-backed responses must carry AccountScope and projection freshness; public search/market data must remain Public unless its declaration says Account. No query route gets transaction approval or compensation machinery.",
    "routes-trading.ts:198-215 dynamically creates a provider from request JSON and returns raw account/positions. The target is a declaration-derived probe capability using provisioned config/credential references; provider-native options remain exact adapter extensions and are not accepted as arbitrary HTTP objects.",
    "routes-trading.ts:220-259 conflates reconnect/sync/price simulation with booleans, sleeps, and message-regex errors. Reconnect and data refresh are resource capabilities; price simulation is a data preview. Only an explicitly selected controlled effect gets transaction recovery/unknown semantics.",
    "routes-trading.ts:261-378 accepts optional raw subAccountId, constructs IBKR Contract, revives dates, and returns native quote/expansion/details/bars. AccountScope is explicit for account-bound queries, while public Instrument/Candle capabilities have Public scope; all native construction/decoding remains in the selected adapter.",
    "routes-trading.ts:380-496 exposes Git log/history/show/status/commit/reject/push. Journal/observation projections are authority for controlled effects and audit; Git remains historical/display evidence. Prepare/approve/reject/ReturnToAgent are capabilities of a selected transaction wrapper, not methods required of every provider.",
    "routes-trading.ts:498-606 has stage-only and one-shot order handlers with arbitrary JSON and stage→commit→push. Keep stage-later/review ergonomics only as declaration-bound draft/prepare/review leaves; preserve provider-native fields and exact evidence, and omit absent capabilities rather than adding Unsupported siblings.",
    "routes-trading.ts:611-694 treats missing/error snapshots as empty, uses Number() and mutable carry-forward. Snapshot/equity curve are data projections with exact Decimal/Money, explicit coverage/freshness/carry policy, and a separate retention capability; no order WAL is used for reads.",
    "routes-simulator.ts:1-15,17-101 is explicitly a dev control panel with direct MockBroker and permissive Zod/JSON errors. Keep simulator as a separately mounted declared capability, authenticated/admin scoped and fixture-only; concrete MockBroker mechanics remain provider evidence, not a universal broker SDK.",
    "routes-simulator.ts:105-221 and simulator.spec.ts:46-208 prove Mock mark/fill/external accounting behavior. Preserve matching thresholds, partial fills, wallet cost/cash invariants, and pending-only cancellation as simulator falsifiers; add fixture identity/projection checks without treating HTTP 200 as durable transaction completion.",
    "routes-trading-wallet.spec.ts and trading-order-entry.spec.ts use casted contexts, vi stubs, hash checks, and call-order assertions. Retain the historical no-mutation/precision/race obligations, but independently exercise declaration codecs, verified Principal, selected transaction wrapper writer, scheduler/provider outcome, and durable receipts.",
    "trading-tools.spec.ts:1-387 co-locates Alice tools with MockBroker/IBKR fixtures and contains useful source-routing, decimal, sentinel, identity, expansion, and input-ergonomics evidence. These remain bounded provider/tool tests; they do not prove the complete provider runtime or require one global SDK.",
    "Unresolved empirical obligations remain explicit: service credential/verifier ownership, adapter-specific sentinel profiles/presence, native idempotency horizon, and native protection/relationship evidence. New capability composition does not resolve those facts or authorize production dispatch."
  ]
}
