{
  "group": "RuntimeComposition",
  "designBasis": "capability-composition-v1",
  "analysesSha256": "adced0f3a77520e350633aa909326fbe9fbbfe2bcd93331483046d64d6177bd2",
  "designSha256": "931249288fc1f74532d18e4e1839705a1fa59313f4c4cdf2fe577a332eac7e7c",
  "contractSha256": "73f5235ae86d3f1c9038dfd214e7207cdfb1833be7bc369f628d7d3f9017dd4f",
  "entries": [
    {
      "id": "MAP-AF81F62711",
      "verdict": "accepted",
      "reason": "健康测试审查 rechecked services/uta/src/domain/trading/__test__/uta-health.spec.ts:1-22: imports UnifiedTradingAccount/MockBroker; createUTA constructs UTA directly and flush advances fake timers for constructor-started _connect. The observed behavior is: createUTA chooses a MockBroker, calls new UnifiedTradingAccount(broker), and flush() advances fake timers by zero because construction launches _connect without awaiting it. The specific incompatibility or retained boundary is: The fixture makes timer flushing and constructor fire-and-forget part of the observable lifecycle. A test can see an object before its connection admission is known, and no durable startup phase is represented. I repaired the entry to the named target: Use AccountRuntimeFixture {accountScope: AccountScope, accountMode: DataOnly|FundedReadOnly|FundedWritable, brokerLayer: Scoped<BrokerConnectionService>, accountHealth: AccountHealth, healthEvents: Stream<HealthEvent>} under a StartupRecoveryCoordinator. The global runtime state is the canonical RuntimeHealth sum type; account fixture state cannot expose a separate admission/recovering flag. Concrete future falsifier: Start the root with no funded or public account registrations: Journal/Projection recovery reaches RuntimeHealth.Ready and lite/read-only query routes work without acquiring a broker carrier. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-D71042F42A",
      "verdict": "accepted",
      "reason": "健康测试审查 rechecked services/uta/src/domain/trading/__test__/uta-health.spec.ts:24-35: fake-timer test expects automatic connect to healthy, Date lastSuccessAt, recovering false. The observed behavior is: After createUTA and flush, the test reads health=healthy, requires lastSuccessAt to be a Date, and recovering=false. The specific incompatibility or retained boundary is: Healthy currently conflates transport/account readiness with a mutable Date snapshot and is observed only after an unstructured timer flush. It does not say which capability was proven or whether journal recovery completed. I repaired the entry to the named target: Use AccountHealth as a capability-derived record/ADT carrying AccountScope, connection generation, tier, reach, capability statuses, failure evidence and observation time `Known(Instant)|Unavailable(reason)`; derive a Healthy/Degraded view from those capabilities. The global RuntimeHealth remains the single Initializing|Recovering|Ready|Stopping|Failed ADT; do not add status, recovering, or admission booleans beside it. Concrete future falsifier: A keyless public registration with no private account read is AccountHealth.Reachable at the data tier while global readiness remains governed by journal/recovery barriers. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-EF93AB390A",
      "verdict": "accepted",
      "reason": "Independent recheck of services/uta/src/domain/trading/__test__/uta-health.spec.ts:37-48: failMode makes initial connect fail and the test observes offline/recovering/lastError. The source has no universal permanent-error mapping. The repaired target keeps unclassified adapter failures transient Unavailable/RecoveryRequired, while only an explicitly declared permanent capability/configuration failure may become CapabilityDisabled; structural absence is not runtime disablement. This is source-consistent and remains an adapter evidence obligation under K02/K07."
    },
    {
      "id": "MAP-1F940280FC",
      "verdict": "accepted",
      "reason": "健康测试审查 rechecked services/uta/src/domain/trading/__test__/uta-health.spec.ts:50-67: first init fails, five-second fake time advances, recovery becomes healthy/non-recovering. The observed behavior is: After an initial failure, advancing 5_000ms runs the first recovery attempt and the account becomes healthy with recovering=false. The specific incompatibility or retained boundary is: Recovery timing is encoded by setTimeout and only tests in-memory state. It is not durable work, has no owner/epoch, and cannot classify transactions that were executing while the connection was down. I repaired the entry to the named target: Use RecoverySchedule{attempt, nextAt, backoff, policyVersion} and ConnectionRecoveryJob{accountScope, generation}. A successful probe transitions Degraded/Offline -> Healthy only after capability ladder completion; transaction jobs are separately classified as ObserveUnknown or RetryRead. Concrete future falsifier: Broker fails once then succeeds: one RecoverySucceeded event, no duplicate account registration. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-7D71DB21A0",
      "verdict": "accepted",
      "reason": "健康测试审查 rechecked services/uta/src/domain/trading/__test__/uta-health.spec.ts:69-91: failMode=100 and fake time advances 5s,10s,20s; offline/recovering stays true. The observed behavior is: Recovery attempts occur at 5s, 10s, and 20s and retain offline/recovering when init keeps failing; close cancels the loop. The specific incompatibility or retained boundary is: The test proves an exponential sequence but does not prevent recovery timers from duplicating, does not fence stale attempts, and does not connect backoff to durable job state. Most importantly, this schedule cannot be reused for mutation retries after Unknown. I repaired the entry to the named target: Represent ConnectionRecoveryJob{accountScope,connectionGeneration,attempt,scheduledAt,policyVersion} and BackoffPolicy{base:5s,factor:2,max:5m}; this ADT is exclusively for connection probes. DispatchRetryPolicy is a separate ADT that has no blind retry transition after DispatchStarted. Concrete future falsifier: Three failures produce deadlines 5/10/20s under the injected clock. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-589FE4B148",
      "verdict": "accepted",
      "reason": "健康测试审查 rechecked services/uta/src/domain/trading/__test__/uta-health.spec.ts:93-110: failMode=2, initial and first recovery fail, second at 15s succeeds. The observed behavior is: The initial attempt and first recovery fail; after another 10_000ms (total 15s) the second recovery succeeds and health is healthy. The specific incompatibility or retained boundary is: Only the happy endpoint is asserted; there is no evidence that the first failed generation was closed, that the second probe is not concurrent, or that pending durable work is reclassified during the interval. I repaired the entry to the named target: Keep one AccountRecoveryState per AccountScope with monotonic attempt and connectionGeneration. A successful probe is only ReachabilityObservation; the coordinator classifies durable jobs before restoring account capability, while global RuntimeHealth readiness remains a journal/recovery barrier. Concrete future falsifier: Fail initial and first probe, then succeed: exactly one active scoped broker layer and one reachable transition exist. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-9C2B1A32E6",
      "verdict": "accepted",
      "reason": "健康测试审查 rechecked services/uta/src/domain/trading/__test__/uta-health.spec.ts:112-127: after healthy connect, three getAccount failures expected to produce degraded. The observed behavior is: Three consecutive getAccount rejections move the health string from healthy to degraded. The specific incompatibility or retained boundary is: Failure counting is mutable and method outcomes are reduced to a threshold. A transport-alive/private-read failure needs capability-specific degradation, otherwise callers may treat an account as wholly unavailable or still safe for writes. I repaired the entry to the named target: Use an AccountCapabilityHealth record keyed by AccountScope and generation, with explicit policyVersion and source-compatible defaults degradedThreshold=3 and offlineThreshold=6. Overall account health is derived from capability variants; RuntimeHealth remains the single global sum type and has no separate threshold/admission flags. Concrete future falsifier: After three private-read failures, list shows Degraded/AccountReadUnavailable; no transaction is dispatched. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-A227BE1784",
      "verdict": "accepted",
      "reason": "Independent recheck of services/uta/src/domain/trading/__test__/uta-health.spec.ts:129-141: six getAccount failures move the UTA offline and start recovery. The repaired target preserves the six-failure/fail-fast boundary but reports scoped CapabilityUnavailable versus transport ConnectionUnavailable, keeps stale projections explicitly stale, and never derives executable RuntimeHealth.Ready or a mutation from private-read loss. Adapter distinction remains an integration obligation under K02/K05/K07."
    },
    {
      "id": "MAP-21CC430684",
      "verdict": "accepted",
      "reason": "Independent recheck of services/uta/src/domain/trading/__test__/uta-health.spec.ts:143-159: after six failed reads, a five-second recovery attempt returns the test UTA to healthy. The target now requires accepted fresh capability evidence and generation fencing before RecoverySucceeded; transaction jobs are reconciled separately. The source timing is retained as connection evidence, not as permission to execute."
    },
    {
      "id": "MAP-56FD13561B",
      "verdict": "accepted",
      "reason": "健康测试审查 rechecked services/uta/src/domain/trading/__test__/uta-health.spec.ts:161-175: four failures produce degraded, next successful getAccount resets health/counter. The observed behavior is: Four failed calls leave degraded; the next successful getAccount changes health to healthy and consecutiveFailures=0. The specific incompatibility or retained boundary is: A success reset is unconditional and does not verify freshness, scope, or whether the read is the capability needed for the caller. A successful market-clock call should not necessarily make account-read capability healthy. I repaired the entry to the named target: Use capability-scoped success evidence: `AccountReadSucceeded` resets AccountRead failures and may restore target reach; `MarketClockSucceeded` resets only MarketClock evidence. Overall health is a derived view over required capabilities, not a single shared counter. Concrete future falsifier: After account failures, a valid account observation resets account failures; only market-clock success leaves account degraded. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-39DD2BB1A1",
      "verdict": "accepted",
      "reason": "Independent recheck of services/uta/src/domain/trading/__test__/uta-health.spec.ts:177-188: getAccount/getPositions share the source failure counter, yet a successful getMarketClock marks the UTA healthy. The repaired target uses an open provider-declared capability-health map, treating those names as examples rather than a closed taxonomy; clock reach cannot clear account or position failures. This directly preserves the observed cross-method behavior without imposing a finite provider capability set."
    },
    {
      "id": "MAP-687A495AFD",
      "verdict": "accepted",
      "reason": "Independent recheck of services/uta/src/domain/trading/__test__/uta-health.spec.ts:191-207: once offline/reconnecting, getAccount fails fast without another broker call. The repaired target keeps that no-IO safety boundary as a scoped CapabilityUnavailable or ConnectionUnavailable according to the failed leaf/transport, while stale projection reads are explicitly labeled and never executable. No global readiness flag or broad string error is introduced."
    },
    {
      "id": "MAP-DBB2C39ABF",
      "verdict": "accepted",
      "reason": "健康测试审查 rechecked services/uta/src/domain/trading/__test__/uta-health.spec.ts:209-222: after offline, stage/commit then push rejects with offline text. The observed behavior is: The test stages and commits a BUY locally, then push rejects while broker is offline; close follows. The specific incompatibility or retained boundary is: Push is still a direct mutation path after local Git commit. It does not create a durable transaction/dispatch intent before checking reachability, so a process crash cannot distinguish prepared work from an attempted remote mutation. I repaired the entry to the named target: Model `push` as an executable transaction command bound to AccountScope, actor, revision, planDigest and capability. When the account is Offline or FundedReadOnly, the command returns a typed refusal; it does not create an executable job. A separate non-executable DraftReview can remain for funded readOnly UX, while write-capable work only enters Queued after authoritative approval and a durable receipt. Concrete future falsifier: Offline funded writable account: push returns ConnectionUnavailable, broker call count and EffectJob count remain zero, while a prior DraftReview remains queryable. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-02559D426B",
      "verdict": "accepted",
      "reason": "健康测试审查 rechecked services/uta/src/domain/trading/__test__/uta-health.spec.ts:224-241: stage and commit succeed offline, result.staged=true and prepared=true without broker contact. The observed behavior is: Offline status does not prevent local staging or commit; commit reports prepared=true without contacting the broker. The specific incompatibility or retained boundary is: This proves proposal preparation can be local, but current Git state is process/in-memory/file-backed and is not a durable approval transaction. `prepared=true` has no digest, revision, before-image, expiry, or recovery evidence. I repaired the entry to the named target: Separate canonical non-executable `DraftReview` from executable `PreparedPlan`; DraftReview may carry a named `analysis:None|PreparationPreview` value containing pure analysis over explicitly supplied facts. Stage always yields a revisioned DraftReview. FundedReadOnly may yield DraftReview with a PreparationPreview analysis when facts are available, but it cannot produce PreparedPlan or Approval. An offline executable prepare is allowed only for a write-capable account when every required account/market/capability fact is explicitly supplied, fresh and serializable; with missing facts it returns PreparationDeferred and never creates locks/jobs. Concrete future falsifier: Funded readOnly PlaceOrder produces DraftReview with all order fields and, when facts are supplied, only a nested PreparationPreview; no locks/jobs/dispatch receipt exist and approve returns AuthorizationDenied(ReadOnly). No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-A1F54D2BA5",
      "verdict": "accepted",
      "reason": "Independent recheck of services/uta/src/domain/trading/__test__/uta-health.spec.ts:244-258: close cancels the pending recovery timeout and sets recovering=false. The source proves timer cancellation only; it does not establish a production three-second shutdown deadline. The repaired target therefore requires a configured bounded shutdown deadline and ordered resource/fiber release, explicitly treating the separate three-second child-cleanup helper as cleanup evidence rather than a runtime contract."
    },
    {
      "id": "MAP-3A811B60A0",
      "verdict": "accepted",
      "reason": "Independent source audit of services/uta/src/domain/trading/index.ts:1-2, contract-ext.ts:1-7, packages/uta-protocol/src/types/contract-ext.ts:1-30, UnifiedTradingAccount.ts:509-544 and trading-tools.spec.ts:275-328: the compatibility modules are declaration-only, while UTA code explicitly reads/writes contract.aliceId. The repaired currentBehavior no longer claims prototype mutation. The target removes the unnecessary barrel compatibility import and keeps exact native decoding/identity binding in an adapter codec, consistent with K02/K03/K07."
    },
    {
      "id": "MAP-7DD89036D6",
      "verdict": "accepted",
      "reason": "trading barrel 审查 rechecked services/uta/src/domain/trading/index.ts:4-6: barrel exports UnifiedTradingAccount and stage parameter types. The observed behavior is: The barrel exposes UnifiedTradingAccount as a class plus UnifiedTradingAccountOptions and stage-place/modify/close parameter types. The specific incompatibility or retained boundary is: Public import establishes the legacy class and stage API as a runtime execution authority, allowing routes/tools to bypass transaction-kernel preparation and durable receipts. I repaired the entry to the named target: Public trading exports contain TradeAction/OrderSpec and serializable canonical DraftReview{analysis:None|PreparationPreview}; PreparationPreview is descriptive and non-executable. UnifiedTradingAccount and its stage parameter records are not public runtime execution aliases; any temporary migration adapter is composition-private and cannot dispatch without TransactionApplication authorization. Concrete future falsifier: A stage command yields a serializable draft/digest; no broker call before durable approval. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-DD2FE66A82",
      "verdict": "accepted",
      "reason": "Independent recheck of services/uta/src/domain/trading/index.ts:8-9: the barrel reexports the map-backed UTAManager runtime class. The repaired target replaces that authority with named query/lifecycle/transaction/capability ports and requires AccountScope for account queries and durable uniqueness; no AccountId-only lookup or runtime alias remains. The source behavior is accurately treated as legacy surface, not native target proof."
    },
    {
      "id": "MAP-EFD22B7831",
      "verdict": "accepted",
      "reason": "trading barrel 审查 rechecked services/uta/src/domain/trading/index.ts:10-15: barrel reexports UTASummary, AggregatedEquity, ContractSearchResult, SnapshotHooks from manager. The observed behavior is: The manager barrel reexports three response types and SnapshotHooks alongside the manager class. The specific incompatibility or retained boundary is: These types appear canonical but are coupled to a mutable manager and expose legacy aggregation/snapshot semantics; SnapshotHooks are callbacks rather than durable events. I repaired the entry to the named target: Canonical protocol views are AccountSummary, AggregatedValuation, and SearchResult, each carrying scope/capability/valuation outcome and sequence/freshness where applicable. AggregatedValuation uses ValuationResult=NoAccounts|Complete{status:Confirmed|Stale|Estimated,...}|Incomplete{availableAccounts,missingScopes,reasons}. SnapshotRequest is a tagged journal event, not a callback; old DTO names can exist only as type-only source aliases during cutover and never as runtime execution aliases. Concrete future falsifier: A stale projection is returned with freshness metadata, not as current zero. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-41FE208558",
      "verdict": "accepted",
      "reason": "trading barrel 审查 rechecked services/uta/src/domain/trading/index.ts:17-24: barrel exports IBroker, Position, PlaceOrderResult, OpenOrder and notes against eager SDK imports. The observed behavior is: The barrel exports the generic broker interface and execution/read model types from brokers/index. The specific incompatibility or retained boundary is: IBroker’s broad methods allow action/result mismatches and PlaceOrderResult implies remote success/possibly fill without action-specific acknowledgement/observation. Exporting it from a domain barrel spreads adapter types. I repaired the entry to the named target: Use a closed ActionContract map pairing each TradeAction tag with its NativePlanRecord, Ack, RemoteOutcome, Failure and compensation assessment. The map is capability/mode-gated: only FundedWritable may produce an executable native plan; DataOnly/FundedReadOnly yields Unavailable without a job. Ack means only the broker acknowledgement level; Confirmed|Rejected|StillWorking|Unknown observations determine remote outcome. No generic IBroker write result is exported from the domain barrel. Concrete future falsifier: A rejected native response maps to Rejected, not a fake PlaceOrderResult. Source leaves this empirical boundary unresolved; the target keeps it unavailable/typed and does not claim support: Adapter-specific native idempotency, listing completeness and atomic protection guarantees are not established by this barrel; absent evidence, the corresponding ActionContract capability remains Unavailable or ReconcileBeforeRetry. This is a source-grounded design acceptance, not a runtime-test claim. Question closure cross-checked ProtocolModels/design.md:17-23, CcxtVenues/design.md:142-147, and IbkrBridge/design.md:289-299; action-specific mapping is settled, while native idempotency/listing/protection evidence remains adapter empirical."
    },
    {
      "id": "MAP-32B6CEE9FA",
      "verdict": "accepted",
      "reason": "Independent recheck of services/uta/src/domain/trading/index.ts:25-30: heterogeneous AccountInfo, Quote, MarketClock, AccountCapabilities and TpSlParams are broadly exported. The repaired target keeps provider-native protection only when its exact action schema declares it. An absent TP/SL leaf/variant is structurally absent and rejected before its handler; another declared variant remains available. No generic optional bag or fabricated Unavailable success path is required by K02/K03."
    },
    {
      "id": "MAP-C466C2AC10",
      "verdict": "accepted",
      "reason": "trading barrel 审查 rechecked services/uta/src/domain/trading/index.ts:31-33: barrel reexports createBroker async concrete factory. The observed behavior is: Consumers can import createBroker from the domain barrel; it resolves preset/engine and constructs concrete brokers asynchronously. The specific incompatibility or retained boundary is: The domain barrel becomes a factory locator and config/SDK boundary, bypassing composition Layers, credential ownership and scoped resource lifetimes. I repaired the entry to the named target: Validated AccountConfig plus a sealed CredentialRef selects a broker Layer; acquiring it yields an AccountScope-bound ConnectionHandle, generation and capability set. Application code cannot import or invoke a concrete broker factory or select an engine ad hoc. Concrete future falsifier: Missing broker pack yields one account capability unavailable while process remains ready for unrelated accounts. Source leaves this empirical boundary unresolved; the target keeps it unavailable/typed and does not claim support: The shipped set of broker packs and their native idempotency guarantees is not proven by this export; absent a pack/capability declaration, keep the account unavailable and do not dispatch. This is a source-grounded design acceptance, not a runtime-test claim. Question closure cross-checked Catalog/design.md:19-35,99-115: v2 pack descriptors and PackLoadFailure are settled, but pack-specific idempotency/late-observation guarantees remain conformance evidence; unverified packs stay unavailable."
    },
    {
      "id": "MAP-C2848F848F",
      "verdict": "accepted",
      "reason": "trading barrel 审查 rechecked services/uta/src/domain/trading/index.ts:35-58: barrel exports TradingGit and complete staging/commit/push/sync type surface. The observed behavior is: The barrel exposes TradingGit plus operation, commit, push, sync, GitState and export types as the public trading workflow. The specific incompatibility or retained boundary is: Git-shaped classes/types become execution and persistence authority; commit hashes can be mistaken for transaction identities, and file-backed state can bypass atomic journal writer. I repaired the entry to the named target: Replace TradingGit runtime exports with serializable transaction commands/events and `PreparedPlanRecord`; preserve a `GitAuditProjection` exporter that consumes committed journal sequence and writes human-readable files only after authority commits. Runtime resolution is canonical AccountId/AccountScope only; Git hashes are never execution aliases or TransactionId/DispatchId. Concrete future falsifier: Commit response loss is recoverable by CommandId query; Git export may lag without changing transaction. Source leaves this empirical boundary unresolved; the target keeps it unavailable/typed and does not claim support: The persisted legacy file shape is not established in this source; import it only as read-only audit/history, never as executable plan or identity. This is a source-grounded design acceptance, not a runtime-test claim. Question closure source audit checked git-persistence.ts:14-49 and uta-protocol/src/types/git.ts:160-174: primary/legacy commit.json paths and GitExportState{commits,head} are known; import is LegacyEvidence/audit-only, never execution identity."
    },
    {
      "id": "MAP-38D1A9619B",
      "verdict": "accepted",
      "reason": "trading barrel 审查 rechecked services/uta/src/domain/trading/index.ts:60-74: barrel reexports snapshot service/scheduler/store/builder/types. The observed behavior is: The barrel exports snapshot creation/scheduling/storage/build APIs and their file-backed types from domain/trading. The specific incompatibility or retained boundary is: Snapshot implementation and schedule are visible as domain services even though files are rebuildable projections; timer/store ownership can compete with JournalStore and execute before recovery. I repaired the entry to the named target: Place snapshot read models under projection-store; schedule them with durable/supervised Effect fibers. Snapshot payload is a versioned schema keyed by AccountScope, sequence and freshness; builder is pure over projection rows. Concrete future falsifier: A projection rebuild from journal yields same snapshot for a sequence. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-0A182AEB30",
      "verdict": "accepted",
      "reason": "trading barrel 审查 rechecked services/uta/src/domain/trading/index.ts:76-89: barrel reexports guard pipeline/registry/built-ins and GuardContext/OperationGuard/GuardRegistryEntry. The observed behavior is: The barrel exposes mutable guard registration/resolution and built-in guard types from the legacy path. The specific incompatibility or retained boundary is: Guards are reachable as generic runtime registry entries and can read broker/Git state or return strings; permissive unknown registry types and dispatch-time IO undermine deterministic preparation. I repaired the entry to the named target: Use a closed, schema-validated GuardSpec union and GuardIssue union. Preparation receives immutable before-images, typed facts, AccountScope and AccountMode/Capability. Authorization, capability, policy and precondition issues are separate variants; all guards pass can yield PreparedPlanRecord only for FundedWritable with required facts, while FundedReadOnly/DataOnly/offline paths yield DraftReview with optional non-executable PreparationPreview analysis or PrepareRejected. There is no mutable string registry or unknown-kind skip path. Concrete future falsifier: Unknown guard config prevents startup/admission instead of silently skipping. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-DEE616DB32",
      "verdict": "accepted",
      "reason": "keyless source builder 审查 rechecked services/uta/src/domain/trading/keyless-data-sources.ts:3-7: LABELS maps closed binance/okx/bybit enum values to labels. The observed behavior is: LABELS is a total Record over KeylessDataSource and is used to generate display labels. The specific incompatibility or retained boundary is: Presentation labels live next to account identity construction, encouraging a string/config value to become a runtime account without venue metadata or capability context. I repaired the entry to the named target: Keep the closed `KeylessSource -> canonical label` mapping at the configuration/application boundary. The label is presentation metadata; `VenueId`, jurisdiction/timezone, public endpoint descriptor and capability registration carry identity and authorization context. Concrete future falsifier: Every supported source gets deterministic label/venue metadata; adding a source requires exhaustive schema/label/capability updates. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-E7FFEA181E",
      "verdict": "accepted",
      "reason": "keyless source builder 审查 rechecked services/uta/src/domain/trading/keyless-data-sources.ts:9-15: comments state keyless UTAs are not persisted to accounts.json and require explicit trading.json opt-in. The observed behavior is: The policy documents opt-in public-data accounts, excludes them from accounts.json, and avoids unsolicited exchange connections. The specific incompatibility or retained boundary is: Opt-in/non-persistence is only a comment. The generated UTA is still a legacy account object and its no-mutation boundary depends on booleans/guards rather than an executable capability model. I repaired the entry to the named target: Decode explicit trading.json selections into durable `KeylessDataRegistration{accountScope,source,venue,connectionMode:Public,capabilities,configRevision}`. SQLite retains the registration/history row across restarts; removing a source records Retired and stops layer acquisition, while an equivalent selected row is reconciled idempotently. Executable actions are Unavailable(DataOnly). Concrete future falsifier: An empty or removed keyless selection creates no new row/layer; a previously selected row becomes Retired and is still queryable as history after restart. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-5B129B67D7",
      "verdict": "accepted",
      "reason": "keyless source builder 审查 rechecked services/uta/src/domain/trading/keyless-data-sources.ts:17-23: buildKeylessDataUTAs deduplicates source array in insertion order and filters generated ids in existingIds. The observed behavior is: The function uses Set to preserve first occurrence, skips `${ex}-readonly` collisions, and returns one config per remaining source. The specific incompatibility or retained boundary is: Dedup/collision is deterministic but uses raw strings and a caller-provided Set. It cannot distinguish an authoritative funded account, stale registry row, disabled account, or incompatible same-id registration. I repaired the entry to the named target: Pure planning returns `KeylessRegistrationPlan{entries: ordered KeylessSource, canonical AccountId, explicit AccountScope, sourceRevision}` after first-seen deduplication. Reconciliation compares the desired registration with durable `AccountRegistry{accountId,subAccountId,kind,configRevision,lifecycle}` and yields AlreadyEquivalent, NeedsRepair or Conflict; raw Set membership is not authority. Concrete future falsifier: Duplicate source entries yield one registration in first-seen order. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-6AE1898715",
      "verdict": "accepted",
      "reason": "keyless source builder 审查 rechecked services/uta/src/domain/trading/keyless-data-sources.ts:24-36: maps selected source to UTAConfig with generated id/label, ccxt-custom preset, booleans keyless/readOnly/asVendor/editable. The observed behavior is: Each selected source becomes enabled ccxt-custom config with empty guards, keyless/readOnly true, asVendor true, editable false and exchange in presetConfig. The specific incompatibility or retained boundary is: Boolean flags and untyped presetConfig can be contradictory or bypassed. The config lacks venue/jurisdiction/session metadata and offers no type-level distinction between historical data and executable account actions. I repaired the entry to the named target: Build `DataSourceRegistration{accountScope,source,venue,jurisdiction,timeZone,connectionMode:Public,label,capabilities}` rather than UTAConfig flags. Capability variants explicitly grant HistoricalBars only when adapter evidence exists and return Unavailable(DataOnly) for AccountRead, PositionRead, OrderRead and Place/Modify/Cancel/Close; `asVendor` is discovery metadata and `editable` is projection metadata. Concrete future falsifier: A keyless account appears in listing as data-only and can expose market history. Source leaves this empirical boundary unresolved; the target keeps it unavailable/typed and does not claim support: Per-venue jurisdiction/session/public-endpoint facts are not in this source; until the adapter catalog supplies them, registration remains Unavailable rather than guessing metadata. This is a source-grounded design acceptance, not a runtime-test claim. Question closure cross-checked Catalog/design.md:11-17,49-53: registration requires explicit venue/jurisdiction/session/endpoint evidence; concrete metadata remains unavailable until adapter catalog evidence."
    },
    {
      "id": "MAP-018E718257",
      "verdict": "accepted",
      "reason": "keyless construction fixture 审查 rechecked services/uta/src/domain/trading/keyless-data-uta.spec.ts:1-5: test documentation says optional binance/okx/bybit read-only, no key, pure construction/no network. The observed behavior is: The spec is explicitly construction-only: it imports factory/builder and promises no network; no runtime capability or plan refusal is exercised. The specific incompatibility or retained boundary is: Construction can prove no eager network call but cannot prove scoped public connection, absence of credential use, or that transaction admission rejects data-only accounts. I repaired the entry to the named target: Keep this unit as a pure decoder/construction test for the closed KeylessSource set and no-network guarantee. Runtime capabilities are verified by a separate scoped composition test: public historical query may be Available, private account/mutation capabilities are Unavailable(DataOnly), and no execution job is created. Concrete future falsifier: Builder does not touch network. Source leaves this empirical boundary unresolved; the target keeps it unavailable/typed and does not claim support: This assigned unit has no remote public-venue run; without adapter evidence historical-data freshness remains an explicitly unverified capability rather than an implied runtime guarantee. This is a source-grounded design acceptance, not a runtime-test claim. Question closure reviewed the keyless construction boundary and Catalog evidence: no-network construction is not public endpoint/freshness proof; historical capability stays Unavailable/Unknown absent scoped run evidence."
    },
    {
      "id": "MAP-857191C07B",
      "verdict": "accepted",
      "reason": "keyless construction fixture 审查 rechecked services/uta/src/domain/trading/keyless-data-uta.spec.ts:11-17: cfgFor builds UTAConfig from raw exchange and uses as unknown as UTAConfig. The observed behavior is: cfgFor interpolates raw ex into id/label/presetConfig and casts an object with keyless/readOnly/editable flags past the config schema. The specific incompatibility or retained boundary is: The cast bypasses closed source validation and permits arbitrary exchange strings or extra/missing config fields into factory tests. I repaired the entry to the named target: Use `decodeKeylessSource(unknown) -> Result<KeylessSource, ConfigFailure>` and a typed `DataSourceRegistrationFixture`; the fixture cannot represent an unsupported exchange or arbitrary presetConfig. Keep the `AccountMode=DataOnly` capability in the registration, not as a casted boolean bag. Concrete future falsifier: `btc` or malformed config is rejected before broker construction. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-41F4367315",
      "verdict": "accepted",
      "reason": "keyless construction fixture 审查 rechecked services/uta/src/domain/trading/keyless-data-uta.spec.ts:19-22: empty source selection expects buildKeylessDataUTAs([], new Set()) to equal []. The observed behavior is: Empty selection returns an empty array and performs no account construction in this unit. The specific incompatibility or retained boundary is: The assertion does not prove startup avoids registry mutation, broker Layer acquisition, or durable account projection creation. I repaired the entry to the named target: The empty selection result is a pure `NoKeylessRegistrations` plan: it emits no registration/acquisition intent and creates no new AccountRow. Previously registered keyless rows are not deleted by this no-op; config reconciliation records them Retired after durable safety checks and keeps their history. Concrete future falsifier: Fresh empty list leaves broker acquisition count and account-registry sequence unchanged. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-8B95CE02C1",
      "verdict": "accepted",
      "reason": "keyless construction fixture 审查 rechecked services/uta/src/domain/trading/keyless-data-uta.spec.ts:24-27: selected binance/okx produce ordered generated ids. The observed behavior is: Selected source order is preserved in generated ids binance-readonly then okx-readonly. The specific incompatibility or retained boundary is: Raw generated strings are not branded identity and do not include account scope/subaccount or venue metadata. I repaired the entry to the named target: Preserve first-seen order in `KeylessRegistrationPlan.entries`; each entry carries branded AccountId, explicit AccountScope, VenueId/jurisdiction and DataOnly capability. Ordering is a deterministic config/projection sequence only and never a remote capability or authorization signal. Concrete future falsifier: Repeated binance is one first-position entry. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-EB5CA76553",
      "verdict": "accepted",
      "reason": "keyless construction fixture 审查 rechecked services/uta/src/domain/trading/keyless-data-uta.spec.ts:29-32: existing binance-readonly id suppresses generated Binance while retaining okx. The observed behavior is: A raw existing-id Set suppresses a generated id and leaves other selected source output. The specific incompatibility or retained boundary is: Same-id suppression can conceal an incompatible existing account or stale lifecycle row; it does not report whether the desired data registration is already healthy. I repaired the entry to the named target: Reconcile generated canonical AccountId against durable registration kind/scope and return `AlreadyEquivalent`, `NeedsRepair` or `Conflict`. A conflict with a funded/user account is never replaced automatically; only an explicit authorized lifecycle command may retire/repair it, and absent that command no public layer is acquired. Concrete future falsifier: User-funded row with same id causes no public connection or mutation. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-38975A9375",
      "verdict": "accepted",
      "reason": "keyless construction fixture 审查 rechecked services/uta/src/domain/trading/keyless-data-uta.spec.ts:34-44: each source createBroker, inspects id/keyless flag, historicalBars supported. The observed behavior is: For binance/okx/bybit, createBroker succeeds without network and exposes id, keyless=true and historicalBars.supported=true. The specific incompatibility or retained boundary is: The test inspects private implementation via casts and only proves construction. It does not prove the scoped runtime never requests credentials or that account/mutation operations are unavailable. I repaired the entry to the named target: Use the public broker-spi capability manifest rather than casts: `HistoricalBars=Available` only with adapter evidence, `CredentialMode=NotRequired(Public)`, `AccountRead/PositionRead/OrderRead/Mutation=Unavailable(DataOnly)`. Construction success does not itself prove endpoint freshness or remote support. Concrete future falsifier: All three construct under no credentials. Source leaves this empirical boundary unresolved; the target keeps it unavailable/typed and does not claim support: The source does not establish venue-specific public endpoint/freshness guarantees; until each adapter declares them, historical capability remains Unavailable or quality-annotated rather than being inferred from construction. This is a source-grounded design acceptance, not a runtime-test claim. Question closure preserves the capability-manifest rule and records the missing per-venue endpoint/freshness/entitlement observation; construction and private flags do not prove HistoricalBars."
    },
    {
      "id": "MAP-DCAD63E801",
      "verdict": "accepted",
      "reason": "keyless construction fixture 审查 rechecked services/uta/src/domain/trading/keyless-data-uta.spec.ts:46-51: OKX keyless assetClassFor returns crypto for FUT and STK-looking native contracts. The observed behavior is: The test expects venue-specific asset classification to return crypto even when native SecType says FUT or STK. The specific incompatibility or retained boundary is: The heuristic silently overrides contradictory native metadata and could turn an instrument into the wrong domain type; the test does not show what decode rule justifies the override. I repaired the entry to the named target: Decode a `VenueInstrumentFact{venue,nativeIdentity,marketKind,assetClassHint,ruleVersion}` at the adapter boundary. An OKX crypto classification is accepted only when this fact and explicit venue rule support synthetic crypto; contradictory native fields otherwise produce ProtocolConflict/Unavailable and no executable instrument. Concrete future falsifier: A tokenized-equity-looking symbol is classified crypto only when venue fact says so. Source leaves this empirical boundary unresolved; the target keeps it unavailable/typed and does not claim support: The assigned source does not contain the shipped OKX instrument-rule evidence; until the adapter supplies it, conflicting FUT/STK-looking metadata remains ProtocolConflict/Unavailable rather than being force-labelled crypto. This is a source-grounded design acceptance, not a runtime-test claim. Question closure checked RuntimeComposition/design.md:70-82 and Catalog evidence: an OKX heuristic is not native rule proof; conflicting classification stays ProtocolConflict/Unavailable until a versioned adapter fact exists."
    },
    {
      "id": "MAP-29525F619A",
      "verdict": "accepted",
      "reason": "keyless construction fixture 审查 rechecked services/uta/src/domain/trading/keyless-data-uta.spec.ts:53-64: keyless Binance getAccount returns zero fields and getPositions/getOrders empty. The observed behavior is: Keyless account reads return a zeroed USD AccountInfo and empty position/order arrays without auth/init. The specific incompatibility or retained boundary is: Zero account state is indistinguishable from a real funded zero balance; empty orders/positions hide that private account capability was never attempted. This can contaminate projections unless manager excludes keyless. I repaired the entry to the named target: Return a tagged `DataOnlyAccountReadUnavailable{scope,reason:DataOnly}` for keyless account/position/order reads. Public history remains a separate capability; authenticated zero balances use `Money(0)` only when a scoped private observation proves zero, and DataOnly rows are excluded from valuation with an explicit reason. Concrete future falsifier: Keyless getAccount produces no AccountMoney row. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-EAE8B7150D",
      "verdict": "accepted",
      "reason": "Independent recheck of services/uta/src/domain/trading/uta-manager.spec.ts:1-15: tests construct UnifiedTradingAccount/MockBroker directly and pass legacy options into UTAManager. The repaired fixture target requires an explicit AccountScope registration and durable journal/projection boundary; duplicate scope is typed rather than a bare DuplicateAccountId. The source test remains a useful MockBroker seam but does not prove durable authorization."
    },
    {
      "id": "MAP-CF1AB5610D",
      "verdict": "accepted",
      "reason": "UTAManager test fixture 审查 rechecked services/uta/src/domain/trading/uta-manager.spec.ts:17-22: beforeEach creates a new in-memory UTAManager. The observed behavior is: Each test starts with a new map-backed manager; no EventLog, ToolCenter, FxService, storage, scope, or recovery worker is configured. The specific incompatibility or retained boundary is: Fresh maps isolate tests but conceal restart behavior, durable uniqueness, pending jobs, and resource cleanup; bugs in shared writer/lease state cannot be observed. I repaired the entry to the named target: Each test starts a `TestRuntimeScope` with isolated SQLite namespace, deterministic Clock/Identity, JournalWriter, ProjectionReader and supervised workers. The fixture supports close/reopen so the same AccountScope, receipt and pending state are reconstructed from durable rows rather than a new Map. Concrete future falsifier: Close/reopen test runtime: account listing and pending state remain. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-2F3E94599C",
      "verdict": "accepted",
      "reason": "Independent recheck of services/uta/src/domain/trading/uta-manager.spec.ts registration/removal cases: add inserts raw string ids, duplicate/removal use generic map behavior, and remove does not close the UTA. The target moves these transitions to explicit AccountScope lifecycle commands, typed AccountAlreadyExists/AccountNotFound, job classification and resource release while retaining the durable row. This is a redesign of the observed map semantics, not a claim that the source already persists them."
    },
    {
      "id": "MAP-7DDACC8390",
      "verdict": "accepted",
      "reason": "UTAManager test fixture 审查 rechecked services/uta/src/domain/trading/uta-manager.spec.ts:55-75: listUTAs returns id/asVendor summaries and preserves asVendor=false option. The observed behavior is: listUTAs maps live UTA objects to summaries containing id, label, asVendor, broker capabilities and health; tests check two accounts and false vendor flag. The specific incompatibility or retained boundary is: Summaries read mutable objects directly and omit scope, capability evidence/freshness, lifecycle, degraded detail, and projection sequence. A list can report a registered account even when its connection is unusable. I repaired the entry to the named target: Read immutable `AccountView{accountScope,kind,label,capabilities,accountHealth,lifecycle,asOf,sequence}` from projection. `asVendor` is rendered as a discovery capability only; it cannot grant approval/dispatch. A listed account may be Degraded/Unavailable while still durably configured. Concrete future falsifier: Offline account lists as Registered+Degraded, not omitted or falsely healthy. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-CF54E62DC9",
      "verdict": "accepted",
      "reason": "Independent recheck of services/uta/src/domain/trading/uta-manager.spec.ts:77-109: exact id lookup works, resolveOne throws on no result, and the supposed multiple-match case never creates ambiguity. The repaired target resolves only canonical AccountScope plus Principal; duplicate scope is a registry invariant and legacy/ambiguous input is InvalidAccountRef, not a runtime candidate list. The unexercised source branch remains correctly identified as unproven."
    },
    {
      "id": "MAP-5C85CDA9F5",
      "verdict": "accepted",
      "reason": "UTAManager test fixture 审查 rechecked services/uta/src/domain/trading/uta-manager.spec.ts:111-140: aggregation tests sum equity/cash/realized and UTA-derived unrealized PnL; empty manager gives zeros. The observed behavior is: Two MockBroker accounts report equity/cash/realized values while positions make unrealized PnL 2,000+3,000; manager sums Decimal strings, and empty manager returns zeros. The specific incompatibility or retained boundary is: The test trusts in-memory account reads, skips keyless/unhealthy accounts implicitly, assumes same currency, and has no FX provenance, durable accounting projection or restart semantics. I repaired the entry to the named target: Use AccountValuationProjection keyed by AccountScope with currency-qualified Money, exposure/PnL components, FX evidence and sequence. Fold results into ValuationResult = NoAccounts | Complete{status:Confirmed|Stale|Estimated,total,accounts} | Incomplete{missingScopes,reasons,availableAccounts}; DataOnly and unavailable rows are never fabricated as zero, and any missing required account evidence makes the aggregate Incomplete. Concrete future falsifier: Two USD accounts reproduce exact Decimal totals as Complete/Confirmed. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-167CD10E5C",
      "verdict": "accepted",
      "reason": "Independent recheck of services/uta/src/domain/trading/uta-manager.spec.ts:142-180: default search excludes asVendor=false without calling that broker, while explicit accountId can search it. The repaired discovery target supports AccountScope or PublicDataScope and keeps discovery separate from trading capability; source selection metadata never grants authorization. Matches, NoMatch, Unavailable and ProjectionFailure remain distinct."
    },
    {
      "id": "MAP-9717307646",
      "verdict": "accepted",
      "reason": "UTAManager test fixture 审查 rechecked services/uta/src/domain/trading/uta-manager.spec.ts:181-229: scoped search by accountId, explicit search bypasses asVendor=false, empty results filtered; no unknown/unhealthy/error cases. The observed behavior is: Explicit accountId restricts search, including asVendor=false; accounts with empty result are omitted from final list. The specific incompatibility or retained boundary is: Scope is raw string and unknown/unhealthy/broker throw cases are untested; filtering empty results conflates no-match with unavailable and native result data is not codec-checked. I repaired the entry to the named target: Use ScopedSearchRequest{accountScope,principal,pattern,assetFilter:Any|Specific(AssetClass)} and exhaustive per-account Matches|NoMatch|Unavailable|ProtocolViolation; merge into an envelope that may contain matches:[] only for NoMatch and always carries outcome/completeness metadata. Explicit scope never bypasses auth, capability or health. Concrete future falsifier: Unknown account makes zero broker calls and returns NotFound. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-C6D49BEAB0",
      "verdict": "accepted",
      "reason": "UTAManager test fixture 审查 rechecked services/uta/src/domain/trading/uta-manager.spec.ts:231-249: getContractDetails returns details for known account and null for unknown; asserts native contract fields. The observed behavior is: Manager forwards a native Contract query to a known UTA and returns native details; unknown account yields null. The specific incompatibility or retained boundary is: Native SDK Contract crosses manager/projection boundary and unknown/null is ambiguous between not found, unavailable and no details. I repaired the entry to the named target: Use ContractDetailsQuery{accountScope,instrumentRef} and ContractDetailsResult=Found(InstrumentDescriptor,ContractDetailsView)|NotFound|Unavailable|ProtocolViolation. The safe view carries canonical identity, venue/asset class and explicit fact variants `Description=Present|Absent`, `LotSize=Known|Unknown`, `TickSize=Known|Unknown`; it never exposes an SDK Contract. Concrete future falsifier: Known query returns decoded details with canonical identity. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-997D4009EE",
      "verdict": "accepted",
      "reason": "UTAManager test fixture 审查 rechecked services/uta/src/domain/trading/uta-manager.spec.ts:251-275: closeAll Promise.allSettled closes all accounts, clears map, swallows close rejection. The observed behavior is: closeAll calls close on every UTA concurrently, clears entries, and intentionally does not throw if one close rejects. The specific incompatibility or retained boundary is: Swallowing cleanup errors hides resource leaks and clears the only registry before durable jobs/leases are classified. It cannot guarantee scheduler quiescence or preserve recovery evidence. I repaired the entry to the named target: Use RootScope ShutdownCoordinator with ordered AdmissionClosed -> ClaimsStopped -> WorkersQuiesced -> JournalDrained -> AccountLayersReleased -> TransportClosed. Return `ShutdownReport{released,failures,unfinishedJobs}`; durable Retired/RecoveryRequired rows remain queryable and cleanup is idempotent. Concrete future falsifier: One broker close rejects: other broker resources release and report contains failure. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-E6F356C3B1",
      "verdict": "accepted",
      "reason": "Independent recheck of services/uta/src/domain/trading/uta-manager.ts:1-21: UTAManager imports broker/native types, factory/config, Git persistence, Alice services, FX and the declaration-only contract extension. The source is a dependency hub, but it does not install a runtime prototype patch. The target correctly cuts the hub into SDK-free application services, durable stores and scoped adapters, while removing the compatibility import path and preserving provider internals behind K01/K02/K07."
    },
    {
      "id": "MAP-B82F61A75F",
      "verdict": "accepted",
      "reason": "UTAManager implementation 审查 rechecked services/uta/src/domain/trading/uta-manager.ts:23-27: imports protocol DTO types and reexports them solely for legacy callers. The observed behavior is: Manager reexports UTASummary, AggregatedEquity and ContractSearchResult from @traderalice/uta-protocol for backward imports. The specific incompatibility or retained boundary is: Type-only compatibility itself is low risk, but its location suggests manager owns protocol schemas and can hide stale DTO assumptions during migration. I repaired the entry to the named target: Keep canonical DTO codecs/types in `@traderalice/uta-protocol`; any migration import is `import type` only and is not a runtime or execution alias. Account/valuation/search views carry explicit scope, capability/valuation outcome, sequence and freshness as required. Concrete future falsifier: No runtime code executes when importing a type-only DTO. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-64E32766E4",
      "verdict": "accepted",
      "reason": "UTAManager implementation 审查 rechecked services/uta/src/domain/trading/uta-manager.ts:31-34: SnapshotHooks has optional raw-string onPostPush/onPostReject callbacks. The observed behavior is: Snapshot hooks accept a string account id and return void/Promise<void>; they fire around Git push/reject. The specific incompatibility or retained boundary is: Callbacks have no transaction/plan revision, command identity, journal sequence or checkpoint; invocation can be repeated or lost and errors are detached from durability. I repaired the entry to the named target: Replace SnapshotHooks with tagged journal events `TransactionCommitted|TransactionRejected{transactionId,accountScope,revision,sourceSequence}` consumed by an idempotent snapshot worker. No callback can mutate manager state or act as a commit/receipt authority. Concrete future falsifier: A repeated journal event produces one snapshot checkpoint. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-6474FE1FB1",
      "verdict": "accepted",
      "reason": "Independent recheck of services/uta/src/domain/trading/uta-manager.ts:36-49: live UTA objects sit in a Map, reconnecting ids in a Set, and optional EventLog/ToolCenter/FxService references are mutable. The repaired pure handle transition is keyed by AccountScope plus connectionGeneration, with durable registry/projection/lease ownership. This preserves an internal lookup optimization without allowing raw AccountId handles to become authority."
    },
    {
      "id": "MAP-09E0F7957B",
      "verdict": "accepted",
      "reason": "UTAManager implementation 审查 rechecked services/uta/src/domain/trading/uta-manager.ts:51-57: mutable setSnapshotHooks/setFxService setters replace manager dependencies after construction. The observed behavior is: Snapshot and FX dependencies can be changed after manager construction; later initUTA reads current fields. The specific incompatibility or retained boundary is: Accounts initialized before/after setter see different services, and no version/freshness guarantees exist. Hidden mutation makes reproducibility and shutdown ownership ambiguous. I repaired the entry to the named target: Acquire immutable application/effect Layers once per RuntimeScope. FX observations and snapshot workers are explicit versioned dependencies; a reconfiguration command starts a new generation and invalidates affected plans, rather than mutating an existing manager. Guardian process restart remains the only reload boundary. Concrete future falsifier: Two accounts use same FX service version for one query. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-81F5FC7879",
      "verdict": "accepted",
      "reason": "UTAManager implementation 审查 rechecked services/uta/src/domain/trading/uta-manager.ts:61-80: initUTA creates broker, loads Git state, constructs UTA with flags/callbacks, adds to map, returns before waitForConnect. The observed behavior is: initUTA resolves a broker from config, loads per-account Git state, creates UTA with guards/keyless/readOnly/asVendor/persistence/health/snapshot callbacks, registers it, and does not await connection. The specific incompatibility or retained boundary is: Initialization makes Git/file state and mutable UTA object authority; startup can advertise account before journal verification/recovery and before connection target reach is known. I repaired the entry to the named target: Bootstrap `AccountRegistration{AccountScope,kind,mode:DataOnly|FundedReadOnly|FundedWritable,configRevision}` only after JournalStore verification/replay and recovery classification. FundedReadOnly registration can expose canonical DraftReview, including a non-executable PreparationPreview analysis when facts are available; keyless is DataOnly; write capability is separately checked before executable Prepare/Approve. Git is an audit projection. Concrete future falsifier: Process dies during bootstrap: restart finds one canonical account row and no duplicate registration. Source leaves this empirical boundary unresolved; the target keeps it unavailable/typed and does not claim support: Legacy Git migration format and whether all accounts have scope metadata require storage migration work. This is a source-grounded design acceptance, not a runtime-test claim. Question closure source/integration audit adopted Catalog/design.md:71-77 and ProtocolModels/design.md:73-85: format and migration semantics are settled, but whether every persisted account has scope metadata remains an empirical inventory prerequisite; missing scope stays LegacyEvidence/OperatorRequired."
    },
    {
      "id": "MAP-C764A73F05",
      "verdict": "accepted",
      "reason": "UTAManager implementation 审查 rechecked services/uta/src/domain/trading/uta-manager.ts:82-123: reconnect uses raw Set, rereads config, removes old before init, waits connect, conditionally registers CCXT tools, stringifies errors. The observed behavior is: reconnectUTA rejects concurrent raw-id reconnect, removes old account, rereads config, creates replacement, awaits connect, registers CCXT tools, logs success; catch returns {success:false,error:string}. The specific incompatibility or retained boundary is: Old account is removed before replacement/recovery classification, so failed reconnect can erase availability. No durable lease/generation fencing, unknown remote outcome handling, or typed failure policy exists. I repaired the entry to the named target: Implement `ReconnectAccount{accountScope,principal}` as a durable generation transition: quiesce the old generation, classify Leased/DispatchStarted/Unknown work, acquire/probe a new scoped Layer, then CAS-replace the generation. No legacy execution alias or ToolCenter side effect is used. Concrete future falsifier: Failed replacement leaves account row and recovery state queryable. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-DBCEAE4A84",
      "verdict": "accepted",
      "reason": "UTAManager implementation 审查 rechecked services/uta/src/domain/trading/uta-manager.ts:125-131: removeUTA deletes map before await close, catches close errors, missing id no-op. The observed behavior is: removeUTA removes the object first, awaits close best-effort, swallows errors, and treats missing id as success. The specific incompatibility or retained boundary is: In-memory deletion hides close failure and can drop runtime access while durable work/locks remain; no typed distinction between Retired, Disabled and NotFound. I repaired the entry to the named target: Use `RetireAccount|DisableAccount` lifecycle commands. Stop admission/claims and classify jobs before releasing the scope; retain the durable row and recovery evidence. Missing canonical identity returns AccountNotFound according to command policy, not a silent map deletion. Concrete future falsifier: Retire with pending unknown job leaves recovery case and no broker dispatch. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-D142657C9D",
      "verdict": "accepted",
      "reason": "UTAManager implementation 审查 rechecked services/uta/src/domain/trading/uta-manager.ts:133-140: registerCcxtToolsIfNeeded inspects brokerEngine and registers ToolCenter tools. The observed behavior is: Manager scans live broker objects for engine==='ccxt' and registers concrete provider tools, logging registration. The specific incompatibility or retained boundary is: Account lifecycle mutates Alice ToolCenter and relies on adapter engine identity, violating Alice-owned agent tools and contextual capability authorization. I repaired the entry to the named target: Publish a contextual `CapabilityManifest{accountScope,operations,revision}` for Alice to render authorized tools. UTA does not inspect broker engine strings or mutate ToolCenter; DataOnly registrations advertise history/discovery only, never mutation operations. Concrete future falsifier: A CCXT account advertises only supported typed tools. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-246FD3AA47",
      "verdict": "accepted",
      "reason": "UTAManager implementation 审查 rechecked services/uta/src/domain/trading/uta-manager.ts:144-153: add stores by raw id and throws generic duplicate Error; remove deletes without closing. The observed behavior is: add/remove are synchronous map mutations independent of broker close or durable lifecycle. The specific incompatibility or retained boundary is: Raw strings permit scope collisions and generic Error messages; remove bypasses resource ownership. I repaired the entry to the named target: Use the same durable AccountLifecycleService as the registration cluster: canonical AccountScope uniqueness, typed duplicate conflict, and scoped resource finalization. A runtime handle can disappear after release without deleting account identity. Concrete future falsifier: Concurrent registration of one AccountScope yields one AccountRegistered and one AccountAlreadyExists receipt. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-C1DA09E298",
      "verdict": "accepted",
      "reason": "Independent recheck of services/uta/src/domain/trading/uta-manager.ts:157-159: get returns a mutable UnifiedTradingAccount for a raw id. The repaired target accepts canonical AccountScope plus Principal and returns AccountView only; commands go through transaction services and native values remain adapter-private. The source therefore supports an authority-boundary change, not a claim that the current getter is safe."
    },
    {
      "id": "MAP-FDF1578687",
      "verdict": "accepted",
      "reason": "UTAManager implementation 审查 rechecked services/uta/src/domain/trading/uta-manager.ts:161-169: listUTAs reads live id/label/asVendor/capabilities/health from each UTA. The observed behavior is: listUTAs projects current in-memory UTA fields and broker capability/health directly into UTASummary. The specific incompatibility or retained boundary is: Live object state is not durable and capability lacks account/subaccount/session context; a status snapshot has no asOf sequence or stale semantics. I repaired the entry to the named target: Build `AccountSummary` from durable projection joining AccountScope/lifecycle, AccountHealth, CapabilitySet and latest sequence/asOf/freshness. `asVendor` is discovery-only metadata; unavailable/degraded reasons are explicit and no in-memory handle is required. Concrete future falsifier: Offline account remains listed with degraded capability. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-CC4FEDA7EF",
      "verdict": "accepted",
      "reason": "Independent recheck of services/uta/src/domain/trading/uta-manager.ts:171-177: has/size report in-memory Map membership/count. The repaired target queries durable registration by explicit AccountScope and reports completeness/sequence, while runtime-handle counts remain diagnostics. Restart and partial-read cases therefore cannot be mistaken for zero configured accounts."
    },
    {
      "id": "MAP-A4D2C6FE82",
      "verdict": "accepted",
      "reason": "Independent recheck of services/uta/src/domain/trading/uta-manager.ts:181-201: no-source resolution returns all live entries, exact source lookup returns one, and generic resolveOne errors include an unreachable multiple branch. The repaired target keeps all-account listing as an explicit read, but canonical AccountScope plus Principal is required for a scoped reference; duplicate-scope corruption or legacy ambiguous input is rejected rather than represented as a runtime ambiguity result."
    },
    {
      "id": "MAP-E7384ED36A",
      "verdict": "accepted",
      "reason": "UTAManager implementation 审查 rechecked services/uta/src/domain/trading/uta-manager.ts:205-221: getAggregatedEquity parallel live getAccount for non-keyless healthy UTAs, nudges unhealthy, nulls exceptions. The observed behavior is: Aggregation filters keyless, calls live account reads for healthy accounts, nudges recovery for unhealthy, and converts errors into info:null. The specific incompatibility or retained boundary is: This read mutates recovery, races account facts, and hides unavailable/error as null. It lacks account scope, projection sequence, FX/valuation provenance and explicit partial-result policy. I repaired the entry to the named target: Aggregate durable valuation projections rather than calling live brokers from a query. Return `ValuationResult=Complete|Incomplete|NoAccounts`; each account carries currency-qualified components, freshness and reason. Recovery nudges are explicit commands, never hidden in reads, and DataOnly accounts are excluded with a reason. Concrete future falsifier: Unhealthy account appears unavailable with reason; healthy peer remains included. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-B5DBB6EB27",
      "verdict": "accepted",
      "reason": "UTAManager implementation 审查 rechecked services/uta/src/domain/trading/uta-manager.ts:223-268: Decimal totals, USD default, FxService conversion/warning, zero rows for null info, string response. The observed behavior is: Aggregation uses Decimal totals; for non-USD it calls FxService four times, adds converted USD values, retains only equity warning, otherwise passes through; missing info becomes zero USD account row. The specific incompatibility or retained boundary is: Base currency fallback and warning string erase currency/provenance; default/stale/unknown FX can enter equity; missing account is indistinguishable from zero and no durable projection/asOf is returned. I repaired the entry to the named target: Use `Money{DecimalString,CurrencyCode}`, `FxObservation{source,observedAt,freshness,uncertainty}` and `ValuationResult=Complete{status:Confirmed|Stale|Estimated,total,accounts}|Incomplete{availableAccounts,missingScopes,reasons}|NoAccounts`. Missing base currency, failed account reads and unknown FX are explicit incompleteness, never implicit USD/zero. Concrete future falsifier: Two USD accounts with valid observations yield the source totals as Complete/Confirmed. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-DC4CB2CF21",
      "verdict": "accepted",
      "reason": "Independent recheck of services/uta/src/domain/trading/uta-manager.ts:272-296: explicit account or asVendor selection calls live search, nudges unhealthy accounts and filters errors to empty results. The repaired target accepts AccountScope or PublicDataScope, separates catalog completeness from per-source outcomes, and removes hidden recovery from reads. It does not infer rejection/cancellation from Partial or Unavailable listing evidence under K02/K05/K08."
    },
    {
      "id": "MAP-BACDB7078D",
      "verdict": "accepted",
      "reason": "UTAManager implementation 审查 rechecked services/uta/src/domain/trading/uta-manager.ts:298-305: raw accountId lookup and native Contract details forwarding/null unknown. The observed behavior is: getContractDetails finds a live UTA by raw id, returns null when absent, and forwards native Contract/ContractDetails. The specific incompatibility or retained boundary is: Raw account and SDK contracts leak, null hides not-found versus no-details/unavailable, and no canonical scope/instrument identity is carried. I repaired the entry to the named target: Use `ContractDetailsQuery{accountScope,instrumentRef}` and `ContractDetailsResult=Found(InstrumentDescriptor,ContractDetailsView)|NotFound|Unavailable|ProtocolViolation`. The public view carries canonical identity, venue/asset class and validated description/lot/tick fields; native SDK objects stay in the adapter. Concrete future falsifier: Known instrument returns safe descriptor with canonical identity. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-A52D6896E9",
      "verdict": "accepted",
      "reason": "UTAManager implementation 审查 rechecked services/uta/src/domain/trading/uta-manager.ts:309-314: closeAll Promise.allSettled closes live UTAs, ignores failures and clears map. The observed behavior is: closeAll closes each UTA concurrently, swallows every rejection, then clears manager entries. The specific incompatibility or retained boundary is: No admission quiescence, job classification, lease handling, journal drain or failure evidence; clearing map can drop recovery authority. I repaired the entry to the named target: Root ShutdownCoordinator owns the same ordered phases and durable state as closeAll: close admission, stop claims, classify unfinished work, drain JournalWriter, release account layers, then close transport/store. Return a typed ShutdownReport; no map clear can erase RecoveryRequired ownership. Concrete future falsifier: Failed broker close leaves report plus durable account/job state. Source leaves this empirical boundary unresolved; the target keeps it unavailable/typed and does not claim support: The host supervisor chooses final process exit status/restart policy; the runtime must return typed ShutdownReport/Exit evidence and never classify Unknown as rejection. This is a source-grounded design acceptance, not a runtime-test claim. Question closure adopted Supervisor/design.md:83-102 and C14: runtime returns typed ShutdownReport/Exit and request-correlated RestartOutcome; host alone maps final process status, with Unknown non-success."
    },
    {
      "id": "MAP-80C471F58F",
      "verdict": "accepted",
      "reason": "UTA composition-root 审查 rechecked services/uta/src/main.ts:1-11: module comment says co-located v1 owns broker/Git approval/snapshots/FX, loopback, Guardian SIGTERM+respawn reload. The observed behavior is: Module documentation assigns broker, Git-like approval, snapshots and FX ownership to main process; config reload is external Guardian SIGTERM/respawn, with no in-process hot reload. The specific incompatibility or retained boundary is: The comment mixes deployment restart with domain execution authority and omits SQLite schema/journal validation, recovery classification, scoped capabilities and startup admission barriers. I repaired the entry to the named target: Main is composition root only: RootScope wires ConfigLayer, JournalStore, ProjectionStore, StartupRecoveryCoordinator, BrokerLayerSet=NoCarriers|Scoped(carrierLayers), MarketLayerSet=NoProviders|Scoped(providerLayers), DurableScheduler and authenticated transport. A zero-carrier/lite startup is valid after journal/projection recovery; Guardian SIGTERM/respawn remains the outer reload boundary and never replaces recovery classification. Concrete future falsifier: Start with no configured broker accounts: process reaches canonical RuntimeHealth.Ready, serves lite/read-only queries, and acquires no broker carrier. Source leaves this empirical boundary unresolved; the target keeps it unavailable/typed and does not claim support: Guardian’s exact restart handshake is not shown in this source; it must consume canonical RuntimeHealth and typed shutdown evidence rather than infer readiness from process liveness. This is a source-grounded design acceptance, not a runtime-test claim. Question closure adopted Supervisor/design.md:27-28,39-63,83-102 and C14: runtime.restart-uta uses existing Guardian protocol, same requestId, new bootId plus RuntimeHealth.Ready; process liveness alone is not proof."
    },
    {
      "id": "MAP-4F1B3065A3",
      "verdict": "accepted",
      "reason": "UTA composition-root 审查 rechecked services/uta/src/main.ts:13-18: imports Hono/serve, broad config helpers, duration parser, JSONL EventLog and ToolCenter. The observed behavior is: Main directly imports transport primitives, broad Alice config, duration parser, file-backed EventLog factory and ToolCenter. The specific incompatibility or retained boundary is: Direct construction pulls file JSONL and Alice agent registry into UTA execution and prevents JournalStore/ProjectionStore/Effect Layers from being the sole authority. I repaired the entry to the named target: Import only typed ConfigLayer, RootScope/effect-runtime, JournalStore/ProjectionStore, DurableScheduler, broker/market adapter Layers and authenticated transport constructors. EventLog/Git exporters, if retained, subscribe after committed journal events and cannot write execution state; Alice ToolCenter remains outside UTA. Concrete future falsifier: Removing JSONL path does not lose journal events; protocol queries remain available after restart. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-071F6FB24A",
      "verdict": "accepted",
      "reason": "Independent recheck of services/uta/src/main.ts:19-33: Main constructs concrete UTAManager, snapshot/FX SDK resources, credentials, order poller and keyless config. The repaired composition target keeps SDK/credentials in scoped adapters and lets adapters bind either AccountScope or PublicDataScope; keyless data does not become a fake account. Scheduler, journal and transport are named layers rather than new manager ownership."
    },
    {
      "id": "MAP-7D7ABA2368",
      "verdict": "accepted",
      "reason": "Independent recheck of services/uta/src/main.ts:34-36: legacy trading and simulator routes receive concrete UTAEngineContext. The repaired target adds DataQueryService to TransportDependencies alongside Auth, account/transaction/capability/projection/health ports; explicit public-data requests never enter transaction preparation. Simulator remains a separately enabled authenticated layer, and loopback/legacy aliceId cannot authorize commands."
    },
    {
      "id": "MAP-77923C5EA9",
      "verdict": "accepted",
      "reason": "UTA composition-root 审查 rechecked services/uta/src/main.ts:38-38: UTA_PORT uses Number(process.env.OPENALICE_UTA_PORT ?? 47333) at module evaluation. The observed behavior is: Port is parsed eagerly from env with Number and defaults to 47333; malformed/unsafe values are not represented as typed failure. The specific incompatibility or retained boundary is: Module evaluation captures configuration before scoped ConfigLayer and may accept NaN, fractional, negative or out-of-range ports. I repaired the entry to the named target: Decode `OPENALICE_UTA_PORT` at runMain into `HttpBindConfig{host:Loopback,port:Port}` with integer range 1..65535; default is 47333. Invalid config fails before bind, and port parsing is not performed at module evaluation. Concrete future falsifier: OPENALICE_UTA_PORT=abc fails typed startup before binding. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-C9D53377C0",
      "verdict": "accepted",
      "reason": "UTA composition-root 审查 rechecked services/uta/src/main.ts:39-39: CATALOG_REFRESH_MS is a process-global six-hour constant. The observed behavior is: A six-hour raw interval is defined in main and later used by setInterval over all live UTAs. The specific incompatibility or retained boundary is: Cadence has no typed Schedule, per-account capability, durable identity, backpressure or Scope ownership; errors are log-only. I repaired the entry to the named target: Use versioned `CatalogRefreshSchedule{interval:Duration,policyVersion}` and scoped supervised workers per eligible AccountScope/venue. A source-compatible default interval is 6 hours; unsupported or incomplete namespace evidence yields CatalogUnavailable/Partial, not an empty catalog. Concrete future falsifier: A disabled adapter has no refresh fiber. Source leaves this empirical boundary unresolved; the target keeps it unavailable/typed and does not claim support: Per-venue namespace coverage is not established by this composition source; until C07 coverage evidence is declared, listing remains Partial/Unavailable and absence is non-authoritative. This is a source-grounded design acceptance, not a runtime-test claim. Question closure adopted C07 and CcxtVenues/design.md:84-112,142-147: catalog refresh uses adapter-declared namespace coverage and emits Partial/Unavailable until complete; no policy question remains."
    },
    {
      "id": "MAP-4030819860",
      "verdict": "accepted",
      "reason": "UTA composition-root 审查 rechecked services/uta/src/main.ts:41-44: startUTAService records startedAt, logs bootstrap and performs imperative construction. The observed behavior is: startUTAService creates ISO timestamp/log then executes config, managers, accounts, services, routes and server sequentially in an async function. The specific incompatibility or retained boundary is: There is no typed effect root or startup phase; logging bootstrap can precede failed schema/config/recovery, and server construction can race unfinished work. I repaired the entry to the named target: Implement runMain through canonical RuntimeHealth phases: Initializing -> Recovering{verifiedJournal,scanProgress} -> Ready{readiness:{schemaVersion,recoveryScanSequence,admissionEpoch}}; shutdown transitions to Stopping and root defects to Failed. startedAt is diagnostic only. BrokerLayerSet=NoCarriers is valid: zero configured carriers still reaches Ready after journal/projection barriers, while a failed scoped account is Degraded. Concrete future falsifier: HTTP bind observed before/after barrier follows explicit decision; if bound early, mutating routes return NotReady. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-FC89E49A77",
      "verdict": "accepted",
      "reason": "Independent recheck of services/uta/src/main.ts:45-54: proxy precedence is selected from environment names and URL userinfo is regex-redacted before logging. The repaired target parses a typed proxy config and sealed CredentialRef inside adapter layers, fails closed on malformed forms, and preserves the full scoped AccountScope across credential rotation. No proxy secret enters plans, digests, journals or diagnostics."
    },
    {
      "id": "MAP-756E9DC555",
      "verdict": "accepted",
      "reason": "UTA composition-root 审查 rechecked services/uta/src/main.ts:56-56: startUTAService awaits broad loadConfig(). The observed behavior is: Root loads all OpenAlice config before constructing UTA dependencies. The specific incompatibility or retained boundary is: Broad config call couples UTA startup to unrelated Alice state and does not expose typed UTA DB path, account scopes, credential refs or schedule requirements. I repaired the entry to the named target: Keep broad config as an input boundary, then decode once to a closed `UtaConfig{journalPath,httpBind,accountRegistrations,marketProviders,schedules,auth,mode}`. ConfigLayer owns file/env IO and validation; internal modules receive refined records and sealed CredentialRefs, never raw config blobs. Concrete future falsifier: Malformed port/schedule fails before bind. Source leaves this empirical boundary unresolved; the target keeps it unavailable/typed and does not claim support: The shipped config decoder module is not shown in this source; if its schema cannot decode a field, fail closed with ConfigSchemaFailure rather than accepting an untyped value or fallback. This is a source-grounded design acceptance, not a runtime-test claim. Question closure adopted Catalog/design.md:57-61,101-115 and ProtocolModels/design.md:33-35: per-engine codecs decode unknown input once and return ConfigSchemaFailure/PackLoadFailure; no untyped fallback is admitted."
    },
    {
      "id": "MAP-2C295F499B",
      "verdict": "accepted",
      "reason": "UTA composition-root 审查 rechecked services/uta/src/main.ts:58-66: constructs JSONL EventLog, ToolCenter and UTAManager with those dependencies. The observed behavior is: Root creates file-backed EventLog, ToolCenter and manager, making them in-process owners of execution/lifecycle. The specific incompatibility or retained boundary is: These are precisely the old authorities that bypass SQLite writer, Alice-owned tools, durable scheduler and protocol/application boundaries. I repaired the entry to the named target: Construct RootScope Layers for JournalStore, ProjectionStore, TransactionApplication, DurableScheduler, broker/market adapters and authenticated transport. Any audit EventLog importer runs only as a post-commit projection; no JSONL/in-memory fallback can become execution authority, and a zero-carrier runtime remains valid. Concrete future falsifier: Process crash after commit preserves receipt/event/job. Source leaves this empirical boundary unresolved; the target keeps it unavailable/typed and does not claim support: Whether historical EventLog contents are imported is not established here; if imported, classify them as read-only audit history with no transaction/dispatch identity or executable recovery semantics. This is a source-grounded design acceptance, not a runtime-test claim. Question closure adopted RuntimeComposition/design.md:98-114 and ProtocolModels/design.md:73-85: historical EventLog may only be consumed as post-commit audit projection; no imported row can supply transaction, dispatch, or recovery authority."
    },
    {
      "id": "MAP-CDD00B4C1B",
      "verdict": "accepted",
      "reason": "UTA composition-root 审查 rechecked services/uta/src/main.ts:69-75: reads config, purges ephemeral UTA files, builds ids/keyless UTAs before recovery. The observed behavior is: Root purges file-backed ephemeral accounts, derives survivor ids, builds keyless configs and logs them before account initialization/recovery. The specific incompatibility or retained boundary is: File deletion and account creation occur before SQLite journal verification/recovery; a startup failure can destroy legacy evidence or create capabilities before classification. I repaired the entry to the named target: After JournalStore verification/replay and recovery classification, reconcile config registrations. `ephemeral=true` retains source semantics only for mock-simulator: if no active/unknown work, writer records Retired(NoEffectsApplied) and permits wiping its test projection/data; if active work exists, retain it under RecoveryCase and do not purge. Keyless registrations follow durable Retired reconciliation and never become funded accounts. Concrete future falsifier: Active account with unfinished work is never purged. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-C8D7A0F889",
      "verdict": "accepted",
      "reason": "UTA composition-root 审查 rechecked services/uta/src/main.ts:76-87: sequentially initializes enabled accounts, catches/logs each init error so one failure does not abort. The observed behavior is: Root loops dataUTAs then survivors sequentially; catches each init error and continues bootstrap. The specific incompatibility or retained boundary is: Isolation depends on try/catch and legacy async connect; errors flatten to warning and startup can continue before journal/recovery gates. I repaired the entry to the named target: Preserve per-account isolation with supervised scoped fibers and typed AccountHealth. Startup opens canonical RuntimeHealth.Ready after journal/recovery barrier and all configured registration decisions; with zero registrations the account-classification set is empty and no broker layer is acquired. One account’s broker failure remains scoped Degraded/Unavailable. Concrete future falsifier: Failed IBKR account + healthy simulator: process readiness barrier completes, healthy account query works, failed account is degraded. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-EE73F2F7D2",
      "verdict": "accepted",
      "reason": "Independent recheck of services/uta/src/main.ts:88: root calls registerCcxtToolsIfNeeded after account initialization. The repaired target publishes provider-declared CapabilityDescriptor leaves to Alice; an undeclared capability has no tool, while a declared-but-unavailable leaf carries availability. UTA does not mutate ToolCenter, and public-data history/discovery remains outside the controlled-effect namespace."
    },
    {
      "id": "MAP-B0317A6228",
      "verdict": "accepted",
      "reason": "Independent recheck of services/uta/src/main.ts:90-100: Main builds SDK currency/credential resources, constructs FxService and mutates the manager with setFxService. The repaired target uses a declared-provider FxObservation layer with sealed CredentialRef and leaves AccountScope identity unchanged across credential rotation; missing provider/binding is FxUnavailable/Incompleteness, never a default rate or plaintext prompt."
    },
    {
      "id": "MAP-58B5F119B7",
      "verdict": "accepted",
      "reason": "UTA composition-root 审查 rechecked services/uta/src/main.ts:102-108: creates SnapshotService over manager/EventLog and hooks post-push/post-reject to takeSnapshot. The observed behavior is: Root creates file-backed snapshot service and installs callbacks invoked after legacy push/reject. The specific incompatibility or retained boundary is: Snapshot trigger is tied to Git callbacks and file EventLog/manager; callback failure/repetition is not tied to committed journal sequence and could run before durable authority. I repaired the entry to the named target: Committed journal events emit durable `SnapshotRequest{accountScope,sourceSequence,trigger}`; a lossless subscriber enqueues idempotent leased projection jobs and records checkpoint/asOf/freshness. Git callbacks are removed from execution authority. Concrete future falsifier: Commit event then process crash: request replays and one snapshot job results. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-E09A9CD997",
      "verdict": "accepted",
      "reason": "UTA composition-root 审查 rechecked services/uta/src/main.ts:110-114: creates Pump-backed snapshot scheduler, starts before HTTP bind and logs interval. The observed behavior is: Snapshot scheduler starts before HTTP server binding and periodically invokes snapshot service when enabled. The specific incompatibility or retained boundary is: The Pump has no durable cursor/lease and can run before journal/recovery; startup log says scheduler started without proving projection/recovery readiness. I repaired the entry to the named target: Run snapshot scheduling as a supervised scoped Fiber after startup recovery. Transaction-triggered SnapshotRequest jobs are durable/non-droppable; periodic telemetry snapshots may coalesce. SQLite checkpoints/retention metadata survive restart, and initial cutover performs no automatic history deletion. Concrete future falsifier: Process crash before snapshot: request remains durable and runs once after restart. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-DFDC1D0116",
      "verdict": "accepted",
      "reason": "UTA composition-root 审查 rechecked services/uta/src/main.ts:116-128: starts raw order-sync poller with pending and external lanes, parseDuration fallback, no retained stop handle. The observed behavior is: Root starts a 10s pending lane and configurable external-order observation lane over manager-resolved UTAs; malformed duration falls back to 15m and returned stop handle is discarded. The specific incompatibility or retained boundary is: Poller scans in-memory/Git pending IDs, has no durable job identity/lease/DispatchStarted/Unknown state, and interval fallback can hide configuration errors. Unknown remote outcomes can lead to blind repeats. I repaired the entry to the named target: Replace the two raw poller lanes with typed DurableScheduler jobs `ObservePending|ObserveExternal|Reconcile`, each carrying AccountScope, DispatchId/native lookup descriptor, lease owner/epoch/deadline and policy version. Unknown pauses forward work; Complete/Partial/Unavailable listing evidence controls absence conclusions. Concrete future falsifier: Worker crash after DispatchStarted creates Unknown and only observe next. Source leaves this empirical boundary unresolved; the target keeps it unavailable/typed and does not claim support: Venue-specific idempotency/lookup guarantees are not established here; without them, a post-DispatchStarted timeout remains Unknown/ObserveRequired and is never blindly redispatched. This is a source-grounded design acceptance, not a runtime-test claim. Question closure adopted C07/C11 and IbkrBridge/design.md:117-125,243-246: durable lookup/observe is fixed, but venue idempotency and lookup guarantees remain empirical; timeout after DispatchStarted stays Unknown."
    },
    {
      "id": "MAP-C762FC3F5D",
      "verdict": "accepted",
      "reason": "UTA composition-root 审查 rechecked services/uta/src/main.ts:130-141: global setInterval every 6h loops all UTAs and refreshCatalog, logs errors, unrefs timer. The observed behavior is: A detached global timer iterates manager accounts and calls refreshCatalog; per-account errors are console warnings; timer handle is only locally available. The specific incompatibility or retained boundary is: Timer is not supervised/cancellable by root Scope, has no schedule identity/backpressure and directly reaches legacy accounts. I repaired the entry to the named target: Use `CatalogRefreshSchedule{interval:6h,policyVersion}` with a supervised scoped Fiber and bounded per-account concurrency. Refresh emits typed CatalogObservation or Unavailable/Partial evidence and retains a durable checkpoint where needed for C07 coverage; shutdown interrupts and awaits the Fiber. Concrete future falsifier: SIGTERM waits until refresh Fiber stops or reports timeout. Source leaves this empirical boundary unresolved; the target keeps it unavailable/typed and does not claim support: Per-venue catalog namespace requirements are not established in this source; a refresh cannot claim ListingComplete until adapter coverage evidence is present. This is a source-grounded design acceptance, not a runtime-test claim. Question closure adopted C07 and CcxtVenues/design.md:84-112,142-147: schedule and completeness contract are fixed; adapter evidence is required before Complete, otherwise Partial/Unavailable."
    },
    {
      "id": "MAP-F840826CE5",
      "verdict": "accepted",
      "reason": "UTA composition-root 审查 rechecked services/uta/src/main.ts:143-152: Hono health route returns ok:true, startedAt and manager list count. The observed behavior is: Health endpoint always reports ok=true with boot timestamp and number of registered legacy UTAs. The specific incompatibility or retained boundary is: It cannot distinguish HTTP bind from journal validity/recovery barrier, account degraded/offline, scheduler health or listing completeness; count of in-memory accounts is not readiness. I repaired the entry to the named target: Expose only canonical `RuntimeHealth = Initializing{bootId,stage} | Recovering{bootId,verifiedJournal,scanProgress} | Ready{bootId,readiness:{schemaVersion,recoveryScanSequence,admissionEpoch}} | Stopping{bootId,shutdownStage} | Failed{bootId,failedStage,redactedCause}`. AccountHealth/CapabilitySet and listing completeness are separate projections; no `ok` flag, account count or independent admission/recovery fields authorize execution. Concrete future falsifier: One broker handshake fails: global journal readiness true, failed account degraded, healthy account readable. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-1C9449F63F",
      "verdict": "accepted",
      "reason": "UTA composition-root 审查 rechecked services/uta/src/main.ts:154-163: builds UTAEngineContext with manager/FxService/SnapshotService and mounts trading route. The observed behavior is: Root injects concrete manager, mutable FX service and snapshot service into trading routes under /api/trading. The specific incompatibility or retained boundary is: Context exposes execution/broker/market objects directly, bypasses actor auth, schema codecs, transaction commands and projection freshness. I repaired the entry to the named target: Inject TransportDependencies{Auth,TransactionService,AccountQueryService,CapabilityService,ProjectionReader,RuntimeHealthReader}. Routes decode schemas and submit Create/Stage/Prepare/Approve/Reject/Recover commands; FundedReadOnly/DataOnly commands can return DraftReview or typed refusal but never approval/dispatch, while all writes use JournalWriter receipts and canonical AccountScope/revision/digest bind approval. Concrete future falsifier: Duplicate command id/same digest returns same receipt after response loss. Source leaves this empirical boundary unresolved; the target keeps it unavailable/typed and does not claim support: The concrete Alice auth middleware is not shown in this source; it must produce a typed Principal/AuthContext before any route reaches application services. This is a source-grounded design acceptance, not a runtime-test claim. Question closure adopted HttpRoutes/design.md:9-15 and 56-62: Auth produces typed Principal/AuthContext and routes call application ports only; unauthenticated commands are rejected before app."
    },
    {
      "id": "MAP-2810B2FE69",
      "verdict": "accepted",
      "reason": "UTA composition-root 审查 rechecked services/uta/src/main.ts:164-168: mounts MockBroker simulator routes with same raw context/direct instances. The observed behavior is: Root mounts simulator endpoints in deployed service and gives routes direct manager access to MockBroker instances. The specific incompatibility or retained boundary is: No auth/authorization/deployment gate; simulator is a second mutation/state authority and can bypass transaction journal. I repaired the entry to the named target: Compose simulator routes only under explicit `RuntimeMode=Development|Test` and `SimulatorAdmin` capability. Requests are authenticated, scoped and sent through the same journal/application contract; production composition has no simulator path and no direct MockBroker state route. Concrete future falsifier: Production route table has no simulator paths. Source leaves this empirical boundary unresolved; the target keeps it unavailable/typed and does not claim support: The deployment mode source/admin principal is not shown here; absent an explicit Development/Test mode and authorization capability, simulator routes remain Disabled/NotFound. This is a source-grounded design acceptance, not a runtime-test claim. Question closure adopted HttpRoutes/design.md:13-15,76-80: RuntimeMode Development/Test and SimulatorAdmin are explicit gates; default/production composition has no simulator path, so no concrete admin identity is needed in this module."
    },
    {
      "id": "MAP-529FB3A498",
      "verdict": "accepted",
      "reason": "UTA composition-root 审查 rechecked services/uta/src/main.ts:170-177: serve binds Hono to hostname 127.0.0.1 and UTA_PORT, logs URL. The observed behavior is: HTTP server binds loopback and configured port then logs listening URL. The specific incompatibility or retained boundary is: Loopback limits network exposure but does not provide authentication/authorization or ensure journal/recovery readiness; log URL alone is not readiness evidence. I repaired the entry to the named target: Keep loopback-only `HttpBindConfig` with typed Port and a scoped server resource. Bind may occur before recovery solely for health/NotReady responses; all mutation/query routes requiring authority reject until canonical RuntimeHealth.Ready and authentication. Concrete future falsifier: Remote socket cannot connect when host is loopback. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-F0D5CAEC8A",
      "verdict": "accepted",
      "reason": "UTA composition-root 审查 rechecked services/uta/src/main.ts:179-193: SIGINT/SIGTERM idempotent shutdown clears timer/stops snapshots/closes server/UTAs/EventLog swallowing errors and process.exit(0). The observed behavior is: Shutdown guard prevents duplicate calls, clears catalog timer, stops snapshot scheduler, closes server/accounts/EventLog with swallowed errors and force exits zero. The specific incompatibility or retained boundary is: No Effect Scope/worker quiescence/journal drain/lease classification; swallowed errors and process.exit can lose durable evidence or let remote-unknown work be treated as clean. I repaired the entry to the named target: Use ShutdownCoordinator phases `AdmissionClosed -> ClaimsStopped -> WorkersQuiesced -> InterruptedWorkClassified -> JournalDrained -> LayersReleased -> TransportClosed`; return typed ShutdownReport and let host map it to process exit. No direct process.exit may precede durable classification/drain. Concrete future falsifier: SIGTERM during leased dispatch leaves job RecoveryRequired and broker observation path. Source leaves this empirical boundary unresolved; the target keeps it unavailable/typed and does not claim support: Host supervisor exit/restart mapping is not shown in this source; it must consume typed shutdown outcome and must not convert Unknown to rejection or clean success. This is a source-grounded design acceptance, not a runtime-test claim. Question closure adopted Supervisor/design.md:83-102 and integration-decisions.md:105-111: host maps typed shutdown/restart outcomes; runtime never maps Unknown to clean/rejected."
    },
    {
      "id": "MAP-62BFB82A86",
      "verdict": "accepted",
      "reason": "UTA composition-root 审查 rechecked services/uta/src/main.ts:195-200: internal dispatch marker skips auto-start; otherwise startUTAService catch logs fatal and process.exit(1). The observed behavior is: Module checks global role-dispatch marker; in normal mode starts service and maps any rejection to console fatal + exit code 1. The specific incompatibility or retained boundary is: Catch flattens expected config/storage/connection failures and defects, losing structured startup evidence and runtime supervision policy. I repaired the entry to the named target: Retain the internal role-dispatch guard, but invoke `runMain` and map exhaustive `Exit` variants: expected Config/Storage/Recovery failures preserve phase evidence and a nonzero typed status; defects follow redacted runtime fatal policy. No catch-all Error.message is the protocol. Concrete future falsifier: Config failure returns typed code and preserves evidence. Source leaves this empirical boundary unresolved; the target keeps it unavailable/typed and does not claim support: The host supervisor’s restart/backoff policy is outside this module; runtime still returns structured Exit and preserves durable evidence before termination. This is a source-grounded design acceptance, not a runtime-test claim. Question closure adopted Supervisor/design.md:100-102 and 129-133: host-owned restart/backoff is intentionally outside runtime; runMain returns structured Exit/evidence before host termination or restart."
    },
    {
      "id": "MAP-3E7F87BDF4",
      "verdict": "accepted",
      "reason": "transport context type 审查 rechecked services/uta/src/types.ts:1-14: comments distinguish Alice EngineContext async SDK from UTA synchronous manager and limit route fields to three. The observed behavior is: The module deliberately separates process context names and documents that UTA routes only need manager, FX and snapshots, avoiding a cast to Alice EngineContext. The specific incompatibility or retained boundary is: The narrow-context principle is sound, but its three fields are still concrete legacy authorities and synchronous manager assumptions; it does not encode auth, capability, transaction or projection boundaries. I repaired the entry to the named target: Retain the process-boundary distinction but define `TransportDependencies{Auth,TransactionService,AccountQueryService,CapabilityService,ProjectionReader,HealthReader}` with typed Principal, AccountScope and protocol failures. Alice SDK convenience remains outside UTA; no manager, broker, FX or snapshot implementation is exposed, and funded readOnly can return canonical DraftReview with a non-executable PreparationPreview analysis but no execution authority. Concrete future falsifier: A route can issue Prepare but cannot access broker SDK or Git. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-6C6CAA25D4",
      "verdict": "accepted",
      "reason": "Independent recheck of services/uta/src/types.ts:16-18: the type module imports concrete UTAManager, FxService and SnapshotService types. The repaired SDK-free contract set includes DataQueryService as well as account/transaction/capability/projection/health and market views; concrete implementations remain composition-private. The boundary is type-only and cannot carry a native broker object."
    },
    {
      "id": "MAP-0FC5D42419",
      "verdict": "accepted",
      "reason": "Independent recheck of services/uta/src/types.ts:20-30: UTAEngineContext requires manager/FX and optionally carries snapshots, with route guards that can return empty/503. The repaired TransportDependencies includes DataQueryService and typed auth/account/public scope; SnapshotAvailability is explicit rather than an empty-array fallback, and no execution-capable manager crosses the route boundary."
    },
    {
      "id": "MAP-780C76C803",
      "verdict": "accepted",
      "reason": "startup resilience harness 审查 rechecked services/uta/src/uta-startup-resilience.spec.ts:1-7: imports child_process/fs/net/os/path and Vitest for real TypeScript child/HTTP observation. The observed behavior is: Spec uses process, filesystem and TCP primitives to launch actual main.ts and inspect HTTP resilience. The specific incompatibility or retained boundary is: Real process/HTTP evidence is valuable but coverage stops before SQLite migration, durable jobs/leases, crash cuts and recovery outcomes. I repaired the entry to the named target: Retain OS child/TCP/filesystem primitives for process-boundary evidence, but decode canonical RuntimeHealth/AccountHealth and inspect a durable SQLite journal plus independent broker ledger in a separate vertical harness. Zero-carrier startup remains a valid case; raw child output is diagnostics only. Concrete future falsifier: Child binds and returns typed readiness. Source leaves this empirical boundary unresolved; the target keeps it unavailable/typed and does not claim support: The independent durable broker-ledger fixture is not present in this spec; until added, this harness cannot claim DispatchStarted/Unknown recovery proof and must report that evidence as unavailable. This is a source-grounded design acceptance, not a runtime-test claim. Question closure preserves C10 evidence boundaries and MockBroker/design.md:207-218: the current child harness lacks an independent ledger/SQLite crash vertical, so DispatchStarted/Unknown proof remains explicitly unavailable."
    },
    {
      "id": "MAP-29B9B4702C",
      "verdict": "accepted",
      "reason": "startup resilience harness 审查 rechecked services/uta/src/uta-startup-resilience.spec.ts:9-14: startup timeout constants allow 30s readiness plus 10s cleanup. The observed behavior is: Test gives 30 seconds for accounts to become observable and ten seconds cleanup margin, citing concurrent-suite/Windows variance. The specific incompatibility or retained boundary is: One coarse deadline cannot distinguish binding, journal recovery, projection rebuild, broker handshake, or an expired lease; timeout failure loses phase diagnosis. I repaired the entry to the named target: Keep bounded test deadlines, but poll canonical `RuntimeHealth` variants and stage/readiness payloads rather than account count. Use per-phase test deadlines; an expired lease is an evidence input to recovery classification, never a direct retry decision. Concrete future falsifier: Server responds but phase=Recovering -> test continues/fails by policy, not success. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-ECA3909911",
      "verdict": "accepted",
      "reason": "startup resilience harness 审查 rechecked services/uta/src/uta-startup-resilience.spec.ts:16-27: listen helper binds ephemeral loopback TCP, rejects errors/missing/non-TCP address, resolves port. The observed behavior is: Fixture listen handles TCP bind/error/address validation and returns ephemeral loopback port. The specific incompatibility or retained boundary is: This only validates test networking; it says nothing about authenticated UTA transport, database authority or readiness barrier. I repaired the entry to the named target: Retain `listen` as an isolated loopback TCP fixture helper. Pair its allocated Port with authenticated UTA protocol tests and a journal-backed runtime; loopback is transport isolation only, not authorization or readiness. Concrete future falsifier: Non-TCP address rejects. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-8B4B00BB08",
      "verdict": "accepted",
      "reason": "startup resilience harness 审查 rechecked services/uta/src/uta-startup-resilience.spec.ts:30-32: closeServer resolves after node Server close callback. The observed behavior is: closeServer wraps callback-based close in a Promise for deterministic fixture cleanup. The specific incompatibility or retained boundary is: Cleanup proves test server close only, not production scope/fiber shutdown or durable flush. I repaired the entry to the named target: Keep test helper. Target runtime shutdown tests must separately assert admission closure, worker quiescence, journal drain, and resource finalizers; do not treat helper completion as shutdown proof. Concrete future falsifier: Closing a fixture server after its callback resolves completes exactly once; a callback that never resolves fails with bounded CleanupFailure and leaves evidence available. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-69F213D397",
      "verdict": "accepted",
      "reason": "startup resilience harness 审查 rechecked services/uta/src/uta-startup-resilience.spec.ts:34-36: delay returns timer Promise used to poll startup. The observed behavior is: delay resolves after a timer and prevents busy polling in test loop. The specific incompatibility or retained boundary is: Test timer has no relation to production scheduler, lease deadlines or recovery; arbitrary sleep can miss phase transitions. I repaired the entry to the named target: Keep as test polling utility but poll typed state with a deadline. Production uses injected Clock/Effect Schedule and durable jobs; tests should not assert timing as remote outcome. Concrete future falsifier: A child that answers Recovering continues polling until its phase deadline; a typed Failed response stops immediately with phase evidence. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-14BB289E6A",
      "verdict": "accepted",
      "reason": "startup resilience harness 审查 rechecked services/uta/src/uta-startup-resilience.spec.ts:38-47: stopChild sends SIGTERM, waits 3s, escalates SIGKILL and waits exit. The observed behavior is: Test cleanup tries graceful SIGTERM, then SIGKILL after three seconds, and waits for process exit. The specific incompatibility or retained boundary is: This only ensures cleanup; it does not test abrupt crash durability, unknown dispatch outcomes or restart classification. SIGKILL intentionally skips finalizers. I repaired the entry to the named target: Retain `stopChild` as bounded cleanup, and add a separate crash harness that kills at each durable boundary, preserves SQLite/journal/broker-ledger artifacts, restarts and classifies state. SIGKILL is unclean death, never successful shutdown. Concrete future falsifier: SIGKILL after DispatchStarted never sends blind retry on restart. Source leaves this empirical boundary unresolved; the target keeps it unavailable/typed and does not claim support: The independent broker-ledger fixture and exact crash-cut injection are not present in this source; without them, this helper cannot prove remote idempotency and must remain cleanup-only. This is a source-grounded design acceptance, not a runtime-test claim. Question closure preserves the cleanup/crash distinction from RuntimeComposition/design.md:130-141 and MockBroker/design.md:207-218: stopChild cannot prove remote idempotency; independent crash cuts/ledger remain required."
    },
    {
      "id": "MAP-42AE8CC4D2",
      "verdict": "accepted",
      "reason": "Independent recheck of services/uta/src/uta-startup-resilience.spec.ts:49-158: fake TWS closes during handshake while a simulator account remains readable and the child stays alive. The repaired target scopes HandshakeClosed by account/generation as ConnectionUnavailable, keeps journal readiness global, and forbids mutation/retry inference from liveness. Native handshake/lookup guarantees remain adapter evidence obligations."
    },
    {
      "id": "MAP-609A59F711",
      "verdict": "accepted",
      "reason": "startup resilience harness 审查 rechecked services/uta/src/uta-startup-resilience.spec.ts:58-87: reserves ports, creates fake TWS, writes failing IBKR + healthy simulator read-only configs, disables snapshots/external observation. The observed behavior is: Test writes legacy accounts.json/trading/snapshot config in temporary home: one failing IBKR read-only account, one healthy simulator, snapshots and external observation off. The specific incompatibility or retained boundary is: Fixture has no SQLite schema/journal/lease/credential-sealing checks and read-only accounts avoid transaction path; legacy JSON is not target authority. I repaired the entry to the named target: Retain the two-account isolation fixture but migrate its configuration to sealed target config plus temporary SQLite. Both listed accounts remain readOnly: they can exercise DraftReview/query and scoped capability degradation, not executable approval. Use a separate write-capable MockBroker fixture only when testing durable Prepared/Unknown classification. Concrete future falsifier: The failed IBKR account remains Degraded while the simulator is readable and the process is globally Ready after journal recovery. Source leaves this empirical boundary unresolved; the target keeps it unavailable/typed and does not claim support: The sealed-config fixture and CredentialRef test helper are not present here; missing helpers must fail closed without plaintext credentials. This is a source-grounded design acceptance, not a runtime-test claim. Question closure reviewed HttpRoutes/design.md:13-15 and ProtocolModels/design.md:17-23: target fixtures must use sealed refs and redacted diagnostics; missing fixture evidence remains an explicit fail-closed test gap."
    },
    {
      "id": "MAP-FADF0D8D68",
      "verdict": "accepted",
      "reason": "UTA composition-root 审查 rechecked services/uta/src/uta-startup-resilience.spec.ts:89-103: spawns main.ts via tsx with isolated OPENALICE_HOME/port, captures stdout/stderr. The observed behavior is: Test launches TypeScript main child with source conditions, isolated home and env port, concatenating stdout/stderr. The specific incompatibility or retained boundary is: Raw text capture cannot prove typed observability, journal state, scheduler/recovery evidence or secret redaction; combined streams lose severity/phase structure. I repaired the entry to the named target: Retain child launch and combined stdout/stderr capture as diagnostics, but target assertions decode RuntimeHealth/AccountHealth and inspect SQLite/journal/ledger. Structured diagnostic events are redacted; raw text never serves as execution or readiness authority. Concrete future falsifier: Startup phase event sequence is valid and redacted. Source leaves this empirical boundary unresolved; the target keeps it unavailable/typed and does not claim support: The structured observability envelope is not defined in this source; until it exists, raw output may diagnose only and cannot satisfy secret-redaction or phase-evidence acceptance. This is a source-grounded design acceptance, not a runtime-test claim. Question closure adopted TargetGaps/design.md:368-380 with Supervisor/design.md:53-63 and ProtocolModels/design.md:17-23: ViewEnvelope/ExecutionContext/named redaction schema are settled; this startup spec still lacks runtime emission/conformance, so raw output remains diagnostics only."
    },
    {
      "id": "MAP-7AFA062D0F",
      "verdict": "accepted",
      "reason": "startup resilience harness 审查 rechecked services/uta/src/uta-startup-resilience.spec.ts:104-145: polls health/listing until two accounts and legacy degraded/healthy fields; retries bind/handshake every 50ms. The observed behavior is: Test polls /__uta/health and /api/trading/uta until health ok=true, count=2, IBKR offline/recovering, simulator healthy/readable; transient errors retry. The specific incompatibility or retained boundary is: Assertions encode ad hoc health fields and account count, not schema/protocol, journal/recovery readiness, capability ADTs, or scheduler state. I repaired the entry to the named target: Retain independent-account process polling, but decode canonical RuntimeHealth and AccountView/ListingResult. Global Ready means journal/recovery barrier, not every account Healthy; failed account capability is explicit and simulator account may be readable. Retry only transport/NotReady responses, never typed storage/config failure. Concrete future falsifier: One account unavailable, one healthy: global readiness follows C06 and listing is Complete with both statuses. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    },
    {
      "id": "MAP-4682CE1389",
      "verdict": "accepted",
      "reason": "startup resilience harness 审查 rechecked services/uta/src/uta-startup-resilience.spec.ts:147-158: expects handshake substring, unaffected account healthy, child alive after delay, no fatal marker. The observed behavior is: Test checks error text mentions handshake closure, healthy account survives, child remains alive and output lacks `[uta] fatal:`. The specific incompatibility or retained boundary is: Error substring/process liveness cannot prove durable transaction state, observations, compensation or no duplicate dispatch. I repaired the entry to the named target: Keep liveness/isolation assertions as supplemental diagnostics, and add typed `ConnectionUnavailable`, canonical RuntimeHealth, journal transaction/job state and independent broker-ledger evidence. A handshake failure alone creates no Ack/Fill and no blind retry; Unknown remains RecoveryRequired/ObserveOnly. Concrete future falsifier: Healthy peer serves while failing account degraded. Source leaves this empirical boundary unresolved; the target keeps it unavailable/typed and does not claim support: Native IBKR idempotency/lookup guarantees are not shown in this source; absent adapter evidence, a post-DispatchStarted timeout remains Unknown and cannot be retried. This is a source-grounded design acceptance, not a runtime-test claim. Question closure adopted the Ibkr and IbkrBridge designs: liveness and handshake failure are not native idempotency proof; post-DispatchStarted timeout remains durable Unknown/RecoveryRequired until adapter evidence."
    },
    {
      "id": "MAP-32D2FDFBF9",
      "verdict": "accepted",
      "reason": "startup resilience harness 审查 rechecked services/uta/src/uta-startup-resilience.spec.ts:159-165: finally SIGTERM/KILL child, closes fake TWS, removes temp home; combined timeout. The observed behavior is: Finally cleanup terminates child, closes fake server, removes temporary home and applies test timeout. The specific incompatibility or retained boundary is: Cleanup deletes all durable evidence and cannot diagnose migration/crash residue; timeout combines unrelated startup/cleanup phases. I repaired the entry to the named target: Retain deterministic finally cleanup, but preserve SQLite/journal/projection/broker-ledger artifacts before removal on failure. Separate startup, assertion and cleanup deadlines; cleanup cannot erase the only recovery evidence. Concrete future falsifier: On failing crash cut, journal/ledger artifact exists and references sequence/transaction. No source-local empirical blocker remains after the target correction. This is a source-grounded design acceptance, not a runtime-test claim."
    }
  ]
}
