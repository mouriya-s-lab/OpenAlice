{
  "group": "Leverup",
  "designBasis": "capability-composition-v1",
  "entries": [
    {
      "id": "MAP-6F4E5CBAB5",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.spec.ts:1-8: the suite explicitly mocks fetch/viem and uses a fixed non-production key for deterministic EIP-712 vectors",
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.ts:1-9: a module comment claims sign→POST→poll and fee roles, but does not prove timing, acceptance, finality, or recovery"
      ],
      "currentBehavior": "The suite is a fast adapter fixture: global fetch is intercepted, viem chain reads are stubbed, and one deterministic private key signs vectors. It exercises the old IBroker facade, not a durable UTA runtime.",
      "problem": "The test boundary makes HTTP, chain readiness, durable dispatch, relayer receipt retention, and recovery look successful without proving any of them. A fixed key is acceptable only as an explicitly synthetic test credential, not as evidence that a remote account can trade.",
      "targetModel": "Use a scoped Leverup fixture resource for this source: typed endpoint scenarios, deployment evidence, and a redacted deterministic signer are composed only for the scenario under test. Keep protocol codec checks pure; add a journal/remote-ledger pair only when a scenario explicitly exercises a controlled trading effect. Treat this fixture as unit evidence only: real/integration acceptance remains a separate gate for durable dispatch, restart/unknown recovery, declared compensation, redaction, and baseline restoration.",
      "pureTransitions": [
        "FixtureInput + DeclaredRoute -> Result<FixtureResponse, FixtureProtocolViolation>: decode only the endpoint leaf named by the scenario.",
        "SigningVector + explicit SigningDomain/SchemaVariant/Nonce -> Result<CanonicalTypedData, SigningCodecError>: encode deterministically without ambient clock, randomness, fetch, or viem state.",
        "HarnessConfig + namespace -> Result<FixtureScope, FixtureSetupError>: validate fixture identity and resource ownership before effects are acquired."
      ],
      "effectBoundary": "The fixture scope owns its HTTP/chain/signer effects. Production domain code never installs ambient fetch; controlled-effect scenarios may place a writer and separate remote-ledger fixture around the provider leaf, while read/codec scenarios use no transaction state.",
      "durability": "Fixture inputs, request/response evidence, and signer address are retained only for the scenario assertion. A controlled trading scenario may persist serializable intent/dispatch evidence, but this test entry does not make that persistence a general harness requirement.",
      "errors": [
        "FixtureRouteMissing(route) returns a test setup failure rather than an empty business result.",
        "FixtureProtocolViolation(route, detail) is distinct from a remote business rejection.",
        "TestCredentialLeak is a harness defect if a secret-like value reaches logs, plans, or wire snapshots."
      ],
      "preservedBehavior": [
        "Keep deterministic fixed-key EIP-712 vectors and the fetch/viem fixture speed.",
        "Replace the implication that this proves live relayer or chain behavior with explicit synthetic-evidence language."
      ],
      "replacementSteps": [
        "Create test-only constructors around the declared Leverup provider leaves and shared semantic units.",
        "Move the fixed key behind a redacted TestCredential/FixtureScope; do not expose it through plans or diagnostics.",
        "Add an independent remote-ledger fixture only to crash/response-loss effect scenarios; keep fixture cleanup from erasing evidence before assertions.",
        "Keep the real-broker acceptance harness separate from these mocks; it must cover the applicable effect/recovery scenarios and finish at the declared baseline without unexpected open orders."
      ],
      "verificationCases": [
        "A malformed fixture body is rejected by its endpoint codec and cannot become an accepted provider result.",
        "A response-loss scenario for an explicitly wrapped trading effect yields Unknown plus one observation record after restart; a quote/reader scenario has no order journal.",
        "The same signing vector is reproducible for supplied nonce/deadline and no key material or complete native payload reaches serialized evidence."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "This entry is a synthetic test boundary, so the fixed outer metadata and typed endpoint scenarios apply without making a test harness a universal broker runtime; journal/remote-ledger recovery is conditional on an explicitly exercised controlled effect.",
      "contractAnchors": [
        "K01",
        "K03",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-5FA94B52E3",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.spec.ts:10-18: imports Vitest, Decimal, legacy Contract/Order types, and a fixed private key/address"
      ],
      "currentBehavior": "Tests construct legacy SDK Contract and Order values directly and use the fixed account address as both signing identity and expected fixture identity.",
      "problem": "Legacy SDK values are structurally convenient but can bypass AccountScope, InstrumentId, unit, relation, and secret-boundary validation. The private key import also couples tests to an adapter implementation detail.",
      "targetModel": "Define typed test constructors for AccountScope, InstrumentId, Quantity, Price, Money, and the exact Leverup leaf inputs. Keep one redacted SignerFixture that exposes address/signatures for assertions but never a private key or SDK object in a serializable domain value; signatures and complete native request bodies are retained only as redacted/digest evidence.",
      "pureTransitions": [
        "LegacyTestValue -> Result<LeverupLeafInput, InputViolation>: parse side, type, size, scope, instrument, and native extension explicitly.",
        "TestCredentialRef + SignerAddressEvidence -> Result<SignerBinding, CredentialBindingError>: associate redacted identity without resolving a key in the domain.",
        "NativeContractFixture -> Result<InstrumentDescriptor, InstrumentProtocolViolation>: decode SDK-shaped data at one compatibility boundary."
      ],
      "effectBoundary": "Legacy SDK imports stay in one compatibility fixture; provider schemas and semantic units are exercised through pure constructors, while credential resolution/signing is an effect supplied by the fixture scope.",
      "durability": "Fixture values are ephemeral by default. Only a scenario explicitly testing a controlled effect may serialize its intent/receipt; the helper itself does not own order persistence, and any signature/native body evidence is redacted or digested rather than copied into logs or projections.",
      "errors": [
        "LegacyProjectionNotRepresentable(reason) is a compatibility-fixture failure, not a synthetic canonical instrument.",
        "CredentialUnavailable(ref) prevents entering the signer effect.",
        "UnitMismatch(field, expected, actual) rejects input before native compilation."
      ],
      "preservedBehavior": [
        "Preserve the address/signature vectors and decimal assertions that catch wire regressions.",
        "Intentionally stop using SDK object construction as proof that the UTA model is valid."
      ],
      "replacementSteps": [
        "Replace direct SDK construction in domain assertions with typed fixture builders and one boundary-only legacy codec.",
        "Expose a redacted signer fixture and bind expected data/effect scenarios to explicit AccountScope.",
        "Assert redaction of signatures and complete native payloads at protocol/journal/diagnostic boundaries."
      ],
      "verificationCases": [
        "A legacy value with missing quantity is rejected as InputViolation before the fixture HTTP port.",
        "A valid fixture signs and verifies the expected address while serialized inputs contain no key or PrivateKeyAccount.",
        "Signature and complete native payload snapshots are redacted/digested rather than exposed in logs, protocol, journal, or projections.",
        "A native contract with an unknown exchange is rejected by the compatibility decoder."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "Legacy Contract/Order and signer fixtures remain useful evidence, but they must be compatibility projections over provider leaf schemas and semantic units rather than a required SDK-shaped domain or durable order model.",
      "contractAnchors": [
        "K03",
        "K04",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-C6C1199DD4",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.spec.ts:20-34: viem mock returns chain id 10143, USDC balance 1_000_000_000n, and 6 decimals"
      ],
      "currentBehavior": "Every chain read is made to look ready: chainId is 10143, balance is 1000 USDC, and token decimals are six. The values are independent stubs, so no endpoint/account/contract identity is checked.",
      "problem": "Public chain reads and a funded-looking balance do not prove the relayer endpoint, account scope, token address, or signing domain agree. A mismatched deployment could pass all current tests and send an invalid signature.",
      "targetModel": "Model ChainDeployment as an immutable provider identity and ChainReadEvidence as a scoped data observation: environment, chainId, RPC/endpoint identities, token/agent addresses, contract-set fingerprint, address, block/as-of, and observed decimals. Use the binding to describe capability availability and to authorize only a matching controlled signing effect.",
      "pureTransitions": [
        "ChainReadEvidence + DeclaredDeployment -> Result<DeploymentBinding, DeploymentMismatch>: compare chain, token, and agent identities.",
        "RawBalance + TokenIdentity/Decimals -> Result<Money<USDC>, ChainDecodeError>: construct a unit only after observed identity matches.",
        "DeploymentBinding + SigningDomain -> Result<SigningAuthority, SigningMismatch>: require equal chainId/verifyingContract only when a signing leaf is composed."
      ],
      "effectBoundary": "Viem calls are effectful and must be decoded into typed observations at the adapter edge. Pure preparation receives those observations; it must not call getChainId or readContract.",
      "durability": "Retain block/as-of and deployment fingerprints with data observations or any effect intent that consumes them; a balance observation alone is neither write authorization nor a dispatch fact.",
      "errors": [
        "ChainDeploymentMismatch(expected, observed) prevents preparation or signing.",
        "TokenDecimalsUnavailable(address) prevents Money construction.",
        "FundedAccountNotAuthorized(scope) is distinct from zero balance and must not be hidden as false."
      ],
      "preservedBehavior": [
        "Keep 10143 and 6-decimal/1000-USDC vectors as fixture values for deterministic conversion tests.",
        "Do not preserve the false inference that those stubs establish live write readiness."
      ],
      "replacementSteps": [
        "Make the mock return typed chain evidence including address, block, and deployment fingerprint.",
        "Add mismatch fixtures for chainId, USDC address, and oneClickAgent.",
        "Bind signer domain and native requests to the same ChainDeployment record."
      ],
      "verificationCases": [
        "Changing chainId from 10143 causes DeploymentMismatch before any controlled relayer effect.",
        "A balance at a different token address is TokenIdentityMismatch, not 1000 USDC.",
        "A six-decimal balance is represented as Money<USDC> with source block evidence."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "Deployment and chain-read identity is already the right provider-side boundary; it is retained as evidence for capability availability and controlled signing, while balance reads remain ordinary data observations.",
      "contractAnchors": [
        "K02",
        "K03",
        "K07"
      ]
    },
    {
      "id": "MAP-A9E4B4C39D",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.spec.ts:36-48: saves global fetch, imports broker/pair/decimal/sign helpers, and imports network constants"
      ],
      "currentBehavior": "The suite saves global fetch and restores it after tests; production helper modules are imported directly, including mutable schema/signing and network constants.",
      "problem": "Global monkey-patching allows unrelated requests to be accidentally intercepted and hides the required effect/resource boundary. Direct helper imports make test success depend on mutable adapter state rather than a serializable plan.",
      "targetModel": "Use an explicit, scoped Leverup port declaration for the native surfaces exercised by a scenario (HTTP, chain reader, clock/nonce for signing, and signer). Keep network constants behind a versioned ChainDeployment decoder; this is an adapter boundary, not a mandatory port set for all providers.",
      "pureTransitions": [
        "Request + PortDeclaration -> Result<EndpointRequest, RouteViolation>: select an exact method/path/query route, not a URL substring.",
        "DeclaredNetwork + ChainEvidence -> Result<ChainDeployment, DeploymentMismatch>: compare declaration with observed identities.",
        "OpenIntent + supplied Instant/Nonce -> Result<PreparedSigningInput, PlanViolation>: freeze time and identity before the signer effect."
      ],
      "effectBoundary": "Only a fixture scope or composition root installs a concrete HTTP implementation. Pure modules consume parsed facts and do not import ambient fetch, viem clients, or endpoint constants.",
      "durability": "Port identity and fixture scope are recorded in test metadata; no global mutable state is used as durable authority.",
      "errors": [
        "PortNotInstalled(port) fails harness setup.",
        "DeploymentMismatch(field) blocks initialization.",
        "UnexpectedRoute(url) is a protocol fixture failure, not a business empty result."
      ],
      "preservedBehavior": [
        "Preserve restoration discipline and direct coverage of network/decimal/signing helpers.",
        "Change tests to prove explicit ports and deployment compatibility rather than a global mock side effect."
      ],
      "replacementSteps": [
        "Introduce a scoped port factory for each exercised provider leaf.",
        "Move fetch restoration into the fixture scope finalizer; keep data readers and controlled trading effects separately resource-scoped.",
        "Record exact deployment identity in each fixture scenario."
      ],
      "verificationCases": [
        "A request through an undeclared port fails as a fixture boundary violation without changing effect state.",
        "Two fixture scopes can use different deployment identities without cross-talk.",
        "The finalizer releases resources when signing or decoding fails."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source uses global fetch and mutable helpers, but the target only requires an explicit adapter port for the exercised native protocol; it does not prescribe one runtime port set or one implementation language for every provider.",
      "contractAnchors": [
        "K01",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-E00D066E13",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.spec.ts:50-59: MockFetchOptions combines Pyth price/data, reader positions, relayer inputHash/error, and status booleans"
      ],
      "currentBehavior": "One optional bag configures all three external surfaces: Pyth binary/number data, an untyped positions array, and relayer success/error/status booleans.",
      "problem": "The bag permits impossible combinations, such as a price without a matching Pyth update, a status for an unrelated inputHash, or position rows with no schema. Optional flags cannot express response provenance or listing completeness.",
      "targetModel": "Replace the optional mock bag with a discriminated, endpoint-local FixtureScenario union: Pyth data, reader listing, relayer submit/status, transport failure, and malformed body. Each variant carries its exact request correlation and evidence kind; it does not define a closed provider-wide action/error set.",
      "pureTransitions": [
        "FixtureScenario -> Result<WireResponse, FixtureProtocolViolation>: select only the matching endpoint variant.",
        "ReaderListingResponse -> Result<ListingResult, ListingDecodeError>: retain Complete/Partial and cursor metadata.",
        "RelayerStatusResponse -> Result<Observation, StatusDecodeError>: map only fields whose terminal meaning is evidenced."
      ],
      "effectBoundary": "Scenario interpretation is test-boundary effect code. Pure fixture constructors reject contradictory inputs before a port is installed; only relayer scenarios that model a controlled effect use dispatch/recovery assertions.",
      "durability": "Scenario IDs and request correlations are test evidence. They never masquerade as remote idempotency, a broker receipt, or a transaction for a pull/reader fixture.",
      "errors": [
        "ScenarioMismatch(endpoint, request) rejects an unrelated fixture.",
        "MalformedBody(endpoint, field) remains a ProtocolViolation, not `success:false`.",
        "UnknownListingCoverage(missing) prevents a NotFound conclusion."
      ],
      "preservedBehavior": [
        "Keep configurable 503, successful inputHash, status, Pyth, and position examples.",
        "Remove the untyped all-in-one options object and booleans as domain evidence."
      ],
      "replacementSteps": [
        "Define endpoint-specific fixture schemas and require matching request keys.",
        "Represent reader pages with wire schema and coverage evidence; do not make one page complete by default.",
        "Represent relayer status as raw evidence plus a versioned interpretation policy, not a filled-order shortcut."
      ],
      "verificationCases": [
        "A status fixture for another inputHash is rejected as ScenarioMismatch.",
        "A reader response missing total-page evidence is Partial and cannot prove absence.",
        "A malformed Pyth binary list yields ProtocolViolation before an effect request."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The all-in-one mock bag must become schema-first endpoint scenarios, matching K03/K07; the scenario union is test-local and does not imply a closed global action or error vocabulary.",
      "contractAnchors": [
        "K03",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-DD63DE7255",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.spec.ts:61-116: installFetchMock dispatches by URL substring, fabricates Hermes binary/parsed JSON, reader page 0, relayer submit/status, and a generic 404"
      ],
      "currentBehavior": "The mock branches on URL substrings. Hermes returns the configured binary and parsed price; reader always returns a single page; relayer POST returns inputHash or 503 and GET returns configured booleans; all unknown URLs become 404.",
      "problem": "Substring routing and `as` casts do not validate method, path, query, body, chain, or response schema. A one-page response cannot establish complete listing absence, and an HTTP 200/boolean payload cannot establish fill, rejection, or chain finality.",
      "targetModel": "Implement exact codecs for the provider’s Pyth, reader, relayer-open, relayer-close, and relayer-status leaves. The fixture response retains request digest, deployment, cursor coverage, and raw evidence; status interpretation is a separate provider policy and does not close the global schema.",
      "pureTransitions": [
        "HttpRequest -> Result<EndpointRequest, RequestDecodeError>: exact method/path/query/body decoding.",
        "EndpointResponse -> Result<WireEvidence, ResponseDecodeError>: validate each field and preserve opaque receipt/status.",
        "WireEvidence + ObservationPolicy -> Result<Observation, EvidenceInsufficient>: classify Ack/StillWorking/Unknown without upgrading booleans."
      ],
      "effectBoundary": "Fetch is used only by the adapter/fixture interpreter. Pure data projections consume decoded Pyth/reader evidence; a controlled open/close effect additionally records dispatch/observation decisions around relayer calls.",
      "durability": "Retain request/response correlation and endpoint fingerprints for fixture assertions. Reader cursors and status payloads are evidence; dispatch records are created only by the controlled trading scenario.",
      "errors": [
        "RouteNotDeclared(method, path) fails as FixtureProtocolViolation.",
        "ResponseDecodeError(endpoint, field) stops processing without a false business rejection.",
        "StatusEvidenceInsufficient(inputHash, fields) yields Unknown/observation retry policy, not Rejected or Filled."
      ],
      "preservedBehavior": [
        "Preserve the useful Hermes binary+parsed fixture, reader position fixtures, and 503 paths.",
        "Intentionally change URL-substring and generic 404 behavior to exact protocol decoding and typed coverage."
      ],
      "replacementSteps": [
        "Replace installFetchMock with a scoped typed HttpPort and endpoint-specific fixture handlers.",
        "Add body/method validation and cursor/coverage fields to reader fixtures.",
        "Persist the relayer inputHash as opaque receipt evidence and keep status interpretation outside transport decoding."
      ],
      "verificationCases": [
        "A POST sent to the relayer status path is rejected as a route/protocol violation before any business result.",
        "A reader response with pageNumber=0 and totalPages=2 is Partial and cannot produce Complete([]).",
        "A status body {executed:false,success:false} without independent protocol meaning remains Unknown; a versioned fixture may yield StillWorking only with evidence.",
        "A 503 after a controlled effect’s DispatchStarted yields Unknown and one observation intent, never Rejected or a second POST."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "Exact route and response codecs preserve the native evidence boundary, while pull/reader/status fixtures remain distinct deliveries; transaction recovery is asserted only in dispatch scenarios.",
      "contractAnchors": [
        "K03",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-AC474EA806",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.spec.ts:118-125: beforeEach installs a default mock and afterEach restores fetch and clears mocks"
      ],
      "currentBehavior": "Each test receives a default global mock and cleanup restores fetch and clears Vitest mocks; there is no journal, worker lease, or independent remote ledger in the lifecycle.",
      "problem": "The lifecycle hides resource ownership and cannot exercise crash cuts or response-loss recovery. Cleanup of an in-memory mock is not equivalent to retaining a remote dispatch fact.",
      "targetModel": "Use withLeverupHarness as a scoped test resource: acquire declared ports, and for a controlled trading scenario open an isolated real SQLite journal/scheduler namespace plus a separate remote-ledger namespace; run the scenario, exercise crash/restart cuts, drain owned resources, and release them. Query/codec scenarios do not receive order state machinery.",
      "pureTransitions": [
        "HarnessConfig -> Result<ScopedHarnessSpec, HarnessSetupError>: validate namespace, port declarations, and optional ledger identities.",
        "LifecycleState + ShutdownSignal -> Result<CleanupDecision, CleanupFailure>: determine whether owned resources stopped and evidence was retained.",
        "JournalSnapshot + RemoteLedgerSnapshot -> RecoveryClassification: compare dispatch identity only when the scenario actually created one."
      ],
      "effectBoundary": "Lifecycle resource management is effectful; domain decisions and recovery classification are pure. Global fetch installation is forbidden in production, and fixture ownership is explicit.",
      "durability": "The isolated SQLite journal, scheduler jobs, and remote-ledger namespace are retained through crash/restart assertions for controlled effects; data/query fixtures only retain their own evidence. Cleanup must not rewrite Unknown as success or delete evidence before assertions.",
      "errors": [
        "HarnessSetupFailure(reason) stops the test before admission.",
        "CleanupFailure(resource) is reported separately from business outcome.",
        "RecoveryEvidenceMissing(identity) requires operator-style test failure, not a fabricated completion."
      ],
      "preservedBehavior": [
        "Preserve automatic setup and cleanup ergonomics.",
        "Add explicit scopes and an independent ledger so tests can cover lease/restart behavior."
      ],
      "replacementSteps": [
        "Replace global beforeEach/afterEach patching with a scoped fixture resource.",
        "Keep finalization while preserving query/codec evidence and, when applicable, the separate ledger snapshot.",
        "Add restart coverage only for the controlled effect leaves that can lose a response.",
        "Use a fresh process/runtime over the same journal namespace to prove exactly one justified observation/recovery action after restart; do not apply this machinery to pull/codec scenarios."
      ],
      "verificationCases": [
        "A thrown signing error releases ports and leaves a classified fixture outcome.",
        "A restart in a response-loss effect scenario reconstructs one observation for the same identity from the isolated SQLite journal; a reader scenario has no order record.",
        "Cleanup cannot turn an Unknown remote effect into Completed."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "Lifecycle ownership is a test-control concern, not an order state machine; controlled-effect tests may retain a separate remote ledger and journal, whereas data/query scenarios only scope and release ports.",
      "contractAnchors": [
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-405AB5279C",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.spec.ts:127-158: helpers construct a broker with a raw key, legacy Order, and legacy Contract"
      ],
      "currentBehavior": "`makeBroker` passes a raw key to config, `makeInitializedBroker` calls init, and order/contract helpers create legacy SDK objects for a CRYPTO_PERP/LEVERUP market order.",
      "problem": "Helpers bypass canonical scope and unit constructors and make initialization the only source of readiness. They also encourage assertions against SDK shapes rather than action/ack/observation associations.",
      "targetModel": "Define makeScope, makeInstrument, makePlaceInput, and makeDeployment constructors for the exact Leverup leaves. Keep one adapter-only legacy Contract/Order projection helper; assertions target scoped provider inputs, data observations, and typed outcomes rather than SDK object identity.",
      "pureTransitions": [
        "TestInput -> Result<LeverupLeafInput, TestInputError>: construct a valid market-open input with explicit scope/instrument.",
        "LeafInput + Facts -> Result<PreparedNativePayload, PrepareFailure>: compile deterministic provider fields without an effect.",
        "NativeOutcome -> Result<CompatibilityProjection, ProjectionError>: map only after typed evidence."
      ],
      "effectBoundary": "Constructors are pure. Ports, signing, and any journal/remote-ledger fixture are supplied only by the scenario that exercises a controlled trading effect; helpers never reach a live endpoint or global client.",
      "durability": "Prepared payloads are assertion values; a controlled effect scenario may serialize its own intent/receipt. SDK fixtures remain ephemeral projections.",
      "errors": [
        "InvalidScopeFixture(reason) and InvalidUnitFixture(field) reject construction.",
        "UnavailableProviderLeaf(leaf) is explicit when the scenario asks for a source-absent capability.",
        "NativeDecodeError(field) prevents a compatibility projection."
      ],
      "preservedBehavior": [
        "Preserve BUY/SELL, CRYPTO_PERP, LEVERUP, and MKT fixture coverage.",
        "Stop treating legacy SDK instances as domain authority."
      ],
      "replacementSteps": [
        "Replace raw helper construction with typed action/deployment builders.",
        "Retain one legacy projection helper solely at the adapter boundary.",
        "Assert scope, plan digest, and typed outcome in every scenario."
      ],
      "verificationCases": [
        "A valid BUY MKT input compiles with explicit AccountScope and InstrumentId.",
        "A legacy Contract lacking pairBase cannot be used as a native Leverup identity.",
        "The helper never serializes a signer object or raw key."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "Helper construction should exercise declared provider leaf schemas and semantic units, with the legacy IBroker shape only at the compatibility edge; generic helpers must not own order WAL or dispatch state.",
      "contractAnchors": [
        "K01",
        "K03",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-7F36FE8E07",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.spec.ts:162-189: tests accept live/testnet configs, reject malformed keys, and expect init to probe only chainId"
      ],
      "currentBehavior": "The tests check id/label and two environment strings, reject a non-hex key, and consider initialization successful after one `getChainId` call. They do not verify endpoint/account/agent deployment agreement.",
      "problem": "A syntactically valid key and public chain probe are insufficient readiness. The `live`/`testnet` label can coexist with mainnet pair addresses and an incompatible relayer, which would make the prepared signature unsafe.",
      "targetModel": "Parse canonical LeverupConfig as CredentialRef plus declared Environment, then resolve a ChainDeployment and fixed CapabilityDescriptors for provider leaves whose schemas require them. If the legacy privateKey shape remains at the compatibility edge, validate its syntax there and discard it before canonical construction; it is not a CredentialRef. Discover AccountScope only for account-bound data/effect leaves; public catalog/quote/instrument leaves must not invent a wallet or subaccount. Readiness is per-leaf availability, not a boolean promise that every action exists.",
      "pureTransitions": [
        "RawConfig -> Result<LeverupConfig, ConfigViolation>: validate exact environment and either a CredentialRef or legacy private-key syntax at the compatibility boundary, without retaining secret material in the canonical config.",
        "DeclaredEnvironment + RuntimeEvidence -> Result<CapabilityAvailability, DeploymentMismatch>: compare identity fields and report per-leaf availability.",
        "CapabilityAvailability + requested provider leaf -> Result<AdmissionFacts, CapabilityUnavailable>: require AccountScope only when that leaf declares account scope."
      ],
      "effectBoundary": "Config parsing is pure; credential and chain/HTTP probes are effects. The adapter cannot mark an affected leaf ready from only getChainId; the application stores per-leaf capability availability and an optional scope discovery result for account-bound leaves.",
      "durability": "Persist configuration/deployment revision only when needed to bind a later data observation or controlled effect; never persist privateKey.",
      "errors": [
        "InvalidCredentialReference(ref) directs operator to configured secret store.",
        "DeploymentMismatch(field) blocks writes.",
        "ScopeDiscoveryPending/Unavailable prevents a default account scope.",
        "CapabilityDegraded(reason) still permits only explicitly read-only operations."
      ],
      "preservedBehavior": [
        "Keep id/label and live/testnet validation behavior where the deployment evidence is valid.",
        "Retain malformed legacy private-key rejection only at the compatibility parser; validate CredentialRef syntax independently without treating a reference as key material.",
        "Change init readiness from one chain probe to an identity-checked capability snapshot."
      ],
      "replacementSteps": [
        "Replace fromConfig cast with a strict named config ADT.",
        "Add endpoint/on-chain identity probes and scope discovery before admitting the affected leaves.",
        "Remove raw key retention from provider state; inject signer effect through CredentialRef."
      ],
      "verificationCases": [
        "A malformed legacy private key is rejected before secret-store or network access; a malformed CredentialRef is rejected by its own schema without resolving the secret.",
        "A testnet label paired with observed live chain/agent fails DeploymentMismatch.",
        "A valid chainId with missing scope yields ScopeDiscoveryPending for an account-bound leaf, while public catalog/quote leaves remain independently describable.",
        "A read-only quote/account leaf can be available while the open-effect leaf is unavailable."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "Configuration and readiness are provider identity/resource concerns. The target composes fixed capability metadata with only the leaves Leverup declares instead of requiring a universal action surface.",
      "contractAnchors": [
        "K02",
        "K07",
        "K11"
      ]
    },
    {
      "id": "MAP-A8A98337B2",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.spec.ts:191-217: search tests expect BTC/USD and 500BTC/USD matches and empty results for unknown/empty queries",
        "services/uta/src/domain/trading/brokers/others/leverup/pairs.ts:8-10,41-43: 500BTC/500ETH pairBase values are documented as placeholders even though the rows remain in the static registry"
      ],
      "currentBehavior": "Search uppercases the query and uses static substring matching; known BTC/USD and high-leverage 500BTC/USD appear, while unknown and empty queries produce an empty array.",
      "problem": "An empty array conflates no matches with an unavailable or incomplete catalog. Static entries include a documented 20-of-23 gap, placeholder high-leverage pairBase values, and testnet aliases, so search results do not establish deployable support.",
      "targetModel": "Use a versioned InstrumentCatalog with canonical InstrumentId, display aliases, pairBase/Pyth binding, category, high-leverage marker, deployment fingerprint, and coverage. Placeholder pairBase rows remain discoverable metadata but are not native identities until address evidence exists. The search leaf returns ListingResult<Instrument> Complete/Partial/Unavailable; it is a data projection and does not imply write capability.",
      "pureTransitions": [
        "CatalogSnapshot + Query -> ListingResult<Instrument>: normalize case, match aliases, and preserve coverage state.",
        "CatalogEntry -> Result<InstrumentId, InstrumentBindingError>: require deployment and native binding evidence.",
        "ListingResult + selected provider leaf -> Result<ResolvedInstrument, CatalogIncomplete>: only a known entry can feed that leaf."
      ],
      "effectBoundary": "Catalog loading is an effect; case normalization and substring matching are pure. The adapter must not turn registry absence into a synthetic SDK Contract.",
      "durability": "Persist catalog revision and deployment fingerprint in prepared plans and native lookup descriptors; search projections may be rebuilt.",
      "errors": [
        "CatalogUnavailable(reason) and CatalogPartial(missing) are not empty search results.",
        "UnknownInstrument(query) directs the caller to select a supported identity.",
        "HighLeverageEvidenceMissing(instrument) blocks that specific capability.",
        "PairAddressUnverified(instrument) blocks native use of documented placeholder pairBase values."
      ],
      "preservedBehavior": [
        "Preserve case-insensitive substring discovery for known BTC/USD and 500BTC/USD.",
        "Keep high-leverage markers visible for catalog discovery, but do not expose placeholder pairBase rows as writable native targets.",
        "Replace `[]` as the representation of unavailable/incomplete catalog state."
      ],
      "replacementSteps": [
        "Create a catalog loader and typed ListingResult projection.",
        "Record documented missing pairs, placeholder high-leverage addresses, and testnet address uncertainty as coverage evidence.",
        "Make provider leaf preparation consume canonical entries only after native address evidence, not search strings or SDK contracts."
      ],
      "verificationCases": [
        "Query BTC preserves BTC/USD and 500BTC/USD discovery when the supplied catalog contains them.",
        "A 500BTC/500ETH placeholder pair remains visible as catalog metadata but cannot compile a native write without address evidence.",
        "An incomplete registry returns Partial even when a known match exists.",
        "No match on a Complete catalog is an empty match set distinct from Unavailable."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "Coverage-aware catalog search already matches the open public Instrument tree: source substring behavior and high-leverage markers are retained as projections, while 20/23 coverage, placeholder pairBase values, availability, and writability remain separate from any account-bound effect and no account default is invented.",
      "contractAnchors": [
        "K02",
        "K04",
        "K05"
      ]
    },
    {
      "id": "MAP-C78F741F42",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.spec.ts:219-239: native-key tests round-trip known BTC/USD and expect a synthetic Contract for FAKE/USD",
        "services/uta/src/domain/trading/brokers/others/leverup/pairs.ts:8-10,41-43: 500BTC/500ETH pairBase entries are placeholder addresses and cannot be treated as verified native identities"
      ],
      "currentBehavior": "Known pair identity round-trips through `getNativeKey`/`resolveNativeKey`; an unknown symbol is turned into a synthetic CRYPTO_PERP/LEVERUP Contract rather than rejected.",
      "problem": "Synthetic unknown Contracts have no pairBase, feed, deployment, or capability evidence and can pass type checks before failing remotely. The fallback hides catalog incompleteness and violates canonical identity.",
      "targetModel": "Define NativeInstrumentKey as a deployment-scoped InstrumentDescriptor reference carrying canonical identity, pairBase/feed evidence state (Verified|Placeholder|Unknown), and revision. resolveNativeKey returns a typed known/unknown result; only a Verified descriptor can enter a native effect. A legacy Contract is created only as a read-only projection for a known descriptor, never as a synthetic or placeholder native target.",
      "pureTransitions": [
        "NativeKey + CatalogSnapshot -> Result<InstrumentDescriptor, InstrumentResolutionError>: require exact identity/deployment match and preserve pairBase/feed evidence state.",
        "InstrumentDescriptor -> Result<LegacyContractProjection, ProjectionError>: create a compatibility view without losing the descriptor link.",
        "Unknown or unverified key -> UnknownInstrument/NativeDescriptorUnverified evidence, not a provider-effect input."
      ],
      "effectBoundary": "Resolution is pure after catalog facts are loaded. Legacy Contract creation is an adapter projection; remote calls and journal writes stay outside.",
      "durability": "Persist descriptor/revision only when a later provider effect or observation must survive restart; catalog search projections can be rebuilt.",
      "errors": [
        "UnknownInstrument(key) tells caller to select a catalog entry.",
        "DeploymentMismatch(key) blocks use across live/testnet identities.",
        "NativeDescriptorIncomplete(missingPairBase) or NativeDescriptorUnverified(state) cannot be dispatched."
      ],
      "preservedBehavior": [
        "Preserve known BTC/USD round-trip semantics.",
        "Intentionally remove synthetic FAKE/USD success; unknown remains observable as a typed failure."
      ],
      "replacementSteps": [
        "Delete the synthetic fallback branch and add a Result-returning resolver.",
        "Version and persist the descriptor used by place/close and observation lookup.",
        "Update compatibility projection callers to handle UnknownInstrument explicitly."
      ],
      "verificationCases": [
        "FAKE/USD produces UnknownInstrument before Pyth or relayer effects.",
        "Known BTC/USD resolves the same verified pairBase/feed under the declared deployment.",
        "500BTC/500ETH retains placeholder evidence for display but cannot enter a native effect.",
        "A descriptor from another deployment is rejected even with the same display symbol."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "Canonical deployment-scoped instrument identity and removal of synthetic unknown Contracts are directly required by K02/K04; the compatibility Contract remains a public projection and does not imply AccountScope or a global SDK contract.",
      "contractAnchors": [
        "K02",
        "K04",
        "K07"
      ]
    },
    {
      "id": "MAP-F8AC526698",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.spec.ts:241-267: place tests reject non-MKT type, unknown pair, and UNSET quantity via Result errors"
      ],
      "currentBehavior": "Place tests cover three early failures: a non-market order type, an unknown pair, and the legacy UNSET_DECIMAL quantity sentinel; each is expected to return an error result before fetch.",
      "problem": "The checks are necessary but incomplete: they do not enforce explicit scope, positive/range-safe quantity, sizing variant, price/protection semantics, deployment binding, or capability evidence. A sentinel check alone still allows malformed Decimal values.",
      "targetModel": "Model one Leverup open-effect leaf with required AccountScope, canonical InstrumentId, side, the exact supported sizing/order fields, and provider-native extensions. Shared Order units may be composed through a HOF, but unsupported order/protection/relation variants are absent or unavailable at this leaf rather than entries in a universal order map.",
      "pureTransitions": [
        "RawOpenInput + ScopeDirectory + LeafCapability -> Result<ResolvedOpenInput, PrepareFailure>: parse and validate the declared fields.",
        "ResolvedOpenInput + MarketFacts -> Result<PreparedNativePayload, PreconditionFailure>: bind immutable sizing and price facts.",
        "PreparedNativePayload + EffectAdmission -> Result<DispatchIntent, DispatchAdmissionFailure>: permit only the selected provider leaf and revision."
      ],
      "effectBoundary": "Validation and compilation are pure. Scope discovery, catalog, Pyth, and capability reads happen before preparation; writer persists accepted plan atomically, while no network send is permitted on failure.",
      "durability": "Only the controlled open effect persists a command digest and prepared payload before dispatch; validation/query paths write no DispatchStarted or transaction state.",
      "errors": [
        "OpenLeafInputUnsupported(type) directs legacy callers to a declared provider leaf.",
        "UnknownInstrument(symbol) and ScopeRequired prevent routing.",
        "InvalidQuantity(reason) rejects zero/negative/overprecision/overflow.",
        "ProtectionUnavailable(spec) and RelationUnavailable(relation) are explicit outcomes for fields not declared by this leaf."
      ],
      "preservedBehavior": [
        "Preserve early MKT/unknown/UNSET rejection behavior and no relayer call on these paths.",
        "Expand validation to prevent unsafe values and preserve named unsupported order semantics rather than silently dropping fields."
      ],
      "replacementSteps": [
        "Replace sentinel-only checks with boundary constructors for the declared leaf schema.",
        "Thread AccountScope and InstrumentDescriptor through the input and native descriptor.",
        "Add acceptance for the declared leaf’s sizing/protection/time/session/relation fields and ensure absent or unavailable variants fail before dispatch."
      ],
      "verificationCases": [
        "Legacy non-MKT, unknown pair, or UNSET quantity fails before Pyth/relayer effects.",
        "A Notional input is accepted only if this leaf declares a quote conversion; retry reuses frozen facts.",
        "A LIMIT, trailing, GTD, bracket, or relation input is structurally absent/unavailable and emits no MKT request.",
        "Missing AccountScope returns ScopeRequired even when a wallet address is available."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The early source validation is retained, but the target is a Leverup open-effect leaf with an exact schema/HOF composition; unsupported fields are absent or unavailable at that leaf rather than enumerated in a universal order map.",
      "contractAnchors": [
        "K01",
        "K03",
        "K04",
        "K08"
      ]
    },
    {
      "id": "MAP-2FF2A5B544",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.spec.ts:269-301: successful place fixture returns Pyth price 60000, relayer inputHash, and expects success=true/orderState Submitted; POST body includes Pyth data"
      ],
      "currentBehavior": "A market BUY is priced from the mocked Pyth response, serialized with quantity/price/amountIn scales, posted to the relayer, and immediately exposed as a Submitted order keyed by inputHash. The test also checks the Pyth binary appears in the POST.",
      "problem": "The fixture proves request assembly only. It does not prove that inputHash is idempotent or durable, that the Pyth bytes are accepted on-chain, or that a successful submit means fill/finality. Immediate in-memory tracking loses the plan on restart.",
      "targetModel": "Compile the Leverup open leaf into a serializable provider payload carrying AccountScope, InstrumentDescriptor, quote/collateral evidence, PriceObservation, Pyth update evidence, native payload digest, SigningDomain/SchemaCapability, and command identity. Wrap only the remote trading effect with durable intent/dispatch/observation; return Ack with an opaque relayer receipt and never infer fill/finality.",
      "pureTransitions": [
        "PythObservation + OpenLeafInput -> Result<NativeOpenData, PriceOrUnitFailure>: compute amount and encoded fields with explicit scales and frozen quote evidence.",
        "NativeOpenData + SigningDomain + SchemaVariant + Nonce -> Result<SigningIntent, SigningInputFailure>: bind the exact schema/domain/identity for the signer effect.",
        "RelayerSubmitEvidence + DispatchAttempt -> Result<Ack|Unknown|Rejected, DispatchClassification>: classify only the response/phase evidence and never infer fill.",
        "CommandId + payloadDigest + JournalWriter -> Result<ExistingReceipt|IdempotencyConflict, DuplicateCommandError>: deduplicate locally and never infer relayer idempotency."
      ],
      "effectBoundary": "Pyth retrieval/decoding is a pull data effect. Signing and relayer POST are controlled trading effects whose writer records durable intent and DispatchStarted before mutation; the provider tree does not transaction-wrap ordinary quote/reader data.",
      "durability": "Persist Pyth evidence references, native descriptor, plan digest, deployment/domain/schema fingerprints, and inputHash only for the controlled effect. A response is an observation handle, not execution truth.",
      "errors": [
        "PriceObservationStale(feed, asOf) blocks preparation.",
        "NativePayloadUnitMismatch(field) and QuoteConversionUnavailable(currency) stop compilation.",
        "RelayerAckOpaque(receipt) records acknowledgement but does not commit a fill.",
        "DispatchOutcomeUnknown(identity, cause) enters ObserveUnknown/RecoveryRequired after response loss.",
        "DuplicateCommand(payloadDigest) returns the existing durable receipt; a conflicting digest is IdempotencyConflict."
      ],
      "preservedBehavior": [
        "Preserve 60000 Pyth fixture, POST inclusion of binary updates, inputHash correlation, and immediate Submitted projection.",
        "Change success=true to a typed Ack and keep order financial state unresolved until fill evidence exists."
      ],
      "replacementSteps": [
        "Split the pure provider compiler from signer and relayer interpreters.",
        "Persist the controlled effect intent and dispatch identity before POST; replace Map insertion with durable observation.",
        "Retain Submitted/Pending only as a projection of validated Ack, not as fill/finality."
      ],
      "verificationCases": [
        "A valid fixture yields Ack plus Submitted/Pending projection and the POST contains exact Pyth bytes.",
        "A crash after DispatchStarted and before response yields one Unknown observation for the same effect identity.",
        "A relayer 200 with inputHash but no execution evidence never yields Filled/Committed.",
        "A stale/malformed Pyth response fails before dispatch.",
        "A same-digest controlled command returns its existing receipt; a conflicting digest is rejected without a new salt or POST."
      ],
      "openQuestions": [
        "Native relayer evidence is absent for duplicate-submit/idempotency scope and inputHash retention horizon. Until a versioned conformance result establishes these guarantees, local journal commandId+payloadDigest deduplication governs retries; a post-DispatchStarted ambiguity remains Unknown/RecoveryRequired and uses observe-before-retry, never blind resubmit.",
        "Native evidence is absent for the accepted EIP-712 schema, chain finality, and semantic effect of a Pyth binary update. Until each is versioned and bound to ChainDeployment, schema capability remains unavailable, binary bytes are retained as evidence only, and relayer Ack/Unknown cannot be upgraded to execution/finality."
      ],
      "contractDisposition": "reframed",
      "contractRationale": "The place fixture is a controlled trading effect and therefore keeps durable intent/dispatch/recovery evidence, but its schema, receipt, and errors are provider-leaf contracts; Pyth acquisition itself is a non-transactional data effect.",
      "contractAnchors": [
        "K03",
        "K05",
        "K07",
        "K08",
        "K12"
      ]
    },
    {
      "id": "MAP-D61B8CB8B4",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.spec.ts:303-313: relayer 503 fixture is expected to return success=false with an error string"
      ],
      "currentBehavior": "A non-OK relayer response is caught by the broker and converted to a boolean failure plus `Error.message` text.",
      "problem": "HTTP transport failure is not evidence that the remote did not receive a request, especially after DispatchStarted. Converting it to `success:false` invites continuation or rejection classification without knowing whether the request was accepted.",
      "targetModel": "Use a typed transport outcome for the Leverup relayer effect with phase (before-send, send-ambiguous, observe) and the existing dispatch identity. FailurePolicy applies only inside this controlled effect and only a protocol-evidenced terminal refusal becomes Rejected.",
      "pureTransitions": [
        "HttpFailure(status, phase) -> RelayerTransportFailure: preserve status and redacted body metadata.",
        "RelayerTransportFailure + DispatchAttempt -> Ack|Unknown: before-send can reject admission; after-start is Unknown.",
        "Unknown + FailurePolicy -> RecoveryCase/ObserveUnknown: never map to known rejection automatically."
      ],
      "effectBoundary": "HTTP status decoding is an adapter effect. Writer records the failure/evidence and durable observe job; domain failure mapping is pure and phase-aware.",
      "durability": "Persist phase, dispatch identity, request digest, status code, and observation cursor. Never overwrite a started dispatch with a generic rejected result.",
      "errors": [
        "RelayerHttpFailure(status, bodyDigest) asks for retry/observation according to phase.",
        "KnownRelayerRejection(reason) may become Rejected only when protocol evidence explicitly identifies a terminal refusal.",
        "UnknownDispatch(identity) suspends remaining steps and creates RecoveryRequired/ObserveUnknown."
      ],
      "preservedBehavior": [
        "Preserve a 503 test and the redacted error detail.",
        "Intentionally replace boolean false/string error with a typed transport outcome and ambiguous-send handling."
      ],
      "replacementSteps": [
        "Add transport phase to the effect fixture and classify 503 before versus after DispatchStarted.",
        "Delete catch-all success/error conversion from the compatibility adapter.",
        "Keep ambiguous transport in observation/recovery; do not let it synthesize another provider leaf or continue a batch."
      ],
      "verificationCases": [
        "503 before dispatch produces a typed transport error and no DispatchStarted.",
        "503 after DispatchStarted leaves the controlled effect Unknown and schedules observation.",
        "A documented terminal refusal is the only status that produces Rejected."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "Phase-aware transport ambiguity is a valid effect-execution rule; it is scoped to the relayer leaf and is not a global BrokerError or mandatory handler behavior.",
      "contractAnchors": [
        "K07",
        "K08"
      ]
    },
    {
      "id": "MAP-A2A0451C27",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.spec.ts:315-359: close tests cover unknown pair/no position and a REST position whose close returns inputHash immediately"
      ],
      "currentBehavior": "Close resolves a pair, reads mocked open positions, returns an error for unknown pair/no position, and for one matching position posts a close request and reports immediate Submitted success with its inputHash.",
      "problem": "The test does not distinguish full close from partial close, does not bind the selected position to a stable scoped identity beyond the first REST match, and treats the submit receipt as success. Incomplete listing or response loss could be mistaken for no position or rejection.",
      "targetModel": "Define the Leverup close-all leaf with scoped PositionRef, ExposureSnapshot before-image, conflict key, commit criterion, evidence-bound CompensationCapability, and an explicit All amount. There is no Units/partial-close leaf while native support is unproven: an omitted legacy quantity is compatibility shorthand for explicit All because the native endpoint has no amount field, while a supplied quantity is rejected and never rewritten as All.",
      "pureTransitions": [
        "LegacyCloseInput(undefined quantity) -> Result<CloseAllInput, CloseTargetError>: map the source's no-amount call to explicit All; a supplied quantity selects the absent Units variant and is rejected.",
        "Complete ScopedPositionListing + CloseAllInput -> Result<CloseTarget, CloseTargetError>: require scope, instrument, positionHash, and complete coverage.",
        "CloseTarget + CloseAllCapability -> Result<PreparedClosePlan, PrepareFailure>: compile only the declared close-all schema.",
        "PreparedClosePlan + SigningAuthority -> Result<SignedClosePlan, SigningFailure>: dispatch returns Ack and observation remains separate."
      ],
      "effectBoundary": "Reader lookup and relayer POST are effects; selection and full/partial classification are pure. JournalWriter persists before-image, preconditions, conflict key, commit criterion, compensation classification, lock, DispatchStarted, and Observe job; no in-memory-only success.",
      "durability": "Persist positionHash/native lookup descriptor, scope, listing as-of/cursor, explicit All amount, plan digest, commit criterion, compensation class, and opaque inputHash. Do not drop quantity or position identity from the durable record.",
      "errors": [
        "UnknownInstrument(pair) and ScopeMismatch stop before remote effects.",
        "ListingIncomplete(missing) prevents a no-position conclusion.",
        "PartialCloseUnavailable is a compatibility/admission result for a missing leaf, never a conversion to All.",
        "A missing native undo guarantee is an explicit compensation limitation, not evidence of exact rollback.",
        "CloseAckOpaque(receipt) yields pending observation, not Filled/Committed."
      ],
      "preservedBehavior": [
        "Preserve unknown-pair/no-position rejection when complete scoped evidence supports it.",
        "Preserve full-close signing and relayer endpoint for the source's omitted-quantity call, represented as explicit All; any supplied quantity is not silently upgraded.",
        "Intentionally remove first-match/quantity-ignored behavior and immediate success semantics."
      ],
      "replacementSteps": [
        "Expose one close-all capability descriptor and keep partial close structurally absent until evidence.",
        "Replace first pairBase match with scoped positionHash selection and complete-listing checks.",
        "Persist close-all intent/dispatch/receipt and use durable observation instead of orderTracking Map."
      ],
      "verificationCases": [
        "A legacy close with omitted quantity maps to explicit All; a supplied partial quantity is rejected as an absent capability and causes zero relayer calls.",
        "Two positions for one display pair require explicit position identity; first-match selection is rejected.",
        "An incomplete reader page returns ListingIncomplete rather than no position.",
        "An explicit All close produces Ack/Submitted and remains pending until execution and residual-exposure evidence.",
        "Response loss after DispatchStarted yields Unknown and durable observation, not a second close."
      ],
      "openQuestions": [
        "Native conformance evidence has not established a partial-quantity close leaf for LeverUp. Until a typed endpoint/capability record proves Units semantics, the provider tree publishes close-all only and the legacy partial request is rejected before DispatchStarted/POST; explicit All remains pending until observation."
      ],
      "contractDisposition": "reframed",
      "contractRationale": "The source close path is a controlled effect, but only an explicit close-all leaf is justified now; partial quantity has structural absence until native evidence, with legacy rejection kept outside the target tree.",
      "contractAnchors": [
        "K02",
        "K04",
        "K08"
      ]
    },
    {
      "id": "MAP-50F8D30457",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.spec.ts:361-375: modify and cancel tests expect false/error outcomes"
      ],
      "currentBehavior": "Modify and cancel are tested as unsupported operations that return `success:false` and an error string; no remote call is expected.",
      "problem": "A boolean false cannot distinguish unsupported capability, invalid target, stale version, or transient transport failure. A caller may incorrectly treat it as a known broker rejection or silently drop attached protection/relation semantics.",
      "targetModel": "Do not add ModifyOrder or CancelOrder leaves to the Leverup capability tree: the source methods are legacy IBroker compatibility evidence only. The outer projection may return typed Unavailable/TargetUnknown without HTTP; no cancel-and-replace fallback or full unsupported handler set belongs in the target provider.",
      "pureTransitions": [
        "LegacyModifyOrCancelInput + LeverupCapabilityTree -> Result<CompatibilityOutcome, AbsentCapability|TargetUnknown>: no provider effect.",
        "RequestedFieldSet -> Result<PreservedInput, InputViolation>: retain requested semantics for diagnostics/future design.",
        "AbsentCapability -> CompatibilityProjection: do not create a dispatch, job, or cancellation state."
      ],
      "effectBoundary": "Capability-tree absence is pure. If native evidence later adds a leaf, it receives its own schema, receipt, idempotency, and observation contract rather than changing this compatibility branch.",
      "durability": "No target capability state is written for an absent leaf; an outer compatibility call may log a typed outcome without creating order/dispatch records.",
      "errors": [
        "AbsentModifyCapability(reason) and AbsentCancelCapability(reason) identify structural absence.",
        "TargetScopeMismatch or OrderVersionConflict requires caller re-resolution.",
        "TransportFailure is never collapsed to absence."
      ],
      "preservedBehavior": [
        "Preserve no remote mutation for current operations.",
        "Replace false/error text with typed unavailable variants and preserve requested fields for a future explicitly supported path."
      ],
      "replacementSteps": [
        "Remove modify/cancel from any generated target capability tree; retain source method mapping only at the legacy boundary.",
        "Migrate callers to discover the actual provider tree before invoking a leaf.",
        "Delete the cancel-and-replace suggestion from the compatibility message."
      ],
      "verificationCases": [
        "Calling the legacy method yields a typed absent-capability projection before DispatchStarted.",
        "An unknown or wrong-scope target is distinguishable from absent capability.",
        "No absent capability call produces a relayer POST or changes an order projection."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "Modify/cancel are source-level IBroker methods, not target Leverup leaves. The compatibility projection may explain their absence, but the new tree must not add full unsupported action handlers or cancel-and-replace.",
      "contractAnchors": [
        "K02",
        "K07",
        "K08"
      ]
    },
    {
      "id": "MAP-AC1BAD4D60",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.spec.ts:377-391: quote tests expect numeric Pyth price with bid=ask=last and unknown pair rejection"
      ],
      "currentBehavior": "The quote path fetches one Pyth price, converts it to a JavaScript number, and returns that value for bid, ask, and last; unknown pairs throw a broker error.",
      "problem": "A single oracle mark does not prove a two-sided quote or tradable spread. Number conversion can lose decimal precision, and the value lacks deployment, feed, confidence, and freshness evidence.",
      "targetModel": "Return a typed quote/mark data leaf containing InstrumentId, exact Decimal MarkPrice, Pyth feed/source, publish time, confidence/exponent, deployment, freshness, and a discriminated QuoteShape (`MarkOnly` or `TwoSided`) with explicit volume-known/unavailable status. A mark-only source exposes no bid/ask fields rather than duplicating the mark.",
      "pureTransitions": [
        "PythWire + InstrumentBinding -> Result<PriceObservation, QuoteDecodeError>: preserve integer/exponent Decimal semantics.",
        "PriceObservation + now + FreshnessPolicy -> Result<QuoteView, StaleMarketData>: classify freshness without inventing spread.",
        "Unknown pair -> UnknownInstrument, not a network call."
      ],
      "effectBoundary": "Pyth fetch is a pull data effect; decoding and freshness are pure. Quote failures or stale data do not acquire order locks, approval, compensation, or dispatch authority.",
      "durability": "Persist quote provenance only when a controlled effect explicitly selects it as evidence; the query cache is rebuildable and does not persist fabricated spread/volume.",
      "errors": [
        "PythMalformed or PythUnavailable prevents a quote.",
        "QuoteStale(asOf, now, maxAge) returns stale evidence, not a fresh quote.",
        "MarkOnly QuoteShape with VolumeUnavailable is explicit when only mark exists; TwoSided requires bid/ask evidence.",
        "UnknownInstrument(symbol) is a domain resolution error."
      ],
      "preservedBehavior": [
        "Preserve a 60000-style latest mark query for known symbols.",
        "Intentionally stop claiming bid=ask=last as a venue quote; keep mark-only semantics explicit."
      ],
      "replacementSteps": [
        "Replace Number conversion with Decimal/exponent codec and typed observation.",
        "Construct MarkOnly versus TwoSided only from evidence that supplies the corresponding fields; represent unavailable liquidity explicitly rather than with optional values.",
        "Carry feed/deployment/freshness metadata into projections and prepared price facts."
      ],
      "verificationCases": [
        "An oracle integer/exponent yields an exact Decimal mark and source timestamp.",
        "The quote leaf reports QuoteShape=MarkOnly and VolumeUnavailable rather than duplicating the mark as bid/ask.",
        "Unknown, stale, or malformed responses are typed data failures with no relayer request."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "Exact mark decoding and explicit bid/ask absence are the correct public quote/Candle data-unit treatment; it requires no account scope and introduces no order transaction or universal quote shape.",
      "contractAnchors": [
        "K03",
        "K04",
        "K05",
        "K12"
      ]
    },
    {
      "id": "MAP-B9BAFEC82F",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.spec.ts:393-431: account test expects 1000 USDC and one long BTC position from a mocked REST record"
      ],
      "currentBehavior": "Account reads combine a six-decimal chain balance with a REST position record; the fixture expects a 1000-USDC balance and a BTC long position with mapped quantity/margin fields.",
      "problem": "The combined response has no explicit AccountScope, block/as-of relationship, listing completeness, fee units, or valuation provenance. A position's margin and fees can be in different token/scale units, so net equity cannot be inferred safely.",
      "targetModel": "Compose AccountSnapshot from separate BalanceObservation and PositionListing data units, each carrying AccountScope/deployment, cursor or block, and freshness. Aggregate only explicit token/FX/multiplier facts; realized PnL and net liquidation remain Known/Partial/Unavailable data projections, never default numeric evidence.",
      "pureTransitions": [
        "ChainBalanceEvidence + TokenIdentity -> Result<Money<USDC>, BalanceDecodeError>.",
        "ReaderListingEvidence -> Result<PositionListing, PositionDecodeError>: retain unknown rows and coverage.",
        "AccountSnapshot -> Result<Valuation, ValuationUnavailable>: aggregate only same-currency/unit facts with explicit FX/multiplier observations."
      ],
      "effectBoundary": "Chain and REST reads are pull data effects; decoding, scope binding, and aggregation are pure and may run concurrently. No writer transaction, order approval, or compensation is involved in assembling the snapshot.",
      "durability": "Persist source cursors/as-of/block and snapshot provenance only as data observations; do not persist fake realized-PnL zero or use approximate valuation as a trading authorization.",
      "errors": [
        "BalanceDecodeError(field) and PositionDecodeError(field) identify malformed external data.",
        "ScopeMismatch(address) prevents mixing another account's position.",
        "FeeUnitUnknown(field) and ValuationUnavailable(reason) retain query data but block executable buying-power claims.",
        "ListingPartial(missing) prevents asserting no positions."
      ],
      "preservedBehavior": [
        "Preserve USDC 1000 and known BTC long field mappings as query projections when their evidence is valid.",
        "Change approximate valuation and zero-PnL assumptions into explicit unknown/observed valuation variants."
      ],
      "replacementSteps": [
        "Add scope/deployment/source metadata to the account assembly.",
        "Decode fees and position values with named unit parsers; leave unknown fee scale unaggregated.",
        "Replace raw aggregate fields with Valuation ADTs and stale/partial markers."
      ],
      "verificationCases": [
        "A six-decimal balance renders exactly 1000 USDC with block evidence.",
        "A REST row with unknown margin token is retained but cannot change USDC valuation.",
        "Partial reader coverage is visible and does not imply an empty set.",
        "A position from another wallet is rejected as ScopeMismatch."
      ],
      "openQuestions": [
        "Reader/native evidence has not established currency and scale for open, execution, funding, and holding fees. Until each fee field is decoded with token/unit/as-of evidence, preserve raw FeeEvidence and FeeUnitUnknown; do not subtract, aggregate, or synthesize zero."
      ],
      "contractDisposition": "retained",
      "contractRationale": "Separating chain balance and reader positions preserves independent data observations and their evidence; aggregation stays partial without spreading order approval or compensation into account reads.",
      "contractAnchors": [
        "K04",
        "K05"
      ]
    },
    {
      "id": "MAP-A3B8CD6164",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.spec.ts:433-471: getOrder tests place, observe a status toggle from Submitted to Filled, and return null for unknown ids"
      ],
      "currentBehavior": "After a place call the test obtains the in-memory inputHash record, then a configured status response can change it from Submitted to Filled; an unknown id returns null.",
      "problem": "The status fixture has only executed/success booleans and optional transaction text. It does not establish fill quantity, execution price, terminality, chain confirmations, or whether false is rejection. In-memory tracking disappears on restart.",
      "targetModel": "Use a durable scoped reference for the submitted open-effect and an Observation ADT: Acked, StillWorking, Rejected only with terminal evidence, Confirmed only with execution/fill evidence, and Unknown with reason. Compatibility OrderView is a projection of that provider leaf, not the source of truth.",
      "pureTransitions": [
        "DurableOrderRef + RelayerStatusEvidence -> Result<Observation, StatusInterpretationError>: apply protocol-specific evidence policy.",
        "Observation + FillFacts -> Result<OrderLifecycleProjection, ProjectionError>: Filled requires quantity/price/execution identity evidence.",
        "Unknown + now -> RecoveryClassification: ObserveUnknown or operator intervention, never silent null."
      ],
      "effectBoundary": "Status GET is a relayer observation effect owned by the controlled trading wrapper. Pure status classification consumes raw payload plus a versioned provider policy; ordinary data queries never enter this path.",
      "durability": "Persist inputHash/lookup descriptor, observation attempts, redacted status evidence, execution identity, and scope for the controlled effect; status after restart uses the descriptor rather than a Map.",
      "errors": [
        "UnknownOrderReference(ref) is NotFound only when a complete scoped listing/lookup proves absence.",
        "StatusSchemaViolation(field) blocks interpretation.",
        "StillWorking(ref) schedules observation.",
        "ExecutionEvidenceInsufficient(ref) remains Unknown/Unreconciled, not Filled.",
        "TerminalRejection(reason) is Rejected only with documented terminal semantics."
      ],
      "preservedBehavior": [
        "Preserve immediate Submitted projection after an unambiguous submit Ack.",
        "Preserve unknown-id null as a compatibility projection only when lookup evidence supports absence.",
        "Intentionally remove executed=true -> Filled absent fill evidence."
      ],
      "replacementSteps": [
        "Replace orderTracking Map with durable dispatch/order records and restartable lookup descriptors.",
        "Define status evidence policy and separate public order projection from raw relayer status.",
        "Update compatibility facade to map Unknown/StillWorking/Confirmed without boolean success shortcuts."
      ],
      "verificationCases": [
        "executed=false/success=false remains Unknown unless a versioned policy proves StillWorking.",
        "executed=true/success=true without fill quantity/price/execution evidence cannot project Filled or finality.",
        "After restart, the same lookup descriptor schedules observation without an in-memory map.",
        "A GET failure after DispatchStarted yields Unknown, not null or Rejected.",
        "Compatibility null is allowed only after complete scope-bound absence evidence."
      ],
      "openQuestions": [
        "Native conformance has not established executed/success terminal semantics, status retention, or transaction finality. Until a versioned policy plus execution/fill evidence exists, statuses remain Acked, StillWorking, or Unknown only as justified; failed GET/absence is Unknown and cannot produce Filled, Rejected, or Committed."
      ],
      "contractDisposition": "retained",
      "contractRationale": "Durable status observation is required for the submitted open-effect receipt, while terminal interpretation remains provider evidence-bound and does not turn the relayer into a universal order SDK.",
      "contractAnchors": [
        "K07",
        "K08",
        "K11"
      ]
    },
    {
      "id": "MAP-CA4DC0FDF9",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.spec.ts:473-486: capability tests expect CRYPTO_PERP, MKT, and an always-open market clock"
      ],
      "currentBehavior": "The compatibility capability object advertises CRYPTO_PERP and MKT and the broker reports the market as open without consulting a calendar or observation source.",
      "problem": "Static capability labels do not include deployment/scope/schema evidence, and `isOpen:true` conflates an always-available chain with the session policy required by an action. It can admit writes when readiness or venue session facts are unknown.",
      "targetModel": "Emit fixed CapabilityDescriptor metadata for the Leverup instance and compose only provider leaves actually declared by its tree (for example search, quote, account/positions, open, or close-all). Source CRYPTO_PERP/MKT labels remain compatibility metadata; each leaf carries scoped availability/evidence only when its schema is account-bound, while public leaves do not require AccountScope. Absent variants are omitted rather than represented by a universal feature matrix. MarketSession is a separate data/admission leaf.",
      "pureTransitions": [
        "ProviderManifest + RuntimeEvidence -> Result<CapabilityDescriptorSet, CapabilityEvidenceError>: validate fixed metadata and provider leaf schemas.",
        "CapabilityDescriptorSet + selected leaf input -> Result<CapabilityAvailability, CapabilityUnavailable>: require AccountScope only for an account-bound leaf.",
        "SessionEvidence + SessionPolicy -> Result<SessionDecision, SessionUnknown|SessionClosed>: keep session separate from capability presence."
      ],
      "effectBoundary": "Capability discovery and session lookup are effects; pure admission consumes immutable descriptors. A static method return cannot widen the provider tree or grant write authority.",
      "durability": "Persist descriptor revision/fingerprint with a controlled effect intent when it is consumed; rebuildable data projections may show degraded/unknown availability explicitly.",
      "errors": [
        "CapabilityEvidenceMissing(leaf) reports named availability for an existing leaf.",
        "SessionUnknown(calendarVersion) blocks a session-dependent effect without inventing Open.",
        "DeploymentCapabilityStale(revision) requires re-discovery/reprepare for the affected leaf."
      ],
      "preservedBehavior": [
        "Preserve the legacy MKT and CRYPTO_PERP labels only as source-declared compatibility metadata, subject to deployment/instrument evidence.",
        "Replace always-open with explicit session evidence and preserve unsupported fields as unavailable."
      ],
      "replacementSteps": [
        "Split getCapabilities into fixed outer metadata plus an open-ended provider tree.",
        "Define MarketSession/SessionPolicy separately from the legacy boolean.",
        "Bind each controlled effect to its leaf revision and invalidate only that leaf when evidence changes."
      ],
      "verificationCases": [
        "MKT appears usable only under matching scope, instrument, deployment, schema, and revision evidence.",
        "A session lookup failure returns SessionUnknown and no effect intent.",
        "A request for LIMIT, protection, or partial close discovers no corresponding Leverup leaf rather than being coerced.",
        "A leaf revision change invalidates only its unsubmitted intent and cannot reinterpret a prior payload."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "Capability reporting must emit fixed outer descriptors and only the open-ended Leverup leaves evidenced by this provider; static source labels are metadata/availability, not a global feature matrix or session authority.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K05"
      ]
    },
    {
      "id": "MAP-B100B9C54C",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.spec.ts:488-552: EIP-712 tests compare nested/flat open signatures and require equal nested/flat close signatures"
      ],
      "currentBehavior": "The tests establish that nested and flat open schemas produce different signatures, while nested and flat close schemas happen to sign the same fields and produce equal signatures. The broker starts nested and can mutate to flat after a failure.",
      "problem": "The equal close bytes do not make the primary type names or protocol variants interchangeable, and different open signatures make an unverified fallback unsafe after an ambiguous send. Tests only prove local hashing, not relayer acceptance or deployment identity.",
      "targetModel": "Make SigningDomain and SigningSchemaVariant immutable provider-leaf records in the prepared open/close effect. A SchemaCapability is evidenced before preparation; the signer selects one canonical primary type and no alternate variant is sent after DispatchStarted or an ambiguous response.",
      "pureTransitions": [
        "OpenData + SigningDomain + SchemaVariant -> Result<CanonicalTypedData, SigningCodecError>: encode the selected nested or flat fields and widths exactly.",
        "SignatureEvidence + DispatchAttempt + RelayerOutcome -> Result<Ack|Unknown|Rejected, DispatchEvidenceError>: treat signature generation as distinct from remote acceptance.",
        "PreparedPlan + SchemaCapability -> Result<FrozenSchemaPlan, SchemaMutationError>: reject any variant change after preparation."
      ],
      "effectBoundary": "Typed-data hashing is pure given domain/variant/time/nonce; private-key signing is an effect. Deployment/domain compatibility is checked before signing and persisted with the payload digest.",
      "durability": "Persist schema variant, primary type, domain fields, type fingerprint, plan digest, and dispatch identity. Do not mutate a prepared plan's variant or discard one signature after sending.",
      "errors": [
        "SigningSchemaUnavailable(variant, fingerprint) blocks prepare.",
        "SigningDomainMismatch(field) rejects before signing.",
        "SignatureGenerationFailed(ref) leaves no DispatchStarted.",
        "AmbiguousDispatch(identity) suspends; it cannot trigger a second schema send."
      ],
      "preservedBehavior": [
        "Preserve the nested-vs-flat differential vector and close field hashing vector as codec regression evidence.",
        "Intentionally remove runtime nested-then-flat fallback and document equal close digest as insufficient protocol proof."
      ],
      "replacementSteps": [
        "Define canonical schema/domain fingerprints as the provider leaf’s declared input contract.",
        "Compile one frozen variant into serializable native payload; remove mutable fallback from in-flight state.",
        "Add ambiguous response/crash cases that assert one observation path and no second schema POST."
      ],
      "verificationCases": [
        "Nested/flat open vectors differ and the plan records the one evidenced variant.",
        "Equal close bytes do not erase protocol metadata/variant mismatch.",
        "A timeout after nested send creates Unknown and exactly one observation path, not flat retry.",
        "Changing agent/chain changes the domain fingerprint and fails pre-dispatch."
      ],
      "openQuestions": [
        "The relayer accepted primary type remains unproven. Until schema capability evidence binds nested or flat (and its domain) before prepare, writes remain unavailable; no alternate-schema retry is allowed after DispatchStarted or an ambiguous response."
      ],
      "contractDisposition": "retained",
      "contractRationale": "Freezing the selected provider schema before signing and forbidding post-send fallback is unchanged; the schema is a leaf contract under the fixed descriptor, not a universal EIP-712 interface.",
      "contractAnchors": [
        "K03",
        "K07",
        "K08"
      ]
    },
    {
      "id": "MAP-B04EEC79F3",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.spec.ts:556-567: decimal conversion vectors cover 10dp quantity, 18dp price, and 6dp USDC amountIn"
      ],
      "currentBehavior": "Tests assert integer conversions for quantity at ten decimals, price at eighteen decimals, and collateral at six decimals.",
      "problem": "The vectors cover scale wiring but not unit identity, negative/zero/NaN input, excess precision, overflow, or rounding policy. A generic conversion can silently truncate an approved amount or be applied to the wrong field.",
      "targetModel": "Use nominal Quantity, Price, and Money<Token> values plus field-specific ScalePolicy as declaration-derived codecs. Preserve provider native integer widths and exact 10/18/6 vectors; rounding and overflow are explicit schema constraints, not a universal order enumeration.",
      "pureTransitions": [
        "DecimalString + QuantityScale -> Result<QtyWei, DecimalEncodingError>: preserve exact or declared ROUND_DOWN semantics.",
        "Price + PriceScale -> Result<PriceWei, DecimalEncodingError>.",
        "Money<Token> + TokenMetadata -> Result<AmountInWei, TokenScaleMismatch>: require the observed collateral token/decimals."
      ],
      "effectBoundary": "Decimal conversion is pure and allocation-free beyond Decimal's own representation. Pyth, chain decimals, and relayer calls are effects supplying facts; no conversion helper reads them implicitly.",
      "durability": "Persist canonical decimal strings and encoded values only when a controlled effect freezes them; pure query/data conversions remain ephemeral observations.",
      "errors": [
        "InvalidDecimal(field) and NonFiniteDecimal(field) reject input.",
        "NegativeOrZeroQuantity(field) rejects order preparation.",
        "PrecisionLoss(field, scale) rejects unless explicit policy permits it.",
        "TokenScaleMismatch(token, expected, observed) blocks amountIn encoding.",
        "IntegerOverflow(field) blocks native serialization."
      ],
      "preservedBehavior": [
        "Preserve the expected 10/18/6 valid vectors and ROUND_DOWN only where it is an explicit native policy.",
        "Add unit and boundary validation instead of relying on BigInt failure or silent truncation."
      ],
      "replacementSteps": [
        "Replace generic helper use with field-named codecs and nominal unit constructors.",
        "Thread observed token decimals and rounding/lot policy through preparation.",
        "Persist encoded values and policy in the canonical plan; do not recalculate on dispatch."
      ],
      "verificationCases": [
        "The documented quantity/price/amount vectors encode exactly at 10/18/6 scales.",
        "An 11th fractional quantity is rejected unless this provider leaf declares a named rounding policy and records the loss.",
        "Negative, zero, non-finite, and width-overflow values are rejected before signing.",
        "A seven-decimal token cannot be encoded as six-decimal USDC without identity evidence."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "Nominal units and field-specific codecs directly implement declaration-derived schemas and preserve the source 10/18/6 vectors without imposing order transaction machinery on pure conversion.",
      "contractAnchors": [
        "K03",
        "K04",
        "K08"
      ]
    },
    {
      "id": "MAP-50F0F7A825",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.spec.ts:569-575: salt test checks generated values use the expected format and differ on immediate calls"
      ],
      "currentBehavior": "`generateOpenSalt` hashes Date.now plus Math.random, and the test checks the result has a bytes32-like format and that two immediate calls differ.",
      "problem": "Global time/randomness makes pure planning nondeterministic and uniqueness is not durable. A process restart or collision can reuse a native identity, while the salt is absent from a journal-backed command/plan identity.",
      "targetModel": "Model OpenNonce as a serializable identity allocated only for the controlled Leverup open effect. The provider compiler accepts explicit nonce/time inputs and remains pure; other data or capability leaves do not inherit salt generation.",
      "pureTransitions": [
        "NonceInput + Scope + CommandId -> Result<OpenSalt, NonceBindingError>: derive or validate a bytes32 identity deterministically from explicit inputs.",
        "OpenSalt + PlanDigest -> Result<SignedNativePlan, SaltMismatch>: ensure the frozen salt equals the persisted plan value.",
        "NonceReservationState + CommandIdentity -> Result<DurableNonceDecision, NonceConflict>: accept one unique binding or return the existing same-digest receipt."
      ],
      "effectBoundary": "Nonce/time allocation is an effect boundary for open dispatch. The domain compiler receives Instant and nonce explicitly; the effect writer owns reservation and replay identity.",
      "durability": "Persist nonce/salt, command/plan digest, scope, allocation revision, and deployment only with the controlled open intent; no relayer deduplication or retention is inferred.",
      "errors": [
        "NonceUnavailable(scope) blocks preparation.",
        "SaltAlreadyBound(salt, command) is an idempotency conflict.",
        "SaltPlanMismatch prevents dispatch of tampered/reinterpreted data.",
        "DispatchUnknown(identity) requires observation rather than a fresh salt retry."
      ],
      "preservedBehavior": [
        "Preserve 32-byte salt shape and uniqueness intent for valid allocations.",
        "Replace Date.now/Math.random side effects with durable, explicit identity allocation."
      ],
      "replacementSteps": [
        "Inject Clock/NonceSource into the open-effect composition and pass outputs to pure signing.",
        "Record salt in the canonical open intent before dispatch.",
        "Remove ambient random generation from the adapter after migration."
      ],
      "verificationCases": [
        "Different command IDs receive distinct durable salts across restart.",
        "Replaying the same command returns the same salt/receipt.",
        "Response loss does not allocate a second salt or submit another schema variant.",
        "Malformed salt is rejected before signing."
      ],
      "openQuestions": [
        "Native relayer deduplication and salt-retention guarantees remain unproven beyond local bytes32 shape. Durable journal uniqueness/replay governs local identity; post-dispatch ambiguity stays Unknown/observe and never receives a new salt, and no relayer guarantee is claimed."
      ],
      "contractDisposition": "retained",
      "contractRationale": "Durable nonce allocation is retained for the open trading effect only; other provider capabilities may use their own identity scheme and do not inherit a salt or order WAL.",
      "contractAnchors": [
        "K08",
        "K12"
      ]
    },
    {
      "id": "MAP-D1A1D77A33",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.ts:1-9: module comment describes an IBroker adapter that signs EIP-712, posts to the OCT relayer, then polls"
      ],
      "currentBehavior": "The module is presented as a thin legacy IBroker adapter around signing, relayer submission, and polling, with wallet funds and relayer/Pyth fees described in comments.",
      "problem": "A module-level description is not an executable capability contract: it says nothing about AccountScope, deployment/schema compatibility, durable dispatch, response ambiguity, or which fee/chain assumptions are evidenced.",
      "targetModel": "Publish fixed CapabilityDescriptor metadata for Leverup plus an open provider capability tree whose leaves declare their own schemas, delivery, effects, resources, and availability. Keep reader/signer/relayer roles and funding/fee facts evidence-bound; do not invent a global ActionContractMap or full handler set.",
      "pureTransitions": [
        "ManifestEvidence -> Result<CapabilityDescriptorSet, ManifestViolation>: validate fixed outer metadata and each declared leaf.",
        "CapabilityDescriptorSet + LeafInput -> Result<LeafAdmission, CapabilityUnavailable>: admit only the selected provider leaf.",
        "RawRelayerResult + ProviderEvidencePolicy -> Result<Ack|StillWorking|Rejected|Unknown, ObservationError> for controlled trading leaves only."
      ],
      "effectBoundary": "Manifest loading is pure after composition supplies deployment facts. HTTP, chain, signer, reader, and cache implementations remain provider effects; the kernel does not branch on a broker-name switch.",
      "durability": "Persist manifest/leaf fingerprints with an intent or observation when a controlled effect consumes them; a comment cannot establish durability or remote finality.",
      "errors": [
        "ManifestUnsupported(version) blocks writes and requests an operator-visible capability diagnostic.",
        "EvidenceMissing(feature) returns Unavailable rather than a guessed success.",
        "FeeUnitUnknown(field) prevents accounting from treating comments as monetary facts."
      ],
      "preservedBehavior": [
        "Preserve the documented division between reader, signer, relayer, and polling roles.",
        "Replace prose capability claims with a typed, versioned manifest and explicit unavailable states."
      ],
      "replacementSteps": [
        "Create the fixed descriptor codec and provider-tree interpreter at the adapter boundary.",
        "Move chain/fee/schema claims into evidence records with source and freshness.",
        "Expose leaf-specific public handles; do not create empty handlers for absent native operations."
      ],
      "verificationCases": [
        "A manifest with an existing close-all leaf but no partial-close leaf exposes structural absence for partial close.",
        "A relayer acknowledgement produces Ack/pending observation, not Filled.",
        "A deployment/schema fingerprint mismatch blocks the selected effect before DispatchStarted."
      ],
      "openQuestions": [
        "No native conformance evidence currently supplies protocol version, accepted EIP-712 primary type, relayer idempotency/retention, status terminal semantics, chain finality, complete reader coverage, or fee units. Until supplied, the manifest exposes only read/degraded facts; write features are Unavailable, observations remain Unknown unless evidence supports StillWorking/Rejected, and fee fields are unaggregated."
      ],
      "contractDisposition": "reframed",
      "contractRationale": "The module comment becomes fixed outer capability metadata plus an open provider tree; it must not become a closed ActionContractMap or claim native guarantees absent from source.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K07",
        "K11"
      ]
    },
    {
      "id": "MAP-CB02240E08",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.ts:11-65: imports zod, Decimal, legacy IBKR types, legacy IBroker types, contract builder, native helpers, and viem PrivateKeyAccount"
      ],
      "currentBehavior": "The adapter imports legacy SDK contracts/orders/results and a concrete viem PrivateKeyAccount alongside native helper types; raw/native values therefore sit on the legacy broker boundary rather than behind one typed codec.",
      "problem": "Native SDK and credential types leak into the broker surface, so the target kernel would inherit structural casts, mutable SDK objects, and a private signer. This prevents serializable plans and makes runtime protocol validation diffuse.",
      "targetModel": "Keep one LegacyContractProjectionCodec and one versioned LeverupWireCodec at the adapter edge. Kernel-facing provider leaves use named Zod/JSON-Schema-derived ADTs and CredentialRef; PrivateKeyAccount, viem clients, Decimal implementation, and legacy SDK classes never cross the SPI.",
      "pureTransitions": [
        "UnknownWire -> Result<LeverupWire, ProtocolDecodeError>: parse once with an exact versioned schema.",
        "LegacyOrderProjection -> Result<OrderView, ProjectionError>: map every relevant field or report explicit loss.",
        "SigningIntent + SignerAddressEvidence -> Result<SignerBinding, CredentialBindingError>: verify the intended address before the isolated signer effect."
      ],
      "effectBoundary": "Zod/JSON Schema decoders are pure boundary code; the adapter interpreter alone constructs viem/legacy objects or calls external clients. No new universal SDK or Effect dependency is required by this provider.",
      "durability": "Prepared controlled-effect records contain encoded strings, descriptors, and fingerprints only; data observations contain redacted wire evidence. No SDK object, client, Decimal instance, or key is persisted.",
      "errors": [
        "ProtocolDecodeError(path) stops interpretation.",
        "LegacyProjectionLoss(field) prevents silently dropping protection/relation fields.",
        "CredentialUnavailable(ref) blocks signing without exposing a secret.",
        "SdkBoundaryViolation(type) is an integration defect."
      ],
      "preservedBehavior": [
        "Preserve the legacy IBroker projection during caller migration and Decimal arithmetic semantics at the adapter boundary.",
        "Remove SDK/private-key types from the new provider contract and centralize assertions in codecs."
      ],
      "replacementSteps": [
        "Define kernel-facing ports and named codecs.",
        "Convert legacy Contract/Order only at the public compatibility edge, then delete direct SDK imports from domain modules.",
        "Replace broker-held PrivateKeyAccount with CredentialRef plus Signer effect."
      ],
      "verificationCases": [
        "A malformed relayer body fails at one codec with a field-specific error.",
        "A prepared controlled-effect value serializes without SDK/client/key values.",
        "An order carrying unsupported TP/SL/relation fields reports explicit capability absence, not a stripped native request."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The adapter codec boundary remains necessary, but Zod/JSON Schema and provider-native codecs are sufficient; no new universal SDK or Effect dependency may leak into the kernel.",
      "contractAnchors": [
        "K03",
        "K07"
      ]
    },
    {
      "id": "MAP-BC274E8422",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.ts:69-78: OrderTrackingRecord stores inputHash, pair, side, Decimal qty, Submitted/Filled/Cancelled/Inactive status, and optional txnHash/reason"
      ],
      "currentBehavior": "Each submitted order is tracked only in an in-memory Map by inputHash. The record stores display pair, side, Decimal quantity, a coarse status, and optional transaction/reason text.",
      "problem": "The record has no AccountScope, canonical InstrumentId, plan/command identity, native lookup descriptor, schema/deployment fingerprint, before-image, attempt epoch, or evidence provenance. `Filled` can be assigned from booleans and all data vanishes on restart.",
      "targetModel": "Replace the process-local orderTracking Map for controlled Leverup effects with durable ScopedOrderRef and DispatchAttempt records: scope, InstrumentId, action/command ID, plan digest, provider lookup/inputHash, schema/deployment fingerprint, encoded quantity, and an Observation ADT. This durability is not applied to reader/quote data.",
      "pureTransitions": [
        "SubmittedPlan + RelayerAck -> Result<DispatchAttempt, AckDecodeError>: preserve opaque inputHash and request identity.",
        "DispatchAttempt + StatusObservation -> Result<OrderObservation, EvidenceInsufficient>: classify pending/unknown/terminal states.",
        "OrderObservation + FillEvent -> Result<OrderLifecycle, FillDecodeError>: only fill evidence can produce Filled."
      ],
      "effectBoundary": "Map updates become writer-owned journal/events/projections only inside the controlled trading wrapper; provider effects return typed evidence and pure status evolution consumes immutable records.",
      "durability": "A durable unique key covers scope + command/plan + attempt identity; an observation ledger retains late evidence. No in-memory map is authoritative, and no relayer idempotency guarantee is claimed.",
      "errors": [
        "OrderIdentityMissing(field) blocks persistence/dispatch.",
        "StatusEvidenceInsufficient(ref) stays Unknown or StillWorking.",
        "DuplicateCommand(payloadDigest) returns the prior receipt; conflicting reuse is IdempotencyConflict.",
        "LateEvidenceWrongEpoch is stored as evidence but cannot mutate current dispatch authority."
      ],
      "preservedBehavior": [
        "Preserve pair/side/quantity and inputHash correlation as redacted diagnostics.",
        "Intentionally remove coarse in-memory terminal statuses and map authority."
      ],
      "replacementSteps": [
        "Introduce durable order/dispatch schemas and migrate all map reads/writes to them.",
        "Add native lookup descriptors and scope/deployment/schema fingerprints.",
        "Map status only through the evidence policy and separate fill projection."
      ],
      "verificationCases": [
        "Restart retains the same inputHash and schedules observation for the controlled effect.",
        "An executed boolean without fill facts does not produce Filled.",
        "Two commands with one ID and different digest fail idempotency.",
        "A late status from an old lease cannot overwrite current dispatch authority."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The in-memory order map is genuinely execution authority for this source, so replacing it with durable effect records is retained; that durability is explicitly scoped to controlled trading effects.",
      "contractAnchors": [
        "K07",
        "K08",
        "K10"
      ]
    },
    {
      "id": "MAP-079143FA81",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.ts:83-96: zod configSchema accepts live/testnet and a privateKey regex; fromConfig parses then casts the key"
      ],
      "currentBehavior": "The config schema accepts an environment enum and a 64-hex private key; `fromConfig` parses an unknown record and casts the parsed private key into the broker config.",
      "problem": "Syntax validation neither resolves a credential safely nor proves environment/endpoint/chain/account identity. The cast erases the distinction between a config secret and an effect-only signer reference.",
      "targetModel": "Define canonical LeverupConfig as declared Environment plus CredentialRef and provider resource settings, not a key value. If legacy privateKey input remains at the compatibility edge, validate its 64-hex syntax there and discard it before canonical construction. Resolve ChainDeployment and, only for account-bound leaves, AccountScope evidence before marking the affected provider leaf available; keep credential-reference validation distinct from secret resolution.",
      "pureTransitions": [
        "UnknownConfig -> Result<LeverupConfig, ConfigViolation>: strict-decode environment and CredentialRef; a legacy private-key shape is checked only by the compatibility parser and never retained as canonical config.",
        "Config + CredentialRef -> Result<SignerBindingRequest, CredentialBindingError>: associate the configured reference with the declared account boundary.",
        "Config + RuntimeEvidence -> Result<DeploymentBinding, DeploymentMismatch>: compare chain, environment, agent, token, and endpoint identities."
      ],
      "effectBoundary": "Zod parsing is pure; secret-store resolution and deployment probes are effects. fromConfig constructs an identity/resource declaration, not a write-capable universal broker.",
      "durability": "Persist config/revision and CredentialRef identity only when binding a later capability or controlled effect; a raw key never enters plans, digests, logs, or receipts.",
      "errors": [
        "ConfigSchemaViolation(path) requests correction.",
        "CredentialUnavailable(ref) directs configured secret resolution.",
        "EnvironmentChainMismatch(expected, observed) blocks admission.",
        "ScopeDiscoveryPending prevents a default account selection."
      ],
      "preservedBehavior": [
        "Preserve live/testnet parsing and malformed-key rejection as syntax behavior.",
        "Replace private-key cast and implicit readiness with secret reference plus evidence-backed binding."
      ],
      "replacementSteps": [
        "Change schema output to named config ADT and CredentialRef.",
        "Add composition-time deployment evidence and optional account-scope discovery, then bind a per-leaf capability revision.",
        "Delete raw key from broker state and pass only signer effect to the native codec."
      ],
      "verificationCases": [
        "Malformed legacy private-key syntax is rejected at the compatibility boundary, while malformed CredentialRef syntax is rejected by its own parser; neither invokes secret-store or network effects.",
        "A valid credential with mismatched chain is not available for the signing leaf.",
        "Secret rotation changes CredentialRef binding without changing AccountScope identity."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "Config parsing and credential references are identity/resource boundaries; they establish provider availability without forcing every read or test path through an order plan.",
      "contractAnchors": [
        "K01",
        "K02",
        "K07"
      ]
    },
    {
      "id": "MAP-BA518AF53A",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.ts:100-128: class stores brokerEngine/id/label/meta/config/network, mutable clients, schemaVariant, and orderTracking Map"
      ],
      "currentBehavior": "The constructor stores broker engine metadata, config, network constants, nullable account/relayer/reader clients, an initialized flag, mutable nested schema variant, and an in-memory order map.",
      "problem": "This state combines pure identity, credentials, readiness, mutable protocol negotiation, clients, and execution authority. It cannot be serialized safely or recovered after a process crash, and it has no explicit scope.",
      "targetModel": "Make the adapter value hold immutable AdapterIdentity and declared deployment/resource requirements. Put clients/signers in a scoped runtime resource; put durable plans/dispatch only in the optional controlled trading wrapper. Capability/schema revisions are immutable snapshots and AccountScope is required where the provider data/effect is account-bound.",
      "pureTransitions": [
        "AdapterConfig -> LeverupAdapterIdentity: construct id/label/network metadata with no network side effect.",
        "AdapterIdentity + ResourceRequirements -> Result<RuntimeRequirements, ResourceFailure>: describe resources for the selected provider leaves.",
        "ReadyRuntimeFacts + LeafInput -> Result<NativeEffectInput, RuntimeInvariantViolation>: permit only the plan-bound deployment/schema without mutation."
      ],
      "effectBoundary": "Runtime resource acquisition, signer resolution, and client calls are effects. Pure action compilation and status evolution do not inspect nullable fields or process-local maps.",
      "durability": "Process-local resources are disposable. Durable records exist for an explicitly wrapped remote effect or for scoped observations; constructor state is never order authority.",
      "errors": [
        "RuntimeNotReady(reason) blocks action dispatch.",
        "ConfigRevisionChanged requires reprepare.",
        "SchemaRevisionConflict cannot mutate an existing plan.",
        "RecoveryRequired retains conflicts and operator ownership."
      ],
      "preservedBehavior": [
        "Preserve id/label/meta and network selection as immutable metadata.",
        "Remove mutable map and schema fallback from execution authority and separate resource lifetime from domain state."
      ],
      "replacementSteps": [
        "Split constructor, runtime resource, provider capability tree, and optional controlled-effect projections.",
        "Carry AccountScope through account-bound data/effect paths.",
        "Replace nullable-client checks with per-leaf readiness evidence and delete orderTracking/schema setter authority."
      ],
      "verificationCases": [
        "Constructing an adapter has no network side effect and no write capability.",
        "Dropping a runtime resource makes only the affected leaf unavailable while data projections remain explicit.",
        "A capability revision cannot reinterpret an existing controlled-effect payload."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The constructor must separate immutable provider identity from scoped resources, but durable dispatch data belongs only to the optional transaction wrapper, not to every adapter instance.",
      "contractAnchors": [
        "K01",
        "K07",
        "K08"
      ]
    },
    {
      "id": "MAP-5E91CFA930",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.ts:132-150: init derives the account, creates relayer/reader clients, probes getChainId against network constants, and marks initialized"
      ],
      "currentBehavior": "Initialization creates the signer account from the configured key, instantiates relayer and reader clients, calls only publicClient.getChainId, sets initialized, and logs the wallet address.",
      "problem": "A chain ID probe is not endpoint/account/scope readiness and does not verify the EIP-712 verifying contract or token addresses. A mutable initialized flag survives no durable evidence and can authorize unsafe writes.",
      "targetModel": "Use initializeLeverup to resolve CredentialRef, verify ChainDeployment endpoint identities, load catalog/schema evidence, and produce fixed CapabilityDescriptors with per-leaf availability. Discover AccountScope only for account-bound account/position/trading leaves; public catalog/quote data may be available without it. Initialization may yield degraded/read-only data leaves and never promises that absent trading leaves exist.",
      "pureTransitions": [
        "CredentialRef + Config -> Result<SignerBindingRequest, CredentialError>: produce a scoped request only for a signing leaf.",
        "DeclaredDeployment + ChainProbe/EndpointProbe -> Result<ChainDeployment, DeploymentMismatch>: compare observed chain and endpoint/address identities.",
        "ChainDeployment + optional ScopeDirectory + Manifest -> Result<CapabilityDescriptorSet, ReadinessFailure>: admit each leaf only from its own evidence and scope requirement."
      ],
      "effectBoundary": "Probes/client construction are effects under a resource scope. Pure readiness reduction checks supplied evidence; the controlled trading wrapper separately applies any recovery barrier before dispatch.",
      "durability": "Persist deployment/capability revision and scope evidence only when required by a data observation or controlled effect; initialization itself does not create a plan or mark an effect complete.",
      "errors": [
        "InvalidCredential(ref) blocks initialization.",
        "ChainDeploymentMismatch(field) blocks writes.",
        "EndpointIdentityUnavailable(endpoint) yields degraded/read-only capability.",
        "ScopeDiscoveryPending/Unavailable prevents write admission.",
        "SchemaCapabilityUnavailable blocks signing, not unrelated read-only queries."
      ],
      "preservedBehavior": [
        "Preserve malformed-key failure, client construction roles, and wallet-address diagnostic.",
        "Change one-chain-probe readiness to per-leaf identity/capability evidence, with scope discovery only for account-bound leaves."
      ],
      "replacementSteps": [
        "Replace initialized boolean with a capability availability ADT.",
        "Add endpoint/contract/address/schema probes and scope-directory binding.",
        "Move logging to a redacted startup observation and keep signer secret in credential service."
      ],
      "verificationCases": [
        "A chainId mismatch fails before a relayer effect.",
        "Healthy RPC with unavailable reader leaves account/position data degraded and open effect unavailable.",
        "Restart performs any controlled-effect recovery barrier before admitting that effect."
      ],
      "openQuestions": [
        "The source proves only an RPC chainId probe; separate reader/relayer health, account-scope directory, accepted-schema, and address-set evidence is absent. Until verified, public data leaves may be degraded while account/trading leaves remain unavailable; healthy RPC alone is insufficient."
      ],
      "contractDisposition": "reframed",
      "contractRationale": "Initialization composes endpoint/deployment evidence into capability descriptors and availability; source readiness remains one RPC probe, and no generic startup path may claim all leaves or write authority.",
      "contractAnchors": [
        "K02",
        "K07",
        "K11"
      ]
    },
    {
      "id": "MAP-61E3A9F0E4",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.ts:152-155: traderAddress returns initialized account.address"
      ],
      "currentBehavior": "The private helper returns the viem account address after `ensureInit`; this bare address is reused as the REST user path and native trader field.",
      "problem": "An address is not an AccountScope. Reusing it without AccountId/subAccountId and deployment binding permits cross-scope observations or a default subaccount assumption.",
      "targetModel": "Use ResolvedAccountScope with AccountId, explicit SubAccountId, wallet address, deployment, and scope-directory revision for account-bound capabilities. Native trader/reader paths are projections from this record; public catalog/Instrument/quote paths have no implicit account scope and reject any accidental scope mixing.",
      "pureTransitions": [
        "ScopeDirectory + account-bound RuntimeFacts -> Result<ResolvedAccountScope, ScopeFailure>.",
        "ResolvedAccountScope -> Result<NativeTraderAddress, AddressBindingError>: require deployment/address match for account-bound native requests.",
        "Address + account-bound reader observation -> Result<ScopedObservation, ScopeMismatch>; public data observations remain unscoped."
      ],
      "effectBoundary": "Address derivation/readers are effects at initialization; scope/address projection is pure. AccountScope is passed to account-bound actions, positions, and observations only; public catalog/Instrument/quote data remains unscoped.",
      "durability": "Persist scope and wallet binding with account-bound observations or controlled-effect records; a display subaccount label is not the durable identity. Public catalog/quote/instrument observations must not receive a fabricated scope.",
      "errors": [
        "ScopeDiscoveryPending and ScopeRequired block address projection.",
        "AddressNotBoundToScope rejects a mismatched wallet.",
        "DeploymentMismatch prevents using the address against another endpoint."
      ],
      "preservedBehavior": [
        "Preserve using the initialized wallet address for valid REST/native requests.",
        "Add explicit scope identity and stop treating address as a complete account key."
      ],
      "replacementSteps": [
        "Replace traderAddress with scope resolution and typed projections.",
        "Carry scope through reader URLs, native effect inputs, lookup descriptors, and observations.",
        "Reject callers that omit explicit subAccountId rather than defaulting."
      ],
      "verificationCases": [
        "A known wallet with no subaccount returns ScopeRequired for an account-bound leaf.",
        "The same address under two deployments yields distinct account bindings and cannot mix observations.",
        "An account-bound observation tagged with another scope is rejected; a public quote remains valid without AccountScope."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "Wallet address versus explicit account scope is required for account/position/trading capabilities and remains necessary there; public catalog, Instrument, Candle, and quote leaves must not invent an AccountId or subaccount from a wallet address.",
      "contractAnchors": [
        "K02",
        "K04",
        "K07"
      ]
    },
    {
      "id": "MAP-796DFD3470",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.ts:157-159: close is an async no-op because viem clients have no explicit close"
      ],
      "currentBehavior": "The legacy broker exposes `close()` but performs no action, relying on viem clients having no explicit close method.",
      "problem": "No-op compatibility hides effect-scope ownership and can leave fetch/worker resources alive. More importantly, closing a process must not erase durable jobs or assume remote dispatch stopped.",
      "targetModel": "Make adapter shutdown a scoped resource finalizer: stop admission to owned leaves, stop new resource claims, finish or classify in-flight effects, close HTTP/RPC resources when supported, and release the scope. Preserve durable recovery records only for controlled remote effects; data readers and queries simply release their resources.",
      "pureTransitions": [
        "RuntimeState + ShutdownRequest -> Result<ShutdownPlan, ShutdownFailure>: determine admission-stop, claim-stop, drain, and classification stages for owned resources.",
        "ShutdownPlan + ResourceState -> Result<ShutdownDecision, ResourceFailure>: finalize local fibers without inventing remote cancellation.",
        "DurableEffectState -> RecoveryClassification only when a controlled effect was started."
      ],
      "effectBoundary": "Finalization is effectful and owned by the composition/root runtime, not the pure provider. Shutdown must not perform hidden writes or remote cancellation.",
      "durability": "Durable jobs/leases remain only for owned controlled effects; closing a data resource releases local state and does not create order records.",
      "errors": [
        "ShutdownTimeout(worker) leaves a durable recovery marker.",
        "WriterDrainFailure stops clean completion and preserves authoritative state.",
        "RemoteOutcomeUnknown(identity) is not rewritten as cancelled by shutdown."
      ],
      "preservedBehavior": [
        "Preserve callable close API and no explicit viem client method assumption.",
        "Give close real scope semantics and preserve unfinished remote work for recovery."
      ],
      "replacementSteps": [
        "Implement a resource finalizer around adapter clients and any selected effect worker.",
        "Remove direct no-op close once the enclosing scope owns lifecycle.",
        "Add restart/shutdown scenarios only for provider leaves that can mutate remotely."
      ],
      "verificationCases": [
        "Closing after a submitted controlled dispatch retains its observation work while stopping new claims.",
        "Closing a data-only resource is idempotent and creates no order state.",
        "A shutdown timeout does not mark a remote effect Cancelled without evidence."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "Shutdown is a scoped resource finalizer. It preserves durable work only for owned controlled-effect workers and must not impose order cancellation/recovery semantics on data readers or fixture-only runtimes.",
      "contractAnchors": [
        "K05",
        "K07",
        "K08"
      ]
    },
    {
      "id": "MAP-6A6E5DA953",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.ts:161-165: ensureInit throws BrokerError(CONFIG) when initialized is false"
      ],
      "currentBehavior": "All operations call `ensureInit`, which throws a generic CONFIG broker error if the mutable initialized flag is false.",
      "problem": "The exception loses whether credentials, deployment, scope discovery, schema, capability, or recovery barrier caused unavailability. It also makes expected domain failure depend on exceptions rather than typed results.",
      "targetModel": "Replace ensureInit with requireCapability(runtime, leaf) returning a typed per-leaf availability result. Credential, deployment, scope, schema, resource, and recovery failures are explicit; the function does not manufacture a universal Action or PreparedPlanRecord.",
      "pureTransitions": [
        "RuntimeSnapshot + RequestedLeaf -> Result<CapabilityHandle, ReadinessFailure>: exhaustive readiness check for that leaf.",
        "ReadinessFailure -> PublicFailure: map to stable redacted protocol error.",
        "CapabilityHandle + LeafInput -> Result<PreparedLeafInput, PrepareFailure> only when the selected leaf requires preparation."
      ],
      "effectBoundary": "Resource access may use Effect at the adapter edge, but readiness/permission classification is pure and failed checks invoke no network or writer mutation.",
      "durability": "Store readiness evidence only when a caller accepts a controlled effect or scoped observation; no job/DispatchStarted is written for a pre-discovery failure.",
      "errors": [
        "CredentialUnavailable(ref) asks for configured credential resolution.",
        "ScopeDiscoveryPending and ScopeRequired distinguish missing scope from uninitialized runtime.",
        "RecoveryBarrierActive(caseId) blocks forward execution while retaining conflicts.",
        "CapabilityUnavailable(leaf) names the unavailable provider leaf without creating an empty handler."
      ],
      "preservedBehavior": [
        "Preserve pre-init rejection behavior and CONFIG-like diagnostic at the compatibility boundary.",
        "Replace throw/string with typed, phase-specific failure variants."
      ],
      "replacementSteps": [
        "Change callers to discover and handle the requested leaf’s typed availability.",
        "Map typed errors to legacy IBroker only at the outer projection.",
        "Ensure readiness does not bypass startup resource/recovery gates for controlled effects."
      ],
      "verificationCases": [
        "Calling place before its leaf is available returns a typed readiness failure with zero HTTP calls.",
        "A degraded quote-only runtime answers read data while open effect remains unavailable.",
        "A recovery barrier blocks a controlled write even when RPC probing succeeds."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "Readiness should return capability handles/availability for the requested provider leaf, not a universal action or PreparedPlanRecord; expected failures remain typed and effect-free.",
      "contractAnchors": [
        "K01",
        "K02",
        "K07"
      ]
    },
    {
      "id": "MAP-E8C004E0EC",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.ts:169-190: searchContracts uppercases static symbols and getContractDetails resolves only exact symbols",
        "services/uta/src/domain/trading/brokers/others/leverup/pairs.ts:8-10,41-43: high-leverage 500BTC/500ETH rows carry placeholder pairBase values"
      ],
      "currentBehavior": "Search uses the static pair array and case-insensitive substring matching; details returns a description for exact `localSymbol` or `symbol` matches and null otherwise.",
      "problem": "The static search/detail projection carries no catalog revision, deployment, coverage, canonical InstrumentId, or endpoint evidence; null details and empty search results therefore conflate unknown entries with an unavailable/partial registry. Resolution also accepts whichever localSymbol/symbol string is present without binding its pairBase/feed to a deployment.",
      "targetModel": "Use a versioned catalog/detail data tree with canonical InstrumentId, aliases, deployment/endpoint fingerprint, pairBase/Pyth binding evidence state (Verified|Placeholder|Unknown), category, and coverage. Detail lookup returns Known/Ambiguous/Unknown/Unavailable as a schema-derived result; placeholder/unknown detail may be displayed but does not imply a native effect capability, and only Verified binding can authorize native compilation.",
      "pureTransitions": [
        "CatalogSnapshot + Query -> ListingResult<InstrumentDescriptor>: pure normalization/matching preserving coverage.",
        "CatalogSnapshot + NativeKey -> Result<InstrumentDescriptor, InstrumentResolutionError>: exact canonical resolution.",
        "InstrumentDescriptor -> ContractDetailsView: adapter projection with provenance."
      ],
      "effectBoundary": "Catalog loading is effectful; matching and detail projection are pure. The adapter cannot invent a descriptor when details are absent.",
      "durability": "Persist catalog revision/native descriptor only when a controlled effect or account-bound lookup needs restartable identity; listing/detail projections are rebuildable data.",
      "errors": [
        "CatalogPartial(missing) and CatalogUnavailable(reason) are not null/empty matches.",
        "AmbiguousInstrument(alias, candidates) requires explicit selection.",
        "UnknownInstrument(symbol) is a domain failure.",
        "DeploymentMismatch(descriptor) blocks use across environments."
      ],
      "preservedBehavior": [
        "Preserve case-insensitive search and exact known detail behavior for documented pairs.",
        "Replace null/empty unavailable semantics with typed listing/resolution outcomes."
      ],
      "replacementSteps": [
        "Introduce catalog loader, coverage metadata, and canonical alias resolver.",
        "Migrate legacy ContractDescription to a redacted projection.",
        "Make provider effect preparation depend on InstrumentId/descriptor rather than symbol strings."
      ],
      "verificationCases": [
        "Known BTC/USD detail includes verified pairBase/feed/deployment provenance.",
        "500BTC/500ETH detail preserves its high-leverage marker and placeholder evidence but cannot authorize a native write.",
        "An incomplete registry returns Partial even when a symbol matches.",
        "An ambiguous alias returns candidates instead of first match.",
        "Unknown details make no Pyth/relayer request."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "Versioned catalog/detail resolution and typed coverage are the right public Instrument data tree; legacy null is an outer projection, and no account scope is required unless a caller composes an account-bound effect.",
      "contractAnchors": [
        "K02",
        "K04",
        "K05"
      ]
    },
    {
      "id": "MAP-796C0C5B19",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.ts:192-203: pairToContract builds a legacy CRYPTO_PERP/LEVERUP Contract with quote/localSymbol and a description",
        "services/uta/src/domain/trading/brokers/others/leverup/pairs.ts:8-10,41-43: 500BTC/500ETH pairBase entries are explicitly placeholder addresses rather than verified native deployments"
      ],
      "currentBehavior": "Known Leverup pairs are converted into SDK Contracts with CRYPTO_PERP, LEVERUP exchange, quote currency, local symbol, and a high-leverage/category description.",
      "problem": "The projection drops canonical InstrumentId, pairBase, Pyth feed, deployment, and capability evidence; a legacy Contract therefore cannot safely be used to create a native request.",
      "targetModel": "Define InstrumentDescriptor as the canonical record: InstrumentId, venue, category/synthetic-perp semantics, quote, deployment, pairBase/feed evidence state (Verified|Placeholder|Unknown), and explicit jurisdiction evidence (Known|Unknown). LegacyContractView is a read-only projection carrying an opaque descriptor key and provider-native extensions. Verified descriptors are required for native effects; placeholder/unknown descriptors remain displayable but cannot authorize a native request.",
      "pureTransitions": [
        "CatalogEntry + DeploymentEvidence -> Result<InstrumentDescriptor, BindingError>: validate native identities and their evidence states.",
        "InstrumentDescriptor -> LegacyContractView: project CRYPTO_PERP/LEVERUP metadata without making it authoritative.",
        "LegacyContractView + SelectedProviderLeafInput -> Result<NativePlan, MissingDescriptor>: require a verified canonical descriptor for the selected native leaf."
      ],
      "effectBoundary": "Pair mapping is pure. The SDK projection is an adapter boundary; no network or persistence is performed in this conversion.",
      "durability": "Persist descriptor/deployment identity, pairBase/feed evidence state, venue/category, jurisdiction evidence, and catalog revision only in a plan or lookup descriptor that needs restart; Contract display fields remain rebuildable projections.",
      "errors": [
        "PairBaseMissing/feedMissing or NativeIdentityUnverified blocks native compilation.",
        "LegacyContractOnly cannot dispatch.",
        "CategoryCapabilityUnavailable rejects unsupported asset behavior.",
        "DeploymentMismatch rejects a contract from another network."
      ],
      "preservedBehavior": [
        "Preserve CRYPTO_PERP, LEVERUP, quote/local symbol, and category/high-leverage display metadata.",
        "Stop treating the SDK Contract as sufficient native identity."
      ],
      "replacementSteps": [
        "Add canonical descriptor construction from catalog entries.",
        "Make pairToContract return a projection linked to InstrumentId/descriptor.",
        "Require descriptor in place/close/getQuote paths and retain native extension fields."
      ],
      "verificationCases": [
        "A known BTC/USD Contract projection round-trips to descriptor, venue/category, and verified pairBase/feed evidence.",
        "A manually constructed Contract with no descriptor cannot enter a native effect.",
        "500BTC/USD retains its high-leverage marker while placeholder pairBase evidence remains display-only and unavailable for native writes."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "Known pair-to-contract mapping is retained as a read-only projection linked to canonical InstrumentDescriptor, with native extensions preserved instead of erased into the legacy Contract.",
      "contractAnchors": [
        "K04",
        "K07"
      ]
    },
    {
      "id": "MAP-D8C97E1870",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.ts:205-209: resolvePair selects a static pair by localSymbol or symbol and returns undefined when absent"
      ],
      "currentBehavior": "Pair resolution tries `localSymbol` first, otherwise `symbol`, then exact-matches the static registry and returns undefined when no entry is found.",
      "problem": "Resolution uses localSymbol-or-symbol exact text and returns undefined on a miss, without catalog coverage, canonical identity, or deployment binding. The missing result then becomes a generic error, while a display string can be reused against a different pairBase/network.",
      "targetModel": "Resolve through a catalog data leaf with input variants (canonical InstrumentId, native pairBase, display alias) and Known/Ambiguous/Unknown/Unavailable outcomes, while preserving binding evidence state (Verified|Placeholder|Unknown). A display-known placeholder is not a verified native identity; AccountScope/ChainDeployment enter only when consumed by an account-bound effect.",
      "pureTransitions": [
        "InstrumentInput + CatalogSnapshot -> Result<InstrumentDescriptor, InstrumentResolutionError>: exhaustive input/alias handling and binding-evidence classification.",
        "Native pairBase + Deployment -> Result<InstrumentId, DeploymentMismatch|UnknownInstrument|NativeBindingUnverified>.",
        "Resolved descriptor + Scope -> Result<ScopedInstrument, InstrumentScopeMismatch>."
      ],
      "effectBoundary": "Resolution is pure after catalog evidence is supplied. Catalog I/O is separate; no fallback synthesizes pair data.",
      "durability": "Store resolved descriptor/catalog revision in any restartable effect lookup; pure catalog query does not require durable order state.",
      "errors": [
        "AmbiguousAlias(candidates) requires caller selection.",
        "UnknownInstrument(input), CatalogUnavailable(reason), and NativeBindingUnverified(state) remain distinct.",
        "InstrumentScopeMismatch prevents cross-scope use."
      ],
      "preservedBehavior": [
        "Preserve exact known pair resolution for localSymbol/symbol callers.",
        "Remove silent fallback to unspecified or synthetic identities."
      ],
      "replacementSteps": [
        "Implement typed resolver for aliases/native keys.",
        "Persist canonical InstrumentId/pairBase and deployment revision only for selected effect/data observations.",
        "Add alias ambiguity and partial-catalog handling."
      ],
      "verificationCases": [
        "A known localSymbol resolves to one verified descriptor.",
        "An alias with two candidates returns Ambiguous and makes no HTTP call.",
        "A placeholder 500BTC/500ETH pair remains displayable but is NativeBindingUnverified for writes.",
        "A pairBase from another deployment is DeploymentMismatch.",
        "Missing catalog evidence returns Unavailable rather than undefined-as-empty."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "Typed alias/native-key resolution preserves public Instrument data behavior; AccountScope is intentionally conditional and enters only when a resolved descriptor is consumed by an account-bound effect.",
      "contractAnchors": [
        "K02",
        "K04",
        "K05"
      ]
    },
    {
      "id": "MAP-90178E421A",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.ts:213-227: placeOrder ensures init, rejects non-MKT, resolves pair, and rejects only UNSET_DECIMAL quantity"
      ],
      "currentBehavior": "The implementation gates on initialized state and MKT, resolves the legacy Contract, then checks only the UNSET quantity sentinel before proceeding to Pyth/native request construction.",
      "problem": "The implementation-level checks omit AccountScope and target version, positive/range checks, Notional versus Units, time/session/protection/relation semantics, and deployment/schema capability. A valid-looking Decimal can still encode an unsafe request.",
      "targetModel": "Compile one Leverup Place/Open leaf through a typed admission pipeline: resolve AccountScope and InstrumentId, validate its exact sizing/order/protection fields, check context-derived leaf capability, then freeze a serializable provider payload. The source MKT gate is evidence for this leaf only, not globally writable MKT support.",
      "pureTransitions": [
        "LegacyOrder + ScopeDirectory -> Result<ResolvedPlaceInput, InputViolation|ScopeFailure>.",
        "ResolvedPlaceInput + LeafCapability -> Result<PreparedNativePayload, CapabilityUnavailable>.",
        "PreparedNativePayload + EffectFacts -> Result<DispatchIntent, PreconditionFailed>: no remote effect before durable acceptance."
      ],
      "effectBoundary": "Input checks and compilation are pure after facts are supplied. Reader/catalog/Pyth/chain observations are acquired outside writer SQL; only the optional controlled effect commits intent and dispatch.",
      "durability": "Persist command digest, scope, action fields, descriptor, and leaf revision only for the controlled open effect; failed preparation creates no DispatchStarted.",
      "errors": [
        "OpenLeafInputUnsupported(type) and InstrumentUnknown(symbol) are explicit compatibility/admission failures for this provider leaf.",
        "QuantityInvalid(reason) covers unset/zero/negative/precision/range.",
        "ScopeRequired or InstrumentScopeMismatch prevents routing.",
        "CapabilityUnavailable(leaf) preserves undeclared fields instead of dropping them.",
        "PreconditionStale(version/asOf) requires reprepare."
      ],
      "preservedBehavior": [
        "Preserve MKT gate, pair lookup, and no-HTTP behavior for malformed orders.",
        "Expand the gate to cover all action fields and typed scope/deployment evidence."
      ],
      "replacementSteps": [
        "Replace inline sentinel checks with boundary parsers for the declared open-leaf schema.",
        "Require explicit scope and canonical descriptor.",
        "Migrate legacy IBroker result mapping at the outer adapter only; do not create a global action map."
      ],
      "verificationCases": [
        "Non-MKT, unknown pair, and UNSET quantity fail without Pyth/relayer calls.",
        "A valid MKT input lacking scope fails ScopeRequired rather than selecting wallet by default.",
        "LIMIT/GTD/trailing/bracket inputs discover no corresponding leaf and emit no MKT request.",
        "Changing the open leaf revision yields ReprepareRequired."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source MKT/UNSET checks become admission for one declared Leverup open leaf. The target may compose shared Order units, but it must not imply universal MKT support or a closed order product.",
      "contractAnchors": [
        "K01",
        "K03",
        "K04",
        "K08"
      ]
    },
    {
      "id": "MAP-5EDC8088C6",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.ts:229-256: placeOrder fetches one Pyth price and builds OpenDataInput with hardcoded USDC/LVUSD, 1x qty*price, and field-specific decimal encodings"
      ],
      "currentBehavior": "The adapter fetches Pyth for the pair feed, chooses long for BUY, hardcodes USDC and LVUSD token addresses, computes `amountIn = qty * price` at 1x, and encodes amount/qty/price/TP/SL using 6/10/18 decimal helpers.",
      "problem": "The conversion assumes every pair is USD-collateralized, 1x, and representable by these token addresses; it does not retain price provenance, quote conversion, leverage, rounding, or scope. Pyth price is a Number upstream, and missing protection semantics can be silently encoded as zeros.",
      "targetModel": "Define provider-native OpenData as the exact input schema for the Leverup open leaf: pairBase, side, collateral TokenId, leverage/sizing policy, unit-qualified integers, PriceObservation, ProtectionSpec, and ChainDeployment address set. The selected controlled open plan additionally binds explicit preconditions, ConflictKey, CommitCriterion, and evidence-bound CompensationCapability; absent native undo evidence is recorded as None(reason), never asserted reversible. Only the controlled effect wrapper adds durable intent/dispatch/recovery.",
      "pureTransitions": [
        "PriceObservation + InstrumentDescriptor + Sizing -> Result<NativeSizing, QuoteConversionFailure>: use Decimal and explicit quote/collateral evidence.",
        "NativeSizing + ProtectionSpec -> Result<OpenDataInput, NativeFieldViolation>: zero means None only when encoded by a named variant.",
        "OpenDataInput + EffectAdmission -> Result<PreparedOpenLeaf, OpenAdmissionError>: freeze the selected preconditions, ConflictKey, CommitCriterion, and CompensationCapability.",
        "OpenDataInput + ScalePolicy -> Result<EncodedOpenData, DecimalEncodingError>."
      ],
      "effectBoundary": "Pyth and chain token metadata are data effects; pure compilation receives typed observations. Signing/relayer mutation is isolated to the optional trading effect and cannot refetch facts implicitly.",
      "durability": "Persist Pyth/update digest, publish time, token metadata, leverage, rounding, encoded fields, deployment fingerprint, preconditions, conflict key, commit criterion, and compensation class only in the frozen open intent. Query/data observations remain separate.",
      "errors": [
        "PythUnavailable/StalePrice(feed) blocks prepare.",
        "CollateralTokenUnknown or QuoteConversionUnavailable blocks amountIn.",
        "NativeFieldOverflow/PrecisionLoss rejects unsafe encoding.",
        "ProtectionEncodingUnavailable refuses requested TP/SL.",
        "CommitCriterionUnavailable or PreconditionsUnavailable blocks controlled admission; unknown compensation evidence cannot be promoted to a reversible claim, while native no-undo remains explicit CompensationCapability=None(reason).",
        "DeploymentAddressMismatch rejects hardcoded/incompatible addresses."
      ],
      "preservedBehavior": [
        "Preserve pair feed selection, BUY->long/SELL->short, 6/10/18 scales, and native OpenData field names.",
        "Remove hardcoded USD/USDC/LVUSD and silent 1x/zero-protection assumptions unless evidence-backed capability declares them."
      ],
      "replacementSteps": [
        "Create deployment-scoped token/address metadata and quote conversion policy.",
        "Replace Number arithmetic with Decimal and persist exact inputs/encoded values in the open intent.",
        "Model protection/leverage/sizing as provider schema variants and omit unsupported variants before dispatch.",
        "Build effect admission from the selected context and bind criterion/conflict/preconditions/compensation metadata before the writer accepts the intent.",
        "Treat Pyth bytes as an evidence reference, not an implicit retry refetch."
      ],
      "verificationCases": [
        "A non-USD or unsupported collateral pair fails QuoteConversionUnavailable rather than sending USDC.",
        "BUY encodes long and SELL encodes short with the same scoped instrument.",
        "A stale Pyth update fails before open dispatch.",
        "TP/SL None uses zero only when this leaf’s native capability documents that encoding; requested protection without evidence is absent/unavailable.",
        "A prepared open carries explicit criterion, preconditions, conflict key, and compensation class; missing criterion blocks before DispatchStarted and unknown undo evidence is never reported as reversible."
      ],
      "openQuestions": [
        "Native evidence is absent for per-pair collateral/token mapping, leverage semantics, the protocol meaning of zero TP/SL, and open commit/compensation semantics. Until descriptor/capability evidence exists, compilation returns CollateralTokenUnknown, QuoteConversionUnavailable, ProtectionEncodingUnavailable, CommitCriterionUnavailable, or PreconditionsUnavailable as applicable; it never assumes USDC/LVUSD, 1x, zero-protection semantics, or a native undo guarantee."
      ],
      "contractDisposition": "retained",
      "contractRationale": "Native open-data compilation keeps provider-specific collateral, leverage, protection, and exact fields as a precise leaf schema; only its controlled effect wrapper receives WAL/CAS/recovery.",
      "contractAnchors": [
        "K03",
        "K04",
        "K08"
      ]
    },
    {
      "id": "MAP-6786905A37",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.ts:258-274: placeOrder generates salt/deadline, builds OpenPositionMessage, and signs with mutable schemaVariant"
      ],
      "currentBehavior": "The broker generates a time/random salt, sets a deadline about 300 seconds ahead, puts OpenDataInput/trader/salt/deadline in a message, and signs according to mutable nested/flat state.",
      "problem": "Time and identity are read implicitly, the selected schema can change after preparation, and the signed message has no durable plan or deployment/domain fingerprint. A deadline or schema fallback cannot be reconstructed safely after crash.",
      "targetModel": "Use serializable provider-leaf signing intents: OpenSigningIntent carries explicit Instant/deadline, durable OpenNonce, AccountScope, SigningDomain, selected SchemaCapability, type fingerprint, and native payload digest; CloseSigningIntent carries explicit deadline, scoped positionHash, AccountScope, SigningDomain, selected SchemaCapability, type fingerprint, dispatch identity, and native payload digest, but no OpenNonce. Sign exactly one frozen leaf input.",
      "pureTransitions": [
        "OpenData + supplied Instant + OpenNonce + Deployment -> Result<OpenSigningIntent, SigningInputError>: compute bounded deadline and identity deterministically.",
        "CloseData + supplied Instant + PositionRef + Deployment -> Result<CloseSigningIntent, SigningInputError>: freeze positionHash/deadline/identity without inventing an open nonce.",
        "FrozenLeafSigningIntent + SchemaCapability -> Result<CanonicalTypedData, SigningSchemaUnavailable>: freeze one accepted schema/domain before signing.",
        "CanonicalTypedData + DispatchPhase -> Result<DispatchPermission, DispatchInvariantViolation>: allow the signer/send effect only for the persisted plan and epoch."
      ],
      "effectBoundary": "Clock allocation and open-nonce allocation are effects; typed-data construction is pure. The writer persists the controlled intent/DispatchStarted before relayer mutation.",
      "durability": "Persist OpenNonce only for open intents; persist close positionHash/deadline, domain/type fingerprints, schema variant, plan digest, scope, deployment, and encoded message for the controlled effect; retries never regenerate or cross-use these fields.",
      "errors": [
        "DeadlineInvalid/Expired requires reprepare.",
        "NonceAlreadyBound is an idempotency conflict.",
        "SigningDomainMismatch or SchemaUnavailable blocks before signing.",
        "CredentialUnavailable returns a typed failure without logging a key.",
        "AmbiguousDispatch forbids alternate-schema retry."
      ],
      "preservedBehavior": [
        "Preserve 300-second deadline intent, trader, salt, and full OpenPositionMessage for open.",
        "Preserve close positionHash/deadline identity without introducing an open-only nonce; replace implicit Date/random/mutable variant with explicit durable inputs and pre-send schema selection."
      ],
      "replacementSteps": [
        "Inject Clock/OpenNonceSource for open and compile a serializable OpenSigningIntent or CloseSigningIntent.",
        "Bind domain to ChainDeployment and schema fingerprint before signing.",
        "Delete mutable schema switching and route post-start failures to observation/recovery."
      ],
      "verificationCases": [
        "Supplied open clock/nonce yields deterministic open message and digest; close input yields deterministic positionHash/deadline intent with no OpenNonce.",
        "An expired intent fails before DispatchStarted.",
        "Schema mismatch cannot sign/send; ambiguous response never tries another variant.",
        "Changing verifyingContract or chainId changes domain fingerprint and is rejected."
      ],
      "openQuestions": [
        "The live accepted schema and protocol version remain unproven. Until a SchemaCapability is bound before signing, schema-unavailable blocks preparation/dispatch; the plan keeps one immutable variant, uses OpenNonce only for open, and routes an ambiguous post-start result to observation/recovery rather than an alternate schema."
      ],
      "contractDisposition": "retained",
      "contractRationale": "Explicit deadline/nonce/domain/schema inputs and immutable signing remain the correct provider effect boundary; the selected schema is a capability descriptor leaf, not a global variant switch.",
      "contractAnchors": [
        "K03",
        "K07",
        "K08"
      ]
    },
    {
      "id": "MAP-AFEC8DA518",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.ts:276-317: placeOrder POSTs sendOpenPosition, stores an in-memory Submitted record, and catches every error into success=false"
      ],
      "currentBehavior": "The signed request is serialized with decimal fields as strings plus Pyth bytes, posted to the relayer, and on any returned inputHash the broker inserts a Submitted record in a Map and returns success=true. Any thrown error is caught and represented as a string failure.",
      "problem": "Dispatch authority occurs outside durable writer control, the inputHash is not retained across restart, and generic catch logic cannot distinguish pre-send failure, ambiguous response, terminal rejection, or accepted-but-unobserved execution. The opaque receipt is not an order/fill proof.",
      "targetModel": "Use a durable dispatch protocol only for the Leverup open/close trading leaves: persist intent/receipt and locks, CAS DispatchStarted with epoch, send one exact native request, decode opaque RelayerReceipt, and create one observation path. Query/data leaves use ordinary effects and never enter this protocol.",
      "pureTransitions": [
        "SignedLeafIntent + DurableDispatchStarted -> Result<DispatchRequest, DispatchInvariantViolation>: construct exactly the persisted method/path/body.",
        "RelayerSubmitResponse -> Result<Ack, SubmitDecodeError>: validate only an opaque inputHash-shaped receipt.",
        "Transport/response evidence + attempt state -> Result<Ack|Unknown|Rejected, DispatchClassification>: use phase and explicit terminal evidence.",
        "Ack|Unknown + DispatchAttempt -> Result<ObserveJobIntent, JobDeduplicationConflict>: bind one observation identity."
      ],
      "effectBoundary": "Relayer HTTP and signer are effects. JournalWriter owns pre-send DispatchStarted and post-effect observation writes; pure classification uses raw evidence and attempt phase.",
      "durability": "Persist request digest/native payload reference, scope, deployment/schema fingerprints, epoch, inputHash if present, redacted response evidence, and observation job for the controlled effect; never overwrite a started attempt with generic rejected.",
      "errors": [
        "PreDispatchTransportFailure returns a typed failure with no DispatchStarted.",
        "AmbiguousSubmit(identity) enters Unknown/RecoveryRequired and suspends forward actions.",
        "RelayerResponseMalformed(field) is ProtocolViolation.",
        "AckOpaque(receipt) gives pending Submitted projection only.",
        "KnownTerminalRejection(reason) is Rejected only with explicit protocol evidence."
      ],
      "preservedBehavior": [
        "Preserve JSON field names, Pyth byte forwarding, inputHash correlation, and Submitted pending projection after a validated Ack.",
        "Remove in-memory tracking and all-errors->false conversion."
      ],
      "replacementSteps": [
        "Introduce the writer-owned boundary and epoch fencing around the selected trading leaf.",
        "Replace Map insertion with durable effect records and restartable observations.",
        "Split transport/error/observation codecs and redact response diagnostics.",
        "Ensure response loss never creates an alternate send."
      ],
      "verificationCases": [
        "A valid inputHash returns Ack/Submitted and writes durable lookup state.",
        "A crash after DispatchStarted before response leaves Unknown and one observation job.",
        "A malformed 200 body is ProtocolViolation, not success=false.",
        "A 503 after DispatchStarted does not continue or mark Rejected.",
        "Repeated observation of one inputHash is idempotent in the local journal."
      ],
      "openQuestions": [
        "Native duplicate-submit/idempotency, inputHash retention, and lookup guarantees remain absent. Until evidence exists, the journal persists one stable DispatchStarted identity and one ObserveJob; any post-boundary timeout or 503 is Unknown/RecoveryRequired and cannot redispatch with a new schema, salt, or request."
      ],
      "contractDisposition": "retained",
      "contractRationale": "Durable dispatch and opaque relayer acknowledgement are retained for the open/close effect leaves because the source can mutate remotely; no such machinery is applied to Pyth or reader data.",
      "contractAnchors": [
        "K07",
        "K08",
        "K12"
      ]
    },
    {
      "id": "MAP-04032EFDC4",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.ts:319-325: modifyOrder and cancelOrder always return false with explanatory strings"
      ],
      "currentBehavior": "Both methods are hard-coded to return failure: modify says cancel-and-replace is unsupported and cancel says market orders cannot be cancelled.",
      "problem": "The text result is not a capability ADT and does not identify target/version/transport state. A caller cannot safely distinguish current unsupported capability from a future endpoint, nor preserve requested relation/protection semantics.",
      "targetModel": "Represent ModifyOrder and CancelOrder as absent from the target Leverup tree. The source IBroker methods remain a compatibility mapping that returns typed AbsentCapability/TargetUnknown before HTTP; no cancel-and-replace leaf or full unsupported handler product is introduced.",
      "pureTransitions": [
        "LegacyModifyOrCancelInput + ProviderTree -> Result<CompatibilityOutcome, AbsentCapability|TargetUnknown>: no effect.",
        "RequestedFieldSet -> Result<PreservedInput, InputViolation>: retain semantics for diagnostics/future native design.",
        "AbsentCapability -> CompatibilityProjection: record no dispatch/job/terminal cancellation."
      ],
      "effectBoundary": "Capability absence is pure. If native evidence later adds a leaf, it receives its own endpoint codec, idempotency, and observation contract rather than mutating this source branch.",
      "durability": "The target writes no effect state for an absent capability; an outer compatibility adapter may return a typed result without a job.",
      "errors": [
        "AbsentModifyCapability(reason) and AbsentCancelCapability(reason) are expected compatibility outcomes.",
        "TargetScopeMismatch or OrderVersionConflict requests re-resolution.",
        "TransportFailure is never collapsed to absence."
      ],
      "preservedBehavior": [
        "Preserve no HTTP mutation for these methods.",
        "Replace false/error strings with typed unavailable outcomes and retain all requested fields for explicit future capability."
      ],
      "replacementSteps": [
        "Remove modify/cancel from generated provider capability output; retain source method handling only at the legacy edge.",
        "Migrate callers to discover the actual provider tree before invoking a leaf.",
        "Delete cancel-and-replace advice from compatibility text."
      ],
      "verificationCases": [
        "Legacy calls return typed absence before DispatchStarted.",
        "Wrong-scope target is distinguished from absent capability.",
        "No absent call produces a relayer POST or changes an order projection."
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source false/error branch is preserved as compatibility evidence, but absent modify/cancel leaves are represented structurally in the target tree; no full unsupported handlers or cancel-and-replace path are required.",
      "contractAnchors": [
        "K02",
        "K07",
        "K08"
      ]
    },
    {
      "id": "MAP-B53E29D3CC",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.ts:327-374: closePosition resolves a pair, picks first matching open REST position, ignores requested quantity, signs positionHash, and submits close without tracking"
      ],
      "currentBehavior": "Close resolves the display pair, fetches open positions, selects the first row whose pairBase matches, ignores the requested close quantity, signs positionHash/deadline, posts close, and returns a Submitted-like response. It does not add the result to orderTracking.",
      "problem": "This silently upgrades partial close to whole-position close and first-match selection is unsafe when multiple positions share a pair. No scope/listing completeness/version is recorded, and close receipt/status cannot be recovered from the Map. A successful inputHash is not proof of closure.",
      "targetModel": "Model the Leverup close-all leaf with scoped PositionRef, ExposureSnapshot before-image, conflict key, commit criterion, evidence-bound CompensationCapability, and the exact native close schema. The Units/partial-close leaf is structurally absent until native evidence: an omitted legacy quantity is compatibility shorthand for explicit All because the native endpoint has no amount field, while a supplied quantity is rejected and never rewritten as All.",
      "pureTransitions": [
        "LegacyCloseInput(undefined quantity) -> Result<CloseAllInput, TargetResolutionError>: map the source's no-amount call to explicit All; a supplied quantity selects the absent Units variant and is rejected.",
        "PositionListing + PositionRef -> Result<CloseTarget, TargetResolutionError>: require complete scoped evidence and exact hash.",
        "CloseAllInput + CloseAllCapability -> Result<PreparedClosePlan, PrepareFailure>: the provider tree has no Units/partial-close leaf until native evidence.",
        "PreparedClosePlan -> Result<SignedCloseRequest, SigningFailure>; writer dispatch/observe state evolves by epoch."
      ],
      "effectBoundary": "Reader lookup is a pull data effect; target selection and close-all preparation are pure. The optional close-all wrapper persists before-image, preconditions, conflict key, commit criterion, compensation classification, intent, DispatchStarted, Ack/Unknown, and observation by epoch.",
      "durability": "Persist scope, descriptor, positionHash, explicit All amount, reader cursor/as-of, plan digest, commit criterion, compensation class, schema/deployment, inputHash, and status evidence for the close-all effect only.",
      "errors": [
        "PositionListingPartial prevents no-position/target selection.",
        "PositionHashUnknown or PositionScopeMismatch blocks dispatch.",
        "PartialCloseUnavailable is a typed absent leaf, never All.",
        "A missing native undo guarantee is an explicit compensation limitation, not evidence of exact rollback.",
        "CloseDispatchUnknown requires observation/recovery and cannot be marked Closed.",
        "CloseTerminalEvidenceMissing prevents closure projection."
      ],
      "preservedBehavior": [
        "Preserve positionHash/deadline/signature and close relayer endpoint for the source's omitted-quantity call, represented as explicit All; any supplied quantity is not silently upgraded.",
        "Intentionally remove quantity-ignore, first-match, untracked receipt, and immediate closure semantics."
      ],
      "replacementSteps": [
        "Add only the close-all capability descriptor; keep partial close omitted until evidence.",
        "Replace pairBase first-match with scoped positionHash resolver and complete ListingResult.",
        "Persist close-all observation and migrate status queries to durable descriptors."
      ],
      "verificationCases": [
        "An omitted legacy quantity maps to explicit All; Units(quantity) produces absent-capability outcome and zero relayer calls; it is never rewritten as All.",
        "Two same-pair rows require explicit scoped positionHash.",
        "An incomplete page yields ListingPartial, not NoPosition.",
        "An explicit All close yields Ack/Submitted only after DispatchStarted and remains pending until execution/residual evidence.",
        "Response loss yields Unknown and one observation job."
      ],
      "openQuestions": [
        "Native partial-close support and close status/finality semantics remain unproven. Until native capability evidence exists, the provider tree publishes close-all only and a legacy Units request is rejected before dispatch; an All Ack is pending and requires observation/residual-exposure evidence before Closed."
      ],
      "contractDisposition": "reframed",
      "contractRationale": "Close-all remains a controlled effect leaf with durable recovery; Units/partial close is structurally absent until native evidence, not a mandatory Unsupported order variant.",
      "contractAnchors": [
        "K02",
        "K04",
        "K08"
      ]
    },
    {
      "id": "MAP-123F8DBC3D",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.ts:378-414: getAccount reads USDC balance and open positions concurrently, hardcodes six decimals, subtracts fee strings, and approximates net liquidation/PnL"
      ],
      "currentBehavior": "The account query runs balance and position reads in parallel, maps the balance as six-decimal USDC, sums margin, subtracts funding/holding fee strings, and returns approximate USD net liquidation with realized PnL set to zero.",
      "problem": "The parallel reads have no shared as-of/block, scope or deployment binding; fee strings have no proven currency/scale, and approximate net liquidation plus zero realized PnL are presented as facts. REST/chain disagreement is not represented.",
      "targetModel": "Build AccountSnapshot from BalanceObservation, PositionListing, and FeeEvidence data units, each carrying AccountScope/deployment and cursor/block/as-of. Valuation is Known/Partial/Unavailable; no query path receives order approval, compensation, or dispatch state.",
      "pureTransitions": [
        "BalanceEvidence + TokenMetadata -> Result<Money<Collateral>, BalanceDecodeError>.",
        "PositionListing + Scope -> Result<ScopedPositions, PositionDecodeError|ScopeMismatch>.",
        "EvidenceSet + ValuationFacts -> Result<AccountSnapshot, SnapshotConflict>: pure reconciliation of source times and units.",
        "AccountSnapshot -> Valuation ADT: never infer unsupported USD totals."
      ],
      "effectBoundary": "Chain/REST calls are data effects and may run concurrently outside any writer. Pure decoding, source reconciliation, fee aggregation, and valuation consume explicit observations; only a caller-selected trading effect may later use the snapshot as evidence.",
      "durability": "Persist source provenance and snapshot revisions as data observations. Do not store zero PnL as a financial event or use approximate values as an effect precondition.",
      "errors": [
        "BalanceSourceUnavailable(reason) may yield stale/partial snapshot, not empty account.",
        "FeeUnitUnknown(field) prevents fee subtraction.",
        "SnapshotAsOfConflict(chain, reader) marks valuation partial.",
        "ValuationUnavailable(reason) blocks risk checks.",
        "ScopeMismatch(address) rejects mixed-account data."
      ],
      "preservedBehavior": [
        "Preserve concurrent balance/position retrieval and six-decimal USDC projection when evidence matches.",
        "Replace hardcoded USD/zero-PnL assumptions with explicit valuation status and event-derived PnL."
      ],
      "replacementSteps": [
        "Define source-specific observation codecs and an account snapshot assembler.",
        "Add scope/deployment/as-of metadata and named fee unit handling.",
        "Remove approximate net liquidation and default realized PnL from the authoritative model.",
        "Expose legacy fields only as redacted projections with stale/partial markers."
      ],
      "verificationCases": [
        "A six-decimal balance renders 1000 USDC.",
        "Unknown fee currency cannot reduce USDC balance.",
        "Incompatible chain/reader as-of values produce Partial/Conflict, not seamless USD.",
        "No realized-PnL event yields Unknown/NotAvailable, not numeric zero evidence."
      ],
      "openQuestions": [
        "Native fee units and a reliable net-liquidation source remain unproven. Until typed observations exist, balance and position facts stay separate and valuation is Partial/Unavailable; no unqualified fee subtraction, default zero PnL, or approximate net liquidation may gate risk."
      ],
      "contractDisposition": "retained",
      "contractRationale": "Account snapshot assembly is a data-unit composition of balances, positions, and fee evidence; it remains outside order approval/compensation and refuses unsupported valuation rather than manufacturing it.",
      "contractAnchors": [
        "K04",
        "K05"
      ]
    },
    {
      "id": "MAP-0233341AE8",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.ts:416-448: getPositions decodes reader rows, maps pairBase to a Contract, uses entry price as market price, margin as market value, and zeroes PnLs"
      ],
      "currentBehavior": "The query obtains raw open positions, resolves pairBase against the static registry, silently skips rows with unknown pair, converts qty/entry/margin, uses entry price as market price and margin as market value, sets multiplier to one, and sets unrealized/realized PnL to zero.",
      "problem": "Silently dropping unknown rows makes the listing appear complete. Entry is not a current mark, margin is not necessarily market value, and one/zero valuation fields have no evidence. Position rows lack scope and source completeness.",
      "targetModel": "Decode reader rows into PositionListing Complete/Partial/Unavailable and PositionObservation carrying scoped InstrumentId, positionHash, side, quantity, entry/protection, collateral, fee evidence, and as-of. Unknown rows remain retained evidence; mark/valuation are separate data-unit projections and no reader result is an execution write.",
      "pureTransitions": [
        "RestPositionRecord + CatalogSnapshot -> Result<PositionObservation, PositionDecodeError|UnknownInstrumentEvidence>.",
        "RawPageSet -> PositionListing: retain cursor/page coverage and all decoded/undecoded rows.",
        "PositionObservation + Mark/FX/Multiplier facts -> Result<PositionValuation, ValuationUnavailable>.",
        "PositionListing + AccountScope -> Result<ScopedPositionListing, ScopeMismatch>."
      ],
      "effectBoundary": "REST fetch/pagination is a data effect; schema parsing, scope binding, and valuation are pure. Close target selection, if needed, is a separate controlled-effect composer.",
      "durability": "Persist raw evidence digest, page/cursor/as-of, scope, descriptor, positionHash, and decode outcome as observation data. Unknown rows remain visible for reconciliation; no order WAL is created by reading.",
      "errors": [
        "ListingPartial(missingPages) blocks absence claims.",
        "UnknownInstrumentEvidence(pairBase) retains row and blocks native close/valuation.",
        "MissingMarkObservation prevents market valuation.",
        "MultiplierUnknown prevents notional conversion.",
        "PositionDecodeError(field) stops only the affected projection while preserving raw evidence."
      ],
      "preservedBehavior": [
        "Preserve known row mapping for pairBase, side, qty, entry, margin, SL/TP, and positionHash.",
        "Replace silent skip and fabricated market/PnL values with partial/unknown typed outcomes."
      ],
      "replacementSteps": [
        "Introduce exact REST schema decoder and coverage-aware paginator.",
        "Carry scope/catalog/deployment identities through each observation.",
        "Separate entry/collateral facts from mark/valuation and PnL event folds.",
        "Let the close-all composer consume positionHash and complete listing evidence without adding transaction behavior here."
      ],
      "verificationCases": [
        "A known BTC row yields scoped long PositionObservation with ten-decimal qty, 18-decimal entry, hash, and as-of.",
        "Unknown pairBase remains retained in a Partial listing and is not silently dropped.",
        "No mark/FX/multiplier yields ValuationUnavailable rather than entry-as-market/margin-as-value/zero PnL.",
        "A second page is required for Complete when totalPages indicates it."
      ],
      "openQuestions": [
        "Reader pagination consistency/total-page guarantees and exact fee/PnL units remain unproven. Until complete cursor/page evidence and typed units exist, the listing is Partial/Unavailable, raw fields are retained, and neither absence nor valuation is inferred."
      ],
      "contractDisposition": "retained",
      "contractRationale": "Coverage-aware position decoding, retained unknown rows, and valuation ADTs are already aligned with K04/K05; close selection is a separate effect composer and is removed from this reader boundary.",
      "contractAnchors": [
        "K04",
        "K05",
        "K12"
      ]
    },
    {
      "id": "MAP-5D7C69A184",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.ts:450-492: getOrders loops in-memory tracked ids and getOrder polls relayer status, mapping executed/success to Filled/Inactive"
      ],
      "currentBehavior": "getOrders iterates the process-local orderTracking Map. getOrder returns null for unknown ids; for Submitted records it GETs relayer status, maps executed+success to Filled with txnHash, executed+failure to Inactive with reason, swallows status errors, and otherwise keeps Submitted.",
      "problem": "There is no durable listing/lookup, and booleans are treated as terminal financial status despite no fill details or documented finality. Swallowed errors make Unknown indistinguishable from an absent order; order listing cannot prove completeness.",
      "targetModel": "Define OrderListing Complete/Partial/Unavailable and provider-specific OrderObservation for the controlled trading effect. Persist NativeLookupDescriptor/status evidence; Confirmed requires execution/fill evidence, and Rejected requires terminal refusal evidence.",
      "pureTransitions": [
        "DurableOrderIndex + Cursor -> Result<OrderListing, ListingFailure>: enumerate persisted refs and external lookup namespaces.",
        "StatusWire + EvidencePolicy -> Result<OrderObservation, ObservationFailure>: preserve raw booleans and classify conservatively.",
        "OrderObservation + FillEvidence -> Result<OrderState, FillEvidenceMissing>: only then expose Filled/Committed.",
        "StatusTransportFailure + prior attempt -> Unknown: schedule observe/recovery."
      ],
      "effectBoundary": "Status/listing calls are adapter effects owned by the controlled trading wrapper; pure classification/lifecycle evolution consume explicit evidence. Data catalog/quote readers do not wait on GET inside SQL.",
      "durability": "Persist lookup descriptor, scope, plan digest, status attempts, transaction identity, evidence digest, cursor coverage, and projection revision for the effect; late observations use stable evidence identity.",
      "errors": [
        "OrderListingPartial(missingNamespace) is not empty.",
        "StatusDecodeError(field) blocks interpretation.",
        "StatusUnknown(ref, cause) remains Unknown and creates observation work.",
        "TerminalRejectionUnproven does not become Inactive.",
        "FillEvidenceMissing(ref) prevents Filled."
      ],
      "preservedBehavior": [
        "Preserve Submitted pending projection, txnHash/reason diagnostic fields, and legacy null for a genuinely complete unknown lookup.",
        "Intentionally remove Map-only authority, swallowed status errors, and boolean->Filled/Inactive mapping without evidence."
      ],
      "replacementSteps": [
        "Replace Map iteration with durable effect lookup descriptors.",
        "Implement typed status evidence and listing completeness for the provider’s actual namespaces.",
        "Map compatibility views only after durable observation/projection updates."
      ],
      "verificationCases": [
        "A status GET failure after DispatchStarted remains Unknown and queryable after restart.",
        "Executed/success without fill/finality evidence remains execution-unclassified.",
        "Only documented terminal refusal yields Rejected.",
        "A listing omitting conditional/held/external namespaces is Partial, not absence."
      ],
      "openQuestions": [
        "Native relayer status retention, namespace listing, terminal semantics, and finality remain unproven. Until conformance exists, durable refs observe by inputHash; missing/failed status is Unknown, incomplete listing is Partial, and booleans cannot produce Filled, Cancelled, or Rejected."
      ],
      "contractDisposition": "retained",
      "contractRationale": "Order listing/status is controlled-effect observation and therefore keeps durable lookup and evidence policy; its external namespaces and terminal meanings remain provider-specific and open-ended.",
      "contractAnchors": [
        "K07",
        "K08",
        "K11"
      ]
    },
    {
      "id": "MAP-481A2438E0",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.ts:494-514: getQuote fetches Pyth and returns bid/ask/last equal to a numeric price with zero volume"
      ],
      "currentBehavior": "The quote query resolves a pair, fetches Pyth, converts the price to a number, sets bid/ask/last equal, volume to zero, and timestamp to publishTime; BrokerError is rethrown through a generic catch mapper.",
      "problem": "The response fabricates a two-sided quote and zero volume from a mark-only oracle. Number conversion loses exactness, and generic catch mapping collapses stale, malformed, network, and unknown-instrument failures.",
      "targetModel": "Use a QuoteObservation data leaf with exact mark and a discriminated QuoteShape (`MarkOnly` or `TwoSided`), plus explicit volume-known/unavailable status, Decimal price/exponent, feed/source/as-of/confidence, deployment, and freshness. Map failures to typed data outcomes; no mark observation becomes a synthetic spread or trading transaction.",
      "pureTransitions": [
        "PythWire + Descriptor -> Result<QuoteObservation, QuoteDecodeError>: retain exact integer/exponent and confidence.",
        "QuoteObservation + FreshnessPolicy -> Result<QuoteView, QuoteStale>: expose mark-only shape explicitly.",
        "Failure -> Result<QuoteView, QuoteFailure>: preserve cause variant for caller policy."
      ],
      "effectBoundary": "Pyth fetch is a pull data effect; decoding/freshness/projection are pure. Query failures do not mutate order state or authorize dispatch.",
      "durability": "Persist quote provenance only when a controlled effect selects it as plan evidence; cache/projection retains source/as-of and stale marker without fabricated volume/spread.",
      "errors": [
        "InstrumentUnknown(symbol) prevents feed lookup.",
        "PythMalformed(field) and PythUnavailable(status) are typed query failures.",
        "QuoteStale(asOf) prevents use as a fresh precondition.",
        "MarkOnly QuoteShape and VolumeUnavailable are explicit variants when the source has no two-sided/liquidity evidence.",
        "TransportFailure(status) is not Rejected."
      ],
      "preservedBehavior": [
        "Preserve Pyth latest-mark query and publish timestamp for known instruments.",
        "Replace bid=ask and zero-volume fabrication with mark-only/unavailable variants and exact Decimal parsing."
      ],
      "replacementSteps": [
        "Create Pyth wire decoder and quote observation ADT.",
        "Expose typed failure mapping rather than BrokerError.from catch-all.",
        "Carry feed/deployment/freshness facts into quote and plan consumers."
      ],
      "verificationCases": [
        "A Pyth integer/exponent is returned as exact Decimal mark with timestamp.",
        "Bid/ask/volume remain unavailable without corresponding source evidence.",
        "Unknown/stale/malformed/network paths are distinguishable and send no relayer request."
      ],
      "openQuestions": [
        "The source provides no external bid/ask/volume stream and no verified freshness, session, or finality policy. Until those observations exist, the quote exposes QuoteShape=MarkOnly plus VolumeUnavailable; stale or malformed data is typed failure and cannot authorize writes."
      ],
      "contractDisposition": "retained",
      "contractRationale": "Pyth mark/quote observations remain non-transactional public pull data with explicit unavailable fields and freshness; they require no fabricated AccountScope and do not mutate or lock order state.",
      "contractAnchors": [
        "K03",
        "K05",
        "K12"
      ]
    },
    {
      "id": "MAP-47A0790F1B",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.ts:516-518: getMarketClock always returns isOpen=true with current Date"
      ],
      "currentBehavior": "The market clock method returns an always-open boolean and a locally generated Date without network, calendar, or deployment evidence.",
      "problem": "A local timestamp and true flag cannot establish venue session, regular versus extended policy, or clock provenance. It can satisfy a precondition while the chain/relayer is unavailable or a policy session is closed.",
      "targetModel": "Define MarketSession Open/Closed/Unknown with venue calendar version, session kind, instant, source, and freshness as a data/admission leaf. SessionPolicy is composed by a selected controlled effect only; it is not a universal always-open broker flag.",
      "pureTransitions": [
        "SessionQuery(venue, instrument, instant, calendarVersion) -> Result<MarketSession, SessionEvidenceFailure>.",
        "MarketSession + SessionPolicy -> Result<SessionAdmission, SessionClosed|SessionUnknown>.",
        "No session source -> SessionUnknown, never Open by default."
      ],
      "effectBoundary": "Clock/calendar lookup is a data effect; session decision is pure with explicit Instant and policy. Writer persistence is needed only when a controlled effect freezes this precondition.",
      "durability": "Persist session provenance in selected effect preconditions and invalidate on expiry; a local Date alone is not a durable market fact.",
      "errors": [
        "CalendarUnavailable(reason) returns SessionUnknown.",
        "SessionClosed(kind, at) rejects RegularOnly/extended policy as applicable.",
        "SessionStale(asOf) requires reprepare.",
        "DeploymentNotReady blocks venue session authorization."
      ],
      "preservedBehavior": [
        "Preserve a query method and explicit current instant in the projection.",
        "Intentionally remove unconditional isOpen=true and separate session from capability/readiness."
      ],
      "replacementSteps": [
        "Replace getMarketClock with calendar/evidence port and MarketSession ADT.",
        "Let only leaves that declare SessionPolicy consume the result.",
        "Map unknown/stale evidence to typed data/admission failure."
      ],
      "verificationCases": [
        "No calendar source returns Unknown rather than Open.",
        "A fresh regular-session observation admits RegularOnly only while valid.",
        "Extended-only evidence does not satisfy RegularOnly.",
        "Healthy public RPC without session evidence does not authorize a session-dependent effect."
      ],
      "openQuestions": [
        "No versioned LeverUp venue calendar/session evidence is present. Until configured or observed calendar evidence exists, MarketSession is Unknown and SessionUnknown blocks RegularOnly/ExtendedAllowed writes; the local always-open flag is not used."
      ],
      "contractDisposition": "retained",
      "contractRationale": "Session/calendar is a scoped public venue/instrument data and admission leaf, not an always-open universal broker flag; account scope is only added by a specific account-bound effect that consumes it.",
      "contractAnchors": [
        "K02",
        "K05",
        "K08"
      ]
    },
    {
      "id": "MAP-4AD307691B",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.ts:522-527: getCapabilities returns static CRYPTO_PERP and MKT capability lists"
      ],
      "currentBehavior": "The broker returns a static AccountCapabilities object advertising only CRYPTO_PERP security type and MKT order type, without account, deployment, schema, or evidence metadata.",
      "problem": "Static lists cannot represent capability revision, scope, sizing mode, protection/relation/close amount, session policy, or evidence expiry. They also make unsupported requests look like ordinary validation rather than an explicit capability decision.",
      "targetModel": "Use immutable fixed CapabilityDescriptor metadata plus an open provider tree. Public leaves such as instrument/catalog/quote can be keyed by deployment/provider scope without AccountScope; account/position/trading leaves declare AccountScope in their own schema when required. Each declared leaf carries its own exact input/result/error schema, HOF-derived types, CLI/AI metadata, delivery/effect/resource requirements, evidence, and availability; unsupported LIMIT/protection/partial-close concepts are omitted, not a global feature matrix.",
      "pureTransitions": [
        "Manifest + RuntimeEvidence -> Result<CapabilityDescriptorSet, CapabilityEvidenceError>: validate fixed metadata and declared leaves.",
        "CapabilityDescriptorSet + LeafInput -> Result<CapabilityDecision, Unavailable(leaf, reason)>: enforce AccountScope only when the leaf declares it.",
        "CapabilityDescriptorSet revision + PreparedLeafIntent -> ReprepareRequired for the affected leaf."
      ],
      "effectBoundary": "Capability discovery is an effect; snapshot validation and leaf admission are pure. A static method return cannot widen the provider tree or grant effect authority.",
      "durability": "Persist descriptor fingerprint/revision with any controlled intent or restartable observation; expired metadata does not rewrite historical outcomes.",
      "errors": [
        "CapabilityEvidenceMissing(leaf) reports unavailable existing leaf evidence.",
        "CapabilityExpired(revision) requests re-discovery/reprepare for that leaf.",
        "ScopeCapabilityMismatch(scope, leaf) blocks the affected leaf.",
        "SchemaVariantUnavailable(variant) blocks only the signing leaf."
      ],
      "preservedBehavior": [
        "Preserve the source method’s CRYPTO_PERP/MKT strings only as compatibility metadata pending context evidence.",
        "Replace static method authority with evidence-backed leaf descriptors and explicit unavailable/structural-absence outcomes."
      ],
      "replacementSteps": [
        "Replace AccountCapabilities as a closed product with a versioned provider manifest and leaf descriptors.",
        "Add per-leaf reasons/evidence references and preserve source labels only as compatibility metadata.",
        "Thread the selected leaf revision through its intent, journal, and observation when that leaf is a controlled effect."
      ],
      "verificationCases": [
        "CRYPTO_PERP/MKT is usable only under matching scope/deployment/instrument/schema evidence.",
        "LIMIT/attached-protection/partial-close discovery returns no corresponding leaf rather than omission-as-success or coercion.",
        "A leaf revision change invalidates an unsubmitted intent and cannot rewrite its native schema."
      ],
      "openQuestions": [
        "Per-instrument deployment support and accepted-schema evidence remain absent. Until a capability snapshot binds scope, instrument, deployment, schema, and revision, static CRYPTO_PERP/MKT are compatibility metadata only and each write feature is Unavailable with no dispatch."
      ],
      "contractDisposition": "reframed",
      "contractRationale": "The static capability method becomes fixed metadata plus composable provider leaves with per-instance availability. Unsupported fields are omitted unless a real leaf exists, avoiding a global feature matrix.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03"
      ]
    },
    {
      "id": "MAP-17D90B12AC",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.ts:531-547: getNativeKey accepts localSymbol/symbol and resolveNativeKey returns known Contract or synthesizes an unknown Contract",
        "services/uta/src/domain/trading/brokers/others/leverup/pairs.ts:8-10,41-43: high-leverage 500BTC/500ETH pairBase values are placeholders"
      ],
      "currentBehavior": "Known symbols resolve through the static pair registry to a Contract; unknown symbols are accepted by constructing a synthetic CRYPTO_PERP/LEVERUP Contract with no pairBase or Pyth binding.",
      "problem": "Synthetic identity can pass later type checks while lacking native routing data, deployment binding, and scope. It turns absence of catalog evidence into an apparently valid order/quote target.",
      "targetModel": "Define deployment-scoped NativeInstrumentKey and return KnownInstrumentDescriptor/UnknownInstrument/CatalogUnavailable while preserving pairBase/feed evidence state (Verified|Placeholder|Unknown). Contract views are projections carrying the canonical descriptor; unknown or placeholder synthetic Contracts cannot enter any provider leaf, and only Verified descriptors can authorize a native effect.",
      "pureTransitions": [
        "NativeInput + CatalogSnapshot -> Result<InstrumentDescriptor, InstrumentResolutionError>: exact known identity with binding evidence state.",
        "Known or placeholder descriptor -> LegacyContractView: project metadata without losing the descriptor link; only Verified may feed a native leaf.",
        "Unknown input -> UnknownInstrument evidence, never a native request."
      ],
      "effectBoundary": "Resolution is pure after catalog facts; legacy Contract projection is an adapter boundary. No network or writer mutation occurs during resolution.",
      "durability": "Persist descriptor, pairBase/feed, catalog/deployment fingerprint, and schema version only in effect/lookup records that need restart; catalog data queries can rebuild projections.",
      "errors": [
        "UnknownInstrument(symbol) directs explicit caller correction.",
        "NativeDescriptorIncomplete(field) or NativeBindingUnverified(state) blocks compilation.",
        "DeploymentMismatch(descriptor) rejects cross-environment reuse.",
        "CatalogPartial(missing) is not unknown-empty."
      ],
      "preservedBehavior": [
        "Preserve known localSymbol/symbol lookup and legacy Contract projection.",
        "Intentionally remove synthetic unknown fallback and make absence explicit."
      ],
      "replacementSteps": [
        "Delete fallback constructor and create a Result-returning resolver.",
        "Carry descriptor identity through place/close/quote/order/position paths.",
        "Migrate callers to typed unknown/unavailable handling."
      ],
      "verificationCases": [
        "FAKE/USD returns UnknownInstrument with no Pyth/relayer request.",
        "Known BTC/USD returns verified pairBase/feed and matching deployment descriptor.",
        "500BTC/500ETH returns a display descriptor with placeholder evidence and no native write path.",
        "A descriptor from another chain deployment is rejected even with the same symbol."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "Deployment-scoped native identity and removal of synthetic unknown Contracts are retained; canonical Instrument is a shared public data unit, while an AccountScope is added only if a later account-bound effect schema requires it.",
      "contractAnchors": [
        "K02",
        "K04",
        "K07"
      ]
    },
    {
      "id": "MAP-406869F527",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.ts:551-555: _account calls ensureInit then returns the PrivateKeyAccount"
      ],
      "currentBehavior": "The provider-specific helper exposes the initialized viem PrivateKeyAccount to callers after the generic readiness check.",
      "problem": "Exposing the concrete signer object makes credential lifetime and secret use part of the broker API. It cannot cross a serializable plan boundary and can be accidentally logged, retained, or used without scoped authorization.",
      "targetModel": "Replace _account with an internal SignerPort accepting an authorized immutable SigningIntent for a controlled provider effect. Public/domain code sees AccountId/address evidence and CredentialRef, never PrivateKeyAccount.",
      "pureTransitions": [
        "SigningIntent + SignerAddressEvidence -> Result<SignerBinding, CredentialBindingError>: verify the credential address against scope and deployment before signing.",
        "SignerBinding + AccountScope -> Result<ScopedSignerIdentity, ScopeMismatch>: expose only address evidence and scope, not a key-bearing object.",
        "SignedPayload + PlanDigest -> Result<SignedPlanEvidence, DigestMismatch>: retain signature evidence only when it matches the frozen plan/domain."
      ],
      "effectBoundary": "Credential retrieval and signTypedData are effects at the adapter boundary. Pure provider/data modules construct and validate intent but never inspect viem account objects.",
      "durability": "Persist CredentialRef identity, signer address evidence, and signature/domain digest only under the controlled effect policy; never persist key/account object.",
      "errors": [
        "CredentialUnavailable(ref) blocks signing.",
        "SignerAddressMismatch(scope) rejects use of a key for another account.",
        "PlanDigestMismatch prevents signing altered native data.",
        "CredentialRotation does not create a new AccountId."
      ],
      "preservedBehavior": [
        "Preserve wallet-address derivation and EIP-712 signatures for valid authorized intents.",
        "Remove concrete account exposure and raw signer retention from the broker surface."
      ],
      "replacementSteps": [
        "Introduce SignerPort/SigningIntent and migrate all `_account` callers.",
        "Delete `_account` after provider-specific consumers move to address evidence/sign effect.",
        "Keep credential resolution in configured secret store and redact all diagnostics."
      ],
      "verificationCases": [
        "A signer produces a signature for an authorized intent without key material in serialized evidence.",
        "A key resolving to another address fails SignerAddressMismatch.",
        "Credential rotation keeps AccountScope but cannot re-sign altered old intent."
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "Signer isolation is a controlled-effect boundary: address evidence and CredentialRef are public, while the provider-native signer remains an implementation detail of the adapter process.",
      "contractAnchors": [
        "K07",
        "K08"
      ]
    },
    {
      "id": "MAP-96C363D9C7",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.ts:557-560: _setSchemaVariant mutates the broker's nested/flat schema variant"
      ],
      "currentBehavior": "A provider-specific helper changes the mutable schemaVariant field used by subsequent open/close signing.",
      "problem": "Mutation permits a caller or fallback path to reinterpret the same prepared request after authorization. It also allows a schema switch after an ambiguous relayer send, creating duplicate/invalid submissions.",
      "targetModel": "Make SchemaCapability immutable and select SchemaVariant during preparation of the affected Leverup signing leaf. Store type/primary/domain fingerprints in the serializable effect intent; no helper mutates an in-flight plan or sends an alternate variant.",
      "pureTransitions": [
        "SchemaEvidence + ChainDeployment -> Result<SchemaCapability, SchemaUnavailable>.",
        "OpenIntent + SchemaCapability -> Result<CanonicalTypedData, SigningSchemaMismatch>.",
        "AmbiguousDispatch + SchemaVariant -> Unknown/Observe: never compile/send another variant automatically."
      ],
      "effectBoundary": "Variant selection is pure from immutable provider evidence; signer/relayer are controlled effects. Runtime changes create a new capability revision and new intent, not mutation.",
      "durability": "Persist variant, primary type, field/type fingerprint, deployment/domain, and plan revision with the controlled effect; no data reader receives schema-switch state.",
      "errors": [
        "SchemaUnavailable(variant) blocks prepare.",
        "SchemaMutationForbidden(planRevision) rejects changes to a prepared plan.",
        "AmbiguousDispatch forbids fallback.",
        "PlanVersionUnsupported requires operator/reprepare path."
      ],
      "preservedBehavior": [
        "Preserve local nested/flat vector comparison for protocol codec tests.",
        "Remove mutable setter as execution control and disallow automatic nested-to-flat retry."
      ],
      "replacementSteps": [
        "Delete _setSchemaVariant as execution control after callers migrate.",
        "Add schema capability/version evidence and frozen fingerprint fields to the provider leaf.",
        "Route post-start failures to observation/recovery, never variant switch."
      ],
      "verificationCases": [
        "Changing variant after prepare leaves serialized intent unchanged and rejects mutation.",
        "Timeout after nested send creates Unknown with no flat POST.",
        "A new capability revision can prepare a new command while the old intent remains bound."
      ],
      "openQuestions": [
        "The live relayer accepted primary schema remains unproven. Until evidence binds one SchemaCapability before prepare, SchemaCapabilityUnavailable blocks signing; the immutable plan cannot switch variants, and post-start ambiguity uses observation/recovery rather than an alternate schema."
      ],
      "contractDisposition": "retained",
      "contractRationale": "Immutable schema capability and no post-send variant fallback remain required for this provider leaf, without creating a global schema registry or universal order handler.",
      "contractAnchors": [
        "K03",
        "K07",
        "K08"
      ]
    },
    {
      "id": "MAP-BC7663242A",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.ts:562-566: _fetchOpenPositionsRaw returns ReaderClient REST records after ensureInit"
      ],
      "currentBehavior": "The helper exposes raw reader records after initialization, with no schema decode, pagination completeness, account-scope binding, or retention policy at this boundary.",
      "problem": "Raw records and casts allow unknown fields/units and silently discard incomplete pages downstream. Consumers can mistake a page of OPEN rows for a complete position set and use unscoped data for close/valuation.",
      "targetModel": "Keep a private ReaderPort that decodes exact wire records into PositionListing Complete/Partial/Unavailable, scoped PositionObservation, raw-evidence digest, source cursor, and explicit fee/valuation unknowns. Do not export raw SDK/REST objects or close/dispatch semantics from this data boundary.",
      "pureTransitions": [
        "RawReaderBody -> Result<ReaderPage, ReaderDecodeError>: exact field/status validation.",
        "ReaderPageSet + CursorEvidence -> PositionListing: retain coverage and undecoded rows.",
        "PositionListing + AccountScope -> Result<ScopedPositionListing, ScopeMismatch>.",
        "PositionListing -> Result<PositionDataProjection, ValuationUnavailable>: keep valuation separate from execution."
      ],
      "effectBoundary": "REST fetch/pagination are data effects; schema decoding, scope binding, and listing classification are pure. A separate close-all composer may consume the typed listing, but the reader does not create effect state.",
      "durability": "Persist source endpoint, page cursor/total coverage, as-of, scope, raw digest, and decode result as data observations. Retain raw evidence for reconciliation without exposing untrusted fields as authority.",
      "errors": [
        "ReaderDecodeError(field) blocks projection.",
        "ListingPartial(missingPage) prevents no-position conclusions.",
        "ReaderScopeMismatch(address) rejects cross-account data.",
        "FeeUnitUnknown(field) blocks valuation.",
        "ReaderUnavailable(reason) is not an empty list."
      ],
      "preservedBehavior": [
        "Preserve reader REST as the source for open-position queries and known record fields.",
        "Replace raw export and page-0 assumptions with typed, coverage-aware observations."
      ],
      "replacementSteps": [
        "Move decoding into adapter boundary and add pagination/cursor evidence.",
        "Replace _fetchOpenPositionsRaw callers with PositionListing/PositionObservation APIs.",
        "Keep close target selection and controlled dispatch in their owning effect entries, not in ReaderPort."
      ],
      "verificationCases": [
        "Malformed row yields field-specific decode result and retains redacted digest.",
        "Multi-page response is Complete only after every declared page/cursor boundary validates.",
        "Unknown pair remains visible as UnknownInstrumentEvidence/Partial, not silently filtered.",
        "A data projection with incomplete listing cannot assert absence or valuation."
      ],
      "openQuestions": [
        "Reader pagination consistency/total-page guarantees and fee-unit semantics remain unproven. Until independent reader conformance, ReaderPort returns Complete only after validated declared pages/cursor boundaries; otherwise it returns Partial/Unavailable, retains raw fee evidence as FeeUnitUnknown, and makes no absence or valuation claim."
      ],
      "contractDisposition": "reframed",
      "contractRationale": "Raw reader exposure is replaced by a private data reader/stream codec only. Pagination, scope, and fee evidence stay data concerns; close targeting and transaction state belong to a separate effect composer.",
      "contractAnchors": [
        "K03",
        "K05",
        "K07"
      ]
    }
  ],
  "crossGroupContracts": [
    "C01 AccountScope (conditional): Leverup wallet-address readers and trading effects require explicit AccountId plus SubAccountId once their concrete schema is account-bound; public Instrument/Candle/quote/catalog leaves remain Public and must not invent a wallet or subaccount. Scope is carried on account facts, orders, positions, locks, and receipts only where applicable.",
    "C02 SigningDomain + ChainDeployment: eip712.ts:43-49 builds name/version/chainId/verifyingContract, while network constants select RPC, reader, relayer, token and pair addresses. A controlled order effect binds these native identities and its provider-declared native fields to one deployment/domain fingerprint before signing; this does not impose cross-provider order combinations.",
    "C03 Schema evidence: eip712.ts:4-10 documents conflicting nested and flat open schemas and :54-97 defines different primary types. Select one evidenced SchemaVariant in the relevant provider leaf, persist its type fingerprint, and never retry another variant after DispatchStarted or an ambiguous response; equal close bytes do not prove interchangeability.",
    "C04 Relayer evidence: relayer-client.ts:42-52 exposes opaque inputHash plus executed/success booleans, and :74-93 uses a local timeout. A controlled trading leaf treats submit as Ack only, not fill/finality; false is not Rejected without terminal evidence, and response loss after DispatchStarted is Unknown with durable observation.",
    "C05 Close amount: LeverupBroker.ts:327-374 ignores requested quantity and picks the first matching pair. The provider tree currently has only an explicit close-all leaf because the native endpoint has no amount field: legacy omitted quantity maps to All, while supplied quantity selects an absent Units leaf and is rejected before DispatchStarted; close-all still requires scoped positionHash and complete account-bound evidence.",
    "C06 Listing completeness: reader-client.ts:80-90 fetches only page=0 and filters OPEN. Account-bound PositionListing is Complete/Partial/Unavailable with cursor/page evidence; an omitted page or unknown row cannot become no-position/NotFound evidence, and public data listings use their own declared scope.",
    "C07 Units and valuation: decimals.ts:15-28 distinguishes qty 10dp, price 18dp and collateral scale with ROUND_DOWN; reader-client.ts:38-49 does not establish fee currency/scale. Provider-declared native fields use nominal Quantity/Price/Money units and retain fee/valuation unknowns rather than subtracting unqualified strings; no cross-provider precision policy is inferred.",
    "C08 Testnet deployment: pairs.ts:62-68 aliases TESTNET_PAIRS to MAINNET_PAIRS while addresses are documented as unknown. A testnet label cannot authorize those native identities; affected capabilities remain unavailable until endpoint/on-chain evidence binds a tested pair catalog.",
    "C09 Failure policy: a 503 or timeout is transport/ambiguous evidence for a controlled trading effect, not a known rejection. Only an evidenced terminal refusal may use a compatibility rejection; Unknown suspends forward work and retains RecoveryRequired conflicts.",
    "C10 Serializable recovery: native requests contain Pyth update bytes and signer-derived fields, but controlled effect intents contain only canonical serializable encodings, evidence references, and deployment/schema fingerprints. CredentialRef/signing effects and HTTP clients stay outside records; plan-version or evidence mismatch requires reprepare/operator recovery."
  ],
  "additionalFindings": [
    "services/uta/src/domain/trading/brokers/others/leverup/reader-client.ts:1-13: comments define reader REST and Monad RPC surfaces and say testnet uses a separate reader base; the provider deployment descriptor must fingerprint both endpoint identities rather than infer environment from a label.",
    "services/uta/src/domain/trading/brokers/others/leverup/reader-client.ts:59-65: PaginatedPositions includes pageNumber/pageSize/totalPages/totalElements, but the broker never records them; PositionListing must retain cursor and coverage before account-bound absence claims.",
    "services/uta/src/domain/trading/brokers/others/leverup/reader-client.ts:80-90: fetchOpenPositions requests size and page=0 then filters status OPEN; the pagination/data reader must validate declared pages and retain CLOSED/unknown-row evidence rather than treat filtered page as complete.",
    "services/uta/src/domain/trading/brokers/others/leverup/relayer-client.ts:42-52: submit returns only inputHash and status has executed/success/optional txnHash/reason; provider codecs preserve opaque fields and require native semantics before Rejected, Confirmed, or Filled.",
    "services/uta/src/domain/trading/brokers/others/leverup/relayer-client.ts:74-93: pollUntilExecuted uses Date.now/setTimeout, returns on executed, and fabricates {executed:false,success:false,reason:\"poll timeout\"}; controlled effect observation uses durable leases/epochs and classifies timeout as Unknown/StillWorking only by evidence.",
    "services/uta/src/domain/trading/brokers/others/leverup/relayer-client.ts:95-105: generic post accepts body: unknown and casts JSON to T; provider endpoint codecs validate method/path/body/response and redact diagnostics.",
    "services/uta/src/domain/trading/brokers/others/leverup/pyth.ts:14-23: Hermes is unauthenticated and PythUpdateResponse is an interface with optional parsed data; provider data metadata records source/freshness and exact schema outcome.",
    "services/uta/src/domain/trading/brokers/others/leverup/pyth.ts:31-47: fetchPythUpdateData builds a public URL, casts JSON, and checks only binary.data; the decoder validates feed identity, binary hex, parsed fields, and stale/freshness policy.",
    "services/uta/src/domain/trading/brokers/others/leverup/pyth.ts:54-72: fetchPythPrice converts parsed price to Number and applies Math.pow(expo), returning Date plus bytes; QuoteObservation keeps integer/exponent Decimal precision and separates mark evidence from native update bytes.",
    "services/uta/src/domain/trading/brokers/others/leverup/decimals.ts:22-28: decimalToWei truncates excess precision with ROUND_DOWN and performs no finite/sign/range checks; field codecs make rounding policy explicit per declared provider schema.",
    "services/uta/src/domain/trading/brokers/others/leverup/pairs.ts:29-33: static registry states only 20 of 23 documented mainnet pairs are captured; public catalog exposes Partial coverage and does not synthesize Contracts.",
    "services/uta/src/domain/trading/brokers/others/leverup/pairs.ts:62-68: testnet pair addresses are undocumented and TESTNET_PAIRS aliases MAINNET_PAIRS; deployment/capability keeps testnet writes unavailable until native pair evidence is captured.",
    "services/uta/src/domain/trading/brokers/others/leverup/eip712.ts:4-10: source recommends nested-first then flat fallback after relayer rejection and leaves a TODO; the signing leaf removes unsafe fallback and requires pre-dispatch schema evidence with observation-only recovery after ambiguity.",
    "services/uta/src/domain/trading/brokers/others/leverup/eip712.ts:121-124: generateOpenSalt hashes Date.now and Math.random; the controlled open effect allocates durable scope/command-bound identity and pure signing accepts supplied nonce.",
    "services/uta/src/domain/trading/brokers/others/leverup/eip712.ts:43-49: SigningDomain hardcodes OneClickAgent/version 1 plus supplied chainId/agent; controlled order signing records these values and deployment/address-set fingerprint so endpoint selection cannot diverge.",
    "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.ts:23-31,83-95,319-325: IBroker/config/modify/cancel are actual legacy source surfaces; the target treats them as compatibility projections and does not turn them into a universal UTA action map.",
    "services/uta/src/domain/trading/brokers/others/leverup/LeverupBroker.ts:169-209,494-518: public search/detail/quote/clock methods are data/admission projections and do not receive AccountScope or trading WAL unless a concrete controlled effect composes their evidence."
  ]
}
