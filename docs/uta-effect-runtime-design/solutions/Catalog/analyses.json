{
  "group": "Catalog",
  "entries": [
    {
      "id": "MAP-9AD9951502",
      "sourceEvidence": [
        "packages/uta-broker-alpaca/src/index.ts:1-1: Line 1 is the sole import and points at the legacy AlpacaBroker class.",
        "services/uta/src/domain/trading/brokers/alpaca/AlpacaBroker.ts:101-124: legacy class implements IBroker and parses raw brokerConfig in fromConfig."
      ],
      "currentBehavior": "index.ts imports AlpacaBroker from services/uta/src/domain/trading/brokers/alpaca, so the released pack directly bundles the legacy IBroker class.",
      "problem": "The pack boundary therefore couples optional packaging to a UTA domain class and exposes neither a scoped connection resource nor the typed action/ack/observation relation needed by the kernel.",
      "preservedBehavior": [
        "Keep lazy SDK loading: importing the core without an Alpaca pack must still work.",
        "Keep paper/live as separate operational environments, but replace the unverified class-level assumption with observed account/environment evidence.",
        "Do not preserve the legacy IBroker mutation path once this module is authoritative."
      ],
      "openQuestions": [
        "Alpaca's SDK surface exposes UUID/client-order-id lookup paths, but this source and the completed adapter design do not prove server-side idempotency or retention across restart; until versioned native conformance records a dedupe window, keep ObserveBeforeRetry with persisted DispatchIdentity/ClientOrderId and never issue a second create-order request."
      ]
    },
    {
      "id": "MAP-DA5BBE56B3",
      "sourceEvidence": [
        "packages/uta-broker-alpaca/src/index.ts:3-3: The wrapper exports numeric BROKER_PACK_API_VERSION = 1 while its createBroker shape is the legacy IBroker factory."
      ],
      "currentBehavior": "The wrapper exports numeric BROKER_PACK_API_VERSION = 1 while its createBroker shape is the legacy IBroker factory.",
      "problem": "Reusing version 1 for a descriptor with different required exports would let the current loader accept a module that cannot provide typed interpreters or recovery semantics; API compatibility is a runtime fact, not a TypeScript inference.",
      "preservedBehavior": [
        "Keep API version independent from OpenAlice product version as broker-packs.md requires.",
        "Keep v1 artifacts eligible for replacement by Alice but not executable by v2 UTA.",
        "Do not use a permissive fallback to the legacy IBroker merely to keep old packs running."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-14D51E5BA4",
      "sourceEvidence": [
        "packages/uta-broker-alpaca/src/index.ts:4-4: BROKER_ENGINE is the stable literal `alpaca` used by the wrapper/loader to identify the engine; it is metadata only."
      ],
      "currentBehavior": "BROKER_ENGINE is the stable literal `alpaca` used by the wrapper/loader to identify the engine; it is metadata only.",
      "problem": "If consumers treat this string as an implementation registry key whose value is an arbitrary broker object, engine identity would silently become capability or transaction authority.",
      "preservedBehavior": [
        "Keep stable engine identity for existing account/preset routing and release assets.",
        "Intentionally remove any use of engine as a generic `IBroker` object key.",
        "Do not add a broker-name branch in transaction-kernel; action semantics belong to adapter definition."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-607170E22C",
      "sourceEvidence": [
        "packages/uta-broker-alpaca/src/index.ts:5-5: The exported configSchema is AlpacaBroker.configSchema, which accepts optional paper, apiKey, and apiSecret fields from the legacy class."
      ],
      "currentBehavior": "The exported configSchema is AlpacaBroker.configSchema, which accepts optional paper, apiKey, and apiSecret fields from the legacy class.",
      "problem": "A class static Zod schema validates construction shape only; optional credential fields can result in a client configured with empty secrets, and it says nothing about observed account identity, typed actions, or native response decoding.",
      "preservedBehavior": [
        "Keep existing paper/live input names at migration boundary.",
        "Preserve write-only handling for apiKey/apiSecret while making write-only a UI hint rather than a security boundary.",
        "Replace optional empty-secret construction with explicit read-only or unavailable state."
      ],
      "openQuestions": [
        "Alpaca getAccount probing and endpoint evidence are required, but this wrapper and the completed adapter design do not identify authoritative response fields for paper/live or trade permission; until a versioned adapter contract records those fields, keep environment/scope/capability unknown and expose no write action."
      ]
    },
    {
      "id": "MAP-6496594BF0",
      "sourceEvidence": [
        "packages/uta-broker-alpaca/src/index.ts:7-9: createBroker accepts `{id,label?,brokerConfig: Record<string,unknown>}`, calls AlpacaBroker.fromConfig, then adds `brokerEngine` with Object.assign and returns an IBroker."
      ],
      "currentBehavior": "createBroker accepts `{id,label?,brokerConfig: Record<string,unknown>}`, calls AlpacaBroker.fromConfig, then adds `brokerEngine` with Object.assign and returns an IBroker.",
      "problem": "The factory erases config/action associations, constructs mutable remote authority before durable transaction admission, and has no way to represent Ack versus fill, Unknown, idempotency, or compensation.",
      "preservedBehavior": [
        "Preserve successful Alpaca order translation and paper/live separation.",
        "Preserve asynchronous status observation instead of claiming immediate fill.",
        "Intentionally remove Object.assign brokerEngine and direct IBroker construction."
      ],
      "openQuestions": [
        "Alpaca lookup endpoints and fill-activity observations are available as adapter paths, but server idempotency/retention, group semantics, and partial-fill completion criteria remain unproven; until native conformance records them, use persisted identity, observe before retry, and retain partial exposure in RecoveryRequired."
      ]
    },
    {
      "id": "MAP-B40F4C1C42",
      "sourceEvidence": [
        "packages/uta-broker-ccxt/src/index.ts:1-1: index.ts imports the legacy CcxtBroker class from services/uta/src/domain/trading/brokers/ccxt."
      ],
      "currentBehavior": "index.ts imports the legacy CcxtBroker class from services/uta/src/domain/trading/brokers/ccxt.",
      "problem": "The optional CCXT pack is structurally tied to the old IBroker class and cannot expose exchange-specific sub-account scope, keyless read-only capability, or typed native outcomes through the target SPI.",
      "preservedBehavior": [
        "Keep the pack optional and retain CCXT public keyless queries where supported.",
        "Keep tested exchange override behavior in adapter modules, not kernel switches.",
        "Remove direct legacy CcxtBroker import and generic IBroker construction."
      ],
      "openQuestions": [
        "Exact idempotency scope and completeness/retention horizon differ by exchange and are not established by this wrapper source; each adapter must record evidence before allowing resend."
      ]
    },
    {
      "id": "MAP-ACA4BDF989",
      "sourceEvidence": [
        "packages/uta-broker-ccxt/src/index.ts:3-3: The CCXT wrapper exports pack API version 1 for the legacy `configSchema`/`createBroker` pair."
      ],
      "currentBehavior": "The CCXT wrapper exports pack API version 1 for the legacy `configSchema`/`createBroker` pair.",
      "problem": "Keeping v1 while replacing a generic CCXT broker with a typed interpreter would create a false compatibility promise at the dynamic module boundary.",
      "preservedBehavior": [
        "Retain independent pack API versus product version.",
        "Reject v1 rather than silently adapting its generic factory.",
        "Keep CCXT public data source readiness independent from trading-write capability."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-B295179D05",
      "sourceEvidence": [
        "packages/uta-broker-ccxt/src/index.ts:4-4: BROKER_ENGINE exports the stable literal `ccxt` and is used by the pack loader as identity metadata."
      ],
      "currentBehavior": "BROKER_ENGINE exports the stable literal `ccxt` and is used by the pack loader as identity metadata.",
      "problem": "Using `ccxt` as a generic provider flag conflates engine identity with whether a particular exchange/account has read, trade, or derivative capabilities.",
      "preservedBehavior": [
        "Keep the literal for release asset naming and source routing.",
        "Do not use it to infer crypto contract identity or write support.",
        "Avoid adding `if engine === ccxt` to transaction-kernel."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-F3151B31E7",
      "sourceEvidence": [
        "packages/uta-broker-ccxt/src/index.ts:5-5: The wrapper re-exports `CcxtBroker.configSchema`; the legacy schema accepts exchange, sandbox/demo flags, options record, and many optional credential fields."
      ],
      "currentBehavior": "The wrapper re-exports `CcxtBroker.configSchema`; the legacy schema accepts exchange, sandbox/demo flags, options record, and many optional credential fields.",
      "problem": "The schema allows arbitrary `options` and optional credential combinations, so parsing does not establish a concrete exchange credential mode, account/sub-account scope, or capability evidence.",
      "preservedBehavior": [
        "Preserve exchange-specific optional credential support and keyless public-data use.",
        "Preserve `sandbox` versus `demoTrading` distinction; do not collapse them to one boolean.",
        "Intentionally remove unrestricted options record from kernel-facing config."
      ],
      "openQuestions": [
        "Required credential subsets and account/sub-account selector semantics are exchange-specific and need per-pack evidence."
      ]
    },
    {
      "id": "MAP-9D3DC42AC9",
      "sourceEvidence": [
        "packages/uta-broker-ccxt/src/index.ts:7-9: createBroker forwards generic id/label/brokerConfig to CcxtBroker.fromConfig, then Object.assigns `brokerEngine: \"ccxt\"`."
      ],
      "currentBehavior": "createBroker forwards generic id/label/brokerConfig to CcxtBroker.fromConfig, then Object.assigns `brokerEngine: \"ccxt\"`.",
      "problem": "The wrapper cannot preserve typed exchange/account relationships or durable dispatch identity. CCXT asynchronous order acceptance, partial fills, sub-account routing, and unknown outcomes are hidden behind an IBroker boolean result.",
      "preservedBehavior": [
        "Preserve CCXT exchange overrides and asynchronous status model; retain notional conversion only with fresh price evidence.",
        "Preserve `keyless` query path, but no write authority.",
        "Remove immediate construction/Object.assign and generic IBroker result."
      ],
      "openQuestions": [
        "Idempotency and full pagination guarantees vary per CCXT exchange; no generic resend permission can be designed without adapter evidence."
      ]
    },
    {
      "id": "MAP-1E5FD0A690",
      "sourceEvidence": [
        "packages/uta-broker-ibkr/src/index.ts:1-1: index.ts imports the legacy IbkrBroker from services/uta/src/domain/trading/brokers/ibkr."
      ],
      "currentBehavior": "index.ts imports the legacy IbkrBroker from services/uta/src/domain/trading/brokers/ibkr.",
      "problem": "The optional pack consequently exposes a class that owns callback/socket state and a generic IBroker façade instead of a scoped connection service that can retain managed-account and connection-generation evidence.",
      "preservedBehavior": [
        "Keep local TWS/Gateway connection and auto-detection semantics as query/setup behavior.",
        "Preserve explicit accountId when supplied and retain all managed accounts in observations.",
        "Remove legacy class import and direct transaction mutation."
      ],
      "openQuestions": [
        "The completed IBKR and IbkrBridge designs still lack a native field/coverage guarantee for TWS/Gateway environment and callback completion/idempotency; until versioned adapter evidence proves them, keep mutation unavailable or Unknown, retain generation-scoped callbacks, and never automatic resend."
      ]
    },
    {
      "id": "MAP-AB521AFE64",
      "sourceEvidence": [
        "packages/uta-broker-ibkr/src/index.ts:3-3: The IBKR wrapper exports API version 1 for the legacy schema/factory contract."
      ],
      "currentBehavior": "The IBKR wrapper exports API version 1 for the legacy schema/factory contract.",
      "problem": "A v1 label cannot safely describe a descriptor requiring callback lifecycle, account-scope selection, and typed observation/compensation; accepting it would hide missing runtime exports.",
      "preservedBehavior": [
        "Retain product-independent pack API compatibility.",
        "Reject generic v1 module instead of coercing it to IBKR SPI.",
        "Keep old releases immutable for repair diagnostics."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-55E14D3598",
      "sourceEvidence": [
        "packages/uta-broker-ibkr/src/index.ts:4-4: BROKER_ENGINE exports the stable literal `ibkr`; the loader uses it as pack identity only."
      ],
      "currentBehavior": "BROKER_ENGINE exports the stable literal `ibkr`; the loader uses it as pack identity only.",
      "problem": "The engine label cannot prove TWS readiness, selected managed account, market entitlement, or writable capability and must not stand in for callback health.",
      "preservedBehavior": [
        "Keep stable pack routing and user-facing provider identity.",
        "Do not infer paper/live from port or engine string.",
        "Do not expose SDK classes through the engine metadata."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-C4C40BAECB",
      "sourceEvidence": [
        "packages/uta-broker-ibkr/src/index.ts:5-5: The wrapper aliases IbkrBroker.configSchema, validating host, port, clientId, optional accountId, and an unused paper boolean."
      ],
      "currentBehavior": "The wrapper aliases IbkrBroker.configSchema, validating host, port, clientId, optional accountId, and an unused paper boolean.",
      "problem": "The preset and old class disagree on the role of paper: the preset derives it from port, while IbkrBroker.fromConfig does not forward `paper`. Parsing therefore cannot establish environment/account evidence or callback capabilities.",
      "preservedBehavior": [
        "Preserve host/port/clientId/accountId fields and defaults at setup input.",
        "Intentionally stop port 7497/4002 from independently classifying paper.",
        "Preserve explicit account selection and TWS login authority."
      ],
      "openQuestions": [
        "Neither the preset/wrapper nor the completed IBKR adapter design identifies an authenticated TWS/Gateway field set that proves paper/live; port remains a hint, and absent evidence writes stay unavailable as EnvironmentUnknown rather than being inferred."
      ]
    },
    {
      "id": "MAP-3392948A39",
      "sourceEvidence": [
        "packages/uta-broker-ibkr/src/index.ts:7-9: createBroker calls IbkrBroker.fromConfig with generic brokerConfig and returns an Object.assign-ed legacy IBroker."
      ],
      "currentBehavior": "createBroker calls IbkrBroker.fromConfig with generic brokerConfig and returns an Object.assign-ed legacy IBroker.",
      "problem": "This hides the callback stream, managed-account identity, and request/order correlation needed to classify unknown outcomes; a Promise return after a socket callback is not a durable dispatch acknowledgement.",
      "preservedBehavior": [
        "Preserve native contract canonicalization and callback-based order status.",
        "Preserve no-TP/SL refusal where unsupported rather than silently sending an unprotected order.",
        "Remove generic `createBroker`, direct IBroker and ad-hoc engine property."
      ],
      "openQuestions": [
        "Native IBKR idempotency and complete query/late-callback retention horizons require empirical adapter evidence."
      ]
    },
    {
      "id": "MAP-AE59785B88",
      "sourceEvidence": [
        "packages/uta-broker-leverup/src/index.ts:1-1: index.ts imports the legacy LeverupBroker from the nested domain/trading/others path."
      ],
      "currentBehavior": "index.ts imports the legacy LeverupBroker from the nested domain/trading/others path.",
      "problem": "The release pack thereby bundles a mutable relayer/client class without exposing typed network/account identity, durable outcome observation, or compensation authority.",
      "preservedBehavior": [
        "Keep the optional lower-tier pack and explicit testnet/live distinction.",
        "Keep relayer-specific native signing and status mapping inside adapter.",
        "Remove import of legacy mutable `LeverupBroker` and its in-memory tracking."
      ],
      "openQuestions": [
        "Chain finality and relayer idempotency evidence are unavailable in this wrapper range; automatic resend stays disabled until verified."
      ]
    },
    {
      "id": "MAP-1B0FB784F5",
      "sourceEvidence": [
        "packages/uta-broker-leverup/src/index.ts:3-3: The LeverUp wrapper exports API version 1 for `configSchema` and generic `createBroker`."
      ],
      "currentBehavior": "The LeverUp wrapper exports API version 1 for `configSchema` and generic `createBroker`.",
      "problem": "Version 1 does not encode the relayer receipt/finality and signer-aware SPI needed for a safe LeverUp adapter.",
      "preservedBehavior": [
        "Retain API independence from product version.",
        "Reject v1 generic class export.",
        "Keep previous immutable release for repair."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-D5792AEA38",
      "sourceEvidence": [
        "packages/uta-broker-leverup/src/index.ts:4-4: BROKER_ENGINE is the stable literal `leverup` used for pack identity."
      ],
      "currentBehavior": "BROKER_ENGINE is the stable literal `leverup` used for pack identity.",
      "problem": "The label cannot establish network, signer ownership, relayer health, or chain finality; treating it as a writable broker flag risks sending on the wrong chain.",
      "preservedBehavior": [
        "Keep release asset routing and catalog display.",
        "Do not add `engine === leverup` to domain decisions.",
        "Preserve explicit live/testnet distinction from preset."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-2564A889E7",
      "sourceEvidence": [
        "packages/uta-broker-leverup/src/index.ts:5-5: The wrapper aliases LeverupBroker.configSchema, which validates network enum and a 0x-prefixed 64-hex private key."
      ],
      "currentBehavior": "The wrapper aliases LeverupBroker.configSchema, which validates network enum and a 0x-prefixed 64-hex private key.",
      "problem": "Schema validation proves only string shape; it neither protects secret lifecycle nor verifies signer/network/account identity or relayer action availability.",
      "preservedBehavior": [
        "Keep regex validation as an ingress check and live/testnet options.",
        "Preserve write-only UI marker, but no longer treat it as storage protection.",
        "Replace raw privateKey engine dict and secret-derived fingerprint."
      ],
      "openQuestions": [
        "Signer address and chain-authorization are adapter-observed identity evidence, but relayer-native stable idempotency-key scope/retention remains unproven; until conformance evidence exists, keep AccountId/AccountScope independent of the signer and block resend."
      ]
    },
    {
      "id": "MAP-50C8158380",
      "sourceEvidence": [
        "packages/uta-broker-leverup/src/index.ts:7-9: createBroker builds `LeverupBroker` from a generic Record and adds the `leverup` property; no relayer receipt or recovery contract is returned."
      ],
      "currentBehavior": "createBroker builds `LeverupBroker` from a generic Record and adds the `leverup` property; no relayer receipt or recovery contract is returned.",
      "problem": "A lost response after a signed relayer submission cannot be distinguished from rejection, and the in-memory class cannot survive restart with its order tracking or schema variant.",
      "preservedBehavior": [
        "Preserve market-only and supported pair behavior.",
        "Preserve explicit relayer/network failure as typed rejection where evidence is conclusive.",
        "Remove direct legacy construction and generic success boolean."
      ],
      "openQuestions": [
        "Relayer receipt retention, chain finality horizon, and deterministic replayability need real adapter evidence."
      ]
    },
    {
      "id": "MAP-75B61EBF36",
      "sourceEvidence": [
        "packages/uta-broker-longbridge/src/index.ts:1-1: index.ts imports the legacy LongbridgeBroker from services/uta/src/domain/trading/brokers/longbridge."
      ],
      "currentBehavior": "index.ts imports the legacy LongbridgeBroker from services/uta/src/domain/trading/brokers/longbridge.",
      "problem": "The pack directly exposes a mutable class with optional FX setter and no typed currency/jurisdiction capability or account-scoped observation interface.",
      "preservedBehavior": [
        "Preserve Longbridge multi-region use and paper/live separation.",
        "Preserve explicit unsupported TIF/order behavior; do not silently broaden.",
        "Remove legacy import and mutable setter-based setup."
      ],
      "openQuestions": [
        "The completed Longbridge design separates scoped region/jurisdiction evidence from connection metadata, but the native endpoint/account-region field set and idempotency horizon remain unproven; keep dispatch unavailable or Unknown and require adapter conformance evidence."
      ]
    },
    {
      "id": "MAP-F093E8F942",
      "sourceEvidence": [
        "packages/uta-broker-longbridge/src/index.ts:3-3: The Longbridge wrapper exports pack API version 1 for the generic class factory."
      ],
      "currentBehavior": "The Longbridge wrapper exports pack API version 1 for the generic class factory.",
      "problem": "v1 cannot express Longbridge native currency, jurisdiction, access-token lifecycle, and FX requirements; accepting it as a target module would erase safety gates.",
      "preservedBehavior": [
        "Keep API version independent from product release.",
        "Reject old generic factory rather than adapt by cast.",
        "Leave prior release immutable for repair."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-E89067CA5C",
      "sourceEvidence": [
        "packages/uta-broker-longbridge/src/index.ts:4-4: BROKER_ENGINE is the stable literal `longbridge` used for pack selection, not for dispatch."
      ],
      "currentBehavior": "BROKER_ENGINE is the stable literal `longbridge` used for pack selection, not for dispatch.",
      "problem": "The label cannot establish supported region, settlement currency, paper/live environment, or FX availability.",
      "preservedBehavior": [
        "Keep release and UI identity.",
        "Do not infer region or paper from engine string.",
        "No kernel broker-name branching."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-1986FE3044",
      "sourceEvidence": [
        "packages/uta-broker-longbridge/src/index.ts:5-5: The wrapper aliases LongbridgeBroker.configSchema, accepting appKey/appSecret/accessToken, paper, and optional custom HTTP/WebSocket URLs."
      ],
      "currentBehavior": "The wrapper aliases LongbridgeBroker.configSchema, accepting appKey/appSecret/accessToken, paper, and optional custom HTTP/WebSocket URLs.",
      "problem": "The class schema mixes secrets and transport endpoints, while optional setFxService is outside this export; parsing does not prove region/currency, account scope, token freshness, or action capability.",
      "preservedBehavior": [
        "Keep optional endpoint override support only as validated operator config.",
        "Keep paper/live hint for UI but gate writes on observation.",
        "Intentionally remove setter mutation and raw secret fields from runtime domain."
      ],
      "openQuestions": [
        "Exact Longbridge region and supported-currency payload fields must be established by adapter SDK evidence."
      ]
    },
    {
      "id": "MAP-E52D407CDC",
      "sourceEvidence": [
        "packages/uta-broker-longbridge/src/index.ts:7-9: createBroker calls LongbridgeBroker.fromConfig with a generic config and returns an Object.assign-ed legacy IBroker; FX is attached later by an optional setter in the factory."
      ],
      "currentBehavior": "createBroker calls LongbridgeBroker.fromConfig with a generic config and returns an Object.assign-ed legacy IBroker; FX is attached later by an optional setter in the factory.",
      "problem": "This path can construct an apparently usable broker without FX, currency/jurisdiction evidence, or durable action identity; `_tpsl` and other unsupported fields risk being accepted by a generic signature.",
      "preservedBehavior": [
        "Preserve venue-specific HK market-order translation and known TIF refusal.",
        "Preserve multi-currency accounting requirement.",
        "Remove direct factory construction and optional setter duck typing."
      ],
      "openQuestions": [
        "The completed Longbridge design fixes TokenExpired as a typed no-blind-refresh outcome and requires complete ListingResult evidence, but native refresh semantics, order idempotency, and listing coverage remain unproven; retain those gaps and keep affected actions in Unknown/Unavailable with observe-before-retry."
      ]
    },
    {
      "id": "MAP-CC2177368A",
      "sourceEvidence": [
        "packages/uta-protocol/src/brokers/preset-catalog.ts:1-14: The catalog module presents itself as the single source of wizard account types, imports Zod for per-preset validation, and imports node crypto createHash/randomBytes for identity helpers."
      ],
      "currentBehavior": "The catalog module presents itself as the single source of wizard account types, imports Zod for per-preset validation, and imports node crypto createHash/randomBytes for identity helpers.",
      "problem": "This shared module currently combines Alice form declarations, UTA config translation, and identity minting. That allows UI metadata and secret-bearing raw values to become runtime authority even though the module is not a broker connection owner.",
      "preservedBehavior": [
        "Preserve one user-facing preset per account type and stable preset ids for migration.",
        "Preserve deterministic pure config parsing.",
        "Remove catalog responsibility for identity authority, SDK configuration, and runtime capability."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-00030A134A",
      "sourceEvidence": [
        "packages/uta-protocol/src/brokers/preset-catalog.ts:16-18: BrokerEngine is a six-member string union: ccxt, alpaca, ibkr, leverup, longbridge, and mock."
      ],
      "currentBehavior": "BrokerEngine is a six-member string union: ccxt, alpaca, ibkr, leverup, longbridge, and mock.",
      "problem": "The union identifies implementation labels but does not bind an engine to config, actions, outcomes, capabilities, or compensation; a string can therefore be mistaken for runtime support.",
      "preservedBehavior": [
        "Keep all six labels and existing preset routing.",
        "Preserve Mock as core engine and optional live pack policy.",
        "Intentionally stop treating engine strings as capability or object registries."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-9F3FB86CB7",
      "sourceEvidence": [
        "packages/uta-protocol/src/brokers/preset-catalog.ts:20-23: ModeOption is an arbitrary string id/label pair used by the wizard to render mode choices."
      ],
      "currentBehavior": "ModeOption is an arbitrary string id/label pair used by the wizard to render mode choices.",
      "problem": "Mode labels are presentation metadata, not evidence of endpoint, account environment, or write authority; consuming them directly would allow a `demo` label to authorize an unverified demo route.",
      "preservedBehavior": [
        "Preserve labels/order for wizard UX.",
        "Preserve distinct testnet/demo/paper options where source offers them.",
        "Intentionally remove boolean mode assumptions from kernel decisions."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-67CE26F92B",
      "sourceEvidence": [
        "packages/uta-protocol/src/brokers/preset-catalog.ts:25-35: SubtitleSegment contains an arbitrary presetConfig field path plus optional label/falseLabel/prefix directives for account-card display."
      ],
      "currentBehavior": "SubtitleSegment contains an arbitrary presetConfig field path plus optional label/falseLabel/prefix directives for account-card display.",
      "problem": "The field-path/truthiness mechanism is a UI formatter; if reused for capability or identity it can treat cosmetic or missing data as security facts.",
      "preservedBehavior": [
        "Preserve subtitle output such as “OKX · Demo Trading” for UX.",
        "Do not use truthiness to classify live/paper or write permission.",
        "Keep arbitrary field path out of protocol/journal schemas."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-601F6E2834",
      "sourceEvidence": [
        "packages/uta-protocol/src/brokers/preset-catalog.ts:37-98: BrokerPresetDef combines metadata, an unparameterized `z.ZodType`, optional modes/subtitle/writeOnly/fingerprint fields, a raw-record `toEngineConfig`, and raw-record `isPaper` callback."
      ],
      "currentBehavior": "BrokerPresetDef combines metadata, an unparameterized `z.ZodType`, optional modes/subtitle/writeOnly/fingerprint fields, a raw-record `toEngineConfig`, and raw-record `isPaper` callback.",
      "problem": "The type is a many-to-few UI-to-engine bridge with erased config and action relationships. It lets arbitrary fields and callbacks determine runtime config/environment without proving a scoped connection, capability, or secret boundary.",
      "preservedBehavior": [
        "Preserve user-facing many presets mapping to few engines.",
        "Preserve field-specific translations (OKX sandbox, Bybit testnet/demo, Alpaca paper, etc.).",
        "Remove raw Record, optional callback, and fingerprint-as-identity authority."
      ],
      "openQuestions": [
        "Per-exchange credential requirements and extension fields remain empirical; absent evidence the typed codec rejects them or keeps them outside write capability."
      ]
    },
    {
      "id": "MAP-E745F1370E",
      "sourceEvidence": [
        "packages/uta-protocol/src/brokers/preset-catalog.ts:100-106: defaultIsPaper lowercases `data.mode` from a raw Record and returns true only for demo, testnet, or paper; all other values are false."
      ],
      "currentBehavior": "defaultIsPaper lowercases `data.mode` from a raw Record and returns true only for demo, testnet, or paper; all other values are false.",
      "problem": "This helper conflates an unvalidated mode string with environment truth and returns false for unknown/missing values, which can accidentally classify live authorization or hide uncertainty.",
      "preservedBehavior": [
        "Keep demo/testnet/paper classifications for UI test filtering where source config permits them.",
        "Do not treat arbitrary non-live as paper.",
        "Do not preserve false-on-unknown as execution authorization."
      ],
      "openQuestions": [
        "Which broker responses encode environment is empirical and delegated to each adapter/live-evidence design."
      ]
    },
    {
      "id": "MAP-EBB7846E5F",
      "sourceEvidence": [
        "packages/uta-protocol/src/brokers/preset-catalog.ts:108-154: BINANCE_PRESET is a CCXT preset with mode live|demo, required apiKey/secret, and toEngineConfig `{exchange:\"binance\", demoTrading: mode==demo, apiKey, secret}`. Its comments deliberately exclude Binance legacy testnet."
      ],
      "currentBehavior": "BINANCE_PRESET is a CCXT preset with mode live|demo, required apiKey/secret, and toEngineConfig `{exchange:\"binance\", demoTrading: mode==demo, apiKey, secret}`. Its comments deliberately exclude Binance legacy testnet.",
      "problem": "The preset-level demo translation is valuable but is still only a declaration; old code could treat flags as proof and exposes raw credentials through a generic record. The source explicitly says futures testnet is dead and spot testnet would violate full-spectrum loading.",
      "preservedBehavior": [
        "Preserve live versus Demo Trading and deliberate absence of `sandbox`.",
        "Preserve full-spectrum warning: do not resurrect unsupported spot-only testnet path.",
        "Replace raw API fields with refs and observed capability."
      ],
      "openQuestions": [
        "Current source does not establish Binance Demo API response/account identity or idempotency horizon; adapter evidence required."
      ]
    },
    {
      "id": "MAP-BB8EAF65B0",
      "sourceEvidence": [
        "packages/uta-protocol/src/brokers/preset-catalog.ts:156-189: OKX_PRESET defines live|demo, apiKey/secret/password, and translates demo to `sandbox:true` while deliberately omitting `demoTrading`."
      ],
      "currentBehavior": "OKX_PRESET defines live|demo, apiKey/secret/password, and translates demo to `sandbox:true` while deliberately omitting `demoTrading`.",
      "problem": "The source preserves a known OKX footgun boundary but a boolean sandbox flag alone cannot prove demo credentials, account scope, or whether all spot/perp/futures/options namespaces are available.",
      "preservedBehavior": [
        "Preserve demo -> sandbox true and no demoTrading flag, because tests explicitly guard this translation.",
        "Preserve credential requirements and no withdrawal authority in hint.",
        "Do not infer write authority from label/sandbox alone."
      ],
      "openQuestions": [
        "OKX API mode/sub-account and idempotency evidence are not present in this catalog source."
      ]
    },
    {
      "id": "MAP-6E3C5642E6",
      "sourceEvidence": [
        "packages/uta-protocol/src/brokers/preset-catalog.ts:191-224: BYBIT_PRESET exposes live, testnet, and demo modes with apiKey/secret; testnet maps sandbox=true and demo maps demoTrading=true."
      ],
      "currentBehavior": "BYBIT_PRESET exposes live, testnet, and demo modes with apiKey/secret; testnet maps sandbox=true and demo maps demoTrading=true.",
      "problem": "Bybit has two non-live environments with different domains/market-data semantics. Collapsing them into one paper boolean or trusting the selected mode risks sending credentials to the wrong endpoint and misrepresenting real versus simulated market data.",
      "preservedBehavior": [
        "Preserve all three source modes and exact sandbox/demo flag mapping.",
        "Preserve difference between testnet and demo market data.",
        "Do not treat `isPaper` true as proof of endpoint or writable safety."
      ],
      "openQuestions": [
        "Exact Bybit endpoint/account evidence and idempotency scope require adapter experiments."
      ]
    },
    {
      "id": "MAP-2F4E92A2AE",
      "sourceEvidence": [
        "packages/uta-protocol/src/brokers/preset-catalog.ts:226-257: HYPERLIQUID_PRESET uses CCXT engine but walletAddress/privateKey authentication, mode live|testnet, and maps testnet to sandbox=true."
      ],
      "currentBehavior": "HYPERLIQUID_PRESET uses CCXT engine but walletAddress/privateKey authentication, mode live|testnet, and maps testnet to sandbox=true.",
      "problem": "Wallet address and private key in a generic record do not prove signer ownership, vault/API-wallet relationship, chain environment, or durable account identity; treating wallet address as a complete AccountScope is unsafe.",
      "preservedBehavior": [
        "Preserve testnet sandbox mapping and wallet-auth flow.",
        "Preserve warning to use dedicated API wallet; do not broaden to main wallet silently.",
        "Replace secret-derived fingerprint with verified identity linkage."
      ],
      "openQuestions": [
        "Vault/API-wallet semantics and native idempotency/finality evidence require adapter experiments."
      ]
    },
    {
      "id": "MAP-B9545FA731",
      "sourceEvidence": [
        "packages/uta-protocol/src/brokers/preset-catalog.ts:259-292: BITGET_PRESET defines live|demo with apiKey/secret/password and maps demo to demoTrading; hint explicitly limits support to Classic accounts and says Unified v3 is unsupported."
      ],
      "currentBehavior": "BITGET_PRESET defines live|demo with apiKey/secret/password and maps demo to demoTrading; hint explicitly limits support to Classic accounts and says Unified v3 is unsupported.",
      "problem": "The UI mode and generic CCXT config do not encode Bitget account model. A Unified/v3 account could parse successfully while the old adapter silently uses Classic routing or wrong product namespace.",
      "preservedBehavior": [
        "Preserve Classic-only warning and demoTrading mapping.",
        "Preserve separate passphrase requirement.",
        "Do not claim Unified support until adapter evidence exists."
      ],
      "openQuestions": [
        "Bitget account-mode endpoint and idempotency evidence are unavailable in this source."
      ]
    },
    {
      "id": "MAP-F62E863C78",
      "sourceEvidence": [
        "packages/uta-protocol/src/brokers/preset-catalog.ts:294-333: CCXT_CUSTOM_PRESET accepts arbitrary exchange, sandbox/demo booleans, and optional apiKey/secret/password/uid/walletAddress/privateKey; toEngineConfig loops fields into a raw Record and isPaper checks either flag."
      ],
      "currentBehavior": "CCXT_CUSTOM_PRESET accepts arbitrary exchange, sandbox/demo booleans, and optional apiKey/secret/password/uid/walletAddress/privateKey; toEngineConfig loops fields into a raw Record and isPaper checks either flag.",
      "problem": "This escape hatch intentionally supports unknown exchanges but its open field set and permissive mode booleans cannot prove required credentials, account scope, endpoint semantics, instrument coverage, or idempotency. It must not become an implicit universal runtime fallback.",
      "preservedBehavior": [
        "Preserve custom exchange escape-hatch visibility and keyless public data for supported exchanges.",
        "Preserve sandbox/demo distinction as declarations.",
        "Intentionally remove permissive raw pass-through as a write path."
      ],
      "openQuestions": [
        "Credential requirements, native plan semantics, and observation completeness remain unavailable until a concrete exchange is selected and empirically profiled; UnverifiedExchange stays query-only/unsupported until then."
      ]
    },
    {
      "id": "MAP-2862195456",
      "sourceEvidence": [
        "packages/uta-protocol/src/brokers/preset-catalog.ts:337-367: ALPACA_PRESET declares paper/live modes, requires non-empty apiKey/apiSecret, marks both write-only, fingerprints mode+apiKey, and translates mode to engine paper boolean."
      ],
      "currentBehavior": "ALPACA_PRESET declares paper/live modes, requires non-empty apiKey/apiSecret, marks both write-only, fingerprints mode+apiKey, and translates mode to engine paper boolean.",
      "problem": "The user-facing mode is mixed with a secret-bearing raw config and the API key is currently an identity input, so rotating credentials can strand an existing account/history while a mode label is not runtime proof of the connected account environment.",
      "preservedBehavior": [
        "Preserve separate paper/live credential guidance and paper boolean translation for the native adapter boundary.",
        "Intentionally remove API-key-based identity derivation because it invalidates durable history on rotation; retain prior IDs through an explicit alias/migration record."
      ],
      "openQuestions": [
        "Which exact Alpaca account response fields are the authoritative environment and account-scope evidence must be fixed by the adapter pack contract."
      ]
    },
    {
      "id": "MAP-DE2769E2F7",
      "sourceEvidence": [
        "packages/uta-protocol/src/brokers/preset-catalog.ts:369-398: IBKR_PRESET accepts host, numeric port, clientId, and optional accountId; defaults host 127.0.0.1, port 7497, clientId 0, passes those values through, and infers paper from ports 7497/4002."
      ],
      "currentBehavior": "IBKR_PRESET accepts host, numeric port, clientId, and optional accountId; defaults host 127.0.0.1, port 7497, clientId 0, passes those values through, and infers paper from ports 7497/4002.",
      "problem": "Port numbers are deployment conventions rather than authenticated environment evidence; optional accountId permits an unbound connection and reconnect generation is not represented in this preset identity.",
      "preservedBehavior": [
        "Preserve host/port/clientId pass-through and auto-discovery when accountId is omitted.",
        "Preserve port hints for UI defaults, but intentionally remove port-only isPaper authorization.",
        "Preserve existing IDs/history via explicit migration; changing from port-derived fingerprinting to account identity must not orphan records."
      ],
      "openQuestions": [
        "The completed IBKR and IbkrBridge designs fix port as a hint and prohibit first-account defaulting, but the exact handshake/account-summary field set proving environment remains unproven; keep DeclaredUnknown/EnvironmentMismatch handling and no write capability until adapter evidence is recorded."
      ]
    },
    {
      "id": "MAP-42A8E8BFCC",
      "sourceEvidence": [
        "packages/uta-protocol/src/brokers/preset-catalog.ts:400-433: LONGBRIDGE_PRESET exposes live/paper mode, requires appKey/appSecret/accessToken, marks all three write-only, fingerprints mode+appKey, and maps mode to native paper."
      ],
      "currentBehavior": "LONGBRIDGE_PRESET exposes live/paper mode, requires appKey/appSecret/accessToken, marks all three write-only, fingerprints mode+appKey, and maps mode to native paper.",
      "problem": "Access tokens expire and paper/live credentials are separate, yet appKey participates in the current derived ID; raw credentials in preset config also cross a public protocol boundary.",
      "preservedBehavior": [
        "Preserve live/paper native flag translation and explicit separate-credential guidance.",
        "Preserve account/history IDs across token rotation or app-key rotation by explicit migration; never rederive from secrets.",
        "Intentionally make region and FX evidence explicit because mode alone cannot describe multi-market account semantics."
      ],
      "openQuestions": [
        "The completed Longbridge design fixes environment/scope/token evidence as separate facts and TokenExpired as a typed failure, but the exact endpoint and response fields proving them remain unproven; preserve those fields as unavailable evidence and block writes until the pack contract records them."
      ]
    },
    {
      "id": "MAP-FA8441EFDE",
      "sourceEvidence": [
        "packages/uta-protocol/src/brokers/preset-catalog.ts:437-468: LEVERUP_PRESET offers live/testnet modes, validates a 0x-prefixed 64-hex private key, marks it write-only, fingerprints mode+privateKey, and maps mode to network/privateKey."
      ],
      "currentBehavior": "LEVERUP_PRESET offers live/testnet modes, validates a 0x-prefixed 64-hex private key, marks it write-only, fingerprints mode+privateKey, and maps mode to network/privateKey.",
      "problem": "A private key is a signer credential with full wallet control; hashing it into an account identity leaks rotation semantics and can create a new identity for the same wallet. The current network field is not proof of chain/contract deployment.",
      "preservedBehavior": [
        "Preserve strict 32-byte key format and live/testnet distinction.",
        "Intentionally remove private-key fingerprinting and mutable raw engine config; signed actions use an effect-owned signer.",
        "Preserve rejection/unsupported behavior when the wallet is not an authorized Trader Agent rather than silently falling back."
      ],
      "openQuestions": [
        "The completed LeverUp protocol design requires a VerifiedNetworkDescriptor, signer authorization, and contract/deployment evidence, but the exact chain/contract/Trader Agent observation payload remains unproven; keep write capability unavailable and do not infer it from network labels."
      ]
    },
    {
      "id": "MAP-52A6B771BA",
      "sourceEvidence": [
        "packages/uta-protocol/src/brokers/preset-catalog.ts:470-495: SIMULATOR_PRESET accepts/coerces starting cash, identifies the mock engine, marks itself paper, and relies on a route-minted random `_instanceId` fingerprint; the hint explicitly says process memory is wiped on restart and the Dev panel injects prices/fills/transfers."
      ],
      "currentBehavior": "SIMULATOR_PRESET accepts/coerces starting cash, identifies the mock engine, marks itself paper, and relies on a route-minted random `_instanceId` fingerprint; the hint explicitly says process memory is wiped on restart and the Dev panel injects prices/fills/transfers.",
      "problem": "The simulated broker state is intentionally ephemeral, but the transaction journal and account identity cannot be allowed to disappear with process memory. A cash number without currency/Decimal semantics also cannot safely model order sizing.",
      "preservedBehavior": [
        "Preserve no-real-money/manual-match semantics and restart wipe of simulated broker state.",
        "Intentionally separate durable UTA history from ephemeral simulated positions; `isPaper` becomes a typed Simulation environment, not a universal authorization shortcut.",
        "Preserve explicit instance uniqueness without deriving identity from cash."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-9A17C2A8CC",
      "sourceEvidence": [
        "packages/uta-protocol/src/brokers/preset-catalog.ts:497-522: BROKER_PRESET_CATALOG is an ordered array: recommended IBKR/Alpaca/Longbridge/Hyperliquid, crypto Binance/OKX/Bybit/Bitget/LeverUp/CCXT Custom, and testing Simulator; comments state UI category grouping and historical ordering."
      ],
      "currentBehavior": "BROKER_PRESET_CATALOG is an ordered array: recommended IBKR/Alpaca/Longbridge/Hyperliquid, crypto Binance/OKX/Bybit/Bitget/LeverUp/CCXT Custom, and testing Simulator; comments state UI category grouping and historical ordering.",
      "problem": "The array currently couples presentation order, preset discovery, and the runtime assumption that every listed engine can be loaded. Reordering or removing a UI item must not alter persisted preset IDs or runtime pack availability.",
      "preservedBehavior": [
        "Preserve all current preset IDs and order/category presentation unless an intentional UI change is separately documented.",
        "Intentionally remove any implicit “catalog presence means installed engine” assumption.",
        "Keep Custom and Simulator visible even when live packs are unavailable, with explicit capability/readiness states."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-E954ABE476",
      "sourceEvidence": [
        "packages/uta-protocol/src/brokers/preset-catalog.ts:524-531: getBrokerPreset performs a linear catalog lookup by arbitrary string and throws a generic Error containing the unknown ID and a joined list of known IDs."
      ],
      "currentBehavior": "getBrokerPreset performs a linear catalog lookup by arbitrary string and throws a generic Error containing the unknown ID and a joined list of known IDs.",
      "problem": "A generic exception loses the distinction between malformed input, unknown persisted preset, and an unavailable engine; callers cannot make typed recovery or quarantine decisions.",
      "preservedBehavior": [
        "Preserve deterministic lookup and useful known-ID diagnostics.",
        "Intentionally replace throw-based control flow with a structured failure so route/domain code can distinguish it from adapter errors."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-4FEAB1D03E",
      "sourceEvidence": [
        "packages/uta-protocol/src/brokers/preset-catalog.ts:533-537: isPaperPreset resolves a preset, calls an optional preset-specific isPaper function, and otherwise uses defaultIsPaper over raw config; it is used as a boolean classification helper."
      ],
      "currentBehavior": "isPaperPreset resolves a preset, calls an optional preset-specific isPaper function, and otherwise uses defaultIsPaper over raw config; it is used as a boolean classification helper.",
      "problem": "Paper/demo/testnet are not one security state: testnet, simulated, read-only keyless, and authenticated environments have different capabilities. A boolean from requested config can authorize the wrong endpoint/account.",
      "preservedBehavior": [
        "Preserve the UI’s paper/demo/testnet labels and each preset’s explicit mode mapping.",
        "Intentionally remove boolean use in factory/E2E checks; Binance testnet remains unsupported by its schema and must not be accepted merely because this helper once returned true."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-ED0CF2A5B8",
      "sourceEvidence": [
        "packages/uta-protocol/src/brokers/preset-catalog.ts:539-555: canonicalize recursively maps arrays and sorts object keys before JSON serialization; it accepts unknown values and uses casts to treat objects as string-keyed records."
      ],
      "currentBehavior": "canonicalize recursively maps arrays and sorts object keys before JSON serialization; it accepts unknown values and uses casts to treat objects as string-keyed records.",
      "problem": "The routine is pure but its open unknown/object treatment allows unsupported values (Date, bigint, non-finite numbers, class instances) to enter an identity hash with ambiguous JSON semantics.",
      "preservedBehavior": [
        "Preserve object-key order invariance and array-order sensitivity.",
        "Intentionally reject unsupported values instead of coercing arbitrary objects; preserve legacy hashes via versioned aliases."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-4E1058C577",
      "sourceEvidence": [
        "packages/uta-protocol/src/brokers/preset-catalog.ts:557-580: deriveUtaId filters configured fingerprintFields, fills missing values with null, canonicalizes JSON, hashes `${preset.id}:${json}` with SHA-256, and returns a preset-id plus eight-hex (32-bit) identifier; fingerprint fields include API/private keys for several presets."
      ],
      "currentBehavior": "deriveUtaId filters configured fingerprintFields, fills missing values with null, canonicalizes JSON, hashes `${preset.id}:${json}` with SHA-256, and returns a preset-id plus eight-hex (32-bit) identifier; fingerprint fields include API/private keys for several presets.",
      "problem": "This identity algorithm is deterministic but credential-sensitive: secret rotation changes IDs and can strand journals/accounts. Eight hex digits also make collisions an explicit durable risk, while old git/sealed bindings must remain addressable.",
      "preservedBehavior": [
        "Preserve deterministic legacy ID lookup and all existing account/history IDs.",
        "Intentionally stop hashing apiKey/privateKey/accessToken and stop treating eight-hex output as the canonical durable identity.",
        "Preserve preset ID in display/config while binding execution to AccountId + AccountScope."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-E9688DBE16",
      "sourceEvidence": [
        "packages/uta-protocol/src/brokers/preset-catalog.ts:582-588: mintInstanceId returns four random bytes as an eight-character hex token; create-route comments use it to seed Mock `_instanceId` and make each simulation fingerprint unique."
      ],
      "currentBehavior": "mintInstanceId returns four random bytes as an eight-character hex token; create-route comments use it to seed Mock `_instanceId` and make each simulation fingerprint unique.",
      "problem": "Random token generation is an effect hidden behind a pure-looking helper and currently feeds the credential-sensitive derived-ID scheme; uniqueness, persistence, and replay semantics are unspecified.",
      "preservedBehavior": [
        "Preserve eight-hex display shape if UI depends on it and unique freshly created simulations.",
        "Intentionally move minting out of generic preset/identity logic and forbid remint on reconnect or restart."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-21AAC99E0D",
      "sourceEvidence": [
        "packages/uta-protocol/src/brokers/presets.ts:1-16: The serialization module imports Zod, the complete broker preset catalog, and presentation-oriented preset types, then documents itself as the shared wizard-schema pipeline."
      ],
      "currentBehavior": "The serialization module imports Zod, the complete broker preset catalog, and presentation-oriented preset types, then documents itself as the shared wizard-schema pipeline.",
      "problem": "A public module that directly couples catalog definitions and Zod conversion can accidentally become the runtime source of truth and expose implementation metadata across the wire.",
      "preservedBehavior": [
        "Preserve the shared wizard’s one source for displayed preset metadata and schema labels.",
        "Intentionally sever the assumption that the JSON-schema rendering pipeline validates or creates broker connections.",
        "Keep write-only credential markers but never include credential values."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-F8525C7A99",
      "sourceEvidence": [
        "packages/uta-protocol/src/brokers/presets.ts:18-32: SerializedBrokerPreset exposes string IDs/labels/descriptions, category and engine unions, optional modes, subtitle fields, and an untyped `schema: Record<string, unknown>`."
      ],
      "currentBehavior": "SerializedBrokerPreset exposes string IDs/labels/descriptions, category and engine unions, optional modes, subtitle fields, and an untyped `schema: Record<string, unknown>`.",
      "problem": "Raw string IDs, engine unions, subtitle records, and an untyped schema make malformed presentation data representable and allow SDK/runtime values to cross the protocol boundary.",
      "preservedBehavior": [
        "Preserve all current catalog metadata and oneOf mode labels for the wizard.",
        "Intentionally replace `Record<string, unknown>` with a constrained view model and prevent UI schema from serving as an execution validator."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-5C311A55C1",
      "sourceEvidence": [
        "packages/uta-protocol/src/brokers/presets.ts:34-54: buildJsonSchema calls `z.toJSONSchema` and casts the result/props to records, replaces mode enum with oneOf titles, mutates writeOnly markers, and returns the raw object."
      ],
      "currentBehavior": "buildJsonSchema calls `z.toJSONSchema` and casts the result/props to records, replaces mode enum with oneOf titles, mutates writeOnly markers, and returns the raw object.",
      "problem": "The cast/mutation pipeline treats generated JSON as trusted and does not distinguish UI hints from validation authority; unsupported Zod values or future schema shapes can leak silently.",
      "preservedBehavior": [
        "Preserve human-readable mode oneOf titles and writeOnly markers used by the wizard.",
        "Intentionally stop trusting a casted mutable record and prevent credentials/defaults from being emitted as values.",
        "Keep mode labels separate from observed environment evidence."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-1D576100CB",
      "sourceEvidence": [
        "packages/uta-protocol/src/brokers/presets.ts:56-72: BUILTIN_BROKER_PRESETS maps every entry in BROKER_PRESET_CATALOG through metadata copies and buildJsonSchema; it exports a process-level array for clients."
      ],
      "currentBehavior": "BUILTIN_BROKER_PRESETS maps every entry in BROKER_PRESET_CATALOG through metadata copies and buildJsonSchema; it exports a process-level array for clients.",
      "problem": "The name “builtin” and eager map blur UI metadata with installed/runtime engine availability; a broken optional pack must not make this export unavailable or imply write capability.",
      "preservedBehavior": [
        "Preserve one serialized entry per catalog preset and current order for wizard compatibility.",
        "Intentionally rename semantics from “builtin runtime” to “presentation catalog”; optional packs remain lazy."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-41D1B056C8",
      "sourceEvidence": [
        "packages/uta-protocol/src/brokers/search-rules.ts:1-14: search-rules.ts documents that normalization is a deliberately isolated heuristic bridging vendor symbols to broker tickers, and defines AssetClassHint as equity/crypto/currency/commodity/unknown."
      ],
      "currentBehavior": "search-rules.ts documents that normalization is a deliberately isolated heuristic bridging vendor symbols to broker tickers, and defines AssetClassHint as equity/crypto/currency/commodity/unknown.",
      "problem": "The heuristic boundary is useful but a bare string return lets callers treat a vendor symbol or normalized query as canonical instrument identity, and the asset-class hint can be mistaken for observed venue metadata.",
      "preservedBehavior": [
        "Preserve centralized heuristic ownership and current identity-vs-symbol caution.",
        "Intentionally return a named pattern rather than allowing normalized text to masquerade as InstrumentId."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-819A30C052",
      "sourceEvidence": [
        "packages/uta-protocol/src/brokers/search-rules.ts:16-23: QUOTE_CURRENCIES is an ordered tuple with USDT/USDC/BUSD before USD and a case-insensitive suffix regex requiring a base of at least two alphanumeric characters."
      ],
      "currentBehavior": "QUOTE_CURRENCIES is an ordered tuple with USDT/USDC/BUSD before USD and a case-insensitive suffix regex requiring a base of at least two alphanumeric characters.",
      "problem": "The regex is intentionally narrow, but a global string constant does not record currency normalization/version and can strip a suffix without proving the venue actually lists that market.",
      "preservedBehavior": [
        "Preserve USDT-before-USD ordering, case-insensitive matching, and the two-character base guard.",
        "Intentionally keep suffix stripping out of canonical identity and out of non-crypto/non-currency classes."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-006AF2BF79",
      "sourceEvidence": [
        "packages/uta-protocol/src/brokers/search-rules.ts:25-55: normalizeBrokerSearchPattern trims input, strips a known quote suffix only for crypto/currency, uppercases the base on a match, and otherwise returns the trimmed symbol; equity, commodity, and unknown return identity."
      ],
      "currentBehavior": "normalizeBrokerSearchPattern trims input, strips a known quote suffix only for crypto/currency, uppercases the base on a match, and otherwise returns the trimmed symbol; equity, commodity, and unknown return identity.",
      "problem": "Returning a string loses whether normalization occurred, which rule version was used, and whether the result was validated by a venue. The default unknown class intentionally avoids heuristics but offers no type-level guard.",
      "preservedBehavior": [
        "Preserve trim, case, suffix order, class-specific behavior, and no heuristic for unknown/equity/commodity.",
        "Intentionally stop exposing a bare normalized string as if it were a contract identity."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-AE04CD83E4",
      "sourceEvidence": [
        "packages/uta-protocol/src/index.ts:1-20: The protocol barrel describes a long-term public wire package and re-exports all types, schemas, UTA client, broker preset catalog/serialization, and search rules."
      ],
      "currentBehavior": "The protocol barrel describes a long-term public wire package and re-exports all types, schemas, UTA client, broker preset catalog/serialization, and search rules.",
      "problem": "A catch-all barrel currently makes internal catalog helpers, legacy broker types, and SDK-dependent protocol types reachable to clients; any export change becomes a broad public API break without a capability/evidence boundary.",
      "preservedBehavior": [
        "Preserve current client/protocol role and coordinated versioning expectation.",
        "Intentionally stop re-exporting implementation-oriented helpers/SDK-linked types from a single catch-all barrel.",
        "Keep presentation and search public because callers need them, but with closed wire types."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-E43DFC70EF",
      "sourceEvidence": [
        "packages/uta-protocol/src/schemas/index.ts:1-10: The schemas barrel is intentionally empty while its header promises one Zod Request/Response pair per UTA HTTP endpoint and shared use by client SDK and Hono zValidator."
      ],
      "currentBehavior": "The schemas barrel is intentionally empty while its header promises one Zod Request/Response pair per UTA HTTP endpoint and shared use by client SDK and Hono zValidator.",
      "problem": "Routes currently lack a single typed boundary; an empty barrel permits raw request/response objects, generic errors, and mismatched client/server assumptions.",
      "preservedBehavior": [
        "Preserve route intent and shared client/server validation.",
        "Intentionally replace empty export with concrete schemas, without using schema success as runtime capability proof.",
        "Keep generic test-connection success/error only as a legacy projection while adopting typed probe results."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-6367D87808",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/factory.ts:1-20: factory.ts imports the legacy IBroker type, loadBrokerEngine, getBrokerPreset, UTAConfig, and FxService; its header defines the factory as preset-to-engine resolver and says presetId is the only persisted tie to implementations."
      ],
      "currentBehavior": "factory.ts imports the legacy IBroker type, loadBrokerEngine, getBrokerPreset, UTAConfig, and FxService; its header defines the factory as preset-to-engine resolver and says presetId is the only persisted tie to implementations.",
      "problem": "This composition module sits inside the legacy trading directory and imports both protocol catalog and SDK-facing legacy interfaces, making it the coupling point that prevents pure domain/runtime separation and hides AccountScope/evidence.",
      "preservedBehavior": [
        "Preserve presetId as the account config linkage and all keyless/read-only distinctions.",
        "Intentionally replace return type IBroker and hidden factory side effects with typed connection/evidence composition.",
        "Preserve existing account IDs via explicit migration rather than changing identity during assembly."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-C3B978232C",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/factory.ts:21-24: BrokerServices exposes optional `fxService?: FxService`, allowing callers to provide an FX service independently of broker support."
      ],
      "currentBehavior": "BrokerServices exposes optional `fxService?: FxService`, allowing callers to provide an FX service independently of broker support.",
      "problem": "An optional service plus duck-typed setter cannot express whether FX is required, which currencies/rate freshness are acceptable, or whether a missing service should block an action versus silently disable conversion.",
      "preservedBehavior": [
        "Preserve Longbridge multi-currency support and skip FX for single-currency brokers.",
        "Intentionally remove optional duck-typed service injection and silent omission of required conversion.",
        "Keep no-FX actions executable when capability evidence says conversion is unnecessary."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-093F5766C9",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/factory.ts:26-40: createBroker resolves a preset, parses raw presetConfig with that preset Zod schema, casts to a string-keyed record, translates to engine config, loads the engine, validates config but discards the parsed value, then calls generic createBroker with id/label/brokerConfig/keyless."
      ],
      "currentBehavior": "createBroker resolves a preset, parses raw presetConfig with that preset Zod schema, casts to a string-keyed record, translates to engine config, loads the engine, validates config but discards the parsed value, then calls generic createBroker with id/label/brokerConfig/keyless.",
      "problem": "The chain allows untyped raw config and discards validated output; `keyless` is hidden in a bag, secret-bearing engine config crosses the generic factory, and validation/load/identity/scope/evidence are not represented as transitions.",
      "preservedBehavior": [
        "Preserve per-preset Zod validation and engine-specific translation semantics (including CCXT keyless).",
        "Intentionally remove raw `Record<string,unknown>`/cast and discarded parse result.",
        "Preserve old IDs and sealed credential binding with explicit migration; no secret rehashing."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-E010E516DB",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/factory.ts:42-47: After creating a broker, factory checks for a `setFxService` property with an unknown cast and invokes it when present, making FX integration depend on optional method presence."
      ],
      "currentBehavior": "After creating a broker, factory checks for a `setFxService` property with an unknown cast and invokes it when present, making FX integration depend on optional method presence.",
      "problem": "Duck typing hides capability requirements and permits a broker to appear ready while a required multi-currency conversion service is absent; casts also bypass exhaustive adapter interfaces.",
      "preservedBehavior": [
        "Preserve FX behavior for Longbridge when supplied.",
        "Intentionally remove reflective setter invocation and unknown casts.",
        "Preserve no-op behavior for brokers/actions whose declared units require no conversion."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-D914E7F49F",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/index.ts:1-13: The brokers barrel re-exports legacy IBroker, positions/orders/account/quote/capability types and broker config fields from a local compat shim."
      ],
      "currentBehavior": "The brokers barrel re-exports legacy IBroker, positions/orders/account/quote/capability types and broker config fields from a local compat shim.",
      "problem": "The public local barrel makes legacy generic interfaces and protocol/SDK dependencies the default import surface, preventing source ownership from moving to broker-spi typed ports and leaking unsupported fields.",
      "preservedBehavior": [
        "Preserve all read model fields required by existing callers during typed projection.",
        "Intentionally remove the compat shim as the canonical surface and separate protocol views from runtime ports."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-16D7FC3B84",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/index.ts:15-16: The brokers barrel re-exports `createBroker` directly from the legacy factory."
      ],
      "currentBehavior": "The brokers barrel re-exports `createBroker` directly from the legacy factory.",
      "problem": "This export makes a side-effecting generic factory appear to be the broker domain API and lets callers bypass explicit scope, environment evidence, pack loading failures, and SecretRef resolution.",
      "preservedBehavior": [
        "Preserve existing account creation behavior through the new assembler.",
        "Intentionally remove the convenience export so callsites cannot bypass composition invariants."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-4D0C34ED53",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/index.ts:18-33: The barrel re-exports BROKER_PRESET_CATALOG, getBrokerPreset, isPaperPreset, and BUILTIN_BROKER_PRESETS from the protocol package, plus preset and serialization types."
      ],
      "currentBehavior": "The barrel re-exports BROKER_PRESET_CATALOG, getBrokerPreset, isPaperPreset, and BUILTIN_BROKER_PRESETS from the protocol package, plus preset and serialization types.",
      "problem": "A single domain barrel conflates UI catalog, boolean environment classification, and runtime imports; callers can pull presentation helpers into effect code and preserve the unsafe isPaper path.",
      "preservedBehavior": [
        "Preserve UI access to catalog and serialized preset types.",
        "Intentionally remove execution access to boolean helper through the legacy barrel.",
        "Preserve existing preset IDs and aliases."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-E981F551DA",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/index.ts:35-36: The barrel explicitly states live broker implementations are not re-exported; they are built and loaded as optional packs by registry.ts."
      ],
      "currentBehavior": "The barrel explicitly states live broker implementations are not re-exported; they are built and loaded as optional packs by registry.ts.",
      "problem": "This is the correct lazy-loading boundary, but the remaining barrel types/factory still provide a path around it and the registry currently returns legacy generic entries.",
      "preservedBehavior": [
        "Preserve optional pack lazy loading and no live implementation re-export.",
        "Intentionally tighten the descriptor boundary rather than re-exporting legacy `IBroker`.",
        "Keep Mock available through a typed built-in adapter path."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-6E311CAABB",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/presets.spec.ts:1-32: presets.spec.ts imports the catalog/preset helpers, serialization array, and registry loader, then frames the suite as Zod-to-engine round-trip tests."
      ],
      "currentBehavior": "presets.spec.ts imports the catalog/preset helpers, serialization array, and registry loader, then frames the suite as Zod-to-engine round-trip tests.",
      "problem": "The test imports a broad public barrel and treats parser acceptance plus generic engine schema acceptance as sufficient, so it cannot assert AccountScope, SecretRef redaction, environment evidence, or durable identity migration.",
      "preservedBehavior": [
        "Preserve coverage of every catalog preset and parser/engine translation drift.",
        "Intentionally stop using generic IBroker shape or `isPaper` as the primary oracle.",
        "Retain legacy ID and sealed-binding assertions introduced by explicit migration."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-D74564C976",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/presets.spec.ts:34-52: SAMPLE_CONFIGS is one `Record<string, Record<string, unknown>>` containing minimally valid raw values for each preset, including fake credentials, IBKR port, and Mock cash."
      ],
      "currentBehavior": "SAMPLE_CONFIGS is one `Record<string, Record<string, unknown>>` containing minimally valid raw values for each preset, including fake credentials, IBKR port, and Mock cash.",
      "problem": "The fixture is untyped, places credential material directly in raw configs, and encodes per-preset validity only by key naming; it cannot detect schema variants or prove that identity excludes secrets.",
      "preservedBehavior": [
        "Preserve a sample for every current preset and the existing values needed to catch translation drift.",
        "Intentionally remove raw secret strings from durable/serialized fixture paths; fake provider owns secret bytes only in memory."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-076B691F74",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/presets.spec.ts:54-71: Catalog integrity tests assert unique preset IDs, one sample config per catalog entry, and that unknown lookup throws a message matching `Unknown broker preset`."
      ],
      "currentBehavior": "Catalog integrity tests assert unique preset IDs, one sample config per catalog entry, and that unknown lookup throws a message matching `Unknown broker preset`.",
      "problem": "The tests pin generic throw text and only uniqueness/fixture presence; they do not assert stable aliases, catalog/runtime separation, or the distinction between unknown preset and unavailable pack.",
      "preservedBehavior": [
        "Preserve all known IDs and ordering/category invariants.",
        "Intentionally replace message regex with structured failure and add runtime availability distinction."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-F4B8428675",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/presets.spec.ts:73-88: Round-trip tests parse each sample with the preset Zod schema, call toEngineConfig, load the engine, and assert the engine config schema accepts the output."
      ],
      "currentBehavior": "Round-trip tests parse each sample with the preset Zod schema, call toEngineConfig, load the engine, and assert the engine config schema accepts the output.",
      "problem": "Schema acceptance is only a shape check; it cannot observe adapter environment/scope, reject secret leakage, or prove dispatch/capability semantics. The engine loader is also a generic legacy entry.",
      "preservedBehavior": [
        "Preserve detection of preset-to-adapter translation drift.",
        "Intentionally add evidence/scope/capability and pre-dispatch invariants; remove assertion that generic schema parse alone proves readiness."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-D3ABE635D3",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/presets.spec.ts:90-174: Translation tests assert venue-specific mode flags: Binance demo/live, OKX sandbox/live, Bybit testnet/demo, Hyperliquid sandbox, Bitget demo/options, Alpaca paper, Longbridge paper, IBKR pass-through, and CCXT empty optional-field dropping."
      ],
      "currentBehavior": "Translation tests assert venue-specific mode flags: Binance demo/live, OKX sandbox/live, Bybit testnet/demo, Hyperliquid sandbox, Bitget demo/options, Alpaca paper, Longbridge paper, IBKR pass-through, and CCXT empty optional-field dropping.",
      "problem": "These assertions protect useful legacy translations but call engine output plain objects and do not bind the resulting connection to observed environment, scope, capability, or typed unsupported fields.",
      "preservedBehavior": [
        "Preserve every currently tested venue flag (including OKX sandbox versus demoTrading distinction, Bitget no options, IBKR pass-through).",
        "Intentionally treat Binance testnet as schema unsupported, not make `isPaper` accept it.",
        "Preserve CCXT dropping empty/undefined optional fields."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-2092F69746",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/presets.spec.ts:176-218: isPaperPreset tests assert OKX/Bybit/Binance demo/testnet/paper modes, Alpaca/Longbridge paper, IBKR port 7497/4002, and CCXT sandbox/demo flags as true/false booleans."
      ],
      "currentBehavior": "isPaperPreset tests assert OKX/Bybit/Binance demo/testnet/paper modes, Alpaca/Longbridge paper, IBKR port 7497/4002, and CCXT sandbox/demo flags as true/false booleans.",
      "problem": "The tests institutionalize requested-mode/port/flag inference as an execution-level boolean and even accept a Binance testnet config that its preset schema does not expose.",
      "preservedBehavior": [
        "Preserve all valid venue mode distinctions for UI and native translation.",
        "Intentionally remove port/flag-only truth and invalid Binance testnet acceptance.",
        "Preserve simulator as Simulated, not generic paper."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-923D3E5DC8",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/presets.spec.ts:220-243: Serialization tests assert every catalog ID is emitted, mode fields become oneOf with human titles, and OKX credential fields receive writeOnly markers via casts into the schema shape."
      ],
      "currentBehavior": "Serialization tests assert every catalog ID is emitted, mode fields become oneOf with human titles, and OKX credential fields receive writeOnly markers via casts into the schema shape.",
      "problem": "The tests assert presentation shape but not redaction, schema versioning, SDK-free imports, or that serialized metadata cannot be consumed as runtime capabilities.",
      "preservedBehavior": [
        "Preserve complete catalog output, oneOf labels, and writeOnly markers.",
        "Intentionally add redaction/version assertions and reject arbitrary record payloads.",
        "Keep presentation-only semantics explicit."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-F45EE8D4C7",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/presets.spec.ts:245-302: deriveUtaId tests deterministic same-input hashes, `${preset.id}-${8hex}` shape, object-key-order invariance, ignored non-fingerprint fields, differing fingerprint values/presets, null normalization, and Mock `_instanceId` uniqueness."
      ],
      "currentBehavior": "deriveUtaId tests deterministic same-input hashes, `${preset.id}-${8hex}` shape, object-key-order invariance, ignored non-fingerprint fields, differing fingerprint values/presets, null normalization, and Mock `_instanceId` uniqueness.",
      "problem": "The tests codify credential-bearing fingerprint fields and a short hash as durable identity while lacking rotation, legacy alias, sealed-binding, collision, scope, and migration scenarios.",
      "preservedBehavior": [
        "Preserve exact old hash outputs for backward lookup and object-key-order behavior where legacy records require it.",
        "Intentionally stop treating hash shape as canonical new ID and stop using secrets/private keys in new identity projections.",
        "Preserve Mock instance uniqueness via durable instance allocation rather than cash/key fingerprinting."
      ],
      "openQuestions": [
        "Legacy git/sealed-account vectors must be captured from existing records before migration; old deriveUtaId output remains lookup-only and never new identity authority."
      ]
    },
    {
      "id": "MAP-EFDE4EFD6B",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/registry.spec.ts:1-28: registry.spec.ts creates a temporary OPENALICE_HOME, disables workspace packs, resets modules before each test, and recursively removes the temporary home/restores environment afterward."
      ],
      "currentBehavior": "registry.spec.ts creates a temporary OPENALICE_HOME, disables workspace packs, resets modules before each test, and recursively removes the temporary home/restores environment afterward.",
      "problem": "The fixture isolates filesystem state but treats cleanup as sufficient proof; it does not model durable pack activation, descriptor versions, connection-generation state, or preservation of account/journal data on failure.",
      "preservedBehavior": [
        "Preserve test isolation, env restoration, module reset, and recursive cleanup.",
        "Intentionally add durable account/history checks and typed pack-store abstraction.",
        "Do not infer runtime correctness from temp cleanup alone."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-964B3CADF0",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/registry.spec.ts:30-56: activateCcxtModule writes a release dist/index.js, package.json, broker-pack.json with schema/API/version/entry/content metadata, and active.json pointer under runtime/broker-packs/ccxt."
      ],
      "currentBehavior": "activateCcxtModule writes a release dist/index.js, package.json, broker-pack.json with schema/API/version/entry/content metadata, and active.json pointer under runtime/broker-packs/ccxt.",
      "problem": "The fixture encodes the current manifest/pointer format but only tests CCXT and permits source strings with legacy exports; it does not assert manifest identity, protocol capabilities, or atomic activation against active account state.",
      "preservedBehavior": [
        "Preserve current path layout and metadata fields needed by core resolver.",
        "Intentionally extend manifest to describe typed descriptor/capabilities and verify content identity.",
        "Preserve active old pack when new activation is invalid."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-2E9FCB05D5",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/registry.spec.ts:58-65: validModuleSource returns source text exporting numeric BROKER_PACK_API_VERSION, string BROKER_ENGINE, a duck-typed configSchema.parse, and generic createBroker that returns id/label/brokerEngine."
      ],
      "currentBehavior": "validModuleSource returns source text exporting numeric BROKER_PACK_API_VERSION, string BROKER_ENGINE, a duck-typed configSchema.parse, and generic createBroker that returns id/label/brokerEngine.",
      "problem": "The fixture bakes in the unsafe four-export module contract and validates only shape/echo behavior; it cannot model config codecs, scoped layer lifecycle, capabilities, observations, compensation, or protocol version evolution.",
      "preservedBehavior": [
        "Preserve API/engine identity checks and dynamic-module boundary behavior.",
        "Intentionally replace generic configSchema/createBroker echo with typed descriptor lifecycle and capability evidence.",
        "Preserve failures for missing exports."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-AB74D13B81",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/registry.spec.ts:67-80: The registry test expects absent live CCXT to reject with BrokerPackUnavailableError/code/engine while Mock remains built in and can create a broker with brokerEngine mock."
      ],
      "currentBehavior": "The registry test expects absent live CCXT to reject with BrokerPackUnavailableError/code/engine while Mock remains built in and can create a broker with brokerEngine mock.",
      "problem": "This couples test behavior to an Error subclass/code and checks Mock by generic brokerEngine string, but not readiness projection, scope, durable simulator instance, or fail-closed behavior when a live pack is missing.",
      "preservedBehavior": [
        "Preserve built-in Mock availability and unavailable-live-engine behavior.",
        "Intentionally replace Error-name/code/message matching and generic brokerEngine assertion with ADT/policy assertions.",
        "Preserve fail-closed no live fallback."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-1729810891",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/registry.spec.ts:82-91: The valid-pack test activates a source module, loads CCXT, creates a broker from id/label/raw brokerConfig, parses a raw mode with configSchema, and asserts echoed fields/brokerEngine."
      ],
      "currentBehavior": "The valid-pack test activates a source module, loads CCXT, creates a broker from id/label/raw brokerConfig, parses a raw mode with configSchema, and asserts echoed fields/brokerEngine.",
      "problem": "It proves only dynamic import and object forwarding; no environment/account probe, SecretRef boundary, capability evidence, action ACK/observation, or JournalWriter ordering is exercised.",
      "preservedBehavior": [
        "Preserve activated-pack loading and config parser acceptance.",
        "Intentionally replace id/label echo checks with consumer-observable connection/capability/action behavior.",
        "Preserve no mutation for load-only path."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-E2F6BC0E7E",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/registry.spec.ts:93-107: Invalid-module tests vary API version, engine identity, or required exports and expect BrokerPackUnavailableError with message regex details."
      ],
      "currentBehavior": "Invalid-module tests vary API version, engine identity, or required exports and expect BrokerPackUnavailableError with message regex details.",
      "problem": "Message substring matching is brittle and cannot distinguish decode failure phases or prevent a malformed descriptor from being treated as an unavailable installation.",
      "preservedBehavior": [
        "Preserve checks for API version, engine identity, and required exports.",
        "Intentionally replace message regex with exhaustive structured failure assertions and safe diagnostics.",
        "Preserve fail-closed behavior."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-2E5F0661A6",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/registry.spec.ts:109-120: The cache-eviction test loads a broken CCXT module, expects a rejection, activates a repaired release, then expects loadBrokerEngine to resolve a generic entry without restarting the process."
      ],
      "currentBehavior": "The cache-eviction test loads a broken CCXT module, expects a rejection, activates a repaired release, then expects loadBrokerEngine to resolve a generic entry without restarting the process.",
      "problem": "Evicting a failed Promise is useful, but cache semantics are tied to a mutable global map and do not define lifecycle generation, in-flight leases, or separation from durable account/transaction state.",
      "preservedBehavior": [
        "Preserve retry after failed load without process restart.",
        "Intentionally separate descriptor-cache eviction from active connection/transaction recovery and require explicit generation handling.",
        "Preserve no blind redispatch of unknown actions."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-507FF8C434",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/registry.spec.ts:122-134: The corrupt-pointer test writes `{not-json` to active.json and expects a BrokerPackUnavailableError whose message says the pointer is invalid."
      ],
      "currentBehavior": "The corrupt-pointer test writes `{not-json` to active.json and expects a BrokerPackUnavailableError whose message says the pointer is invalid.",
      "problem": "The malformed JSON path is collapsed into generic unavailable/error text; callers cannot distinguish pointer decode corruption from absent installation or decide whether to retain/rollback the last known active release.",
      "preservedBehavior": [
        "Preserve actionable corrupt-pointer handling and fail closed.",
        "Intentionally replace message matching with structured phase/cause and explicit last-known-good policy.",
        "Preserve account/config readability on registry failure."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-CEF7AFFAF1",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/registry.ts:1-6: registry.ts header states live engines are optional runtime packs and Mock is the only built-in engine so unused vendor SDKs are not evaluated at UTA startup."
      ],
      "currentBehavior": "registry.ts header states live engines are optional runtime packs and Mock is the only built-in engine so unused vendor SDKs are not evaluated at UTA startup.",
      "problem": "The lazy optional-pack intent is valuable, but the current boundary still returns legacy IBroker objects and does not describe scoped capabilities, evidence, or compensation; startup safety alone is not action safety.",
      "preservedBehavior": [
        "Preserve optional live packs and built-in Mock startup behavior.",
        "Intentionally extend lazy loading from generic broker object to typed capability/evidence descriptor.",
        "Preserve fail-closed startup when a requested pack is absent."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-D47789CDE0",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/registry.ts:8-21: The registry imports pathToFileURL/resolve, Zod types, legacy IBroker/types, MockBroker, protocol BrokerEngine, and core pack resolver/version helpers; all vendor loading is orchestrated here."
      ],
      "currentBehavior": "The registry imports pathToFileURL/resolve, Zod types, legacy IBroker/types, MockBroker, protocol BrokerEngine, and core pack resolver/version helpers; all vendor loading is orchestrated here.",
      "problem": "This import graph couples registry to the legacy service directory, Zod runtime shape, and generic IBroker while making core pack resolution and runtime descriptor concerns indistinguishable.",
      "preservedBehavior": [
        "Preserve path/core resolver integration and Mock availability.",
        "Intentionally remove legacy IBroker/Zod-shaped entry from registry contract.",
        "Preserve no vendor SDK import during unrelated startup."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-D74C77540F",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/registry.ts:23-26: BrokerEngineEntry declares a Zod `configSchema` and a generic createBroker accepting id/optional label/raw brokerConfig and returning legacy IBroker."
      ],
      "currentBehavior": "BrokerEngineEntry declares a Zod `configSchema` and a generic createBroker accepting id/optional label/raw brokerConfig and returning legacy IBroker.",
      "problem": "The interface encodes untyped config bags and a generic broker object, so it cannot guarantee AccountScope, capability evidence, typed action semantics, or serializable-plan/Effect separation.",
      "preservedBehavior": [
        "Preserve per-engine config validation and broker operation access through a typed adapter.",
        "Intentionally remove raw brokerConfig and generic IBroker return.",
        "Preserve keyless/read-only semantics as explicit capability variants."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-D37C32D3D3",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/registry.ts:28-33: BrokerPackModule describes four exports: numeric BROKER_PACK_API_VERSION, string BROKER_ENGINE, Zod configSchema, and generic createBroker."
      ],
      "currentBehavior": "BrokerPackModule describes four exports: numeric BROKER_PACK_API_VERSION, string BROKER_ENGINE, Zod configSchema, and generic createBroker.",
      "problem": "The export contract has no schema for capability evidence, actions, observations, account scope, protocol manifest, or compensation; arbitrary module values are cast later.",
      "preservedBehavior": [
        "Preserve API version and engine identity checks.",
        "Intentionally expand beyond four generic exports to the target SPI and fail closed on malformed descriptors.",
        "Preserve separation of descriptor code from serializable plan data."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-3BB0EF71F5",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/registry.ts:35-42: BrokerPackUnavailableError is an Error subclass with fixed code BROKER_PACK_UNAVAILABLE and engine, and its default/detail messages mix absent installation with invalid installed/workspace pack failures."
      ],
      "currentBehavior": "BrokerPackUnavailableError is an Error subclass with fixed code BROKER_PACK_UNAVAILABLE and engine, and its default/detail messages mix absent installation with invalid installed/workspace pack failures.",
      "problem": "One exception class collapses absent, corrupt pointer, protocol mismatch, import failure, and workspace policy cases; callers must parse message text and cannot choose safe retry/rollback/recovery behavior.",
      "preservedBehavior": [
        "Preserve actionable install guidance for absent packs.",
        "Intentionally replace Error subclass/message parsing with exhaustive failure variants.",
        "Preserve fail-closed loading and no fallback from broken installed pack."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-30B07B33AF",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/registry.ts:44-50: workspaceEntries maps installable engines ccxt/alpaca/ibkr/leverup/longbridge to source entry paths under packages/*/src/index.ts."
      ],
      "currentBehavior": "workspaceEntries maps installable engines ccxt/alpaca/ibkr/leverup/longbridge to source entry paths under packages/*/src/index.ts.",
      "problem": "A raw engine-to-path record makes workspace source resolution packaging policy and runtime engine identity one untyped map; a path can point to stale/unsafe source and bypass installed release provenance.",
      "preservedBehavior": [
        "Preserve source workspace loading for approved dev/test scenarios and engine coverage.",
        "Intentionally make policy explicit and fail closed; no workspace fallback after broken installed release.",
        "Preserve installed release precedence."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-AD49E3FECC",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/registry.ts:52-55: The registry cache is a Map keyed by BrokerEngine to Promise<BrokerEngineEntry>, and clearBrokerEngineCache simply clears that map."
      ],
      "currentBehavior": "The registry cache is a Map keyed by BrokerEngine to Promise<BrokerEngineEntry>, and clearBrokerEngineCache simply clears that map.",
      "problem": "Caching promises by engine conflates immutable pack definition with active connection/credential state and does not identify content/protocol generation; clearing can invalidate assumptions while transactions are in flight.",
      "preservedBehavior": [
        "Preserve cache reuse and explicit clear function for tests/dev.",
        "Intentionally separate descriptor cache from runtime connection and transaction state.",
        "Preserve failed-load eviction policy in load function."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-D0D9F4AA44",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/registry.ts:58-67: loadBrokerEngine checks cache, invokes uncached loading, deletes the cache entry on rejection, and returns a Promise<BrokerEngineEntry> to callers."
      ],
      "currentBehavior": "loadBrokerEngine checks cache, invokes uncached loading, deletes the cache entry on rejection, and returns a Promise<BrokerEngineEntry> to callers.",
      "problem": "Promise-level retry behavior is useful but return type hides whether failure is absent/invalid, and a rejected load can be retried without coordinating active connection generations, leases, or transaction Unknown outcomes.",
      "preservedBehavior": [
        "Preserve failed-load cache eviction and reusable successful definitions.",
        "Intentionally require generation-aware connection recovery and prohibit blind action retry.",
        "Preserve absent engine as typed unavailability."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-5C8AA54A45",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/registry.ts:69-111: loadBrokerEngineUncached handles Mock directly; validates installable engine, resolves active pack, imports installed module or (if allowed) workspace source, wraps each failure in BrokerPackUnavailableError, and throws when no pack exists."
      ],
      "currentBehavior": "loadBrokerEngineUncached handles Mock directly; validates installable engine, resolves active pack, imports installed module or (if allowed) workspace source, wraps each failure in BrokerPackUnavailableError, and throws when no pack exists.",
      "problem": "The branch order correctly enforces installed-before-workspace, but dynamic module validation is generic and installed/workspace/Mock outcomes collapse to one Error; no typed readiness or rollback semantics exist.",
      "preservedBehavior": [
        "Preserve built-in Mock, installable-engine validation, installed precedence, workspace env policy, cache failure eviction, and actionable unavailable behavior.",
        "Intentionally remove generic broker object and all workspace fallback after a broken installed release.",
        "Preserve no SDK evaluation for unrequested engines."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-D804E25471",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/registry.ts:113-124: validateModule accepts raw unknown, casts it to Partial<BrokerPackModule>, checks API version and engine string, verifies configSchema/createBroker presence, then returns a generic BrokerEngineEntry."
      ],
      "currentBehavior": "validateModule accepts raw unknown, casts it to Partial<BrokerPackModule>, checks API version and engine string, verifies configSchema/createBroker presence, then returns a generic BrokerEngineEntry.",
      "problem": "The cast bypasses boundary validation and checks only presence, allowing malformed schemas, functions, SDK objects, missing capability/evidence, or incompatible action handlers to pass as a valid adapter.",
      "preservedBehavior": [
        "Preserve API/engine checks and missing required export rejection.",
        "Intentionally eliminate `as Partial` and presence-only validation.",
        "Preserve fail-closed behavior before connection/action effects."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-13ABF99986",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/registry.ts:126-131: workspacePacksAllowed permits workspace packs when explicit env is 1, denies when 0, otherwise allows NODE_ENV=test or OPENALICE_LAUNCHER=dev."
      ],
      "currentBehavior": "workspacePacksAllowed permits workspace packs when explicit env is 1, denies when 0, otherwise allows NODE_ENV=test or OPENALICE_LAUNCHER=dev.",
      "problem": "Environment-string policy is concise but untyped and can accidentally enable source packs in an unsafe launch context; it does not express signed release provenance or account risk policy.",
      "preservedBehavior": [
        "Preserve explicit env override semantics and default test/dev behavior used by existing fixtures.",
        "Intentionally avoid treating workspace allowance as capability/evidence or live deploy authorization.",
        "Preserve production fail-closed default."
      ],
      "openQuestions": []
    },
    {
      "id": "MAP-24015C5378",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/types.ts:1-7: The local brokers/types.ts is a compat shim that re-exports every canonical broker type from `@traderalice/uta-protocol` so existing relative imports continue to compile until the directory moves."
      ],
      "currentBehavior": "The local brokers/types.ts is a compat shim that re-exports every canonical broker type from `@traderalice/uta-protocol` so existing relative imports continue to compile until the directory moves.",
      "problem": "The shim hides ownership and keeps callers dependent on the broad protocol barrel/legacy type surface; leaving it in place after migration would preserve the unwanted import graph and generic IBroker contract.",
      "preservedBehavior": [
        "Preserve source compatibility only during the atomic caller migration.",
        "Intentionally delete the shim rather than retain aliases/deprecated re-exports.",
        "Preserve durable identity and sealed binding while type ownership changes."
      ],
      "openQuestions": []
    }
  ]
}
