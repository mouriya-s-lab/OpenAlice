<!-- Generated by scripts/uta-effect-runtime-solutions.mjs from ownership.json + analyses.json. -->

# Ibkr — source investigation and migration evidence


## Entries

### MAP-0CE70CB540

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/ibkr-types.ts:35-44](../../../../services/uta/src/domain/trading/brokers/ibkr/ibkr-types.ts#L35-L44)
- symbol: TickSnapshot
- id: MAP-0CE70CB540
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/ibkr-types.ts:35-44: TickSnapshot has optional unvalidated numeric bid/ask/last/volume/high/low/lastTimestamp fields.
- currentBehavior: Snapshot accumulation represents missing ticks as undefined and later getQuote converts absent last/bid/ask/volume to string zero; no quality/source/currency schema exists.
- problem: Optional raw numbers cannot distinguish absent, invalid, delayed, stale, or real zero values and cannot express partial/unavailable observations.
- preservedBehavior:
  - Keep live and delayed tick code handling and tickSnapshotEnd barrier.
  - Stop mapping absence to zero in domain/accounting.
- openQuestions: —

### MAP-0D20097BFE

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:225-229](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts#L225-L229)
- symbol: close lifecycle
- id: MAP-0D20097BFE
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:225-229: close clears the heartbeat timer, stops account subscription, and calls client.disconnect directly.
- currentBehavior: Manual close performs local cleanup but does not coordinate pending bridge requests, supervised fibers, journal workers, or durable recovery classification.
- problem: A caller can close while a request/dispatch is pending, leaving unresolved Deferreds or ambiguous remote outcome; cleanup ordering is not tied to Scope interruption.
- preservedBehavior:
  - Keep unsubscribe before disconnect and deterministic timer cancellation.
  - Add pending-request and recovery coordination.
- openQuestions: —

### MAP-1141592A9E

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:49-71](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts#L49-L71)
- symbol: native contract ownership clone
- id: MAP-1141592A9E
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:49-71: cloneContract shallow-copies a native Contract and deep-copies comboLegs and deltaNeutralContract before ownership changes; constants and OptionMarkOverlay are declared beside it.
- currentBehavior: Routing and cache boundaries clone native Contract instances, including mutable combo leg and delta-neutral subobjects; the clone is passed to EClient or returned to a caller.
- problem: The helper is correctly defensive but remains coupled to SDK classes and its clone depth is not a schema/ownership contract; exposing it would encourage kernel dependence on mutable native objects.
- preservedBehavior:
  - Preserve non-mutation of staged/cached contracts and deep copy of combo/delta-neutral data.
  - Keep the helper private to the adapter rather than broadening native capability.
- openQuestions: —

### MAP-18B5B86E3E

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:169-180](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts#L169-L180)
- symbol: interval heartbeat
- id: MAP-18B5B86E3E
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:169-180: startHeartbeat creates a 45-second setInterval currentTime probe, logs warning/marks bridge dead on rejection, and calls unref.
- currentBehavior: Heartbeat is a detached raw timer; failure only toggles local dead state and emits a warning, with no durable relation to pending dispatches or reconnect attempts.
- problem: The timer is not supervised, has no cancellation guarantee beyond manual close, and cannot distinguish stale generation or schedule reconciliation after liveness loss.
- preservedBehavior:
  - Keep 45-second liveness intent and no process pinning as an operational policy, not an untracked timer.
  - Add deterministic finalization and recovery trigger.
- openQuestions: —

### MAP-1E12763B4D

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/ibkr-contracts.ts:32-45](../../../../services/uta/src/domain/trading/brokers/ibkr/ibkr-contracts.ts#L32-L45)
- symbol: classifyIbkrError
- id: MAP-1E12763B4D
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/ibkr-contracts.ts:32-45: classifyIbkrError turns a numeric TWS code/message into BrokerError with a message containing raw code/text.
  - services/uta/src/domain/trading/brokers/ibkr/request-bridge.ts:488-517: bridge routes reqId-specific errors to pending/order waiters and handles system connectivity errors separately.
- currentBehavior: Errors cross the adapter as legacy BrokerError code/message pairs without request/order/generation context, so the same text can be interpreted differently by read and mutation callers.
- problem: The result is a legacy Error/BrokerError with message-based semantics, cannot distinguish request/read/write/connection context, and cannot preserve a mutation's unknown remote outcome.
- preservedBehavior:
  - Retain native code and redacted diagnostic text for operator evidence.
  - Preserve separate system/request/order routing rather than turning all errors into one rejection.
- openQuestions: —

### MAP-2024564D67

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:384-476](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts#L384-L476)
- symbol: expandContract
- id: MAP-2024564D67
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:384-476: expandContract handles issuer directories, conId option/future leaves, filtered sorting/limits, and option parameter grids; it returns raw Contracts or unvalidated grid records with hints.
- currentBehavior: Expansion parses string keys; issuer queries return sorted bonds; numeric underlyings produce concrete FUT/OPT contracts or an OPT grid, with limit clamped to 1..200 and hints when truncated.
- problem: The expansion grammar and hints are useful but raw SDK values, optional filter bag, and unvalidated grid objects cannot safely cross protocol; an explicit limit is not pagination or proof of complete absence.
- preservedBehavior:
  - Keep issuer/bond, OPT/FUT, grid, sorting, filtering, and bounded output behavior.
  - Do not treat hints as silently complete data; expose pagination/total explicitly.
- openQuestions: —

### MAP-2992D9D142

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:724-757](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts#L724-L757)
- symbol: fetchOptionMark
- id: MAP-2992D9D142
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:724-757: fetchOptionMark requests bid/ask snapshot, validates finite positive values, computes midpoint and multiplier-aware overlay, caches success, catches every error to cache null and return null.
- currentBehavior: Any error—including entitlement, closed market, timeout, malformed callback, and programming defects—is converted into a 60-second null cache while the old broker mark is retained.
- problem: Catch-all suppression erases failure category and can let an unusable/stale mark appear as a successful read; downstream cannot decide whether to retry, degrade, or stop a precondition.
- preservedBehavior:
  - Preserve positive bid/ask validation, midpoint and multiplier arithmetic, and old value retention for expected unavailable data.
  - Stop hiding programmer/protocol defects in the same fallback.
- openQuestions: —

### MAP-2C43F936E4

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts:1-87](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts#L1-L87)
- symbol: broker contract-I/O fixtures and doubles
- id: MAP-2C43F936E4
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts:1-87: bareBroker constructs an uninitialized prototype; fixtures use native Contract/Order and a mocked bridge/client with callback-shaped methods.
- currentBehavior: The harness bypasses construction with Object.create, injects private bridge/client fields through casts, and records native SDK objects as the only observable request/response values. This is useful for exercising adapter seams without TWS but cannot prove a scoped service or schema boundary.
- problem: Keeping bare-prototype/cast fixtures as the target test harness would let tests pass without resource acquisition, account scope, callback decoding, or durable dispatch identity; it would also make malformed SDK fields invisible.
- preservedBehavior:
  - Keep recorded native request arguments as evidence of routing and order semantics.
  - Remove private-field casts and bare prototypes from Layer conformance fixtures once the port exists; retain focused no-wire unit fixtures when the absence of a resource is the behavior under test.
- openQuestions: —

### MAP-34DBADFED6

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts:242-259](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts#L242-L259)
- symbol: FX close-position routing test
- id: MAP-34DBADFED6
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts:242-259: closePosition finds a USD.CHF cached position and delegates a cloned contract to placeOrder without changing the position contract.
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:561-585: closePosition matches by conId or resolveSymbol, chooses opposite side and requested/current quantity, clones the TWS contract, and delegates market DAY order.
- currentBehavior: The close helper uses the observed position’s native contract and side, preserves IDEALPRO routing, and delegates to the ordinary order path.
- problem: Matching by display symbol is ambiguous, quantity is read from a potentially stale cache, and the delegated write has no exposure precondition or durable close identity.
- preservedBehavior:
  - Keep venue contract clone and opposite-side market close semantics.
  - Replace display-symbol fallback with canonical InstrumentId; preserve it only as presentation lookup.
- openQuestions: —

### MAP-39D2A7D5F0

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:509-543](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts#L509-L543)
- symbol: legacy modifyOrder
- id: MAP-39D2A7D5F0
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:509-543: modifyOrder fetches open/completed order, merges Partial&lt;Order&gt; fields into original.order in place, resolves its contract, probes, reuses numeric orderId via placeOrder, and returns string result.
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts:218-240: test verifies polluted conId modification routes through canonical CASH/IDEALPRO/CHF contract.
- currentBehavior: The method loads a legacy order snapshot, mutates that native object, and reuses its numeric id to submit a replacement; it does not capture version/before-image or persist intent.
- problem: Partial&lt;Order&gt; permits invalid combinations and omits full order semantics; in-place mutation destroys the exact before-image needed for compensation, while orderId reuse without version fencing races fills/cancelations.
- preservedBehavior:
  - Preserve canonical routing and numeric order-id modification semantics.
  - Replace in-place mutation with immutable plan and version precondition.
- openQuestions: —

### MAP-3AE4A4B8AE

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts:218-240](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts#L218-L240)
- symbol: canonical modification routing test
- id: MAP-3AE4A4B8AE
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts:218-240: getOrder returns a polluted conId contract; modifyOrder emits canonical CASH/IDEALPRO/CHF routing fields after resolution.
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:509-539: modifyOrder fetches original, mutates it, resolves original.contract, then calls placeOrder with same numeric order id.
- currentBehavior: Modification keeps the order id while replacing the contract sent to TWS with canonical routing; caller pollution is ignored for conId resolution.
- problem: The test does not bind a versioned before-image or prove that the order being modified still has the fetched contract/version; in-place mutation can alter cached observation before dispatch.
- preservedBehavior:
  - Preserve canonical FX routing and same numeric broker order id for a valid modification.
  - Change in-place mutation to immutable plan generation.
- openQuestions: —

### MAP-3B7869F8BB

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:662-699](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts#L662-L699)
- symbol: getPositions and option refresh orchestration
- id: MAP-3B7869F8BB
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:662-699: getPositions copies cached positions, selects OPT/FOP rows, refreshes selected rows through a cursor-limited worker pool, and leaves other rows untouched.
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:669-675: comments state updatePortfolio marks freeze overnight and getQuote is the fresher snapshot channel.
- currentBehavior: The query returns a shallow copy of the account cache, concurrently refreshes only option/FOP marks with a hard-coded concurrency of eight, and silently returns cached values when overlay fails.
- problem: The result is an ephemeral cache snapshot with no AccountScope/asOf/freshness; concurrency is not capability/rate-policy data, and failures are invisible to accounting or reconciliation.
- preservedBehavior:
  - Keep option/FOP-only snapshot overlay, hard cap intent, and non-option untouched semantics.
  - Expose partial/freshness instead of silently returning an apparently complete array.
- openQuestions: —

### MAP-3FBCE06D0D

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:606-660](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts#L606-L660)
- symbol: getAccount accounting projection
- id: MAP-3FBCE06D0D
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:606-660: getAccount reads cache, converts position PnL/market value by ExchangeRate, prefers finite broker NetLiquidation, reconstructs as fallback, and emits legacy decimal-string AccountInfo with zero defaults.
  - services/uta/src/domain/trading/brokers/ibkr/request-bridge.ts:644-678: account values are stored under composite currency keys and swapped from pending to live cache at accountDownloadEnd.
- currentBehavior: Account data is cache-backed; foreign positions are converted, broker NetLiquidation wins when finite, fallback uses cash plus sided market value, and absent tags become zero-valued legacy fields.
- problem: The adapter loses AccountScope, source timestamps, key currency provenance, and uncertainty; zero defaults can masquerade as valid cash/margin and a stale cache can be used for execution.
- preservedBehavior:
  - Preserve mixed-currency conversion and broker NetLiquidation precedence.
  - Preserve reconstruction only as typed fallback; remove zero-as-valid for executable decisions.
- openQuestions: —

### MAP-444B5B305F

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:759-775](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts#L759-L775)
- symbol: getOrders and getOpenOrders
- id: MAP-444B5B305F
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:759-775: getOrders/getOpenOrders call reqOpenOrders, filter by numeric orderId/enrich fill data, and document that manual TWS orders are excluded because reqAllOpenOrders is deferred.
  - services/uta/src/domain/trading/brokers/ibkr/request-bridge.ts:244-269: open/completed order collectors are single-slot and resolve on openOrdersEnd/completedOrdersEnd or timeout.
  - packages/ibkr/src/client/orders.ts:18-28: EClient declares reqAllOpenOrders and reqCompletedOrders; declaration presence does not establish runtime permission or namespace completeness.
- currentBehavior: Listing covers only this clientId’s open orders, returns raw CollectedOpenOrder rows, and attaches ephemeral average-fill data; absence from this list is not proved absence from IBKR.
- problem: A partial namespace listing cannot reconcile unknown dispatches or external/manual orders, and single-slot collectors cannot express completeness, scope, pagination, or late callbacks.
- preservedBehavior:
  - Keep clientId open-order query and fill enrichment as one supported namespace.
  - Make manual/external omission explicit and add complete listing contract rather than claiming all orders.
- openQuestions:
  - Runtime permission and namespace-completeness behavior for reqAllOpenOrders and permId lookup must be observed before advertising Complete coverage; SDK declarations alone are insufficient.

### MAP-486EF87033

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts:291-325](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts#L291-L325)
- symbol: nativeKey hub/leaf identity tests
- id: MAP-486EF87033
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts:291-325: getNativeKey prioritizes conId, uses issuer:e1400789 for a BOND issuer directory, uses bare symbol for convenience, refuses issuer directories in resolveNativeKey, and round-trips conId/symbol.
- currentBehavior: The adapter already separates three key namespaces: numeric tradeable conId, issuer directory, and symbol convenience; issuer directories are explicitly rejected by resolution.
- problem: The string grammar is only conventionally typed, does not include account/venue context, and the issuer refusal currently throws generic Error rather than a protocol-level non-tradeable result.
- preservedBehavior:
  - Preserve conId precedence and issuer directory refusal.
  - Preserve symbol convenience for search/display, but require an explicit resolution step before mutation.
- openQuestions: —

### MAP-5368C5F5A6

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:289-292](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts#L289-L292)
- symbol: single contract details query
- id: MAP-5368C5F5A6
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:289-292: getContractDetails awaits contractDetailsQuery and returns the first row or null.
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:294-313: contractDetailsQuery returns an array from requestCollector and propagates collector rejection.
- currentBehavior: The helper collapses an empty successful array to null but allows a rejected query to throw, and returns the raw first ContractDetails object.
- problem: Null conflates NoMatch with an unavailable/error path for callers that add catch fallbacks; raw details are unvalidated and do not carry identity or observation provenance.
- preservedBehavior:
  - Keep first-row convenience for explicitly read-only queries only.
  - Make empty success explicit and preserve thrown failures as typed.
- openQuestions: —

### MAP-54D5DEDE7A

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts:120-158](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts#L120-L158)
- symbol: polluted conId lookup test
- id: MAP-54D5DEDE7A
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts:120-158: a polluted conId input is sent through a clean lookup with empty symbol/secType/exchange/currency, then the canonical USD.CHF CASH/IDEALPRO/CHF contract is used.
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:322-348: resolveConIdContract creates a query containing only conId and removes a rejected promise so a later lookup retries.
- currentBehavior: The test proves conId is authoritative: display-only USDCHF/STK/SMART/USD fields do not narrow the contract-details query, and the returned canonical FX routing is used for the order.
- problem: The current contract proves a broker-specific invariant but leaves the identity relationship implicit and does not tie the canonical lookup to account/connection scope or a persisted observation.
- preservedBehavior:
  - Keep clean conId lookup and USD.CHF IDEALPRO routing.
  - Do not allow caller display fields to override canonical venue/currency.
- openQuestions: —

### MAP-54F1CD6C8B

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts:160-176](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts#L160-L176)
- symbol: symbol convenience non-mutation test
- id: MAP-54F1CD6C8B
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts:160-176: symbol-form STK input remains without exchange/currency while the sent clone receives STK/SMART/USD defaults and no contract-details lookup occurs.
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:350-381: resolveRoutableContract clones a no-conId contract and applies STK defaults, while non-STK missing routing fields fail.
- currentBehavior: The adapter applies convenience defaults to a clone, preserving the caller-owned Contract and avoiding a lookup for a symbol-form stock.
- problem: The useful ownership rule is not encoded in a typed distinction between convenience search and executable identity; defaulting can become an accidental trade assumption if a symbol is persisted without its resolved InstrumentId.
- preservedBehavior:
  - Preserve non-mutation and STK/SMART/USD defaults for explicitly tagged convenience reads.
  - Change executable symbol-only actions to fail until identity resolution is complete.
- openQuestions: —

### MAP-58696726F2

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/ibkr-types.ts:28-33](../../../../services/uta/src/domain/trading/brokers/ibkr/ibkr-types.ts#L28-L33)
- symbol: PendingRequest
- id: MAP-58696726F2
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/ibkr-types.ts:28-33: PendingRequest stores resolve/reject callbacks, a raw setTimeout handle, and generic T=unknown default.
- currentBehavior: All request families share an untyped pending entry; timeout rejects Error and generic unknown values can complete callers without a relation between request and result schema.
- problem: The callback/result/error relationship is erased, cancellation is not Scope-aware, and a timed-out mutation cannot be classified as Unknown versus NotSent.
- preservedBehavior:
  - Keep reqId/orderId routing and timeout protection.
  - Replace generic callbacks/timers with typed family and Scope cancellation.
- openQuestions: —

### MAP-5F63915F5B

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:350-381](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts#L350-L381)
- symbol: resolveRoutableContract
- id: MAP-5F63915F5B
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:350-381: resolveRoutableContract resolves conId without caller routing pollution, preserves aliceId, defaults STK symbol fields, and throws when non-STK fields are missing.
- currentBehavior: ConId path returns canonical clone and copies caller aliceId; symbol/STK path receives SMART/USD defaults; non-STK path validates required fields and fails loudly.
- problem: The API takes mutable SDK Contract and an untyped aliceId hint; STK defaults can make a symbol appear executable without a canonical InstrumentId, while missing fields are legacy EXCHANGE errors.
- preservedBehavior:
  - Keep canonical conId precedence, STK convenience, and loud non-STK refusal.
  - Intentionally do not broaden unsupported routing defaults.
- openQuestions: —

### MAP-661D1A0287

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:545-559](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts#L545-L559)
- symbol: legacy cancelOrder
- id: MAP-661D1A0287
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:545-559: cancelOrder probes liveness, calls EClient.cancelOrder, waits on the orderPending promise, and then synthesizes a Cancelled OrderState; requestOrder is resolved by any matching openOrder callback or by orderStatus('Cancelled').
  - services/uta/src/domain/trading/brokers/ibkr/request-bridge.ts:233-241,738-777: requestOrder has one orderId slot; openOrder resolves it, while orderStatus separately resolves it for status Cancelled with an empty Contract/Order.
- currentBehavior: The method can report success after an unrelated/mere openOrder callback because the shared orderPending promise is not cancellation-specific; a Cancelled status also resolves it, after which a synthetic local OrderState lacking native identity/fill/version is returned. Timeout/error is generic and no durable dispatch exists.
- problem: Cancellation is not proven by the current pending promise: an openOrder callback may satisfy the waiter before venue cancellation, and the synthesized state can hide a concurrent fill. Connection loss after cancel send is unknown, while no journal barrier or completeness evidence exists.
- preservedBehavior:
  - Keep liveness probe and waiting for venue cancellation evidence.
  - Correct synthetic Cancelled state to a typed observation tied to native evidence.
- openQuestions: —

### MAP-69B1FD6A99

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/ibkr-types.ts:13-24](../../../../services/uta/src/domain/trading/brokers/ibkr/ibkr-types.ts#L13-L24)
- symbol: IbkrBrokerConfig
- id: MAP-69B1FD6A99
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/ibkr-types.ts:13-24: IbkrBrokerConfig declares optional id/label/host/port/clientId/accountId and comments a default TWS paper port plus first managed-account auto-detection.
- currentBehavior: The config interface permits an incomplete endpoint, client identity, and account selection; comments authorize first-account fallback without representing a linked-account directory or environment evidence.
- problem: Optional host/port/clientId/accountId primitives permit ambiguous or invalid connection/account state and cannot represent linked accounts, account jurisdiction, tier/reach, or capability context.
- preservedBehavior:
  - Keep TWS/Gateway auth delegation and endpoint defaults as explicit configuration.
  - Replace first-account auto-detection with an explicit directory/selector.
- openQuestions:
  - Current source only supplies account codes; subAccountId and environment evidence need observed provider fields, not inferred defaults.

### MAP-6D904561E8

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:841-870](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts#L841-L870)
- symbol: getMarketClock
- id: MAP-6D904561E8
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:841-870: getMarketClock uses TWS currentTime when available, falls back to local Date on any error, and computes Mon-Fri 09:30-16:00 America/New_York as boolean isOpen.
  - services/uta/src/domain/trading/brokers/ibkr/README.md:54-71: README documents separate pre/regular/after-hours/overnight sessions and that updatePortfolio can freeze while reqMktData sees overnight data.
- currentBehavior: A single account-wide NYSE clock uses local fallback and ignores holidays, half-days, instrument venue, session policy, and per-contract tradingHours.
- problem: An available local Date can conceal TWS clock failure and report open for non-NYSE contracts; boolean isOpen cannot express RegularOnly versus ExtendedAllowed or an unknown calendar.
- preservedBehavior:
  - Keep TWS server time as preferred clock and support query-only fallback if clearly marked degraded.
  - Preserve existing regular-hours baseline only as a named fallback policy, never as proof for other instruments.
- openQuestions:
  - The current mapped method does not query ContractDetails.tradingHours; exact IBKR calendar encoding and holiday semantics need runtime evidence before enabling venue-specific writes.

### MAP-6F9134EF6E

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts:475-535](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts#L475-L535)
- symbol: dead-connection and write-probe tests
- id: MAP-6F9134EF6E
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts:475-535: dead connection rejects cache-backed reads and writes; placeOrder probes currentTime before client.placeOrder; probe failure marks dead without sending; modify/cancel share the probe.
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:141-162: _ensureAlive refuses known-dead state and _ensureWriteAlive probes then marks dead on failure.
- currentBehavior: Known-dead reads fail; writes perform an immediate currentTime probe, and a failed probe marks the bridge dead before any native order/cancel call.
- problem: The test protects an important safety gate but currently observes string errors and local mocks only; it does not prove durable dispatch classification when the probe fails or when the probe succeeds and the process dies after send.
- preservedBehavior:
  - Keep immediate probe ordering and no-wire-on-probe-failure.
  - Replace generic message assertions with typed health evidence and crash-cut scenarios.
- openQuestions: —

### MAP-70788F3360

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts:178-191](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts#L178-L191)
- symbol: non-stock routing refusal test
- id: MAP-70788F3360
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts:178-191: a CASH contract without exchange is rejected with a missing-routing error and neither contract lookup nor client.placeOrder is called.
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:369-381: missing secType/exchange/currency/symbol fields are collected and raised as BrokerError before send.
- currentBehavior: A non-STK, non-conId request must carry symbol/localSymbol, secType, exchange, and currency; the current gate fails loudly before any bridge or EClient request.
- problem: String matching on BrokerError is weaker than a typed capability/validation failure and does not ensure routing metadata agrees with instrument identity or jurisdiction.
- preservedBehavior:
  - Keep no-wire-on-missing-routing behavior.
  - Replace regex/string assertions with typed failure decoding while retaining actionable missing-field details.
- openQuestions: —

### MAP-755E02823B

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:561-585](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts#L561-L585)
- symbol: legacy closePosition
- id: MAP-755E02823B
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:561-585: closePosition reads cached positions, matches conId or display symbol, chooses opposite side/quantity, clones the position Contract, sets market DAY order, and delegates to placeOrder.
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts:242-259: FX close test expects cloned IDEALPRO routing and untouched position contract.
- currentBehavior: Close is a read-then-write convenience operation based on current cache; it may match by display symbol, uses requested quantity without a pure exposure proof, and delegates into legacy PlaceOrder.
- problem: The cache can be stale or ambiguous, external fills can change exposure between read and send, and delegation drops a durable exposure precondition/compensation policy.
- preservedBehavior:
  - Keep opposite side, market DAY, and canonical native clone.
  - Keep explicit venue routing; eliminate display-symbol authority and implicit current cache.
  - Risk-reducing close may have policy exception, but must still bind scope/precondition.
- openQuestions: —

### MAP-770FB4496A

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts:262-289](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts#L262-L289)
- symbol: attached TP/SL refusal tests
- raw sourceTargetChapters (provenance, includes duplicates): 5, 9, 9, 17, 18
- id: MAP-770FB4496A
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts:262-289: placeOrder refuses takeProfit/stopLoss before bridge access; an empty tpsl object passes the gate and reaches later bridge access on a bare broker.
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:480-489: placeOrder checks tpsl.takeProfit/stopLoss and returns a failure instead of placing a naked entry.
- currentBehavior: Attached protection is intentionally rejected when either protection leg is present; the empty object is treated as absent and does not claim protection.
- problem: The legacy optional object conflates omitted protection with an empty configuration and only returns a string failure; without an explicit capability decision, a caller could record a protected plan that IBKR never received.
- preservedBehavior:
  - Retain no bridge access for unsupported TP/SL.
  - Retain empty object as explicit no-protection, not as a success claim of bracket semantics.
- openQuestions: —

### MAP-786C1F3DCE

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:269-287](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts#L269-L287)
- symbol: CASH hub expansion
- id: MAP-786C1F3DCE
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:269-287: expandCashHub builds CASH query, calls contractDetailsQuery, maps details to ContractDescription, catches every error, logs warning, and returns [] .
- currentBehavior: Any CASH expansion error—network, timeout, malformed response, or no matches—is converted into an empty successful array after a warning.
- problem: Empty output is indistinguishable from a conclusive no-match result and loses evidence needed to retry or mark capability degraded; warning logs are not durable outcome data.
- preservedBehavior:
  - Keep CASH hub query construction and resulting pair mapping.
  - Remove catch-to-empty fallback; only definitive empty callback yields NoMatch.
- openQuestions: —

### MAP-7BA90560A5

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:182-223](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts#L182-L223)
- symbol: init connection and account readiness
- id: MAP-7BA90560A5
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:182-223: init tears down dead half-open client, skips connected client, waits handshake, requests delayed market data, picks configured/first managed account, starts account updates, waits initial download, marks alive, and logs host/account/clientId.
- currentBehavior: Initialization is imperative and partly idempotent: it can skip a connected client, selects one account, sets market-data type 3, then starts account updates and waits for initial download before marking alive.
- problem: isConnected is not proof of current generation or private account reach; selecting accounts[0] drops linked accounts; markAlive lacks typed capability evidence, and logs contain operational identifiers without structured redaction.
- preservedBehavior:
  - Keep half-open teardown, idempotent reconnect intent, delayed-data fallback, and first private-read barrier.
  - Preserve all linked accounts instead of silently choosing first.
- openQuestions:
  - TWS managedAccounts only supplies account codes in current bridge; sub-account/jurisdiction metadata must come from a verified account observation source.

### MAP-8094360130

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:598-604](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts#L598-L604)
- symbol: fxRate account helper
- id: MAP-8094360130
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:598-604: fxRate reads ExchangeRate:&lt;currency&gt; from Map, returns null on absence or Decimal construction error, and accepts zero/negative/non-finite Decimal values.
- currentBehavior: FX lookup is a private helper returning bare Decimal/null with no rate provenance, observedAt, base currency validation, positivity/finite check, or source quality.
- problem: Zero/negative/non-finite rates can enter calculations; null loses whether the tag was missing, malformed, stale, or unauthorized, so downstream cannot express valuation uncertainty.
- preservedBehavior:
  - Keep ExchangeRate:&lt;currency&gt; lookup and explicit no-garbage behavior.
  - Add finite-positive validation and evidence rather than silently accepting invalid Decimal.
- openQuestions: —

### MAP-86EFC621F8

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:478-507](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts#L478-L507)
- symbol: legacy placeOrder
- id: MAP-86EFC621F8
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:478-507: placeOrder refuses nonempty TP/SL, ensures alive, resolves contract, probes write liveness, gets next order id, registers order request, calls EClient.placeOrder, waits for one callback, and returns PlaceOrderResult or string error.
  - services/uta/src/domain/trading/brokers/ibkr/request-bridge.ts:231-242: requestOrder resolves on openOrder callback or rejects on timeout, using orderId-keyed pending state.
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:872-879: the adapter advertises MKT, LMT, STP, STP LMT, TRAIL, MOC, LOC, and REL as candidate order types for every context.
  - packages/ibkr/src/order.ts:45-75: the native Order has orderType, lmtPrice, auxPrice, tif, goodAfterTime, goodTillDate, percentOffset (documented REL-only at line 72), and trailingPercent (documented TRAILLIMIT-only at line 75).
  - packages/ibkr/src/client/orders.ts:348-385,430-435,457-474: the wire encoder sends orderType, tif, goodAfterTime, goodTillDate, and percentOffset as distinct native fields.
  - packages/ibkr/ref/samples/Python/Testbed/OrderSamples.py:95-107,111-126,437-470: shipped samples map MOC to orderType MOC, MOO to MKT plus tif OPG, LOC to LOC plus lmtPrice, and LOO to LMT plus tif OPG plus lmtPrice.
  - packages/ibkr/ref/samples/Python/Testbed/OrderSamples.py:211-235: shipped Relative/Pegged-to-Primary sample maps REL to auxPrice offset and lmtPrice price cap; the sample documents product and paper-trading limits that are not universal capability proof.
- currentBehavior: The method sends a native mutation immediately after a local liveness check and treats an openOrder callback as success; no journal receipt, dispatch identity, client idempotency key, or observation criterion is required. The separate capability method advertises MOC, LOC, and REL, but the order API accepts an unvalidated native orderType string, so no typed compatibility or venue/account proof exists.
- problem: A process crash after TWS acceptance and before Promise completion becomes indistinguishable from a failed send; success does not mean fill, and caught errors collapse rejection, timeout, and unknown remote outcome. Dropping MOC/LOC/REL or treating OPG as an ordinary session flag would also lose advertised native intent; accepting every advertised value would overclaim support.
- preservedBehavior:
  - Keep explicit TP/SL refusal and canonical route compilation.
  - Preserve every advertised candidate MOC, LOC, and REL through the provider placement leaf's exact schema mappings; preserve OPG as the explicit opening TIF for MOO/LOO rather than dropping it or inferring it from SessionPolicy.
  - Preserve native order request fields including notional/trailing/expiry/relations when typed capability allows; do not narrow to the current minimal test order.
  - Stop treating openOrder as fill confirmation.
- openQuestions:
  - IBKR client-side idempotency guarantees for replaying the same dispatch identity are not established; automatic resend remains forbidden without evidence.
  - Runtime account/venue/product acceptance for the advertised MOC, LOC, REL and OPG combinations is not exposed by this adapter; the compiler must require capability evidence instead of treating IbkrBroker.ts:877 as proof.
  - The native percentOffset field is documented as REL-only, but this source shard does not establish its unit convention or complete venue/product support; percent-relative orders remain unavailable until observed evidence is captured.

### MAP-8C37B26E30

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:881-918](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts#L881-L918)
- symbol: nativeKey identity grammar
- id: MAP-8C37B26E30
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:881-918: getNativeKey uses conId leaf, issuer:&lt;issuerId&gt; bond directory, or bare symbol; resolveNativeKey refuses issuer directories, parses exact numeric keys as conId, and fills STK/SMART/USD for symbols.
- currentBehavior: Native identity grammar intentionally refuses bond directories for trading and treats bare symbols as US stock convenience; numeric conId is canonical for a leaf.
- problem: Strings and numeric parsing are unbranded; symbol defaults omit AccountScope/venue evidence and generic Error cannot distinguish directory, malformed key, or unavailable resolution.
- preservedBehavior:
  - Preserve explicit IBKR hub-versus-leaf refusal and symbol convenience only for discovery.
  - Keep numeric conId canonical identity and round-trip behavior.
- openQuestions: —

### MAP-8EE3F8826D

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:777-798](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts#L777-L798)
- symbol: getOrder and fill enrichment
- id: MAP-8EE3F8826D
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:777-798: getOrder checks client open listing then completed listing, matches string numeric orderId, and enriches avgFillPrice from fill cache/callback.
  - services/uta/src/domain/trading/brokers/ibkr/request-bridge.ts:738-797: openOrder/completedOrder callbacks collect raw SDK objects; orderStatus cache stores filled/avgFillPrice but discards permId/clientId parameters.
- currentBehavior: Order lookup is a two-list fallback with string orderId and optional average fill; missing from both lists returns null without completeness/identity evidence.
- problem: The result cannot prove an unknown dispatch was not accepted, lacks broker/client/perm identities and version/as-of, and ephemeral fill cache can be lost across restart.
- preservedBehavior:
  - Keep open-first then completed lookup for supported read path.
  - Preserve avgFillPrice when validated, but persist provenance and retain all identities.
- openQuestions: —

### MAP-904AF53A43

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:92-102](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts#L92-L102)
- symbol: fromConfig factory
- id: MAP-904AF53A43
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:92-102: fromConfig parses config.brokerConfig through static Zod and constructs a concrete IbkrBroker with parsed host/port/clientId/accountId.
- currentBehavior: The static factory returns a mutable broker object that owns its bridge and EClient; composition callers can invoke methods before a scoped Layer has acquired readiness.
- problem: Construction is treated as service acquisition, so connection lifecycle and resource finalization are invisible to Effect Scope and dispatch callers retain ambient authority.
- preservedBehavior:
  - Keep one composition entry point for configured IBKR.
  - Remove object-construction authority from legacy callers after migration.
- openQuestions: —

### MAP-913C2331F4

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:294-313](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts#L294-L313)
- symbol: contractDetailsQuery
- id: MAP-913C2331F4
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:294-313: contractDetailsQuery clones input, adds SMART/USD only for no-conId/no-issuer STK queries, allocates reqId, collects ContractDetails, seeds canonical cache, and returns raw details.
- currentBehavior: Query input is cloned; STK symbol convenience gets routing defaults, while conId/issuer/non-STK queries retain explicit fields. Every detail seeds a local conId cache.
- problem: The defaulting rule is valuable but generic collector and mutable Contract assumptions are untyped; a caller cannot tell whether a query was convenience-narrowed or identity-authoritative.
- preservedBehavior:
  - Keep clean clone and STK-only SMART/USD default.
  - Prevent defaults from narrowing conId or CASH family queries.
- openQuestions: —

### MAP-96E3C803EA

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/ibkr-contracts.ts:1-12](../../../../services/uta/src/domain/trading/brokers/ibkr/ibkr-contracts.ts#L1-L12)
- symbol: IBKR native contract helper boundary
- id: MAP-96E3C803EA
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/ibkr-contracts.ts:1-12: helper imports native Contract, legacy BrokerError, contract-ext/buildContract, and SecType, and comments treat SDK Contract as the native application type.
- currentBehavior: The helper file is under the legacy domain path and exposes SDK Contract construction and legacy error dependencies to neighboring broker code.
- problem: A domain-facing helper makes mutable SDK Contract appear to be a domain value and gives generic BrokerError construction a route into protocol callers.
- preservedBehavior:
  - Keep native helper behavior at the adapter edge.
  - Retain display extraction without allowing it to become a trade identity.
- openQuestions: —

### MAP-9747579870

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:800-839](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts#L800-L839)
- symbol: getQuote and requestSnapshot
- id: MAP-9747579870
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:800-839: getQuote resolves routed Contract, registers snapshot, calls reqMktData(snapshot=true, regulatorySnapshot=false), and maps optional TickSnapshot fields to Quote strings with zero defaults for last/bid/ask/volume.
  - services/uta/src/domain/trading/brokers/ibkr/request-bridge.ts:680-734: tick callbacks accept live and delayed tick types; tickSnapshotEnd resolves the accumulated object.
- currentBehavior: One-time quote snapshots can receive live or delayed ticks and overnight data; absent numeric fields become string 0, while timeout/entitlement/source quality is not represented.
- problem: Zero cannot distinguish absent from real zero, and the legacy Quote hides live/delayed quality, currency, observedAt, entitlement, and partial fields. Direct request effect is not tied to declared market-data budget.
- preservedBehavior:
  - Keep snapshot mode, delayed fallback policy, overnight-capable channel, and regulatorySnapshot=false default.
  - Remove zero defaults and expose missing/quality explicitly.
- openQuestions: —

### MAP-9C13E5C386

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:231-267](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts#L231-L267)
- symbol: searchContracts
- id: MAP-9C13E5C386
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:231-267: searchContracts strips an optional dot suffix, calls reqMatchingSymbols, expands CASH hubs, passes through other rows including BOND issuer hubs, and returns raw ContractDescription values.
  - services/uta/src/domain/trading/brokers/ibkr/request-bridge.ts:519-523: symbolSamples resolves the pending request with SDK ContractDescription values.
- currentBehavior: Search combines TWS symbolSamples with an inline CASH family expansion: `.USD` is parsed as a quote-currency filter, CASH hubs are expanded, BOND issuer hubs remain addressable, and every row is returned as an SDK object.
- problem: String suffix parsing and raw ContractDescription output do not distinguish tradeable leaves from directories, do not carry jurisdiction/venue context, and let an unresolved row enter downstream code.
- preservedBehavior:
  - Keep `.USD` narrowing and CASH inline expansion policy.
  - Keep BOND issuer hubs visible rather than silently dropping them.
  - Change raw SDK rows to typed leaf/hub metadata.
- openQuestions: —

### MAP-A333A49CA4

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/ibkr-contracts.ts:24-30](../../../../services/uta/src/domain/trading/brokers/ibkr/ibkr-contracts.ts#L24-L30)
- symbol: resolveSymbol
- id: MAP-A333A49CA4
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/ibkr-contracts.ts:24-30: resolveSymbol returns localSymbol first, then symbol, or null when neither exists, for display-oriented contract identity.
- currentBehavior: The helper exposes a nullable display string with localSymbol precedence; callers can accidentally reuse it as an order/position lookup key.
- problem: A presentation fallback is not a canonical instrument identity and cannot disambiguate contracts sharing a symbol; null also lacks an explicit reason at the protocol boundary.
- preservedBehavior:
  - Keep localSymbol-first display selection.
  - Keep null/absent display distinct from canonical identity failure.
- openQuestions: —

### MAP-AF239E68C8

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts:193-204](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts#L193-L204)
- symbol: conId lookup coalescing and clone test
- id: MAP-AF239E68C8
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts:193-204: two concurrent getQuote calls for one conId cause one reqContractDetails call and return independent Contract objects; mutating one does not affect the other.
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:315-320: rememberCanonicalContract stores a Promise of a cloned canonical Contract and does not replace an existing entry.
- currentBehavior: Concurrent reads coalesce by numeric conId; each caller receives a clone, so one mutation cannot contaminate the other result.
- problem: Promise-map coalescing is a good local policy but conId alone ignores account/connection generation, has no bounded eviction, and can retain SDK objects as stale authority.
- preservedBehavior:
  - Keep one wire lookup and independent returned ownership.
  - Do not persist the Promise cache or expose SDK mutation.
- openQuestions: —

### MAP-B2954789E1

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts:389-473](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts#L389-L473)
- symbol: option mark and valuation tests
- id: MAP-B2954789E1
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts:389-473: option tests compute bid/ask midpoint with multiplier-aware value/PnL, preserve broker cache on unavailable snapshots, coalesce same-conId refreshes, and skip snapshots for non-option positions.
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:701-757: refreshOptionPosition caches TTL/in-flight requests; fetchOptionMark validates positive bid/ask, derives position math, and catch-all returns null.
- currentBehavior: Option/FOP marks can be overlaid from one snapshot request; positive bid/ask yields midpoint and derived value/PnL, unavailable data preserves updatePortfolio values, and non-options do not request snapshots.
- problem: The tests currently use null/preserved values as the failure signal without a quality ADT, source timestamp, entitlement classification, or bounded scheduler policy.
- preservedBehavior:
  - Keep multiplier-aware midpoint math and one-flight coalescing.
  - Keep non-option no-request and cached mark preservation, but expose why the mark is stale.
- openQuestions: —

### MAP-B620DB7F24

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:104-131](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts#L104-L131)
- symbol: IbkrBroker instance state and constructor
- id: MAP-B620DB7F24
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:104-131: instance stores mutable bridge/client/config/account fields plus conId/option Promise maps; constructor directly instantiates RequestBridge and EClient.
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:114-123: contract and option caches are local maps with only numeric conId keys and TTL timestamps.
- currentBehavior: Connection resources, account identity, canonical contract cache, option marks, and mutation authority are all fields of one long-lived object. Cache entries survive until object close/reconnect and have no durable version.
- problem: Ambient mutable state cannot be scoped or fenced across reconnects, linked accounts, or scheduler leases; native cache values can outlive the connection that produced them.
- preservedBehavior:
  - Keep request coalescing and short-lived option overlay as optimizations.
  - Change their key/lifetime to explicit scope and generation.
- openQuestions: —

### MAP-B6CBC19102

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:322-348](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts#L322-L348)
- symbol: resolveConIdContract
- id: MAP-B6CBC19102
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:322-348: resolveConIdContract coalesces conId lookup, queries with only conId, requires a matching conId, clones output, and deletes only the observed rejected Promise.
- currentBehavior: Identity resolution is clean and retryable on rejected Promises; it returns a cloned mutable SDK Contract and uses generic BrokerError for no canonical match.
- problem: The raw numeric Promise does not carry AccountScope, connection generation, schema version, or typed distinction between no-match and transport failure.
- preservedBehavior:
  - Preserve clean query, exact conId check, cloning and retry after transient failure.
  - Do not treat any rejected query as NoMatch.
- openQuestions: —

### MAP-BB811E09D4

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/ibkr-types.ts:46-50](../../../../services/uta/src/domain/trading/brokers/ibkr/ibkr-types.ts#L46-L50)
- symbol: AccountDownloadResult
- id: MAP-BB811E09D4
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/ibkr-types.ts:46-50: AccountDownloadResult stores values in Map&lt;string,string&gt; and positions in mutable Position[].
  - services/uta/src/domain/trading/brokers/ibkr/request-bridge.ts:644-678: account value updates preserve composite currency keys and swap pending buffers into the live cache.
- currentBehavior: Account subscription uses an untyped string map plus mutable position array as the in-memory cache; it has no scope, sequence, timestamp, or projection checkpoint.
- problem: String keys hide currency/unit/provenance and mutable arrays are not rebuildable authority; cache reset/reconnect cannot prove continuity or freshness.
- preservedBehavior:
  - Keep composite per-currency/base key handling and atomic pending-to-live swap intent.
  - Replace raw map/array crossing with typed immutable observation.
- openQuestions: —

### MAP-C0E918CDCE

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts:206-216](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts#L206-L216)
- symbol: failed canonical lookup retry test
- id: MAP-C0E918CDCE
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts:206-216: first requestCollector rejection makes getQuote reject; a subsequent call retries and succeeds with IDEALPRO, yielding two wire lookups.
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:340-347: resolveConIdContract deletes the matching rejected Promise before rethrowing.
- currentBehavior: Transient canonical resolution errors do not poison the cache: the failed call exposes the original error and the next call performs a fresh lookup.
- problem: The retry behavior currently relies on generic Promise rejection and cannot distinguish transport timeout, malformed ContractDetails, or a conId that is conclusively absent.
- preservedBehavior:
  - Keep two calls for failure-then-success and preserve canonical IDEALPRO result.
  - Stop asserting raw thrown Error text as the contract.
- openQuestions: —

### MAP-C44B9CCD9F

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:872-879](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts#L872-L879)
- symbol: flat getCapabilities
- id: MAP-C44B9CCD9F
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:872-879: getCapabilities returns fixed supportedSecTypes and supportedOrderTypes arrays for every account/context.
- currentBehavior: Every IBKR account is advertised as supporting STK/OPT/FUT/FOP/CASH/WAR/BOND and eight order types regardless of permissions, venue, session, entitlement, environment, or account scope.
- problem: Static arrays can admit unsupported actions and provide no idempotency, compensation, or data quality evidence; they also conflate read discovery with mutation authority.
- preservedBehavior:
  - Retain known supported security/order types, including advertised MOC/LOC/REL, as candidate capabilities with their source evidence, not universal guarantees.
  - Preserve exact OPG opening semantics and reject the placement leaf input when contract/account/venue evidence is absent.
  - Preserve explicit refusal for unsupported TP/SL/bracket until proven.
- openQuestions:
  - The current adapter does not expose account permissions/SDK capability fingerprint; runtime discovery must supply evidence before marking each action available.

### MAP-C4E977F76D

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts:327-387](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts#L327-L387)
- symbol: mixed-currency account math tests
- id: MAP-C4E977F76D
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts:327-387: mixed-currency tests convert HKD PnL using ExchangeRate:HKD, prefer broker NetLiquidation, use fallback reconstruction only when unavailable/non-finite, and avoid garbage summation when FX is absent.
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:606-660: getAccount parses base currency, position values, ExchangeRate tags, broker NetLiquidation, and legacy decimal-string outputs.
- currentBehavior: Per-position PnL uses explicit TWS currency rates; account NetLiquidation is treated as the broker-level fact whenever finite, while local reconstruction is fallback-only.
- problem: Tests validate numeric outcomes but do not carry source, timestamp, FX validity, or valuation uncertainty; bare strings and Decimal arithmetic can hide stale or zero/negative rate semantics.
- preservedBehavior:
  - Preserve HKD conversion and broker NetLiquidation precedence.
  - Preserve reconstruction only when broker value is absent/non-finite, but expose its source and uncertainty.
- openQuestions: —

### MAP-C5F72FFBD4

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:1-47](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts#L1-L47)
- symbol: legacy IbkrBroker module contract and imports
- id: MAP-C5F72FFBD4
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:1-47: module documentation describes a Promise-based IBroker bridge and imports EClient, native Contract/Order, and legacy AccountInfo/Position/Quote/MarketClock/PlaceOrderResult/BrokerError types.
  - historical architecture evidence pinned at Git a5f23756531cc552b7e12b6d655ae1ffbcd28b64 (former lines 230-256): broker-spi contains typed ports and brokers/&lt;broker&gt; owns SDK interpreters while the kernel cannot depend on concrete adapters.
- currentBehavior: The class is the complete legacy adapter surface: it couples connection, request bridging, contract resolution, order writes, accounting reads, market clock, capabilities, and identity under one IBroker Promise object.
- problem: This coupling lets callers bypass write-ahead jobs and exposes native SDK values across the protocol seam; a Promise return is not a typed acknowledgement/observation and cannot represent Unknown after process loss.
- preservedBehavior:
  - Retain all IBKR-specific routing, callback and accounting semantics in the adapter.
  - Delete generic IBroker writes only after typed scheduler path owns all callers; do not leave a silent fallback.
- openQuestions: —

### MAP-D20AD6C750

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/ibkr-contracts.ts:14-21](../../../../services/uta/src/domain/trading/brokers/ibkr/ibkr-contracts.ts#L14-L21)
- symbol: makeContract
- id: MAP-D20AD6C750
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/ibkr-contracts.ts:14-21: makeContract calls buildContract and defaults symbol requests to STK, SMART, and USD while accepting an explicit SecType, exchange, and currency.
- currentBehavior: makeContract is a convenience constructor whose defaults also apply when a caller passes a non-STK SecType without explicitly supplying matching venue/currency.
- problem: A generic constructor can turn a symbol into an accidental executable identity and can attach STK/SMART/USD assumptions to a CASH, bond, option, or future request.
- preservedBehavior:
  - Retain explicit SecType/route construction for adapter queries.
  - Preserve STK/SMART/USD defaults only for the tagged convenience read path.
- openQuestions: —

### MAP-DD39DF4F2F

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/ibkr-types.ts:52-59](../../../../services/uta/src/domain/trading/brokers/ibkr/ibkr-types.ts#L52-L59)
- symbol: CollectedOpenOrder
- id: MAP-DD39DF4F2F
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/ibkr-types.ts:52-59: CollectedOpenOrder carries raw Contract, Order, OrderState and optional avgFillPrice from orderStatus.
  - packages/ibkr/src/wrapper.ts:132-140: EWrapper.orderStatus declares permId and clientId callback parameters; mapping must retain both.
- currentBehavior: Open/completed callbacks pass mutable SDK objects and optional average fill into legacy OpenOrder; no account/client/perm identity, observation time, status version, or outcome variant is modeled.
- problem: Raw callback rows cannot support durable correlation, listing completeness, fill corrections, still-working/unknown outcomes, or compensation decisions after restart.
- preservedBehavior:
  - Retain average fill information when callback supplies it.
  - Separate it from order status and preserve raw evidence in a redacted native record.
- openQuestions: —

### MAP-E39D85CC2A

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/ibkr-contracts.ts:47-77](../../../../services/uta/src/domain/trading/brokers/ibkr/ibkr-contracts.ts#L47-L77)
- symbol: classifyCode
- id: MAP-E39D85CC2A
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/ibkr-contracts.ts:47-77: classifyCode uses numeric branches plus message regexes for connectivity, market-closed, exchange failures, and an UNKNOWN fallback.
  - services/uta/src/domain/trading/brokers/ibkr/request-bridge.ts:488-517: 2100-2199 errors are ignored; reqId=-1 502/504/1100 marks dead and 1101/1102 emits a restored callback without rejecting a request.
- currentBehavior: The classifier maps known numeric branches to coarse BrokerError codes, uses regex against free-form messages for market/session status, swallows the informational 2100-2199 range in the bridge, and treats every unrecognized code as UNKNOWN.
- problem: Regex classification and a broad UNKNOWN bucket erase whether an error is an expected rejection, unavailable connection, capability/entitlement issue, protocol violation, or defect; swallowed informational events also lose health evidence.
- preservedBehavior:
  - Keep 2100-2199 as non-failure informational input without allowing it to complete a request.
  - Keep 1100 as dead and 1101/1102 as restored hints, never as private-read or writable readiness.
- openQuestions:
  - The source does not identify all 10xxx entitlement/error codes; unknown native code must remain explicit until runtime evidence adds a table entry.

### MAP-E721ADE1B2

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:701-722](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts#L701-L722)
- symbol: refreshOptionPosition
- id: MAP-E721ADE1B2
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:701-722: refreshOptionPosition uses conId success/failure TTL and in-flight Promise maps, overlays cached result, and removes the matching in-flight entry in finally.
- currentBehavior: A numeric conId keys both positive and negative TTL caches; concurrent calls coalesce, and null overlay preserves the input Position.
- problem: ConId alone is insufficient across AccountScope/connection generation; TTL has no observation provenance and null does not distinguish stale, unavailable, or invalid data.
- preservedBehavior:
  - Keep successful/failed TTL and one-flight deduplication.
  - Key and classify by scope/generation and preserve reasons.
- openQuestions: —

### MAP-E8631F6112

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts:89-118](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts#L89-L118)
- symbol: canonical conId replay test
- id: MAP-E8631F6112
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.spec.ts:89-118: each recorded canonical fixture is passed through placeOrder and the sent Contract is compared on conId, symbol/localSymbol, secType, exchange, primaryExchange, currency, tradingClass, and multiplier.
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:480-507: placeOrder resolves a routable contract and passes it to client.placeOrder after the liveness probe.
- currentBehavior: The corpus replay treats every canonical fixture as a place-order routing oracle: caller identity may be polluted, but the wire contract must match the recorded Contract across routing and display fields.
- problem: A direct equality test on mutable SDK objects does not state which fields form InstrumentId identity, which are routing attributes, and which are presentation metadata; it can miss schema/version drift or accidentally accept a contract from the wrong account/connection.
- preservedBehavior:
  - Preserve every currently asserted canonical field, including multiplier and tradingClass.
  - Intentionally stop asserting raw object shape outside the adapter; those assertions move to schema conformance.
- openQuestions: —

### MAP-E874ED272F

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/index.ts:1-2](../../../../services/uta/src/domain/trading/brokers/ibkr/index.ts#L1-L2)
- symbol: IBKR barrel exports
- id: MAP-E874ED272F
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/index.ts:1-2: barrel exports only concrete IbkrBroker and IbkrBrokerConfig; no Layer, typed interpreter, schemas, capabilities, or remote-outcome service is exposed.
- currentBehavior: Consumers import a concrete legacy class/config as the entire IBKR public surface, so registry and callers can bypass scoped runtime, typed plans, and durable outcomes.
- problem: The barrel makes compatibility object authority persistent and provides no discoverable target service package; deleting it prematurely would strand callers, while retaining it after cutover leaves dual write authorities.
- preservedBehavior:
  - Keep package discoverability and read/query capabilities.
  - Remove generic IbkrBroker/PlaceOrderResult authority after Slice 6.
  - Do not create fake broker when credentials/readiness absent.
- openQuestions: —

### MAP-E99E61B1B5

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:133-167](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts#L133-L167)
- symbol: dead-connection and write-liveness gates
- id: MAP-E99E61B1B5
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:133-167: _ensureAlive refuses a known-dead bridge; _ensureWriteAlive probes currentTime immediately before writes, marks dead on failure, and forwards a legacy callback listener.
- currentBehavior: Known-dead cache reads/writes throw BrokerError; write probes share a coalesced bridge currentTime request, and probe failures claim that no order was transmitted.
- problem: The boolean connectionDead and callback listener are not sufficient evidence for generation, account scope, lease ownership, or post-send unknown outcome; all failures become messages.
- preservedBehavior:
  - Keep stale-read containment and immediate pre-write probe.
  - Replace string BrokerError with typed health/failure variants.
- openQuestions: —

### MAP-F35B2FF883

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:315-320](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts#L315-L320)
- symbol: canonical contract cache seeding
- id: MAP-F35B2FF883
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:315-320: rememberCanonicalContract stores the first conId contract as a resolved Promise and never replaces an existing/in-flight entry.
- currentBehavior: Any ContractDetails response can seed an unbounded numeric conId Promise cache; the first value wins even across account/connection changes.
- problem: Cache lifetime and authority are ambiguous: stale routing can be reused after reconnect, no eviction is defined, and mutable SDK values are retained despite clone-on-return.
- preservedBehavior:
  - Keep no-replace in-flight coalescing.
  - Add explicit scoping, validation, bounded lifetime, and invalidation.
- openQuestions: —

### MAP-F4D1FDCBB6

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:73-90](../../../../services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts#L73-L90)
- symbol: static config schema and fields
- id: MAP-F4D1FDCBB6
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:73-90: static Zod schema defaults host/port/clientId, accepts optional accountId, and includes paper; configFields exposes the same primitive controls and says TWS/Gateway handles authentication.
  - services/uta/src/domain/trading/brokers/ibkr/ibkr-types.ts:13-24: IbkrBrokerConfig also makes id/label/host/port/clientId/accountId optional.
- currentBehavior: External config is parsed for primitive endpoint values; paper is accepted/displayed but is not passed to IbkrBrokerConfig or used to verify the TWS environment. Account selection is one optional string.
- problem: Port, paper-mode labels, and optional accountId cannot prove live/paper environment, linked-account scope, valid client-id range, or private-read authority. Config comments are not runtime evidence.
- preservedBehavior:
  - Keep existing TWS/Gateway login model and default paper endpoint as an explicit default only.
  - Stop presenting paper as proof when no TWS account environment evidence exists.
- openQuestions:
  - The current source does not expose a reliable TWS paper/live environment callback; implementation must choose an explicit observed endpoint/account evidence source rather than infer from port.

### MAP-FCA7E87D0D

- mapping source: [services/uta/src/domain/trading/brokers/ibkr/ibkr-types.ts:1-10](../../../../services/uta/src/domain/trading/brokers/ibkr/ibkr-types.ts#L1-L10)
- symbol: IBKR internal type boundary
- id: MAP-FCA7E87D0D
- sourceEvidence:
  - services/uta/src/domain/trading/brokers/ibkr/ibkr-types.ts:1-10: adapter-local types import raw Contract/Order/OrderState SDK classes and the protocol Position type from the legacy broker surface.
- currentBehavior: The file mixes SDK object aliases with a protocol Position dependency, so mutable native records and domain projections share a module boundary.
- problem: The file is under the legacy domain path and imports a protocol Position alongside SDK values, so adapter-native representation and domain/projection representation are not separated by a typed codec.
- preservedBehavior:
  - Keep SDK callback decoding in the IBKR adapter.
  - Retain native account/order identity fields instead of dropping them during mapping.
- openQuestions: —
