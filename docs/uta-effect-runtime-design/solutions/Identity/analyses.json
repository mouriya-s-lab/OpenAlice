{
  "group": "Identity",
  "designBasis": "capability-composition-v1",
  "entries": [
    {
      "id": "MAP-60408331F8",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/OrderHelper.ts:1-44: the module imports IBKR Order and UNSET_DECIMAL and maintains a seven-name SENTINEL_DECIMAL_FIELDS list (totalQuantity through filledQuantity)",
        "packages/ibkr/src/order.ts:47-75,195-209: Order has seven Decimal fields initialized to UNSET_DECIMAL",
        "packages/ibkr/src/order.ts:71-72,95-110,120-135,180-185,218-225: Order also has UNSET_INTEGER and UNSET_DOUBLE fields outside that seven-name list"
      ],
      "currentBehavior": "The list is the only declared inventory of Decimal-valued Order fields stripped before wire output; toWire leaves the SDK's UNSET_DOUBLE and UNSET_INTEGER fields enumerable. Its authority is placed in domain/trading even though all three sentinel classes are IBKR binary-protocol conventions.",
      "problem": "A closed Decimal-only list in a shared domain module can silently miss a newly added SDK field, and the generic spread path can serialize UNSET_DOUBLE/UNSET_INTEGER as prices, quantities, offsets, or durations. The domain import makes broker-native representation part of every consumer's type graph.",
      "targetModel": "Keep an IBKR-native `NativeOrderCodec` with a versioned allowlist covering every declared `UNSET_DECIMAL`, `UNSET_DOUBLE`, and `UNSET_INTEGER` field, plus an explicit unknown-field/schema-drift policy. The source full `Order`/IBroker-shaped surface is historical evidence, not a shared UTA contract. An effect-capable provider declares an exact order input schema, and local HOFs compose only the sizing, price, timing, protection, relation, and provider-extension fields that it actually supports; there is no global rule about which sizing/price combinations are legal and no empty handler requirement. The resulting intent is scoped by AccountScope and InstrumentId and never exposes a sentinel or SDK Order.",
      "pureTransitions": [
        "NativeOrderRecord -> Result<ProviderOrderSnapshot, NativeProtocolViolation[]>: recognize the provider-declared sentinel classes and schema version, retaining unknown raw fields as protocol evidence",
        "Declared order schema + ActionContext -> Result<PreparedEffectPayload, ValidationIssue[]>: reject unsupported combinations before the transaction wrapper rather than dropping provider fields",
        "ProviderOrderIntent -> Result<IBKRNativeOrderPayload, NativeEncodingFailure>: encode absent optional values as IBKR sentinels only inside the IBKR adapter"
      ],
      "effectBoundary": "No IO belongs in sentinel conversion. Each provider adapter owns its SDK/native schema and codec; application/protocol codecs consume the resulting declared semantic order or a redacted wire view. The transaction wrapper is applied only to this effect-capable leaf.",
      "durability": "Only controlled effect execution persists the versioned intent, schema fingerprint, explicit absence, and adapter codec identity; native sentinel numbers never enter journal, HTTP, MCP, Git, or data-query state.",
      "errors": [
        "NativeProtocolViolation(field, observedValue) -> reject the provider response and record typed observation evidence",
        "NativeSchemaVersionUnsupported(version/field) -> require adapter upgrade or reprepare",
        "OrderEncodingUnsupported(field, codecVersion) -> require reprepare rather than emitting an ambiguous payload"
      ],
      "preservedBehavior": [
        "Continue removing all seven known IBKR sentinel Decimal values before any UI, MCP, Git, or JSON boundary",
        "Also remove every declared UNSET_DOUBLE/UNSET_INTEGER field at the native boundary, while keeping field-specific defaults inside broker adapters"
      ],
      "replacementSteps": [
        "Create one declaration-backed native order codec per provider and register its capability leaf at the composition boundary",
        "Change TradingGit projection and protocol encoders to accept typed domain/projection values, then delete the shared domain sentinel authority"
      ],
      "verificationCases": [
        "A market order containing UNSET_DECIMAL, UNSET_DOUBLE, and UNSET_INTEGER fields produces no numeric sentinel literals in its projection",
        "A native schema fixture with a field absent from the codec is rejected as schema drift rather than serialized",
        "A real limit price and explicit zero-valued domain field survive encode/decode unchanged"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The seven Decimal fields are retained as evidence of the IBKR sentinel boundary, but the source full-Order/IBroker-shaped surface is not a UTA contract; this entry now places exact sentinel handling in one provider codec and composes order inputs from only the capabilities that provider declares.",
      "contractAnchors": [
        "K02",
        "K03",
        "K07",
        "K08"
      ]
    },
    {
      "id": "MAP-AE40B3965B",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/OrderHelper.ts:46-70: OrderView exposes nullable action/order fields and unsetToUndef converts only Decimal sentinels",
        "services/uta/src/domain/trading/OrderHelper.ts:74-90: read uses `|| undefined` for tif, outsideRth, parentId, and ocaGroup",
        "packages/ibkr/src/order.ts:53-65,67-75: runtime Order defaults tif/ocaGroup/goodTillDate to empty and outsideRth to false/parentId to 0 without presence bits"
      ],
      "currentBehavior": "read projects a selected IBKR Order subset to OrderView. Decimal sentinels become undefined, but false outsideRth and numeric parentId 0 are erased by truthiness; a runtime Order instance alone also cannot distinguish an omitted field from its default false/0/empty value. The view has no instrument, account scope, order identity, version, or provenance.",
      "problem": "The partial nullable view permits combinations that do not describe a valid action and loses meaningful explicit values. Claiming that a class instance proves explicit false/0 would invent presence evidence; downstream code must decode presence-aware wire data or mark default-origin ambiguous.",
      "targetModel": "Replace OrderView with an `OrderSnapshot`/`OrderIntent` split whose fields are derived from the selected provider schema. A presence-aware native decoder emits `Optional<T> = Absent | Present<T>`; preserve explicit `outsideRth: Present(false)` and `parentId: Present(0)`, while a class-only value is `AmbiguousDefault(field)` unless adapter metadata supplies presence. Bind an executable intent to AccountScope and InstrumentId, and represent an existing order with `ScopedOrderRef { scope, instrumentId, brokerOrderId, orderVersion }`; provider-specific fields remain exact extensions.",
      "pureTransitions": [
        "PresenceAwareNativeOrder -> Result<OrderSnapshot, NativeProtocolViolation[]>: decode fields without truthiness coercion",
        "Declared OrderIntent schema + BeforeImage -> Result<ModifyEffect, ValidationIssue[]>: distinguish omitted patch fields from explicit false/zero values",
        "SDK Order without wire-presence metadata -> Result<AmbiguousDefault(field), NativeProtocolViolation[]> where the default could mean omitted"
      ],
      "effectBoundary": "The adapter reads raw presence-aware wire data and SDK Order metadata, then emits the typed snapshot. No service, clock, or SDK type is allowed in the pure domain constructor; application supplies observation time and account scope.",
      "durability": "Order snapshots are projection/journal records with explicit schema version and presence tags; a missing field is encoded as intentional absence, not an SDK default. Modify before-images retain the version used for optimistic checks.",
      "errors": [
        "NativeProtocolViolation(field, reason) -> reject malformed order observation",
        "AmbiguousDefault(field) -> require presence-bearing native response before treating the value as explicit",
        "InvalidRelation(action, providerField) -> return ValidationFailed to the selected effect caller",
        "MissingOrderScope -> refuse to treat an unscoped native order as executable"
      ],
      "preservedBehavior": [
        "Continue hiding all IBKR sentinels from callers",
        "Preserve explicit false/zero when wire presence is known, and intentionally refuse to infer presence from the current SDK class defaults"
      ],
      "replacementSteps": [
        "Introduce presence-aware typed native response decoders in each broker adapter and migrate all OrderView consumers",
        "Make protocol/order observation and modify schemas explicit, then remove OrderView and unsetToUndef from domain/trading"
      ],
      "verificationCases": [
        "A wire response marking outsideRth=false remains `Present(false)` while an omitted field is `Absent`",
        "A wire response marking parentId=0 remains `Present(0)`; a class-only parentId=0 without presence metadata is reported ambiguous",
        "A modify request omitting lmtPrice does not clear the existing limit, while an explicit typed clear operation is handled by its own ADT"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "False and zero presence are retained as observable wire facts, while snapshot and intent are separate schema-bound semantic units whose provider fields remain exact extensions instead of one universal SDK order shape.",
      "contractAnchors": [
        "K03",
        "K04",
        "K07",
        "K08"
      ]
    },
    {
      "id": "MAP-7476DDDBBC",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/OrderHelper.ts:72-103: toWire shallow-copies Order or Partial<Order>, iterates the seven-name Decimal sentinel list, and returns Record<string, unknown>",
        "packages/ibkr/src/order.ts:47-75,89-135,180-185,195-225: Order contains Decimal, UNSET_DOUBLE, and UNSET_INTEGER fields beyond the helper's allowlist"
      ],
      "currentBehavior": "toWire accepts a complete or partial SDK Order, spreads every enumerable property, and deletes only recognized sentinel Decimal properties. It is used by TradingGit to project place/observe orders and modify changes before external observation, so unrecognized numeric sentinels and arbitrary enumerable fields can cross the boundary.",
      "problem": "Record<string, unknown> and object spread preserve arbitrary SDK fields, functions, secrets, or unknown future properties; a cast back to Order only hides the loss of schema proof. A partial patch has no typed relationship to its target order and can leak UNSET_DOUBLE/UNSET_INTEGER.",
      "targetModel": "Use a named, provider-specific `OrderProjectionCodec` and `ModifyOrderPatchCodec` derived from the declared input/output schema, with an allowlisted field set and explicit handling for all three IBKR sentinel classes where IBKR is selected. Encode snapshot, create, and modify views separately; a modify view carries explicit patch presence, ScopedOrderRef, and expected OrderVersion, never a generic record. Provider extensions are preserved or rejected by that provider leaf; the projection is never asked to represent fields or operations that the leaf did not declare.",
      "pureTransitions": [
        "Typed OrderSnapshot/patch -> exact wire projection: enumerate declared fields, omit Absent and every declared native sentinel, and return a serializable record",
        "Unknown native object or sentinel field -> Result<Projection, ProtocolViolation>: reject fields outside the declared safe view rather than spreading them"
      ],
      "effectBoundary": "Encoding is pure. TradingGit, HTTP, MCP, and UI receive only the projection; native adapter codecs are the only consumers allowed to turn domain data back into SDK objects.",
      "durability": "The journal stores canonical encoded operation payloads and schema versions; projection of Git status is rebuildable from those fields. Unvalidated maps, raw SDK objects, and sentinel literals are never authorities.",
      "errors": [
        "ProjectionSchemaViolation(unknownField) -> fail the boundary and redact the native payload",
        "NativeSentinelFieldUnmapped(field,class) -> fail schema compatibility",
        "ModifyVersionRequired -> reject an unversioned patch as a precondition failure",
        "SerializationFailure(field) -> do not publish the selected operation"
      ],
      "preservedBehavior": [
        "TradingGit status/log/show/export must remain free of Decimal, double, and integer sentinel literals",
        "Partial modify requests continue to omit untouched fields rather than overwriting them"
      ],
      "replacementSteps": [
        "Add exact protocol/projection encoders and update TradingGit.projectOperation/projectCommit to call them",
        "Migrate adapter native serialization to their own codecs and delete the generic helper and casts"
      ],
      "verificationCases": [
        "A staged market order's serialized JSON contains no UNSET_DECIMAL, UNSET_DOUBLE, or UNSET_INTEGER literal",
        "An operation carrying an undeclared SDK property is rejected by the explicit schema, never spread blindly",
        "A partial modify carrying only limit price cannot serialize broker credentials, numeric sentinels, or unrelated Order fields"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The projection safety finding remains, but projection codecs are provider/schema-specific and HOF-composed; no full order variant set or generic record is required across providers.",
      "contractAnchors": [
        "K02",
        "K03",
        "K07",
        "K08"
      ]
    },
    {
      "id": "MAP-ACEFA5D60A",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/contract-builder.spec.ts:1-3: the entire suite imports the shared builders and Decimal while no broker-neutral fixtures are present"
      ],
      "currentBehavior": "The test module's imports make every test depend on the shared IBKR-shaped builder boundary; it does not separate native contract codec behavior from pure position arithmetic.",
      "problem": "A test suite organized around one SDK-shaped helper encourages the shared domain module to remain the authority and makes it impossible to prove that the kernel can consume SDK-free observations.",
      "targetModel": "Split fixtures into `IbkrNativeContractFixture` tests for decode/encode and `PositionObservation`/`derivePositionMath` fixtures for pure account-data calculations. Position fixtures carry AccountScope because they model account facts; public Instrument fixtures carry explicit Public scope and never invent an account. Each fixture carries InstrumentId, currency, and valuation provenance where relevant.",
      "pureTransitions": [
        "Decoded native fixture -> Result<InstrumentDescriptor, ValidationIssue[]>",
        "PositionObservation fixture -> deterministic Position/Exposure result or typed ObservationFailure"
      ],
      "effectBoundary": "Tests should instantiate no real broker connection; adapter codec cases may use SDK objects only inside the adapter test package. Domain math tests remain SDK-free.",
      "durability": "The test contract must assert schema-versioned serializable snapshots where persistence is involved, not object identity or SDK class identity.",
      "errors": [
        "FixtureDecodeFailure -> assert the precise ValidationIssue variant",
        "UnexpectedNativeValue -> assert adapter ProtocolViolation rather than a thrown message"
      ],
      "preservedBehavior": [
        "Keep the existing distinction between contract validation and multiplier-aware position math",
        "Retain regression coverage for OPT/FUT requirements and Decimal arithmetic"
      ],
      "replacementSteps": [
        "Move SDK-specific imports and fixture construction under brokers/ibkr tests",
        "Rewrite shared builder tests around typed domain observations and remove the shared builder import from this suite"
      ],
      "verificationCases": [
        "A malformed IBKR option fixture fails with field-tagged validation while a valid option decodes to a broker-neutral descriptor",
        "A pure position fixture can run with no Contract class and still proves multiplier-aware market value/PnL",
        "Changing the SDK class shape cannot change domain math test inputs"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The test boundary moves from one SDK fixture to a declaration-derived native codec plus SDK-free position/data units, preserving the empirical arithmetic obligations without making IBKR types a shared contract.",
      "contractAnchors": [
        "K03",
        "K05",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-4FA5723DDD",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/contract-builder.spec.ts:5-58: tests assert localSymbol defaults to symbol, OPT/FUT structural fields, unknown BANANA rejection, and minimal CRYPTO/CRYPTO_PERP acceptance"
      ],
      "currentBehavior": "Tests observe IBKR Contract fields and generic thrown Error text. buildContract defaults localSymbol to symbol, leaves multiplier empty for STK, requires derivative fields, rejects an unknown SecType, and accepts crypto variants with universal fields.",
      "problem": "The expected behavior is valuable but coupled to an SDK object and regex matching of error messages. Native SecType taxonomy is being asserted as if it were the domain instrument model, and no account/venue/jurisdiction identity is tested.",
      "targetModel": "Use adapter codec fixtures for the native allowlist and a serializable `InstrumentDescriptor` semantic unit with required identity/context fields plus `scope: Public | AccountScope` when the provider result is account-bound, and precise provider-declared structural extensions. Focused constructors may refine option, future, cash, crypto, or equity facts where their schema proves those relationships; the provider descriptor set remains extensible. `InstrumentId`, `VenueId`, listing jurisdiction, and capability evidence remain separate fields.",
      "pureTransitions": [
        "NativeContractSchema -> Result<InstrumentDescriptor, ValidationIssue[]>: preserve local/native symbol separately from canonical identity",
        "InstrumentDescriptor -> Result<NativeContractRequest, NativeEncodingFailure>: apply IBKR defaults only in the IBKR compiler"
      ],
      "effectBoundary": "No network is required for these cases. The adapter schema owns Contract construction; application receives a domain descriptor and capability query result.",
      "durability": "Persist canonical instrument identity and variant fields, not mutable SDK defaults. Native codec version is recorded with plan/catalog metadata.",
      "errors": [
        "UnknownNativeSecType(value) -> ProtocolViolation at IBKR boundary",
        "MissingDerivativeField(kind, field) -> ValidationFailed with field payload",
        "InstrumentContextMissing -> QueryFailure/CapabilityUnavailable when venue or jurisdiction is not resolved"
      ],
      "preservedBehavior": [
        "Keep localSymbol=symbol as an IBKR native default when no native local symbol exists",
        "Keep strict derivative field requirements and CRYPTO_PERP support, but intentionally move their enforcement to the adapter/domain boundary"
      ],
      "replacementSteps": [
        "Replace `toThrow` regexes with exhaustive issue assertions",
        "Add canonical identity/context and provider-extension fixtures, then remove direct Contract assertions from the shared builder suite"
      ],
      "verificationCases": [
        "An OPT missing strike produces MissingDerivativeField(strike), not a message-order-dependent assertion",
        "A valid STK with no multiplier is accepted by the IBKR native declaration, and only its explicit non-derivative unit-one policy yields `MultiplierOne`; absent multiplier evidence for another provider remains unavailable",
        "A crypto symbol EURUSD under equity context is not suffix-normalized or reclassified"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The native default and derivative observations stay required, but the replacement uses semantic Instrument descriptors with provider-declared structural extensions rather than a closed domain union.",
      "contractAnchors": [
        "K03",
        "K04",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-C3F4ACD09F",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/contract-builder.spec.ts:60-145: tests cover derived/pass-through values, multiplier inheritance/override, avgCostSource and risk",
        "services/uta/src/domain/trading/brokers/contract-builder.spec.ts:147-205: raw OPT/FOP contracts without multipliers throw while STK multiplier=1 is allowed"
      ],
      "currentBehavior": "buildPosition derives both marketValue and unrealizedPnL when both upstream values are absent, trusts a paired upstream set verbatim, chooses explicit multiplier over Contract multiplier over '1', preserves avgCostSource/risk, and throws for OPT/FOP multiplier 1 or empty.",
      "problem": "The suite treats arbitrary upstream strings as trustworthy and verifies a generic throw. It does not distinguish broker-reported valuation from local derivation, account/instrument units, or the case where only one of the two upstream values is present (the implementation mixes sources).",
      "targetModel": "Test `PositionObservation` with `ValuationEvidence = BrokerReported{marketValue, unrealizedPnL, source, asOf} | Incomplete{kind: MarketValueOnly | PnlOnly | NoComponents, knownComponent?} | Derived{inputs, formulaVersion}`; use refined `Quantity`, `Money`, `Multiplier`, `InstrumentId`, and `AccountScope`. The observation is retained for query/accounting even when incomplete; execution preparation requiring complete valuation rejects it.",
      "pureTransitions": [
        "Native row -> Result<PositionObservation, ObservationFailure[]>: parse scope/instrument/quantity and preserve any known broker valuation component",
        "PositionObservation + fresh complete unit-checked inputs -> Result<DerivedValuation, ValuationFailure>: derive a coherent pair only for `NoComponents`",
        "PositionObservation with `MarketValueOnly` or `PnlOnly` -> Incomplete observation; Prepare requiring complete valuation -> `PreconditionFailed(ValuationIncomplete)`",
        "Raw option/future observation with multiplier 1 -> Err(InvalidContractMultiplier)"
      ],
      "effectBoundary": "Adapters parse native Decimal/string fields and label which broker components were present; pure tests pass decoded values and valuation evidence. Persistence stores the evidence and observation time; no test needs a live broker.",
      "durability": "Position projection records broker-reported, incomplete, or derived provenance plus formula/observation version. Replay cannot fabricate missing components or recompute a broker value under a new multiplier.",
      "errors": [
        "InvalidContractMultiplier(instrument, observed) -> reject position observation and prevent exposure projection",
        "ValuationIncomplete(kind) -> retain the observation for query/accounting but reject execution preparation that requires a complete pair",
        "DerivedInputsIncomplete -> do not derive a partial pair",
        "DecimalParseFailure(field, raw) -> typed protocol failure"
      ],
      "preservedBehavior": [
        "Preserve Decimal arithmetic, explicit multiplier override, avgCostSource, risk metadata, and STK multiplier 1",
        "Preserve partial broker position scope/instrument/quantity and known component instead of discarding it; remove only the old mixed-provenance completion"
      ],
      "replacementSteps": [
        "Move raw Contract decoding into adapter tests and expose only PositionObservation to domain math",
        "Update each broker's position mapper and Accounting projection to persist valuation evidence, then make Prepare enforce completeness and delete shared buildPosition tests"
      ],
      "verificationCases": [
        "A 5-lot option with multiplier 100 yields the existing 35,000 market value and 6,000 unrealized PnL when complete inputs are available",
        "An OPT callback with no multiplier returns InvalidContractMultiplier before persistence",
        "Only marketValue supplied yields retained `Incomplete(MarketValueOnly)` with no fabricated PnL, and Prepare returns ValuationIncomplete"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "Multiplier arithmetic, valuation provenance, and incomplete observations remain valuable data facts; they are explicitly kept outside transaction approval and only consulted by effect admission when a chosen operation requires them.",
      "contractAnchors": [
        "K04",
        "K05",
        "K08"
      ]
    },
    {
      "id": "MAP-D0EA3C0CF2",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/contract-builder.ts:1-23: module documentation promises every broker funnels through buildContract/buildPosition and imports Contract, Decimal, Position, assertContract, and position math"
      ],
      "currentBehavior": "A shared module presents IBKR Contract as the cross-broker output funnel. All adapters are expected to construct the same mutable SDK object before positions enter the legacy Position type.",
      "problem": "The funnel makes the most specific broker SDK the de facto domain contract and forces heterogeneous venues to fake IBKR fields. It also combines native validation, domain math, and output shaping, so a kernel consumer cannot prove it is SDK-free.",
      "targetModel": "Keep a pure `derivePositionMath` over domain value objects and introduce broker-specific `NativeContractCodec`/`NativePositionDecoder`. Selected data/effect capability ports carry `InstrumentDescriptor`, `PositionObservation`, and `ExposureSnapshot`; adapter-specific native requests never cross into domain or protocol. Canonical identity is constructed only from an evidence-backed venue/listing/native-key tuple and a durable uniqueness-checked alias mapping; a nominal brand alone is not proof.",
      "pureTransitions": [
        "Typed PositionObservation -> Result<PositionValuation, ObservationFailure>",
        "InstrumentReference + native metadata + Public|AccountScope scope -> Result<InstrumentDescriptor, ValidationIssue[]>",
        "Resolved native identity tuple + catalog state -> Result<InstrumentId, IdentityMappingFailure>: reject unresolved or conflicting mappings"
      ],
      "effectBoundary": "Only adapter modules access SDK classes and remote callbacks. The effect runtime depends on named provider capability ports composed at startup, not a broker-name switch or universal broker interface. Catalog/identity lookup is an application effect supplying facts to the pure constructor; position readers remain data effects.",
      "durability": "Journal/projection schemas store domain descriptors and observations with versions; native Contract instances are ephemeral codec values and cannot be persisted. Alias mappings use a durable uniqueness key scoped by venue/listing, adding account scope only for account-bound identities, and record conflicts instead of last-write-wins.",
      "errors": [
        "AdapterDecodeFailure -> typed provider observation failure",
        "ProviderSemanticCapabilityUnavailable -> capability-unavailable result at the selected leaf, without fabricating a shared kind",
        "IdentityUnresolvedOrAmbiguous -> QueryFailure/PreconditionFailed",
        "PositionValuationFailure -> block projection/update rather than returning a malformed Position"
      ],
      "preservedBehavior": [
        "Retain a single multiplier-aware arithmetic policy and immediate rejection of structurally unsafe derivatives",
        "Preserve broker-specific risk/provenance data through typed fields rather than dropping it"
      ],
      "replacementSteps": [
        "Create adapter-local codecs for IBKR and each current broker",
        "Migrate buildPosition callers to decoder output and move math into domain, then delete the shared Contract funnel",
        "Add catalog/native-alias uniqueness checks before any descriptor can feed execution"
      ],
      "verificationCases": [
        "An Alpaca position and a CCXT position both produce the same SDK-free valuation shape",
        "A runtime adapter scenario decodes a native row to a descriptor and emits a serializable event; passing an unresolved/ambiguous native key yields a typed failure before state commit",
        "A provider-specific instrument extension remains queryable even when no common trading capability is declared"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source module’s claim that every broker funnels through IBKR Contract remains evidence of the defect, while the target replaces it with provider-local codecs and composition-root registration without a global broker switch.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K07"
      ]
    },
    {
      "id": "MAP-E748A339C5",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/contract-builder.ts:27-47: BuildContractInput mixes raw symbol/exchange/currency with optional derivative fields and IBKR conId/tradingClass/description"
      ],
      "currentBehavior": "One record accepts all instrument types and optional fields. SecType is the only discriminant; option/future requirements are enforced later by assertContract rather than represented in the input type.",
      "problem": "Optional-field soup permits invalid combinations (for example a future without expiry or an option with a nonsensical right) and exposes IBKR identity fields to non-IBKR callers. Raw strings do not prove canonical identity or unit relationships.",
      "targetModel": "Use `InstrumentDescriptor` as a semantic unit with required evidence-backed identity/context and `scope: Public | AccountScope` (never a fabricated account for public data), plus a precise provider extension for native facts. Focused constructors can refine equity, option, future, cash, crypto, combo, or news relationships when that provider declares them; IBKR’s current SecType cases stay in the IBKR adapter. New providers are not forced into a closed global kind list.",
      "pureTransitions": [
        "Untrusted instrument input -> Result<InstrumentDescriptor, ValidationIssue[]>: parse one declared descriptor schema and preserve provider extension fields",
        "Resolved native identity tuple + catalog uniqueness state -> Result<InstrumentId, IdentityMappingFailure>",
        "InstrumentDescriptor + selected capability -> Result<NativePlan, NativePlanFailure>: add native fields only after mapping"
      ],
      "effectBoundary": "Parsing and variant construction are pure. Resolving conId/tradingClass, venue/listing context, or alias uniqueness is application/adapter IO and returns versioned facts before native plan compilation.",
      "durability": "Prepared records persist the canonical descriptor, InstrumentId, context, native plan version, and conflict-free native alias mapping; they do not persist a generic input record or secret/native object.",
      "errors": [
        "InstrumentSchemaMismatch(fields) -> ValidationFailed",
        "InvalidIdentifier(raw) -> ProtocolViolation/ValidationFailed at input boundary",
        "NativeIdentityMissingOrAmbiguous -> PreconditionFailed until the adapter resolves the venue key",
        "ProviderCapabilityUnavailable(operation) -> unavailable leaf; retain readable provider data where its schema permits"
      ],
      "preservedBehavior": [
        "Preserve support for localSymbol, expiry, strike, right, multiplier, primary exchange, descriptive display data, and combo legs where the target variant needs them",
        "Keep native conId/tradingClass available for IBKR but isolate them from broker-neutral identity"
      ],
      "replacementSteps": [
        "Define declaration-backed constructors/codecs for each provider and focused semantic descriptor, then update current callers",
        "Add durable mapping conflict checks, then delete BuildContractInput after callers use typed descriptors or provider request schemas"
      ],
      "verificationCases": [
        "An OPT without strike cannot be constructed, while an equity cannot carry an option right",
        "Two listings with the same display symbol produce distinct InstrumentIds until an evidence-backed mapping proves equality; conflicting mappings are rejected",
        "A caller cannot pass conId as the canonical identity for a non-IBKR instrument, and an unmappable native kind is unavailable rather than invented"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "Required identity/context products and derivative relationships remain precise, but providers may publish exact native facts or extensions when no common Instrument semantic exists; capability absence is not forced into a closed taxonomy.",
      "contractAnchors": [
        "K02",
        "K03",
        "K04",
        "K07"
      ]
    },
    {
      "id": "MAP-BD0C97191F",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/contract-builder.ts:49-74: buildContract allocates an IBKR Contract, copies fields with truthiness checks, defaults localSymbol, then calls assertContract"
      ],
      "currentBehavior": "The function sets symbol/secType/exchange/currency, defaults localSymbol to symbol, copies optional fields, and synchronously throws via assertContract. It returns a mutable SDK instance to every caller.",
      "problem": "Construction, validation, and SDK allocation are inseparable; ordinary malformed broker data becomes a thrown Error. Truthiness checks also make field presence semantics depend on JavaScript coercion, while no canonical InstrumentId is created.",
      "targetModel": "Move it to `brokers/ibkr/contract-codec.ts` as `compileIbkrContract(IBKRInstrumentPlan): Result<NativeContract, NativeEncodingFailure>`. Domain first decodes an evidence-backed descriptor; the IBKR compiler applies localSymbol default, the currently observed native field requirements, and native sentinels explicitly. Its table is the versioned IBKR declaration for currently known values, not a cross-provider instrument universe or permission to silently reinterpret future labels.",
      "pureTransitions": [
        "IBKRInstrumentPlan -> Result<NativeContract, NativeEncodingFailure>: deterministic field encoding, with `strike=0` handled by refined numeric validation rather than truthiness",
        "Descriptor + mapping fingerprint -> Result<IBKRInstrumentPlan, NativePlanFailure>: reject missing/conflicting identity evidence",
        "Malformed adapter input -> Err(NativeProtocolViolation(fields))"
      ],
      "effectBoundary": "Compilation itself is pure and creates a native SDK value only inside the IBKR adapter. Contract detail lookup and catalog identity resolution happen earlier in application preparation, not in this function.",
      "durability": "Store the serializable plan, identity mapping fingerprint, and adapter codec version; reconstruct the SDK Contract only at dispatch. A plan whose version or mapping is unsupported returns PlanVersionUnsupported.",
      "errors": [
        "NativeEncodingFailure(field, reason) -> typed dispatch/preparation failure",
        "PlanVersionUnsupported(version) -> reprepare",
        "IdentityMappingConflict -> PreconditionFailed",
        "InvalidInstrumentContext -> reject before native allocation"
      ],
      "preservedBehavior": [
        "Keep localSymbol fallback and derivative fields that existing IBKR callers rely on",
        "Replace synchronous generic throw with a typed Result at the boundary"
      ],
      "replacementSteps": [
        "Migrate IBKR contract callers to this adapter compiler and give every other provider its own declaration-backed codec",
        "Remove shared buildContract and assertContract calls after adapter codecs own validation"
      ],
      "verificationCases": [
        "Valid STK plan compiles with localSymbol equal to symbol",
        "A malformed option or conflicting identity mapping returns a typed failure without constructing a contract visible to domain",
        "A zero/empty field is handled according to refined field semantics, not silently skipped by `if`"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "IBKR compile defaults and field requirements remain native facts, but the compiler is an IBKR declaration implementation and does not define the universe of provider instruments or operations.",
      "contractAnchors": [
        "K03",
        "K07",
        "K08"
      ]
    },
    {
      "id": "MAP-4D036C0223",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/contract-builder.ts:78-100: BuildPositionInput contains a Contract, Decimal quantity, raw monetary strings, optional multiplier, optional valuation pair, avgCostSource, and PositionRisk"
      ],
      "currentBehavior": "The input accepts a mutable SDK Contract plus unparsed currency and valuation strings. `marketValue` and `unrealizedPnL` are optional independently, and risk metadata is passed through without a provenance or account scope.",
      "problem": "The shape cannot tell whether a value came from the broker or local math, whether the currency matches the instrument/account, or whether an optional field is absent versus a native sentinel already decoded incorrectly.",
      "targetModel": "Use adapter codec fixtures for the native allowlist and `PositionObservation`/`derivePositionMath` fixtures for pure account data calculations. Position observations carry AccountScope because they are account facts; public Instrument descriptors use Public scope and do not inherit that account field. Each fixture carries currency and valuation provenance where relevant.",
      "pureTransitions": [
        "Native position row -> Result<PositionObservation, ObservationFailure[]>: parse identity/scope/units and preserve each known broker valuation component",
        "PositionObservation -> Result<ExposureSnapshot, ValuationFailure>: enforce currency/units and valuation evidence",
        "NoComponents + fresh complete unit-checked inputs -> Derived valuation; MarketValueOnly/PnlOnly -> Incomplete evidence and `PreconditionFailed(ValuationIncomplete)` for Prepare"
      ],
      "effectBoundary": "Adapters own SDK/raw string parsing and broker risk interpretation. Domain math consumes constructed observations; application supplies account scope, observation timestamp, and freshness evidence.",
      "durability": "Persist account scope, canonical instrument identity, decimal strings, multiplier, exact valuation evidence/formula version, risk provenance, and observation sequence. Never persist the native Contract object or fabricate a missing component.",
      "errors": [
        "DecimalParseFailure(field, raw) -> reject observation",
        "CurrencyMismatch(instrumentCurrency, valueCurrency) -> valuation failure",
        "ObservationScopeMissing -> do not project position into an account",
        "ValuationIncomplete(kind) -> retain query observation but reject preparation requiring complete valuation"
      ],
      "preservedBehavior": [
        "Retain broker/wallet avg cost distinction and leveraged risk metadata",
        "Retain Decimal precision and multiplier as an explicit output field and preserve partial scope/instrument/quantity facts"
      ],
      "replacementSteps": [
        "Add adapter-specific row decoders and convert callers to PositionObservation",
        "Update Accounting/position projection and Prepare admission to persist/enforce valuation evidence, then delete the SDK-shaped BuildPositionInput"
      ],
      "verificationCases": [
        "A CCXT position with quote USDT is not persisted as USD without an explicit currency conversion observation",
        "A position lacking account scope is rejected even if symbol and quantity are valid",
        "A row with only marketValue becomes retained `Incomplete(MarketValueOnly)` and Prepare refuses it without manufacturing PnL; risk fields remain SDK-free"
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The fixture’s split between native decoding and SDK-free valuation is retained, with the new contract making identity, scope, and valuation evidence explicit rather than adding order lifecycle to a position reader.",
      "contractAnchors": [
        "K04",
        "K05",
        "K08"
      ]
    },
    {
      "id": "MAP-5BDBD294C0",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/contract-builder.ts:102-161: multiplier selection, OPT/FOP guard, paired pass-through/partial derivation, and returned Position all use the SDK Contract"
      ],
      "currentBehavior": "The function selects explicit multiplier, then Contract multiplier, then '1'; rejects OPT/FOP 1/empty; trusts both upstream valuation fields when present; otherwise derives missing fields; and returns Position with the Contract embedded.",
      "problem": "A single function both interprets native identity and performs pure arithmetic. A partially supplied upstream valuation is mixed with derived output, and the multiplier guard is a generic Error rather than a typed observation failure.",
      "targetModel": "Implement `normalizePositionObservation` and `derivePositionValuation` over typed values and tagged `ValuationEvidence`. `Position` references `InstrumentId` and `AccountScope`, never Contract. `InvalidDerivativeMultiplier` is an exhaustive `ObservationFailure`; `Incomplete` preserves partial observations and only `NoComponents` with complete fresh inputs can become `Derived`.",
      "pureTransitions": [
        "Raw decoded row -> Result<PositionObservation, ObservationFailure[]>: choose explicit multiplier, then validated native multiplier, then unit one only for allowed non-derivative kinds",
        "Broker pair -> BrokerReported valuation preserving both values/source/as-of without recomputation",
        "One-sided marketValue/PnL -> Incomplete(MarketValueOnly/PnlOnly) preserving the known component",
        "NoComponents + complete fresh unit-checked quantity/price/multiplier -> Derived pair with formulaVersion",
        "OPT/FOP multiplier 1/empty -> Err(InvalidDerivativeMultiplier)"
      ],
      "effectBoundary": "No IO in arithmetic/evolution. Native decoder and observation service provide inputs/freshness/source facts; journal writer persists the result and provenance after application acceptance. Prepare performs the complete-valuation admission check.",
      "durability": "Persist formula version for derived values and source/as-of for broker values; persist incomplete evidence without a synthetic counterpart. If a multiplier or source is unknown, write a typed failed observation rather than a misleading position.",
      "errors": [
        "InvalidDerivativeMultiplier(instrument, multiplier) -> stop projection and require adapter correction",
        "ValuationIncomplete(kind) -> query/accounting may retain it but Prepare rejects",
        "DerivedInputsIncomplete -> do not derive a pair",
        "PositionInstrumentMismatch -> precondition/protocol failure"
      ],
      "preservedBehavior": [
        "Preserve arithmetic outcomes and explicit multiplier override for valid inputs",
        "Preserve the final defense against 100x option undercount and retain partial position facts, with typed failures instead of throw"
      ],
      "replacementSteps": [
        "Extract/use position-math as the sole pure calculation and migrate adapter decoders",
        "Change Position and projection schemas to remove Contract, add valuation evidence and Prepare admission, then delete buildPosition"
      ],
      "verificationCases": [
        "A complete 5x option with multiplier 100 produces the current valuation",
        "A raw option with no multiplier cannot reach persisted Position state",
        "A marketValue-only observation remains queryable as Incomplete and Prepare rejects it; after restart a broker-reported pair is replayed without recalculation"
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The exact multiplier precedence, partial valuation retention, and replay provenance remain the required behavior; only the owning codec and observation schema change.",
      "contractAnchors": [
        "K04",
        "K05",
        "K08"
      ]
    },
    {
      "id": "MAP-5A77E2D561",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/fuzzy-rank.spec.ts:1-17: fixture helper constructs Contract, sets symbol/localSymbol/description/secType, and derives symbols from ContractDescription.contract"
      ],
      "currentBehavior": "Every ranking fixture creates an IBKR Contract and expects fuzzyRankContracts to return ContractDescription objects. Optional base/quote/name hints are attached outside the contract.",
      "problem": "The ranking policy is tested through SDK objects, so a shared catalog API can accidentally keep leaking vendor classes. Fixtures also have no canonical instrument identity or venue context, making cross-source result handling untestable.",
      "targetModel": "Use an SDK-free `InstrumentDescriptor` catalog fixture with evidence-backed `InstrumentId`, displaySymbol, optional localSymbol, base, quote, displayName, provider-declared kind evidence, `VenueId`, listing jurisdiction, and `scope: Public | AccountScope`. A brand is only nominal; fixture construction consumes a resolved catalog/native-key mapping and rejects duplicate uniqueness keys. Adapter tests separately verify native-to-descriptor decoding.",
      "pureTransitions": [
        "InstrumentDescriptor[] + SearchQuery -> RankedInstrumentDescriptor[] with score and stable ordinal",
        "Native catalog row -> Result<InstrumentDescriptor, CatalogDecodeFailure>"
      ],
      "effectBoundary": "Ranking fixtures are pure. Native Contract construction is confined to IBKR codec tests; application search performs IO and passes decoded entries to the ranker.",
      "durability": "Catalog projections persist canonical identity and source metadata with refresh/version timestamps; ranking output is a read result, not execution authority.",
      "errors": [
        "CatalogDecodeFailure(field) -> mark source degraded and continue only with explicit failure metadata",
        "MissingInstrumentId -> reject an entry instead of ranking a symbol-only object"
      ],
      "preservedBehavior": [
        "Keep symbol/local/name/base/quote signals available to ranking",
        "Keep tests for missing optional display fields, but express absence in the catalog schema"
      ],
      "replacementSteps": [
        "Rewrite entry and symbolsOf helpers around InstrumentDescriptor/RankedInstrumentDescriptor",
        "Add broker-specific decoder tests and remove ContractDescription from the shared ranker suite"
      ],
      "verificationCases": [
        "A catalog entry represented by an `InstrumentDescriptor` ranks with the documented score and source ordinal",
        "Two listings with the same display symbol remain distinct by InstrumentId",
        "A native row without canonical identity is rejected rather than becoming an executable search hit"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "Ranking fixtures preserve optional display signals and deterministic ordering, but the declared input/output schema now carries canonical identity evidence and does not allocate SDK objects.",
      "contractAnchors": [
        "K03",
        "K04",
        "K05",
        "K12"
      ]
    },
    {
      "id": "MAP-C0F9438F7F",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/fuzzy-rank.spec.ts:19-69: tests cover empty/zero filtering, exact symbol/base/name, prefix tiers, name word boundaries, and escaped regex metacharacters"
      ],
      "currentBehavior": "The suite verifies deterministic tier precedence and that a query such as B.B is escaped before regex matching. It observes only output symbol order, not score metadata or identity.",
      "problem": "Symbol-only assertions cannot detect duplicate identity, wrong venue, score ties, or a ranker that returns an SDK object while preserving visible symbols. The policy is implicit in test ordering rather than a named result contract.",
      "targetModel": "Expose a pure `rankCatalog(query, entries)` returning `RankedEntry { entry, score, sourceOrdinal }`. Keep exact=100, prefix=80, name-boundary prefix=70, whole-word=50, substring=30, quote=20, none=0 as a named scoring policy and require identity-preserving output; source ordinal is explicit input, never incidental array settlement order.",
      "pureTransitions": [
        "SearchQuery + InstrumentDescriptor -> ScoreTier: lowercase/trim and escaped literal matching",
        "InstrumentDescriptor[] + explicit source ordinals -> stable RankedEntry[]: discard score 0, order descending score then source ordinal, and leave unresolved/ambiguous identities as typed source failures"
      ],
      "effectBoundary": "No IO; application/adapter callers supply the entries. Protocol encodes score/order only if the public contract chooses to expose them, never the native regex or SDK object.",
      "durability": "Ranking is not journal authority. If paginated result cursors are persisted for a query, they include query normalization/version and source snapshot version.",
      "errors": [
        "InvalidSearchQuery -> typed QueryFailure at request boundary",
        "InstrumentDescriptorIdentityMissing -> source decode failure, not a zero-score executable result"
      ],
      "preservedBehavior": [
        "Keep exact and prefix precedence, word-boundary behavior, and literal metacharacter escaping",
        "Keep empty queries from producing broad catalog sweeps"
      ],
      "replacementSteps": [
        "Make scores/tier precedence explicit in the pure ranker and update tests to assert score plus InstrumentId",
        "Add cross-source duplicate/tie cases before application integration"
      ],
      "verificationCases": [
        "CORN ranks before CORNING on exact-symbol score",
        "GFI outranks GS for gold because Gold is a word boundary in GFI but not GS",
        "B.B does not match BBB",
        "An equal-score pair preserves source ordinal and does not reorder across venues"
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The documented score tiers and stable source-ordinal tie rule are retained exactly as pure query behavior; the capability composition only changes the descriptor boundary around them.",
      "contractAnchors": [
        "K03",
        "K05",
        "K06",
        "K12"
      ]
    },
    {
      "id": "MAP-BD469BCA6D",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/fuzzy-rank.spec.ts:71-101: tests limit 5/default 50, stable ties, quote-only fallback, and missing Contract fields"
      ],
      "currentBehavior": "The test expects a hard length cap of 5 or silently defaulted 50, relies on source order for ties, treats quote-only matches as weakest, and accepts a Contract with only symbol set.",
      "problem": "A length-only assertion hides dropped matches and gives no continuation/truncation signal. Missing SDK fields are tolerated without deciding whether the result is safe to display or resolve to identity.",
      "targetModel": "Return `CatalogSearchPage { items, query, totalMatched: Known(number) | Unknown(reason), nextCursor, truncated, sourceSnapshot, completeness }`; use a validated `PageRequest {limit,cursor}` and preserve deterministic tie/quote rules. An entry may omit display hints only after its evidence-backed InstrumentId and venue are valid.",
      "pureTransitions": [
        "RankedInstrumentDescriptor[] + PageRequest + source completeness -> CatalogSearchPage: slice by explicit cursor/limit and set truncated/nextCursor",
        "InstrumentDescriptor with missing optional labels -> ranked item; descriptor with missing/ambiguous identity -> DecodeFailure"
      ],
      "effectBoundary": "Pagination and ranking are pure over a fixed source snapshot. Application obtains/refreshes snapshots and protocol exposes explicit degraded/truncation metadata; incomplete sources make totalMatched unknown rather than a fabricated count.",
      "errors": [
        "PageRequestInvalid(limit/cursor) -> QueryFailure before catalog access",
        "CatalogIncomplete(source/evidence) -> `totalMatched: Unknown` and degraded completeness, not a fabricated count",
        "IdentityMissing(entry) -> CatalogDecodeFailure and row exclusion"
      ],
      "durability": "Catalog snapshot/version, source precedence, and cursor policy must be stable for a page; no journal write is needed for a read, but refresh metadata and skipped-row evidence belong in the projection store.",
      "preservedBehavior": [
        "Keep stable ties and quote-only low priority",
        "Keep a UI-friendly bounded page, but intentionally expose that it is bounded instead of pretending the result set is complete"
      ],
      "replacementSteps": [
        "Replace length assertions with page metadata assertions and add a cursor/truncation case",
        "Replace missing Contract fixture with a valid SDK-free entry with optional labels omitted"
      ],
      "verificationCases": [
        "80 matching entries with limit 5 return five plus truncated=true and a continuation cursor",
        "A second page cannot repeat or skip an item when source order is stable",
        "A symbol-only row lacking InstrumentId is rejected before it appears in the result envelope"
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The bounded limit, stable ties, and quote fallback remain observable behavior, now represented by an explicit page envelope that reports truncation and incomplete sources instead of silently discarding them.",
      "contractAnchors": [
        "K05",
        "K06",
        "K12"
      ]
    },
    {
      "id": "MAP-FBB735D928",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/fuzzy-rank.ts:1-18: shared ranker is documented for Alpaca/CCXT/Mock but imports ContractDescription and Contract from IBKR; IBKR bypasses it"
      ],
      "currentBehavior": "Enumerating brokers share this ranker, while IBKR uses a server-side endpoint. Despite the split, the shared utility's input/output classes are IBKR SDK classes.",
      "problem": "The utility's claimed cross-broker scope is undermined by a vendor-shaped boundary. A future adapter must either fabricate IBKR Contracts or bypass the policy, creating inconsistent search behavior.",
      "targetModel": "Make ranking a domain/query function over `InstrumentDescriptor` and `RankedInstrumentDescriptor`; adapters choose server-side search or local enumeration but decode both into the same broker-neutral descriptor/result contract before application aggregation. Source preference is explicit (`sourceOrdinal`/precedence), not an accident of Promise settlement or object iteration.",
      "pureTransitions": [
        "InstrumentDescriptor[] + normalized query + explicit source ordinals -> stable scored descriptors",
        "Search capability variant (`EnumerateCatalog` | `VenueSearch`) -> application query strategy without broker-name branching in domain; malformed rows become typed skipped-row evidence"
      ],
      "effectBoundary": "Adapters own native requests and SDK decoding. Application owns fan-out, source authorization, failure aggregation, skipped-row accounting, and protocol encoding; domain ranker has no IO.",
      "durability": "Store catalog identity/refresh state, source precedence, and skipped-row/source-failure evidence in projection store; do not persist ContractDescription. Query result includes source snapshot/version and completeness when reproducibility matters.",
      "errors": [
        "UnsupportedSearchCapability(source) -> SourceStatus.Unavailable or Partial, never a silent source omission",
        "CatalogDecodeFailure(source,row) -> skipped-row evidence and degraded completeness",
        "IdentityConflict -> preserve conflicting hits as an explicit source failure; do not merge by display symbol"
      ],
      "preservedBehavior": [
        "Keep full-catalog ranking for Alpaca, CCXT, and Mock",
        "Keep IBKR's venue-side ranking path, but normalize its results into the same broker-neutral result type"
      ],
      "replacementSteps": [
        "Create the common InstrumentDescriptor catalog schema and migrate three enumerating adapters",
        "Delete ContractDescription/Contract imports and adapt IBKR response decoder separately"
      ],
      "verificationCases": [
        "Alpaca and CCXT results can be aggregated without casting native objects and with source ordinals preserved",
        "A malformed CCXT row appears in source evidence as skipped/degraded rather than silently reducing an authoritative total; an Alpaca pre-catalog echo is non-executable",
        "IBKR server-side results and local ranked results share identity/display fields",
        "A new broker can implement either search capability without modifying a generic kernel switch"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The shared ranking policy is retained as a pure unit, while server-side and enumerating providers independently declare their search leaf and normalize into the metadata contract without a kernel broker switch.",
      "contractAnchors": [
        "K01",
        "K02",
        "K05",
        "K07"
      ]
    },
    {
      "id": "MAP-1D24B84F9B",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/fuzzy-rank.ts:20-29: FuzzyRankInput is Pick<Contract> plus optional base/quote/name strings, and missing fields are intentionally tolerated"
      ],
      "currentBehavior": "The ranker derives symbol/local/description from a partial SDK Contract and optional broker hints. No field carries canonical identity, source, venue, asset kind, or jurisdiction.",
      "problem": "A partial Pick is not a safe catalog record: missing fields are silently interpreted as empty strings, and a symbol can be returned without enough information to resolve an order. Native metadata and domain identity are conflated.",
      "targetModel": "Define `InstrumentDescriptor` with required `InstrumentId`, `VenueId`, displaySymbol, provider-declared kind evidence, listing jurisdiction, and `scope: Public | AccountScope`; use AccountScope only when the catalog result is account-bound or can feed an order. Optional localSymbol, normalized base/quote, display name, and source aliases are separate fields, and public data never receives a guessed account or subaccount.",
      "pureTransitions": [
        "Native row -> Result<InstrumentDescriptor, CatalogDecodeFailure> with exact required field checks",
        "InstrumentDescriptor + query -> RankScore using optional display signals without inventing identity"
      ],
      "effectBoundary": "Boundary decoders parse unknown native rows and return typed failures. Pure ranker consumes trusted entries; application resolves any selected InstrumentId to an executable action context.",
      "durability": "Catalog entries persist canonical ID and native alias mapping; aliases are search hints and cannot replace InstrumentId in a prepared action.",
      "errors": [
        "MissingCanonicalIdentity -> CatalogDecodeFailure",
        "InvalidVenueContext -> QueryFailure",
        "AmbiguousAlias -> explicit selection required, no automatic order resolution"
      ],
      "preservedBehavior": [
        "Keep optional base/quote/name signals for CCXT and Alpaca ranking",
        "Keep graceful omission of display fields, but not omission of executable identity"
      ],
      "replacementSteps": [
        "Implement schema decoder per broker and update Alpaca/CCXT/Mock candidate construction",
        "Change ranker signatures and delete the Contract Pick type"
      ],
      "verificationCases": [
        "An entry with no name still ranks by symbol/base",
        "Two rows with same symbol but different VenueId are not merged by the type",
        "An alias match returns the canonical InstrumentId and requires no aliceId mutation"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "Optional base/quote/name ranking hints remain, but a partial Contract is replaced by a descriptor assembled from identity evidence and explicit scope/context before it can feed an effect.",
      "contractAnchors": [
        "K03",
        "K04",
        "K05"
      ]
    },
    {
      "id": "MAP-A4E9FFB7DA",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/fuzzy-rank.ts:31-34: FuzzyRankOptions has only optional numeric limit with documented default 50 and no cursor/total/truncation"
      ],
      "currentBehavior": "Callers can provide limit, otherwise 50 is silently selected; no validation prevents negative/non-integer limits and no result metadata explains discarded matches.",
      "problem": "A silent cap can make a search look complete while hiding matches, and an invalid numeric limit has language-level slice semantics rather than a domain decision. The return type cannot support deterministic pagination.",
      "targetModel": "Replace options with `CatalogPageRequest {limit: PositiveInt, cursor?: Cursor, snapshot?: CatalogSnapshotId}` and return `CatalogSearchPage` containing items, `totalMatched: Known | Unknown`, `nextCursor`, `truncated`, source precedence, and source degradation/completeness.",
      "pureTransitions": [
        "Unknown page input -> Result<PageRequest, QueryFailure>",
        "Ranked entries + validated page request -> CatalogSearchPage with explicit truncation/cursor"
      ],
      "effectBoundary": "Validation/ranking/page slicing are pure. Application obtains a stable catalog snapshot and protocol decodes/encodes the request/response; no broker IO in this function.",
      "durability": "A cursor is tied to query normalization and catalog snapshot version; the projection store owns snapshot metadata, not the transaction journal.",
      "errors": [
        "InvalidLimit(value) -> QueryFailure",
        "CursorExpired(snapshot) -> restart search",
        "SourceIncomplete(reason) -> page marks degraded/incomplete rather than claiming total completeness"
      ],
      "preservedBehavior": [
        "Keep a default UI page size only as an explicit protocol default",
        "Keep caller-requested bounding, now with visible truncation"
      ],
      "replacementSteps": [
        "Define page schema and validate protocol limits",
        "Migrate ranker callers and tests, then remove FuzzyRankOptions"
      ],
      "verificationCases": [
        "Limit 0 and negative limits are rejected",
        "More than 50 matches return truncation metadata when the default page is used",
        "A cursor from an older catalog snapshot is rejected rather than returning unstable duplicates"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source’s default and explicit result caps remain useful query policy, but pagination is now a finite data-result contract with cursor/snapshot/completeness rather than hidden truncation.",
      "contractAnchors": [
        "K05",
        "K06",
        "K12"
      ]
    },
    {
      "id": "MAP-4C9A6FE792",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/fuzzy-rank.ts:36-73: score lowercases query/fields, assigns 100/80/70/50/30/20/0 tiers, and escapes regex metacharacters"
      ],
      "currentBehavior": "score uses symbol, localSymbol, base, quote, and name/description; exact matches win, then prefixes and word-aware name matches, then substrings, with quote equality as the weakest signal. It returns a bare number.",
      "problem": "The deterministic policy is useful but hidden behind SDK field extraction and a bare number. Case/trim policy, tie semantics, and quote fallback are not part of a named result contract, so different callers may diverge.",
      "targetModel": "Create `SearchQuery` (trimmed literal plus asset/context hint), `RankTier` enum, and `rankScore(entry, query): RankScore`. Keep literal regex escaping as an implementation detail and attach tier/score to the ranked result for audit/debug.",
      "pureTransitions": [
        "Raw query -> Result<SearchQuery, QueryFailure>: trim, reject empty for search",
        "SearchQuery + InstrumentDescriptor -> RankScore: evaluate exact/prefix/boundary/substring/quote tiers deterministically"
      ],
      "effectBoundary": "Pure only. The caller decides whether to use venue-side ranking or this local ranker and may redact score details from public protocol.",
      "durability": "If ranking reproducibility is needed, persist rank policy version with the catalog snapshot; ranking itself does not mutate journal state.",
      "errors": [
        "EmptySearchQuery -> QueryFailure for broad search endpoints",
        "InvalidUnicode/queryEncoding -> ProtocolViolation at request decoding"
      ],
      "preservedBehavior": [
        "Retain all current tier ordering and escaped literal semantics",
        "Retain source-order tie breaking in the enclosing ranking function"
      ],
      "replacementSteps": [
        "Extract score into a pure SDK-free function and define named score/tier values",
        "Update result schema and tests to assert rank tiers/identity, then remove Contract-derived field access"
      ],
      "verificationCases": [
        "Exact base beats POPCORN prefix; word-boundary Gold beats embedded Goldman",
        "A query containing `.` remains literal",
        "Quote-only USDT ranks below an exact base match",
        "A case-insensitive query returns the same rank as uppercase input"
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The rank score’s pure tier ordering, literal escaping, and case/trim behavior are retained exactly; the new schema only names the query and result units.",
      "contractAnchors": [
        "K03",
        "K05",
        "K06"
      ]
    },
    {
      "id": "MAP-41607450D3",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/brokers/fuzzy-rank.ts:75-99: fuzzyRankContracts trims/query-filters, stable-sorts, slices to limit/default 50, then allocates ContractDescription and Object.assign(new Contract(), input)"
      ],
      "currentBehavior": "The function returns only top ContractDescription objects, silently drops zero-score and over-limit entries, and clones arbitrary Contract fields. It has no source failure, duplicate, canonical identity, or truncation metadata.",
      "problem": "It combines pure ranking, pagination, and vendor object allocation. Shallow cloning lets SDK fields cross the shared boundary and silent slicing hides incomplete search results.",
      "targetModel": "Split `rankCatalog` (pure ranked descriptors) from `pageCatalogResults` (pure page envelope) and adapter codecs. Result items carry evidence-backed InstrumentId/display/context and stable source ordinal/preference; native ContractDescription allocation is removed from this module. Dedup occurs only after runtime identity resolution, and identity conflicts remain source failures.",
      "pureTransitions": [
        "InstrumentDescriptor[] + SearchQuery + explicit source ordinals -> RankedInstrumentDescriptor[]",
        "RankedInstrumentDescriptor[] + PageRequest + completeness -> CatalogSearchPage with Known/Unknown totalMatched",
        "Native broker response -> Result<InstrumentDescriptor, CatalogDecodeFailure> plus typed skipped-row evidence; scope remains Public or explicit AccountScope"
      ],
      "effectBoundary": "Only adapters allocate SDK objects and perform catalog IO. Application aggregates pages/failures and protocol encodes the named result envelope; source precedence is supplied as data.",
      "durability": "Catalog refresh/projection owns snapshot versions, native alias mappings, source precedence, and skipped-row evidence; no transaction event is emitted for a read-only rank operation.",
      "errors": [
        "CatalogDecodeFailure -> source-level degraded response",
        "SkippedNativeRow(source,row,reason) -> completeness evidence, not silent loss",
        "PagePolicyViolation -> QueryFailure",
        "DuplicateInstrumentIdentity -> deterministic dedup only when equality evidence is durable; conflicts require resolution"
      ],
      "preservedBehavior": [
        "Preserve trim, zero-score filtering, descending score and source-order tie behavior through explicit source ordinal",
        "Preserve bounded UI results with explicit metadata instead of silent slice"
      ],
      "replacementSteps": [
        "Migrate Alpaca, CCXT, Mock, and test callers to the split functions",
        "Remove ContractDescription/Contract construction and generic Object.assign from fuzzy-rank.ts"
      ],
      "verificationCases": [
        "A ranked page contains canonical IDs and never an SDK instance",
        "Two sources returning the same proven InstrumentId are deduplicated by declared source precedence; same symbols without equality evidence remain separate",
        "A page with omitted tail reports nextCursor/truncated and unknown total when a source is incomplete",
        "Malformed native fields become skipped-row/source evidence without crashing other source pages"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "Ranking, paging, adapter decoding, and identity deduplication are separated so a data query does not inherit order approval/WAL semantics, while malformed rows remain explicit source evidence.",
      "contractAnchors": [
        "K01",
        "K05",
        "K06",
        "K07"
      ]
    },
    {
      "id": "MAP-B7395F4C42",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/contract-discipline.spec.ts:10-22: makeContract creates a mutable Contract with universal defaults and copies optional native derivative/display fields"
      ],
      "currentBehavior": "The fixture uses Partial<Contract>, fills AAPL/STK/NASDAQ/USD by default, and conditionally assigns expiry/strike/right/multiplier/localSymbol. It is shared evidence for SDK optional-field assumptions.",
      "problem": "Partial<Contract> lets tests bypass the same refined construction rules production should enforce and makes default fields indistinguishable from absent native values.",
      "targetModel": "Use explicit fixtures for each provider-declared `InstrumentDescriptor` shape or semantic constructor and a separate `decodeIbkrContract(raw)` fixture only where native sentinel behavior is under test. Required fields are encoded by the selected declaration, not `Partial`.",
      "pureTransitions": [
        "Provider-declared descriptor fixture -> valid domain InstrumentDescriptor",
        "Native fixture object -> Result<InstrumentDescriptor, ValidationIssue[]>"
      ],
      "effectBoundary": "SDK fixture allocation is limited to broker adapter codec tests; domain tests use plain serializable values and no SDK imports.",
      "durability": "Fixture snapshots should use canonical schema values so a replay test does not depend on mutable Contract defaults.",
      "errors": [
        "FixtureConstructionFailure -> compile/test failure for invalid variant",
        "NativeDecodeFailure -> structured ValidationIssue assertion"
      ],
      "preservedBehavior": [
        "Retain native defaults only inside the relevant provider codec where the declaration proves them valid; do not carry SDK defaults into domain fixtures",
        "Retain explicit optional derivative coverage through variant constructors"
      ],
      "replacementSteps": [
        "Replace makeContract with makeInstrument/makeNativeContract fixtures in the relevant suites",
        "Remove Partial<Contract> from domain contract-discipline tests"
      ],
      "verificationCases": [
        "An option fixture cannot omit expiry/strike/right/multiplier without constructing an invalid value",
        "A legacy JSON native record with an unknown SecType yields a typed decode failure",
        "A valid STK fixture has an explicit domain identity/context"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The fixture’s useful native-default evidence remains in IBKR codec tests, while domain fixtures construct valid semantic units and cannot use Partial<Contract> to bypass schema constraints.",
      "contractAnchors": [
        "K03",
        "K04",
        "K07",
        "K12"
      ]
    },
    {
      "id": "MAP-99C18EAB60",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/contract-discipline.spec.ts:24-44: tests assert the exact 16-value IBKR SecType list and case-sensitive isSecType narrowing",
        "packages/ibkr/src/contract.ts:75-116: current closed SecType union contains STK, OPT, FUT, FOP, IND, CASH, BOND, CMDTY, WAR, IOPT, FUND, BAG, NEWS, CFD, CRYPTO, and CRYPTO_PERP"
      ],
      "currentBehavior": "SEC_TYPES mirrors the current 16-value IBKR/OpenAlice taxonomy; isSecType accepts only exact case-sensitive strings and rejects crypto/FUTURES/empty/null. `coerceSecType` warns and drops an unknown raw wire value to `''`.",
      "problem": "The test treats a venue's native taxonomy as a global instrument taxonomy, while the current coercion can erase an unknown raw value before a typed failure records it. It does not test canonical identity, listing jurisdiction, account scope, or capability context that determines whether a recognized kind is usable.",
      "targetModel": "Keep an IBKR-only `SecTypeCodec` for the currently observed 16-value native ADT: `STK`, `OPT`, `FUT`, `FOP`, `IND`, `CASH`, `BOND`, `CMDTY`, `WAR`, `IOPT`, `FUND`, `BAG{legs}`, `NEWS`, `CFD`, `CRYPTO`, and `CRYPTO_PERP`; preserve each native field/leg before mapping to a common Instrument semantic only when exact. Provider-native extensions remain valid descriptors in their own schema. Domain tests use `MarketContext {venue, listingJurisdiction, scope: Public | AccountScope, session}`; capability derivation is separate and context-sensitive.",
      "pureTransitions": [
        "Raw native secType -> Result<IbkrSecType, NativeProtocolViolation> while preserving unknown raw value",
        "Each currently observed IbkrSecType variant + decoded fields -> Result<InstrumentDescriptor, ValidationIssue[]> through the IBKR declaration",
        "InstrumentDescriptor + context -> ActionAvailability for the selected capability, without inferring capability from kind alone"
      ],
      "effectBoundary": "SecType decoding belongs in the IBKR adapter. Domain and protocol consume broker-neutral descriptors, precise provider extensions, and typed availability; another provider never has to map its labels into an IBKR set.",
      "durability": "Persist canonical kind/identity and context, not the adapter SecType string as the only identity; schema/mapping version records native interpretation and raw unknown diagnostics when needed.",
      "errors": [
        "UnknownNativeSecType(raw) -> ProtocolViolation preserving the observed value",
        "ProviderCapabilityUnavailable(operation) -> capability-unavailable result when a common semantic is not declared",
        "ContextUnavailable -> CapabilityUnavailable/QueryFailure",
        "InstrumentIdentityAmbiguous -> require explicit selection"
      ],
      "preservedBehavior": [
        "Keep exact case-sensitive acceptance of all 16 current wire values in the adapter",
        "Keep CRYPTO_PERP support while mapping it to a domain crypto-perpetual variant and preserve BAG legs/NEWS query-only semantics"
      ],
      "replacementSteps": [
        "Move taxonomy test to brokers/ibkr codec tests",
        "Add exhaustive native-variant and domain context/capability tests, retain unknown raw evidence, and remove SEC_TYPES as a domain authority"
      ],
      "verificationCases": [
        "Runtime codec fixtures cover all currently observed values and preserve BAG legs; lowercase crypto, FUTURES, empty, and null yield typed non-success",
        "A decoded CRYPTO_PERP maps to its declared crypto-perpetual semantic while a recognized CASH in a closed session is queryable but unavailable for PlaceOrder",
        "A provider-native extension remains queryable without being coerced into an unrelated IBKR kind"
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The exact 16-value, case-sensitive IBKR observation and raw-unknown loss are retained as adapter evidence only; they no longer define a cross-provider instrument taxonomy.",
      "contractAnchors": [
        "K02",
        "K03",
        "K07"
      ]
    },
    {
      "id": "MAP-36F0A228A0",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/contract-discipline.spec.ts:46-76: tests valid STK, empty symbol/exchange, unknown BANANA via forced cast, and runtime legacy JSON leakage"
      ],
      "currentBehavior": "The suite constructs mutable Contracts and checks `validateContract` returns false with string messages for missing universal fields or an unknown SecType. The forced cast explicitly simulates old JSON loading.",
      "problem": "String search assertions and forced casts prove only message text, not a safe boundary decoder. The validator does not check canonical identity, jurisdiction, account scope, or whether a decoded field belongs to the selected instrument variant.",
      "targetModel": "Use protocol/native Schema decoding that maps unknown input to `ValidationIssue` variants (`MissingField`, `UnknownNativeKind`, `InvalidIdentity`, `InvalidContext`). Construct `InstrumentDescriptor` only after exhaustive parsing.",
      "pureTransitions": [
        "Unknown persisted record -> Result<DecodedRecord, ValidationIssue[]>",
        "Decoded instrument -> Result<InstrumentDescriptor, ValidationIssue[]>"
      ],
      "effectBoundary": "Boundary tests may feed serialized legacy records; no broker IO. Journal loader treats decode failure as corruption/recovery stop according to the shared protocol.",
      "durability": "Versioned records are either decoded into typed state or rejected; an invalid historical Contract is not silently replayed as an empty/default instrument.",
      "errors": [
        "ValidationFailed(issues) -> protocol/application 400 or recovery failure depending on boundary",
        "ReplayViolation(recordId, reason) -> stop recovery and require operator handling"
      ],
      "preservedBehavior": [
        "Continue rejecting missing symbol/secType/exchange/currency and unknown native kinds",
        "Retain regression coverage for malformed legacy persisted data, but assert structured issues"
      ],
      "replacementSteps": [
        "Replace forced casts with unknown input through the schema decoder",
        "Separate IBKR native field checks from domain identity/context checks and update all loader callers"
      ],
      "verificationCases": [
        "BANANA from a serialized old record yields UnknownNativeKind, not a thrown message",
        "An empty exchange and currency produce field-specific issues",
        "A syntactically valid symbol lacking venue/listing context cannot become an executable descriptor"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "Legacy malformed-record rejection remains required, but schema decoding now yields typed issues at the boundary and does not impose a universal provider record.",
      "contractAnchors": [
        "K03",
        "K07",
        "K11"
      ]
    },
    {
      "id": "MAP-3562FB9C00",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/contract-discipline.spec.ts:78-108: OPT/FOP tests require expiry, strike, right, multiplier, accept a complete option, and reject right X"
      ],
      "currentBehavior": "The tests exercise per-SecType required fields and assert invalid right through message regex. They use the IBKR Contract object as both input and expected valid output.",
      "problem": "Requirements are represented as a runtime field bag and test assertions do not capture refined expiry/strike/right/multiplier units or account/venue capability. A valid native shape may still be untradeable in a given context.",
      "targetModel": "Use a provider-declared option/future-option descriptor, where that provider exposes those relations, with evidence-backed underlying `InstrumentId`, expiry: `MarketDate`, quote-unit strike, `Call | Put`, and positive contract multiplier. Adapter codecs retain C/P/CALL/PUT normalization; capability checks consume context separately. No provider is required to expose a global `OptionInstrument` or `FutureOptionInstrument` variant.",
      "pureTransitions": [
        "Native option fields -> Result<provider-declared option descriptor, ValidationIssue[]>",
        "Provider-declared option descriptor + ActionContext -> declared capability availability",
        "Invalid right or missing derivative field -> typed ValidationFailed"
      ],
      "effectBoundary": "IBKR option wire parsing stays in brokers/ibkr; domain constructor is pure. Contract-chain lookup is application IO and provides authoritative InstrumentId/underlying relation.",
      "durability": "Persist normalized right, expiry, strike, multiplier, underlying ID, and codec version. Do not persist raw Contract or unvalidated right strings.",
      "errors": [
        "MissingDerivativeField(field) -> ValidationFailed",
        "InvalidOptionRight(value) -> NativeProtocolViolation",
        "OptionUnderlyingUnresolved -> PreconditionFailed before preparation"
      ],
      "preservedBehavior": [
        "Keep all four structural requirements and accept C/P/CALL/PUT native forms",
        "Keep refusal of invalid right values, now as a typed issue"
      ],
      "replacementSteps": [
        "Move native shape tests to the adapter codec; add provider-declared option constructor and context tests",
        "Migrate option chain/order callers to InstrumentId and typed option fields"
      ],
      "verificationCases": [
        "OPT with 20260720/150/C/100 constructs a valid descriptor under the selected provider declaration",
        "FOP missing multiplier fails before position math",
        "A CALL native value normalizes to Call while X yields InvalidOptionRight"
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "OPT/FOP required fields and right normalization remain empirical constraints in the relevant provider schema; capability and identity are evaluated separately rather than folded into those fields.",
      "contractAnchors": [
        "K03",
        "K04",
        "K07"
      ]
    },
    {
      "id": "MAP-2044134F6B",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/contract-discipline.spec.ts:110-120: FUT tests require expiry and multiplier but not strike/right"
      ],
      "currentBehavior": "The native validation accepts complete futures with YYYYMM expiry and multiplier while rejecting missing expiry/multiplier; strike/right are not required.",
      "problem": "The test proves only a static IBKR field rule and leaves expiry format, multiplier units, canonical identity, and venue calendar semantics implicit.",
      "targetModel": "Use a provider-declared future descriptor carrying evidence-backed `InstrumentId`, native underlying code as an adapter extension, expiry: `ContractMonth`, contractMultiplier: `PositiveDecimal`, `VenueId`, listing jurisdiction, and operation-relevant `Public | AccountScope`. Keep strike/right out of the future declaration; derive capability from venue/session/account context. This does not impose a global FutureInstrument shape on providers.",
      "pureTransitions": [
        "Native FUT row -> Result<provider-declared future descriptor, ValidationIssue[]> validating the provider's YYYYMM/venue rules",
        "Provider-declared future descriptor + MarketContext -> declared capability availability"
      ],
      "effectBoundary": "Adapter parses native expiry/multiplier; application resolves venue contract identity and calendar. Domain transition has no IO.",
      "durability": "Persist normalized contract month and multiplier as decimal strings, canonical InstrumentId, and mapping version; native field names stay adapter-local.",
      "errors": [
        "InvalidContractMonth(raw) -> ValidationFailed",
        "MissingMultiplier -> ValidationFailed",
        "VenueCalendarUnavailable -> CapabilityUnavailable/PreconditionFailed"
      ],
      "preservedBehavior": [
        "Retain no strike/right requirement for futures",
        "Retain required expiry/multiplier and multiplier-aware valuation"
      ],
      "replacementSteps": [
        "Keep the native FUT test in the IBKR codec suite and add the provider-declared future descriptor constructor",
        "Migrate futures position/order paths away from Contract"
      ],
      "verificationCases": [
        "ES with 202606 and multiplier 50 decodes into the selected provider's future descriptor and preserves the multiplier",
        "A future with strike/right but no expiry is rejected for missing expiry, not accepted as an option",
        "A valid future in a closed venue session returns unavailable capability rather than a malformed contract"
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The absence of strike/right requirements for futures and required expiry/multiplier remain precise native/domain facts, with venue capability represented separately.",
      "contractAnchors": [
        "K03",
        "K04",
        "K07"
      ]
    },
    {
      "id": "MAP-B82909F33A",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/contract-discipline.spec.ts:122-135: STK, CRYPTO, and CRYPTO_PERP pass with universal fields, including BYBIT/USDT examples"
      ],
      "currentBehavior": "The validator treats STK/CRYPTO/CRYPTO_PERP as requiring only symbol, secType, exchange, and currency. Crypto classification is carried by native SecType and no context is required.",
      "problem": "A native kind alone cannot distinguish a synthetic venue instrument, spot versus perpetual, listing jurisdiction, settlement currency, account/sub-account, or whether keyless/read-only credentials can trade. Universal fields are insufficient for executable authority.",
      "targetModel": "Use provider-declared instrument descriptors for equity/crypto facts, carrying canonical IDs, venue/market context, quote/settlement currency, and `scope: Public | AccountScope`; derive read/trade capabilities separately. Minimal public crypto rows stay public, while an account-bound action requires its explicit AccountScope.",
      "pureTransitions": [
        "Decoded native row -> Result<instrument descriptor, ValidationIssue[]>",
        "Instrument + optional account scope/venue/session context -> CapabilitySet with query/read/trade availability"
      ],
      "effectBoundary": "Native SecType decoding and exchange-specific fields stay in adapters. Capability lookup and account scope are application/broker services; domain availability decision is pure over supplied facts.",
      "durability": "Persist explicit kind, quote/settlement currency, venue, scope, and capability fingerprint; do not infer kind again from a display symbol during replay.",
      "errors": [
        "SettlementContextMissing -> CapabilityUnavailable",
        "ReadOnlyAccountForTrade -> CapabilityUnavailable with reason",
        "SyntheticInstrumentUnresolved -> ValidationFailed/PreconditionFailed"
      ],
      "preservedBehavior": [
        "Retain acceptance of minimal crypto native rows for catalog/read paths",
        "Intentionally require context for execution and preserve keyless/read-only query capability"
      ],
      "replacementSteps": [
        "Add domain variant and capability tests; keep minimal native cases in adapter decoder tests",
        "Migrate crypto broker paths to canonical InstrumentId and AccountScope"
      ],
      "verificationCases": [
        "BYBIT BTC/USDT spot and perpetual records have distinct InstrumentIds/kinds",
        "A keyless account can search/read but PlaceOrder returns CapabilityUnavailable",
        "An equity-looking EURUSD under equity context is not classified as currency"
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "Minimal STK/CRYPTO/CRYPTO_PERP rows remain readable data, while execution availability now depends on declared identity, scope, and capability instead of native kind alone.",
      "contractAnchors": [
        "K02",
        "K04",
        "K07"
      ]
    },
    {
      "id": "MAP-73CA066D9E",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/contract-discipline.spec.ts:138-147: assertContract tests expect a joined Error for invalid OPT and no throw for valid STK"
      ],
      "currentBehavior": "The test treats a concatenated Error message as the public validation contract. A valid Contract returns silently; invalid output throws synchronously.",
      "problem": "Message matching loses issue structure and conflates expected malformed input with programmer defects. It also makes HTTP, journal, and broker callers parse text to decide recovery.",
      "targetModel": "Assert `Result<InstrumentDescriptor, ValidationIssue[]>`/`TransactionFailure.ValidationFailed` variants exhaustively. A separate `assertImpossible` may throw only for post-construction invariant defects, not ordinary native data rejection.",
      "pureTransitions": [
        "Native record -> Result<DecodedInstrument, ValidationIssue[]>",
        "ValidationIssue[] -> protocol failure envelope with stable codes/fields"
      ],
      "effectBoundary": "No IO in validation. Application maps expected issues to request/observation failures; runtime logs defect-only throws with redaction.",
      "durability": "Persist failed decode/audit evidence only through typed journal or observation failure schema; never use joined message text as replay data.",
      "errors": [
        "ValidationFailed(issues) -> caller reports field-level rejection",
        "ProtocolViolation -> stop malformed adapter stream",
        "InvariantDefect -> supervised defect path, not user validation"
      ],
      "preservedBehavior": [
        "Invalid options still fail before they can reach execution",
        "Valid instruments still produce a successful result rather than an exception"
      ],
      "replacementSteps": [
        "Rewrite assertions to inspect variant tags and payloads",
        "Delete assertContract from shared domain and migrate native boundary callers to typed Results"
      ],
      "verificationCases": [
        "Invalid OPT returns a list containing symbol/required-field variants without parsing a message",
        "A valid STK result is a typed success",
        "An impossible internal state is distinguishable from user/native invalid data"
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "Invalid native data still fails before any effect can run and valid data still succeeds, but the observed message-based assertion is replaced with typed boundary outcomes.",
      "contractAnchors": [
        "K03",
        "K07",
        "K11"
      ]
    },
    {
      "id": "MAP-4D916C23A7",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/contract-discipline.ts:1-19: comments describe a canonical SecType taxonomy, per-kind requirements, and a phased broker-output enforcement plan",
        "packages/ibkr/src/contract.ts:56-78,98-116: the current closed 16-value semantic set and no-new-SecType sign-off rule are the native source of truth"
      ],
      "currentBehavior": "This range is documentation only. It claims IBKR taxonomy is canonical and says Phase 1 merely exposes machinery while Phase 2 wires throwing validation into builders; those phase claims can diverge from executable ownership.",
      "problem": "Comments establish the wrong authority boundary: native IBKR taxonomy is described as domain canonical, and migration status is encoded in prose rather than one source of truth. Future readers may keep adding fields here instead of adapter codecs or inventing a new native kind.",
      "targetModel": "Documentation should state that the source module currently claims a shared broker-neutral taxonomy, while the target assigns the currently observed IBKR SecType semantics and fields/legs to an IBKR-local codec. Provider schemas remain open to exact native extensions; domain Instrument descriptors and capability metadata are not an exhaustive cross-provider list. Capability/context is derived separately from structural decode.",
      "pureTransitions": [
        "No runtime transition; documentation must point to the actual adapter codec and domain constructors",
        "Native codec Result -> domain descriptor is the documented boundary, with UnknownNativeSecType(raw) retaining unrecognized wire evidence"
      ],
      "effectBoundary": "No effect or persistence behavior belongs to this comment-only range; ownership statements must align with module imports and public exports.",
      "durability": "Document that serialized domain records, not native Contracts, are journal authority; native validation/mapping versions and unresolved raw labels are adapter metadata.",
      "errors": [
        "DocumentationDrift -> architecture review when imports contradict ownership",
        "NativeValidationFailure -> typed boundary failure as described by the selected provider contract",
        "ProviderCapabilityUnavailable -> unavailable rather than an invented semantic or SecType"
      ],
      "preservedBehavior": [
        "Retain the useful explanation of why permissive native defaults need validation",
        "Remove obsolete phase-status claims rather than appending contradictory notes"
      ],
      "replacementSteps": [
        "Rewrite comments alongside the adapter/domain cutover",
        "Delete the shared module once no code imports its validation API"
      ],
      "verificationCases": [
        "A runtime codec scenario can locate and exercise the IBKR codec for each currently observed semantic kind",
        "No comment claims that SDK SecType is a cross-broker identity or imposes handlers on unrelated providers",
        "The documented failure path names typed variants rather than generic throws"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source comments’ taxonomy plan remains evidence, but the target documents IBKR codec ownership and open provider schemas rather than a broker-neutral exhaustive kind list.",
      "contractAnchors": [
        "K02",
        "K03",
        "K07"
      ]
    },
    {
      "id": "MAP-8AF74BCDDF",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/contract-discipline.ts:21-28: imports Contract, UNSET_DOUBLE, and SecType from @traderalice/ibkr and re-exports SecType",
        "packages/ibkr/src/contract.ts:81-95: coerceSecType logs and drops unknown raw values to empty SecType"
      ],
      "currentBehavior": "Domain/trading directly imports the IBKR SDK and makes its SecType union available to internal callers. The native UNSET_DOUBLE sentinel is part of the shared validator's implementation, and unknown raw SecType input is erased by the SDK coercion path.",
      "problem": "The import establishes a prohibited dependency from domain to broker SDK and makes adapters depend on a shared vendor vocabulary. Re-exporting SecType prevents a clean cross-broker SPI; coercion also loses evidence needed to classify an unsupported native kind.",
      "targetModel": "Define the UTA-facing Instrument descriptor and explicit identity/context fields. Add an IBKR-local `SecTypeCodec` that imports the SDK and maps the currently observed 16-value native ADT—including BAG legs and NEWS query-only structure—while capturing unknown raw input as `UnknownNativeSecType(raw)` before lossy coercion. Other providers expose their own exact schema or a declared semantic mapping; the broker SPI carries only the selected leaf’s typed descriptor/failure.",
      "pureTransitions": [
        "Raw unknown value -> Result<IbkrSecType, NativeProtocolViolation> retaining raw evidence",
        "Each currently observed IbkrSecType variant + native fields -> Result<InstrumentDescriptor, ValidationIssue[]> through the IBKR declaration",
        "Provider-specific native extension -> provider descriptor or ProviderCapabilityUnavailable for an operation that lacks a common semantic"
      ],
      "effectBoundary": "Only brokers/ibkr may import @traderalice/ibkr. Domain validation consumes already decoded values; no SDK object, SecType string, or sentinel crosses the boundary.",
      "durability": "Journal schema stores domain kind/identity with encoding version; native SecType and unknown raw diagnostics are retained only as adapter metadata when required.",
      "errors": [
        "NativeProtocolViolation(value) -> adapter observation failure",
        "UnknownNativeSecType(raw) -> QueryFailure/ProtocolViolation with evidence",
        "ProviderCapabilityUnavailable(operation) -> unavailable capability leaf",
        "LegacyRecordDecodeFailure -> recovery stop"
      ],
      "preservedBehavior": [
        "Keep exact case-sensitive allowlist enforcement for all 16 current SecType values in the IBKR adapter",
        "Keep CRYPTO_PERP support through a domain variant and never silently turn unknown native input into an empty identity"
      ],
      "replacementSteps": [
        "Migrate all imports of SecType/validator to adapter-local codecs or domain constructors",
        "Delete the SDK import and compatibility re-export from contract-discipline.ts"
      ],
      "verificationCases": [
        "Runtime adapter boundary decodes a valid native SecType into a broker-neutral descriptor and emits no SDK object; an unknown raw value yields UnknownNativeSecType(raw)",
        "A BAG fixture preserves leg identities/ratios and CRYPTO_PERP maps to its declared crypto-perpetual semantic where supported",
        "A provider with a native-only instrument can still return its exact query descriptor without claiming an unrelated IBKR kind"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The current 16 IBKR values remain an adapter-local codec table with BAG/NEWS facts; other providers retain their own schemas and only expose common semantics when declared and evidenced.",
      "contractAnchors": [
        "K02",
        "K03",
        "K07"
      ]
    },
    {
      "id": "MAP-0093FE974C",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/contract-discipline.ts:30-36: SEC_TYPES is an IBKR SecType array with the OpenAlice CRYPTO_PERP extension",
        "packages/ibkr/src/contract.ts:75-116: SEC_TYPE_VALUES and SecType currently contain 16 closed values"
      ],
      "currentBehavior": "The array is a runtime allowlist and compile-time native type assertion for the current 16 values. It treats CRYPTO_PERP as the only intentional deviation and the current coercer drops any unknown raw string.",
      "problem": "A global allowlist cannot represent venue-specific instrument context or capability. Keeping it in domain invites non-IBKR adapters to treat native labels as canonical and invites lossy unknown handling.",
      "targetModel": "Move `IBKR_SEC_TYPES` to the IBKR adapter as a declaration-backed table for all 16 currently observed native values, preserving kind-specific fields/legs and mapping to a common Instrument semantic only when the mapping is exact. Provider-native labels and extensions stay in their own schemas; the old domain array is not a global authority and absence of a common mapping reports unavailable capability rather than forcing a fake kind.",
      "pureTransitions": [
        "Raw native string -> Result<IbkrSecType, NativeProtocolViolation> retaining unknown raw value",
        "IbkrSecType + decoded fields -> Result<InstrumentDescriptor, ValidationIssue[]> with exhaustive 16-case mapping"
      ],
      "effectBoundary": "No IO in codec conversion; adapter code only. Capability derivation consumes the resulting descriptor plus MarketContext.",
      "durability": "Record codec/mapping version with native observations and prepared plans to prevent silent reinterpretation after taxonomy changes; record unknown labels as failure evidence.",
      "errors": [
        "UnknownNativeSecType(raw) -> ProtocolViolation retaining evidence",
        "ProviderCapabilityUnavailable(operation) -> unavailable capability result",
        "MappingVersionUnsupported -> PlanVersionUnsupported/reprepare"
      ],
      "preservedBehavior": [
        "Keep all 16 listed native kinds, CRYPTO_PERP mapping, and unknown rejection",
        "Preserve BAG legs and NEWS/non-tradable query semantics rather than collapsing them into a generic symbol"
      ],
      "replacementSteps": [
        "Move array and its tests into brokers/ibkr",
        "Delete domain SEC_TYPES and make HTTP/config schemas use protocol/domain kind decoders rather than this array"
      ],
      "verificationCases": [
        "The IBKR codec covers each current value and an unknown raw string cannot pass or become empty identity",
        "CRYPTO_PERP is decoded with its declared semantics and BAG retains its legs",
        "A non-IBKR provider can return a precise native extension without mutating the IBKR table"
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The current IBKR allowlist, CRYPTO_PERP, BAG, NEWS, and unknown-raw behavior remain source-backed adapter obligations, while the old global array authority is removed.",
      "contractAnchors": [
        "K02",
        "K03",
        "K07"
      ]
    },
    {
      "id": "MAP-41CDC6BAAB",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/contract-discipline.ts:38-42: SEC_TYPE_SET and isSecType perform case-sensitive string membership against SEC_TYPES"
      ],
      "currentBehavior": "isSecType narrows unknown values only by exact string membership. It is a runtime boundary predicate but accepts no context or normalized identity.",
      "problem": "A boolean type guard erases why a value failed and is too weak as a persistence/transport decoder. Using it throughout domain code duplicates permissive unknown-string handling and bypasses typed issue payloads.",
      "targetModel": "Use `decodeIbkrSecType(value): Result<IbkrSecType, NativeProtocolViolation>` at the IBKR native boundary; preserve all currently observed values through the adapter declaration, including BAG leg structure and NEWS query-only status. Domain receives a constructed descriptor and never calls a generic string guard; another provider has its own decoder.",
      "pureTransitions": [
        "raw unknown -> Result<IbkrSecType, NativeProtocolViolation> retaining the observed value",
        "each currently observed IbkrSecType variant -> InstrumentDescriptor mapping with kind-specific field validation",
        "provider descriptor + declared capability -> availability without inferring an operation from a raw label"
      ],
      "effectBoundary": "Pure adapter codec; protocol/JSON parsing invokes it at the edge. No generic guard is exported to kernel or public protocol.",
      "durability": "Invalid values are rejected during record decode; valid domain kind is serialized with schema version rather than revalidated from a raw SecType later.",
      "errors": [
        "UnknownNativeSecType(value) -> typed protocol/observation failure",
        "LegacyKindNotMappable(version) -> replay violation",
        "ProviderCapabilityUnavailable(operation) -> explicit unavailable result"
      ],
      "preservedBehavior": [
        "Keep case-sensitive native acceptance",
        "Keep null/empty/unknown rejection, now carrying a structured reason"
      ],
      "replacementSteps": [
        "Replace isSecType use with adapter decoder and exhaustive mapping",
        "Remove SEC_TYPE_SET/isSecType from the domain module"
      ],
      "verificationCases": [
        "`crypto`, `FUTURES`, empty, and null each produce a non-success variant",
        "A valid STK maps to one descriptor under the IBKR declaration",
        "No application caller branches on a raw boolean to decide trade capability"
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "Case-sensitive acceptance and explicit rejection of empty/unknown IBKR values remain, but the guard becomes an adapter decoder and cannot decide capability for another provider.",
      "contractAnchors": [
        "K02",
        "K03",
        "K07"
      ]
    },
    {
      "id": "MAP-54ECEAEE5E",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/contract-discipline.ts:46-51: ContractRequirements universal keys are SDK Contract keys and bySecType is a partial map of SDK keys"
      ],
      "currentBehavior": "The requirement bag lists symbol/secType/exchange/currency universally and allows optional per-SecType arrays of keyof Contract. It has no identity, venue, market, account, or jurisdiction relation.",
      "problem": "`keyof Contract` makes SDK field names the validation model and permits missing entries for kinds that may have context-specific requirements. It cannot express units, canonical identity, or capability conditions.",
      "targetModel": "Replace the `keyof Contract` requirement bag with declaration-backed native schemas and small semantic Instrument descriptor constructors. Each provider retains its own required fields, including the currently observed IBKR derivative and BAG rules; domain records require identity/context and `Public | AccountScope` only where the chosen capability needs them. No global per-SecType requirement map is carried into the domain.",
      "pureTransitions": [
        "Native provider schema + declared semantic descriptor -> Result<InstrumentDescriptor, ValidationIssue[]>",
        "InstrumentDescriptor + selected capability context -> ActionAvailability/Precondition; a provider-native field with no common operation remains an exact extension"
      ],
      "effectBoundary": "Native field requirements are adapter-local. Domain constructor and capability decision are pure over supplied facts; application obtains venue/account/session facts.",
      "durability": "Persist canonical descriptor/context and capability fingerprint, not a dynamic list of Contract keys. Schema versions make requirement changes explicit.",
      "errors": [
        "MissingRequiredNativeField -> selected adapter ValidationFailed",
        "IdentityRelationViolation -> domain ValidationFailed",
        "CapabilityUnavailable -> application admission rejection"
      ],
      "preservedBehavior": [
        "Keep universal symbol/exchange/currency checks where they map to a broker-neutral descriptor",
        "Keep derivative-specific structural checks, but do not use the generic requirement bag as domain authority"
      ],
      "replacementSteps": [
        "Define provider schemas and semantic constructors",
        "Migrate buildContract/validateContract and protocol decoders, then delete ContractRequirements"
      ],
      "verificationCases": [
        "A future requires expiry/multiplier by its variant, while an option also requires strike/right",
        "A descriptor without venue/listing jurisdiction cannot be executable",
        "An unrelated SDK field cannot satisfy a domain identity requirement"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The field-bag defect is addressed with schema-first provider declarations and small semantic constructors; no global per-SecType requirement map is carried into the domain.",
      "contractAnchors": [
        "K02",
        "K03",
        "K04",
        "K07"
      ]
    },
    {
      "id": "MAP-BE2FC8EECC",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/contract-discipline.ts:53-62: SECTYPE_REQUIREMENTS requires universal fields for every kind, OPT/FOP four derivative fields, FUT expiry/multiplier, and defaults other kinds to multiplier 1 downstream"
      ],
      "currentBehavior": "Validation applies static structural rules keyed by native SecType. Non-derivatives are accepted with only universal fields and later get multiplier '1'. No venue/account/session facts are consulted.",
      "problem": "Static structure is necessary but not sufficient for action availability. A multiplier default can be correct for STK and catastrophically wrong for a misdecoded derivative; no typed distinction exists.",
      "targetModel": "Retain the native rule table inside each adapter and preserve the currently observed IBKR multiplier rules as explicit descriptor constraints; a provider may declare its own precise multiplier policy. BAG preserves legs and NEWS is query-only where the IBKR schema says so. Derive `ActionAvailability` from context independently.",
      "pureTransitions": [
        "Native row + provider declaration -> Result<domain descriptor, ValidationIssue[]> with the provider’s multiplier policy",
        "Domain descriptor + context + selected action -> Available | Unavailable(reason)",
        "Unknown/missing multiplier -> typed invalid observation for derivative data; unknown native kind remains provider protocol evidence rather than a fabricated common kind"
      ],
      "effectBoundary": "Adapters parse native fields; application queries capabilities/session/account; domain evaluates supplied context without IO.",
      "durability": "Persist validated multiplier and instrument kind in snapshots/plans; capability fingerprint and freshness are part of preparation.",
      "errors": [
        "MissingDerivativeField -> ValidationFailed",
        "InvalidMultiplierPolicy -> ObservationFailure",
        "SessionClosed/AccountReadOnly -> CapabilityUnavailable"
      ],
      "preservedBehavior": [
        "Keep OPT/FOP/FUT structural requirements",
        "Keep unit multiplier default only for non-derivative kinds that explicitly permit it"
      ],
      "replacementSteps": [
        "Move SECTYPE_REQUIREMENTS into native codecs and define domain variants",
        "Add capability/context derivation and migrate all broker output paths"
      ],
      "verificationCases": [
        "An OPT with missing multiplier is rejected rather than defaulted to 1",
        "A complete STK can derive with multiplier 1",
        "A structurally valid future in an unavailable venue session cannot be prepared for PlaceOrder"
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "Derivative multiplier guards and non-derivative unit-one policy remain explicit where the provider declares them; availability is a separate capability result and data observations remain usable.",
      "contractAnchors": [
        "K03",
        "K04",
        "K05",
        "K07"
      ]
    },
    {
      "id": "MAP-880959554F",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/contract-discipline.ts:64-66: ValidationResult failure is `errors: string[]`"
      ],
      "currentBehavior": "Validation can aggregate multiple issues, but each issue is an unstructured string. Callers must inspect or concatenate messages to distinguish missing fields, unknown kinds, and invalid values.",
      "problem": "String arrays cannot carry field type, observed value, instrument context, or stable protocol codes. They also make expected failure indistinguishable from a defect when passed through generic Error.",
      "targetModel": "Define a bounded `ValidationIssue` ADT such as `MissingField`, `UnknownKind`, `InvalidValue`, `InvalidRelationship`, and `UnsupportedContext`, each carrying typed field/context payloads. `Result` preserves all issues without flattening; provider leaves may extend the error schema explicitly.",
      "pureTransitions": [
        "Native/domain input -> Result<ValidValue, NonEmptyReadonlyArray<ValidationIssue>>",
        "ValidationIssue[] -> named protocol failure schema with stable code and redacted observed data"
      ],
      "effectBoundary": "Pure validation. Transport and application map variants to status/presentation; runtime logging uses redacted encoders and does not parse messages.",
      "durability": "If persisted, issue codes/payloads are versioned. Ordinary rejected commands do not become journal events unless the transaction protocol explicitly records a rejection receipt.",
      "errors": [
        "MissingField(field) -> caller supplies the field or rejects request",
        "InvalidValue(field, reason) -> typed 400/observation failure",
        "UnsupportedContext(kind, context) -> CapabilityUnavailable"
      ],
      "preservedBehavior": [
        "Aggregate multiple errors so callers can repair a malformed record in one response",
        "Keep field-specific information currently embedded in messages"
      ],
      "replacementSteps": [
        "Introduce issue constructors/codecs and migrate validator/builders/tests",
        "Remove string[] and all callers that parse joined Error messages"
      ],
      "verificationCases": [
        "Invalid OPT returns four tagged missing-field issues",
        "Unknown BANANA carries observed value but protocol redacts sensitive fields",
        "HTTP/application mapping is stable even if human wording changes"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "A bounded ValidationIssue ADT remains appropriate for boundary safety, but it is a local error schema composed with each provider leaf rather than a universal contract-error catalogue.",
      "contractAnchors": [
        "K03",
        "K07",
        "K11"
      ]
    },
    {
      "id": "MAP-933E16B57D",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/contract-discipline.ts:68-100: validateContract mutates no state, checks truthy universal fields, SecType membership, static fields, and option rights"
      ],
      "currentBehavior": "validateContract accepts a mutable IBKR Contract, checks required field presence via hasContractField, and returns either ok or string errors. It does not validate canonical identity, venue/jurisdiction, market session, account scope, or capability.",
      "problem": "The API is tied to a concrete SDK and static field truthiness. It can declare a syntactically complete native contract valid even when it has no stable identity or cannot be used in the requested account/session.",
      "targetModel": "Replace with two decoders: each adapter’s `decodeNativeContract(raw): Result<InstrumentDescriptor, ValidationIssue[]>` preserves its exact native schema, while the IBKR decoder handles the currently observed SecType values and BAG/derivative fields. Domain `validateActionContext(descriptor, context, selectedCapability)` is separate; public descriptors stay public, account-bound actions require explicit AccountScope, no mutable SDK object enters domain, and a brand alone does not prove runtime identity uniqueness.",
      "pureTransitions": [
        "unknown native record -> Result<InstrumentDescriptor, ValidationIssue[]> with raw provider evidence before lossy coercion",
        "InstrumentDescriptor + evidence-backed identity mapping + selected capability context -> Result<ResolvedInstrument, PreconditionFailure>; unresolved/conflicting mapping cannot produce an effect"
      ],
      "effectBoundary": "Native decode is adapter-local and pure over response data; identity lookup/session/capability facts are application/broker ports. The kernel receives resolved typed values.",
      "durability": "Persist decoded descriptor, context evidence/version, and validation schema version. Invalid persisted records stop replay rather than defaulting.",
      "errors": [
        "ValidationFailed(issues) -> caller-facing expected rejection",
        "ProtocolViolation(nativeField) -> selected adapter observation failure",
        "CapabilityUnavailable(reason) -> admission rejection for that capability only"
      ],
      "preservedBehavior": [
        "Keep no mutation and all current structural checks",
        "Intentionally add identity/context checks at the correct boundary rather than treating field presence as executability"
      ],
      "replacementSteps": [
        "Implement adapter schemas and domain validators, migrate build/search/position callers",
        "Delete validateContract from domain/trading after all imports are removed"
      ],
      "verificationCases": [
        "A valid-looking native record without canonical ID cannot produce an execution effect",
        "Invalid option right and missing exchange produce distinct issues in the IBKR decoder",
        "A read-only account can decode/query an instrument while action validation rejects mutation"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "Structural native decode, identity resolution, and action availability are kept separate; only an execution-capable leaf enters the transaction wrapper and read/query leaves remain independent.",
      "contractAnchors": [
        "K02",
        "K03",
        "K07",
        "K08"
      ]
    },
    {
      "id": "MAP-7B846115CB",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/contract-discipline.ts:102-112: assertContract calls validateContract and throws `new Error` with joined errors"
      ],
      "currentBehavior": "All validation failures are converted to a synchronous generic exception with human-readable text. This API is wired into buildContract and indirectly into adapter search/position paths.",
      "problem": "Expected malformed native input becomes an untyped exception; callers cannot decide retry, source degradation, user correction, or recovery by variant. Joined text is not a durable protocol.",
      "targetModel": "Delete assertContract from the domain path. Native codecs return `Result`; application maps expected validation to `ValidationFailed`/`ProtocolViolation`, while only impossible internal invariant failures may throw under supervised runtime policy.",
      "pureTransitions": [
        "Validation result -> Either/Result without throwing",
        "Expected failure -> protocol/journal failure envelope via exhaustive mapping"
      ],
      "effectBoundary": "No IO in assertion/decoding. Effect boundary handles logging/supervision for defects only; it does not catch-and-stringify expected validation.",
      "durability": "Failed preparation may create a typed command receipt; no native object or message is stored as transaction state. Replay uses versioned valid records only.",
      "errors": [
        "ValidationFailed(issues) -> caller fixes input",
        "ProtocolViolation -> source/adapter degraded or recovery stop",
        "InvariantDefect -> supervised defect with correlation metadata"
      ],
      "preservedBehavior": [
        "Stop invalid contracts before dispatch",
        "Continue aggregating all ordinary validation issues"
      ],
      "replacementSteps": [
        "Migrate buildContract and any direct callers to typed Results",
        "Remove assertContract and update tests/application error mappers"
      ],
      "verificationCases": [
        "Invalid OPT reaches HTTP/protocol as structured field failures, not Error.message",
        "Broker row corruption does not get retried as a valid order",
        "A genuine impossible internal state remains distinguishable from malformed user/native data"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The source’s throw-to-string defect is replaced by typed leaf errors and supervised defects, preserving expected validation semantics without requiring transaction machinery for data decoding.",
      "contractAnchors": [
        "K03",
        "K07",
        "K11"
      ]
    },
    {
      "id": "MAP-BA4B6EEB3B",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/contract-discipline.ts:114-124: hasContractField treats empty strings, UNSET_DOUBLE, and nullish values as absent for a dynamic keyof Contract",
        "packages/ibkr/src/contract.ts:81-95: coerceSecType logs an unknown raw SecType and returns empty string, so raw evidence must be captured before coercion"
      ],
      "currentBehavior": "The helper uses SDK sentinel semantics to decide field presence. It dynamically indexes any Contract key and returns a boolean with no field-specific error or context.",
      "problem": "Sentinel interpretation is only valid where the exact native SDK contract is known; exporting it from domain encourages other brokers to copy IBKR assumptions. A boolean loses whether a field was absent, malformed, or intentionally zero.",
      "targetModel": "Keep `hasNativeIbkrField` private to the IBKR schema decoder and return typed `Present<T> | Absent | Invalid` values. Domain constructors receive refined values, including legitimate zero where allowed.",
      "pureTransitions": [
        "Native field + field codec -> NativeFieldState",
        "NativeFieldState -> Result<refined domain field, ValidationIssue>"
      ],
      "effectBoundary": "Pure native parsing, no IO. Only IBKR adapter code knows UNSET_DOUBLE; protocol schemas use explicit optional/absent encodings.",
      "durability": "Persist refined field values and explicit absence; never re-run a generic keyof Contract sentinel test during journal replay.",
      "errors": [
        "UnexpectedSentinel(field) -> NativeProtocolViolation",
        "InvalidNumericField(field, raw) -> ValidationFailed",
        "AbsentRequiredField(field) -> MissingField"
      ],
      "preservedBehavior": [
        "Treat IBKR empty string and UNSET_DOUBLE as absent for required native fields",
        "Do not classify a legitimate zero as absent when the domain field permits zero"
      ],
      "replacementSteps": [
        "Move helper into brokers/ibkr native codecs and replace boolean checks with field decoders",
        "Delete generic keyof Contract access from domain/trading"
      ],
      "verificationCases": [
        "IBKR strike UNSET_DOUBLE is MissingField while strike 0 follows the option domain's explicit validity policy",
        "An empty native multiplier is absent for derivatives",
        "A CCXT numeric zero is not interpreted using IBKR's sentinel"
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "IBKR sentinel presence and legitimate-zero distinctions remain exact native evidence, but the helper is private to that codec and cannot be reused as a cross-provider field test.",
      "contractAnchors": [
        "K03",
        "K07"
      ]
    },
    {
      "id": "MAP-C6D66D3913",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/contract-ext.ts:1-7: file only side-effect imports @traderalice/uta-protocol to augment IBKR Contract with optional aliceId"
      ],
      "currentBehavior": "Importing this shim activates a declaration merge; no runtime identity construction or validation occurs. Multiple adapter modules import it solely to make `contract.aliceId` type-check.",
      "problem": "Attaching a fork identity to a mutable SDK object conflates display/native symbol with canonical identity and relies on ambient side effects. The optional field can be absent, stale, or copied across venues without proof.",
      "targetModel": "Delete declaration merging and represent canonical identity as branded `InstrumentId` in `InstrumentDescriptor`; keep broker native key in a per-adapter `NativeInstrumentKey` mapping. Protocol search/order schemas carry the explicit identity field.",
      "pureTransitions": [
        "Native key + venue/listing context + optional account scope when account-bound -> Result<InstrumentId, IdentityMappingFailure>",
        "InstrumentId -> native key only through the owning adapter’s resolved catalog mapping"
      ],
      "effectBoundary": "Native key lookup may use broker/catalog IO in application; the mapping constructor is pure over returned facts. No SDK object mutation or side-effect import.",
      "durability": "Persist InstrumentId and native key mapping with venue/adapter version; never depend on declaration merge fields during replay.",
      "errors": [
        "NativeIdentityUnresolved -> QueryFailure/PreconditionFailed",
        "AmbiguousNativeIdentity -> require explicit selection",
        "CrossVenueIdentityMismatch -> ProtocolViolation"
      ],
      "preservedBehavior": [
        "Search results still provide a stable downstream identifier",
        "Native Contract fields remain available inside adapter implementation until each caller migrates"
      ],
      "replacementSteps": [
        "Migrate all contract.aliceId readers/writers to explicit InstrumentId fields",
        "Remove shim imports from adapters and delete contract-ext.ts after TypeScript no longer needs augmentation"
      ],
      "verificationCases": [
        "Two brokers' BTC symbols do not compare equal merely because their display text matches",
        "A selected search result carries InstrumentId without mutating an SDK Contract",
        "Missing native key mapping blocks execution rather than producing undefined identity"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The ambient aliceId declaration merge is removed in favor of an explicit identity field and provider alias mapping, while native keys remain exact adapter metadata.",
      "contractAnchors": [
        "K02",
        "K03",
        "K07"
      ]
    },
    {
      "id": "MAP-CF1FF8D3C3",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/contract-search-rules.spec.ts:4-39: crypto/currency tests trim/case-normalize, prefer USDT/USDC before USD, preserve unknown BTC quote, enforce two-character base floor, and leave bare bases unchanged"
      ],
      "currentBehavior": "The tests specify the heuristic bridge from concatenated vendor symbols to broker patterns: BTCUSD→BTC, SOLUSDT→SOL, unknown ETHBTC unchanged, and LUSD protected from stripping. Longer suffixes must win.",
      "problem": "The heuristic is useful but can be mistaken for identity resolution. A pure string result has no context saying it is only a search hint, and suffix tables can create false positives if expanded without observed evidence.",
      "targetModel": "Define `InstrumentSearchPattern { rawVendorSymbol, normalizedHint, assetClassHint, normalizationRuleVersion }`; the normalized hint is never an InstrumentId. Broker application resolves hits into canonical descriptors and reports no-match/ambiguous results explicitly.",
      "pureTransitions": [
        "Raw vendor symbol + AssetClassHint -> Result<InstrumentSearchPattern, QueryFailure>: trim/case and longest-known suffix with base-length floor",
        "Search pattern -> broker query request, preserving raw symbol for audit"
      ],
      "effectBoundary": "Normalization is pure. Broker search, identity resolution, and account/venue selection happen in application/broker-spi services.",
      "durability": "Persist raw query and normalization version only if audit/provenance requires it; never persist normalized text as canonical instrument identity.",
      "errors": [
        "EmptyQuery -> typed QueryFailure/no broad sweep",
        "AmbiguousNormalization -> preserve raw hint and require resolver selection",
        "NoMatch -> explicit query result, not identity construction"
      ],
      "preservedBehavior": [
        "Retain longest suffix matching, uppercase result, unknown quote pass-through, and two-character floor",
        "Retain the rule that data-vendor symbol and trading identity are different namespaces"
      ],
      "replacementSteps": [
        "Move implementation to a domain/query module with named hint type",
        "Update AI/HTTP/UI callers and resolver to consume hint plus raw provenance"
      ],
      "verificationCases": [
        "SOLUSDT strips USDT rather than USD; ETHBTC stays unchanged",
        "LUSD remains intact",
        "A normalized BTC hint returning a BYBIT and Coinbase hit leaves both distinct InstrumentIds",
        "Raw vendor symbol is preserved alongside the hint in a query response"
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "The observed suffix precedence, unknown-quote pass-through, base-length floor, and raw-query provenance remain pure search behavior and never become an identity or order authority.",
      "contractAnchors": [
        "K03",
        "K04",
        "K05",
        "K06"
      ]
    },
    {
      "id": "MAP-F61189D796",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/contract-search-rules.spec.ts:41-54: equity and commodity inputs pass through unchanged, including equity-looking EURUSD"
      ],
      "currentBehavior": "The normalizer does no crypto suffix stripping for equity/commodity hints; it preserves case/content according to the implementation's trim/normalization policy, and the test protects EURUSD as a ticker.",
      "problem": "Pass-through is correct as a heuristic but does not imply that a vendor ticker equals a listing identity. The current test does not require the resolved hit to carry venue/listing jurisdiction.",
      "targetModel": "Return an identity-neutral `InstrumentSearchPattern` with `normalizationRule = IdentityPassThrough`; resolver output must contain `InstrumentId`, `VenueId`, listing jurisdiction, and display symbol separately.",
      "pureTransitions": [
        "Vendor symbol + equity/commodity hint -> pass-through search pattern",
        "Broker hit -> Result<InstrumentDescriptor, IdentityResolutionFailure> with canonical identity"
      ],
      "effectBoundary": "Pass-through normalization is pure. Resolver and catalog lookup are application/broker IO; protocol encodes raw and resolved fields separately.",
      "durability": "Catalog projection persists canonical listing identity, not the vendor query. Raw search provenance may be retained as non-authoritative metadata.",
      "errors": [
        "IdentityResolutionFailure -> no executable result",
        "AmbiguousListing(symbol, venues) -> explicit selection required",
        "UnsupportedAssetClass -> QueryFailure"
      ],
      "preservedBehavior": [
        "Keep AAPL and commodity gold query strings unchanged",
        "Keep EURUSD equity behavior and do not add speculative FX stripping"
      ],
      "replacementSteps": [
        "Retain pure pass-through cases in the new normalizer tests",
        "Add resolver output assertions and remove any caller that treats the query itself as InstrumentId"
      ],
      "verificationCases": [
        "EURUSD under equity context is not rewritten to EUR",
        "AAPL hits on two venues carry distinct listing contexts",
        "A commodity query returns a descriptor only after broker resolution"
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "Equity/commodity pass-through, including EURUSD, remains unchanged as a search hint; resolution supplies identity and listing evidence later.",
      "contractAnchors": [
        "K03",
        "K04",
        "K05"
      ]
    },
    {
      "id": "MAP-D9BFDA8A15",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/contract-search-rules.spec.ts:56-64: omitted or unknown asset-class hints pass BTCUSD through without stripping"
      ],
      "currentBehavior": "When the caller does not know the asset class, the normalizer conservatively returns the input rather than guessing crypto/currency. The test uses the same pass-through for an explicit unknown value.",
      "problem": "The string result does not communicate that asset-class context remains unresolved. Downstream callers could accidentally execute against a first match or silently treat no match as a broker failure.",
      "targetModel": "Use `AssetClassContext = Known(kind) | Unknown(reason)` in `InstrumentSearchPattern`; unknown context yields an identity-preserving hint and requires application-level result classification (`NoMatch`, `Ambiguous`, `SourceUnavailable`).",
      "pureTransitions": [
        "Raw symbol + Unknown context -> pass-through hint with unresolved context",
        "Hint + broker results -> typed query result; never infer canonical kind from the string alone"
      ],
      "effectBoundary": "Only the resolver/broker can supply authoritative kind/venue facts. Application may ask multiple sources but returns explicit source/context metadata.",
      "durability": "Store unresolved context as query metadata if needed; prepared plans require resolved kind/context and cannot use an unknown hint directly.",
      "errors": [
        "UnresolvedAssetClassForExecution -> PreconditionFailed",
        "NoMatch -> query response variant",
        "SourceUnavailable -> degraded-source result"
      ],
      "preservedBehavior": [
        "Keep conservative BTCUSD pass-through for unknown/omitted hints",
        "Do not add speculative suffix stripping"
      ],
      "replacementSteps": [
        "Add explicit unknown context to request schema and resolver result",
        "Migrate AI/HTTP/UI consumers from raw string assumptions"
      ],
      "verificationCases": [
        "Unknown BTCUSD is sent as a literal search hint and is not converted into BTC InstrumentId",
        "A query can return no match without being reported as invalid input",
        "Execution admission refuses a plan whose asset context is unresolved"
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "Conservative pass-through for unknown asset context remains required, while the result now names unresolved context and cannot be promoted to an executable capability.",
      "contractAnchors": [
        "K03",
        "K04",
        "K05",
        "K06"
      ]
    },
    {
      "id": "MAP-6CEBFDFFDD",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/contract-search-rules.spec.ts:66-74: empty input remains empty and surrounding whitespace is trimmed before crypto normalization"
      ],
      "currentBehavior": "An empty string returns empty; surrounding whitespace is removed before suffix handling. searchTradeableContracts then maps an empty normalized pattern to an empty result.",
      "problem": "At the pure helper level empty is harmless, but a protocol/application endpoint needs to distinguish an intentional empty query from a broad sweep and report it as a typed query outcome rather than silently hiding a request error.",
      "targetModel": "Parse `SearchRequest` into `NonEmptySearchText` or `EmptySearchQuery`; only the latter returns a named `QueryFailure.Empty`/empty-result policy at the protocol boundary. Keep trim and normalization deterministic.",
      "pureTransitions": [
        "Raw text -> Result<SearchText, QueryFailure.Empty>",
        "Non-empty SearchText + asset context -> InstrumentSearchPattern"
      ],
      "effectBoundary": "No IO in parser. HTTP/AI protocol validates the request; application decides whether empty is a user-facing empty page or a 400, never a broker sweep.",
      "durability": "No persistence is required for an empty query; if the caller audits requests, it records the explicit empty outcome and normalization version, never a transaction or provider stream.",
      "errors": [
        "EmptySearchQuery -> explicit QueryFailure with caller correction",
        "PatternNormalizationFailure -> protocol validation failure"
      ],
      "preservedBehavior": [
        "Keep trimming and empty output semantics for pure helper compatibility",
        "Prevent empty pattern from invoking any broker search"
      ],
      "replacementSteps": [
        "Move parser into the named query request codec",
        "Update HTTP/AI tests to assert explicit empty-query response and delete the compatibility barrel after migration"
      ],
      "verificationCases": [
        "`  BTCUSD  ` becomes BTC under crypto context",
        "Whitespace-only request never calls a broker",
        "An empty query response includes no false `utasConfigured`/source success claim"
      ],
      "openQuestions": [],
      "contractDisposition": "retained",
      "contractRationale": "Trim and empty-input behavior remains deterministic, but an empty query is an explicit finite query outcome and never starts a provider stream or transaction.",
      "contractAnchors": [
        "K03",
        "K05",
        "K06"
      ]
    },
    {
      "id": "MAP-9890D2522D",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/contract-search-rules.ts:1-7: file is only an export* compatibility shim to @traderalice/uta-protocol"
      ],
      "currentBehavior": "The file defines no search logic; it preserves relative imports under domain/trading by re-exporting every symbol from the protocol package.",
      "problem": "The barrel obscures ownership and encourages business normalization to live in the shared protocol package. It also makes a future domain/protocol dependency cycle likely.",
      "targetModel": "Place the pure search-pattern constructor in domain/application-owned code and define only serializable request/response codecs in protocol. Export a deliberate named public surface rather than export*.",
      "pureTransitions": [
        "No runtime behavior; import migration must preserve the one pure normalizer",
        "Protocol request -> domain SearchPattern through an explicit codec"
      ],
      "effectBoundary": "No IO or side effect should occur on import. Application service calls the normalizer; protocol package contains serializable schemas only.",
      "durability": "No persistence owner; the target query result/projection schemas own versioned encoding.",
      "errors": [
        "ProtocolDecodeFailure -> caller-facing query failure",
        "ImportBoundaryViolation -> build/dependency check failure"
      ],
      "preservedBehavior": [
        "Relative callers continue to receive the same deterministic normalization while migration is in progress",
        "No new normalization heuristics are introduced by deleting the shim"
      ],
      "replacementSteps": [
        "Migrate all imports to the target domain/query helper and protocol codec",
        "Delete this barrel only after grep confirms no relative import remains"
      ],
      "verificationCases": [
        "An AI and HTTP request through the application query service produce the same normalized hint and source-status envelope",
        "A runtime import/use of the target normalizer performs no protocol business or SDK allocation, while the protocol codec still round-trips the named serializable fields",
        "Deleting the shim leaves no second normalization implementation or changed suffix behavior"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The compatibility barrel remains only historical import evidence; the target exposes named schema/normalizer leaves through composition and keeps protocol codecs serializable.",
      "contractAnchors": [
        "K01",
        "K03",
        "K06"
      ]
    },
    {
      "id": "MAP-30D95BD47F",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/contract-search.spec.ts:9-14: makeDesc creates ContractDescription, fills native Contract via makeContract, attaches aliceId/symbol, and initializes derivativeSecTypes"
      ],
      "currentBehavior": "The fixture models a search hit as an SDK ContractDescription whose Contract carries augmented aliceId and display symbol; derivativeSecTypes is a raw string array.",
      "problem": "The fixture bakes the prohibited identity augmentation and native object into the public search result. It cannot test canonical InstrumentId versus display symbol or typed derivative metadata.",
      "targetModel": "Use `InstrumentSearchHit { descriptor: InstrumentDescriptor, providerMetadata }`, with canonical identity, display fields, scope, venue, listing jurisdiction, derivative metadata, and capability evidence owned by the descriptor or exact provider extension rather than duplicated fields or a global kind/capability list. The descriptor ID is constructed from evidence-backed catalog/native-key facts and a durable uniqueness check; display symbol is never a substitute for scope+identity. Native Contract construction remains in broker codec fixtures.",
      "pureTransitions": [
        "Decoded native search row -> Result<InstrumentSearchHit, CatalogDecodeFailure>",
        "Search hit -> protocol safe view without SDK object or augmented fields"
      ],
      "effectBoundary": "Native adapters decode broker rows; application/query code supplies source scope, identity evidence, and protocol projection. The pure fixture/domain hit constructor performs no IO.",
      "durability": "Search hits are read projections; catalog identity/native alias mapping is durable, while result pages include snapshot/as-of metadata.",
      "errors": [
        "MissingInstrumentId -> CatalogDecodeFailure",
        "DerivativeMetadataInvalid -> ProtocolViolation",
        "SourceScopeUnavailable -> degraded source"
      ],
      "preservedBehavior": [
        "Keep source and derivative information visible to callers",
        "Keep makeDesc-like deterministic test data, but in a broker-neutral shape"
      ],
      "replacementSteps": [
        "Replace ContractDescription fixture with decoded broker-neutral hits",
        "Update MockBroker/search consumers and remove contract-ext dependency from this test"
      ],
      "verificationCases": [
        "A hit retains canonical identity even if display symbol changes",
        "Provider-declared derivative metadata remains typed and visible without becoming a global derivative-kind taxonomy",
        "HTTP/AI output serializes no SDK fields"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The deterministic fixture remains, but search hits are composed from canonical identity evidence, descriptor extensions, and source scope rather than a ContractDescription with aliceId.",
      "contractAnchors": [
        "K02",
        "K03",
        "K04",
        "K05",
        "K07"
      ]
    },
    {
      "id": "MAP-45D5DE1B9D",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/contract-search.spec.ts:17-30: default aggregate includes enabled UTA, skips asVendor:false UTA, and never calls the excluded broker"
      ],
      "currentBehavior": "Default search resolves all manager UTAs then filters by a legacy boolean `asVendor !== false`; failures and result ordering are tested only by source inclusion.",
      "problem": "Participation is not a typed capability/authorization decision and no account scope, market context, identity deduplication, ranking, limit, or degraded-source failure is observable. A boolean flag can hide a configured but unavailable source.",
      "targetModel": "Application query accepts `AccountSelector`/source policy only for account-bound fan-out and resolves each selected account’s `AccountScope`, `CapabilitySet`, market context, read permission, and explicit source precedence. Public catalog queries use `Public` scope without inventing an account. It returns `CatalogSearchPage` with hits, deterministic ranking/dedup, `totalMatched: Known | Unknown`, and per-source `SourceStatus`; dedup is allowed only after runtime identity evidence, while conflicts are surfaced.",
      "pureTransitions": [
        "Resolved source facts + query -> inclusion decision `Included | Excluded(reason)` with source ordinal",
        "Decoded source hits -> aggregate rank/dedup/page result using evidence-backed identity mappings and explicit completeness"
      ],
      "effectBoundary": "Application owns account authorization, source fan-out, catalog IO, and protocol encoding; pure aggregation consumes resolved source facts and evidence-backed hits.",
      "durability": "Catalog identity/alias mappings and source snapshot metadata are projection-owned; query pages are read-only and carry snapshot/as-of/completeness rather than journal mutations.",
      "errors": [
        "CapabilityUnavailable(source, reason) -> source status, not silent drop",
        "QueryFailure -> whole request failure only for malformed request",
        "CatalogDecodeFailure(source) -> degraded source with unaffected valid hits"
      ],
      "preservedBehavior": [
        "Default search still excludes accounts explicitly marked non-participating for vendor search",
        "Retain concurrent fan-out and avoid invoking excluded sources"
      ],
      "replacementSteps": [
        "Replace asVendor filter with typed source participation/capability policy",
        "Add aggregation assertions for identity, dedup, rank, limit, and source failures"
      ],
      "verificationCases": [
        "A disabled source is not called and is represented as an intentional policy exclusion",
        "A readable keyless source contributes hits but cannot authorize an order",
        "Two sources return the same canonical ID and aggregate output follows the declared source precedence"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "Legacy asVendor participation is replaced by resolved read capability and policy; default fan-out remains a data query and does not grant execution authority.",
      "contractAnchors": [
        "K02",
        "K05",
        "K06",
        "K09"
      ]
    },
    {
      "id": "MAP-6C47FFB1F4",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/contract-search.spec.ts:32-42: explicit raw source `disabled` bypasses asVendor filtering and invokes that broker"
      ],
      "currentBehavior": "Supplying a raw source string causes manager.resolve(source) to target that account even when its legacy asVendor flag is false. The source id is unbranded and no authorization is tested.",
      "problem": "A caller can bypass default participation with an arbitrary string and potentially access a source without account-level read authorization. Read override must not imply transaction authority.",
      "targetModel": "Decode an explicit account source as branded `AccountId`/`ConnectionId` or a permitted account selector, authorize it for the caller, resolve full `AccountScope`, then query only read capabilities. Public sources remain `Public` scope. Return source-specific failures and never construct executable plans from a hit alone.",
      "pureTransitions": [
        "Authenticated principal + source selector -> Result<AuthorizedQueryTarget, QueryFailure>",
        "Authorized target + decoded hits -> scoped InstrumentSearchHit[]"
      ],
      "effectBoundary": "Transport authenticates and application authorizes; broker adapter performs search IO. Domain query/ranking remains pure.",
      "durability": "Read authorization and source selector may be recorded in audit logs if required; no transaction mutation. Result carries account scope/provenance for later explicit selection.",
      "errors": [
        "InvalidAccountId -> QueryFailure",
        "ReadAuthorizationDenied -> QueryFailure",
        "CapabilityUnavailable -> source failure",
        "ReadOnlySourceCannotTrade -> CapabilityUnavailable at later action admission"
      ],
      "preservedBehavior": [
        "Explicit source search can intentionally query a non-default source",
        "The source is invoked exactly once when authorized"
      ],
      "replacementSteps": [
        "Replace raw source with authenticated nominal selector and scope resolution",
        "Add tests proving read override cannot bypass keyless/trade capability restrictions"
      ],
      "verificationCases": [
        "Explicit disabled-but-readable account returns search hits when authorized",
        "Unknown source returns typed QueryFailure without broker call",
        "A returned hit cannot be passed as an order identity without a separate action capability check"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "Explicit source selection remains possible, but it is decoded and authorized as a scoped read target; a query leaf cannot silently acquire an order effect.",
      "contractAnchors": [
        "K02",
        "K05",
        "K06",
        "K07"
      ]
    },
    {
      "id": "MAP-97CB3270A6",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/contract-search.ts:1-14: comments claim shared AI/HTTP shape and say contract.aliceId is canonical while callers have separate fan-out implementations"
      ],
      "currentBehavior": "The comments describe a single trading identity layer, but they are not enforcement: protocol output still carries an SDK Contract and AI has separate search behavior. aliceId augmentation is treated as the downstream key.",
      "problem": "Documentation promises a shared boundary that the implementation does not provide, and it names an SDK-attached field as canonical identity. This permits symbol/native ID confusion across surfaces.",
      "targetModel": "Define one application/account-query service with a named request/result schema whose hit scope is `Public | AccountScope`. Search hints are untrusted; adapters resolve `InstrumentId` plus descriptor/context through durable, uniqueness-checked mappings; AI and HTTP consume the same safe query response without fabricating account ownership for public instruments.",
      "pureTransitions": [
        "SearchRequest + resolved sources -> shared `CatalogSearchPage` contract",
        "Catalog hit + identity mapping evidence -> `InstrumentSearchHit` or `IdentityResolutionFailure`"
      ],
      "effectBoundary": "Application/account-query service owns authorization and broker fan-out; AI/HTTP are transport consumers of the same serializable response, while domain normalization/ranking remains pure.",
      "durability": "Catalog projection is the read authority and carries sequence/as-of/freshness. Search itself does not mutate transaction journal.",
      "errors": [
        "QueryFailure -> transport query failure schema",
        "IdentityResolutionFailure -> hit-level/source-level error",
        "ProtocolViolation -> reject malformed native/protocol data"
      ],
      "preservedBehavior": [
        "AI and HTTP should continue showing the same logical search results",
        "Preserve separation between data-vendor search hints and trading identities"
      ],
      "replacementSteps": [
        "Replace prose with an executable application contract and migrate AI/HTTP/UI callers",
        "Remove aliceId/SDK references from public result types"
      ],
      "verificationCases": [
        "Same request through AI and HTTP produces identical canonical result fields",
        "A vendor symbol never appears as InstrumentId without adapter evidence",
        "Protocol output contains no Contract instance"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The desired shared AI/HTTP shape is kept as one composed query contract, while canonical identity comes from evidence-backed mappings rather than the source comment’s SDK-attached aliceId.",
      "contractAnchors": [
        "K01",
        "K02",
        "K05",
        "K06"
      ]
    },
    {
      "id": "MAP-75A64813DC",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/contract-search.ts:16-25: module imports UTAManager, legacy AssetClassHint, and re-exports protocol ContractSearchHit whose contract field is an IBKR ContractDescription contract"
      ],
      "currentBehavior": "Domain code depends on UTAManager and a shared protocol hit type that embeds a concrete IBKR SDK contract. This couples orchestration, protocol shape, and legacy manager in one module.",
      "problem": "The target dependency direction forbids domain->manager/protocol SDK leakage. The shared type is not serializable/broker-neutral, and callers cannot distinguish query data from executable native request data.",
      "targetModel": "Move orchestration to application/account-query and define a capability-specific broker-spi `InstrumentSearchPort` for this catalog leaf. Protocol has named serializable request/response schemas with `Public | AccountScope` scope; adapters decode SDK objects into `InstrumentSearchHit` only after evidence-backed identity resolution and uniqueness checks before application aggregation.",
      "pureTransitions": [
        "Resolved query facts + decoded source rows -> broker-neutral `InstrumentSearchHit` or typed source failure",
        "InstrumentSearchHit -> serializable protocol view without SDK objects"
      ],
      "effectBoundary": "Application query service invokes the selected search-leaf adapters and owns source-status aggregation; protocol codecs encode only named serializable request/response records.",
      "durability": "Projection store owns catalog snapshots; protocol and journal stores use versioned safe views, never SDK-derived ContractDescription.",
      "errors": [
        "ProtocolDecodeFailure -> transport 400",
        "SourceQueryFailure -> per-source degraded result",
        "IdentityResolutionFailure -> hit not executable"
      ],
      "preservedBehavior": [
        "Search remains available to all current surfaces",
        "Asset class remains a caller hint, not a forced identity classification"
      ],
      "replacementSteps": [
        "Introduce application service/ports and migrate imports",
        "Remove re-export/type import and UTAManager dependency from domain/trading/contract-search.ts"
      ],
      "verificationCases": [
        "A runtime application query using the selected capability-specific search port returns a serializable hit/source-status response without a Contract instance",
        "An adapter fixture containing a native Contract is decoded before the application boundary and cannot be passed through as the public hit",
        "Application distinguishes typed no-match from source-unavailable outcomes"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The manager/protocol SDK coupling is removed at the application boundary; each adapter declares its query leaf and output schema, and only the UTA-facing metadata is shared.",
      "contractAnchors": [
        "K01",
        "K02",
        "K03",
        "K07"
      ]
    },
    {
      "id": "MAP-68DA0FDCA7",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/contract-search.ts:27-43: searchTradeableContracts accepts raw pattern/source and AssetClassHint, normalizes, resolves one/all UTAs, and filters default targets by asVendor"
      ],
      "currentBehavior": "The function trims through the normalization helper, returns [] for empty, resolves a raw source or all manager UTAs, and uses a boolean participation flag for default fan-out. It performs manager/account IO from domain code.",
      "problem": "Raw strings are used as source identity, AssetClassHint is not a resolved market context, and domain code owns asynchronous account resolution. An empty array conflates no match, unavailable source, no configured targets, and invalid request.",
      "targetModel": "Application decodes `SearchRequest {pattern, assetClassHint, selector, page}`; resolves `Public` scope for public catalogs or authenticated `AccountScope` for account-bound selectors, plus MarketContext/CapabilitySet and explicit source precedence; domain normalizer builds a hint; broker-spi query ports execute against resolved contexts.",
      "pureTransitions": [
        "SearchRequest + resolved Public|AccountScope/MarketContext/CapabilitySet -> PreparedSearchQuery",
        "SourceResult[] + explicit precedence/completeness -> CatalogSearchPage"
      ],
      "effectBoundary": "Application resolves manager/accounts and performs broker IO; domain normalizer/ranker receives only resolved values and has no UTAManager or network dependency.",
      "durability": "Read query uses projection/catalog snapshot; no mutation. Query response carries source snapshot/as-of and freshness so stale data is not mistaken for empty.",
      "errors": [
        "InvalidSourceSelector -> QueryFailure",
        "NoAuthorizedTargets -> QueryFailure/empty policy",
        "CapabilityUnavailable -> per-source status",
        "EmptyPattern -> QueryFailure.Empty"
      ],
      "preservedBehavior": [
        "Retain source override and default participation policy after authorization",
        "Retain normalization behavior and no broker call for empty pattern"
      ],
      "replacementSteps": [
        "Split pure normalization from application orchestration",
        "Migrate HTTP/AI callers and delete raw-manager function"
      ],
      "verificationCases": [
        "Explicit account query does not invoke any other UTA",
        "Default query invokes only authorized participating sources",
        "No configured accounts, no match, and source failure are distinct response variants"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "Normalization, source resolution, and provider IO are separated into pure/query effects; empty/no-match/unavailable outcomes remain distinct without adding order lifecycle to search.",
      "contractAnchors": [
        "K02",
        "K05",
        "K06",
        "K09"
      ]
    },
    {
      "id": "MAP-A999864CD2",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/contract-search.ts:44-65: Promise.allSettled fans out searches, silently drops rejected sources, flattens native descriptions, copies derivativeSecTypes, and optionally calls assetClassFor without rank/dedup/limit"
      ],
      "currentBehavior": "Concurrent source failures are discarded; fulfilled ContractDescriptions become hits carrying raw Contract, source id, raw derivative strings, and optional broker asset class. `Promise.allSettled` iterates results in the original target order returned by `manager.resolve`, followed by each broker's result order; no deterministic cross-source score/dedup/page metadata exists.",
      "problem": "Silently dropping failures makes an incomplete catalog look authoritative. Native SDK objects and heuristic asset class leak out, duplicate instruments are possible, and result limits/ranking are undefined.",
      "targetModel": "Each adapter returns decoded `InstrumentSearchHit`/`SourceSearchFailure` plus skipped-row evidence; each hit carries `scope: Public | AccountScope`. Application aggregates `SourceResult` variants, ranks/deduplicates only by evidence-backed canonical InstrumentId + venue/context, and returns `CatalogSearchPage {totalMatched: Known | Unknown, nextCursor, truncated, completeness}` with source statuses. Equal display symbols without equality evidence remain separate; identity conflicts are not silently merged.",
      "pureTransitions": [
        "SourceResult[] -> aggregate source status, completeness, and valid hits",
        "InstrumentSearchHit[] -> deterministic rank/dedup/page envelope using explicit source precedence",
        "Broker native row -> typed hit or decode failure/skipped-row evidence"
      ],
      "effectBoundary": "Adapters perform native query IO and row decoding; application aggregates source results and emits protocol pages. Domain transitions only rank/dedup trusted typed hits.",
      "durability": "Catalog projection owns identity mappings, source snapshots, skipped-row evidence, and completeness; the query itself does not mutate the transaction journal.",
      "errors": [
        "SourceQueryFailure(source, kind) -> degraded source status",
        "CatalogDecodeFailure(source, row) -> row/source degradation",
        "DuplicateInstrumentIdentity -> deterministic merge/conflict policy",
        "QueryLimitInvalid -> request failure"
      ],
      "preservedBehavior": [
        "One broker failure does not abort valid source results",
        "Broker-provided asset classification remains available only as decoded/contextual metadata, not an identity guess"
      ],
      "replacementSteps": [
        "Create source-result and broker-neutral hit codecs; migrate each broker search implementation",
        "Add deterministic aggregation/ranking/dedup/pagination and remove allSettled silent-drop/native projection"
      ],
      "verificationCases": [
        "One rejected source appears in sourceStatuses while another source's hit remains visible",
        "Two venue hits with same symbol remain distinct unless canonical IDs prove identity",
        "A duplicated canonical ID is merged by stable precedence",
        "A 50-item page reports truncation and source completeness"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "Source failures, skipped rows, ranking, and page completeness become explicit data-query evidence; no all-source handler or order transaction is required for a read result.",
      "contractAnchors": [
        "K01",
        "K02",
        "K05",
        "K06",
        "K07"
      ]
    },
    {
      "id": "MAP-3D1C0DC287",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/order-entry.spec.ts:1-23: makeFakeUta structurally casts commit/push/reject spies through unknown and fabricates a PushResult"
      ],
      "currentBehavior": "The test double supplies only the methods executeOneShotOrder touches and bypasses UnifiedTradingAccount's actual state, journal, scheduler, and broker contracts using a cast.",
      "problem": "A structural fake can make the one-shot helper look correct while hiding missing durability, approval, dispatch identity, and unknown-outcome behavior. It tests method calls rather than observable transaction state.",
      "targetModel": "Use a typed mock of the selected provider leaf plus a SQLite or real journal/projection/scheduler fixture only for this order-effect test, implementing named application ports rather than a structural UTA cast. Commands produce transaction receipts/events/jobs; the selected provider leaf returns typed Ack/RemoteOutcome values. Query, position, and catalog readers remain separate data effects.",
      "pureTransitions": [
        "Transaction command + current state/facts -> DecisionResult",
        "Events -> evolved state; durable job -> selected provider-leaf Ack/RemoteOutcome"
      ],
      "effectBoundary": "Test harness effects are explicit mock implementations of the selected journal writer, scheduler, and provider leaf; no structural UTA cast or real network.",
      "durability": "Assertions inspect receipt/event/job atomicity and transaction identity before dispatch, not whether commit/push spies were called.",
      "errors": [
        "ValidationFailed/CapabilityUnavailable -> no job",
        "StorageFailure -> no dispatch",
        "OutcomeUnknown -> durable reconciliation state"
      ],
      "preservedBehavior": [
        "Retain a fast deterministic test double where testing a pure helper is appropriate",
        "Replace this particular double because the helper itself is being removed"
      ],
      "replacementSteps": [
        "Build the smallest typed selected-provider-leaf/application harness",
        "Delete makeFakeUta and migrate all expectations to state/event/projection outcomes"
      ],
      "verificationCases": [
        "A valid selected order effect goes Draft→Prepared/Authorized→Queued and has a durable job before provider-leaf dispatch",
        "A journal failure produces no broker call",
        "A mock timeout yields Unknown requiring observation rather than a retry"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The fake UTA is replaced because this source tests an order effect, but the harness boundary is still provider-agnostic and uses typed ports; data readers are not pulled into its durable lifecycle.",
      "contractAnchors": [
        "K08",
        "K11",
        "K12"
      ]
    },
    {
      "id": "MAP-8BBBB36AD2",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/order-entry.spec.ts:25-38: happy-path test expects stage, commit(message), push(pending-hash), PushResult abc, and no reject"
      ],
      "currentBehavior": "The helper executes callback staging, synchronous TradingGit commit, then push with the returned pending hash; success returns the raw PushResult and the test asserts call order indirectly.",
      "problem": "A successful Git push is not a durable transaction approval or broker acknowledgement. The test never proves write-ahead job identity, approval binding, state replay, or projection result.",
      "targetModel": "Replace with an order-effect scenario: `CreateDraft` -> `Prepare` -> `Authorize` -> `Queued`, asserting `PreparedPlanRecord`, authorization actor-or-policy/digest/revision, durable `EffectJob`, dispatch identity, Ack, observation, and resulting projection. Git audit remains a projection; this scenario does not make Git a universal provider contract.",
      "pureTransitions": [
        "Draft command + facts -> Prepared plan or typed failure",
        "Authorized plan -> Queued state + durable intent",
        "Ack/observation -> state decision (not automatic fill)"
      ],
      "effectBoundary": "Journal writer atomically commits command receipt/events/jobs; scheduler dispatches only after commit; the selected provider-leaf double supplies acknowledgement/observation. Git audit is a projection, not authority.",
      "durability": "Receipt/event/execution projection/job identity must be durable before any dispatch; response loss is recovered by querying receipt.",
      "errors": [
        "StorageUnavailable -> typed failure and no remote mutation",
        "ApprovalRevisionMismatch -> Authorization/Precondition failure",
        "Unknown -> ReconciliationRequired"
      ],
      "preservedBehavior": [
        "A complete user form can still provide all order intent",
        "Successful execution still yields a queryable result, but no longer through PushResult as the mutation contract"
      ],
      "replacementSteps": [
        "Add typed lifecycle harness and remove one-shot fake assertions",
        "Migrate HTTP route to transaction response/query endpoints and Git audit projection"
      ],
      "verificationCases": [
        "Crash between durable queue commit and response leaves one queryable transaction/job",
        "Crash before dispatch leaves no broker mutation",
        "Broker Ack without fill leaves transaction in the criterion-appropriate working state"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "Git stage/commit/push remains observable legacy behavior only as audit projection; the effect scenario now proves durable intent/approval/dispatch ordering and provider receipt semantics.",
      "contractAnchors": [
        "K08",
        "K10",
        "K12"
      ]
    },
    {
      "id": "MAP-9D7DDC4081",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/order-entry.spec.ts:40-95: tests stage/commit/push errors separately, ensure reject is not attempted on commit failure, and stringify non-Error throws"
      ],
      "currentBehavior": "The suite asserts string phase errors, skips later phases after an earlier failure, and treats any non-Error throwable as a string. It does not model remote accepted-but-unobserved outcomes or durable recovery.",
      "problem": "Phase strings flatten validation, storage, capability, broker rejection, and unknown remote states. The test's lack of a durable state means it cannot catch the dangerous distinction between commit failure, dispatch failure, and an accepted remote order with lost acknowledgement.",
      "targetModel": "Use exhaustive local `TransactionFailure`/`QueryFailure` variants and `RemoteOutcome = Confirmed | Rejected | StillWorking | Unknown` only at the controlled effect boundary. Persisted recovery transitions are for effect execution; a query or native decode error follows its own leaf schema and does not acquire rollback behavior.",
      "pureTransitions": [
        "Command/state/facts -> Rejected or Accepted events + intents",
        "Remote outcome + criterion -> Committed/Reconciling/RecoveryRequired decision",
        "Unexpected throwable -> defect channel, not user failure string"
      ],
      "effectBoundary": "The effect runtime catches defects and records typed failures; the journal writer owns durable transitions for controlled effects, while each provider scheduler/adapter owns dispatch and observation. Data readers and search callbacks do not run through this lifecycle.",
      "durability": "Failure events, receipts, leases, dispatch identity, and unknown state are committed atomically. Recovery workers observe unknown before any retry.",
      "errors": [
        "ValidationFailed -> caller correction",
        "StorageFailure -> retry/read receipt according to writer policy",
        "BrokerRejected -> terminal/abort path with broker evidence",
        "OutcomeUnknown -> RecoveryRequired/observation job",
        "Defect -> supervised runtime failure"
      ],
      "preservedBehavior": [
        "Stage-equivalent validation still prevents commit/dispatch when input is invalid",
        "A failed operation is never masked by an unrelated reject error"
      ],
      "replacementSteps": [
        "Rewrite tests around typed failure envelopes and crash/restart cuts",
        "Remove errorMessage/string phase mapping and one-shot helper"
      ],
      "verificationCases": [
        "Commit storage failure produces no unauthenticated reject and no dispatch",
        "Remote timeout after dispatch creates Unknown and observation work, not blind retry",
        "Non-Error programmer defect remains visible as defect telemetry rather than a fake domain message"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "Typed expected failures and Unknown recovery remain order-effect obligations, while query/protocol failures and programmer defects are kept on separate branches.",
      "contractAnchors": [
        "K08",
        "K10",
        "K11",
        "K12"
      ]
    },
    {
      "id": "MAP-82BEA44FEB",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/order-entry.ts:26-33: OrderEntryPhase is stage/commit/push and OrderEntryResult returns PushResult or string error"
      ],
      "currentBehavior": "The public union exposes Git pipeline phases and a raw PushResult. It provides no transaction id, revision, actor, action tag, approval state, durable job, or typed failure payload.",
      "problem": "The result shape makes a Git push look like the trading outcome and cannot represent Prepared/AwaitingApproval/Queued/Unknown/reconciliation states. String errors force transport callers to infer semantics.",
      "targetModel": "Replace with named application responses for the selected capability’s create/prepare/approve/reject/query/recovery commands. Use `TransactionId`, `TransactionRevision`, `TransactionState`, `TransactionFailure`, and typed `TransactionResult` only for controlled effects; the capability tree decides which command exists, and Git audit remains a projection.",
      "pureTransitions": [
        "TransactionCommand + StateEnvelope -> DecisionResult",
        "DecisionResult -> serializable events/effect intents",
        "Projection query -> GetTransactionResponse with typed status"
      ],
      "effectBoundary": "Application invokes transaction kernel and journal writer; protocol maps named schemas; durable scheduler and broker own execution. No UTA/PushResult in public mutation type.",
      "durability": "All command receipts/events/jobs/projections use versioned schemas and transaction identity; response may be queried after process death.",
      "errors": [
        "TransactionFailure.ValidationFailed",
        "TransactionFailure.AuthorizationDenied",
        "TransactionFailure.StorageUnavailable",
        "TransactionFailure.CapabilityUnavailable",
        "TransactionFailure.DispatchFailure",
        "TransactionFailure.OutcomeUnknown"
      ],
      "preservedBehavior": [
        "Callers still receive an immediate success/failure response for accepted commands",
        "The Git audit can still expose human-readable commit history as a projection"
      ],
      "replacementSteps": [
        "Define protocol response schemas and migrate routes/clients",
        "Delete OrderEntryResult/Phase after no consumer uses raw push responses"
      ],
      "verificationCases": [
        "Prepare returns a transaction plan/revision, not a Git hash",
        "Approve binds actor/revision/digest and returns durable state",
        "GetTransaction distinguishes Rejected, StillWorking, Committed, and Unknown"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "The public result is replaced with named effect commands and receipts, but the capability tree decides which command exists; no universal action map or raw push result is exposed.",
      "contractAnchors": [
        "K02",
        "K08",
        "K10"
      ]
    },
    {
      "id": "MAP-684E054D47",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/order-entry.ts:35-73: executeOneShotOrder invokes caller stage, synchronous uta.commit, then uta.push(preparedHash), catching each phase separately"
      ],
      "currentBehavior": "A form callback stages an action, commit publishes a pending hash, and push executes it. Comments explicitly treat user-supplied form intent as manual approval and bypass the normal approval gate; commit failure returns without calling reject.",
      "problem": "This function couples staging/Git mutation/broker mutation, has no write-ahead durable effect intent, bypasses approval/revision/digest checks, and cannot distinguish broker rejection from unknown remote outcome. A synchronous commit exception also does not prove remote non-execution in all future implementations.",
      "targetModel": "Replace with application commands that create/replace a draft, prepare a serializable `PreparedPlanRecord`, authorize according to the selected capability's declared policy (authenticated actor or already-authorized policy), queue a durable effect, and recover unknown outcomes. The plan carries the selected provider's exact order payload plus scope, revision, before-image, criterion, and `FailurePolicy`; the four legacy form intents remain distinct effect commands only where declared, and form submission cannot serve as authorization by itself.",
      "pureTransitions": [
        "Draft + facts/policy -> PreparedPlanRecord or typed failure",
        "Authorize(plan revision,digest,actor-or-policy) -> Accepted authorization event + queue intent",
        "Dispatch/observation -> Ack/RemoteOutcome and state transition; Unknown suspends remaining forward steps under FailurePolicy"
      ],
      "effectBoundary": "JournalWriter atomically persists receipt/events/jobs; scheduler claims and dispatches; adapter observes broker. TradingGit only receives projection events after authoritative state changes.",
      "durability": "Plan revision, AccountScope, InstrumentId, ScopedOrderRef before-image, compensation assessment, locks, criterion, expiry, FailurePolicy, and dispatch identity are durable before remote mutation. Commit hash is optional audit projection data, not execution authorization; RecoveryRequired retains its durable RecoveryCase exclusion.",
      "errors": [
        "AuthorizationRequired/Denied -> caller supplies the declared authorization",
        "PreconditionFailed(revision/version) -> reprepare",
        "StorageFailure -> no dispatch",
        "BrokerRejected -> typed remote failure",
        "OutcomeUnknown -> durable reconciliation"
      ],
      "preservedBehavior": [
        "Keep the four currently exposed form intentions as distinct user choices where the selected provider declares them; do not require every provider to implement all four",
        "Keep rejection from inventing an unauthenticated rollback token when preparation failed",
        "Intentionally remove the implicit form-is-approval bypass"
      ],
      "replacementSteps": [
        "Migrate three HTTP one-shot routes and any tool/CLI caller to transaction commands",
        "Retire executeOneShotOrder only after durable scheduler and Git projection consume the new events"
      ],
      "verificationCases": [
        "Place form creates Draft/Prepared but does not call broker before declared authorization and durable queue",
        "Process death after authorization before claim leaves a durable queued job",
        "Timeout after dispatch produces Unknown and an observation job, never an automatic second place"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "One-shot form entry is moved to the controlled effect wrapper with declared authorization and recovery; the four legacy intents remain only where a provider declares them, not as mandatory handlers.",
      "contractAnchors": [
        "K08",
        "K09",
        "K10",
        "K11"
      ]
    },
    {
      "id": "MAP-D69EDB1AEE",
      "assessment": "designed",
      "sourceEvidence": [
        "services/uta/src/domain/trading/order-entry.ts:75-77: errorMessage returns Error.message or String(err) for every caught value"
      ],
      "currentBehavior": "All stage/commit/push thrown values are flattened to strings before returning to the route. The conversion loses failure type, transaction/dispatch identity, precondition data, and whether the throwable was expected or a defect.",
      "problem": "String coercion prevents exhaustive transport mapping and can leak native/provider details. It also encourages callers to treat a broker timeout and validation rejection as the same phase error.",
      "targetModel": "Remove errorMessage. Expected failures are typed `Result`/`TransactionFailure` values; defects remain supervised exceptions with redacted observability. Protocol has explicit failure codecs and stable codes/payloads.",
      "pureTransitions": [
        "Typed domain/application failure -> protocol failure envelope preserving variant/payload",
        "Unknown throwable -> Defect classification with correlation, not a domain error string"
      ],
      "effectBoundary": "Effect runtime owns defect capture/logging; application owns expected failure mapping; transport encodes redacted failure schema. No generic String(err) at the domain boundary.",
      "durability": "Expected failure receipts/events retain typed codes and relevant IDs; native error payloads are redacted and never used as replay state.",
      "errors": [
        "TransactionFailure -> caller-specific corrective action",
        "BrokerRejection/DispatchFailure -> reconciliation or user-visible rejection",
        "Defect -> supervised restart/alert",
        "ProtocolViolation -> boundary rejection"
      ],
      "preservedBehavior": [
        "Human-readable messages may remain as presentation fields derived from typed variants",
        "Non-Error defects remain observable rather than silently disappearing"
      ],
      "replacementSteps": [
        "Change stage/commit/dispatch APIs to return typed failures and update HTTP error mapping",
        "Delete errorMessage and phase-string assertions after all callers migrate"
      ],
      "verificationCases": [
        "A storage error maps to 503/StorageUnavailable while ValidationFailed maps to 400 without message parsing",
        "Broker timeout retains dispatch identity and becomes Unknown",
        "A thrown programmer defect is not presented as an ordinary user validation failure"
      ],
      "openQuestions": [],
      "contractDisposition": "reframed",
      "contractRationale": "Error-string flattening is removed at the provider/effect boundary; exact schema errors, delivery errors, and defects remain separately encoded and observable.",
      "contractAnchors": [
        "K03",
        "K07",
        "K08"
      ]
    }
  ],
  "crossGroupContracts": [
    "InstrumentIdentityContract: InstrumentId is only a nominal type; adapters construct it from an evidence-backed venue/listing/native-key tuple, persist a uniqueness-checked alias mapping scoped by venue/listing and by account only where the identity is account-bound, and surface unresolved or conflicting mappings. Display symbol, `aliceId`, conId, and SDK Contract are not interchangeable.",
    "ScopeContract: public Candle/News/Instrument/catalog facts use explicit `Public` scope and never receive a fabricated account or subaccount; account positions, account-bound search targets, and executable order facts carry an explicit `AccountScope`. Read/keyless capability is not promoted to transaction authority.",
    "NativeSentinelContract: IBKR UNSET_DECIMAL/UNSET_DOUBLE/UNSET_INTEGER handling remains adapter-local; OrderSpec, OrderSnapshot, protocol, journal, and projections use explicit absent/typed values and never SDK objects or sentinel numbers.",
    "PositionObservationContract: broker-reported pair, incomplete known component, or derived valuation must preserve multiplier, currency, InstrumentId, AccountScope, freshness, and observation provenance; one-sided upstream valuation is retained as Incomplete and only a selected Prepare whose effect explicitly requires complete valuation rejects it.",
    "CatalogSearchContract: normalizer, ranker, account-query fan-out, and protocol must share deterministic score/tie/source-precedence/dedup/page/truncation/source-failure semantics; duplicate canonical IDs require equality evidence and partial-source failure cannot be silently resolved.",
    "TransactionEntryContract: HTTP/UI form routes, TradingGit projection, transaction-kernel, journal-store, and durable-scheduler must cut over together from executeOneShotOrder to Draft/Prepare/Approve/Queue; Git commit/push is not broker execution authority.",
    "NativeInstrumentContract: the currently observed IBKR SecType values and structural rules remain in brokers/ibkr codecs, while the UTA-facing Instrument descriptor carries evidence-backed identity/context and precise provider extensions. Other providers may expose their own native kinds; only exact, declared mappings yield shared semantics or execution capability, and absence is reported as capability unavailable rather than by a universal handler or closed global taxonomy."
  ],
  "additionalFindings": [
    "services/uta/src/domain/trading/OrderHelper.ts:68-89 uses truthiness for outsideRth and parentId, so false and 0 disappear in addition to sentinel conversion; any replacement must preserve explicit values and distinguish wire presence from SDK defaults.",
    "packages/ibkr/src/order.ts:47-75,89-135,180-185,195-225 defines Decimal, UNSET_DOUBLE, and UNSET_INTEGER fields beyond OrderHelper's seven-name list; native codec/projection must enumerate all three sentinel classes and reject schema drift.",
    "packages/ibkr/src/contract.ts:75-116 defines the current 16-value SecType set; :81-95 coerceSecType warns and drops unknown raw values to '', so the replacement must capture UnknownNativeSecType(raw) before coercion and preserve all kind-specific structure.",
    "services/uta/src/domain/trading/git/TradingGit.ts:546-560 is the current public projection choke point: place/observe and modify operations call OrderHelper.toWire before status/commit views, so its replacement must be integrated with this projection rather than only broker dispatch.",
    "services/uta/src/domain/trading/brokers/alpaca/AlpacaBroker.ts:217-232 refreshCatalog atomically replaces active tradable cache and preserves stale cache on failure; :235-256 emits a pre-catalog echo when cache is null. Source status must distinguish Complete, Stale/Partial, and Unavailable, and echo hits are non-executable.",
    "services/uta/src/domain/trading/brokers/ccxt/CcxtBroker.ts:423-471 filters active stable-quote markets, pre-sorts swap/future/spot/option then USDT/USD/USDC, and skips malformed marketToContract rows after warning; target source status must preserve explicit preference ordinals and skipped-row evidence.",
    "services/uta/src/http/routes-trading.ts:61-77 and :563-605 map one-shot stage/commit/push strings to HTTP statuses for three routes; this is the concrete route cutover surface for transaction responses and must not be left on a compatibility phase union.",
    "services/uta/src/domain/trading/contract-search-rules.md:28-49 explicitly says vendor symbols are search hints and aliceId is the old downstream key; the new design keeps that namespace separation, adds runtime identity resolution/uniqueness checks, and retains normalization rule provenance.",
    "services/uta/src/domain/trading/contract-discipline.spec.ts:1-9: imports Contract and all validator symbols, then defines makeContract(overrides: Partial<Contract>) with SDK defaults; this unmapped test setup must split into IBKR codec fixtures and SDK-free domain constructors.",
    "services/uta/src/domain/trading/contract-search.spec.ts:1-7: imports ContractDescription, UTAManager, UnifiedTradingAccount, MockBroker/makeContract, and contract-ext side-effect augmentation; migration must replace this legacy manager/SDK harness with an application account-query harness and explicit InstrumentDescriptor/InstrumentId fixtures.",
    "services/uta/src/domain/trading/contract-search-rules.spec.ts:1-2: the test imports normalization through the relative compatibility barrel; this unmapped import is evidence that callers need to move to the single domain search-pattern parser before the barrel can be deleted.",
    "Identity boundary clarification: OrderHelper.ts:48-49 and contract-builder.ts:1-16,51-54,105-107 explicitly document an IBKR-as-superset/global output funnel; those comments remain source evidence of the legacy coupling, but the target assigns sentinel/Contract decoding to provider-local declarations and composes only selected UTA capability leaves."
  ]
}
