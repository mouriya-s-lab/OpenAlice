# 走查确认

依据：只读 `../uta-design.md`。每条 trace 都从一个最小刺激开始，按 3–6 步推演到可观察结果；“回写位置原文引用”取自主文档已经回写的 R、§2、§3 或 Q 行。

覆盖：§2.12 表前 11 项（T01–T11）、Q2 三变体（T12–T14）、Q7 基线与变体（T15–T16）、Q11（T17）、Q15（T18），以及 broker SDK 自动重试特别注入（T19），共 19 条。

## 确认 trace

|编号|回写位置原文引用|最小刺激推演（3–6 步）|结论|
|---|---|---|---|
|T01 写 C1 创建期拒绝|回写 R8、Q8（`uta-design.md:209,137`）：“校验或权限不过 → 指令与否决决定作为一次写落盘（客户端能看到自己提交过什么、为何被拒）；权限类拒绝额外记安全事件，参数类不记。”|1. 客户端提交一个权限不通过的委托。<br>2. 核心在创建期校验失败，写入指令与带原因的否决决定。<br>3. 因为是权限类拒绝，再写一条安全事件。<br>4. 指令不进入递送，集成没有调用。|闭合：指令、否决、安全事件和无递送结果均由 R8/Q8 唯一推出。|
|T02 写 C2 过期与否决|回写 R7、Q9（`uta-design.md:208,138`）：“到期 → 一条**过期**决定（与否决不同的变体，终态显示‘过期’）”。|1. 指令已保留，但批准前一直没有决定。<br>2. 等待达到批准前 deadline。<br>3. 核心追加一条过期决定，而不是否决。<br>4. 指令终态显示“过期”，不进入递送。|闭合：过期的决定种类和终态已与否决分开。|
|T03 写 C3 / 流 C3 缓冲确认与旧会话待发项|回写 R12、2.10（`uta-design.md:213,256`）：“核心重启后旧会话的确认（含滞留在传输缓冲里的）一律丢弃，不调用；核心侧该许可已落盘 → 结果不明（Q2 变体）。”|1. 旧会话确认滞留在传输缓冲中，尚未形成回票时核心重启。<br>2. 重启重放记录，发现旧会话确认不再有效。<br>3. 核心丢弃该确认，集成不调用；无许可的旧尝试号不留独立帐票。<br>4. 无许可时重新派发并分配新尝试号；若旧许可已落盘，则该指令归为结果不明。|闭合：旧会话确认、调用限制、尝试号和重启后的两种分支均已规定。|
|T04 写 C4 回票先于确认|回写 R11（`uta-design.md:212`）：“回票由集成经账户变动流提交，与许可确认是两条路径、无顺序保证。回票携带 client key 或已知 broker 原生 id → 直接关联到指令”。|1. 指令已有 client key，账户变动流先收到 broker 回票。<br>2. 核心用 client key 直接关联回票与指令。<br>3. 随后才到达许可确认，核心不要求两条路径先后。<br>4. 回票按 R4 进入指令视图并向订阅者分发。|闭合：先到回票不需要等待确认，也不被当作外部变动。|
|T05 写 C5 人工凭证与回票|回写 R10（`uta-design.md:211`）：“broker 回票是事实，人工凭证是补充。人工凭证之后到达的回票按 R4 折叠为准；人工凭证保留为审计，并追加一条‘人工结论与回票不一致’记录 + 告警；不再查询。”|1. 人工凭证先给出一个递送结论。<br>2. 同一指令随后收到 broker 回票。<br>3. 核心按 R4 以回票事实折叠指令状态。<br>4. 保留人工凭证，追加不一致记录并告警，停止查询。|闭合：回票优先、审计保留、不一致记录和停止查询均已闭合。|
|T06 写 C6 改单 / 撤单目标结果不明|回写 R9（`uta-design.md:210`）：“目标指令处于结果不明或无 broker 原生 id → 创建时拒绝（原因：目标结果不明），不排队等待”。|1. 客户端针对结果不明且没有 broker 原生 id 的指令提交改单。<br>2. 核心在创建期识别目标条件。<br>3. 核心写入拒绝，不创建排队项，也不递送给集成。<br>4. 目标收敛后，客户端可重新提交新的改单。|闭合：拒绝时点、原因和不排队都由 R9 给出。|
|T07 流 C1 回填中退订再订阅|回写 2.6 订阅（`uta-design.md:230`）：“退订删除订阅及其游标、回填进度、待确认缺口；再订阅是新订阅：从当前 seq（或按回填声明从头回填）开始”。|1. 一个订阅正在回填，并保有游标、回填进度和待确认缺口。<br>2. 客户端退订，核心删除这四类订阅状态。<br>3. 客户端再次订阅，核心创建新订阅。<br>4. 新订阅从当前 seq 开始；若声明回填，则从声明窗口起点重新回填。|闭合：退订后的状态删除和再订阅起点没有遗留分支。|
|T08 流 C2 能力缩减|回写 2.6 订阅、2.8（`uta-design.md:230,246`）：“能力缩减（重新声明）→ 受影响订阅挂起并通知”；“游标保留；自动恢复并通知”。|1. 集成重新声明能力，导致某个已订阅流不再支持。<br>2. 核心将受影响订阅置为挂起并写明原因，同时保留游标。<br>3. 投递流发送挂起通知，期间不要求客户端退订。<br>4. 集成重新声明支持或会话重建后，核心自动恢复并通知。|闭合：挂起原因、游标保留、恢复条件和通知均已回写。|
|T09 流 C4 凭据切换在途请求|回写 2.8（`uta-design.md:246`）：“凭据切换 / 会话重建期间：在途一次性读返回 typed 不可用（会话重建）；在途写按 R1——无许可的尝试在新会话重新派发，有许可的进入结果不明。”|1. 凭据切换触发集成会话重建，同时存在在途一次性读和写。<br>2. 一次性读返回带原因的 typed 不可用。<br>3. 没有调用许可的写在新会话重新派发；已经落盘许可的写进入结果不明。<br>4. 核心分别沿读失败和 R1 写路径处理，不把两者合并。|闭合：读与写的会话重建分支已有明确结果。|
|T10 流 C5 fence 时序|回写 2.10（`uta-design.md:255`）：“锁不可得拒启；持有者死 → fence+1 落盘后服务；旧持有者写被拒退出”。|1. 第一核心持有数据目录文件锁，第二核心启动。<br>2. 第二核心拿不到锁，拒绝启动并报告持有者。<br>3. 第一核心死亡后，第二核心取得锁，先把 fence+1 写入存储头，再开始服务。<br>4. 旧核心若恢复并以较小 fence 写入，被拒并退出。|闭合：拒启、接管写入顺序和旧持有者处理均唯一。|
|T11 流 C6 公共 feed 配额|回写 3.1（`uta-design.md:323`）：“公共行情 feed 每 broker 一个，是独立来源，其订阅配额归 feed 会话按 broker 声明计，账户订阅行情走公共 feed 时计入 feed 配额而非账户”。|1. 两个账户的行情订阅都经同一 broker 的公共 feed。<br>2. 核心把两者计入该 feed 会话的配额。<br>3. 核心不按账户分别增加配额，也不把公共 feed 归入某个账户。<br>4. 达到 feed 声明的限额时，按订阅配额规则整体拒绝超限接纳。|闭合：配额归属唯一，不因账户数量产生第二种计数方式。|
|T12 Q2-A 许可后核心崩溃|回写 Q2、R1、2.10（`uta-design.md:131,202,256`）：“许可已落盘、核心在收回票前被 kill -9”；“调用许可已落盘、无回票 → 结果不明 → 2.4，不再许可”。|1. 指令、已交集成和调用许可已经写入并得到介质确认。<br>2. 核心在收到回票前被 kill -9。<br>3. 重启重放后发现许可存在而回票不存在，将指令判为结果不明。<br>4. 核心不再发放许可，转入 2.4 的查询渠道。|闭合：调用记录至多一条，且许可后的崩溃不会生成第二条许可。|
|T13 Q2-B 确认已进 socket 缓冲但双方日志看不到|回写 Q2、R12、R3（`uta-design.md:131,213,204`）：“确认已进 socket 缓冲但集成日志无记录”；“旧会话的确认（含滞留在传输缓冲里的）一律丢弃，不调用”；“结果不明期间不创建新的 broker 尝试”。|1. 调用许可已落盘，确认字节已进 socket 缓冲，但核心和集成日志都看不到确认的接收记录。<br>2. 日志不可见不能证明集成未调用，重启后也不能证明调用已经发生。<br>3. 核心把旧会话缓冲中的确认丢弃；因许可已落盘且没有确定回票，指令归为结果不明。<br>4. R3 禁止新的 broker 尝试和新的调用许可，随后只进 2.4 的查询或人工路径。|闭合：结果必须是结果不明；不发放新许可、不创建新尝试，且不把日志缺失当作未递。|
|T14 Q2-C 集成拿到确认后调用前崩溃|回写 Q2、R1、2.4（`uta-design.md:131,202,217-221`）：“集成拿到确认后调用前崩溃”；“许可是安全边界，不是调用事实”。|1. 核心已落盘调用许可，集成已拿到确认。<br>2. 集成在调用 broker 前崩溃，核心没有回票；fixture 中调用记录为零。<br>3. 核心仍不能把许可解释为未调用，因而将指令判为结果不明。<br>4. 核心不再发放许可，按能力声明依次查询，仍无结论则转人工。|闭合：即使 fixture 观察到调用记录为零，核心仍按结果不明处理，且不会自动重发。|
|T15 Q7-A 同账户并发基线|回写 Q7、R2（`uta-design.md:136,203`）：“同账户按提交顺序递送”；“同账户（同子账户）的写按批准顺序递送；跨账户互不等待”。|1. UI 与 AI 在 100ms 内向同一账户各提交一笔委托。<br>2. 核心按批准顺序建立同账户队列。<br>3. 核心先递送队首，调用不重叠，再递送后一笔。<br>4. 另一个账户的队列独立推进，不被这两笔等待。|闭合：同账户顺序、调用不重叠和跨账户隔离均可观察。|
|T16 Q7-B 第一笔结果不明|回写 Q7、R2、R3（`uta-design.md:136,203-204`）：“头一笔不明时后一笔等待并告警”；“结果不明期间不创建新的 broker 尝试”。|1. 同账户队首指令的调用许可已落盘，但没有确定回票，判为结果不明。<br>2. 核心告警并保持后一笔在队列中等待。<br>3. 等待期间不为后一笔调用 broker，也不为队首创建新的 broker 尝试或许可。<br>4. 其他账户继续处理，不受该队列阻塞。|闭合：不明阻塞、告警、不重发和跨账户独立均已规定。|
|T17 Q11 行情订阅断线|回写 Q11、2.6 流（`uta-design.md:140,229`）：“broker 无续传”；“连续性断裂……→ 新代数，首条 = 来源缺口（前一代数、其最后 seq、原因）”。|1. 订阅 200 个 instrument 的 tick，断线前最后记录为某代数的最后 seq。<br>2. 集成断线 30s，且 broker 没有可信续传游标。<br>3. 重连时核心开启新代数，第一条写来源缺口，带前一代数、最后 seq 和断线原因。<br>4. 新代数继续发送记录，订阅者先看到缺口而不是静默跳过。|闭合：断线前后各自保持流内顺序，缺口与新代数均可观察。|
|T18 Q15 一次性读 fan-out|回写 Q15、2.7（`uta-design.md:144,236`）：“无 source 读持仓，3 账户其一 broker 挂”；“逐账户汇总返回，不合并、不重排……持仓……响应同时记为对账响应帐票”。|1. 客户端无 source 读持仓，核心解析为三个账户的 fan-out。<br>2. 两个账户在 deadline 内返回持仓，另一个账户返回 typed 不可用。<br>3. 核心逐账户返回三份结果，不合并、不重排。<br>4. 各账户返回的持仓响应按账户作为对账响应帐票保留。|闭合：部分失败、deadline、逐账户结果和留票均有确定路径。|
|T19 特别注入：broker SDK 自动重试|回写 R1（`uta-design.md:202`）：“集成使用的 broker SDK / HTTP 客户端**必须关闭对写请求的自动重试**；只有能力声明中 broker 文档化保证同 client key 幂等时，才允许按该能力规则重放同一尝试”。|1. 写请求已按 R1 发送，SDK 在传输超时后准备自动重试。<br>2. 集成必须关闭该 SDK 的写请求自动重试，因而不能由客户端库自行再次调用 broker。<br>3. 若许可已落盘而回票缺失，核心按结果不明进入 2.4，而不是以 SDK 重试补救。<br>4. 只有能力声明明确 broker 对同 client key 提供文档化幂等时，才可按该能力规则重放同一尝试；这不是通用自动重试。|闭合：broker SDK 自动重试被 R1 禁止，结果不明只走查询、对账回读或人工凭证。|

## 最终卡点表

|编号|卡点|依据|处理|
|---|---|---|---|

空：19 条 trace 均能由主文档回写内容推出，未发现需要补规则的未定义分支。

## 词汇扫描

本文件扫描结果：禁用词命中数为 0。
