# 讨论者最终产出：SteadyFlow

原始产物 `agent://SteadyFlow` 的逐字导出（JSON 或 Markdown 原样）。

```
{
  "text": "## D4 — command lifecycle\n**Verdict: amend.**\n\n- **Strongest objection:** the draft names the entry kinds and a coarse crash matrix, but does not give a unique transition for authorization deduplication, partial/embedded fills, expiry-vs-approval races, capability-specific `unknown`, config-version binding, or same-account races (`local://decision-register-draft.md:34-44`). The detail contract requires separate receipt/observation and capability-derived unknown handling, but leaves those branches open (`plans/uta-refactor/design/01-detail-constraints.md:155-164,220-233`).\n- **Amendment text:** “Add a normative transition table specifying preconditions, append order, CAS/idempotency scope, crash recovery, terminal outcome, and capability branch. Add durable `decisionId` deduplication, an intent/attempt `configRevision` snapshot, expiry linearization, explicit `Unsupported`/read-only outcomes, and a per-account queue/CAS conflict result. If an accepted response contains real fills, append the receipt and a separately source-tagged, correlated observation; never infer fills from the receipt.”\n- **Falsifier:** a deterministic model/replay test covering every listed crash, duplicate, expiry, fill, capability, config, and race case produces exactly one specified outcome without adding any branch.\n- **Cheaper/boring alternative:** one per-account FIFO writer plus expected-position CAS and this explicit transition table; no workflow engine or new orchestration abstraction.\n\n### D4 scenario table\n| Scenario | Unique outcome? | Gap / evidence |\n|---|---|---|\n| Immediate fill | **Partial** | `accepted` is only a receipt; fill observation timing/fold is not fixed (`local://decision-register-draft.md:37-38`; constraints `:155-164`). |\n| Partial fill | **No** | Remaining quantity, later fill, cancel, and compensation semantics are absent (constraints `:76,155-164`). |\n| Provider rejection | **Partial** | Rejected receipt fields exist, but terminal/retry/intent outcome is not specified (constraints `:232-233`). |\n| Timeout after possible acceptance | **Partial** | `unknown` is fixed; read-by-key result, bounded retry, and final state are not (`local://decision-register-draft.md:37,44`; constraints `:161-164`). |\n| Crash before attempt append | **Yes, conditionally** | Durable-before-call implies no provider call if the append boundary held; torn-tail recovery belongs to the persistence contract (`local://decision-register-draft.md:36,44`; constraints `:250-258`). |\n| Crash after attempt append, before provider response | **No** | Reconcile/read-by-key versus `awaiting-review` is named, not selected (`local://decision-register-draft.md:42-44`). |\n| Crash after response, before receipt append | **Partial** | The response cannot become a fact; it must be treated as unknown, but append/reconcile triggering is unspecified (constraints `:155-164`). |\n| Duplicate approval delivery | **No** | Multiple decisions/effective fold exists, but no durable decision identity or duplicate-delivery rule (`local://decision-register-draft.md:35`). |\n| Stale/expired approval | **No** | Expiry can append reversal, but its linearization against approval and boundary time are unspecified (`local://decision-register-draft.md:34,42`). |\n| Read-only provider | **No** | Capability absence is explicit in principle, but D4 lacks the receipt and intent terminal variant (`plans/uta-refactor/design/01-detail-constraints.md:220-233`). |\n| NaN guard config | **No** | D10 rejects at write only; current `Number('NaN')` paths permit forever (`local://decision-register-draft.md:79`; `services/uta/src/domain/trading/guards/cooldown.ts:8-32`, `max-position-size.ts:8-48`). |\n| Config replacement during an in-flight attempt | **No** | Drain finishes the critical section, but no revision/snapshot says which config governs the attempt (`local://decision-register-draft.md:87`; constraints `:139-144`). |\n| Two intents racing on one account | **Partial** | A serial order is required, but FIFO/tie-break, loser result, and expected-position conflict semantics are absent (`plans/uta-refactor/design/01-detail-constraints.md:139-144`; `local://decision-register-draft.md:42`). |\n\n## D6 — replacement envelope\n**Verdict: amend.**\n\n- **Strongest objection:** “all consumers are in this monorepo” is not proof that no staggered or external caller exists. The packet finds 48/49 in-repo endpoint consumers and no repository caller for legacy quote, but explicitly says an external client cannot be ruled out because UTA is unauthenticated loopback (`local://w1-surface-map.md:1-5,176-186`).\n- **Concrete break risks:** UI hand-copied types/API (`ui/src/api/types.ts:273-715`, `ui/package.json:13-47`); SDK and 26 tools (`src/services/uta-client/UTAAccountSDK.ts:127-362`, `src/tool/trading.ts:209-931`); CLI catalog/provenance (`src/server/cli-commands.ts:212-269`, `src/server/cli.ts:285-375`); connector hash/409 semantics (`src/services/connector-client/uta-review.ts:73-217,249-266`); Bars source/details/historical routing (`src/domain/market-data/bars/bar-service.ts:263-434`); simulator/demo mutations (`ui/src/api/simulator.ts:1-95`, `services/uta/src/http/routes-simulator.ts:109-221`); BFF/config/test-connection and health/restart (`src/webui/routes/trading-proxy.ts:115-187`, `src/webui/routes/trading-config.ts:40-360`, `services/uta/src/http/routes-trading.ts:199-215`).\n- **Amendment text:** “Allow no facade only after a release gate inventories deployment versions and proves all old callers are upgraded or blocked, including the dead quote path and direct/staggered binaries. Contract-check every listed consumer and preserve write/CAS, mode, error/status, precision, side-effect, provenance, and health semantics. If an external or staggered caller exists, use a full one-release compatibility adapter; a read-only facade is insufficient because old surfaces include wallet writes, one-shot writes, and simulator mutations.” (`local://w1-surface-map.md:176-186`)\n- **Falsifier:** deployment telemetry proves no external/staggered client, or proves the only external client is read-only legacy quote; then atomic migration (or a tiny read-only alias for that one path) is cheaper than a full adapter.\n- **Cheaper/boring alternative:** with that proof, do the atomic migration and delete the facade; otherwise retain a temporary full adapter rather than a misleading read-only one.\n\n## D7 — security/principals\n**Verdict: amend.**\n\n- **Strongest objection:** every launcher has an environment injection seam, but none currently generates or transmits the bearer: dev (`scripts/guardian/dev.ts:249-272`), prod (`scripts/guardian/prod.mjs:280-323`), Electron (`apps/desktop/src/main.ts:786-807,845-903`), Bun role dispatch (`packages/cli/bin/openalice-bun.ts:34-55`, `scripts/guardian/runtime-process-spec.mjs:1-24`), and Docker’s Guardian-only entry (`Dockerfile:107-170`). The client sends only JSON headers (`packages/uta-protocol/src/client/UTAClient.ts:13-74`), and BFF forwarding strips `Authorization`/`Principal` (`src/webui/routes/trading-proxy.ts:27-34`).\n- **Concrete injection behavior:** generate one cryptographically random token per Guardian run (overwriting any supplied env value), inject it into both Alice and UTA; keep it stable across UTA respawns. Dev’s `baseEnv`, prod’s `makeUTASpec`/Alice env, and Electron’s two child envs can carry it. Bun’s `--internal-role uta` and Docker inherit it only from Guardian; direct UTA role/artifact without it must fail closed. Connector remains Alice-mediated; any future direct UTA connector must receive the same authenticated channel or be rejected.\n- **Amendment text:** “UTA client/BFF attach `Authorization: Bearer <run-token>`; UTA accepts a `Principal` only as an Alice-authenticated, server-stamped assertion (not an arbitrary caller header), records it on intents/decisions, and returns 401 otherwise. Reject non-loopback `OPENALICE_UTA_URL` before constructing/sending a local bearer. A remote UTA requires a separately specified authenticated transport and must never receive this local token.” Current URL resolution accepts any explicit URL (`src/services/uta-supervisor/url.ts:10-14`), while the topology evidence says remote detached UTA is unsupported (`local://w1-lifecycle-topology.md:138-140`).\n- **Falsifier:** a supported remote deployment is specified with Guardian-controlled mTLS or an equivalent authenticated principal-binding protocol; then non-loopback rejection can be replaced by that explicit remote transport contract.\n- **Cheaper/boring alternative:** enforce loopback-only transport plus one Guardian token and make Alice the sole principal authority; do not add generic remote support in this release.\n\n## D10 — rules/guards\n**Verdict: amend.**\n\n- **Strongest objection:** D10 promises fail-closed parsing only at config write, while current schemas accept arbitrary guard options (`src/core/config.ts:259-323,419-488`) and runtime behavior is unsafe: unknown kinds warn-and-skip (`services/uta/src/domain/trading/guards/registry.ts:23-31`), NaN numeric options allow forever (`cooldown.ts:8-32`, `max-position-size.ts:8-48`), and empty whitelist throws (`symbol-whitelist.ts:7-13`). Per-account init catches that throw and silently omits the account (`services/uta/src/main.ts:76-87`); malformed account data fails earlier in `readUTAsConfig` (`src/core/config.ts:719-793`).\n- **On-disk check:** no invalid active guard value was observed. The only current legacy guard fields are empty arrays in `crypto.json:1-6` and `securities.json:1-6`; backups likewise contain `guards: []` (for example `/Users/mouriya/.openalice/data/_backup/2026-07-16T12-53-41-502Z-pre-0022_connector_service_config/config/crypto.json:5`). `accounts.json:1-6` is sealed, so possible account guards are not inspectable from ciphertext without the sealing key. Thus “invalid legacy config already exists” is not established by the disk evidence.\n- **Amendment text:** “Use one strict, kind-aware parser at write, startup, and before provider construction: rule-declared finite numeric domains, non-empty validated whitelist strings, and registered kinds. Unknown/invalid persisted account config is preserved/quarantined and exposed as `config-invalid`/non-writable with a structured error, never warn-and-skip; global invalid config blocks startup. Migrate the unconsumed legacy `crypto.guards`/`securities.guards` fields explicitly (empty arrays are a no-op), with backup, rather than silently interpreting or ignoring them.”\n- **Falsifier:** a migration proof establishes those legacy sections are intentionally dead metadata and no account contains guards; then backup-and-remove migration is sufficient and global startup blocking is unnecessary.\n- **Cheaper/boring alternative:** strict account schema plus an explicit one-time backup/remove migration for dead legacy sections; no compatibility guard registry or permissive fallback."
}

[You have received this identical output 4 times. Re-reading 'agent://SteadyFlow' will not change it — use a narrower selector (path:A-B), or proceed with the edit.]
```
