# 讨论者最终产出：DivergentFlow

原始产物 `agent://DivergentFlow` 的逐字导出（JSON 或 Markdown 原样）。

```
{
  "assignment": "D4 / D8 / D9 stress-test",
  "artifact": "positions only, no artifacts written",
  "D4": {
    "verdict": "amend",
    "objection": "Cut is right; the unit of approval is wrong. D4 scopes intent to one operation, but today one commit() freezes the whole stagingArea into ONE pendingHash that three consumers present/check/spend (services/uta/src/domain/trading/git/TradingGit.ts:81-85,119-127,241-249; ui/src/components/PushApprovalPanel.tsx:412-431; src/services/connector-client/uta-review.ts:190-200). Losing the batch loses (a) real: one-click N-operation approval and the provenance 'approved together'; (b) fake: atomicity, which never existed — the push loop catches per-operation and continues, landing mixed success/rejected (TradingGit.ts:141-155). Multi-leg atomicity lives INSIDE one operation payload, not across staged ops: tpsl rides one placeOrder and Alpaca sends one order_class:bracket call (services/uta/src/domain/trading/brokers/alpaca/AlpacaBroker.ts:337-348); cross-op linkage is only parentId/ocaGroup fields (services/uta/src/domain/trading/UnifiedTradingAccount.ts:716-717) and IBKR's transmit-chain bracket is unimplemented, refusing loudly (services/uta/src/domain/trading/brokers/ibkr/IbkrBroker.ts:481-488). So D4 loses no atomicity the product has.",
    "amendment": "1) intent.proposed gains optional proposalId; intents from one user/agent action share it and the ordered member list is recorded once. 2) authorization.decided subject widens from intentId+intentHash to scope{proposalId?, intentIds[]} + scopeHash; define TWO folds: intentOutcome (per intent, partial approval expressible) and proposalOutcome (over the scope, so UI renders one click). 3) attempt.started stays strictly one intent / one idempotency key / one provider placement; a proposal never merges attempts (01-detail-constraints.md §2.2/§4.4). 4) Add normative negative: a proposal provides NO provider-level atomicity; multi-order atomicity must live in a single operation payload or be a declared batch-placement capability (A7). 5) The one-shot UI route must return intentId/proposalId — today services/uta/src/domain/trading/order-entry.ts returns only phase/PushResult and the UI keeps no handle to the proposal it created.",
    "falsifier": "A real requirement where approving a strict subset is worse than approving none (an OCA pair where the unprotected single leg is dangerous). That forces a proposal-scoped all-or-nothing execution gate — the Execution consumer must hold sibling intents back — a materially bigger state machine than this amendment.",
    "cheaper_alternative": "Keep the single pending batch per account verbatim (TradingGit.ts:81-85) and add only actor, expiresAt and inspection. Cost: an agent proposing five independent ideas serializes behind one human decision and one rejection discards all five. If concurrent proposals per account is not a product requirement, D4 is over-built."
  },
  "D8": {
    "verdict": "amend",
    "objection": "D8 enumerates a stream over 'ledger appends, observations, health and readiness' — but observations ARE ledger entries (D4 §5 observation.recorded), so that item is double-counted, while health/readiness are exactly what A8 and 01-detail-constraints.md §7.2 forbid putting in the ledger. One shape is being given to three things with three durability needs. 'Derived from ledger appends' also reads as an outbox — and the reference design explicitly did NOT adopt that: plans/uta-refactor/design/00-core-contract.md §7 lists 'ProcessManager as reliable outbox' under 不采纳. The ledger is already cursor-addressable per account (position, expectedPosition CAS, hash chain, hash, D5); a second durable store over the same entries buys nothing and invents a dual-writer question.",
    "amendment": "1) Replace the first clause: the trading ledger IS the durable event stream; per account the cursor is `position`; no second durable store is created for ledger-derived events. 2) GET /v2/events?after= becomes a read-only fan-in over each account's ledger cursor plus one current-state readiness read; every item tagged {source, accountId, position}; the response returns the next-cursor map — no opaque composite cursor, per-source positions stay visible. 3) SSE is a lossy notification only: it may drop, it carries no cursor authority, and it never tells a consumer it is caught up. 4) Health/readiness get NO durable journal (§7.2 permits own current-state storage; §3.4 makes staleness a consumer policy). 5) Explicitly do not reuse data/event-log/events.jsonl for anything durable — two instances at one path wrote duplicate seq while both reported lastSeq:1 (local://w1-ledger-persistence.md §4 probe), and recovery skips malformed lines (src/core/event-log.ts:303-344); both are the recovery shortcuts D5 forbids.",
    "falsifier": "A required feature needing health HISTORY rather than state — e.g. 'alert me that account X was offline for ten continuous minutes'. Then health needs durable history, its only non-ledger home is a second journal, and a composite cursor becomes mandatory. Second: a consumer that cannot persist a cursor (ephemeral UI session) yet must not miss events — requires server-side retention.",
    "cheaper_alternative": "Ship no SSE and no long-poll: one polling endpoint at ~2s replacing the seven independent intervals (ui/src/live/account-health.ts:39-47 at 5s; PushApprovalPanel.tsx:397-400 at 3s; plus 15s/30s/60s). D8 specifies two notification mechanisms for one need; pick one. Long-poll+SSE only wins if sub-second latency matters, and the tightest current cadence is a human clicking Approve at 3s — it does not."
  },
  "D9": {
    "verdict": "amend",
    "direction_verdict": "accept Main's pull (Alice-side desk bridge pulls UTA events) over IssueSystemMap recommendation A (UTA pushes to an authenticated Alice bridge)",
    "objection": "Three concrete defects. (a) 'deterministic id -> idempotent' is false as implemented: createIssue refuses an existing id and returns conflict, not the original result (src/workspaces/issues/mutate.ts:83-120,128-146) — a crash after create-but-before-cursor-advance makes the retry FAIL and lose the request. (b) Cursor ordering unspecified and the write unsafe: create-then-advance is mandatory, and the Issue write is a plain writeFile, not temp+rename (src/workspaces/file-service.ts:117-142), while the scanner isolates unreadable Issue files (src/workspaces/issues/declaration.ts:403-412) — a torn write silently drops a request after the cursor moved. (c) The derived id can be invalid: issueId must match ^[a-zA-Z0-9][a-zA-Z0-9_-]*$ (declaration.ts:66-74), so uta-<accountId>-<requestId> from raw ids can be rejected outright. ON DIRECTION: pull is right for a reason better than convenience — UTA is optional and its crash leaves Alice running (local://w1-lifecycle-topology.md §1). Under pull, Alice-down is cursor lag and UTA-down is no-new-events; neither side needs a durability guarantee from the other. Under push, UTA must know Alice's endpoint, retry Alice, and hold a durable outbox — reintroducing the refused reliable-outbox and inverting who owns delivery. Main's direction keeps ownership cleanest; keep it.",
    "third_shape": "The desk Issue (plans/telegram-connector-issue.md) is genuinely better as a NOTIFICATION TIMELINE and worse as a DECISION SURFACE. For: the memo precedent is built and shipped — connectorDesk with at-most-one live desk per connector per Project (declaration.ts:315-325,386-388), when as heartbeat, [[no-reply]] to keep silent fires cheap; and comment appends are ALREADY replay-safe via deterministic ids returning the existing comment (src/workspaces/issues/comments.ts:138-149), precisely what createIssue lacks. Against: comments are {id,author,at,markdown,replyTo?,delivery?,via?} (comments.ts:23-34) with no typed payload, and local://w1-issue-system.md §4 states freeform comments cannot be authority — so the desk still needs the typed decision endpoint and replaces only the per-request Issue. Also: one desk per Project fans all accounts into one comment stream, so the agent prompt cannot be narrowed per account, and routing through the desk heartbeat converts minute-scale reconciliation into desk cadence (plan offers 1h-24h; scanner polls at 60s, src/workspaces/schedule/scanner.ts:59). Recommendation: adopt the desk as the TIMELINE, keep typed decisions, keep a per-request Issue only when the request carries a distinct deliverable; if adopted, desk PER ACCOUNT, cadence <=5m, [[no-reply]] on silent fires.",
    "amendment": "Add to D9: (a) the bridge reads its own link store before creating; conflict from createIssue is success-by-reference, never an error. (b) The link store is written temp+rename and its cursor advances only after the Issue file is durable AND re-readable. (c) The bridge owns a charset-safe, length-capped id derivation. (d) The typed decision endpoint requires a caller-supplied idempotency key — D4's authorization.decided requires one, but the route as written accepts only :id. (e) State the desk alternative explicitly and pick one.",
    "falsifier": "If the bridge's link store cannot be made crash-safe relative to Issue creation — the launcher store and .alice/issues cannot be ordered — then create-then-advance cannot be made lossless, and the desk-comment shape (needing only an idempotent comment append, already replay-safe) becomes strictly safer.",
    "cheaper_alternative": "No per-request Issues at all: post one comment onto a single desk Issue per account; comment dispatch to a fixed owner already exists (src/workspaces/issues/comment-delivery.ts:41-142). Cost: the request stops being an addressable work item — no assignee, no status, no per-request done. [INFERENCE] Requires the desk Issue to carry an exact @resumeId owner and the bridge comment to be attributed as a distinct owner; I did not verify that dispatch path end-to-end."
  },
  "d_work_requested_payload": {
    "criterion": "Addressability, not completeness. The agent will read UTA regardless (reads are open to any consumer, 01-detail-constraints.md §3.3). The requirement is that every further read is addressable by an id in the payload, so no scan of unrelated history is needed.",
    "required_beyond_Main": [
      "kind as a discriminating variant, so the field set is exhaustive per kind rather than optional soup",
      "causal entry reference: entryId + position of the entry that raised the request (the receipt.unknown, the guard reversal, the watch.triggered observation) plus its why — without it the agent scans the ledger to learn what happened (W1 defect D7)",
      "authority: binding | recommendation, derived from the account AuthorizationPolicy at emit time — D9 says an agent decision may be a mere recommendation; if the payload does not say which, the agent must re-query to learn whether its answer counts",
      "decision vocabulary: admissible actions (approve|reject|withdraw) and the exact scopeHash/intentHash the decision must bind to",
      "asOf + freshness of the last relevant observation (not a snapshot) — authorization on stale prices is what INV-7.9-8 exists to prevent",
      "decode context: providerId + projectionVersion (D3 envelope) and the operation payload in decimal strings; without the projection version the opaque payload is unreadable and the first agent move is a lookup",
      "idempotencyKey of the underlying intent, distinct from requestId, so a decision and any compensation correlate without re-derivation"
    ],
    "deliberately_excluded": "positions, balances, health, any embedded account snapshot — read-only observations; embedding them creates a second stale copy in a Markdown Issue and pushes the agent to reason from it instead of from UTA",
    "falsifier": "If the Issue detail must be reconstructable without querying UTA — e.g. an auditor reading only .alice/issues — then the payload must embed the observation set and the addressability criterion is wrong."
  },
  "cross_cutting_note": "D4's expiry consumer, D8's cursor and D9's bridge cursor are three independent notions of 'where am I'. State in the register whether the bridge cursor may lag the ledger position: yes, and it must, or Alice-down becomes a UTA problem.",
  "axioms_untouched": "A1-A8 not relitigated; no Rust proposed."
}

[You have received this identical output 3 times. Re-reading 'agent://DivergentFlow' will not change it — use a narrower selector (path:A-B), or proceed with the edit.]
```
