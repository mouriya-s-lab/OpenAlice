# 讨论者最终产出：SteadyCore

原始产物 `agent://SteadyCore` 的逐字导出（JSON 或 Markdown 原样）。

```
{
  "decisions": [
    {
      "id": "D1",
      "verdict": "amend",
      "strongest_objection": "The conclusion (TypeScript first) is sound, but the register overstates its benchmark. The 200-connection Node baseline shows 10.417% mean client CPU and 25.3% maximum, and the harness is synthetic localhost JSON—not provider decoding, TLS/proxy, reconnect storms, ledger writes, or Issue callbacks (`local://w1-language-runtime.md:376-385,466-476`).",
      "amendment": "Keep TypeScript with Node as default and Bun 1.4.0 support; state that the probe establishes only feasibility at the target load, not a capacity/SLO result. Before closing D1, run the full UTA with a compiled active Pack through the file-URL loader, representative provider-like replay/fan-out, and startup/RSS/CPU plus sustained duration checks (the current Effect evidence explicitly calls this acceptance gate: `local://w1-effect-evaluation.md:225`). Treat a serializable Pack ABI as a boundary invariant, not a commitment to a future Rust sidecar.",
      "falsifier": "A representative provider replay—not the localhost harness—sustained at least 25,000 processed messages/s for 10 minutes with p99 ≤5 µs, zero loss, and bounded queues while the TypeScript implementation failed; this is the stated falsifier, and no such evidence exists (`local://w1-language-runtime.md:466-476`).",
      "alternative": "The cheaper fallback is still TypeScript/Node with the existing Promise/AbortController patterns (Option A), not a new language; choose it if the measured Effect/runtime budget fails."
    },
    {
      "id": "D2",
      "verdict": "amend",
      "strongest_objection": "“Exactly two seams” omits the independently trusted dynamic Pack loader. `loadBrokerEngine` imports an active module by file URL and validates only API/version/schema/factory shape (`services/uta/src/domain/trading/brokers/registry.ts:52-128`); Pack-local dependency trees can have distinct class identities and the docs prohibit cross-package `instanceof` (`docs/broker-packs.md:67-77`). A Promise-facing TypeScript signature does not enforce what a downloaded module actually returns (`[inference]`).",
      "amendment": "Retain Option B, but name three containment points: HTTP decode/wire, dynamic Pack-loader ABI, and provider transport adapter. The loader must normalize values and thrown errors before returning them to the core; the ABI crosses only serializable provider-indexed records, decimal strings, raw envelopes, and stable error tags—never `Effect`, `Context`, `Layer`, `Schema`, `Decimal`, SDK, or other class instances (`local://w1-provider-capability.md:94-97,123-136`). If an uninterruptible section is used, limit it to local durable append/receipt publication; provider I/O must remain deadline/cancellation-bounded so drain cannot wait forever (`local://decision-register-draft.md:16,87`).",
      "falsifier": "If a released active-Pack fixture proves the loader can admit only serializable records/errors, with no duplicate class or Effect value crossing, and the loader is formally part of the provider-adapter boundary, the separate-loader amendment is unnecessary.",
      "alternative": "The boring implementation is a Promise-only Pack ABI plus one normalization wrapper inside `loadBrokerEngine`; keep Effect confined to core orchestration and do not make Packs import it (`local://w1-effect-evaluation.md:217-220`)."
    },
    {
      "id": "D5",
      "verdict": "amend",
      "strongest_objection": "The design has a crash-visibility gap: D4 requires `intent.proposed` + `authorization.decided` in one append batch, while D5 specifies individual framed lines but no durable batch boundary (`local://decision-register-draft.md:34-38,48-50`). A crash after a valid prefix can expose a partial business batch; “TornTail” alone does not classify valid frames ahead of a manifest. Windows is also under-specified: only Darwin `FileHandle.sync`/directory behavior was probed, while existing Windows tests cover lock-owner renames, not ledger data (`local://w1-ledger-persistence.md:103-109,138-141`).",
      "amendment": "Retain append-only segments, expected-position CAS, and a local writer queue, but define exact UTF-8 byte-length/hash/canonicalization and make multi-entry append atomic via a durable storage-level batch marker (or restrict append to one business entry). Recovery must distinguish uncommitted/valid-ahead-of-manifest frames from torn tails and quarantine them. Define a Windows-tested acknowledgement policy: either prove file flush + temp/rename + directory durability and publish, or return `DurabilityFailure`/a named weaker durability result; “best effort” alone is not a contract. Adapt the runtime lock only for ledger ownership/migration and reuse its bounded Windows rename retries; do not inherit takeover/kill semantics blindly (`packages/guardian-runtime/src/runtime-lock.ts:183-231,323-430`). For 0044, default to source-hash-verified read-only `commit.json` archive plus a fresh ledger; make `legacy.trading-git.commit` conversion conditional on a strict lossless replay fixture and per-account progress marker. Opaque legacy entries preserve bytes but cannot by themselves become typed replay/state (`services/uta/src/domain/trading/git-persistence.ts:18-48`; `plans/uta-refactor/design/01-detail-constraints.md:72,101-102,211-214`).",
      "falsifier": "A real legacy fixture with outstanding/pending provider orders shows that archive-only/fresh-ledger startup misclassifies or duplicates remote orders, while strict conversion into the new ledger preserves replay and reconciliation; that would falsify the archive-first amendment and justify conversion. Current evidence has no real commit sample and marks provider recovery unknown (`local://w1-ledger-persistence.md:127,138-141`).",
      "alternative": "The cheapest boring cutover is to freeze `commit.json` as an immutable read-only legacy projection, start the new ledger empty, and block new writes until open remote orders are reconciled; no dual-write or opaque conversion unless continuity is proven necessary."
    },
    {
      "id": "D11",
      "verdict": "amend",
      "strongest_objection": "Brands are erased at runtime and the register does not state constructors/parsers at every boundary. The fixed constraints require `unknown`→validated domain values and forbid casts as capability/shape proof (`plans/uta-refactor/design/01-detail-constraints.md:276-293`). Existing code demonstrates the gaps: Pack dependencies can contain different Decimal classes (`docs/broker-packs.md:67-77`), and FX currently uses `number`, client-receive timestamps, default rates, and an unknown-currency 1:1 fallback (`services/uta/src/domain/trading/fx-service.ts:1-17,145-221`; `local://w1-ledger-persistence.md:26-30`).",
      "amendment": "Keep nominal brands, but specify one runtime parser/constructor at HTTP, file/ledger, environment, and Pack boundaries. Financial values cross those boundaries only as canonical decimal strings; reject non-finite values, provider sentinels, invalid scale/range, and ambiguous numeric JSON, and define rounding/quantization. Make `Money` currency/unit-bearing and scope provider/native/order references by account/provider where needed; make `Cursor` provider/channel-scoped and opaque. Parse `Instant`/`AsOf` strictly with source-time semantics; `recordedAt` is local append time and position—not wall-clock timestamps—determines replay order. FX must be a freshness/source variant with decimal-string rate; any default/1:1 fallback is display-only and cannot authorize or guard a write.",
      "falsifier": "If an active-Pack plus malformed-input fixture proves that no raw numbers/classes/invalid times cross into the domain, and a code-path audit proves FX is strictly display-only (never a guard, authorization, or trading input), the additional runtime/scope policy is unnecessary.",
      "alternative": "Use one small shared brand/parser module and keep `Decimal` process-local; avoid bespoke runtime classes for every identifier and never pass `Decimal` instances through the dynamic Pack ABI (`local://w1-provider-capability.md:95-97,123`)."
    },
    {
      "id": "D12",
      "verdict": "amend",
      "strongest_objection": "The no-auto-resubmit policy remains defensible, but the proposed drain is not operationally specified. Current shutdown closes the server, closes managers, and exits without tracking active requests/attempts (`services/uta/src/main.ts:179-189`); D2 proposes an uninterruptible region spanning provider I/O (`local://decision-register-draft.md:16`), while production Guardian force-kills after its grace window and otherwise leaves an unexpected UTA offline (`scripts/guardian/prod.mjs:291-300,483-534`). Durable attempts make reconciliation possible, not blind retry or indefinite outage safety (`local://w1-lifecycle-topology.md:24`; `local://w1-ledger-persistence.md:121-123`).",
      "amendment": "Preserve `/__uta/health` as process liveness and keep “no automatic provider re-submit” explicit. Define `running → draining → stopped`: atomically reject new intents with `ServiceDraining`, stop pollers/streams, let an active attempt reach a bounded provider deadline plus durable receipt/unknown append, then close scopes and cursors. On deadline/kill, restart recovery must classify `attempt.started` without receipt as `unknown` and reconcile by declared read-by-key before any write. `/v2/readiness` must expose `writable=false` and structured drain/offline reasons plus per-account freshness/capability variants. Treat Guardian no-respawn as an availability trade-off (operator/flag restart) unless a bounded restart-loop policy and startup reconciliation are separately adopted.",
      "falsifier": "If an external supervisor reliably restarts UTA and startup reconciliation completes before writable readiness, while Guardian itself never needs to respawn it, the current no-auto-respawn choice is sufficient and this availability amendment can be reduced (`local://w1-lifecycle-topology.md:24,126`).",
      "alternative": "Keep the current offline-on-crash/no-Guardian-respawn behavior and explicit flag/operator restart; implement only the drain gate, readiness semantics, and durable-attempt reconciliation before adding a backoff loop (`scripts/guardian/prod.mjs:291-300`; `local://w1-lifecycle-topology.md:126`)."
    }
  ]
}

[You have received this identical output 4 times. Re-reading 'agent://SteadyCore' will not change it — use a narrower selector (path:A-B), or proceed with the edit.]
```
