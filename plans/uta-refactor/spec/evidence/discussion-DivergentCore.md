# 讨论者最终产出：DivergentCore

原始产物 `agent://DivergentCore` 的逐字导出（JSON 或 Markdown 原样）。

```
"## Reframing (read this first)\n\nThree of the four decisions are stated as *mechanism* where the axioms only demand a *contract*:\n\n- **D5 states a file format** (`len sha256 json\\n`, prevHash, batch marker, quarantine/, ledger.lock/ with owner token+heartbeat) and calls it the persistence contract. The contract is six properties: append-only, ordered, single-writer, expected-position CAS, durable-before-exposure, observable corruption, replayable-from-order. SQLite satisfies all six in one dependency; the framed JSONL satisfies them in ~5 hand-rolled mechanisms. The register never argues the fork.\n- **D3 describes \"projections\" as if the distribution model were new.** It isn't. D3 says so itself (\"through the existing Broker Pack lifecycle\"), and `src/core/broker-packs.ts:78-135` already is that lifecycle. The delta is a module **ABI**, not a mechanism.\n- **D2's parenthetical \"(missing handler → TS2345 proven)\" attaches to Effect a guarantee that conditional types supply alone.** The packet's own read-only proof is pure TypeScript (`Provider<P>`/`WriteHandler<P> = never`), no Effect in the type.\n\nAlso note a hard boundary none of the four decisions names: A3's compile-time completeness **cannot cross a dynamically imported pack**. The loader validates module shape at runtime (`registry.ts:118-128`, `import(pathToFileURL(...))`). Effect Layers check in-process services; a pack that omits a declared capability is a *runtime* failure there, always. D2 and D3 both inherit this and neither says it.\n\n---\n\n## D1 — Language\n\n**(1) Verdict: amend** (narrow; the TypeScript position itself is correct).\n\n**(2) Strongest objection.** The escape-hatch sentence makes an unfalsifiable claim (\"can later be attached ... without touching the domain\") that is `[inferred]` in the packet, not evidenced, and it omits the only real precondition: credentials. Secrets live in the AES-256-GCM sealed `accounts.json` with the machine-bound key at `<home>/sealing.key` outside `data/` (`src/core/sealing.ts:32-38`; `docs/data-locations.md`). The lifecycle packet states the consequence: a Rust service \"must either read this format exactly or ship an explicit migration/dual-reader period; it must not ask operators to paste broker secrets.\" A sidecar that gains ambient config access re-opens exactly the secret boundary D7 is closing.\n\n**(3) Amendment text.** Replace the escape-hatch bullet with: \"The provider *transport executor* seam is serializable (records, decimal strings, tagged errors, raw envelopes) and receives credentials as an **injected value**; no transport plugin reads `accounts.json`, `sealing.key`, or the sealed envelope. Whether a sidecar is ever built is out of scope. `[INFERENCE]` The seam is free only because serializability is independently required — plug-in class identity is already banned across packs (`docs/broker-packs.md:67-70`), and A7's existential envelope already forces records — not because this decision buys anything.\"\n\n**(4) Falsifying scenario.** A first-migration provider whose SDK can only be authenticated at import/connection time from an ambient file path or env var (so the plugin must be handed a path, not a value) would break the injected-credential rule, and with it the hatch. `[INFERENCE]` — no such provider was identified in the packet; IBKR TWS/Client-Portal evidence is split across two adapters and neither shows it.\n\n**(5) Cheaper/boring alternative.** Drop the sentence entirely. The option survives without being advertised (serializability is mandated elsewhere), and a decision register is a bad place for a claim that cannot be falsified by any subsequent measurement.\n\n---\n\n## D2 — Effect boundary\n\n**(1) Verdict: amend.**\n\n**(2) Strongest objection.** Two parts. *(a)* The static claim is over-credited: what `/tmp/w1-effect-eval` proves is (i) a read-only `WriteHandler<P>` is unconstructible via conditional types alone, and (ii) dropping `LedgerLive` at `runPromise` yields TS2345 on the Requirements channel. Both are proven for a 74-line sketch, not for `services/uta`; and (i) needs no Effect at all. *(b)* More importantly, the check has a boundary the register doesn't draw: D2 keeps the Pack ABI Promise-facing, so inside the process you get Effect Layers and at the pack boundary you get a runtime module-shape validator. That is **two handler-record implementations and a mapping between them**, and the mapping is where completeness silently breaks. A3 says \"缺失在类型层暴露\"; at the pack boundary that can only ever be runtime.\n\n**(3) Amendment text.** Replace the parenthetical and add a boundary clause: \"Effect owns the **runtime** disciplines — `Scope`/`acquireRelease` per provider connection, bounded `Queue`/`PubSub` fan-out, one-permit `Semaphore` per account write channel, `Schedule` for classified retry, `TestClock`, `uninterruptibleMask` around attempt→provider→receipt. A3's static demand is expressed as Layer `Requirements` *in-process*; the read-only-writability guarantee is ordinary conditional types and would hold without Effect. Across the Pack boundary, A3's check is a **runtime** module-shape validator (an extended `registry.ts:118-128`), and this is a permanent limit of dynamic loading, not a gap to close.\" Mark the falsifier as **open**: the packet records that pinned Bun 1.4.0 was unavailable (shell has 1.4.1; `scripts/build-bun-runtime-feasibility.ts:10-14` rejects the mismatch) and that no full-UTA startup/RSS benchmark was run. D2's own stated falsifier is therefore untested, not passed.\n\n**(4) Falsifying scenario.** SIGTERM arriving during the D12 drain, with a promise-chain serializer instead of `Semaphore`+`uninterruptibleMask`: if the attempt→provider→receipt section cannot be held uninterruptible under plain Node signal handling, `interruptibleMask` is load-bearing for A5 rather than ergonomic, and the dependency earns its cost. Testable against D12's own acceptance criteria.\n\n**(5) Cheaper/boring alternative.** Option A, using a ~20-line `Handlers<S extends Spec>` record for completeness plus lifecycle wrappers, with precedents already in-repo: `store.ts:41-42` (promise-chain write serialization) and `IbkrBroker.init/close` half-open teardown. Cost: hand-rolled cancellation across 100+ sockets; risk concentrates in one stream-supervisor module — which is also the module D1's sidecar would replace, making Effect-the-least-durable investment there. Effect's measured cost is real but small (`effect@3.22.2` ~33 MB installed, +118.5 KiB minified, +0.252 MiB compiled); the cost that matters is the learning curve against contributors, `[INFERENCE]`.\n\n---\n\n## D3 — Provider projection model\n\n**(1) Verdict: amend** (the model is right; the framing hides the migration).\n\n**(2) Strongest objection.** \"Projection = declaration + codec\" is **the same thing as today's Broker Pack with an `x-uta` manifest**. Today a pack exports `{BROKER_PACK_API_VERSION, BROKER_ENGINE, configSchema, createBroker → IBroker}` (`docs/broker-packs.md:38-45`) and is distributed as an immutable release with `active.json` atomic swap, checksum/manifest validation, realpath containment (`src/core/broker-packs.ts:78-135`). D3 reuses all of that verbatim. So the honest claim is: *the ABI changes from a method bag to declaration+translation+transport-plugin, and the declaration gets validated.* Saying so explicitly matters because it fixes the migration cost: `BROKER_PACK_API_VERSION` 1→2 per `docs/broker-packs.md` (\"increment whenever UTA Core changes the exported module contract\"), which lands as a coordinated rewrite of five wrapper packages in one cutover, not a gradual adoption. Second gap: D3 declares one capability tuple per release, but `ccxt-custom` permits any exchange (`packages/uta-protocol/src/brokers/preset-catalog.ts:294-333`), so capability is a function of *venue × config*, not of the release. With the \"unknown → `none`/`unsupported`\" fallback, every `ccxt-custom` account becomes non-retryable — correct, but unsaid.\n\n**(3) Amendment text.** Add: \"There is no new distribution mechanism. A projection release **is** a Broker Pack release: same immutable release dir, same `active.json` swap, same manifest/checksum/containment validation (`src/core/broker-packs.ts:78-135`). The delta is the module ABI (declaration + translation + transport plugin replacing `createBroker → IBroker`) plus declaration validation; `BROKER_PACK_API_VERSION` increments, and all five wrappers migrate together. The manifest may carry a **venue-keyed capability table**; the effective tuple is resolved when account config loads, not when the release loads.\" And: \"`x-uta` is a **build-time** contract: generation and validation run in the pack build (each wrapper already has a `tsup.config.ts`); the release ships compiled code plus a capability manifest, and runtime parses no vendor document.\"\n\n**(4) Falsifying scenario.** For the build-time amendment: a vendor whose terms or fetch flow require the document at activation time rather than vendored into the release (redistribution restriction, or a spec that changes between pack build and account creation). Then runtime overlay survives and my simplification fails. `[INFERENCE]` — needs product/legal input, not in the packet.\n\n**(5) Cheaper/boring alternative.** Build-time generation exactly as amended; it deletes the runtime document parser + generated-parser-at-runtime surface and keeps only the manifest validator. The existing catalog/download/activate path already assumes prebuilt releases, so it costs nothing new. Second alternative if you want to keep it minimal: treat the `x-uta` declaration as *data the pack exports* (a plain object in the module) rather than a document extension the loader must parse — the vendor OAS stays a build input, never a runtime artifact.\n\n---\n\n## D5 — Persistence contract\n\n**(1) Verdict: amend.**\n\n**(2) Strongest objection.** D5 presents one implementation as the contract and never argues against the obvious alternative. On the repo constraint: `AGENTS.md:5-6` (\"Persisted state is file-backed rather than database-backed\") is descriptive prose in the framing paragraph — it is **not** in \"Product and Architecture Boundaries\" (`AGENTS.md:58-78`) or \"Code Conventions\" (`AGENTS.md:182-187`) — and SQLite is already inside the product's ecosystem: `docs/project-structure.md:242-245` records Auto Prediction owning SQLite state while Alice deliberately does not reproduce it, and the transfer spec treats `.codex/state_5.sqlite` as ordinary untracked runtime state. A8 governs *ownership*, not engine. Against that, SQLite WAL with `BEGIN IMMEDIATE` + `synchronous=FULL` + a `stream_head` row gives, in one dependency: writer exclusion, single-statement expected-position CAS (`UPDATE head SET position=? WHERE position=?`; `changes()=0` → `Conflict`), an atomic commit boundary that makes the torn-tail class **unrepresentable**, and fsync ordering — which deletes D5's frame, quarantine policy, batch marker, dir-lock + owner token + heartbeat + stale reclamation + Windows rename retries (`packages/guardian-runtime/src/runtime-lock.ts:183-231,323-430`), and the hand-rolled `segment fsync → manifest tmp+rename → dir fsync (best effort on Windows)` sequence whose cross-platform correctness the packet marks `[unavailable]` (\"no Windows runtime or true power-loss test was run\"). The hash chain is orthogonal — store it in a column; append-only is enforceable with `BEFORE UPDATE/DELETE` triggers. Cost on the other side, and it is the reason the JSONL answer may still win: no SQLite dependency exists in any manifest today, so this is UTA's first binary dependency across five launchers × three platforms, ASAR unpacking (today only `node-pty` and `dugite/git`, `package.json:194-197`), Electron 39's ABI under `ELECTRON_RUN_AS_NODE` (`apps/desktop/src/main.ts:786-807`), and a Bun/Node driver split. The decisive unknown — left as a criterion, not an assertion — is whether a SQLite path exists needing no native build in all five launchers.\n\nThree smaller items in the same decision: the `len` prefix buys nothing when D5 itself forbids resync (\"interior corruption → account ledger open fails structured\"), since JSON string escaping already makes `\\n` an unambiguous delimiter; the frame is still defensible because hashing the raw post-prefix bytes avoids needing a canonicalizer; and the \"batch marker\" (recommended in the packet, implied by D4's \"one append batch\") creates a third recovery class — frame present, marker absent — that disappears if a batch *is* one frame.\n\n**(3) Amendment text.** Replace the opening with the contract, then the decision: \"**Contract (normative):** append-only; a total order assigned by one writer; append carries `expectedPosition` and mismatches return structured `Conflict`; durability completes before exposure; a corrupt record is observable, never silently absent; replay follows persisted order. **Storage is an implementation choice behind a single `AppendStore` seam** (`append(entries, expectedPosition) → Appended|Conflict|DurabilityFailure`, `replay(from)`, `open() → Open|TornTail|Corrupt`). Candidate A (chosen only if): framed segments + manifest + per-account lock — rejected if the [criterion X] holds. Candidate B: per-account SQLite, WAL, `synchronous=FULL`, `BEGIN IMMEDIATE`, `stream_head` CAS, hash chain in-row — adopted if a driver exists requiring no native build under Node 22 (prod/Docker), Electron 39's Node ABI, and pinned Bun 1.4.0.\" Plus: \"A multi-entry append is **one frame**; there is no separate batch-commit marker.\" Plus: \"Drop the byte-length prefix; records are newline-delimited, integrity is `SHA256(exact bytes after the prefix)` + in-body `prevHash`.\" Plus: \"The per-account `ledger.lock/` lease protocol is justified only by a **named** concurrent writer (today: migration 0044 while UTA runs). Since §7.3 makes a second writer a structured `Conflict` rather than corruption, the lock is an optimization; if retained, state what it prevents that CAS does not.\"\n\n**(4) Falsifying scenario.** Run the crash matrix on Windows: if a rename-durable manifest over a non-durable segment directory yields states the recovery policy cannot distinguish (frame present, head not advanced, directory entry lost), the hand-rolled durability sequence fails §7.3 and SQLite — or a one-file-per-append rename design — wins. Directly grounded: the packet marks Windows dir-fsync behavior `[unavailable]` and offers only simulated rename-retry tests.\n\n**(5) Cheaper/boring alternative.** Two, in ascending order of change. *(i)* Keep files, drop the lease: one segment per append written to a temp file and `rename`d (the pattern already in `snapshot/store.ts:55-56`), plus `head.json` via tmp+rename; recovery = contiguous prefix; stale locks reclaimed at boot by the sole owner instead of via heartbeat/quarantine-rename. Deletes the batch-marked/window class and most of the lock. *(ii)* SQLite behind the same seam — deletes four mechanisms and buys them back as a binary-distribution axis. Separately: **D5 and D8 disagree in a load-bearing way.** D8 publishes one `GET /v2/events?after=<cursor>` **per home**, D5 makes the ledger **per account**; positions from independent streams have no common order. State the resolution — the home cursor is the outbox's own sequence written after each ledger append, not a ledger position — or the cursor will be defined by whoever implements it first.\n\n---\n\n## Cross-cutting\n\n- D2 and D3 should state where A3's static check stops: inside the process (Layers/generics) and **at** the pack boundary it is a runtime validator, permanently.\n- D3's \"existential boundary ... never `as`\" is consistent with A7 and with `registry.ts` — no objection, and the pack-boundary serializability rule should be written into the pack ABI text, since it is also what makes D1's hatch cost nothing.\n- The repo's own durable-ledger idiom is the boring end of the spectrum (`docs/data-locations.md`: inbox receipts and Office Day ledger use whole-document atomic replacement and never-pruned identities), which supports keeping files — it does not support the framed-segment-plus-lease end of it.\n"

[You have received this identical output 3 times. Re-reading 'agent://DivergentCore' will not change it — use a narrower selector (path:A-B), or proceed with the edit.]
```
